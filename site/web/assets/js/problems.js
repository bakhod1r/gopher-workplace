/* AUTO-GENERATED by site/cmd/gencatalog — do not edit by hand.
   Source of truth: the challenges/ tree. Re-run build.sh to refresh. */
window.PROBLEMS = {
  "01-language-basics/01-variables-and-constants/junior/byteunits": {
    "id": "01-language-basics/01-variables-and-constants/junior/byteunits",
    "title": "Byte Units",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Byte Units</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>A storage dashboard needs the binary size constants <code>KiB</code>, <code>MiB</code>, and <code>GiB</code>.</p>\n<p>Declare them with an <code>iota</code> shift block so the values stay DRY instead of</p>\n<p>hand-typed magic numbers.</p>\n<h2>Task</h2>\n<p>Implement the <code>const</code> block in byteunits.go so that:</p>\n<p>1. <code>KiB == 1024</code>, <code>MiB == 1024<em>1024</code>, <code>GiB == 1024</em>1024*1024</code>.</p>\n<p>2. The values stay derived from <code>iota</code> \u2014 do not hand-type each number.</p>\n<p>3. <code>Bytes(n)</code> returns <code>n</code> whole KiB in bytes.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  KiB\nOutput: 1024\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  MiB\nOutput: 1048576 (1024*1024)\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Bytes(2)\nOutput: 2048\n</code></pre>\n<p>_Explanation:_ 2 whole KiB in bytes.</p>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\"><code>iota</code></span><span class=\"tchip\">Shift by iota</span><span class=\"tchip\">Skipping a value</span></div></details>",
    "education": "<h1><code>iota</code> and constant blocks</h1>\n<h2>Intuition</h2>\n<p>Inside a <code>const (...)</code> block, <code>iota</code> is the index of the current <em>line</em>. It</p>\n<p>resets to 0 at the start of every block and increments by one per ConstSpec:</p>\n<pre class=\"md\"><code>\nconst (\n\tA = iota   // 0\n\tB = iota   // 1\n\tC = iota   // 2\n)\n</code></pre>\n<p>Writing <code>= iota</code> on every line is noise, so Go lets you omit it: **a constant</p>\n<p>line with no expression repeats the previous line's expression**, with <code>iota</code></p>\n<p>now holding the new index.</p>\n<pre class=\"md\"><code>\nconst (\n\tA = iota   // 0\n\tB          // 1  \u2014 repeats \"= iota\"\n\tC          // 2\n)\n</code></pre>\n<h2>Building scales</h2>\n<p>Because the repeated expression is evaluated fresh each line, any formula</p>\n<p>involving <code>iota</code> becomes a sequence. The binary storage units are the canonical</p>\n<p>example:</p>\n<pre class=\"md\"><code>\nconst (\n\t_  = iota             // burn index 0\n\tKiB = 1 &lt;&lt; (10 * iota) // 1 &lt;&lt; 10 = 1024\n\tMiB                    // 1 &lt;&lt; 20 = 1048576\n\tGiB                    // 1 &lt;&lt; 30 = 1073741824\n)\n</code></pre>\n<p>Three things to see there:</p>\n<p>1. <code>_ = iota</code> <strong>skips</strong> index 0 without naming anything, so <code>KiB</code> lands on</p>\n<p>   <code>iota == 1</code>. Without it, <code>KiB</code> would be <code>1 &lt;&lt; 0</code> = 1.</p>\n<p>2. <code>MiB</code> and <code>GiB</code> carry no expression \u2014 they repeat <code>1 &lt;&lt; (10 * iota)</code>.</p>\n<p>3. <code>1 &lt;&lt; (10 <em> iota)</code> is a </em>shift*: multiply by 1024 per step. These are all</p>\n<p>   powers of two, which is exactly what \"binary prefix\" means.</p>\n<h2>Why derive instead of typing the numbers</h2>\n<p><code>GiB = 1073741824</code> compiles fine and is wrong-looking to review, easy to mistype,</p>\n<p>and says nothing about the pattern. The derived form documents the rule, and the</p>\n<p>compiler does the arithmetic \u2014 constant expressions cost nothing at run time.</p>\n<h2>Approach</h2>\n<p>1. Start the <code>const</code> block with <code>_ = iota</code> to burn slot 0.</p>\n<p>2. Define <code>KiB = 1 &lt;&lt; (10 * iota)</code>; the next lines repeat the expression for MiB and GiB.</p>\n<p>3. <code>Bytes(n)</code> is <code>n * KiB</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nconst (\n\t_   = iota\n\tKiB = 1 &lt;&lt; (10 * iota)\n\tMiB\n\tGiB\n)\n\nfunc Bytes(n int) int {\n\treturn n * KiB\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>At <code>KiB</code> iota is 1, so <code>1 &lt;&lt; 10 = 1024</code>; MiB gets iota 2 \u2192 <code>1 &lt;&lt; 20</code>; GiB iota 3 \u2192 <code>1 &lt;&lt; 30</code>.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>iota</code> counts <strong>lines in the block</strong>, not named constants. A line declaring</li>\n</ul>\n<p>  three names (<code>A, B, C = iota, iota, iota</code>) still advances it by one.</p>\n<ul>\n<li>A blank line or comment does not advance <code>iota</code>; only ConstSpecs do.</li>\n<li><code>iota</code> is only meaningful inside <code>const</code>. There is no <code>var</code> equivalent.</li>\n<li>Reset is per block: a second <code>const (...)</code> starts at 0 again.</li>\n<li>Skipping with <code>_</code> is idiomatic when index 0 would be a meaningless or unsafe</li>\n</ul>\n<p>  value \u2014 for units, or for an enum where the zero value should mean \"unset\".</p>\n<h2>KiB is not KB</h2>\n<p><code>KiB</code> is 1024 bytes (binary); <code>KB</code> is 1000 bytes (decimal). Disk vendors use the</p>\n<p>decimal one, operating systems usually the binary one \u2014 the reason a \"500 GB\"</p>\n<p>drive shows up as ~465 GiB.</p>",
    "starter": "// Package byteunits \u2014 Gopher Workplace challenge.\npackage byteunits\n\n// TODO(candidate): declare the binary storage constants KiB, MiB, GiB.\n// Each step up is 1024x (2^10) the previous one:\n//\n//\tKiB = 1024\n//\tMiB = 1024 * 1024\n//\tGiB = 1024 * 1024 * 1024\n//\n// Derive them from iota (do not hand-type each number).\nconst (\n\tKiB = 0\n\tMiB = 0\n\tGiB = 0\n)\n\n// Bytes returns n whole KiB expressed in bytes.\nfunc Bytes(n int) int {\n\t// TODO(candidate): implement this.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "byteunits.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/01-variables-and-constants/junior/discard": {
    "id": "01-language-basics/01-variables-and-constants/junior/discard",
    "title": "Discarded Remainder",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Discarded Remainder</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>A pagination helper already computes both halves of one division: how many whole</p>\n<p>pages fit, and what is left over. Two thin views sit on top of it \u2014 one wants</p>\n<p>only the page count, the other only the remainder.</p>\n<p>Go has no \"just give me the first one\" rule. A call that returns two values must</p>\n<p>be received in full, or it does not compile. The blank identifier <code>_</code> is how you</p>\n<p>accept a value and immediately throw it away.</p>\n<h2>Task</h2>\n<p>Implement <code>Pages</code> and <code>Leftover</code> in discard.go so that:</p>\n<p>1. Both <strong>call <code>Split</code></strong> rather than recomputing the arithmetic.</p>\n<p>2. <code>Pages</code> keeps the page count and discards the remainder.</p>\n<p>3. <code>Leftover</code> keeps the remainder and discards the page count.</p>\n<p>4. Both stay correct for <code>size == 0</code>, which <code>Split</code> already handles.</p>\n<p>Do <strong>not</strong> change the function signatures or the tests.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Pages(10, 3)\nOutput: 3\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Leftover(10, 3)\nOutput: 1\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Pages(2, 5)\nOutput: 0\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">4</span></summary><div class=\"tchips\"><span class=\"tchip\">Blank identifier</span><span class=\"tchip\">Multi-value assignment</span><span class=\"tchip\">Unused variables are errors</span><span class=\"tchip\">Named results</span></div></details>",
    "education": "<h1>The blank identifier</h1>\n<h2>Intuition</h2>\n<p><code>_</code> is a name you are allowed to assign to and never allowed to read. It exists</p>\n<p>because Go requires you to account for every value \u2014 and sometimes the honest</p>\n<p>answer is \"I do not want this one\".</p>\n<pre class=\"md\"><code>\npages, _ := Split(10, 3)   // keep the count, drop the remainder\n_, rest := Split(10, 3)    // the other way round\n</code></pre>\n<p>Assigning to <code>_</code> compiles to nothing. It is not a variable: it has no address,</p>\n<p>no type, and cannot be read back.</p>\n<h2>Why it is needed</h2>\n<p>Two Go rules make it necessary rather than merely convenient.</p>\n<p><strong>A multi-value call must be received in full.</strong> There is no \"take the first\"</p>\n<p>shorthand:</p>\n<pre class=\"md\"><code>\npages := Split(10, 3)      // compile error: 2 values, 1 variable\n</code></pre>\n<p><strong>Unused local variables are a compile error.</strong> So you cannot dodge the first</p>\n<p>rule by naming the value and ignoring it:</p>\n<pre class=\"md\"><code>\npages, rest := Split(10, 3)\nreturn pages               // compile error: rest declared and not used\n</code></pre>\n<p><code>_</code> satisfies the first rule without triggering the second.</p>\n<h2>Where else it shows up</h2>\n<pre class=\"md\"><code>\nfor _, v := range items { }        // index not needed\nfor i := range items { }           // value not needed (no _ required)\n\nvar _ io.Writer = (*MyType)(nil)   // compile-time interface check\nimport _ \"net/http/pprof\"          // import for side effects only\nfunc (Handler) ServeHTTP(_ http.ResponseWriter, r *http.Request) { }\n</code></pre>\n<p>The last one is worth noting: an unused <em>parameter</em> is legal in Go, so naming it</p>\n<p><code>_</code> is a message to the reader, not a requirement.</p>\n<h2>Not a licence to ignore errors</h2>\n<pre class=\"md\"><code>\ndata, _ := os.ReadFile(path)   // the error is real; you just hid it\n</code></pre>\n<p>This compiles and is almost always a bug \u2014 <code>data</code> is nil and nothing said why.</p>\n<p>Discard a value when it is genuinely irrelevant, not when handling it is</p>\n<p>inconvenient. Linters flag <code>_</code> on error returns for good reason.</p>\n<h2>Approach</h2>\n<p>1. <code>Split</code> returns two values; you must receive both.</p>\n<p>2. Keep the one you want and assign the other to <code>_</code>.</p>\n<p>3. <code>Pages</code> keeps the first, <code>Leftover</code> the second.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Split(n, size int) (pages, rest int) {\n\tif size &lt;= 0 {\n\t\treturn 0, n\n\t}\n\treturn n / size, n % size\n}\n\nfunc Pages(n, size int) int {\n\tp, _ := Split(n, size)\n\treturn p\n}\n\nfunc Leftover(n, size int) int {\n\t_, r := Split(n, size)\n\treturn r\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>Pages(10, 3)</code> calls <code>Split</code> which returns <code>3, 1</code>; binding <code>p, _</code> keeps 3 and discards the remainder.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>_</code> cannot be read: <code>x := _</code> does not compile.</li>\n<li><code>_ = someExpr</code> is a legal statement, occasionally used to silence \"declared</li>\n</ul>\n<p>  and not used\" during debugging. Remove it before committing.</p>\n<ul>\n<li>Each <code>_</code> is independent; <code>_, _ := f()</code> is fine but pointless \u2014 with nothing</li>\n</ul>\n<p>  new on the left, use <code>f()</code> alone if it is called for effect.</p>\n<ul>\n<li><code>_</code> in a <code>range</code> is only needed for the <em>value</em>: <code>for i := range xs</code> already</li>\n</ul>\n<p>  drops it.</p>",
    "starter": "// Package discard \u2014 Gopher Workplace challenge.\npackage discard\n\n// Split reports how many whole pages of the given size fit in n items, and how\n// many items are left over.\n//\n// Callers often want only one of the two. Go has no \"ignore the rest\" rule: a\n// multi-value call must be received in full, and the blank identifier `_` is\n// how you throw a value away without naming it.\n//\n// This one is written for you \u2014 the exercise is in the two functions below.\n//\n// Examples:\n//\n//\tSplit(10, 3) => 3, 1\n//\tSplit(9, 3)  => 3, 0\n//\tSplit(2, 5)  => 0, 2\nfunc Split(n, size int) (pages, rest int) {\n\tif size <= 0 {\n\t\treturn 0, n\n\t}\n\treturn n / size, n % size\n}\n\n// Pages returns only the number of whole pages, discarding the remainder.\n//\n// TODO(candidate): call Split and keep the first value only. You cannot write\n// `pages := Split(n, size)` \u2014 the call yields two values and both must be\n// received. Use the blank identifier for the one you do not want.\n//\n// Examples:\n//\n//\tPages(10, 3) => 3\n//\tPages(9, 3)  => 3\n//\tPages(2, 5)  => 0\nfunc Pages(n, size int) int {\n\t// TODO(candidate): implement this using Split.\n\tpanic(\"not implemented\")\n}\n\n// Leftover returns only the remainder, discarding the page count.\n//\n// Examples:\n//\n//\tLeftover(10, 3) => 1\n//\tLeftover(9, 3)  => 0\n//\tLeftover(2, 5)  => 2\nfunc Leftover(n, size int) int {\n\t// TODO(candidate): implement this using Split.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "discard.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/01-variables-and-constants/junior/endpoint": {
    "id": "01-language-basics/01-variables-and-constants/junior/endpoint",
    "title": "Derived Endpoint",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Derived Endpoint</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>An API client pins its host and version at build time, then builds every request</p>\n<p>URL from them. The host and version are <code>const</code> \u2014 fixed, known to the compiler.</p>\n<p>The assembled root is a package-level <code>var</code>, because it is <em>computed</em> from them.</p>\n<p>The point is that <code>Root</code> must be <strong>derived</strong>, not retyped. Paste the finished</p>\n<p>URL and the two constants become decoration: bump <code>Version</code> to <code>\"v3\"</code> and the</p>\n<p>client keeps calling <code>v2</code>.</p>\n<h2>Task</h2>\n<p>Implement endpoint.go so that:</p>\n<p>1. <code>BaseURL</code> is <code>\"https://api.example.com\"</code> and <code>Version</code> is <code>\"v2\"</code>.</p>\n<p>2. <code>Root</code> is a package-level <code>var</code> <strong>built from those constants</strong> \u2014</p>\n<p>   <code>BaseURL + \"/\" + Version</code> \u2014 not a hand-written string.</p>\n<p>3. <code>Path(resource)</code> returns <code>Root</code>, a slash, then the resource.</p>\n<p>4. <code>Path(\"\")</code> returns <code>Root</code> unchanged \u2014 no trailing slash.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Root\nOutput: \"https://api.example.com/v2\"\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Path(\"users\")\nOutput: \"https://api.example.com/v2/users\"\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Path(\"\")\nOutput: \"https://api.example.com/v2\"\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">4</span></summary><div class=\"tchips\"><span class=\"tchip\">Package-level <code>var</code></span><span class=\"tchip\"><code>const</code> vs <code>var</code></span><span class=\"tchip\">Constant expressions</span><span class=\"tchip\">Single source of truth</span></div></details>",
    "education": "<h1><code>const</code> vs <code>var</code>, and package-level state</h1>\n<h2>Intuition</h2>\n<p>A <code>const</code> is a value the compiler knows. A <code>var</code> is storage the program owns.</p>\n<pre class=\"md\"><code>\nconst Version = \"v2\"                  // fixed at compile time\nvar Root = BaseURL + \"/\" + Version    // a variable, initialised before main runs\n</code></pre>\n<p>Constants may only hold values the compiler can compute: numbers, strings,</p>\n<p>booleans, runes, and expressions over them. Anything that needs the machine at</p>\n<p>run time \u2014 a function call, a slice, a map, a struct, <code>time.Now()</code> \u2014 must be a</p>\n<p><code>var</code>.</p>\n<h2>Package-level variables initialise before <code>main</code></h2>\n<p>Go initialises package-level variables in <strong>dependency order</strong>, not source</p>\n<p>order, then runs <code>init()</code> functions, then <code>main</code>:</p>\n<pre class=\"md\"><code>\nvar Root = BaseURL + \"/\" + Version   // depends on two constants\nvar host = parse(Root)               // depends on Root \u2014 Go runs it second\n</code></pre>\n<p>You never have to order the declarations by hand; the compiler works out what</p>\n<p>each one needs. A cycle is a compile error.</p>\n<h2>Derive, do not duplicate</h2>\n<p>The whole point of the exercise:</p>\n<pre class=\"md\"><code>\nvar Root = \"https://api.example.com/v2\"   // pasted: goes stale silently\nvar Root = BaseURL + \"/\" + Version        // derived: one place to change\n</code></pre>\n<p>Both produce the same string today. Bump <code>Version</code> to <code>\"v3\"</code> and only the second</p>\n<p>one follows. Because both operands are constants, the compiler folds the</p>\n<p>concatenation \u2014 the derived form costs nothing at run time and documents where</p>\n<p>the value comes from.</p>\n<h2>Constant expressions are free</h2>\n<pre class=\"md\"><code>\nconst KB = 1 &lt;&lt; 10\nconst timeout = 30 * 1000       // computed at compile time\nvar greeting = \"hello, \" + name // name is a var \u2192 run-time concatenation\n</code></pre>\n<p>The first two exist only in the compiled instructions. The third allocates.</p>\n<h2>Approach</h2>\n<p>1. Declare <code>BaseURL</code> and <code>Version</code> as constants.</p>\n<p>2. Build <code>Root</code> from them, not by pasting the string.</p>\n<p>3. <code>Path</code> joins <code>Root</code> and the resource, returning <code>Root</code> unchanged when empty.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nconst (\n\tBaseURL = \"https://api.example.com\"\n\tVersion = \"v2\"\n)\n\nvar Root = BaseURL + \"/\" + Version\n\nfunc Path(resource string) string {\n\tif resource == \"\" {\n\t\treturn Root\n\t}\n\treturn Root + \"/\" + resource\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>Root</code> concatenates the two constants with a slash. <code>Path(\"users\")</code> appends <code>/users</code>; an empty resource returns <code>Root</code> with no trailing slash.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Package-level <code>var</code>s are global mutable state. Prefer <code>const</code> when the value</li>\n</ul>\n<p>  never changes, and prefer passing values as parameters over reaching for a</p>\n<p>  package-level variable.</p>\n<ul>\n<li>A <code>var</code> cannot be used where a constant is required \u2014 array lengths, <code>case</code></li>\n</ul>\n<p>  values in a constant switch, other constant declarations.</p>\n<ul>\n<li>Initialisation order across <em>packages</em> follows imports: imported packages are</li>\n</ul>\n<p>  fully initialised first.</p>\n<ul>\n<li><code>init()</code> runs after variable initialisation, once per package, and cannot be</li>\n</ul>\n<p>  called by hand. Use it sparingly; explicit setup is easier to follow and test.</p>",
    "starter": "// Package endpoint \u2014 Gopher Workplace challenge.\npackage endpoint\n\n// The service's address is fixed at build time.\n//\n// TODO(candidate): give BaseURL and Version their values.\n//\n//\tBaseURL = \"https://api.example.com\"\n//\tVersion = \"v2\"\nconst (\n\tBaseURL = \"\"\n\tVersion = \"\"\n)\n\n// Root is the API root: BaseURL, a slash, then Version.\n//\n// TODO(candidate): declare Root as a package-level var built *from the\n// constants above* \u2014 do not paste the finished string. Constants are known at\n// compile time, so the concatenation costs nothing at run time, and changing\n// Version updates Root for free.\n//\n//\tRoot == \"https://api.example.com/v2\"\nvar Root = \"\"\n\n// Path returns the full URL of a resource under Root: Root, a slash, then the\n// resource name. An empty resource yields Root unchanged (no trailing slash).\n//\n// Examples:\n//\n//\tPath(\"users\")  => \"https://api.example.com/v2/users\"\n//\tPath(\"orders\") => \"https://api.example.com/v2/orders\"\n//\tPath(\"\")       => \"https://api.example.com/v2\"\nfunc Path(resource string) string {\n\t// TODO(candidate): implement this.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "endpoint.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/01-variables-and-constants/junior/plan-limits": {
    "id": "01-language-basics/01-variables-and-constants/junior/plan-limits",
    "title": "Challenge 01 \u2014 Plan Rate Limits",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Challenge 01 \u2014 Plan Rate Limits</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>You maintain the billing service. Each subscription tier gets a different</p>\n<p>API rate limit. The <code>Tier</code> type and the <code>Limit</code> function are stubbed out \u2014</p>\n<p>you need to implement them from scratch.</p>\n<h2>Task</h2>\n<p>Implement plan.go so that:</p>\n<p>1. <code>Free</code>, <code>Pro</code>, <code>Enterprise</code> are <strong>distinct, ascending</strong> values (<code>0, 1, 2</code>).</p>\n<p>2. <code>Limit</code> returns the correct requests-per-minute for each tier.</p>\n<p>3. An unknown tier falls back to the <code>Free</code> allowance.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Limit(Free)\nOutput: 60\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Limit(Pro)\nOutput: 600\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Limit(Tier(99))\nOutput: 60\n</code></pre>\n<p>_Explanation:_ Unknown tier falls back to Free's limit.</p>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">2</span></summary><div class=\"tchips\"><span class=\"tchip\">01-language-basics</span><span class=\"tchip\">01-variables-and-constants</span></div></details>",
    "education": "<h1>Enumerations with a named type</h1>\n<h2>Intuition</h2>\n<p>Go has no <code>enum</code> keyword. The idiom is a <strong>named integer type</strong> plus a <code>const</code></p>\n<p>block numbered by <code>iota</code>:</p>\n<pre class=\"md\"><code>\ntype Tier int\n\nconst (\n\tFree Tier = iota   // 0\n\tPro                // 1\n\tEnterprise         // 2\n)\n</code></pre>\n<p><code>Pro</code> and <code>Enterprise</code> repeat the previous line's expression \u2014 including its</p>\n<p>type \u2014 so all three are <code>Tier</code>, not plain <code>int</code>.</p>\n<h2>Why the named type earns its keep</h2>\n<p>Compare with three loose integers:</p>\n<pre class=\"md\"><code>\nconst Free, Pro, Enterprise = 0, 1, 2\n\nfunc Limit(t int) int\nLimit(42)          // compiles: 42 is a fine int\nLimit(userID)      // compiles: nothing says this is not a tier\n</code></pre>\n<p>With <code>type Tier int</code>, <code>Limit(t Tier)</code> rejects both. A stray <code>int</code> cannot wander</p>\n<p>into a tier parameter; you must say <code>Tier(n)</code> and mean it. The type is the</p>\n<p>documentation the compiler can check.</p>\n<h2>The zero value is the first constant</h2>\n<p><code>iota</code> starts at 0, so the first constant is also the type's zero value. That is</p>\n<p>a design decision, not an accident: <code>var t Tier</code> gives <code>Free</code>. Choose the order</p>\n<p>so the zero value is the safest default \u2014 the free tier, the \"unset\" state, the</p>\n<p>least privileged role. If no member is a sane default, burn index 0:</p>\n<pre class=\"md\"><code>\nconst (\n\tUnknown Tier = iota\n\tFree\n\tPro\n)\n</code></pre>\n<h2>Mapping a member to data</h2>\n<p>A lookup function keeps the table in one place and handles unknown input:</p>\n<pre class=\"md\"><code>\nfunc Limit(t Tier) int {\n\tswitch t {\n\tcase Pro:\n\t\treturn 600\n\tcase Enterprise:\n\t\treturn 6000\n\tdefault:\n\t\treturn 60      // Free, and anything unrecognised\n\t}\n}\n</code></pre>\n<p>Note the <code>default</code>: a <code>Tier</code> can hold any integer \u2014 <code>Tier(99)</code> is valid Go \u2014 so</p>\n<p>an exhaustive-looking switch still needs a fallback. The type narrows intent, it</p>\n<p>does not constrain the value set.</p>\n<h2>Derive related numbers</h2>\n<p>When the values follow a rule, express the rule rather than the results:</p>\n<pre class=\"md\"><code>\nconst base = 60\n// Pro = base * 10, Enterprise = base * 100\n</code></pre>\n<p>A reviewer then sees the relationship, and changing <code>base</code> moves all of them.</p>\n<h2>Approach</h2>\n<p>1. Declare <code>Free, Pro, Enterprise</code> with <code>iota</code> (0, 1, 2).</p>\n<p>2. Map Pro\u2192600 and Enterprise\u21926000 in a switch.</p>\n<p>3. <code>default</code> returns 60, covering Free and unknown tiers.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\ntype Tier int\n\nconst (\n\tFree Tier = iota\n\tPro\n\tEnterprise\n)\n\nfunc Limit(t Tier) int {\n\tswitch t {\n\tcase Pro:\n\t\treturn 600\n\tcase Enterprise:\n\t\treturn 6000\n\tdefault:\n\t\treturn 60\n\t}\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>iota</code> makes the tiers distinct and ascending; <code>Limit(Tier(99))</code> matches no case and hits <code>default</code> \u2192 60.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>The constants are <em>untyped</em> if you omit the type on the first line</li>\n</ul>\n<p>  (<code>Free = iota</code>), which weakens the checking you wanted. Write the type once,</p>\n<p>  on the first line; the rest inherit it.</p>\n<ul>\n<li>Ordering is an API promise. Inserting a member in the middle renumbers</li>\n</ul>\n<p>  everything after it \u2014 which matters if the values are ever serialised.</p>\n<ul>\n<li>Comparing across types needs a conversion: <code>t == Tier(n)</code>, not <code>t == n</code>.</li>\n</ul>",
    "starter": "package plan\n\n// Tier identifies a customer subscription tier.\ntype Tier int\n\n// TODO(candidate): declare the tier constants Free, Pro, Enterprise as\n// distinct, ascending values (0, 1, 2) using iota.\nconst (\n\tFree       Tier = 0\n\tPro        Tier = 0\n\tEnterprise Tier = 0\n)\n\n// Limit returns the requests-per-minute for a tier:\n// Free => 60, Pro => 600, Enterprise => 6000. Unknown tier => 60.\n//\n// TODO(candidate): implement this. Map each tier to a multiple of 60\n// (Pro = 60*10, Enterprise = 60*100) and default to 60.\nfunc Limit(t Tier) int {\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "plan.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/01-variables-and-constants/junior/retries": {
    "id": "01-language-basics/01-variables-and-constants/junior/retries",
    "title": "Retry Budget",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Retry Budget</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>The HTTP client should retry a failed request 3 times. Declare the retry</p>\n<p>constant carefully: in Go, <code>'3'</code> in single quotes is a rune, not the number 3 \u2014</p>\n<p>an easy way to make the retry budget wildly wrong.</p>\n<h2>Task</h2>\n<p>Declare the <code>MaxRetries</code> constant in retries.go so that:</p>\n<p>1. <code>MaxRetries == 3</code> (an integer, not a character).</p>\n<p>2. <code>Budget()</code> returns <code>4</code> (first attempt + 3 retries).</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  MaxRetries\nOutput: 3\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Budget()\nOutput: 4\n</code></pre>\n<p>_Explanation:_ First attempt plus 3 retries.</p>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  type of MaxRetries\nOutput: untyped int constant\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Rune literals</span><span class=\"tchip\">Integer literals</span><span class=\"tchip\">Untyped constant math</span></div></details>",
    "education": "<h1>Rune literals vs integer literals</h1>\n<h2>Intuition</h2>\n<p><code>3</code> and <code>'3'</code> are both integer constants in Go. They are not the same number.</p>\n<pre class=\"md\"><code>\n3     // untyped integer constant, value 3\n'3'   // untyped rune constant, value 51 \u2014 the Unicode code point of the digit\n</code></pre>\n<p>Single quotes mean <strong>character</strong>, and a character is stored as its code point.</p>\n<p><code>'0'</code> is 48, <code>'A'</code> is 65, <code>'a'</code> is 97. The digit characters run consecutively</p>\n<p>from 48, which is why <code>'7' - '0'</code> gives 7 \u2014 the classic char-to-digit trick.</p>\n<h2>Why the mistake survives compilation</h2>\n<p>An untyped rune constant is still an integer constant, so it goes wherever an</p>\n<p>integer is expected:</p>\n<pre class=\"md\"><code>\nconst MaxRetries = '3'      // 51\nattempts := 1 + MaxRetries  // 52 \u2014 compiles cleanly\n</code></pre>\n<p>No conversion, no warning. The program simply retries 51 times. Nothing about</p>\n<p>the syntax is wrong; only the meaning is.</p>\n<h2>The three quoting forms</h2>\n<p>| Literal | Type | Meaning |</p>\n<p>|---------|------|---------|</p>\n<p>| <code>'3'</code> | rune (int32) | one character, value 51 |</p>\n<p>| <code>\"3\"</code> | string | a one-character string |</p>\n<p>| <code>3</code> | integer | the number three |</p>\n<p>Double quotes never produce a number: <code>1 + \"3\"</code> does not compile, which is why</p>\n<p>the string version of this mistake is caught immediately and the rune version is</p>\n<p>not.</p>\n<h2>Untyped constants inherit kinds</h2>\n<p>An untyped constant carries a <em>kind</em> \u2014 integer, rune, float, string, bool \u2014 and</p>\n<p>that kind propagates through constant arithmetic:</p>\n<pre class=\"md\"><code>\nconst a = '3'      // rune kind\nconst b = a + 1    // still rune kind, value 52\nvar n int = b      // fine: the value fits an int\nfmt.Printf(\"%c\", b) // '4' \u2014 printing as a character reveals the kind\n</code></pre>\n<p>The kind decides how the value <em>reads</em>, not whether it compiles.</p>\n<h2>Approach</h2>\n<p>1. Declare <code>MaxRetries = 3</code> (the number, not the rune <code>'3'</code>).</p>\n<p>2. <code>Budget</code> is <code>1 + MaxRetries</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nconst MaxRetries = 3\n\nfunc Budget() int {\n\treturn 1 + MaxRetries\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>The first try plus <code>MaxRetries</code> retries gives 4 total attempts.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>A rune is an alias for <code>int32</code>; <code>byte</code> is an alias for <code>uint8</code>. <code>'\u00e9'</code> needs 2</li>\n</ul>\n<p>  bytes in UTF-8 but is one rune, value 233.</p>\n<ul>\n<li><code>%c</code> prints a number as a character, <code>%d</code> as a number, <code>%q</code> as a quoted</li>\n</ul>\n<p>  character. Reach for <code>%c</code>/<code>%q</code> when a value looks 48 too big.</p>\n<ul>\n<li>Digit conversion is <code>'7' - '0'</code>, never <code>int('7')</code> \u2014 the latter gives 55.</li>\n</ul>",
    "starter": "// Package retries \u2014 Gopher Workplace challenge.\npackage retries\n\n// TODO(candidate): declare MaxRetries as the integer 3. Beware: '3' in single\n// quotes is a rune (value 51), not the number 3.\nconst MaxRetries = 0\n\n// Budget returns the total number of attempts (first try + retries).\nfunc Budget() int {\n\t// TODO(candidate): implement this \u2014 first attempt plus MaxRetries.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "retries.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/01-variables-and-constants/junior/shadow": {
    "id": "01-language-basics/01-variables-and-constants/junior/shadow",
    "title": "Shadowed Tally",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Shadowed Tally</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>A billing service adds up only the <em>positive</em> line items on an invoice \u2014 refunds</p>\n<p>and zero-value rows are skipped. A teammate's first draft looked right but always</p>\n<p>returned <code>0</code>, and nobody could see why until they looked at the accumulator</p>\n<p>inside the loop.</p>\n<h2>Task</h2>\n<p>Implement <code>Tally</code> in shadow.go so that it:</p>\n<p>1. Returns the sum of every value in <code>nums</code> that is strictly greater than <code>0</code>.</p>\n<p>2. Ignores zero and negative values.</p>\n<p>3. Returns <code>0</code> for a nil or empty slice.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Tally([1 -2 3])\nOutput: 4\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Tally(nil)\nOutput: 0\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Tally([-1 -5])\nOutput: 0\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Variable scope</span><span class=\"tchip\">Shadowing with <code>:=</code></span><span class=\"tchip\">Assignment vs declaration</span></div></details>",
    "education": "<h1>Scope and shadowing</h1>\n<h2>Intuition</h2>\n<p>A variable exists from its declaration to the end of the <strong>block</strong> that contains</p>\n<p>it \u2014 the nearest enclosing pair of braces. Inside a nested block you may declare</p>\n<p>a new variable with the same name. It does not overwrite the outer one; it</p>\n<p><em>hides</em> it for the rest of that block. That is shadowing.</p>\n<pre class=\"md\"><code>\nsum := 0\nfor _, n := range nums {\n\tsum := sum + n     // NEW sum, born and dead each iteration\n}\n// sum is still 0\n</code></pre>\n<p>The inner <code>sum</code> is initialised from the outer one, then thrown away when the</p>\n<p>iteration ends. The accumulator never moves.</p>\n<h2><code>:=</code> declares, <code>=</code> assigns</h2>\n<p>That single character is the whole difference:</p>\n<pre class=\"md\"><code>\nsum := sum + n   // declare a new sum in this block\nsum = sum + n    // assign to the existing sum\nsum += n         // same, shorter\n</code></pre>\n<p><code>:=</code> requires at least one new name on the left. That rule is what makes the</p>\n<p>following legal and confusing:</p>\n<pre class=\"md\"><code>\nv, err := f()    // both new\nw, err := g()    // w is new, err is REUSED \u2014 not shadowed, same variable\n</code></pre>\n<p>Reuse only happens in the <em>same</em> block. Inside a new block, <code>err</code> would be a</p>\n<p>fresh, shadowed variable \u2014 the reason \"the error I set is nil outside the <code>if</code>\"</p>\n<p>is such a common bug.</p>\n<h2>Where blocks come from</h2>\n<p>Every <code>{}</code> opens a block, and some statements open one implicitly:</p>\n<pre class=\"md\"><code>\nif v := compute(); v &gt; 0 {   // v lives in the if, including its else\n\t...\n} else {\n\t...                      // v visible here\n}\n// v gone here\n\nfor i := 0; i &lt; n; i++ {     // i belongs to the for\n}\n// i gone here\n</code></pre>\n<p>The <code>if</code>/<code>for</code>/<code>switch</code> init statement is scoped to the whole statement \u2014 a</p>\n<p>useful way to keep a temporary from leaking into the function.</p>\n<h2>Why Go allows it at all</h2>\n<p>Shadowing keeps short names usable in small scopes without fear of collision,</p>\n<p>and it makes blocks self-contained. The cost is exactly the bug above, which is</p>\n<p>why <code>go vet -shadow</code> (and linters like <code>govet</code>'s shadow check) exist.</p>\n<h2>Approach</h2>\n<p>1. Accumulate into a <code>sum</code> declared before the loop.</p>\n<p>2. Add each positive value.</p>\n<p>3. Avoid re-declaring <code>sum</code> inside the loop (which would shadow it).</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Tally(nums []int) int {\n\tsum := 0\n\tfor _, n := range nums {\n\t\tif n &gt; 0 {\n\t\t\tsum += n\n\t\t}\n\t}\n\treturn sum\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>For <code>[1 -2 3]</code>, 1 and 3 are positive, so <code>sum</code> reaches 4; negatives are skipped.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Unused <em>variables</em> are a compile error, but a shadowed variable that you do</li>\n</ul>\n<p>  assign is \"used\" \u2014 so the compiler will not save you here.</p>\n<ul>\n<li>The inner declaration may have a different type: <code>x := \"1\"</code> inside a block</li>\n</ul>\n<p>  where the outer <code>x</code> is an <code>int</code> compiles fine.</p>\n<ul>\n<li>Package-level names can be shadowed too, including built-ins. <code>len := 3</code> is</li>\n</ul>\n<p>  legal and disables <code>len(...)</code> for that block.</p>",
    "starter": "// Package shadow \u2014 Gopher Workplace challenge.\npackage shadow\n\n// Tally returns the sum of the positive integers in nums.\n// Zero and negative values are ignored. A nil or empty slice sums to 0.\n//\n// Examples:\n//\n//\tTally([]int{1, -2, 3}) => 4\n//\tTally(nil)             => 0\n//\tTally([]int{-1, -5})   => 0\nfunc Tally(nums []int) int {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "shadow.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/01-variables-and-constants/junior/swap": {
    "id": "01-language-basics/01-variables-and-constants/junior/swap",
    "title": "Value Swap",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Value Swap</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>A utility package needs a one-liner that exchanges two values. Go's multiple</p>\n<p>assignment does this without a temporary variable \u2014 your job is to wire it up.</p>\n<h2>Task</h2>\n<p>Implement <code>Swap</code> in swap.go so that it:</p>\n<p>1. Returns <code>a</code> and <code>b</code> with their values exchanged.</p>\n<p>2. Uses multiple assignment \u2014 no temporary variable.</p>\n<p>3. Handles equal inputs (returns them unchanged).</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Swap(1, 2)\nOutput: 2, 1\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Swap(9, 9)\nOutput: 9, 9\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Swap(-3, 7)\nOutput: 7, -3\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Multiple assignment</span><span class=\"tchip\">Assignment vs no-op</span><span class=\"tchip\">Tuple return</span></div></details>",
    "education": "<h1>Multiple assignment</h1>\n<h2>Intuition</h2>\n<p>Go assigns in two phases. First it evaluates <strong>every</strong> expression on the right of</p>\n<p>the <code>=</code>, then it copies those results into the operands on the left. Nothing on</p>\n<p>the left is written until everything on the right has been read.</p>\n<p>That is why the classic swap needs no temporary:</p>\n<pre class=\"md\"><code>\na, b = b, a\n</code></pre>\n<p>The right side evaluates to the pair <em>(old b, old a)</em>. Only then does assignment</p>\n<p>happen. In a language without this rule you would need:</p>\n<pre class=\"md\"><code>\ntmp := a\na = b\nb = tmp\n</code></pre>\n<h2>Approach</h2>\n<p>1. Return the arguments reversed: <code>return b, a</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Swap(a, b int) (int, int) {\n\treturn b, a\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>Multiple return values let <code>Swap(1, 2)</code> hand back <code>2, 1</code> directly.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>a, b = a, b</code> is legal and does nothing. Every variable is reassigned its own</li>\n</ul>\n<p>  value. Easy to write by accident when the operands are longer expressions.</p>\n<ul>\n<li>The left operands must be assignable \u2014 variables, fields, indexes. You cannot</li>\n</ul>\n<p>  assign to a constant or a function result.</p>\n<ul>\n<li>Declaration and assignment differ: <code>a, b := b, a</code> only compiles where <code>a</code> and</li>\n</ul>\n<p>  <code>b</code> are <em>new</em> names in this scope. Inside a loop or <code>if</code>, that introduces</p>\n<p>  shadows instead of touching the outer variables \u2014 see the <em>Shadowed Tally</em></p>\n<p>  puzzle.</p>\n<ul>\n<li>The count must match exactly: <code>a, b = f()</code> works when <code>f</code> returns two values,</li>\n</ul>\n<p>  but <code>a = f()</code> does not compile.</p>\n<h2>Multiple results</h2>\n<p>A Go function may return several values, and multiple assignment is how you</p>\n<p>receive them:</p>\n<pre class=\"md\"><code>\nfunc Split(n, size int) (int, int)\n\npages, rest := Split(10, 3)   // 3, 1\n</code></pre>\n<p>Both must be received. If you only want one, discard the rest with the blank</p>\n<p>identifier \u2014 see the <em>Discarded Remainder</em> puzzle.</p>",
    "starter": "// Package swap \u2014 Gopher Workplace challenge.\npackage swap\n\n// Swap returns its two arguments with their values exchanged.\n//\n// Examples:\n//\n//\tSwap(1, 2)  => 2, 1\n//\tSwap(9, 9)  => 9, 9\n//\tSwap(-3, 7) => 7, -3\nfunc Swap(a, b int) (int, int) {\n\t// TODO(candidate): implement this \u2014 return a and b with values exchanged.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "swap.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/01-variables-and-constants/junior/temperature": {
    "id": "01-language-basics/01-variables-and-constants/junior/temperature",
    "title": "Temperature Convert",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Temperature Convert</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>A weather widget shows Fahrenheit, but the sensor reports Celsius. Write the</p>\n<p><code>CToF</code> helper that converts between them \u2014 and watch the constant arithmetic,</p>\n<p>where integer division is an easy trap.</p>\n<h2>Task</h2>\n<p>Implement <code>CToF</code> in temperature.go so it computes</p>\n<p><code>F = C*(9/5) + 32</code> correctly. The <code>freezingF</code> constant is already provided \u2014 do</p>\n<p>not change the signature or the tests.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  CToF(0)\nOutput: 32\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  CToF(100)\nOutput: 212\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  CToF(-40)\nOutput: -40\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Untyped constants</span><span class=\"tchip\">Constant kind</span><span class=\"tchip\">Mixed arithmetic</span></div></details>",
    "education": "<h1>Untyped constants and constant kinds</h1>\n<h2>Intuition</h2>\n<p>An untyped constant has no type \u2014 it has a <strong>kind</strong>: integer, float, rune,</p>\n<p>string, bool, complex. The kind decides how arithmetic behaves; the type is</p>\n<p>chosen later, from the context where the constant is used.</p>\n<pre class=\"md\"><code>\nconst ratio = 9 / 5      // integer kind: 1\nconst ratio = 9.0 / 5.0  // float kind: 1.8\n</code></pre>\n<p>Both operands of <code>9 / 5</code> are integer constants, so the division is integer</p>\n<p>division and the fraction is gone before any variable is involved. Writing one</p>\n<p>operand with a decimal point makes the whole expression float-kinded.</p>\n<h2>The trap: it compiles either way</h2>\n<pre class=\"md\"><code>\nfunc ToF(c float64) float64 {\n\treturn c*9/5 + 32     // fine \u2014 c is float64, so the whole expression is\n}\n\nconst ratio = 9 / 5       // 1\nfunc ToF(c float64) float64 {\n\treturn c*ratio + 32   // silently wrong: c * 1\n}\n</code></pre>\n<p>In the first version the untyped constants join a <code>float64</code> expression and adopt</p>\n<p>that type. In the second, the division happened <em>between two constants</em> first,</p>\n<p>in integer kind. Nothing warns you: <code>1</code> is a perfectly good number.</p>\n<h2>Constants are arbitrary precision</h2>\n<p>Untyped constant arithmetic is exact and unbounded until the value is assigned:</p>\n<pre class=\"md\"><code>\nconst big = 1 &lt;&lt; 100          // fine as a constant\nvar x int = big               // compile error: overflows int\nconst third = 1.0 / 3.0       // exact, far beyond float64 precision\nvar f float64 = third         // rounded here, at the point of use\n</code></pre>\n<p>That is why constant expressions can be written naturally without worrying about</p>\n<p>intermediate overflow \u2014 the rounding happens once, when the value lands in a</p>\n<p>typed variable.</p>\n<h2>Kind conversion rules</h2>\n<p>| Expression | Kind | Value |</p>\n<p>|------------|------|-------|</p>\n<p>| <code>9 / 5</code> | integer | 1 |</p>\n<p>| <code>9.0 / 5</code> | float | 1.8 |</p>\n<p>| <code>float64(9) / 5</code> | typed float64 | 1.8 |</p>\n<p>| <code>9 / 5.0</code> | float | 1.8 |</p>\n<p>One float operand is enough: mixing kinds in a constant expression promotes to</p>\n<p>the \"wider\" one (integer \u2192 rune \u2192 float \u2192 complex).</p>\n<h2>Approach</h2>\n<p>1. Use floating-point arithmetic so <code>9/5</code> is 1.8, not 1.</p>\n<p>2. Compute <code>c*9.0/5.0 + 32</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nconst freezingF = 32\n\nfunc CToF(c float64) float64 {\n\treturn c*9.0/5.0 + freezingF\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>CToF(100)</code>: <code>100*1.8 = 180</code>, plus 32 \u2192 212. Integer <code>9/5</code> would truncate to 1 and break it.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Give a constant a type only when you want to <em>restrict</em> it. `const ratio</li>\n</ul>\n<p>  float64 = 1.8<code> is fine here; </code>const n int = 3<code> stops </code>n` being used as a</p>\n<p>  <code>float64</code> later.</p>\n<ul>\n<li><code>9 / 5</code> inside a <code>float64</code> expression is still 1 \u2014 the parentheses matter:</li>\n</ul>\n<p>  <code>c <em> 9 / 5</code> is <code>(c</em>9)/5</code> (correct, because <code>c<em>9</code> is float64) but <code>c </em> (9/5)</code></p>\n<p>  is <code>c * 1</code>.</p>\n<ul>\n<li>Integer division truncates toward zero, it does not round: <code>7/2 == 3</code>,</li>\n</ul>\n<p>  <code>-7/2 == -3</code>.</p>",
    "starter": "// Package temperature \u2014 Gopher Workplace challenge.\npackage temperature\n\n// freezingF is the Fahrenheit value of 0 \u00b0C.\nconst freezingF = 32\n\n// CToF converts a Celsius temperature to Fahrenheit: F = C*(9/5) + 32.\n//\n// Examples:\n//\n//\tCToF(0)   => 32\n//\tCToF(100) => 212\n//\tCToF(-40) => -40\nfunc CToF(c float64) float64 {\n\t// TODO(candidate): implement this. Mind that 9/5 with integer operands\n\t// truncates to 1 \u2014 use a floating-point constant so the ratio is 1.8.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "temperature.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/01-variables-and-constants/junior/typedconst": {
    "id": "01-language-basics/01-variables-and-constants/junior/typedconst",
    "title": "Typed Batch Limit",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Typed Batch Limit</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>A batching service caps how many items go out at once, and retries each batch a</p>\n<p>fixed number of times. Both numbers are constants \u2014 but they behave differently,</p>\n<p>because one carries a type and the other does not.</p>\n<p>A <strong>typed</strong> constant is nailed to its type: <code>MaxBatch</code> is a <code>byte</code> everywhere it</p>\n<p>appears, so comparing it with an <code>int</code> needs an explicit conversion. An</p>\n<p><strong>untyped</strong> constant has a <em>kind</em>, not a type, and adopts whatever type its</p>\n<p>context needs \u2014 <code>Retries</code> multiplies a <code>float64</code> with no conversion at all.</p>\n<h2>Task</h2>\n<p>Implement typedconst.go so that:</p>\n<p>1. <code>MaxBatch</code> is a <strong>typed</strong> constant \u2014 <code>byte</code>, value <code>200</code>. Write the type.</p>\n<p>2. <code>Retries</code> is an <strong>untyped</strong> constant, value <code>3</code>. Do not write a type.</p>\n<p>3. <code>Fits(n)</code> reports whether <code>n</code> is between <code>0</code> and <code>MaxBatch</code> inclusive.</p>\n<p>   Negative <code>n</code> never fits.</p>\n<p>4. <code>Budget(base)</code> returns <code>base</code> multiplied by <code>Retries</code>.</p>\n<p>Convert in the direction that cannot lose information: widen <code>MaxBatch</code> to</p>\n<p><code>int</code>, do not narrow <code>n</code> to <code>byte</code>. Narrowing wraps \u2014 <code>byte(256)</code> is <code>0</code>, which</p>\n<p>would make an absurdly large batch \"fit\".</p>\n<p>Do <strong>not</strong> change the function signatures or the tests.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Fits(200)\nOutput: true\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Fits(201)\nOutput: false\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Budget(1.5)\nOutput: 4.5\n</code></pre>\n<p>_Explanation:_ Untyped Retries multiplies a float64 directly.</p>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">4</span></summary><div class=\"tchips\"><span class=\"tchip\">Typed constants</span><span class=\"tchip\">Untyped constants</span><span class=\"tchip\">Conversion direction</span><span class=\"tchip\">Constant vs variable</span></div></details>",
    "education": "<h1>Typed vs untyped constants</h1>\n<h2>Intuition</h2>\n<p>A constant may be declared with a type or without one, and the two behave very</p>\n<p>differently at the point of use.</p>\n<pre class=\"md\"><code>\nconst MaxBatch byte = 200   // typed:   a byte, everywhere, always\nconst Retries = 3           // untyped: an integer *kind*, type decided later\n</code></pre>\n<p>An <strong>untyped</strong> constant is converted implicitly to whatever its context needs.</p>\n<p>A <strong>typed</strong> constant is not converted at all \u2014 mixing it with another type is a</p>\n<p>compile error until you say so explicitly.</p>\n<pre class=\"md\"><code>\nvar f float64 = Retries     // fine: adopts float64\nvar r rune = Retries        // fine: adopts rune\nvar n int = MaxBatch        // compile error: byte is not int\nvar n int = int(MaxBatch)   // fine: explicit\n</code></pre>\n<h2>Which to reach for</h2>\n<p>Default to <strong>untyped</strong>. It composes with any numeric type without ceremony,</p>\n<p>which is why the standard library's constants (<code>math.Pi</code>, <code>time.Nanosecond</code>'s</p>\n<p>multipliers) are mostly untyped.</p>\n<p>Choose <strong>typed</strong> when the type is part of the meaning and you want the compiler</p>\n<p>to enforce it \u2014 an enum member (<code>const Free Tier = iota</code>), a value that must</p>\n<p>stay inside a byte, a duration that should never be confused with a plain</p>\n<p>integer.</p>\n<h2>Conversions truncate, silently</h2>\n<p>Narrowing a value into a smaller type wraps modulo its range. No panic, no</p>\n<p>warning:</p>\n<pre class=\"md\"><code>\nbyte(255)   // 255\nbyte(256)   // 0\nbyte(456)   // 200\nint8(200)   // -56\n</code></pre>\n<p>So the <em>direction</em> of a conversion is a correctness decision, not a style</p>\n<p>choice. Widening (<code>int(aByte)</code>) can never lose information; narrowing can. When</p>\n<p>comparing values of two sizes, widen the small one:</p>\n<pre class=\"md\"><code>\nn &lt;= int(MaxBatch)     // right: nothing wraps\nbyte(n) &lt;= MaxBatch    // wrong: n == 256 becomes 0 and passes\n</code></pre>\n<h2>Constant conversions are checked at compile time</h2>\n<p>Because constants are known to the compiler, an out-of-range <em>constant</em> is</p>\n<p>caught rather than wrapped:</p>\n<pre class=\"md\"><code>\nconst c byte = 256      // compile error: constant 256 overflows byte\nvar n = 300\nb := byte(n)            // no error: runtime conversion, wraps to 44\n</code></pre>\n<p>Same-looking code, opposite outcome \u2014 the difference is whether the value is a</p>\n<p>constant.</p>\n<h2>Approach</h2>\n<p>1. <code>MaxBatch byte = 200</code> is typed; comparing with an <code>int</code> needs <code>int(MaxBatch)</code>.</p>\n<p>2. <code>Retries = 3</code> is untyped; it adapts to float64 in <code>Budget</code>.</p>\n<p>3. <code>Fits</code> guards negatives.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nconst (\n\tMaxBatch byte = 200\n\tRetries       = 3\n)\n\nfunc Fits(n int) bool {\n\treturn n &gt;= 0 &amp;&amp; n &lt;= int(MaxBatch)\n}\n\nfunc Budget(base float64) float64 {\n\treturn base * Retries\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>Fits(200)</code> converts <code>MaxBatch</code> to int and compares; <code>Budget(1.5)</code> multiplies by the untyped 3 \u2192 4.5 with no conversion.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>byte</code> is an alias for <code>uint8</code>, <code>rune</code> for <code>int32</code>. Aliases are the <em>same</em></li>\n</ul>\n<p>  type, so no conversion is needed between <code>byte</code> and <code>uint8</code>.</p>\n<ul>\n<li>A typed constant restricts arithmetic too: <code>MaxBatch * 2</code> is a <code>byte</code></li>\n</ul>\n<p>  expression and overflows at 400 \u2192 144.</p>\n<ul>\n<li>Do not sprinkle conversions to silence the compiler. Each one is a claim that</li>\n</ul>\n<p>  the value fits; make sure it does.</p>",
    "starter": "// Package typedconst \u2014 Gopher Workplace challenge.\npackage typedconst\n\n// TODO(candidate): declare the two batching constants.\n//\n//\tMaxBatch \u2014 a *typed* constant of type byte, value 200. Writing the type\n//\t           pins it: it is a byte everywhere it appears, so mixing it with\n//\t           an int needs an explicit conversion.\n//\tRetries  \u2014 an *untyped* constant, value 3. With no type written down it\n//\t           adapts to whatever context it lands in \u2014 int here, float64\n//\t           there \u2014 with no conversion at all.\n//\n// Declare MaxBatch with an explicit byte type and Retries with none.\nconst (\n\tMaxBatch byte = 0\n\tRetries       = 0\n)\n\n// Fits reports whether n items fit in a single batch, i.e. whether n is at most\n// MaxBatch. Negative n never fits.\n//\n// MaxBatch is a byte and n is an int, so this comparison needs a conversion.\n//\n// Examples:\n//\n//\tFits(0)   => true\n//\tFits(200) => true\n//\tFits(201) => false\n//\tFits(-1)  => false\nfunc Fits(n int) bool {\n\t// TODO(candidate): implement this.\n\tpanic(\"not implemented\")\n}\n\n// Budget returns the total time budget for one item: base seconds per attempt,\n// across Retries attempts.\n//\n// Retries is untyped, so it multiplies a float64 directly \u2014 no conversion.\n//\n// Examples:\n//\n//\tBudget(1.5) => 4.5\n//\tBudget(0)   => 0\n//\tBudget(0.1) => 0.30000000000000004\nfunc Budget(base float64) float64 {\n\t// TODO(candidate): implement this.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "typedconst.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/01-variables-and-constants/junior/zerovalues": {
    "id": "01-language-basics/01-variables-and-constants/junior/zerovalues",
    "title": "Zero Value Config",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Zero Value Config</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>A service boots from a <code>Config</code> struct. Ops only ever want to pin the port in the</p>\n<p>defaults \u2014 everything else should fall back to Go's zero values so unset fields</p>\n<p>are unambiguous. A <code>Tags</code> slice that comes back as a non-nil empty slice, or a</p>\n<p><code>Host</code> pre-filled with a stray string, breaks the \"unset means zero\" contract the</p>\n<p>rest of the system relies on.</p>\n<h2>Task</h2>\n<p>Implement <code>DefaultConfig</code> in zerovalues.go so that it returns a</p>\n<p><code>Config</code> where:</p>\n<p>1. <code>Port</code> is <code>8080</code>.</p>\n<p>2. <code>Host</code> is <code>\"\"</code>, <code>Debug</code> is <code>false</code>, <code>Tags</code> is <code>nil</code> \u2014 each left at its type's</p>\n<p>   zero value, not explicitly re-set to look like it.</p>\n<p>3. Each call returns a fresh value: mutating one result never affects the next.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<p>&gt; <strong>Note \u2014 avoid the magic number.</strong> Writing <code>Config{Port: 8080}</code> passes the</p>\n<p>&gt; tests, but hard-coding <code>8080</code> in the body is a <em>magic number</em>: an unexplained</p>\n<p>&gt; literal. Prefer naming it once \u2014 <code>const DefaultPort = 8080</code> \u2014 and returning</p>\n<p>&gt; <code>Config{Port: DefaultPort}</code>. The tests only grade the returned values, so this</p>\n<p>&gt; is style guidance, not a hard gate; get into the habit anyway.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  DefaultConfig().Port\nOutput: 8080\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  DefaultConfig().Host\nOutput: \"\"\n</code></pre>\n<p>_Explanation:_ Unset string field keeps its zero value.</p>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  DefaultConfig().Tags\nOutput: nil\n</code></pre>\n<p>_Explanation:_ A nil slice, not an empty non-nil one.</p>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Zero values</span><span class=\"tchip\"><code>var</code> vs composite literal</span><span class=\"tchip\">nil vs empty slice</span></div></details>",
    "education": "<h1>Zero values</h1>\n<h2>Intuition</h2>\n<p>Go has no uninitialised memory. Every declaration without an explicit value gets</p>\n<p>its type's <strong>zero value</strong>, and that value is always usable:</p>\n<p>| Type | Zero value |</p>\n<p>|------|-----------|</p>\n<p>| <code>int</code>, <code>float64</code>, all numerics | <code>0</code> |</p>\n<p>| <code>string</code> | <code>\"\"</code> |</p>\n<p>| <code>bool</code> | <code>false</code> |</p>\n<p>| pointer, slice, map, channel, func, interface | <code>nil</code> |</p>\n<p>| struct | every field set to <em>its</em> zero value |</p>\n<p>| array | every element set to its zero value |</p>\n<pre class=\"md\"><code>\nvar n int        // 0\nvar s string     // \"\"\nvar c Config     // Config{Host: \"\", Port: 0, Tags: nil}\n</code></pre>\n<p>This is why Go has no \"undefined\" and no constructor requirement.</p>\n<h2>Useful zero values are a design goal</h2>\n<p>The standard library leans on this. A <code>sync.Mutex</code> is ready to lock at zero; a</p>\n<p><code>bytes.Buffer</code> is ready to write at zero. When you design a struct, ask what its</p>\n<p>zero value should mean, and pick field types so that \"empty\" is correct rather</p>\n<p>than broken.</p>\n<pre class=\"md\"><code>\ntype Config struct {\n\tHost  string\n\tPort  int\n\tTags  []string\n}\n\nvar c Config      // usable: empty host, port 0, no tags\nc.Tags = append(c.Tags, \"beta\")   // append to a nil slice works\n</code></pre>\n<h2><code>nil</code> slice vs empty slice</h2>\n<p>Both have length 0, and both work with <code>len</code>, <code>range</code> and <code>append</code>. They are not</p>\n<p>identical:</p>\n<pre class=\"md\"><code>\nvar a []string        // nil    \u2014 len 0, cap 0, a == nil\nb := []string{}       // empty  \u2014 len 0, but b != nil\n</code></pre>\n<p>The difference is visible where identity matters: <code>a == nil</code> is true and `b ==</p>\n<p>nil<code> is false, and </code>encoding/json<code> marshals the first as </code>null` and the second</p>\n<p>as <code>[]</code>. Prefer the nil form unless you specifically need the other \u2014 it is the</p>\n<p>zero value, and it costs no allocation.</p>\n<h2>Declaration forms</h2>\n<pre class=\"md\"><code>\nvar c Config              // all-zero struct\nc := Config{}             // same thing, written as a literal\nc := Config{Port: 8080}   // named field set; Host and Tags stay zero\n</code></pre>\n<p>A composite literal fills the fields you name and zeroes the rest, so partially</p>\n<p>configured structs need no boilerplate.</p>\n<h2>Approach</h2>\n<p>1. Return a <code>Config</code> literal setting only <code>Port: 8080</code>.</p>\n<p>2. Every unnamed field takes its type's zero value automatically.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\ntype Config struct {\n\tHost  string\n\tPort  int\n\tDebug bool\n\tTags  []string\n}\n\nfunc DefaultConfig() Config {\n\treturn Config{Port: 8080}\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>Config{Port: 8080}</code> leaves Host \"\", Debug false, and Tags nil \u2014 Go zeroes them without explicit assignment.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>A <code>nil</code> map is readable but <strong>not writable</strong>: <code>m[\"k\"]</code> returns the zero value,</li>\n</ul>\n<p>  <code>m[\"k\"] = 1</code> panics. Maps need <code>make</code> before writing; slices do not need it</p>\n<p>  before <code>append</code>.</p>\n<ul>\n<li>A <code>nil</code> pointer dereference panics. A zero <em>struct</em> is safe; a zero *pointer to</li>\n</ul>\n<p>  struct* is not.</p>\n<ul>\n<li><code>var s []string</code> and <code>s := []string(nil)</code> are the same thing written twice.</li>\n<li>Zero values are set for you \u2014 writing <code>var n int = 0</code> adds nothing.</li>\n</ul>",
    "starter": "// Package zerovalues \u2014 Gopher Workplace challenge.\npackage zerovalues\n\n// Config holds server settings. Any field the caller leaves unset must keep\n// Go's zero value for its type.\ntype Config struct {\n\tHost  string\n\tPort  int\n\tDebug bool\n\tTags  []string\n}\n\n// DefaultConfig returns the baseline Config: only Port is set, to 8080.\n// Every other field is left at its type's zero value \u2014 Host \"\", Debug false,\n// Tags nil (not an empty non-nil slice).\n//\n// Tip: naming the port as a constant (`const DefaultPort = 8080`) instead of a\n// bare literal is good style \u2014 but only the returned values are graded.\n//\n// Examples:\n//\n//\tDefaultConfig().Port  => 8080\n//\tDefaultConfig().Host  => \"\"\n//\tDefaultConfig().Tags  => nil\nfunc DefaultConfig() Config {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "zerovalues.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/01-variables-and-constants/middle/byte-shift": {
    "id": "01-language-basics/01-variables-and-constants/middle/byte-shift",
    "title": "Binary Byte Units",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Binary Byte Units</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>Storage sizes are powers of 1024. <code>iota</code> plus a left shift generates KB..TB in</p>\n<p>one block \u2014 the classic Go idiom.</p>\n<h2>Task</h2>\n<p>In units.go:</p>\n<p>1. Define <code>KB, MB, GB, TB</code> as <code>1 &lt;&lt; (10 * iota)</code> (skip the <code>iota==0</code> slot with</p>\n<p>   <code>_</code>).</p>\n<p>2. Implement <code>Humanize(n)</code> returning the largest unit dividing <code>n</code> evenly and</p>\n<p>   its symbol; <code>(n, \"B\")</code> below KB.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  KB\nOutput: 1024\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Humanize(1048576)\nOutput: 1, \"MB\"\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Humanize(512)\nOutput: 512, \"B\"\n</code></pre>\n<p>_Explanation:_ Below KB, reported in bytes.</p>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">iota with shift</span><span class=\"tchip\">Blank identifier in const</span><span class=\"tchip\">Untyped\u2192typed</span></div></details>",
    "education": "<h1>iota with shifts: binary units</h1>\n<h2>Intuition</h2>\n<p>Storage sizes step by factors of 1024 (2^10). One <code>iota</code> expression generates</p>\n<p>the whole ladder:</p>\n<pre class=\"md\"><code>\ntype ByteSize uint64\nconst (\n\t_  ByteSize = iota      // skip iota==0 with the blank identifier\n\tKB ByteSize = 1 &lt;&lt; (10 * iota) // iota==1 -&gt; 1&lt;&lt;10 == 1024\n\tMB                              // iota==2 -&gt; 1&lt;&lt;20\n\tGB                              // iota==3 -&gt; 1&lt;&lt;30\n\tTB                              // iota==4 -&gt; 1&lt;&lt;40\n)\n</code></pre>\n<p>The blank identifier <code>_</code> consumes the <code>iota==0</code> slot (where <code>1&lt;&lt;0</code> would be 1),</p>\n<p>so <code>KB</code> starts at the first useful power.</p>\n<h2>Approach</h2>\n<p>1. Define KB..TB with <code>1 &lt;&lt; (10 * iota)</code>, skipping slot 0.</p>\n<p>2. In <code>Humanize</code>, try units largest-first.</p>\n<p>3. Return the first that divides <code>n</code> evenly; else bytes.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\ntype ByteSize uint64\n\nconst (\n\t_  ByteSize = iota\n\tKB          = 1 &lt;&lt; (10 * iota)\n\tMB\n\tGB\n\tTB\n)\n\nfunc Humanize(n ByteSize) (ByteSize, string) {\n\tunits := []struct {\n\t\tv ByteSize\n\t\ts string\n\t}{{TB, \"TB\"}, {GB, \"GB\"}, {MB, \"MB\"}, {KB, \"KB\"}}\n\tfor _, u := range units {\n\t\tif n &gt;= u.v &amp;&amp; n%u.v == 0 {\n\t\t\treturn n / u.v, u.s\n\t\t}\n\t}\n\treturn n, \"B\"\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>Humanize(1048576)</code> checks TB, GB (no), then MB \u2014 <code>1048576 % MB == 0</code> \u2192 <code>1, \"MB\"</code>.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>uint64</code> (not <code>int</code>) because <code>1 &lt;&lt; 40</code> overflows a 32-bit int.</li>\n<li>The <code>_</code> line is what shifts KB onto <code>iota==1</code>; without it KB would be 1.</li>\n<li>Binary KB = 1024. If you need decimal (1000), that is <code>kB</code>, a different unit.</li>\n</ul>",
    "starter": "// Package units defines binary byte-size constants with iota shifting.\npackage units\n\n// ByteSize counts bytes.\ntype ByteSize uint64\n\n// KB, MB, GB, TB as 1<<(10*n) using iota.\n//\n// TODO(candidate): define with a single iota expression 1 << (10 * iota),\n// skipping the iota==0 slot with the blank identifier.\nconst (\n\t_  ByteSize = 0\n\tKB ByteSize = 0\n\tMB ByteSize = 0\n\tGB ByteSize = 0\n\tTB ByteSize = 0\n)\n\n// Humanize returns the largest unit that divides n evenly, as (count, symbol).\n// For n < KB it returns (n, \"B\"). Prefer larger units on ties.\n//\n// TODO(candidate): implement.\nfunc Humanize(n ByteSize) (ByteSize, string) {\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "units.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/01-variables-and-constants/middle/clamp": {
    "id": "01-language-basics/01-variables-and-constants/middle/clamp",
    "title": "Clamp to Range",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Clamp to Range</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>Clamping needs valid bounds. When <code>lo &gt; hi</code> you normalize them in a local</p>\n<p>scope \u2014 a good place to practice <code>if</code>-init variables and block scope without</p>\n<p>shadowing the parameters.</p>\n<h2>Task</h2>\n<p>In clamp.go:</p>\n<p>1. If <code>lo &gt; hi</code>, swap them (locally).</p>\n<p>2. Return <code>v</code> limited to <code>[lo, hi]</code>.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Clamp(5, 0, 10)\nOutput: 5\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Clamp(-3, 0, 10)\nOutput: 0\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Clamp(5, 10, 0)\nOutput: 5\n</code></pre>\n<p>_Explanation:_ Reversed bounds are swapped first.</p>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Block scope</span><span class=\"tchip\">Shadowing risk</span><span class=\"tchip\">Multiple assignment</span></div></details>",
    "education": "<h1>Block scope and shadowing</h1>\n<h2>Intuition</h2>\n<p>A variable exists only inside the block (<code>{ ... }</code>) where it is declared, and</p>\n<p>disappears at the block's closing brace. Re-declaring a name with <code>:=</code> inside a</p>\n<p>narrower block creates a <strong>new</strong> variable that <em>shadows</em> the outer one:</p>\n<pre class=\"md\"><code>\nlo, hi := 0, 10\nif lo &gt; hi {\n\tlo, hi := hi, lo // NEW lo, hi \u2014 the outer pair is untouched!\n\t_ = lo; _ = hi\n}\n// outer lo, hi still 0, 10\n</code></pre>\n<p>To modify the outer variables, use <code>=</code> (assignment), not <code>:=</code> (declaration):</p>\n<pre class=\"md\"><code>\nif lo &gt; hi {\n\tlo, hi = hi, lo // updates the outer pair\n}\n</code></pre>\n<h2>Approach</h2>\n<p>1. Normalize the bounds when <code>lo &gt; hi</code>.</p>\n<p>2. Clamp <code>v</code> below <code>lo</code> and above <code>hi</code>.</p>\n<p>3. Otherwise return <code>v</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Clamp(v, lo, hi int) int {\n\tif lo, hi = order(lo, hi); v &lt; lo {\n\t\treturn lo\n\t} else if v &gt; hi {\n\t\treturn hi\n\t}\n\treturn v\n}\n\nfunc order(lo, hi int) (int, int) {\n\tif lo &gt; hi {\n\t\treturn hi, lo\n\t}\n\treturn lo, hi\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>Clamp(5, 10, 0)</code> swaps to <code>[0, 10]</code>, and 5 is inside, so it returns 5.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>:=</code> requires at least one new name on the left; if all names exist in the</li>\n</ul>\n<p>  current scope it is a compile error \u2014 but in a <em>nested</em> block it silently</p>\n<p>  shadows instead.</p>\n<ul>\n<li><code>go vet</code>'s shadow analysis and linters can flag suspicious shadows.</li>\n<li>Multiple assignment (<code>lo, hi = hi, lo</code>) swaps without a temporary.</li>\n</ul>",
    "starter": "// Package clamp restricts a value to a range using block-scoped locals.\npackage clamp\n\n// Clamp returns v limited to [lo, hi]. If lo > hi the bounds are swapped first.\n// Use a short variable declaration in an if-init to normalize the bounds\n// without shadowing lo/hi in the outer scope.\n//\n// TODO(candidate): implement.\nfunc Clamp(v, lo, hi int) int {\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "clamp.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/01-variables-and-constants/middle/frame-budget": {
    "id": "01-language-basics/01-variables-and-constants/middle/frame-budget",
    "title": "Frame Budget",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Frame Budget</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>A 60 FPS game gets 1/60 s per frame. Computing that from an integer constant</p>\n<p>exposes integer-division truncation: <code>1000000/60 = 16666</code>, not 16667.</p>\n<h2>Task</h2>\n<p>In frame.go:</p>\n<p>1. Implement <code>FrameBudgetMicros()</code> = <code>1_000_000 / TargetFPS</code> (integer division).</p>\n<p>2. Implement <code>OverBudget(us)</code> returning true when <code>us</code> exceeds the budget.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  FrameBudgetMicros()\nOutput: 16666\n</code></pre>\n<p>_Explanation:_ 1_000_000 / 60, integer-divided.</p>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  OverBudget(20000)\nOutput: true\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  OverBudget(1000)\nOutput: false\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Integer const division</span><span class=\"tchip\">Named constants</span><span class=\"tchip\">Strict vs non-strict</span></div></details>",
    "education": "<h1>Integer constant division</h1>\n<h2>Intuition</h2>\n<p>When both operands are integers, <code>/</code> is <strong>integer division</strong>: it truncates</p>\n<p>toward zero, discarding any remainder.</p>\n<pre class=\"md\"><code>\nconst TargetFPS = 60\nconst budgetMicros = 1_000_000 / TargetFPS // 16666, not 16666.67\n</code></pre>\n<p>The result is exact and computed at compile time, but the fraction is gone.</p>\n<h2>Approach</h2>\n<p>1. <code>1_000_000 / TargetFPS</code> with integer division gives the budget.</p>\n<p>2. <code>OverBudget</code> compares against it.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nconst TargetFPS = 60\n\nfunc FrameBudgetMicros() int {\n\treturn 1_000_000 / TargetFPS\n}\n\nfunc OverBudget(us int) bool {\n\treturn us &gt; FrameBudgetMicros()\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>At 60 FPS the budget is 16666 \u00b5s; a 20000 \u00b5s frame is over.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>1000000 / 60</code> is 16666. If you want rounding, do it explicitly</li>\n</ul>\n<p>  (<code>(x + d/2) / d</code>), do not sprinkle magic <code>+1</code>s.</p>\n<ul>\n<li>Mixing a float makes it float division: <code>1000000.0 / 60</code> is 16666.67.</li>\n<li>Digit separators (<code>1_000_000</code>) are cosmetic and improve readability.</li>\n</ul>",
    "starter": "// Package frame computes a frame time budget from a target FPS.\npackage frame\n\n// TargetFPS is the frames-per-second goal.\nconst TargetFPS = 60\n\n// FrameBudgetMicros returns the per-frame budget in microseconds\n// (1_000_000 / TargetFPS), using integer constant division.\n//\n// TODO(candidate): implement \u2014 mind integer division truncation.\nfunc FrameBudgetMicros() int {\n\tpanic(\"not implemented\")\n}\n\n// OverBudget reports whether a frame taking us microseconds missed the budget.\n//\n// TODO(candidate): implement.\nfunc OverBudget(us int) bool {\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "frame.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/01-variables-and-constants/middle/int-limits": {
    "id": "01-language-basics/01-variables-and-constants/middle/int-limits",
    "title": "Integer Limits",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Integer Limits</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>The max/min of a machine int come straight from its bit pattern \u2014 no <code>math</code></p>\n<p>package needed, just constant bit operations the compiler folds.</p>\n<h2>Task</h2>\n<p>In limits.go:</p>\n<p>1. Define <code>MaxUint = ^uint(0)</code>, <code>MaxInt</code> (all bits but the sign), <code>MinInt</code>.</p>\n<p>2. Implement <code>FitsInInt(v)</code> reporting whether <code>v &lt;= MaxInt</code>.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  MaxUint\nOutput: all bits set (^uint(0))\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  FitsInInt(uint(MaxInt))\nOutput: true\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  FitsInInt(MaxUint)\nOutput: false\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Bit complement</span><span class=\"tchip\">Sign bit shift</span><span class=\"tchip\">Two's complement</span></div></details>",
    "education": "<h1>Integer limits from bit patterns</h1>\n<h2>Intuition</h2>\n<p>The maximum and minimum of a machine integer are just bit patterns you can build</p>\n<p>with constant operations \u2014 no <code>math</code> package needed:</p>\n<pre class=\"md\"><code>\nconst MaxUint = ^uint(0)          // all bits set\nconst MaxInt  = int(^uint(0) &gt;&gt; 1) // clear the sign bit\nconst MinInt  = -MaxInt - 1        // two's complement\n</code></pre>\n<p><code>^</code> is bitwise NOT. <code>^uint(0)</code> flips every bit of zero, giving the largest</p>\n<p>unsigned value. Shifting right by one clears the top (sign) bit, giving the</p>\n<p>largest <strong>signed</strong> value.</p>\n<h2>Approach</h2>\n<p>1. <code>MaxUint = ^uint(0)</code> sets all bits.</p>\n<p>2. <code>MaxInt = int(MaxUint &gt;&gt; 1)</code> clears the sign bit.</p>\n<p>3. <code>MinInt = -MaxInt - 1</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nconst (\n\tMaxUint = ^uint(0)\n\tMaxInt  = int(MaxUint &gt;&gt; 1)\n\tMinInt  = -MaxInt - 1\n)\n\nfunc FitsInInt(v uint) bool {\n\treturn v &lt;= uint(MaxInt)\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>Shifting <code>MaxUint</code> right by one drops the top bit, yielding the largest positive int; <code>FitsInInt</code> compares against it.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>int(^uint(0))</code> alone is <strong>-1</strong>, not the max: all-bits <em>signed</em> is -1. You must</li>\n</ul>\n<p>  shift first.</p>\n<ul>\n<li><code>MinInt = -MaxInt - 1</code>: two's complement is asymmetric, one more negative than</li>\n</ul>\n<p>  positive.</p>\n<ul>\n<li>A typed <code>int</code> constant that overflows is a <strong>compile error</strong>, so build wide</li>\n</ul>\n<p>  values as <code>uint</code> and convert, or keep them untyped.</p>",
    "starter": "// Package limits derives integer bounds from bit patterns at compile time.\npackage limits\n\n// MaxUint is the largest uint: all bits set.\n// MaxInt is the largest int: all bits set except the sign bit.\n// MinInt is the smallest int.\n//\n// TODO(candidate): define the three using ^uint(0) and shifts, no math package.\nconst (\n\tMaxUint = uint(0)\n\tMaxInt  = int(0)\n\tMinInt  = int(0)\n)\n\n// FitsInInt reports whether the unsigned value v is <= MaxInt.\n//\n// TODO(candidate): implement.\nfunc FitsInInt(v uint) bool {\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "limits.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/01-variables-and-constants/middle/iota-perms": {
    "id": "01-language-basics/01-variables-and-constants/middle/iota-perms",
    "title": "Permission Bits",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Permission Bits</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>A filesystem stores permissions as bit flags packed into one byte. You define</p>\n<p>the flags with <code>iota</code> and test membership with bitwise AND.</p>\n<h2>Task</h2>\n<p>In perms.go:</p>\n<p>1. Define <code>Read</code>, <code>Write</code>, <code>Execute</code> as consecutive power-of-two bits using</p>\n<p>   <code>iota</code> (<code>Read=1</code>, <code>Write=2</code>, <code>Execute=4</code>).</p>\n<p>2. Implement <code>Has(set, want)</code> returning true only if <code>set</code> contains every bit</p>\n<p>   in <code>want</code>.</p>\n<p>Do not change signatures or tests.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Read, Write, Execute\nOutput: 1, 2, 4\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Has(Read|Write, Read)\nOutput: true\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Has(Read, Write)\nOutput: false\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">iota bit flags</span><span class=\"tchip\">Typed constants</span><span class=\"tchip\">Bitwise AND mask</span></div></details>",
    "education": "<h1>iota bit flags</h1>\n<h2>Intuition</h2>\n<p><code>iota</code> is a per-<code>const</code>-block counter: it is 0 on the first <code>ConstSpec</code> line and</p>\n<p>increases by one on each following line. Pair it with a left shift and each flag</p>\n<p>lands on its own bit:</p>\n<pre class=\"md\"><code>\nconst (\n\tRead    Permission = 1 &lt;&lt; iota // 1 &lt;&lt; 0 == 1\n\tWrite                          // 1 &lt;&lt; 1 == 2\n\tExecute                        // 1 &lt;&lt; 2 == 4\n)\n</code></pre>\n<p>Only the first line carries the expression; the bare lines below <strong>repeat</strong> it,</p>\n<p>and <code>iota</code> keeps climbing, so you never write the shifts by hand.</p>\n<h2>Approach</h2>\n<p>1. Define flags with <code>1 &lt;&lt; iota</code> so each is a distinct bit.</p>\n<p>2. <code>Has</code> tests <code>set&amp;want == want</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\ntype Permission uint8\n\nconst (\n\tRead Permission = 1 &lt;&lt; iota\n\tWrite\n\tExecute\n)\n\nfunc Has(set, want Permission) bool {\n\treturn set&amp;want == want\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>Read|Write</code> is <code>0b011</code>; <code>Has(..., Read)</code> masks bit 0 and matches \u2192 true.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Give the <strong>first</strong> constant the type (<code>Permission</code>); the whole run inherits it.</li>\n<li>Power-of-two spacing is what keeps flags from colliding. <code>1 &lt;&lt; iota</code>, not</li>\n</ul>\n<p>  <code>iota</code> \u2014 plain <code>iota</code> gives 0,1,2 and 0 is not a usable flag.</p>\n<ul>\n<li>A zero value means \"no permissions\", which is a sensible default.</li>\n</ul>",
    "starter": "// Package perms models Unix-style permission bits with iota.\npackage perms\n\n// Permission is a single-bit permission flag.\ntype Permission uint8\n\n// Define Read, Write, Execute as consecutive power-of-two bits using iota,\n// so Read=1, Write=2, Execute=4.\n//\n// TODO(candidate): replace the placeholders with an iota bit-flag block.\nconst (\n\tRead    Permission = 0\n\tWrite   Permission = 0\n\tExecute Permission = 0\n)\n\n// Has reports whether set contains every bit in want.\n//\n// TODO(candidate): implement using bitwise AND.\nfunc Has(set, want Permission) bool {\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "perms.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/01-variables-and-constants/middle/pi-precision": {
    "id": "01-language-basics/01-variables-and-constants/middle/pi-precision",
    "title": "High-Precision Pi",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">High-Precision Pi</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>Untyped constants carry arbitrary precision until assigned to a typed value.</p>\n<p>A <code>Pi</code> written to 20+ digits rounds correctly to whichever float type uses it.</p>\n<h2>Task</h2>\n<p>In geo.go:</p>\n<p>1. Define <code>Pi</code> as an untyped constant with \u226520 significant digits.</p>\n<p>2. Implement <code>Area(r)</code> returning <code>Pi<em>r</em>r</code>.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Area(1)\nOutput: ~3.14159\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Area(2)\nOutput: ~12.566\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Area(0)\nOutput: 0\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Untyped constants</span><span class=\"tchip\">Constant rounding</span><span class=\"tchip\">Constant vs var</span></div></details>",
    "education": "<h1>Untyped constants and precision</h1>\n<h2>Intuition</h2>\n<p>An untyped constant has no type and carries <strong>arbitrary precision</strong> until it is</p>\n<p>assigned to a typed value. Only at that point is it rounded to fit.</p>\n<pre class=\"md\"><code>\nconst Pi = 3.14159265358979323846  // untyped; ~full float64 precision available\nvar r float64 = 2\narea := Pi * r * r                 // Pi rounds to float64 here\n</code></pre>\n<p>Writing extra digits is free: they cost nothing and only round at the point of</p>\n<p>use, to whatever float type the expression needs.</p>\n<h2>Approach</h2>\n<p>1. Declare <code>Pi</code> as an untyped constant with 20+ digits.</p>\n<p>2. The compiler rounds it to float64 at the point of use.</p>\n<p>3. <code>Area</code> is <code>Pi<em>r</em>r</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nconst Pi = 3.14159265358979323846264338327950288\n\nfunc Area(r float64) float64 {\n\treturn Pi * r * r\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>Area(1)</code> returns Pi rounded to float64; the extra precision avoids compounding rounding error.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>No type on the left (<code>const Pi = ...</code>) keeps it untyped.</li>\n<li>Untyped constants still have a <em>default</em> type (float for a float literal) used</li>\n</ul>\n<p>  when the context does not force one, e.g. <code>x := Pi</code>.</p>\n<ul>\n<li>Precision only matters at conversion time; <code>Pi * Pi</code> stays exact until stored.</li>\n</ul>",
    "starter": "// Package geo uses a high-precision untyped constant for circle math.\npackage geo\n\n// Pi is an untyped constant with more precision than float64 can hold; the\n// compiler rounds it at the point of use.\n//\n// TODO(candidate): define Pi to at least 20 significant digits as an untyped\n// constant (no type on the left).\nconst Pi = 3.0\n\n// Area returns Pi*r*r as float64.\n//\n// TODO(candidate): implement.\nfunc Area(r float64) float64 {\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "geo.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/01-variables-and-constants/middle/revoke": {
    "id": "01-language-basics/01-variables-and-constants/middle/revoke",
    "title": "Revoke Permissions",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Revoke Permissions</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>Removing a permission is not <code>-</code>; it is clearing bits with the AND-NOT operator</p>\n<p><code>&amp;^</code>, which leaves untouched bits alone.</p>\n<h2>Task</h2>\n<p>In perms.go:</p>\n<p>1. Define <code>Read, Write, Execute</code> with <code>1 &lt;&lt; iota</code>.</p>\n<p>2. Implement <code>Revoke(set, drop)</code> clearing <code>drop</code>'s bits from <code>set</code>.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Revoke(Read|Write, Write)\nOutput: Read\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Revoke(Read, Write)\nOutput: Read\n</code></pre>\n<p>_Explanation:_ Dropping an absent bit is a no-op.</p>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Revoke(Read|Write|Execute, Read)\nOutput: Write|Execute\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">iota bit flags</span><span class=\"tchip\">AND-NOT <code>&amp;^</code></span><span class=\"tchip\">Idempotence</span></div></details>",
    "education": "<h1>Clearing bits with AND-NOT</h1>\n<h2>Intuition</h2>\n<p>Go has a dedicated <strong>bit-clear</strong> operator <code>&amp;^</code> (\"AND NOT\"). <code>a &amp;^ b</code> keeps every</p>\n<p>bit of <code>a</code> except those set in <code>b</code>:</p>\n<pre class=\"md\"><code>\nset := Read | Write | Execute\nset &amp;^ Write // Read | Execute \u2014 Write's bit cleared, others untouched\n</code></pre>\n<p>It is equivalent to <code>a &amp; ^b</code> but reads as one intent: \"remove these bits\".</p>\n<h2>Approach</h2>\n<p>1. Use AND-NOT (<code>&amp;^</code>) to clear the bits in <code>drop</code>.</p>\n<p>2. Bits not in <code>drop</code> are untouched.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\ntype Permission uint8\n\nconst (\n\tRead Permission = 1 &lt;&lt; iota\n\tWrite\n\tExecute\n)\n\nfunc Revoke(set, drop Permission) Permission {\n\treturn set &amp;^ drop\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>Revoke(Read|Write, Write)</code> clears bit 1, leaving <code>Read</code>.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Do not reach for <code>^</code> (XOR) to clear \u2014 that adds absent bits back.</li>\n<li><code>&amp;^</code> is left-associative and has the same precedence as <code>*</code>.</li>\n<li>Pairs perfectly with <code>|</code> (set bits) and <code>&amp;</code> (test bits) for flag sets.</li>\n</ul>",
    "starter": "// Package perms clears permission bits with AND-NOT.\npackage perms\n\n// Permission is a single-bit permission flag.\ntype Permission uint8\n\n// Read, Write, Execute bit flags via iota.\n//\n// TODO(candidate): define with 1<<iota.\nconst (\n\tRead    Permission = 0\n\tWrite   Permission = 0\n\tExecute Permission = 0\n)\n\n// Revoke clears the bits in drop from set, leaving the rest intact.\n//\n// TODO(candidate): implement using the AND-NOT operator.\nfunc Revoke(set, drop Permission) Permission {\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "perms.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/01-variables-and-constants/middle/score-tiers": {
    "id": "01-language-basics/01-variables-and-constants/middle/score-tiers",
    "title": "Score Tiers",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Score Tiers</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>Reward thresholds climb in fixed steps. An <code>iota</code> expression writes the pattern</p>\n<p>once and lets the block repeat it.</p>\n<h2>Task</h2>\n<p>In tiers.go:</p>\n<p>1. Define <code>Bronze=100, Silver=200, Gold=300</code> using <code>(iota+1)*100</code> written once.</p>\n<p>2. Implement <code>Rank(score)</code> returning the highest tier \u2264 score, else 0.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Bronze, Silver, Gold\nOutput: 100, 200, 300\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Rank(250)\nOutput: Silver\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Rank(50)\nOutput: 0\n</code></pre>\n<p>_Explanation:_ Below Bronze.</p>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">iota in expressions</span><span class=\"tchip\">Implicit repetition</span><span class=\"tchip\">Ordered comparison</span></div></details>",
    "education": "<h1>iota in expressions</h1>\n<h2>Intuition</h2>\n<p><code>iota</code> is just an integer you can put inside any constant expression. Scale or</p>\n<p>offset it and the block repeats the expression with <code>iota</code> advancing:</p>\n<pre class=\"md\"><code>\ntype Tier int\nconst (\n\tBronze Tier = (iota + 1) * 100 // (0+1)*100 == 100\n\tSilver                         // (1+1)*100 == 200\n\tGold                           // (2+1)*100 == 300\n)\n</code></pre>\n<p>Only <code>Bronze</code> needs the expression; <code>Silver</code> and <code>Gold</code> inherit it.</p>\n<h2>Approach</h2>\n<p>1. Define thresholds with <code>(iota + 1) * 100</code>.</p>\n<p>2. <code>Rank</code> returns the highest tier whose threshold <code>&lt;= score</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\ntype Tier int\n\nconst (\n\tBronze Tier = (iota + 1) * 100\n\tSilver\n\tGold\n)\n\nfunc Rank(score int) Tier {\n\tswitch {\n\tcase score &gt;= int(Gold):\n\t\treturn Gold\n\tcase score &gt;= int(Silver):\n\t\treturn Silver\n\tcase score &gt;= int(Bronze):\n\t\treturn Bronze\n\tdefault:\n\t\treturn 0\n\t}\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>Rank(250)</code>: 250 \u2265 Silver(200) but &lt; Gold(300), so Silver.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>iota</code> starts at 0, so plain <code>iota * 100</code> gives 0,100,200. Use <code>(iota+1)</code> when</li>\n</ul>\n<p>  you want the run to start at the step, not at zero.</p>\n<ul>\n<li>The expression is copied verbatim to bare lines; an explicit value on one line</li>\n</ul>\n<p>  breaks the run for the lines below it.</p>",
    "starter": "// Package tiers ranks scores using an iota expression.\npackage tiers\n\n// Tier is a reward tier threshold.\ntype Tier int\n\n// Bronze=100, Silver=200, Gold=300 via a repeated iota expression.\n//\n// TODO(candidate): define with (iota+1)*100 written once.\nconst (\n\tBronze Tier = 0\n\tSilver Tier = 0\n\tGold   Tier = 0\n)\n\n// Rank returns the highest tier whose threshold is <= score, or 0 below Bronze.\n//\n// TODO(candidate): implement.\nfunc Rank(score int) Tier {\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "tiers.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/01-variables-and-constants/middle/temp-scale": {
    "id": "01-language-basics/01-variables-and-constants/middle/temp-scale",
    "title": "Typed Temperature Constants",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Typed Temperature Constants</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>Typing a constant (<code>Celsius</code>) makes it participate in a domain type's API and</p>\n<p>prevents accidental mixing with plain floats.</p>\n<h2>Task</h2>\n<p>In temp.go:</p>\n<p>1. Define <code>AbsoluteZero Celsius = -273.15</code> and <code>Boiling Celsius = 100</code>.</p>\n<p>2. Implement <code>Valid(c)</code> returning true when <code>c &gt;= AbsoluteZero</code>.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Valid(-273.15)\nOutput: true\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Valid(-300)\nOutput: false\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Valid(100)\nOutput: true\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Typed constants</span><span class=\"tchip\">Domain types</span><span class=\"tchip\">Comparison</span></div></details>",
    "education": "<h1>Typed constants</h1>\n<h2>Intuition</h2>\n<p>Putting a type on a constant makes it a value of a <em>domain</em> type, not a bare</p>\n<p>number:</p>\n<pre class=\"md\"><code>\ntype Celsius float64\nconst (\n\tAbsoluteZero Celsius = -273.15\n\tBoiling      Celsius = 100\n)\n</code></pre>\n<p><code>AbsoluteZero</code> is a <code>Celsius</code>, so it slots naturally into any API that speaks</p>\n<p><code>Celsius</code> and resists being mixed up with, say, a <code>Fahrenheit</code> or a raw float.</p>\n<h2>Approach</h2>\n<p>1. Declare <code>AbsoluteZero</code> and <code>Boiling</code> as typed <code>Celsius</code> constants.</p>\n<p>2. <code>Valid</code> checks <code>c &gt;= AbsoluteZero</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\ntype Celsius float64\n\nconst (\n\tAbsoluteZero Celsius = -273.15\n\tBoiling      Celsius = 100\n)\n\nfunc Valid(c Celsius) bool {\n\treturn c &gt;= AbsoluteZero\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>Valid(-300)</code> is below absolute zero \u2192 false; exactly -273.15 is valid.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Give each constant the type, or type the first and let bare lines inherit it.</li>\n<li>Typed and untyped constants mix in arithmetic, but two <strong>different</strong> named</li>\n</ul>\n<p>  types do not: <code>Celsius + Fahrenheit</code> will not compile.</p>\n<ul>\n<li>Comparisons (<code>&gt;=</code>, <code>&lt;</code>) work within the type.</li>\n</ul>",
    "starter": "// Package temp uses typed constants for temperature limits.\npackage temp\n\n// Celsius is a temperature in degrees Celsius.\ntype Celsius float64\n\n// AbsoluteZero and Boiling as typed Celsius constants.\n//\n// TODO(candidate): define AbsoluteZero=-273.15, Boiling=100.\nconst (\n\tAbsoluteZero Celsius = 0\n\tBoiling      Celsius = 0\n)\n\n// Valid reports whether c is at or above absolute zero.\n//\n// TODO(candidate): implement.\nfunc Valid(c Celsius) bool {\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "temp.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/01-variables-and-constants/middle/weekday-iota": {
    "id": "01-language-basics/01-variables-and-constants/middle/weekday-iota",
    "title": "Weekday Enum",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Weekday Enum</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>Days of the week are a natural enum. <code>iota</code> numbers them 0..6 with no manual</p>\n<p>values.</p>\n<h2>Task</h2>\n<p>In weekday.go:</p>\n<p>1. Define <code>Sunday..Saturday</code> in one const block (<code>Sunday=0</code>, <code>Saturday=6</code>).</p>\n<p>2. Implement <code>IsWeekend</code> returning true for Saturday and Sunday.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Sunday, Saturday\nOutput: 0, 6\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  IsWeekend(Saturday)\nOutput: true\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  IsWeekend(Monday)\nOutput: false\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">iota counter</span><span class=\"tchip\">Named typed enum</span><span class=\"tchip\">Range over ints</span></div></details>",
    "education": "<h1>iota enumerations</h1>\n<h2>Intuition</h2>\n<p>An enum is a small set of named integer values. <code>iota</code> numbers them for you,</p>\n<p>starting at 0, so you never assign the numbers by hand:</p>\n<pre class=\"md\"><code>\ntype Day int\nconst (\n\tSunday Day = iota // 0\n\tMonday            // 1\n\tTuesday           // 2\n\t// ...\n\tSaturday          // 6\n)\n</code></pre>\n<p>Naming the type (<code>type Day int</code>) gives the constants a distinct type, so the</p>\n<p>compiler can catch a <code>Day</code> used where a plain <code>int</code> was expected and vice versa.</p>\n<h2>Approach</h2>\n<p>1. Define the seven days with a single <code>iota</code> run (Sunday = 0).</p>\n<p>2. <code>IsWeekend</code> checks for Saturday or Sunday.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\ntype Day int\n\nconst (\n\tSunday Day = iota\n\tMonday\n\tTuesday\n\tWednesday\n\tThursday\n\tFriday\n\tSaturday\n)\n\nfunc IsWeekend(d Day) bool {\n\treturn d == Saturday || d == Sunday\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>iota</code> numbers the days 0\u20136; <code>IsWeekend(Saturday)</code> matches \u2192 true.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>iota</code> resets to 0 in <strong>every</strong> new <code>const (...)</code> block.</li>\n<li>Because the zero value is <code>Sunday</code>, an unset <code>Day</code> looks like Sunday. If 0</li>\n</ul>\n<p>  should mean \"invalid\", start the run at <code>iota + 1</code> and reserve 0.</p>\n<ul>\n<li>Bare lines repeat the previous expression; keep them bare to stay on the</li>\n</ul>\n<p>  sequence.</p>",
    "starter": "// Package weekday enumerates days with iota.\npackage weekday\n\n// Day is a day of the week, Sunday first.\ntype Day int\n\n// Sunday..Saturday, Sunday=0.\n//\n// TODO(candidate): define the seven days with a single iota run.\nconst (\n\tSunday    Day = 0\n\tMonday    Day = 0\n\tTuesday   Day = 0\n\tWednesday Day = 0\n\tThursday  Day = 0\n\tFriday    Day = 0\n\tSaturday  Day = 0\n)\n\n// IsWeekend reports whether d is Saturday or Sunday.\n//\n// TODO(candidate): implement.\nfunc IsWeekend(d Day) bool {\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "weekday.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/01-variables-and-constants/senior/iota-offset": {
    "id": "01-language-basics/01-variables-and-constants/senior/iota-offset",
    "title": "Off-By-One iota Flags",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Off-By-One iota Flags</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p>A permission block compiles, <code>Has</code> looks right, yet every flag is double what it</p>\n<p>should be \u2014 masks silently overlap. The bug is one token in the iota expression.</p>\n<h2>Task</h2>\n<p>Fix the single line between the <code>CHANGE CODE</code> markers in perms.go so</p>\n<p><code>Read=1, Write=2, Execute=4</code>. Do not touch anything else.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Read, Write, Execute\nOutput: 1, 2, 4\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Has(Read|Execute, Read)\nOutput: true\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Has(Write, Read)\nOutput: false\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">iota base</span><span class=\"tchip\"><code>1 &lt;&lt; iota</code></span><span class=\"tchip\">Bit overlap</span></div></details>",
    "education": "<h1>The iota base expression</h1>\n<h2>Intuition</h2>\n<p>The <strong>first</strong> line of a <code>const</code> block sets the expression that every bare line</p>\n<p>below repeats. <code>iota</code> starts at 0 on that first line, so the base expression is</p>\n<p>evaluated with <code>iota == 0</code>:</p>\n<pre class=\"md\"><code>\nconst (\n\tRead Permission = 1 &lt;&lt; iota // iota==0 -&gt; 1\n\tWrite                       // iota==1 -&gt; 2\n\tExecute                     // iota==2 -&gt; 4\n)\n</code></pre>\n<p>If the base expression is skewed \u2014 say <code>1 &lt;&lt; (iota + 1)</code> \u2014 the whole run shifts:</p>\n<p>Read becomes 2, Write 4, Execute 8. The code compiles and <em>looks</em> like flags,</p>\n<p>but the values are wrong from the very first one.</p>\n<h2>Approach</h2>\n<p>1. The first flag must be <code>1 &lt;&lt; iota</code> (iota 0 \u2192 bit 0).</p>\n<p>2. The bug's <code>1 &lt;&lt; (iota + 1)</code> shifts every flag up one, so Read becomes 2.</p>\n<p>3. Drop the <code>+ 1</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\ntype Permission uint8\n\nconst (\n\tRead Permission = 1 &lt;&lt; iota\n\tWrite\n\tExecute\n)\n\nfunc Has(set, want Permission) bool { return set&amp;want == want }\n</code></pre>\n<h2>Walkthrough</h2>\n<p>With the offset, Read is 2 and masks fail; <code>1 &lt;&lt; iota</code> restores Read=1, Write=2, Execute=4.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Debug flag values by pinning the first: it must be <code>1 &lt;&lt; 0 == 1</code>.</li>\n<li>The off-by-one hides because the <em>relationships</em> still look plausible</li>\n</ul>\n<p>  (each is double the last) \u2014 only the absolute values are wrong.</p>\n<ul>\n<li>Comment the expected numbers next to each constant during review.</li>\n</ul>",
    "starter": "// Package perms packs permission bits. One planted bug shifts every flag.\npackage perms\n\n// Permission is a single-bit permission flag.\ntype Permission uint8\n\nconst (\n\t// CHANGE CODE BELOW THIS LINE\n\tRead Permission = 1 << (iota + 1)\n\t// CHANGE CODE ABOVE THIS LINE\n\tWrite\n\tExecute\n)\n\n// Has reports whether set contains every bit in want.\nfunc Has(set, want Permission) bool { return set&want == want }\n",
    "debug": "",
    "file": "perms.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/01-variables-and-constants/senior/kb-shift": {
    "id": "01-language-basics/01-variables-and-constants/senior/kb-shift",
    "title": "Byte-Scale Shift",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Byte-Scale Shift</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p><code>1 &lt;&lt; iota</code> gives 2,4,8 \u2014 not 1024,1048576. Binary units need the shift to jump</p>\n<p>10 bits per step: <code>1 &lt;&lt; (10 * iota)</code>.</p>\n<h2>Task</h2>\n<p>Fix the single line between the markers in units.go so</p>\n<p><code>KB=1024, MB=1048576, GB=1073741824</code>.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  KB\nOutput: 1024\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  MB\nOutput: 1048576\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  GB\nOutput: 1073741824\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">iota position</span><span class=\"tchip\">Shift scaling</span><span class=\"tchip\">Implicit repeat</span></div></details>",
    "education": "<h1>Shift stride in iota units</h1>\n<h2>Intuition</h2>\n<p>Binary size units scale by 1024 per step, which is a <strong>10-bit</strong> shift, not one</p>\n<p>bit. The <code>iota</code> slot picks the exponent, so the multiplier is <code>10 * iota</code>:</p>\n<pre class=\"md\"><code>\nconst (\n\t_  ByteSize = iota          // consumes iota==0\n\tKB ByteSize = 1 &lt;&lt; (10 * iota) // iota==1 -&gt; 1&lt;&lt;10 == 1024\n\tMB                              // iota==2 -&gt; 1&lt;&lt;20\n\tGB                              // iota==3 -&gt; 1&lt;&lt;30\n)\n</code></pre>\n<p><code>1 &lt;&lt; iota</code> (stride 1) would give 2, 4, 8 \u2014 powers of two, not powers of 1024.</p>\n<h2>Approach</h2>\n<p>1. Each unit is <code>1 &lt;&lt; (10 * iota)</code>, a 1024\u00d7 step.</p>\n<p>2. The bug uses <code>1 &lt;&lt; iota</code> (2\u00d7 step); restore the <code>10 *</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\ntype ByteSize uint64\n\nconst (\n\t_ ByteSize = iota\n\tKB ByteSize = 1 &lt;&lt; (10 * iota)\n\tMB\n\tGB\n)\n</code></pre>\n<h2>Walkthrough</h2>\n<p>The bug makes KB <code>1 &lt;&lt; 1 = 2</code>; <code>1 &lt;&lt; (10 * iota)</code> gives <code>1 &lt;&lt; 10 = 1024</code>.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>The blank <code>_ = iota</code> line matters: it puts KB on <code>iota==1</code>, so <code>10*iota == 10</code>.</li>\n<li>Use <code>uint64</code>; <code>1 &lt;&lt; 40</code> (TB) overflows 32-bit ints.</li>\n<li>Verify by pinning KB to exactly 1024 in a test.</li>\n</ul>",
    "starter": "// Package units builds binary size constants. A planted shift bug breaks scaling.\npackage units\n\n// ByteSize counts bytes.\ntype ByteSize uint64\n\nconst (\n\t_ ByteSize = iota\n\t// CHANGE CODE BELOW THIS LINE\n\tKB ByteSize = 1 << iota\n\t// CHANGE CODE ABOVE THIS LINE\n\tMB\n\tGB\n)\n",
    "debug": "",
    "file": "units.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/01-variables-and-constants/senior/maxint-signbit": {
    "id": "01-language-basics/01-variables-and-constants/senior/maxint-signbit",
    "title": "Max Int Sign Bit",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Max Int Sign Bit</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p><code>int(^uint(0))</code> is all bits set \u2014 which as a <em>signed</em> int is <code>-1</code>, not the</p>\n<p>maximum. The largest signed value needs the sign bit cleared with <code>&gt;&gt; 1</code>.</p>\n<h2>Task</h2>\n<p>Fix the single line between the markers in limits.go so <code>MaxInt</code></p>\n<p>is the largest positive int.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  MaxInt\nOutput: largest positive int\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Overflows()\nOutput: true\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  MaxInt+1\nOutput: wraps negative\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Two's complement</span><span class=\"tchip\">Sign-bit shift</span><span class=\"tchip\">Conversion order</span></div></details>",
    "education": "<h1>Deriving MaxInt safely</h1>\n<h2>Intuition</h2>\n<p><code>^uint(0)</code> is all-bits-set: the largest <strong>unsigned</strong> value. Interpreting those</p>\n<p>same bits as a <strong>signed</strong> int gives -1, because the top bit is the sign bit. To</p>\n<p>get the largest positive signed value, clear that sign bit with <code>&gt;&gt; 1</code>:</p>\n<pre class=\"md\"><code>\nvar allBits = ^uint(0)      // e.g. 2^64 - 1\nMaxInt := int(allBits &gt;&gt; 1) // 2^63 - 1\n</code></pre>\n<p>The right shift happens in <code>uint</code> (logical shift, zero-filled), then convert.</p>\n<h2>Approach</h2>\n<p>1. <code>int(allBits)</code> is -1 (all bits including the sign bit).</p>\n<p>2. Shift right by one to clear the sign bit: <code>int(allBits &gt;&gt; 1)</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nvar allBits = ^uint(0)\n\nvar MaxInt = int(allBits &gt;&gt; 1)\n\nfunc Overflows() bool { return MaxInt+1 &lt; MaxInt }\n</code></pre>\n<h2>Walkthrough</h2>\n<p>All bits set as a signed int is -1, not the max. Clearing the top bit yields the largest positive value.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Convert <strong>after</strong> shifting: <code>int(allBits &gt;&gt; 1)</code>. Shifting a signed -1 right is</li>\n</ul>\n<p>  an <em>arithmetic</em> shift (sign-extends) and stays -1.</p>\n<ul>\n<li>A typed-int constant that overflows is a compile error; build wide values in</li>\n</ul>\n<p>  <code>uint</code>.</p>\n<ul>\n<li>The stdlib exposes <code>math.MaxInt</code> (Go 1.17+) so you rarely hand-roll this.</li>\n</ul>",
    "starter": "// Package limits derives the max signed int from a bit pattern.\n// A planted bug forgets the sign-bit shift.\npackage limits\n\n// allBits is every bit set; kept as a var so the conversion below happens at\n// run time (a constant conversion would overflow int at compile time).\nvar allBits = ^uint(0)\n\n// MaxInt is meant to be the largest positive int.\n// CHANGE CODE BELOW THIS LINE\nvar MaxInt = int(allBits)\n\n// CHANGE CODE ABOVE THIS LINE\n\n// Overflows reports whether adding 1 to MaxInt would overflow (it always does).\nfunc Overflows() bool { return MaxInt+1 < MaxInt }\n",
    "debug": "",
    "file": "limits.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/01-variables-and-constants/senior/percent-order": {
    "id": "01-language-basics/01-variables-and-constants/senior/percent-order",
    "title": "Integer Percent Order",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Integer Percent Order</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p><code>part / total * 100</code> evaluates left to right: for <code>1/4</code> the integer division is</p>\n<p>0 first, so the result is always 0. Multiply before dividing.</p>\n<h2>Task</h2>\n<p>Fix the single line between the markers in percent.go so integer</p>\n<p>percentages compute correctly (floored).</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Percent(1, 4)\nOutput: 25\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Percent(3, 8)\nOutput: 37\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Percent(1, 3)\nOutput: 33\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Integer division</span><span class=\"tchip\">Evaluation order</span><span class=\"tchip\">Scale first</span></div></details>",
    "education": "<h1>Evaluation order with integer division</h1>\n<h2>Intuition</h2>\n<p><code>*</code> and <code>/</code> share the same precedence and associate <strong>left to right</strong>. With</p>\n<p>integers, doing the division first can truncate to 0 before you ever multiply:</p>\n<pre class=\"md\"><code>\npart, total := 1, 4\npart / total * 100 // (1/4)*100 == 0*100 == 0\npart * 100 / total // (1*100)/4 == 25   \u2713\n</code></pre>\n<p>Same operators, same operands \u2014 only the order differs, and integer truncation</p>\n<p>makes that order decisive.</p>\n<h2>Approach</h2>\n<p>1. Integer <code>part / total</code> truncates to 0 before the <code>* 100</code>.</p>\n<p>2. Multiply first: <code>part * 100 / total</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Percent(part, total int) int {\n\treturn part * 100 / total\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>Percent(1, 4)</code>: the bug computes <code>1/4 = 0</code>, then <code>*100 = 0</code>. Multiplying first gives <code>100/4 = 25</code>.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Watch for intermediate overflow the other way: <code>part * 100</code> must not overflow</li>\n</ul>\n<p>  the type. Widen if needed.</p>\n<ul>\n<li>Floating point avoids truncation but adds rounding \u2014 pick per use.</li>\n<li>Parenthesize when intent matters: <code>(part * 100) / total</code>.</li>\n</ul>",
    "starter": "// Package percent computes integer percentages. A planted evaluation-order bug\n// truncates the result to 0.\npackage percent\n\n// Percent returns what percent part is of total, as an int (floored).\n// total is assumed > 0.\nfunc Percent(part, total int) int {\n\t// CHANGE CODE BELOW THIS LINE\n\treturn part / total * 100\n\t// CHANGE CODE ABOVE THIS LINE\n}\n",
    "debug": "",
    "file": "percent.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/01-variables-and-constants/senior/revoke-xor": {
    "id": "01-language-basics/01-variables-and-constants/senior/revoke-xor",
    "title": "XOR vs AND-NOT",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">XOR vs AND-NOT</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p><code>Revoke</code> uses <code>^</code> (XOR), which <em>toggles</em> bits \u2014 so revoking a permission the</p>\n<p>user never had actually <strong>grants</strong> it. The intended operator is bit-clear <code>&amp;^</code>.</p>\n<h2>Task</h2>\n<p>Fix the single line between the markers in perms.go so <code>Revoke</code></p>\n<p>clears bits and is a no-op for absent bits.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Revoke(Read|Write, Write)\nOutput: Read\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Revoke(Read, Write)\nOutput: Read\n</code></pre>\n<p>_Explanation:_ XOR would wrongly add Write.</p>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Revoke(Read|Write|Execute, Read)\nOutput: Write|Execute\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">XOR toggles</span><span class=\"tchip\">AND-NOT clears</span><span class=\"tchip\">Idempotence</span></div></details>",
    "education": "<h1>XOR toggles, AND-NOT clears</h1>\n<h2>Intuition</h2>\n<p><code>^</code> between two values is <strong>XOR</strong>: it flips each bit where the operand is set.</p>\n<p>That is the wrong tool for \"remove a flag\", because flipping an <em>absent</em> bit</p>\n<p>turns it on:</p>\n<pre class=\"md\"><code>\nset := Read           // Write bit is 0\nset ^ Write           // Read | Write  &lt;- XOR ADDED Write!\n</code></pre>\n<p>To clear, use bit-clear <code>&amp;^</code>:</p>\n<pre class=\"md\"><code>\nset &amp;^ Write          // removes Write if present, no-op if absent\n</code></pre>\n<h2>Approach</h2>\n<p>1. XOR toggles bits, so revoking an absent bit sets it.</p>\n<p>2. Use AND-NOT (<code>&amp;^</code>) to clear only.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\ntype Permission uint8\n\nconst (\n\tRead Permission = 1 &lt;&lt; iota\n\tWrite\n\tExecute\n)\n\nfunc Revoke(set, drop Permission) Permission {\n\treturn set &amp;^ drop\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>Revoke(Read, Write)</code> with XOR would add Write; <code>&amp;^</code> leaves an absent bit untouched, returning Read.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>XOR is for toggling and for cheap swaps/checksums, not for clearing.</li>\n<li><code>&amp;^</code> is idempotent for clearing; XOR is its own inverse (applying twice</li>\n</ul>\n<p>  restores).</p>\n<ul>\n<li>Always test the \"clear an absent bit\" case \u2014 that is where the two diverge.</li>\n</ul>",
    "starter": "// Package perms clears permission bits. A planted XOR bug toggles instead.\npackage perms\n\n// Permission is a single-bit permission flag.\ntype Permission uint8\n\nconst (\n\tRead Permission = 1 << iota\n\tWrite\n\tExecute\n)\n\n// Revoke clears the bits in drop from set. Dropping an absent bit is a no-op.\nfunc Revoke(set, drop Permission) Permission {\n\t// CHANGE CODE BELOW THIS LINE\n\treturn set ^ drop\n\t// CHANGE CODE ABOVE THIS LINE\n}\n",
    "debug": "",
    "file": "perms.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/01-variables-and-constants/senior/rgb-pack": {
    "id": "01-language-basics/01-variables-and-constants/senior/rgb-pack",
    "title": "RGB Bit Packing",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">RGB Bit Packing</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p>Packing RGB into <code>0x00RRGGBB</code> needs red shifted 16 bits, green 8, blue 0. The</p>\n<p>code shifts red by only 8, colliding it with green.</p>\n<h2>Task</h2>\n<p>Fix the single line between the markers in color.go so red occupies</p>\n<p>bits 16\u201323.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Pack(0x12, 0x34, 0x56)\nOutput: 0x123456\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Red(0x123456)\nOutput: 0x12\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Pack(255, 0, 0)\nOutput: 0xFF0000\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Bit shifting</span><span class=\"tchip\">Conversion width</span><span class=\"tchip\">OR composition</span></div></details>",
    "education": "<h1>Bit packing with shifts</h1>\n<h2>Intuition</h2>\n<p>Several small fields can share one integer by giving each its own <strong>bit lane</strong>.</p>\n<p>For 8-bit RGB in a <code>uint32</code> as <code>0x00RRGGBB</code>, red occupies bits 16\u201323, green 8\u201315,</p>\n<p>blue 0\u20137:</p>\n<pre class=\"md\"><code>\nfunc Pack(r, g, b uint8) uint32 {\n\treturn uint32(r)&lt;&lt;16 | uint32(g)&lt;&lt;8 | uint32(b)\n}\n</code></pre>\n<p>Convert each channel to the wider type <strong>before</strong> shifting, then OR the</p>\n<p>non-overlapping lanes together. Unpack with a shift-and-mask:</p>\n<pre class=\"md\"><code>\nred := uint8(v &gt;&gt; 16)\n</code></pre>\n<h2>Approach</h2>\n<p>1. Red occupies bits 16\u201323, so shift it by 16, not 8.</p>\n<p>2. <code>r&lt;&lt;16 | g&lt;&lt;8 | b</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Pack(r, g, b uint8) uint32 {\n\treturn uint32(r)&lt;&lt;16 | uint32(g)&lt;&lt;8 | uint32(b)\n}\n\nfunc Red(v uint32) uint8 { return uint8(v &gt;&gt; 16) }\n</code></pre>\n<h2>Walkthrough</h2>\n<p>The bug shifts red by 8, colliding with green. Shifting by 16 places red in the high byte \u2192 0x123456.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Convert to <code>uint32</code> <strong>before</strong> shifting past 8 bits; a <code>uint8 &lt;&lt; 16</code> is 0.</li>\n<li>Lanes must not overlap \u2014 sum the field widths and keep them within the word.</li>\n<li>Mask on unpack (<code>&amp; 0xFF</code>) if the field is not at the top.</li>\n</ul>",
    "starter": "// Package color packs 8-bit RGB into a uint32. A planted shift bug swaps a channel.\npackage color\n\n// Pack encodes r,g,b (each 0..255) into 0x00RRGGBB.\nfunc Pack(r, g, b uint8) uint32 {\n\t// CHANGE CODE BELOW THIS LINE\n\treturn uint32(r)<<8 | uint32(g)<<8 | uint32(b)\n\t// CHANGE CODE ABOVE THIS LINE\n}\n\n// Red extracts the red channel from a packed value.\nfunc Red(v uint32) uint8 { return uint8(v >> 16) }\n",
    "debug": "",
    "file": "color.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/01-variables-and-constants/senior/status-zero": {
    "id": "01-language-basics/01-variables-and-constants/senior/status-zero",
    "title": "Zero-Value Enum Trap",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Zero-Value Enum Trap</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p>If the first enum member is <code>iota</code> (0), then a freshly declared <code>var s Status</code></p>\n<p>is silently a valid state \u2014 the zero value collides with a real one. Shift the</p>\n<p>run so 0 stays reserved for \"unknown\".</p>\n<h2>Task</h2>\n<p><code>Pending</code> starts at plain <code>iota</code> (0), so a zero-valued <code>Status</code> reads as</p>\n<p><code>Pending</code> and the values are 0,1,2. Fix only the marked line so</p>\n<p><code>Pending=1, Shipped=2, Delivered=3</code> and the zero value is unknown.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  IsKnown(Pending)\nOutput: true\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  IsKnown(Status(0))\nOutput: false\n</code></pre>\n<p>_Explanation:_ A zero-valued Status must be unknown.</p>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  IsKnown(Delivered)\nOutput: true\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Zero value</span><span class=\"tchip\">iota offset</span><span class=\"tchip\">Enum hygiene</span></div></details>",
    "education": "<h1>The zero value and enums</h1>\n<h2>Intuition</h2>\n<p>Every Go variable is born with its type's <strong>zero value</strong> \u2014 0 for integers. For</p>\n<p>an <code>iota</code> enum whose first member is 0, that means an unset variable silently</p>\n<p>equals the first state:</p>\n<pre class=\"md\"><code>\ntype Status int\nconst (\n\tPending Status = iota // 0  &lt;- also the zero value!\n\tShipped               // 1\n\tDelivered             // 2\n)\nvar s Status // 0 == Pending, even though nobody set it\n</code></pre>\n<p>Reserve 0 for \"unknown/invalid\" by starting the run at <code>iota + 1</code>:</p>\n<pre class=\"md\"><code>\nconst (\n\tPending Status = iota + 1 // 1\n\tShipped                   // 2\n\tDelivered                 // 3\n)\n// zero value 0 now means \"unset\", distinct from every real state\n</code></pre>\n<h2>Approach</h2>\n<p>1. With <code>Pending = iota</code> (0), a zero-valued Status equals Pending and looks known.</p>\n<p>2. Start at <code>iota + 1</code> so 0 is reserved for unknown.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\ntype Status int\n\nconst (\n\tPending Status = iota + 1\n\tShipped\n\tDelivered\n)\n\nfunc IsKnown(s Status) bool {\n\treturn s == Pending || s == Shipped || s == Delivered\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>The bug makes <code>Status(0)</code> == Pending \u2192 known. Offsetting by one keeps 0 outside the defined set.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Decide deliberately what 0 means for every enum.</li>\n<li><code>var s Status</code> and <code>Status(0)</code> and a struct field you forgot to set are all the</li>\n</ul>\n<p>  same value.</p>\n<ul>\n<li>Stringer/validation code should treat the reserved 0 as \"unknown\".</li>\n</ul>",
    "starter": "// Package status enumerates order states. A planted zero-value trap misclassifies.\npackage status\n\n// Status is an order lifecycle state.\ntype Status int\n\nconst (\n\t// CHANGE CODE BELOW THIS LINE\n\tPending Status = iota\n\t// CHANGE CODE ABOVE THIS LINE\n\tShipped\n\tDelivered\n)\n\n// IsKnown reports whether s is one of the defined states.\n// A brand-new zero-valued Status must be reported as unknown.\nfunc IsKnown(s Status) bool {\n\treturn s == Pending || s == Shipped || s == Delivered\n}\n",
    "debug": "",
    "file": "status.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/01-variables-and-constants/senior/sum-shadow": {
    "id": "01-language-basics/01-variables-and-constants/senior/sum-shadow",
    "title": "Shadowed Accumulator",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Shadowed Accumulator</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p>The loop looks like it accumulates into <code>total</code>, but <code>SumPositive</code> always</p>\n<p>returns 0. A <code>:=</code> inside the <code>if</code> block creates a new <code>total</code> that dies each</p>\n<p>iteration \u2014 the outer one never changes.</p>\n<h2>Task</h2>\n<p>Fix the single line between the markers in accumulate.go so the</p>\n<p>outer <code>total</code> accumulates. Keep the signature and the surrounding code.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  SumPositive([1 2 3])\nOutput: 6\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  SumPositive([1 -2 3])\nOutput: 4\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  SumPositive(nil)\nOutput: 0\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Shadowing</span><span class=\"tchip\">Block scope</span><span class=\"tchip\"><code>=</code> vs <code>:=</code></span></div></details>",
    "education": "<h1>Shadowing inside loops</h1>\n<h2>Intuition</h2>\n<p><code>:=</code> <strong>declares</strong>. Used inside a loop body or <code>if</code> block, it introduces a new</p>\n<p>variable scoped to that block \u2014 even if a variable of the same name exists</p>\n<p>outside. The outer one is untouched:</p>\n<pre class=\"md\"><code>\ntotal := 0\nfor _, x := range xs {\n\tif x &gt; 0 {\n\t\ttotal := total + x // NEW total, dies at block end\n\t\t_ = total\n\t}\n}\nreturn total // always 0\n</code></pre>\n<p>Each iteration creates and discards a fresh <code>total</code>. The accumulator never grows.</p>\n<h2>Approach</h2>\n<p>1. <code>total := total + x</code> declares a new inner <code>total</code>, discarded each iteration.</p>\n<p>2. Assign to the outer one: <code>total = total + x</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc SumPositive(xs []int) int {\n\ttotal := 0\n\tfor _, x := range xs {\n\t\tif x &gt; 0 {\n\t\t\ttotal = total + x\n\t\t\t_ = total\n\t\t}\n\t}\n\treturn total\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>The <code>:=</code> shadows <code>total</code> inside the <code>if</code>, so the outer sum never grows. Using <code>=</code> accumulates correctly to 6.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Use <code>+=</code> or <code>=</code> to touch the outer variable; reserve <code>:=</code> for genuinely new</li>\n</ul>\n<p>  names.</p>\n<ul>\n<li><code>go vet -vettool</code> shadow checks and linters (e.g. <code>govet</code>'s shadow pass) catch</li>\n</ul>\n<p>  many of these.</p>\n<ul>\n<li>The blank-identifier line <code>_ = total</code> here only exists to make the shadow</li>\n</ul>\n<p>  compile; the real fix removes the need for it.</p>",
    "starter": "// Package accumulate sums positives. A planted shadowing bug drops the result.\npackage accumulate\n\n// SumPositive returns the sum of the positive elements of xs.\nfunc SumPositive(xs []int) int {\n\ttotal := 0\n\tfor _, x := range xs {\n\t\tif x > 0 {\n\t\t\t// CHANGE CODE BELOW THIS LINE\n\t\t\ttotal := total + x\n\t\t\t// CHANGE CODE ABOVE THIS LINE\n\t\t\t_ = total\n\t\t}\n\t}\n\treturn total\n}\n",
    "debug": "",
    "file": "accumulate.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/01-variables-and-constants/senior/tier-threshold": {
    "id": "01-language-basics/01-variables-and-constants/senior/tier-threshold",
    "title": "Tier Threshold Off-By-One",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Tier Threshold Off-By-One</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p><code>iota * 100</code> makes Bronze <code>0</code> \u2014 a free tier nobody has to earn. The thresholds</p>\n<p>must start at 100.</p>\n<h2>Task</h2>\n<p>Fix the single line between the markers in tiers.go so</p>\n<p><code>Bronze=100, Silver=200, Gold=300</code>.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Bronze, Silver, Gold\nOutput: 100, 200, 300\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Bronze (bug)\nOutput: 0\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Gold\nOutput: 300\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">iota starts at 0</span><span class=\"tchip\">Offset expression</span><span class=\"tchip\">Implicit repeat</span></div></details>",
    "education": "<h1>Offsetting iota</h1>\n<h2>Intuition</h2>\n<p><code>iota</code> counts from 0. If the first meaningful value should be the <em>step</em> (say</p>\n<p>100), you must offset the expression, or the first constant lands on 0:</p>\n<pre class=\"md\"><code>\nconst (\n\tBronze Tier = iota * 100 // 0  &lt;- a free tier!\n\tSilver                   // 100\n\tGold                     // 200\n)\n</code></pre>\n<p>Add one before scaling so the run starts at the step:</p>\n<pre class=\"md\"><code>\nconst (\n\tBronze Tier = (iota + 1) * 100 // 100\n\tSilver                         // 200\n\tGold                           // 300\n)\n</code></pre>\n<h2>Approach</h2>\n<p>1. <code>iota * 100</code> makes Bronze 0.</p>\n<p>2. Offset: <code>(iota + 1) * 100</code> so Bronze is 100.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\ntype Tier int\n\nconst (\n\tBronze Tier = (iota + 1) * 100\n\tSilver\n\tGold\n)\n</code></pre>\n<h2>Walkthrough</h2>\n<p>The bug zeroes Bronze; <code>(iota + 1) * 100</code> gives 100, 200, 300.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>iota <em> k</code> starts at 0; <code>(iota + 1) </em> k</code> starts at k.</li>\n<li>The offset must be inside the repeated expression so every bare line inherits</li>\n</ul>\n<p>  it.</p>\n<ul>\n<li>Test the boundary values, not just the ordering.</li>\n</ul>",
    "starter": "// Package tiers sets reward thresholds. A planted iota bug zeroes the first tier.\npackage tiers\n\n// Tier is a reward threshold.\ntype Tier int\n\nconst (\n\t// CHANGE CODE BELOW THIS LINE\n\tBronze Tier = iota * 100\n\t// CHANGE CODE ABOVE THIS LINE\n\tSilver\n\tGold\n)\n",
    "debug": "",
    "file": "tiers.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/01-variables-and-constants/senior/uint-underflow": {
    "id": "01-language-basics/01-variables-and-constants/senior/uint-underflow",
    "title": "Unsigned Underflow",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Unsigned Underflow</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p><code>have - sold</code> on <code>uint</code> wraps around when <code>sold &gt; have</code>, producing a gigantic</p>\n<p>number instead of 0. The zero value and unsigned wraparound combine into a</p>\n<p>classic inventory bug.</p>\n<h2>Task</h2>\n<p>Fix the single line between the markers in stock.go so overselling</p>\n<p>returns 0, never a wrapped value.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Remaining(10, 3)\nOutput: 7\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Remaining(5, 5)\nOutput: 0\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Remaining(2, 9)\nOutput: 0\n</code></pre>\n<p>_Explanation:_ Not a wrapped 18446744073709551609.</p>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Unsigned wraparound</span><span class=\"tchip\">Guard before subtract</span><span class=\"tchip\">Zero value</span></div></details>",
    "education": "<h1>Unsigned underflow</h1>\n<h2>Intuition</h2>\n<p>Unsigned integers have no negative range. Subtracting past zero <strong>wraps around</strong></p>\n<p>to the top of the range \u2014 modular arithmetic, not a negative result:</p>\n<pre class=\"md\"><code>\nvar a uint = 2\na - 9 // 18446744073709551609 on 64-bit, not -7\n</code></pre>\n<p>There is no overflow panic; the wrap is silent and the value is enormous.</p>\n<h2>Approach</h2>\n<p>1. <code>have - sold</code> on <code>uint</code> wraps when <code>sold &gt; have</code>.</p>\n<p>2. Guard: <code>if sold &gt;= have { return 0 }</code> before subtracting.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Remaining(have, sold uint) uint {\n\tif sold &gt;= have {\n\t\treturn 0\n\t}\n\treturn have - sold\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>Remaining(2, 9)</code> would wrap to a huge value; the guard returns 0 instead.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Guard before subtracting: <code>if sold &gt;= have { return 0 }</code>.</li>\n<li><code>len(x) - 1</code> on an empty slice underflows the same way \u2014 check for empty first.</li>\n<li>If you genuinely need signed differences, use a signed type and convert</li>\n</ul>\n<p>  deliberately.</p>",
    "starter": "// Package stock tracks inventory. A planted unsigned-underflow bug lurks.\npackage stock\n\n// Remaining returns how many units are left after selling sold from have.\n// It must never report more than were on hand; overselling yields 0.\nfunc Remaining(have, sold uint) uint {\n\t// CHANGE CODE BELOW THIS LINE\n\treturn have - sold\n\t// CHANGE CODE ABOVE THIS LINE\n}\n",
    "debug": "",
    "file": "stock.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/01-variables-and-constants/staff/abs-minint": {
    "id": "01-language-basics/01-variables-and-constants/staff/abs-minint",
    "title": "Abs of the Most-Negative",
    "level": "staff",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Abs of the Most-Negative</h1><div class=\"p-meta\"><span class=\"pill lv-staff\">staff</span></div></div><h2>Context</h2>\n<p><code>-x</code> for <code>int8(-128)</code> overflows: <code>128</code> has no <code>int8</code> representation, so <code>-x</code></p>\n<p>stays <code>-128</code>, and <code>int(-x)</code> is <code>-128</code>. Two's-complement asymmetry again. Widen</p>\n<p><em>before</em> negating.</p>\n<h2>Task</h2>\n<p>Fix the code between the markers in absval.go so <code>Abs(-128)</code> is 128.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Abs(int8(-5))\nOutput: 5\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Abs(int8(-128))\nOutput: 128\n</code></pre>\n<p>_Explanation:_ The most negative int8 must widen correctly.</p>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Abs(int8(7))\nOutput: 7\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Two's complement asymmetry</span><span class=\"tchip\">Widen then negate</span><span class=\"tchip\">Conversion timing</span></div></details>",
    "education": "<h1>abs() and the most-negative value</h1>\n<h2>Intuition</h2>\n<p>Because of two's-complement asymmetry, the most-negative value has no positive</p>\n<p>counterpart in the same width. Negating it overflows and returns itself:</p>\n<pre class=\"md\"><code>\nvar x int8 = -128\n-x        // still -128 (128 doesn't fit int8)\nint(-x)   // -128 \u2014 the overflow happened before widening\n</code></pre>\n<p>Widen <strong>first</strong>, then negate in the larger type where the result fits:</p>\n<pre class=\"md\"><code>\n-int(x)   // 128\n</code></pre>\n<h2>Approach</h2>\n<p>1. Negating <code>int8(-128)</code> overflows int8.</p>\n<p>2. Widen first: <code>-int(x)</code> computes in <code>int</code>, not <code>int8</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Abs(x int8) int {\n\tif x &lt; 0 {\n\t\treturn -int(x)\n\t}\n\treturn int(x)\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>-x</code> on -128 stays -128 (int8 overflow); <code>-int(x)</code> widens to int then negates \u2192 128.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>-int8(-128)</code> overflows; <code>-int(int8(-128))</code> does not.</li>\n<li>Standard libraries often document that <code>abs(MinInt)</code> is undefined/overflowing \u2014</li>\n</ul>\n<p>  handle it explicitly if it can occur.</p>\n<ul>\n<li>The same asymmetry breaks <code>x / -1</code> for MinInt in some languages.</li>\n</ul>",
    "starter": "// Package absval returns absolute value for int8. Planted naive negate mishandles min.\npackage absval\n\n// Abs returns the absolute value of x as an int (widened) so that the most\n// negative int8 has a correct positive result.\nfunc Abs(x int8) int {\n\t// CHANGE CODE BELOW THIS LINE\n\tif x < 0 {\n\t\treturn int(-x)\n\t}\n\treturn int(x)\n\t// CHANGE CODE ABOVE THIS LINE\n}\n",
    "debug": "",
    "file": "absval.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/01-variables-and-constants/staff/fixed-cents": {
    "id": "01-language-basics/01-variables-and-constants/staff/fixed-cents",
    "title": "Fixed-Point Conversion Order",
    "level": "staff",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Fixed-Point Conversion Order</h1><div class=\"p-meta\"><span class=\"pill lv-staff\">staff</span></div></div><h2>Context</h2>\n<p><code>int64(dollars) <em> 100</code> truncates the dollars to a whole number </em>before* scaling,</p>\n<p>so <code>2.50</code> becomes 200 cents. Scale in floating point, then convert.</p>\n<h2>Task</h2>\n<p>Fix the single line between the markers in money.go so fractional</p>\n<p>dollars survive.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Cents(1.99)\nOutput: 199\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Cents(0.5)\nOutput: 50\n</code></pre>\n<p>_Explanation:_ Truncating dollars first would give 0.</p>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Cents(2.0)\nOutput: 200\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Conversion truncates</span><span class=\"tchip\">Order of operations</span><span class=\"tchip\">Fixed-point money</span></div></details>",
    "education": "<h1>Conversion order in fixed-point</h1>\n<h2>Intuition</h2>\n<p>Converting a float to an integer <strong>truncates</strong> the fraction. Do it before</p>\n<p>scaling and you throw the cents away:</p>\n<pre class=\"md\"><code>\nint64(2.50) * 100 // int64(2.50) == 2, then *100 == 200  \u2717\nint64(2.50 * 100) // 2.50*100 == 250.0, then truncate == 250  \u2713\n</code></pre>\n<p>Scale in floating point first, convert last.</p>\n<h2>Approach</h2>\n<p>1. <code>int64(dollars) * 100</code> truncates dollars before scaling.</p>\n<p>2. Scale first: <code>int64(dollars * 100)</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Cents(dollars float64) int64 {\n\treturn int64(dollars * 100)\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>Cents(0.5)</code>: the bug truncates 0.5 to 0, then \u00d7100 = 0. Scaling first gives <code>int64(50.0) = 50</code>.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>int64(x)</code> rounds toward zero, not to nearest. Add 0.5 (with care for sign) if</li>\n</ul>\n<p>  you want nearest.</p>\n<ul>\n<li>Floating dollar inputs are themselves imprecise; prefer integer cents</li>\n</ul>\n<p>  end-to-end where possible.</p>\n<ul>\n<li>Multiplying before converting can overflow for huge inputs \u2014 bound them.</li>\n</ul>",
    "starter": "// Package money converts dollars to integer cents. Planted truncation order bug.\npackage money\n\n// Cents returns the number of whole cents in a dollar amount, rounding down.\nfunc Cents(dollars float64) int64 {\n\t// CHANGE CODE BELOW THIS LINE\n\treturn int64(dollars) * 100\n\t// CHANGE CODE ABOVE THIS LINE\n}\n",
    "debug": "",
    "file": "money.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/01-variables-and-constants/staff/float32-precision": {
    "id": "01-language-basics/01-variables-and-constants/staff/float32-precision",
    "title": "Float Width & Precision",
    "level": "staff",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Float Width &amp; Precision</h1><div class=\"p-meta\"><span class=\"pill lv-staff\">staff</span></div></div><h2>Context</h2>\n<p><code>0.1</code> is not exact in binary; the rounding error per add is ~10x larger in</p>\n<p><code>float32</code> than <code>float64</code>. Accumulating in <code>float32</code> drifts past a 1e-9 tolerance.</p>\n<p>The literal <code>0.1</code> is an untyped constant \u2014 the <em>variable's</em> type decides the</p>\n<p>precision.</p>\n<h2>Task</h2>\n<p>Fix the single line between the markers in sums.go so the running sum</p>\n<p>holds enough precision.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  SumTenths(10)\nOutput: ~1.0\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  SumTenths(3)\nOutput: ~0.3\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  accumulator type\nOutput: float64, not float32\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Untyped constant</span><span class=\"tchip\">float32 vs float64</span><span class=\"tchip\">Tolerance</span></div></details>",
    "education": "<h1>Float width and error growth</h1>\n<h2>Intuition</h2>\n<p><code>0.1</code> cannot be represented exactly in binary floating point; each store rounds.</p>\n<p><code>float32</code> has 24 bits of mantissa, <code>float64</code> has 53, so the per-operation error</p>\n<p>is far larger in <code>float32</code> and accumulates faster:</p>\n<pre class=\"md\"><code>\nvar t float32\nfor i := 0; i &lt; 10; i++ { t += 0.1 }\n// t drifts from 1.0 by ~1e-7, past a 1e-9 tolerance\n</code></pre>\n<p>The literal <code>0.1</code> is an <em>untyped</em> constant of full precision \u2014 it is the</p>\n<p><strong>variable's</strong> type that fixes how much precision survives each <code>+=</code>.</p>\n<h2>Approach</h2>\n<p>1. <code>float32</code> accumulates 0.1 with visible error.</p>\n<p>2. Use <code>float64</code> for a tighter total.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc SumTenths(n int) float64 {\n\tvar total float64\n\tfor i := 0; i &lt; n; i++ {\n\t\ttotal += 0.1\n\t}\n\treturn float64(total)\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>Summing 0.1 in float32 drifts; float64 keeps the running total close to the expected value.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Never compare floats with <code>==</code>; use an epsilon sized for the type and the</li>\n</ul>\n<p>  operation count.</p>\n<ul>\n<li>Accumulate in the widest reasonable type, narrow only at the boundary.</li>\n<li>Summation order and techniques (Kahan) matter once N is large.</li>\n</ul>",
    "starter": "// Package sums accumulates a decimal that only float64 represents tightly.\n// Planted narrow float type loses precision.\npackage sums\n\n// SumTenths adds 0.1 exactly n times and returns the total.\nfunc SumTenths(n int) float64 {\n\t// CHANGE CODE BELOW THIS LINE\n\tvar total float32\n\t// CHANGE CODE ABOVE THIS LINE\n\tfor i := 0; i < n; i++ {\n\t\ttotal += 0.1\n\t}\n\treturn float64(total)\n}\n",
    "debug": "",
    "file": "sums.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/01-variables-and-constants/staff/hex-byte": {
    "id": "01-language-basics/01-variables-and-constants/staff/hex-byte",
    "title": "Byte Extraction Stride",
    "level": "staff",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Byte Extraction Stride</h1><div class=\"p-meta\"><span class=\"pill lv-staff\">staff</span></div></div><h2>Context</h2>\n<p>A byte is 8 bits, so byte <code>n</code> lives at bit offset <code>8<em>n</code>. Shifting by <code>4</em>n</code></p>\n<p>(a nibble stride) overlaps adjacent bytes \u2014 the truncation to <code>uint8</code> then hides</p>\n<p>it for n=0 only.</p>\n<h2>Task</h2>\n<p>Fix the single line between the markers in extract.go so <code>ByteAt</code></p>\n<p>uses the correct 8-bit stride.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  ByteAt(0x1122334455667788, 0)\nOutput: 0x88\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  ByteAt(0x1122334455667788, 1)\nOutput: 0x77\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  ByteAt(0x1122334455667788, 7)\nOutput: 0x11\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Bit stride</span><span class=\"tchip\">Conversion truncation</span><span class=\"tchip\">Shift amount type</span></div></details>",
    "education": "<h1>Bit strides when slicing a word</h1>\n<h2>Intuition</h2>\n<p>To pull out field <code>n</code> you shift by <code>n * width</code>. A byte is 8 bits wide, so byte</p>\n<p><code>n</code> sits at bit offset <code>8 * n</code>:</p>\n<pre class=\"md\"><code>\nfunc ByteAt(v uint64, n uint) uint8 {\n\treturn uint8(v &gt;&gt; (8 * n))\n}\n</code></pre>\n<p>A stride of 4 (a nibble) reads a value straddling two bytes; the final</p>\n<p><code>uint8(...)</code> truncation then hides the error for <code>n == 0</code>, where both strides</p>\n<p>agree.</p>\n<h2>Approach</h2>\n<p>1. Each byte is 8 bits, so byte <code>n</code> is shifted by <code>8*n</code>.</p>\n<p>2. The bug uses <code>4*n</code> (nibble stride).</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc ByteAt(v uint64, n uint) uint8 {\n\treturn uint8(v &gt;&gt; (8 * n))\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>ByteAt(..., 1)</code> needs <code>&gt;&gt; 8</code>; the bug's <code>&gt;&gt; 4</code> lands mid-byte. The 8-bit stride selects 0x77.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Stride = field width in bits. Byte \u2192 8, nibble \u2192 4, 16-bit word \u2192 16.</li>\n<li>The truncating conversion keeps only the low bits after the shift; it does not</li>\n</ul>\n<p>  save you from a wrong shift amount.</p>\n<ul>\n<li>Shift counts are unsigned and must be <code>&lt; 64</code> for a <code>uint64</code>.</li>\n</ul>",
    "starter": "// Package extract pulls one byte out of a 64-bit word. Planted wrong shift stride.\npackage extract\n\n// ByteAt returns byte n (0 = least significant) of v.\nfunc ByteAt(v uint64, n uint) uint8 {\n\t// CHANGE CODE BELOW THIS LINE\n\treturn uint8(v >> (4 * n))\n\t// CHANGE CODE ABOVE THIS LINE\n}\n",
    "debug": "",
    "file": "extract.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/01-variables-and-constants/staff/iota-reset": {
    "id": "01-language-basics/01-variables-and-constants/staff/iota-reset",
    "title": "iota Sequence Break",
    "level": "staff",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">iota Sequence Break</h1><div class=\"p-meta\"><span class=\"pill lv-staff\">staff</span></div></div><h2>Context</h2>\n<p><code>iota</code> still increments per ConstSpec line even when you assign an explicit</p>\n<p>value \u2014 but hardcoding <code>Redirect = 7</code> desyncs the <em>named</em> value from the counter,</p>\n<p>and the following bare lines resume from iota (3, 4), leaving a gap. The fix is</p>\n<p>to let <code>Redirect</code> follow the run.</p>\n<h2>Task</h2>\n<p>Fix the single line between the markers in codes.go so the classes</p>\n<p>are 0,1,2,3,4 in order.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Info, Success\nOutput: 0, 1\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  ClientError\nOutput: 3\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  ServerError\nOutput: 4\n</code></pre>\n<p>_Explanation:_ The explicit 7 broke the sequence.</p>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">iota per line</span><span class=\"tchip\">Implicit repetition</span><span class=\"tchip\">Sequence integrity</span></div></details>",
    "education": "<h1>iota and implicit repetition</h1>\n<h2>Intuition</h2>\n<p><code>iota</code> increments on every <code>ConstSpec</code> line, but a <strong>bare</strong> line does not repeat</p>\n<p><code>iota</code> \u2014 it repeats the <em>previous line's expression</em> verbatim. Put an explicit</p>\n<p>value in the middle and the bare lines below copy <strong>that constant</strong>, not the</p>\n<p>counter:</p>\n<pre class=\"md\"><code>\nconst (\n\tInfo    Class = iota // 0\n\tSuccess              // 1\n\tRedirect = 7         // explicit; iota is now 2 but unused here\n\tClientError          // repeats \"= 7\"  -&gt; 7, NOT 3\n\tServerError          // repeats \"= 7\"  -&gt; 7\n)\n</code></pre>\n<p>Keep every member on the <code>iota</code> expression to preserve the sequence:</p>\n<pre class=\"md\"><code>\nRedirect // bare -&gt; repeats \"iota\" -&gt; 2\n</code></pre>\n<h2>Approach</h2>\n<p>1. Assigning <code>Redirect = 7</code> overrides the running <code>iota</code> and the repeat below it.</p>\n<p>2. Use <code>Redirect Class = iota</code> to keep the sequence 2, 3, 4.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\ntype Class int\n\nconst (\n\tInfo    Class = iota // 0\n\tSuccess              // 1\n\tRedirect Class = iota\n\tClientError // should be 3\n\tServerError // should be 4\n)\n</code></pre>\n<h2>Walkthrough</h2>\n<p>The literal 7 makes ClientError repeat 7 too. <code>iota</code> restores Redirect=2, so ClientError=3, ServerError=4.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Implicit repetition copies the whole RHS expression, including a literal.</li>\n<li>If you must inject a specific value, re-establish the pattern on the next line</li>\n</ul>\n<p>  (<code>= iota</code>) or the run stays frozen.</p>\n<ul>\n<li>Reordering members silently renumbers them \u2014 enums are position-based.</li>\n</ul>",
    "starter": "// Package codes numbers HTTP-like classes with iota. A planted explicit value\n// breaks the running sequence.\npackage codes\n\n// Class is a response class index.\ntype Class int\n\nconst (\n\tInfo    Class = iota // 0\n\tSuccess              // 1\n\t// CHANGE CODE BELOW THIS LINE\n\tRedirect = 7\n\t// CHANGE CODE ABOVE THIS LINE\n\tClientError // should be 3\n\tServerError // should be 4\n)\n",
    "debug": "",
    "file": "codes.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/01-variables-and-constants/staff/kib-scale": {
    "id": "01-language-basics/01-variables-and-constants/staff/kib-scale",
    "title": "Kibi vs Kilo",
    "level": "staff",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Kibi vs Kilo</h1><div class=\"p-meta\"><span class=\"pill lv-staff\">staff</span></div></div><h2>Context</h2>\n<p>A kibibyte (KiB) is <code>1024</code>, not <code>1000</code>. The decimal factor silently</p>\n<p>under-reports every size by ~2.4% \u2014 the kind of unit bug that corrupts capacity</p>\n<p>math at scale.</p>\n<h2>Task</h2>\n<p>Fix the single line between the markers in scale.go so <code>KiB</code> is the</p>\n<p>binary factor.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  KiB\nOutput: 1024\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Bytes(2)\nOutput: 2048\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Bytes(1)\nOutput: 1024\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Binary vs decimal units</span><span class=\"tchip\">Named constant</span><span class=\"tchip\">Compile-time fold</span></div></details>",
    "education": "<h1>Kibibytes vs kilobytes</h1>\n<h2>Intuition</h2>\n<p>Binary and decimal prefixes are different constants:</p>\n<pre class=\"md\"><code>\nconst KiB = 1024 // 2^10, kibibyte  (binary)\nconst kB  = 1000 // 10^3, kilobyte  (decimal)\n</code></pre>\n<p>Memory, buffers, and page sizes are binary (1024). Disk marketing and SI units</p>\n<p>are decimal (1000). Using 1000 where 1024 is meant under-reports by ~2.4% and</p>\n<p>the gap compounds per level (MiB, GiB\u2026).</p>\n<h2>Approach</h2>\n<p>1. A kibibyte is 1024 bytes, not 1000.</p>\n<p>2. Set <code>KiB = 1024</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nconst KiB = 1024\n\nfunc Bytes(n int) int { return n * KiB }\n</code></pre>\n<h2>Walkthrough</h2>\n<p>The decimal 1000 makes <code>Bytes(2)</code> 2000; the binary 1024 gives 2048.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Prefer <code>1 &lt;&lt; 10</code>, <code>1 &lt;&lt; 20</code>\u2026 for binary units; it states the intent.</li>\n<li>Name the constant (<code>KiB</code>) so the factor lives in one place.</li>\n<li>Be explicit in APIs about which prefix you mean; the ambiguity of \"KB\" is the</li>\n</ul>\n<p>  root cause.</p>",
    "starter": "// Package scale converts kibibytes to bytes. Planted decimal factor.\npackage scale\n\n// KiB is the binary kilobyte factor.\n// CHANGE CODE BELOW THIS LINE\nconst KiB = 1000\n\n// CHANGE CODE ABOVE THIS LINE\n\n// Bytes returns n kibibytes in bytes.\nfunc Bytes(n int) int { return n * KiB }\n",
    "debug": "",
    "file": "scale.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/01-variables-and-constants/staff/minint-shift": {
    "id": "01-language-basics/01-variables-and-constants/staff/minint-shift",
    "title": "MinInt Signed Shift",
    "level": "staff",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">MinInt Signed Shift</h1><div class=\"p-meta\"><span class=\"pill lv-staff\">staff</span></div></div><h2>Context</h2>\n<p>Two's complement puts the most-negative value at <code>-1 &lt;&lt; 63</code> for a 64-bit word.</p>\n<p><code>-1 &lt;&lt; 62</code> is only a quarter of the way there. The asymmetry (<code>|MinInt| &gt; MaxInt</code>)</p>\n<p>is a memory-model fact the compiler encodes in the constant.</p>\n<h2>Task</h2>\n<p>Fix the single line between the markers in limits.go so <code>MinInt</code></p>\n<p>equals <code>math.MinInt64</code>.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  MinInt\nOutput: most negative 64-bit value\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  SymmetricTo()\nOutput: true\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  shift amount\nOutput: 63, not 62\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Signed shift</span><span class=\"tchip\">Two's complement asymmetry</span><span class=\"tchip\">Constant width</span></div></details>",
    "education": "<h1>Two's complement and MinInt</h1>\n<h2>Intuition</h2>\n<p>In two's complement the most-negative value has only the <strong>sign bit</strong> set. For a</p>\n<p>64-bit word that is <code>-1 &lt;&lt; 63</code>:</p>\n<pre class=\"md\"><code>\nconst MinInt = -1 &lt;&lt; 63 // -9223372036854775808\n</code></pre>\n<p><code>-1 &lt;&lt; 62</code> is only a quarter as far and is a <em>valid but wrong</em> value \u2014 it still</p>\n<p>fits the type, so nothing complains.</p>\n<h2>Approach</h2>\n<p>1. A 64-bit signed minimum is <code>-1 &lt;&lt; 63</code>.</p>\n<p>2. The bug shifts by 62, quartering the magnitude.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nconst MinInt = -1 &lt;&lt; 63\n\nfunc SymmetricTo() bool { return MinInt &lt; -(MinInt + 1) }\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>-1 &lt;&lt; 62</code> is not the true minimum; <code>-1 &lt;&lt; 63</code> sets the sign bit alone, the most negative value.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>-1 &lt;&lt; 63</code> sets the sign bit; the compiler represents it exactly as an int.</li>\n<li>The asymmetry means <code>MinInt = -MaxInt - 1</code>, never <code>-MaxInt</code>.</li>\n<li>Signed right-shift is arithmetic (sign-extending); do bit tricks in the</li>\n</ul>\n<p>  unsigned domain when you need logical shifts.</p>",
    "starter": "// Package limits builds MinInt from a signed shift. Planted off-by-one shift.\npackage limits\n\n// MinInt is meant to be the most negative int64-width value.\n// CHANGE CODE BELOW THIS LINE\nconst MinInt = -1 << 62\n\n// CHANGE CODE ABOVE THIS LINE\n\n// SymmetricTo reports whether -MinInt would overflow (it must: no positive\n// counterpart exists in two's complement).\nfunc SymmetricTo() bool { return MinInt < -(MinInt + 1) }\n",
    "debug": "",
    "file": "limits.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/01-variables-and-constants/staff/overflow-mul": {
    "id": "01-language-basics/01-variables-and-constants/staff/overflow-mul",
    "title": "Widen Before Multiply",
    "level": "staff",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Widen Before Multiply</h1><div class=\"p-meta\"><span class=\"pill lv-staff\">staff</span></div></div><h2>Context</h2>\n<p><code>int64(w <em> h)</code> computes <code>w</em>h</code> in <code>int32</code> first \u2014 it overflows and wraps, and the</p>\n<p>widening conversion happens <em>after</em> the damage. Widen the operands first.</p>\n<h2>Task</h2>\n<p>Fix the single line between the markers in area.go so large products</p>\n<p>do not overflow.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Area(100000, 100000)\nOutput: 10000000000\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Area(2, 3)\nOutput: 6\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Area(46341, 46341)\nOutput: no 32-bit overflow\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Operand type wins</span><span class=\"tchip\">Widen first</span><span class=\"tchip\">Silent overflow</span></div></details>",
    "education": "<h1>Widen before you multiply</h1>\n<h2>Intuition</h2>\n<p>An arithmetic expression is evaluated in the operands' type, and the result</p>\n<p>wraps <strong>before</strong> any surrounding conversion:</p>\n<pre class=\"md\"><code>\nvar w, h int32 = 100000, 100000\nint64(w * h) // w*h overflows int32 first, THEN widens the wrong value\nint64(w) * int64(h) // widen first, multiply in int64 -&gt; correct\n</code></pre>\n<p>The conversion <code>int64(...)</code> cannot recover bits already lost to the int32 wrap.</p>\n<h2>Approach</h2>\n<p>1. <code>w * h</code> in <code>int32</code> overflows before widening.</p>\n<p>2. Widen each factor first: <code>int64(w) * int64(h)</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Area(w, h int32) int64 {\n\treturn int64(w) * int64(h)\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>Area(100000, 100000)</code> overflows int32 in the bug; converting to int64 before multiplying keeps the full product.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Convert <strong>each operand</strong> before the operation, not the result.</li>\n<li>Choose a result type wide enough for the maximum product.</li>\n<li><code>go vet</code> will not catch this; it is valid code with wrong semantics.</li>\n</ul>",
    "starter": "// Package area multiplies two 32-bit dimensions. Planted narrow intermediate wraps.\npackage area\n\n// Area returns w*h without overflow for 32-bit inputs.\nfunc Area(w, h int32) int64 {\n\t// CHANGE CODE BELOW THIS LINE\n\treturn int64(w * h)\n\t// CHANGE CODE ABOVE THIS LINE\n}\n",
    "debug": "",
    "file": "area.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/01-variables-and-constants/staff/signbit-mask": {
    "id": "01-language-basics/01-variables-and-constants/staff/signbit-mask",
    "title": "Sign Bit Mask",
    "level": "staff",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Sign Bit Mask</h1><div class=\"p-meta\"><span class=\"pill lv-staff\">staff</span></div></div><h2>Context</h2>\n<p>For an 8-bit two's-complement value the sign is bit 7 \u2014 mask <code>0x80</code>. The code</p>\n<p>masks <code>0x40</code> (bit 6), so it misreads <code>0x40</code> and <code>0x80</code>.</p>\n<h2>Task</h2>\n<p>Fix the single line between the markers in sign.go so it tests the</p>\n<p>correct sign bit.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Negative(0x80)\nOutput: true\n</code></pre>\n<p>_Explanation:_ Bit 7 set.</p>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Negative(0x7F)\nOutput: false\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Negative(0xFF)\nOutput: true\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Sign bit position</span><span class=\"tchip\">Bit mask</span><span class=\"tchip\">Two's complement</span></div></details>",
    "education": "<h1>The sign bit</h1>\n<h2>Intuition</h2>\n<p>In an N-bit two's-complement value the <strong>top</strong> bit (bit N-1) is the sign: set</p>\n<p>means negative. For 8 bits that is bit 7, mask <code>0x80</code>:</p>\n<pre class=\"md\"><code>\nfunc Negative(b uint8) bool { return b&amp;0x80 != 0 }\n</code></pre>\n<p>Masking <code>0x40</code> (bit 6) tests the wrong bit, so it misreads exactly the values</p>\n<p>where bit 7 and bit 6 differ \u2014 including <code>0x80</code> (-128) and <code>0x40</code> (64).</p>\n<h2>Approach</h2>\n<p>1. The sign bit of a byte is bit 7 \u2192 mask <code>0x80</code>.</p>\n<p>2. The bug masks <code>0x40</code> (bit 6).</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Negative(b uint8) bool {\n\treturn b&amp;0x80 != 0\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>Negative(0x80)</code> needs bit 7; <code>&amp; 0x40</code> misses it. <code>&amp; 0x80</code> correctly reports the sign bit.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Sign bit position is width-dependent: <code>0x80</code> for 8-bit, <code>0x8000</code> for 16-bit,</li>\n</ul>\n<p>  <code>1&lt;&lt;63</code> for 64-bit.</p>\n<ul>\n<li><code>b &amp; mask != 0</code> tests presence; do not compare <code>== mask</code> unless only that bit</li>\n</ul>\n<p>  may be set.</p>\n<ul>\n<li>Converting <code>uint8</code> to <code>int8</code> also reveals the sign, if you prefer that route.</li>\n</ul>",
    "starter": "// Package sign inspects the sign bit of a byte. Planted wrong mask.\npackage sign\n\n// Negative reports whether the int8 value stored in b (as its raw bits) is\n// negative \u2014 i.e. whether the sign bit (bit 7) is set.\nfunc Negative(b uint8) bool {\n\t// CHANGE CODE BELOW THIS LINE\n\treturn b&0x40 != 0\n\t// CHANGE CODE ABOVE THIS LINE\n}\n",
    "debug": "",
    "file": "sign.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/01-variables-and-constants/staff/untyped-ratio": {
    "id": "01-language-basics/01-variables-and-constants/staff/untyped-ratio",
    "title": "Untyped Constant Division",
    "level": "staff",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Untyped Constant Division</h1><div class=\"p-meta\"><span class=\"pill lv-staff\">staff</span></div></div><h2>Context</h2>\n<p><code>233 / 144</code> with two integer operands is integer division \u2014 the untyped constant</p>\n<p>becomes <code>1</code>, losing the fraction <em>before</em> it ever reaches <code>float64</code>. Make at</p>\n<p>least one operand a floating literal so the constant division stays exact.</p>\n<h2>Task</h2>\n<p>Fix the single line between the markers in ratio.go so <code>Value()</code></p>\n<p>returns \u22481.618.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Value()\nOutput: ~1.618\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  233/144 as int\nOutput: 1 (truncated)\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  233.0/144\nOutput: ~1.618\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Integer constant division</span><span class=\"tchip\">Untyped float constant</span><span class=\"tchip\">Type at use</span></div></details>",
    "education": "<h1>Untyped constant division stays exact</h1>\n<h2>Intuition</h2>\n<p>Constant division follows the operands' types <strong>at compile time</strong>. Two integer</p>\n<p>operands give integer division \u2014 the fraction is gone before the value is ever a</p>\n<p>float:</p>\n<pre class=\"md\"><code>\nconst R = 233 / 144   // 1   (integer division, then used as float64 -&gt; 1.0)\nconst R = 233.0 / 144.0 // 1.618...  (untyped float division, full precision)\n</code></pre>\n<p>Make at least one operand a floating literal and the whole constant expression</p>\n<p>is evaluated in untyped floating point, keeping precision until it is assigned.</p>\n<h2>Approach</h2>\n<p>1. <code>233 / 144</code> with integer operands truncates to 1.</p>\n<p>2. Make one operand a float: <code>233.0 / 144</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nconst GoldenApprox = 233.0 / 144\n\nfunc Value() float64 { return GoldenApprox }\n</code></pre>\n<h2>Walkthrough</h2>\n<p>Integer division loses the fraction; <code>233.0 / 144</code> keeps full precision \u2192 ~1.618.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>One float operand promotes the whole expression: <code>233.0 / 144</code> also works.</li>\n<li>Untyped constants keep arbitrary precision; the loss here is from integer</li>\n</ul>\n<p>  <em>semantics</em>, not float rounding.</p>\n<ul>\n<li>The same rule applies to <code>/</code> in <code>const</code> and in runtime expressions alike.</li>\n</ul>",
    "starter": "// Package ratio holds a compile-time constant ratio. Planted typed truncation.\npackage ratio\n\n// GoldenApprox approximates the golden ratio as an untyped constant so it keeps\n// full precision until used. A planted bug forces integer truncation.\n// CHANGE CODE BELOW THIS LINE\nconst GoldenApprox = 233 / 144\n\n// CHANGE CODE ABOVE THIS LINE\n\n// Value returns the ratio as a float64.\nfunc Value() float64 { return GoldenApprox }\n",
    "debug": "",
    "file": "ratio.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/02-data-types/junior/almostequal": {
    "id": "01-language-basics/02-data-types/junior/almostequal",
    "title": "Float Almost-Equal",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Float Almost-Equal</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>A pricing test kept failing at random: <code>total == 0.3</code> was false even though the</p>\n<p>math looked right. Floating-point values are stored in binary and can't</p>\n<p>represent most decimals exactly, so <code>0.1 + 0.2</code> is <code>0.30000000000000004</code>.</p>\n<p>Comparisons need a tolerance \u2014 and the absolute value comes from the standard</p>\n<p>library <code>math</code> package.</p>\n<h2>Task</h2>\n<p>Implement <code>AlmostEqual</code> in almostequal.go so it reports</p>\n<p>whether <code>a</code> and <code>b</code> differ by <strong>less than <code>1e-9</code></strong>. Use <strong><code>math.Abs</code></strong> from the</p>\n<p><code>math</code> package for the absolute difference \u2014 add <code>import \"math\"</code> yourself. Do</p>\n<p>not compare floats with <code>==</code>.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  AlmostEqual(0.1+0.2, 0.3)\nOutput: true\n</code></pre>\n<p>_Explanation:_ 0.1+0.2 is 0.30000000000000004; the difference is ~5.5e-17 &lt; 1e-9, so almost equal even though == is false.</p>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  AlmostEqual(1.0, 1.0001)\nOutput: false\n</code></pre>\n<p>_Explanation:_ Difference 1e-4 is far above the 1e-9 tolerance.</p>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  AlmostEqual(-2.5, -2.5)\nOutput: true\n</code></pre>\n<p>_Explanation:_ Identical values, difference 0.</p>\n<p><strong>Example 4:</strong></p>\n<pre class=\"md\"><code>\nInput:  AlmostEqual(0, 1e-12)\nOutput: true\n</code></pre>\n<p>_Explanation:_ 1e-12 &lt; 1e-9, inside tolerance.</p>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">4</span></summary><div class=\"tchips\"><span class=\"tchip\">Floats are inexact</span><span class=\"tchip\">Never <code>==</code> on floats</span><span class=\"tchip\">Importing a package</span><span class=\"tchip\"><code>math.Abs</code></span></div></details>",
    "education": "<h1>Comparing floats with a tolerance</h1>\n<h2>Intuition</h2>\n<p>Binary floats can't represent most decimals exactly, so <code>==</code> is the wrong tool.</p>\n<p>Compare within a small tolerance instead:</p>\n<pre class=\"md\"><code>\nmath.Abs(a-b) &lt;= eps\n</code></pre>\n<h2>Approach</h2>\n<p>1. Import the math package.</p>\n<p>2. Compute the absolute difference math.Abs(a-b).</p>\n<p>3. Return true when that difference is strictly less than the epsilon 1e-9.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nimport \"math\"\n\nfunc AlmostEqual(a, b float64) bool {\n\treturn math.Abs(a-b) &lt; 1e-9\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>AlmostEqual(0.1+0.2, 0.3): a-b = 4.4e-17, math.Abs = 4.4e-17, and 4.4e-17 &lt; 1e-9 -&gt; true.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>If <code>a</code> or <code>b</code> is NaN, <code>a-b</code> is NaN and the comparison is false \u2014 NaN is never</li>\n</ul>\n<p>  \"almost equal\".</p>\n<ul>\n<li>Absolute tolerance suits values near the same magnitude; relative tolerance</li>\n</ul>\n<p>  scales across magnitudes.</p>\n<ul>\n<li>The right <code>eps</code> depends on the operations performed, not a universal constant.</li>\n</ul>",
    "starter": "// Package almostequal \u2014 Gopher Workplace challenge.\npackage almostequal\n\n// AlmostEqual reports whether a and b are within a small tolerance of each\n// other. Floating-point arithmetic is inexact \u2014 0.1 + 0.2 is not exactly 0.3 \u2014\n// so floats must be compared with a tolerance (epsilon), never with ==.\n//\n// Two values count as equal when their absolute difference is < 1e-9. Use the\n// standard library's math.Abs for the absolute value (add `import \"math\"`).\n//\n// Examples:\n//\n//\tAlmostEqual(0.1+0.2, 0.3) => true   // == would be false here\n//\tAlmostEqual(1.0, 1.0)     => true\n//\tAlmostEqual(1.0, 1.0001)  => false\n//\tAlmostEqual(-2.5, -2.5)   => true\nfunc AlmostEqual(a, b float64) bool {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "almostequal.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/02-data-types/junior/average": {
    "id": "01-language-basics/02-data-types/junior/average",
    "title": "Average",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Average</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>A metrics endpoint reports the mean of a batch of integer samples. The naive</p>\n<p>version divided the integer sum by the integer count and returned <code>2</code> for the</p>\n<p>average of <code>1, 2, 4</code> \u2014 the <code>.333\u2026</code> vanished. The fix is about <em>when</em> you cross</p>\n<p>from integer to floating-point.</p>\n<h2>Task</h2>\n<p>Implement <code>Average</code> in average.go so that it returns the arithmetic</p>\n<p>mean of <code>nums</code> as a <code>float64</code>:</p>\n<p>1. Sum the values and divide by the count.</p>\n<p>2. Convert to <code>float64</code> <strong>before</strong> dividing so the result keeps its fractional</p>\n<p>   part (integer division truncates).</p>\n<p>3. An empty or nil slice returns <code>0</code>.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Average([]int{1, 2})\nOutput: 1.5\n</code></pre>\n<p>_Explanation:_ (1+2)/2 in float = 1.5.</p>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Average([]int{1, 2, 4})\nOutput: 2.3333333333333335\n</code></pre>\n<p>_Explanation:_ 7/3 kept in floating point.</p>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Average(nil)\nOutput: 0\n</code></pre>\n<p>_Explanation:_ Empty/nil guard returns 0, avoiding divide-by-zero.</p>\n<p><strong>Example 4:</strong></p>\n<pre class=\"md\"><code>\nInput:  Average([]int{10, 20, 30, 5})\nOutput: 16.25\n</code></pre>\n<p>_Explanation:_ 65/4 = 16.25.</p>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Explicit conversion</span><span class=\"tchip\">Integer division truncates</span><span class=\"tchip\">Conversion order</span></div></details>",
    "education": "<h1>Integer vs float average</h1>\n<h2>Intuition</h2>\n<p>The mean of integers is a <em>ratio</em>, so the division must happen in floating point.</p>\n<p>Summing in <code>int</code> and dividing by the count as ints truncates the fraction:</p>\n<pre class=\"md\"><code>\nfloat64(sum) / float64(n) // 7/2 -&gt; 3.5, not 3\n</code></pre>\n<h2>Approach</h2>\n<p>1. If the slice is empty or nil, return 0.</p>\n<p>2. Sum the ints into an int accumulator.</p>\n<p>3. Convert both sum and len to float64 before dividing so the fraction survives.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Average(nums []int) float64 {\n\tif len(nums) == 0 {\n\t\treturn 0\n\t}\n\tsum := 0\n\tfor _, n := range nums {\n\t\tsum += n\n\t}\n\treturn float64(sum) / float64(len(nums))\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>Average([]int{1,2,4}): sum=7, len=3, float64(7)/float64(3)=2.3333333333333335.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Convert to <code>float64</code> before dividing, not after.</li>\n<li>Guard <code>n == 0</code> to avoid a divide-by-zero panic.</li>\n<li>Summing many ints can overflow; widen the accumulator if needed.</li>\n</ul>",
    "starter": "// Package average \u2014 Gopher Workplace challenge.\npackage average\n\n// Average returns the arithmetic mean of nums as a float64. The sum and count\n// are integers, so they must be converted to float64 before dividing \u2014 plain\n// integer division would truncate the fractional part.\n// An empty or nil slice returns 0.\n//\n// Examples:\n//\n//\tAverage([]int{1, 2})    => 1.5\n//\tAverage([]int{1, 2, 4}) => 2.3333333333333335\n//\tAverage(nil)            => 0\nfunc Average(nums []int) float64 {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "average.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/02-data-types/junior/bytecat": {
    "id": "01-language-basics/02-data-types/junior/bytecat",
    "title": "Strings and Bytes",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Strings and Bytes</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>A string and a <code>[]byte</code> are convertible both ways with a plain type conversion.</p>\n<p>Each conversion copies the data.</p>\n<h2>Task</h2>\n<p>Implement <code>FromBytes(b)</code> (bytes \u2192 string) and <code>ToBytes(s)</code> (string \u2192 bytes).</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  FromBytes([]byte{'G','o'})\nOutput: \"Go\"\n</code></pre>\n<p>_Explanation:_ string([]byte) copies the bytes into a string.</p>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  FromBytes(nil)\nOutput: \"\"\n</code></pre>\n<p>_Explanation:_ Converting a nil slice yields the empty string.</p>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  ToBytes(\"Go\")\nOutput: [71 111]\n</code></pre>\n<p>_Explanation:_ []byte(string) copies the string's bytes.</p>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">string([]byte)</span><span class=\"tchip\">[]byte(string)</span><span class=\"tchip\">Immutability</span></div></details>",
    "education": "<h1>String \u2194 []byte conversion</h1>\n<h2>Intuition</h2>\n<p>Strings and byte slices convert with an ordinary conversion:</p>\n<pre class=\"md\"><code>\n[]byte(\"Go\")          // []byte{71, 111}\nstring([]byte{71, 111}) // \"Go\"\n</code></pre>\n<p>A string is an immutable sequence of bytes; a <code>[]byte</code> is a mutable one. Each</p>\n<p>conversion <strong>copies</strong>, so mutating the slice never changes the source string.</p>\n<h2>Approach</h2>\n<p>1. FromBytes: return string(b) - a plain conversion that copies.</p>\n<p>2. ToBytes: return []byte(s) - a plain conversion that copies.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc FromBytes(b []byte) string {\n\treturn string(b)\n}\n\nfunc ToBytes(s string) []byte {\n\treturn []byte(s)\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>ToBytes(\"Go\"): 'G'=71, 'o'=111, so []byte(\"Go\") = [71 111].</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Each conversion allocates and copies (O(n)); avoid it in hot loops.</li>\n<li>Converting a <code>[]byte</code> with invalid UTF-8 to a string keeps the bytes as-is;</li>\n</ul>\n<p>  ranging over it later yields <code>utf8.RuneError</code> for bad sequences.</p>\n<ul>\n<li><code>string(65)</code> is <code>\"A\"</code> (rune conversion), <em>not</em> <code>\"65\"</code> \u2014 a common surprise;</li>\n</ul>\n<p>  <code>string([]byte{65})</code> is also <code>\"A\"</code>.</p>",
    "starter": "// Package bytecat builds a string from bytes.\npackage bytecat\n\n// FromBytes returns the string formed by the given bytes, in order.\n//\n// TODO(candidate): convert the []byte to a string.\nfunc FromBytes(b []byte) string {\n\tpanic(\"not implemented\")\n}\n\n// ToBytes returns the bytes of s.\n//\n// TODO(candidate): convert the string to a []byte.\nfunc ToBytes(s string) []byte {\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "bytecat.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/02-data-types/junior/c2f": {
    "id": "01-language-basics/02-data-types/junior/c2f",
    "title": "Celsius to Fahrenheit",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Celsius to Fahrenheit</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p><code>c*9/5</code> in integers truncates. Converting to <code>float64</code> first keeps the fraction,</p>\n<p>so <code>37\u00b0C</code> gives <code>98.6</code>, not <code>98</code>.</p>\n<h2>Task</h2>\n<p>Implement <code>ToF(c)</code> returning <code>c*9/5 + 32</code> as a <code>float64</code>.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  ToF(0)\nOutput: 32\n</code></pre>\n<p>_Explanation:_ Freezing point.</p>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  ToF(100)\nOutput: 212\n</code></pre>\n<p>_Explanation:_ Boiling point.</p>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  ToF(37)\nOutput: 98.6\n</code></pre>\n<p>_Explanation:_ float64(37)*9/5+32 keeps the .6; integer math would give 98.</p>\n<p><strong>Example 4:</strong></p>\n<pre class=\"md\"><code>\nInput:  ToF(-40)\nOutput: -40\n</code></pre>\n<p>_Explanation:_ The scales cross at -40.</p>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Type conversion</span><span class=\"tchip\">Integer vs float division</span><span class=\"tchip\">Mixed arithmetic</span></div></details>",
    "education": "<h1>Numeric type conversion</h1>\n<h2>Intuition</h2>\n<p>Go never converts numeric types implicitly. To mix an <code>int</code> with float math you</p>\n<p>convert explicitly, and <em>when</em> you convert matters:</p>\n<pre class=\"md\"><code>\nfloat64(c)*9/5 + 32 // float division, keeps 1.8\nc*9/5              // int division: 9/5 == 1, fraction lost\n</code></pre>\n<p>Converting <code>c</code> to <code>float64</code> first makes the whole expression floating point.</p>\n<h2>Approach</h2>\n<p>1. Convert c to float64 first so the division is floating point.</p>\n<p>2. Apply float64(c)*9/5 + 32.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc ToF(c int) float64 {\n\treturn float64(c)*9/5 + 32\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>ToF(37): float64(37)*9 = 333, /5 = 66.6, +32 = 98.6.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>float64(c) * 9 / 5</code>: the <code>9</code> and <code>5</code> become untyped constants that adopt</li>\n</ul>\n<p>  float64 here, so the division is floating.</p>\n<ul>\n<li>Converting the <em>result</em> (<code>float64(c*9/5)</code>) is too late \u2014 truncation already</li>\n</ul>\n<p>  happened.</p>\n<ul>\n<li>Narrowing conversions (<code>int(f)</code>) truncate toward zero and can overflow.</li>\n</ul>",
    "starter": "// Package c2f converts Celsius to Fahrenheit with correct numeric types.\npackage c2f\n\n// ToF converts an integer Celsius temperature to Fahrenheit as a float64,\n// using the formula c*9/5 + 32 without losing the fraction.\n//\n// TODO(candidate): convert to float64 before the division.\nfunc ToF(c int) float64 {\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "c2f.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/02-data-types/junior/checksum": {
    "id": "01-language-basics/02-data-types/junior/checksum",
    "title": "Checksum",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Checksum</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>A wire protocol appends a one-byte checksum to each frame: the sum of the payload</p>\n<p>bytes, kept in a single <code>uint8</code>. Because the field is exactly 8 bits wide, the</p>\n<p>sum naturally wraps around past 255 \u2014 that overflow is part of the spec, not a</p>\n<p>bug.</p>\n<h2>Task</h2>\n<p>Implement <code>Checksum</code> in checksum.go so that it returns the</p>\n<p>modulo-256 sum of the bytes in <code>data</code>, accumulated in a <code>uint8</code>:</p>\n<p>1. Add every byte together; the running total is a <code>uint8</code>.</p>\n<p>2. When the total exceeds 255 it wraps around (<code>255 + 1 == 0</code>).</p>\n<p>3. A nil or empty slice returns <code>0</code>.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Checksum([]byte{1,2,3})\nOutput: 6\n</code></pre>\n<p>_Explanation:_ 1+2+3, no wrap.</p>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Checksum([]byte{255,1})\nOutput: 0\n</code></pre>\n<p>_Explanation:_ 255+1 wraps a uint8 to 0.</p>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Checksum([]byte{200,100})\nOutput: 44\n</code></pre>\n<p>_Explanation:_ 300 mod 256 = 44.</p>\n<p><strong>Example 4:</strong></p>\n<pre class=\"md\"><code>\nInput:  Checksum([]byte{255,255,255,255})\nOutput: 252\n</code></pre>\n<p>_Explanation:_ 1020 mod 256 = 252.</p>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Fixed-width integers</span><span class=\"tchip\"><code>byte</code> is <code>uint8</code></span><span class=\"tchip\">Zero value</span></div></details>",
    "education": "<h1>Sums and modular reduction</h1>\n<h2>Intuition</h2>\n<p>A simple checksum accumulates byte (or digit) values and reduces the total with</p>\n<p><code>%</code> to a fixed range:</p>\n<pre class=\"md\"><code>\nsum := 0\nfor _, b := range data { sum += int(b) }\nreturn sum % 256\n</code></pre>\n<h2>Approach</h2>\n<p>1. Declare a uint8 accumulator (zero value 0).</p>\n<p>2. Add each byte to it; uint8 arithmetic wraps modulo 256 automatically.</p>\n<p>3. Return the accumulator.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Checksum(data []byte) uint8 {\n\tvar sum uint8\n\tfor _, b := range data {\n\t\tsum += b\n\t}\n\treturn sum\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>Checksum([]byte{200,100}): 0+200=200, 200+100=300 which wraps in uint8 to 44.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>The modulus defines the checksum width; pick it deliberately.</li>\n<li>Byte order and starting value matter for cross-implementation agreement.</li>\n<li>A plain sum catches single-byte errors but not reordering (that needs a</li>\n</ul>\n<p>  position-weighted scheme like Luhn or Fletcher).</p>",
    "starter": "// Package checksum \u2014 Gopher Workplace challenge.\npackage checksum\n\n// Checksum returns the modulo-256 sum of every byte in data: the bytes added\n// together as uint8, letting the fixed 8-bit width wrap around on overflow.\n// A nil or empty slice checksums to 0.\n//\n// Examples:\n//\n//\tChecksum([]byte{1, 2, 3})      => 6\n//\tChecksum([]byte{255, 1})       => 0   // 256 wraps to 0\n//\tChecksum([]byte{200, 100})     => 44  // 300 mod 256\nfunc Checksum(data []byte) uint8 {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "checksum.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/02-data-types/junior/clockmod": {
    "id": "01-language-basics/02-data-types/junior/clockmod",
    "title": "24-Hour Clock",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">24-Hour Clock</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>Adding hours wraps around midnight. Because Go's <code>%</code> can be negative, wrapping</p>\n<p>backward needs an extra step to stay in <code>0..23</code>.</p>\n<h2>Task</h2>\n<p>Implement <code>AddHours(h, add)</code> returning the resulting hour in <code>0..23</code>. <code>add</code> may</p>\n<p>be negative or larger than 24.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  AddHours(10, 5)\nOutput: 15\n</code></pre>\n<p>_Explanation:_ No wrap needed.</p>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  AddHours(23, 1)\nOutput: 0\n</code></pre>\n<p>_Explanation:_ Past midnight wraps to 0.</p>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  AddHours(0, -1)\nOutput: 23\n</code></pre>\n<p>_Explanation:_ Going back an hour from 0 wraps to 23.</p>\n<p><strong>Example 4:</strong></p>\n<pre class=\"md\"><code>\nInput:  AddHours(6, -30)\nOutput: 0\n</code></pre>\n<p>_Explanation:_ 6-30=-24; normalized to 0.</p>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Modulo wrap</span><span class=\"tchip\">Negative remainder</span><span class=\"tchip\">Normalize</span></div></details>",
    "education": "<h1>Modular wraparound</h1>\n<h2>Intuition</h2>\n<p>Clock arithmetic is arithmetic modulo 24. Adding hours and taking <code>% 24</code> folds</p>\n<p>any value back into range \u2014 almost. Because Go's <code>%</code> follows the dividend's</p>\n<p>sign, a negative sum yields a negative remainder, so you normalize:</p>\n<pre class=\"md\"><code>\n((h+add)%24 + 24) % 24 // always 0..23\n</code></pre>\n<p>The <code>+24</code> pushes a negative remainder positive; the final <code>%24</code> trims a value</p>\n<p>that was already in range.</p>\n<h2>Approach</h2>\n<p>1. Compute h+add.</p>\n<p>2. Take % 24 (may be negative in Go).</p>\n<p>3. Add 24 and take % 24 again to force a non-negative 0..23 result.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc AddHours(h, add int) int {\n\treturn ((h+add)%24 + 24) % 24\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>AddHours(0,-1): (0-1)%24 = -1, (-1+24)%24 = 23.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>-1 % 24 == -1</code>, not 23 \u2014 the source of the bug.</li>\n<li>The double-mod is needed only when the input can be negative.</li>\n<li>For powers of two you can mask (<code>x &amp; 23</code> only if 24 were a power of two \u2014 it</li>\n</ul>\n<p>  is not, so use <code>%</code>).</p>",
    "starter": "// Package clockmod does 24-hour clock arithmetic.\npackage clockmod\n\n// AddHours returns the hour of day after adding add hours to h (0..23).\n// add may be negative or large; the result is always in 0..23.\n//\n// TODO(candidate): implement with modulo that never returns negative.\nfunc AddHours(h, add int) int {\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "clockmod.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/02-data-types/junior/complexabs": {
    "id": "01-language-basics/02-data-types/junior/complexabs",
    "title": "Complex Magnitude",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Complex Magnitude</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>Go has built-in complex numbers. <code>real()</code> and <code>imag()</code> extract the parts;</p>\n<p><code>math.Hypot</code> gives the magnitude without overflow.</p>\n<h2>Task</h2>\n<p>Implement <code>Magnitude(c)</code> returning <code>sqrt(re^2 + im^2)</code>.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Magnitude(complex(3,4))\nOutput: 5\n</code></pre>\n<p>_Explanation:_ sqrt(9+16)=5.</p>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Magnitude(complex(0,0))\nOutput: 0\n</code></pre>\n<p>_Explanation:_ Zero vector.</p>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Magnitude(complex(-3,-4))\nOutput: 5\n</code></pre>\n<p>_Explanation:_ Squares drop the sign.</p>\n<p><strong>Example 4:</strong></p>\n<pre class=\"md\"><code>\nInput:  Magnitude(complex(1,0))\nOutput: 1\n</code></pre>\n<p>_Explanation:_ Pure real.</p>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">complex128</span><span class=\"tchip\">real / imag</span><span class=\"tchip\">math.Hypot</span></div></details>",
    "education": "<h1>Complex numbers</h1>\n<h2>Intuition</h2>\n<p>Go has <code>complex64</code> and <code>complex128</code> as built-in types. Construct with the</p>\n<p>built-in <code>complex(re, im)</code>; pull the parts with <code>real(c)</code> and <code>imag(c)</code>, each a</p>\n<p>float. The magnitude is <code>sqrt(re\u00b2 + im\u00b2)</code>, best computed with <code>math.Hypot</code>:</p>\n<pre class=\"md\"><code>\nmath.Hypot(real(c), imag(c))\n</code></pre>\n<h2>Approach</h2>\n<p>1. Extract parts with the built-ins real(c) and imag(c).</p>\n<p>2. Return math.Hypot(re, im), which computes sqrt(re^2+im^2) without overflow.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nimport \"math\"\n\nfunc Magnitude(c complex128) float64 {\n\treturn math.Hypot(real(c), imag(c))\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>Magnitude(complex(3,4)): real=3, imag=4, math.Hypot(3,4)=5.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>real</code>, <code>imag</code>, and <code>complex</code> are <strong>built-ins</strong>, not in the <code>math</code> package.</li>\n<li><code>complex128</code> holds two <code>float64</code>s; <code>complex64</code> two <code>float32</code>s.</li>\n<li>The <code>math/cmplx</code> package has richer operations (<code>cmplx.Abs</code>, <code>cmplx.Phase</code>).</li>\n</ul>",
    "starter": "// Package complexabs computes the magnitude of a complex number.\npackage complexabs\n\n// Magnitude returns the absolute value (modulus) of c: sqrt(re^2 + im^2).\n//\n// TODO(candidate): implement using real(), imag(), and math.Hypot.\nfunc Magnitude(c complex128) float64 {\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "complexabs.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/02-data-types/junior/escapes": {
    "id": "01-language-basics/02-data-types/junior/escapes",
    "title": "Escape Sequences",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Escape Sequences</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>TSV output uses a literal tab between fields and a newline at the end. Both are</p>\n<p>escape sequences inside an interpreted string.</p>\n<h2>Task</h2>\n<p>Implement <code>Row(name, value)</code> returning <code>name</code>, a tab, <code>value</code>, and a newline.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Row(\"id\", \"42\")\nOutput: \"id\\t42\\n\"\n</code></pre>\n<p>_Explanation:_ name, a tab byte, value, then a newline byte.</p>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Row(\"a\", \"b\")\nOutput: \"a\\tb\\n\"\n</code></pre>\n<p>_Explanation:_ Same TSV shape.</p>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Row(\"\", \"\")\nOutput: \"\\t\\n\"\n</code></pre>\n<p>_Explanation:_ Empty fields still get the tab and newline.</p>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Interpreted literal</span><span class=\"tchip\"><code>\\t</code> and <code>\\n</code></span><span class=\"tchip\">Concatenation</span></div></details>",
    "education": "<h1>Interpreted string literals</h1>\n<h2>Intuition</h2>\n<p>Double-quoted strings are <strong>interpreted</strong>: backslash escape sequences become</p>\n<p>single characters. <code>\\t</code> is one tab byte, <code>\\n</code> one newline byte, <code>\\\\</code> one</p>\n<p>backslash, <code>\\\"</code> a quote:</p>\n<pre class=\"md\"><code>\n\"a\\tb\\n\" // 4 bytes: 'a', tab, 'b', newline\n</code></pre>\n<h2>Approach</h2>\n<p>1. Concatenate name + \"\\t\" + value + \"\\n\".</p>\n<p>2. \\t and \\n are single bytes inside an interpreted (double-quoted) literal.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Row(name, value string) string {\n\treturn name + \"\\t\" + value + \"\\n\"\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>Row(\"id\",\"42\"): \"id\" + tab + \"42\" + newline = \"id\\t42\\n\".</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>len(\"\\t\")</code> is 1, not 2 \u2014 the escape is a single byte.</li>\n<li>Unknown escapes are a compile error (<code>\\q</code> is invalid).</li>\n<li><code>\u00e9</code> / <code>\\U0001F600</code> embed Unicode code points; <code>\\x41</code> embeds a raw byte.</li>\n<li>For text full of backslashes, a raw `<code> </code>...<code> </code>` literal is clearer.</li>\n</ul>",
    "starter": "// Package escapes builds a tab-separated, newline-terminated row.\npackage escapes\n\n// Row returns name, a tab, value, and a trailing newline, using an interpreted\n// string literal with escape sequences.\n//\n// TODO(candidate): implement using \"\\t\" and \"\\n\".\nfunc Row(name, value string) string {\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "escapes.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/02-data-types/junior/evenodd": {
    "id": "01-language-basics/02-data-types/junior/evenodd",
    "title": "Integer Parity",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Integer Parity</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>Parity is the classic use of the remainder operator <code>%</code> \u2014 but negative numbers</p>\n<p>need care, because <code>-7 % 2</code> is <code>-1</code> in Go, not <code>1</code>.</p>\n<h2>Task</h2>\n<p>Implement <code>Parity(n)</code> returning <code>\"even\"</code> or <code>\"odd\"</code>, correct for negatives.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Parity(0)\nOutput: \"even\"\n</code></pre>\n<p>_Explanation:_ 0%2==0.</p>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Parity(3)\nOutput: \"odd\"\n</code></pre>\n<p>_Explanation:_ 3%2==1.</p>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Parity(-4)\nOutput: \"even\"\n</code></pre>\n<p>_Explanation:_ -4%2==0 holds for negatives.</p>\n<p><strong>Example 4:</strong></p>\n<pre class=\"md\"><code>\nInput:  Parity(-7)\nOutput: \"odd\"\n</code></pre>\n<p>_Explanation:_ -7%2==-1 which is not 0, so odd. Testing ==1 would wrongly fail here.</p>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Remainder <code>%</code></span><span class=\"tchip\">Sign of <code>%</code></span><span class=\"tchip\">Even test</span></div></details>",
    "education": "<h1>Remainder and its sign</h1>\n<h2>Intuition</h2>\n<p><code>%</code> gives the remainder of integer division. For parity, <code>n % 2</code> is 0 when <code>n</code></p>\n<p>is even. The reliable test is <code>n%2 == 0</code>.</p>\n<h2>Approach</h2>\n<p>1. Test n%2 == 0 for even (works for negatives).</p>\n<p>2. Otherwise return \"odd\".</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Parity(n int) string {\n\tif n%2 == 0 {\n\t\treturn \"even\"\n\t}\n\treturn \"odd\"\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>Parity(-7): -7%2 = -1, not 0, so return \"odd\".</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>a % b</code> sign follows <code>a</code> (the dividend), unlike Python where it follows <code>b</code>.</li>\n<li><code>%</code> is only for integers; use <code>math.Mod</code> for floats.</li>\n<li>Division by zero panics; <code>%</code> by zero panics too.</li>\n</ul>",
    "starter": "// Package evenodd classifies integers by parity.\npackage evenodd\n\n// Parity returns \"even\" or \"odd\" for n, correct for negatives too.\n//\n// TODO(candidate): implement using the remainder operator.\nfunc Parity(n int) string {\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "evenodd.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/02-data-types/junior/firstrune": {
    "id": "01-language-basics/02-data-types/junior/firstrune",
    "title": "First Rune",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">First Rune</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>Indexing a string with <code>s[0]</code> gives a <strong>byte</strong>, which splits multi-byte</p>\n<p>characters. Ranging over a string yields <strong>runes</strong> (Unicode code points).</p>\n<h2>Task</h2>\n<p>Implement <code>First(s)</code> returning the first rune. Empty string returns rune <code>0</code>.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  First(\"hello\")\nOutput: 'h'\n</code></pre>\n<p>_Explanation:_ First rune.</p>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  First(\"etage\" with accent)\nOutput: 'e-acute'\n</code></pre>\n<p>_Explanation:_ Ranging decodes the 2-byte accented char as one rune.</p>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  First(\"day\" CJK)\nOutput: first CJK rune\n</code></pre>\n<p>_Explanation:_ 3-byte rune returned whole.</p>\n<p><strong>Example 4:</strong></p>\n<pre class=\"md\"><code>\nInput:  First(\"\")\nOutput: 0\n</code></pre>\n<p>_Explanation:_ Empty string, loop never runs, return rune 0.</p>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Bytes vs runes</span><span class=\"tchip\">for range on string</span><span class=\"tchip\">Early return</span></div></details>",
    "education": "<h1>Bytes vs runes</h1>\n<h2>Intuition</h2>\n<p>A Go string is a read-only sequence of <strong>bytes</strong> holding UTF-8. <code>s[i]</code> indexes a</p>\n<p>byte. A <strong>rune</strong> is a Unicode code point (an <code>int32</code>), and one rune may occupy</p>\n<p>1\u20134 bytes. Ranging over a string decodes UTF-8 and yields runes:</p>\n<pre class=\"md\"><code>\nfor _, r := range s { return r } // first rune, whole character\n</code></pre>\n<h2>Approach</h2>\n<p>1. Range over the string, which decodes UTF-8 into runes.</p>\n<p>2. Return the rune on the first iteration.</p>\n<p>3. If the loop never runs (empty string), return 0.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc First(s string) rune {\n\tfor _, r := range s {\n\t\treturn r\n\t}\n\treturn 0\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>First(\"hello\"): first range iteration yields r='h' at byte 0, return it immediately.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>The range index is a <strong>byte offset</strong>, so it jumps by rune width (0, then the</li>\n</ul>\n<p>  byte length of each rune).</p>\n<ul>\n<li><code>len(s)</code> is the byte count, not the rune count; use <code>utf8.RuneCountInString</code>.</li>\n<li>Converting <code>[]rune(s)</code> gives random access to runes at the cost of a copy.</li>\n</ul>",
    "starter": "// Package firstrune returns the first rune of a string.\npackage firstrune\n\n// First returns the first rune of s, or the rune 0 (utf8.RuneError is not used)\n// for an empty string. Multi-byte runes count as one.\n//\n// TODO(candidate): implement by ranging over s and returning the first rune.\nfunc First(s string) rune {\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "firstrune.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/02-data-types/junior/hexdigit": {
    "id": "01-language-basics/02-data-types/junior/hexdigit",
    "title": "Hex Digit",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Hex Digit</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>Hex characters are contiguous: <code>'0'..'9'</code> then <code>'a'..'f'</code>. You reach them with</p>\n<p>byte arithmetic on character literals.</p>\n<h2>Task</h2>\n<p>Implement <code>Digit(n)</code> returning the lowercase hex char for <code>0..15</code>, else <code>'?'</code>.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Digit(0)\nOutput: '0'\n</code></pre>\n<p>_Explanation:_ '0'+0.</p>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Digit(10)\nOutput: 'a'\n</code></pre>\n<p>_Explanation:_ 'a'+(10-10).</p>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Digit(15)\nOutput: 'f'\n</code></pre>\n<p>_Explanation:_ 'a'+5.</p>\n<p><strong>Example 4:</strong></p>\n<pre class=\"md\"><code>\nInput:  Digit(16)\nOutput: '?'\n</code></pre>\n<p>_Explanation:_ Out of 0..15 range.</p>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Char arithmetic</span><span class=\"tchip\">byte type</span><span class=\"tchip\">Range guard</span></div></details>",
    "education": "<h1>Character arithmetic with bytes</h1>\n<h2>Intuition</h2>\n<p>ASCII character codes are contiguous within each run: <code>'0'..'9'</code> are 48..57,</p>\n<p><code>'a'..'f'</code> are 97..102. So you compute a digit character by offsetting from the</p>\n<p>base:</p>\n<pre class=\"md\"><code>\n'0' + byte(n)      // n in 0..9 -&gt; '0'..'9'\n'a' + byte(n-10)   // n in 10..15 -&gt; 'a'..'f'\n</code></pre>\n<h2>Approach</h2>\n<p>1. For 0..9 return '0'+byte(n).</p>\n<p>2. For 10..15 return 'a'+byte(n-10).</p>\n<p>3. Otherwise return '?'.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Digit(n int) byte {\n\tswitch {\n\tcase n &gt;= 0 &amp;&amp; n &lt;= 9:\n\t\treturn '0' + byte(n)\n\tcase n &gt;= 10 &amp;&amp; n &lt;= 15:\n\t\treturn 'a' + byte(n-10)\n\tdefault:\n\t\treturn '?'\n\t}\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>Digit(10): in 10..15 branch, 'a'(97)+byte(0)=97='a'.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>'0'</code> is a rune constant (value 48); in a <code>byte</code> context it fits since it is</li>\n</ul>\n<p>  ASCII.</p>\n<ul>\n<li>The digit/letter runs are contiguous <em>separately</em> \u2014 you cannot span the gap</li>\n</ul>\n<p>  between <code>'9'</code> and <code>'a'</code> with one offset.</p>\n<ul>\n<li>Guard the input range, or you produce garbage bytes.</li>\n</ul>",
    "starter": "// Package hexdigit maps a nibble to its lowercase hex character.\npackage hexdigit\n\n// Digit returns the lowercase hex character for n (0..15): '0'..'9' then\n// 'a'..'f'. For n outside 0..15 it returns '?'.\n//\n// TODO(candidate): implement using byte/rune arithmetic.\nfunc Digit(n int) byte {\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "hexdigit.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/02-data-types/junior/majority": {
    "id": "01-language-basics/02-data-types/junior/majority",
    "title": "Boolean Majority",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Boolean Majority</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>Three sensors vote; the system trusts the reading only if at least two agree.</p>\n<h2>Task</h2>\n<p>Implement <code>Majority(a, b, c)</code> returning true when \u22652 of the three are true.</p>\n<p>Use boolean operators only \u2014 no counting with ints.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Majority(false,false,false)\nOutput: false\n</code></pre>\n<p>_Explanation:_ No pair true.</p>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Majority(true,true,false)\nOutput: true\n</code></pre>\n<p>_Explanation:_ a&amp;&amp;b true.</p>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Majority(false,true,true)\nOutput: true\n</code></pre>\n<p>_Explanation:_ b&amp;&amp;c true.</p>\n<p><strong>Example 4:</strong></p>\n<pre class=\"md\"><code>\nInput:  Majority(true,true,true)\nOutput: true\n</code></pre>\n<p>_Explanation:_ All pairs true.</p>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Boolean operators</span><span class=\"tchip\">Pairwise agreement</span><span class=\"tchip\">Short-circuit</span></div></details>",
    "education": "<h1>Boolean logic</h1>\n<h2>Intuition</h2>\n<p><code>bool</code> has two values, <code>true</code> and <code>false</code>. The operators combine them: <code>&amp;&amp;</code></p>\n<p>(and) is true only when both sides are, <code>||</code> (or) is true when either is, <code>!</code></p>\n<p>negates. A \"majority of three\" is just every pair OR'd together:</p>\n<pre class=\"md\"><code>\n(a &amp;&amp; b) || (a &amp;&amp; c) || (b &amp;&amp; c)\n</code></pre>\n<h2>Approach</h2>\n<p>1. A majority of three exists iff some pair is both true.</p>\n<p>2. Return (a&amp;&amp;b) || (a&amp;&amp;c) || (b&amp;&amp;c).</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Majority(a, b, c bool) bool {\n\treturn (a &amp;&amp; b) || (a &amp;&amp; c) || (b &amp;&amp; c)\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>Majority(false,true,true): a&amp;&amp;b=false, a&amp;&amp;c=false, b&amp;&amp;c=true -&gt; true.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>&amp;&amp;</code> and <code>||</code> <strong>short-circuit</strong>: the right side is not evaluated if the left</li>\n</ul>\n<p>  already decides the result. That matters when the right side has side effects</p>\n<p>  or could panic (e.g. <code>p != nil &amp;&amp; p.ok</code>).</p>\n<ul>\n<li><code>!</code> binds tighter than <code>&amp;&amp;</code>, which binds tighter than <code>||</code>. Parenthesize when</li>\n</ul>\n<p>  in doubt.</p>\n<ul>\n<li>Go has no implicit truthiness: only a <code>bool</code> fits where a condition is</li>\n</ul>\n<p>  expected, never an int or pointer.</p>",
    "starter": "// Package majority evaluates boolean voting.\npackage majority\n\n// Majority reports whether at least two of the three votes are true.\n//\n// TODO(candidate): implement using boolean logic only.\nfunc Majority(a, b, c bool) bool {\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "majority.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/02-data-types/junior/nancheck": {
    "id": "01-language-basics/02-data-types/junior/nancheck",
    "title": "Finite Float Check",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Finite Float Check</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>Floating point has special values: <code>NaN</code> (not-a-number) and <code>\u00b1Inf</code>. They arise</p>\n<p>from <code>0.0/0.0</code>, <code>math.Sqrt(-1)</code>, overflow, etc. and must often be rejected.</p>\n<h2>Task</h2>\n<p>Implement <code>Finite(x)</code> returning true only for normal finite numbers.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Finite(3.14)\nOutput: true\n</code></pre>\n<p>_Explanation:_ Normal number.</p>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Finite(math.NaN())\nOutput: false\n</code></pre>\n<p>_Explanation:_ NaN is not finite.</p>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Finite(math.Inf(1))\nOutput: false\n</code></pre>\n<p>_Explanation:_ +Inf is not finite.</p>\n<p><strong>Example 4:</strong></p>\n<pre class=\"md\"><code>\nInput:  Finite(math.Inf(-1))\nOutput: false\n</code></pre>\n<p>_Explanation:_ -Inf is not finite.</p>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">NaN</span><span class=\"tchip\">\u00b1Inf</span><span class=\"tchip\">math.IsNaN / IsInf</span></div></details>",
    "education": "<h1>NaN and infinity</h1>\n<h2>Intuition</h2>\n<p>IEEE-754 floats include <code>NaN</code> and <code>\u00b1Inf</code>. Detect them with the <code>math</code> helpers,</p>\n<p>never with <code>==</code>:</p>\n<pre class=\"md\"><code>\n!math.IsNaN(x) &amp;&amp; !math.IsInf(x, 0) // finite?\n</code></pre>\n<p><code>math.IsInf(x, 0)</code> tests either sign; pass <code>1</code> or <code>-1</code> for a specific one.</p>\n<h2>Approach</h2>\n<p>1. Reject NaN with math.IsNaN(x).</p>\n<p>2. Reject +/-Inf with math.IsInf(x, 0) (0 means either sign).</p>\n<p>3. Return true only when neither holds.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nimport \"math\"\n\nfunc Finite(x float64) bool {\n\treturn !math.IsNaN(x) &amp;&amp; !math.IsInf(x, 0)\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>Finite(math.Inf(1)): IsNaN=false, IsInf(x,0)=true, so !false &amp;&amp; !true = false.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>x != x</code> is true exactly when <code>x</code> is NaN \u2014 a valid but cryptic idiom; prefer</li>\n</ul>\n<p>  <code>math.IsNaN</code>.</p>\n<ul>\n<li>Sorting or comparing slices with NaN gives inconsistent orderings.</li>\n<li><code>math.Inf(1)</code> is +Inf, <code>math.Inf(-1)</code> is -Inf.</li>\n</ul>",
    "starter": "// Package nancheck detects non-finite floats.\npackage nancheck\n\n// Finite reports whether x is a normal finite number (not NaN, not \u00b1Inf).\n//\n// TODO(candidate): implement using math.IsNaN and math.IsInf.\nfunc Finite(x float64) bool {\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "nancheck.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/02-data-types/junior/narrowing": {
    "id": "01-language-basics/02-data-types/junior/narrowing",
    "title": "Narrowing Conversion",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Narrowing Conversion</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>A service reads 64-bit counters but a legacy protocol field is only 32-bit</p>\n<p>wide. The team needs a conversion that matches exactly what the wire format</p>\n<p>does when a value overflows \u2014 Go's numeric narrowing, not a saturating clamp.</p>\n<h2>Task</h2>\n<p>Implement <code>ToInt32</code> in narrowing.go so it converts an <code>int64</code></p>\n<p>to an <code>int32</code>, wrapping around (two's-complement) when the value does not fit.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  ToInt32(42)\nOutput: 42\n</code></pre>\n<p>_Explanation:_ Fits.</p>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  ToInt32(2147483647)\nOutput: 2147483647\n</code></pre>\n<p>_Explanation:_ int32 max, still fits.</p>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  ToInt32(2147483648)\nOutput: -2147483648\n</code></pre>\n<p>_Explanation:_ One past max wraps two's-complement to int32 min.</p>\n<p><strong>Example 4:</strong></p>\n<pre class=\"md\"><code>\nInput:  ToInt32(4294967296)\nOutput: 0\n</code></pre>\n<p>_Explanation:_ 2^32 wraps to 0 (low 32 bits are all zero).</p>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Explicit conversion</span><span class=\"tchip\">Narrowing</span><span class=\"tchip\">Two's-complement wrap</span></div></details>",
    "education": "<h1>Narrowing conversions</h1>\n<h2>Intuition</h2>\n<p>Converting to a smaller integer type keeps only the low bits \u2014 it <strong>wraps</strong>, it</p>\n<p>does not clamp or error:</p>\n<pre class=\"md\"><code>\nint8(200)  // -56 (200 mod 256, reinterpreted as signed)\nuint8(300) // 44\n</code></pre>\n<h2>Approach</h2>\n<p>1. Apply the explicit conversion int32(n).</p>\n<p>2. Go's spec drops the high bits and wraps (two's-complement); no clamping or range check.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc ToInt32(n int64) int32 {\n\treturn int32(n)\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>ToInt32(2147483648): low 32 bits represent int32 min, so int32(2147483648) = -2147483648.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Conversions never panic on overflow; they truncate bits.</li>\n<li>Signedness matters: <code>int8(200)</code> reinterprets the high bit as sign.</li>\n<li>Check the range first, or clamp, when the input may exceed the target type.</li>\n</ul>",
    "starter": "// Package narrowing \u2014 Gopher Workplace challenge.\npackage narrowing\n\n// ToInt32 converts a 64-bit integer to a 32-bit one. Go requires an explicit\n// conversion between numeric types; when the value does not fit in int32, the\n// high bits are dropped and the result wraps around (two's-complement), it is\n// not clamped.\n//\n// Examples:\n//\n//\tToInt32(42)          => 42\n//\tToInt32(-7)          => -7\n//\tToInt32(2147483647)  => 2147483647   // int32 max, still fits\n//\tToInt32(2147483648)  => -2147483648  // one past max, wraps to int32 min\n//\tToInt32(4294967296)  => 0            // 2^32 wraps to 0\nfunc ToInt32(n int64) int32 {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "narrowing.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/02-data-types/junior/quote": {
    "id": "01-language-basics/02-data-types/junior/quote",
    "title": "Quote a String",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Quote a String</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>To embed a double quote inside a double-quoted string you escape it with <code>\\\"</code>.</p>\n<h2>Task</h2>\n<p>Implement <code>Wrap(s)</code> returning <code>s</code> surrounded by literal <code>\"</code> characters.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Wrap(\"hello\")\nOutput: \"hello\" (with quote bytes)\n</code></pre>\n<p>_Explanation:_ hello surrounded by literal quote bytes.</p>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Wrap(\"\")\nOutput: just two quote bytes\n</code></pre>\n<p>_Explanation:_ Empty input yields just the two quotes.</p>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Wrap(\"a b\")\nOutput: \"a b\" (with quote bytes)\n</code></pre>\n<p>_Explanation:_ Spaces preserved.</p>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Escaped quote</span><span class=\"tchip\">Concatenation</span><span class=\"tchip\">Empty string</span></div></details>",
    "education": "<h1>Escaping quotes</h1>\n<h2>Intuition</h2>\n<p>Inside a double-quoted string, a literal double quote must be escaped as <code>\\\"</code>,</p>\n<p>because an unescaped <code>\"</code> would end the string:</p>\n<pre class=\"md\"><code>\n\"\\\"\" // a string of length 1: one quote character\n</code></pre>\n<h2>Approach</h2>\n<p>1. Build \"\\\"\" + s + \"\\\"\".</p>\n<p>2. The \\\" escape places a literal double-quote byte inside the interpreted literal.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Wrap(s string) string {\n\treturn \"\\\"\" + s + \"\\\"\"\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>Wrap(\"hello\"): quote + hello + quote = a 7-byte string beginning and ending with a double quote.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>len(\"\\\"\")</code> is 1.</li>\n<li>A raw literal sidesteps escaping quotes but <strong>cannot contain a backtick</strong>, so</li>\n</ul>\n<p>  it cannot hold a backtick the way <code>\"...\"</code> holds a quote.</p>\n<ul>\n<li>The standard <code>strconv.Quote</code> adds quotes <em>and</em> escapes contents \u2014 different</li>\n</ul>\n<p>  job from simple wrapping.</p>",
    "starter": "// Package quote wraps a string in double quotes.\npackage quote\n\n// Wrap returns s surrounded by literal double-quote characters, e.g.\n// Wrap(hello) => \"hello\" (five letters between two quote bytes).\n//\n// TODO(candidate): implement using the \\\" escape.\nfunc Wrap(s string) string {\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "quote.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/02-data-types/junior/rawpath": {
    "id": "01-language-basics/02-data-types/junior/rawpath",
    "title": "Raw String Path",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Raw String Path</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>A Windows path is full of backslashes. In an interpreted <code>\"...\"</code> string each <code>\\</code></p>\n<p>must be doubled; a raw `<code> </code>...<code> </code>` literal takes the characters verbatim.</p>\n<h2>Task</h2>\n<p>Implement <code>TempPath()</code> returning <code>C:\\Users\\temp\\log.txt</code> using a <strong>raw</strong> string</p>\n<p>literal (backticks).</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  TempPath()\nOutput: C:\\Users\\temp\\log.txt\n</code></pre>\n<p>_Explanation:_ Raw literal keeps every backslash verbatim.</p>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  len(TempPath())\nOutput: 21\n</code></pre>\n<p>_Explanation:_ Three literal single-byte backslashes, no escaping.</p>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  TempPath() equals the escaped form\nOutput: true\n</code></pre>\n<p>_Explanation:_ Equal to \"C:\\\\Users\\\\temp\\\\log.txt\".</p>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Raw string literal</span><span class=\"tchip\">No escapes</span><span class=\"tchip\">Single line vs multi</span></div></details>",
    "education": "<h1>Raw string literals</h1>\n<h2>Intuition</h2>\n<p>Go has two string literal forms. Interpreted literals use double quotes and</p>\n<p>process escape sequences (<code>\\n</code>, <code>\\t</code>, <code>\\\\</code>). <strong>Raw</strong> literals use backticks and</p>\n<p>take every character verbatim \u2014 no escapes, so a backslash is just a backslash:</p>\n<pre class=\"md\"><code>\n`C:\\Users\\temp` // 13 characters, three literal backslashes\n\"C:\\\\Users\\\\temp\" // same string, doubled backslashes\n</code></pre>\n<h2>Approach</h2>\n<p>1. Return a backtick-delimited raw string literal.</p>\n<p>2. Inside backticks backslashes are literal, so no doubling is needed.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc TempPath() string {\n\treturn `C:\\Users\\temp\\log.txt`\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>C:\\Users\\temp\\log.txt</code> is taken character-for-character, giving the path with single backslashes.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>The only thing a raw literal cannot contain is a backtick.</li>\n<li>Carriage returns (<code>\\r</code>) are stripped, but newlines are kept, so raw multi-line</li>\n</ul>\n<p>  strings embed real line breaks.</p>\n<ul>\n<li>Because nothing is escaped, <code>\\n</code> inside backticks is two characters, not a</li>\n</ul>\n<p>  newline.</p>",
    "starter": "// Package rawpath returns a Windows-style path using a raw string literal.\npackage rawpath\n\n// TempPath returns the literal string C:\\Users\\temp\\log.txt using a raw string\n// literal (backticks), so no backslash needs escaping.\n//\n// TODO(candidate): return the path as a raw string literal.\nfunc TempPath() string {\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "rawpath.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/02-data-types/junior/round2": {
    "id": "01-language-basics/02-data-types/junior/round2",
    "title": "Round to Decimals",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Round to Decimals</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p><code>math.Round</code> rounds to the nearest whole number. To round to N decimals you</p>\n<p>scale up, round, and scale back down.</p>\n<h2>Task</h2>\n<p>Implement <code>Round(x, places)</code> rounding to <code>places</code> decimals, half away from zero.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Round(3.14159, 2)\nOutput: 3.14\n</code></pre>\n<p>_Explanation:_ Scale by 100, round, divide.</p>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Round(2.5, 0)\nOutput: 3\n</code></pre>\n<p>_Explanation:_ math.Round is half away from zero.</p>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Round(-2.675, 2)\nOutput: -2.68\n</code></pre>\n<p>_Explanation:_ Half away from zero on negatives too.</p>\n<p><strong>Example 4:</strong></p>\n<pre class=\"md\"><code>\nInput:  Round(1.005, 2)\nOutput: 1.0\n</code></pre>\n<p>_Explanation:_ 1.005 is stored slightly below, so it rounds down - a float fact.</p>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">math.Round</span><span class=\"tchip\">Scaling</span><span class=\"tchip\">Float imprecision</span></div></details>",
    "education": "<h1>Rounding floats to decimals</h1>\n<h2>Intuition</h2>\n<p><code>math.Round</code> only rounds to whole numbers. To keep N decimals, shift the decimal</p>\n<p>point by multiplying by 10^N, round to an integer, then shift back:</p>\n<pre class=\"md\"><code>\np := math.Pow(10, float64(places))\nmath.Round(x*p) / p\n</code></pre>\n<h2>Approach</h2>\n<p>1. Compute p = 10^places via math.Pow.</p>\n<p>2. Multiply x by p, round to an integer with math.Round, divide back by p.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nimport \"math\"\n\nfunc Round(x float64, places int) float64 {\n\tp := math.Pow(10, float64(places))\n\treturn math.Round(x*p) / p\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>Round(3.14159, 2): p=100, 314.159 -&gt; math.Round=314, 314/100=3.14.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>math.Round</code> rounds <strong>half away from zero</strong> (<code>2.5 -&gt; 3</code>, <code>-2.5 -&gt; -3</code>), unlike</li>\n</ul>\n<p>  banker's rounding.</p>\n<ul>\n<li>Because <code>1.005</code> is stored slightly below 1.005, <code>Round(1.005, 2)</code> can give</li>\n</ul>\n<p>  1.00 \u2014 a float fact, not a bug.</p>\n<ul>\n<li>For exact decimal money, use integer minor units, not rounded floats.</li>\n</ul>",
    "starter": "// Package round2 rounds floats to a fixed number of decimal places.\npackage round2\n\n// Round returns x rounded to places decimal places, half away from zero.\n//\n// TODO(candidate): implement using math.Round and scaling.\nfunc Round(x float64, places int) float64 {\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "round2.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/02-data-types/junior/runecount": {
    "id": "01-language-basics/02-data-types/junior/runecount",
    "title": "Rune Count",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Rune Count</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>A form validator must limit a username to N characters. A first attempt used</p>\n<p><code>len(s)</code>, but users with accented or non-Latin names hit the limit too early \u2014</p>\n<p><code>len</code> counts bytes, not characters.</p>\n<h2>Task</h2>\n<p>Implement <code>Count</code> in runecount.go so it returns the number of</p>\n<p><strong>runes</strong> (characters) in <code>s</code>, correct for multi-byte UTF-8 input.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Count(\"abc\")\nOutput: 3\n</code></pre>\n<p>_Explanation:_ ASCII, bytes==runes.</p>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Count(\"hello\" with accent)\nOutput: 5\n</code></pre>\n<p>_Explanation:_ The accented char is 2 bytes but 1 rune; len would give 6.</p>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Count(\"CJK pair\")\nOutput: 2\n</code></pre>\n<p>_Explanation:_ 3 bytes each, 2 runes.</p>\n<p><strong>Example 4:</strong></p>\n<pre class=\"md\"><code>\nInput:  Count(\"a&lt;emoji&gt;b\")\nOutput: 3\n</code></pre>\n<p>_Explanation:_ The emoji is 4 bytes, still 1 rune.</p>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Strings are bytes</span><span class=\"tchip\"><code>len</code> is a byte count</span><span class=\"tchip\">Runes</span></div></details>",
    "education": "<h1>Counting runes, not bytes</h1>\n<h2>Intuition</h2>\n<p><code>len(s)</code> is the <strong>byte</strong> count. The number of characters is the rune count:</p>\n<pre class=\"md\"><code>\nutf8.RuneCountInString(s) // characters\nlen(s)                    // bytes (&gt;= rune count)\n</code></pre>\n<p>Ranging over a string also yields runes, so counting iterations works too.</p>\n<h2>Approach</h2>\n<p>1. Convert the string to []rune, which decodes UTF-8.</p>\n<p>2. Return len of that rune slice (a range-count loop works equally).</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Count(s string) int {\n\treturn len([]rune(s))\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>Count of a 5-character accented word: []rune decodes to 5 code points, length 5.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>len(s)</code> equals the rune count only for pure ASCII.</li>\n<li><code>for range s</code> decodes UTF-8; the index is a byte offset that jumps by rune</li>\n</ul>\n<p>  width.</p>\n<ul>\n<li>A rune is a code point, not necessarily a full grapheme (combining marks).</li>\n</ul>",
    "starter": "// Package runecount \u2014 Gopher Workplace challenge.\npackage runecount\n\n// Count returns the number of characters (runes) in s, not the number of bytes.\n// Go strings are UTF-8: a multi-byte character like \"\u00e9\" or \"\u65e5\" is several bytes\n// but a single rune, so len(s) (a byte count) is the wrong answer.\n//\n// Examples:\n//\n//\tCount(\"abc\")   => 3\n//\tCount(\"h\u00e9llo\") => 5   // \u00e9 is 2 bytes, still 1 rune\n//\tCount(\"\u65e5\u672c\")   => 2   // 3 bytes each, 2 runes\n//\tCount(\"\")      => 0\nfunc Count(s string) int {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "runecount.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/02-data-types/junior/truncate": {
    "id": "01-language-basics/02-data-types/junior/truncate",
    "title": "Truncate Float",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Truncate Float</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>A billing report needs the whole-dollar part of each line amount \u2014 <code>9.99</code></p>\n<p>becomes <code>9</code>, <code>-9.99</code> becomes <code>-9</code>. A teammate reached for rounding and got <code>10</code>,</p>\n<p>which threw off the ledger. The correct primitive here is a plain numeric</p>\n<p>conversion, whose truncation behaviour is precisely defined.</p>\n<h2>Task</h2>\n<p>Implement <code>WholePart</code> in truncate.go so that it returns the</p>\n<p>integer part of <code>amount</code>:</p>\n<p>1. Convert the <code>float64</code> to an <code>int</code>, dropping the fractional part.</p>\n<p>2. Truncation is <strong>toward zero</strong>: <code>9.99 \u2192 9</code> and <code>-9.99 \u2192 -9</code> (not <code>-10</code>).</p>\n<p>3. Exact integers like <code>4.0</code> return <code>4</code>.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  WholePart(9.99)\nOutput: 9\n</code></pre>\n<p>_Explanation:_ float-&gt;int truncates toward zero, dropping .99.</p>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  WholePart(-9.99)\nOutput: -9\n</code></pre>\n<p>_Explanation:_ Truncation is toward zero, not floor.</p>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  WholePart(0.999)\nOutput: 0\n</code></pre>\n<p>_Explanation:_ Fraction dropped.</p>\n<p><strong>Example 4:</strong></p>\n<pre class=\"md\"><code>\nInput:  WholePart(12345.678)\nOutput: 12345\n</code></pre>\n<p>_Explanation:_ Whole part kept.</p>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\"><code>float64</code>\u2192<code>int</code> conversion</span><span class=\"tchip\">Truncation toward zero</span><span class=\"tchip\">Conversion vs rounding</span></div></details>",
    "education": "<h1>Float-to-int truncation</h1>\n<h2>Intuition</h2>\n<p>Converting a <code>float64</code> to an <code>int</code> <strong>truncates toward zero</strong> \u2014 it drops the</p>\n<p>fraction, it does not round:</p>\n<pre class=\"md\"><code>\nint(3.9)  // 3\nint(-3.9) // -3\n</code></pre>\n<h2>Approach</h2>\n<p>1. Convert the float64 to int with int(amount).</p>\n<p>2. Go truncates toward zero, which is exactly the required whole-dollar behavior.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc WholePart(amount float64) int {\n\treturn int(amount)\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>WholePart(-9.99): int(-9.99) drops the fraction toward zero -&gt; -9.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>int(x)</code> truncates toward zero, so negatives round <em>up</em> toward zero.</li>\n<li>Converting a float larger than the int range is implementation-defined \u2014</li>\n</ul>\n<p>  guard the domain.</p>\n<ul>\n<li>For \"floor\" use <code>math.Floor</code>; for nearest use <code>math.Round</code>.</li>\n</ul>",
    "starter": "// Package truncate \u2014 Gopher Workplace challenge.\npackage truncate\n\n// WholePart returns the whole-dollar part of a monetary amount by converting a\n// float64 to an int. A float64\u2192int conversion in Go truncates toward zero (it\n// drops the fraction, it does not round), so 9.99 yields 9 and -9.99 yields -9.\n//\n// Examples:\n//\n//\tWholePart(9.99)  => 9\n//\tWholePart(-9.99) => -9\n//\tWholePart(4.0)   => 4\nfunc WholePart(amount float64) int {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "truncate.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/02-data-types/junior/vowelcount": {
    "id": "01-language-basics/02-data-types/junior/vowelcount",
    "title": "Vowel Count",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Vowel Count</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>Counting vowels means walking characters and testing membership \u2014 a natural fit</p>\n<p>for ranging over runes and a <code>switch</code>.</p>\n<h2>Task</h2>\n<p>Implement <code>Vowels(s)</code> counting ASCII vowels (a,e,i,o,u, any case). Accented</p>\n<p>letters like <code>\u00e9</code> do not count.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Vowels(\"hello\")\nOutput: 2\n</code></pre>\n<p>_Explanation:_ e and o.</p>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Vowels(\"AEIOU\")\nOutput: 5\n</code></pre>\n<p>_Explanation:_ Case-insensitive, all five.</p>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Vowels(\"xyz\")\nOutput: 0\n</code></pre>\n<p>_Explanation:_ No vowels.</p>\n<p><strong>Example 4:</strong></p>\n<pre class=\"md\"><code>\nInput:  Vowels(\"\")\nOutput: 0\n</code></pre>\n<p>_Explanation:_ Empty string.</p>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Ranging runes</span><span class=\"tchip\">Rune literals</span><span class=\"tchip\">switch/case list</span></div></details>",
    "education": "<h1>Runes and character tests</h1>\n<h2>Intuition</h2>\n<p>Ranging over a string yields runes, which you compare against <strong>rune literals</strong></p>\n<p>like <code>'a'</code>. A <code>switch</code> with a case list is the clean way to test membership:</p>\n<pre class=\"md\"><code>\nfor _, r := range s {\n\tswitch r {\n\tcase 'a', 'e', 'i', 'o', 'u', 'A', 'E', 'I', 'O', 'U':\n\t\tn++\n\t}\n}\n</code></pre>\n<h2>Approach</h2>\n<p>1. Range over the string rune by rune.</p>\n<p>2. For each rune, check membership in the ASCII vowel set (both cases).</p>\n<p>3. Increment a counter and return it.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Vowels(s string) int {\n\tcount := 0\n\tfor _, r := range s {\n\t\tswitch r {\n\t\tcase 'a', 'e', 'i', 'o', 'u', 'A', 'E', 'I', 'O', 'U':\n\t\t\tcount++\n\t\t}\n\t}\n\treturn count\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>Vowels(\"hello\"): runes h,e,l,l,o; e and o match -&gt; 2.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>'a'</code> is a rune (int32) constant; <code>\"a\"</code> is a string \u2014 different types.</li>\n<li>To fold case, add both <code>'a'</code> and <code>'A'</code>, or convert with</li>\n</ul>\n<p>  <code>unicode.ToLower</code>.</p>\n<ul>\n<li>Accented vowels are distinct runes; decide deliberately whether they count.</li>\n</ul>",
    "starter": "// Package vowelcount counts vowels in a string, rune-aware.\npackage vowelcount\n\n// Vowels counts the ASCII vowels (a,e,i,o,u, any case) in s, iterating runes.\n//\n// TODO(candidate): implement by ranging over runes.\nfunc Vowels(s string) int {\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "vowelcount.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/02-data-types/middle/atoi": {
    "id": "01-language-basics/02-data-types/middle/atoi",
    "title": "Manual Atoi",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Manual Atoi</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>Parsing \"123\" means folding digits into a running total: `total = total*10 +</p>\n<p>digit<code>. A character's digit value is </code>c - '0'`.</p>\n<h2>Task</h2>\n<p>Implement <code>Parse(s)</code> (optional leading <code>-</code>), returning <code>(value, ok)</code>; <code>ok=false</code></p>\n<p>on any non-digit. Don't use <code>strconv</code>.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Parse(\"42\")\nOutput: (42, true)\n</code></pre>\n<p>_Explanation:_ digit-by-digit: 4, then 4*10+2=42</p>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Parse(\"-17\")\nOutput: (-17, true)\n</code></pre>\n<p>_Explanation:_ leading '-' sets sign, magnitude 17 negated</p>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Parse(\"1a\")\nOutput: (0, false)\n</code></pre>\n<p>_Explanation:_ 'a' is not a digit -&gt; invalid</p>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Digit value</span><span class=\"tchip\">Horner fold</span><span class=\"tchip\">Validation</span></div></details>",
    "education": "<h1>Parsing integers by hand</h1>\n<h2>Intuition</h2>\n<p>Each digit shifts the running value up one decimal place: <code>n = n*10 + (c-'0')</code>.</p>\n<p><code>c - '0'</code> converts a digit character to its numeric value because ASCII digits</p>\n<p>are contiguous.</p>\n<h2>Approach</h2>\n<p>1. Empty string -&gt; (0,false). 2. Detect a leading '-'; if the string is just '-' it's invalid. 3. Scan remaining bytes; each must be '0'..'9'. 4. Accumulate n = n*10 + (c-'0'). 5. Negate if a '-' was seen.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Parse(s string) (int, bool) {\n\tif s == \"\" {\n\t\treturn 0, false\n\t}\n\tneg := false\n\ti := 0\n\tif s[0] == '-' {\n\t\tneg = true\n\t\ti = 1\n\t}\n\tif i &gt;= len(s) {\n\t\treturn 0, false\n\t}\n\tn := 0\n\tfor ; i &lt; len(s); i++ {\n\t\tc := s[i]\n\t\tif c &lt; '0' || c &gt; '9' {\n\t\t\treturn 0, false\n\t\t}\n\t\tn = n*10 + int(c-'0')\n\t}\n\tif neg {\n\t\tn = -n\n\t}\n\treturn n, true\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>Parse(\"-17\"): neg=true, i=1. c='1' -&gt; n=1. c='7' -&gt; n=17. negate -&gt; -17. return (-17,true).</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Range over <strong>bytes</strong> here (ASCII digits); ranging runes also works but bytes</li>\n</ul>\n<p>  are simpler for digits.</p>\n<ul>\n<li>Handle the sign once, after (or before) the digit loop \u2014 not per digit.</li>\n<li>Real parsers also detect overflow; this simplified one does not.</li>\n</ul>",
    "starter": "// Package atoi parses a base-10 integer by hand.\npackage atoi\n\n// Parse converts a decimal string (optional leading '-') to an int. On any\n// invalid character it returns (0, false).\n//\n// TODO(candidate): implement digit-by-digit; do not use strconv.\nfunc Parse(s string) (int, bool) {\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "atoi.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/02-data-types/middle/baseconv": {
    "id": "01-language-basics/02-data-types/middle/baseconv",
    "title": "Base Conversion",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Base Conversion</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>Writing a number in base b is repeated division: the remainders are the digits,</p>\n<p>least significant first, so you reverse at the end.</p>\n<h2>Task</h2>\n<p>Implement <code>Format(n, base)</code> for base 2..16, lowercase digits. <code>Format(0,b)=\"0\"</code>.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Format(255, 16)\nOutput: \"ff\"\n</code></pre>\n<p>_Explanation:_ 255 = 15*16+15 -&gt; digits f,f</p>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Format(5, 2)\nOutput: \"101\"\n</code></pre>\n<p>_Explanation:_ 5 in binary</p>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Format(0, 2)\nOutput: \"0\"\n</code></pre>\n<p>_Explanation:_ zero is the special case</p>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Repeated division</span><span class=\"tchip\">Digit mapping</span><span class=\"tchip\">Reverse order</span></div></details>",
    "education": "<h1>Positional base conversion</h1>\n<h2>Intuition</h2>\n<p>To write <code>n</code> in base <code>b</code>, repeatedly take <code>n % b</code> (the next digit) and <code>n /= b</code>,</p>\n<p>until <code>n</code> is 0. Digits emerge least-significant first, so reverse:</p>\n<pre class=\"md\"><code>\nconst digits = \"0123456789abcdef\"\nfor n &gt; 0 { buf = append(buf, digits[n%base]); n /= base }\n// reverse buf\n</code></pre>\n<h2>Approach</h2>\n<p>1. n==0 returns \"0\". 2. Repeatedly take n%base as the next digit (indexing \"0123456789abcdef\"). 3. Divide n by base. 4. Digits come out least-significant first, so reverse the buffer before returning.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Format(n, base int) string {\n\tif n == 0 {\n\t\treturn \"0\"\n\t}\n\tconst digits = \"0123456789abcdef\"\n\tvar buf []byte\n\tfor n &gt; 0 {\n\t\tbuf = append(buf, digits[n%base])\n\t\tn /= base\n\t}\n\tfor i, j := 0, len(buf)-1; i &lt; j; i, j = i+1, j-1 {\n\t\tbuf[i], buf[j] = buf[j], buf[i]\n\t}\n\treturn string(buf)\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>Format(255,16): 255%16=15 -&gt; 'f', n=15; 15%16=15 -&gt; 'f', n=0. buf=['f','f'], reversed still \"ff\".</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Handle <code>n == 0</code> specially, or you return an empty string.</li>\n<li>Indexing <code>digits[n%base]</code> gives a <code>byte</code>; build a <code>[]byte</code> and convert once.</li>\n<li>This version assumes non-negative <code>n</code>; a sign needs separate handling.</li>\n</ul>",
    "starter": "// Package baseconv formats a non-negative int in an arbitrary base 2..16.\npackage baseconv\n\n// Format returns n written in the given base (2..16) using lowercase digits\n// 0-9a-f. n is non-negative; Format(0, base) is \"0\".\n//\n// TODO(candidate): implement by repeated division, building digits.\nfunc Format(n, base int) string {\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "baseconv.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/02-data-types/middle/caesar": {
    "id": "01-language-basics/02-data-types/middle/caesar",
    "title": "Caesar Cipher",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Caesar Cipher</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>Each letter shifts by <code>n</code> within its case, wrapping z\u2192a. Non-letters stay put.</p>\n<p>Rune arithmetic plus a modulo does the wrap.</p>\n<h2>Task</h2>\n<p>Implement <code>Shift(s, n)</code> (letters only, case preserved, wrap, <code>n</code> any int).</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Shift(\"abc\", 1)\nOutput: \"bcd\"\n</code></pre>\n<p>_Explanation:_ each letter +1</p>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Shift(\"xyz\", 3)\nOutput: \"abc\"\n</code></pre>\n<p>_Explanation:_ wraps past z back to a</p>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Shift(\"Hello, World!\", 13)\nOutput: \"Uryyb, Jbeyq!\"\n</code></pre>\n<p>_Explanation:_ ROT13; punctuation/space unchanged</p>\n<p><strong>Example 4:</strong></p>\n<pre class=\"md\"><code>\nInput:  Shift(\"abc\", -1)\nOutput: \"zab\"\n</code></pre>\n<p>_Explanation:_ negative shift normalized modulo 26</p>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Rune ranges</span><span class=\"tchip\">Modular wrap</span><span class=\"tchip\">Building strings</span></div></details>",
    "education": "<h1>Modular letter shifting</h1>\n<h2>Intuition</h2>\n<p>Map a letter to 0..25 by subtracting its case base, add the shift, take it mod 26</p>\n<p>(normalized non-negative), and add the base back:</p>\n<pre class=\"md\"><code>\nbase + rune(((int(r-base)+n)%26+26)%26)\n</code></pre>\n<p>Non-letters are left untouched.</p>\n<h2>Approach</h2>\n<p>1. Normalize the shift: shift = ((n%26)+26)%26 so negatives/large values work. 2. For each byte, if it's a-z map within lowercase, if A-Z within uppercase, using (c-base+shift)%26. 3. Non-letters pass through.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Shift(s string, n int) string {\n\tshift := ((n % 26) + 26) % 26\n\tout := []byte(s)\n\tfor i := 0; i &lt; len(out); i++ {\n\t\tc := out[i]\n\t\tswitch {\n\t\tcase c &gt;= 'a' &amp;&amp; c &lt;= 'z':\n\t\t\tout[i] = 'a' + byte((int(c-'a')+shift)%26)\n\t\tcase c &gt;= 'A' &amp;&amp; c &lt;= 'Z':\n\t\t\tout[i] = 'A' + byte((int(c-'A')+shift)%26)\n\t\t}\n\t}\n\treturn string(out)\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>Shift(\"xyz\",3): shift=3. 'x'-&gt;(23+3)%26=0-&gt;'a', 'y'-&gt;1-&gt;'b', 'z'-&gt;2-&gt;'c'. \"abc\".</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Handle upper and lower case with their own base.</li>\n<li>Normalize the modulo (<code>(x%26+26)%26</code>) so negative shifts wrap correctly.</li>\n<li>ROT13 is <code>Shift(s,13)</code> and is its own inverse.</li>\n</ul>",
    "starter": "// Package caesar implements a Caesar shift cipher over ASCII letters.\npackage caesar\n\n// Shift returns s with each ASCII letter advanced by n places (wrapping within\n// its case). Non-letters pass through unchanged. n may be large or negative.\n//\n// TODO(candidate): implement with rune ranging and modular arithmetic.\nfunc Shift(s string, n int) string {\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "caesar.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/02-data-types/middle/clamp01": {
    "id": "01-language-basics/02-data-types/middle/clamp01",
    "title": "Saturate to [0,1]",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Saturate to [0,1]</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>Colours and probabilities live in [0,1]. Saturating clamps out-of-range values \u2014</p>\n<p>and NaN must be pinned to a safe default.</p>\n<h2>Task</h2>\n<p>Implement <code>Saturate(x)</code> limiting to [0,1]; NaN \u2192 0.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Saturate(0.5)\nOutput: 0.5\n</code></pre>\n<p>_Explanation:_ already in range</p>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Saturate(2)\nOutput: 1\n</code></pre>\n<p>_Explanation:_ above 1 clamps to 1</p>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Saturate(-1)\nOutput: 0\n</code></pre>\n<p>_Explanation:_ below 0 clamps to 0</p>\n<p><strong>Example 4:</strong></p>\n<pre class=\"md\"><code>\nInput:  Saturate(NaN)\nOutput: 0\n</code></pre>\n<p>_Explanation:_ NaN maps to 0</p>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Range clamp</span><span class=\"tchip\">NaN handling</span><span class=\"tchip\">Ordering</span></div></details>",
    "education": "<h1>Saturating floats, and NaN</h1>\n<h2>Intuition</h2>\n<p>Clamp with two comparisons \u2014 but NaN breaks the naive version, because every</p>\n<p>comparison with NaN is false, so a <code>NaN</code> would slip through unchanged:</p>\n<pre class=\"md\"><code>\nif math.IsNaN(x) { return 0 }\nif x &lt; 0 { return 0 }\nif x &gt; 1 { return 1 }\nreturn x\n</code></pre>\n<h2>Approach</h2>\n<p>1. If x is NaN (math.IsNaN) return 0. 2. If x&lt;0 return 0. 3. If x&gt;1 return 1. 4. Otherwise return x.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nimport \"math\"\n\nfunc Saturate(x float64) float64 {\n\tif math.IsNaN(x) {\n\t\treturn 0\n\t}\n\tif x &lt; 0 {\n\t\treturn 0\n\t}\n\tif x &gt; 1 {\n\t\treturn 1\n\t}\n\treturn x\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>Saturate(2): not NaN, not &lt;0, but 2&gt;1 -&gt; return 1.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Order matters: test NaN before the range checks.</li>\n<li><code>min</code>/<code>max</code> builtins (Go 1.21+) also propagate NaN, so they don't fix this.</li>\n</ul>",
    "starter": "// Package clamp01 saturates a float to [0,1].\npackage clamp01\n\n// Saturate returns x limited to the range [0, 1]. NaN maps to 0.\n//\n// TODO(candidate): implement.\nfunc Saturate(x float64) float64 {\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "clamp01.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/02-data-types/middle/csvfield": {
    "id": "01-language-basics/02-data-types/middle/csvfield",
    "title": "CSV Field Quoting",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">CSV Field Quoting</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>An export endpoint writes CSV. Per RFC 4180 a field needs quoting only if it</p>\n<p>contains a comma, quote, or newline \u2014 and inner quotes are doubled.</p>\n<h2>Task</h2>\n<p>Implement <code>Quote(s)</code> following that rule.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Quote(\"plain\")\nOutput: \"plain\"\n</code></pre>\n<p>_Explanation:_ no special chars -&gt; unchanged</p>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Quote(\"a,b\")\nOutput: \"\\\"a,b\\\"\"\n</code></pre>\n<p>_Explanation:_ comma forces quoting</p>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Quote(\"say \\\"hi\\\"\")\nOutput: \"\\\"say \\\"\\\"hi\\\"\\\"\\\"\"\n</code></pre>\n<p>_Explanation:_ inner quotes doubled and field wrapped</p>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Conditional quoting</span><span class=\"tchip\">Escaping</span><span class=\"tchip\">String scanning</span></div></details>",
    "education": "<h1>CSV escaping</h1>\n<h2>Intuition</h2>\n<p>RFC 4180 says a field is quoted only when it contains a delimiter (comma), a</p>\n<p>quote, or a newline. When quoted, every inner <code>\"</code> is written as <code>\"\"</code>:</p>\n<pre class=\"md\"><code>\nif strings.ContainsAny(s, \",\\\"\\n\") {\n\treturn `\"` + strings.ReplaceAll(s, `\"`, `\"\"`) + `\"`\n}\nreturn s\n</code></pre>\n<h2>Approach</h2>\n<p>1. Scan for comma, double-quote, or newline. 2. If none present, return s unchanged. 3. Otherwise emit a leading '\"', copy each char doubling any '\"', then a trailing '\"'.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Quote(s string) string {\n\tneed := false\n\tfor i := 0; i &lt; len(s); i++ {\n\t\tswitch s[i] {\n\t\tcase ',', '\"', '\\n':\n\t\t\tneed = true\n\t\t}\n\t}\n\tif !need {\n\t\treturn s\n\t}\n\tout := []byte{'\"'}\n\tfor i := 0; i &lt; len(s); i++ {\n\t\tif s[i] == '\"' {\n\t\t\tout = append(out, '\"', '\"')\n\t\t} else {\n\t\t\tout = append(out, s[i])\n\t\t}\n\t}\n\tout = append(out, '\"')\n\treturn string(out)\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>Quote(\"a,b\"): comma found -&gt; wrap: '\"' + a,b + '\"' = \"a,b\" quoted.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Doubling quotes must happen <em>before</em> wrapping, on the content only.</li>\n<li>A plain field is returned unchanged \u2014 don't over-quote.</li>\n<li>The real <code>encoding/csv</code> writer does this; here you learn the rule directly.</li>\n</ul>",
    "starter": "// Package csvfield quotes a single CSV field per RFC 4180.\npackage csvfield\n\n// Quote returns s as a CSV field: if s contains a comma, double-quote, or\n// newline, it is wrapped in double quotes with inner quotes doubled; otherwise\n// s is returned unchanged.\n//\n// TODO(candidate): implement.\nfunc Quote(s string) string {\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "csvfield.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/02-data-types/middle/epsiloneq": {
    "id": "01-language-basics/02-data-types/middle/epsiloneq",
    "title": "Float Tolerance Equality",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Float Tolerance Equality</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p><code>0.1 + 0.2 != 0.3</code> in binary floats. Comparing floats needs a tolerance, not</p>\n<p><code>==</code>.</p>\n<h2>Task</h2>\n<p>Implement <code>Equal(a, b, eps)</code> = true when <code>|a-b| &lt;= eps</code>; NaN never equal.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Equal(0.1+0.2, 0.3, 1e-9)\nOutput: true\n</code></pre>\n<p>_Explanation:_ float rounding diff &lt; eps</p>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Equal(1.0, 1.001, 1e-9)\nOutput: false\n</code></pre>\n<p>_Explanation:_ difference 0.001 exceeds eps</p>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Equal(NaN, NaN, 1)\nOutput: false\n</code></pre>\n<p>_Explanation:_ NaN never equals anything</p>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Float inexactness</span><span class=\"tchip\">Absolute tolerance</span><span class=\"tchip\">NaN</span></div></details>",
    "education": "<h1>Comparing floats</h1>\n<h2>Intuition</h2>\n<p>Binary floats cannot represent most decimals exactly, so <code>0.1+0.2</code> is</p>\n<p><code>0.30000000000000004</code>. Compare with a tolerance:</p>\n<pre class=\"md\"><code>\nmath.Abs(a-b) &lt;= eps\n</code></pre>\n<h2>Approach</h2>\n<p>1. If either a or b is NaN, return false. 2. Otherwise return math.Abs(a-b) &lt;= eps.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nimport \"math\"\n\nfunc Equal(a, b, eps float64) bool {\n\tif math.IsNaN(a) || math.IsNaN(b) {\n\t\treturn false\n\t}\n\treturn math.Abs(a-b) &lt;= eps\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>Equal(0.1+0.2,0.3,1e-9): |0.30000000000000004-0.3| ~ 5.5e-17 &lt;= 1e-9 -&gt; true.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>If <code>a</code> or <code>b</code> is NaN, <code>a-b</code> is NaN and <code>NaN &lt;= eps</code> is false \u2014 so NaN is</li>\n</ul>\n<p>  correctly reported unequal, for free.</p>\n<ul>\n<li>Absolute tolerance fails across very different magnitudes; relative tolerance</li>\n</ul>\n<p>  (<code>|a-b| &lt;= eps*max(|a|,|b|)</code>) scales better.</p>\n<ul>\n<li><code>eps == 0</code> reduces to exact equality (fine for identical values).</li>\n</ul>",
    "starter": "// Package epsiloneq compares floats within a tolerance.\npackage epsiloneq\n\n// Equal reports whether a and b are within eps of each other (absolute\n// tolerance). NaN is never equal to anything.\n//\n// TODO(candidate): implement using math.Abs, guarding against NaN.\nfunc Equal(a, b, eps float64) bool {\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "epsiloneq.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/02-data-types/middle/gcd": {
    "id": "01-language-basics/02-data-types/middle/gcd",
    "title": "Greatest Common Divisor",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Greatest Common Divisor</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>The Euclidean algorithm repeatedly replaces the pair <code>(a, b)</code> with <code>(b, a%b)</code></p>\n<p>until <code>b</code> is 0. Signs must be normalized so the result is non-negative.</p>\n<h2>Task</h2>\n<p>Implement <code>GCD(a, b)</code> (non-negative, <code>GCD(0,0)=0</code>).</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  GCD(12, 8)\nOutput: 4\n</code></pre>\n<p>_Explanation:_ Euclid: 12%8=4, 8%4=0 -&gt; 4</p>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  GCD(0, 9)\nOutput: 9\n</code></pre>\n<p>_Explanation:_ gcd with 0 is the other value</p>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  GCD(-12, 8)\nOutput: 4\n</code></pre>\n<p>_Explanation:_ absolute values used, result non-negative</p>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Modulo loop</span><span class=\"tchip\">Sign handling</span><span class=\"tchip\">Multiple assignment</span></div></details>",
    "education": "<h1>The Euclidean algorithm</h1>\n<h2>Intuition</h2>\n<p><code>gcd(a,b) == gcd(b, a mod b)</code>, and <code>gcd(a,0) == a</code>. Iterating that recurrence</p>\n<p>shrinks the pair fast until the second term hits 0:</p>\n<pre class=\"md\"><code>\nfor b != 0 { a, b = b, a%b }\nreturn a // (abs)\n</code></pre>\n<h2>Approach</h2>\n<p>1. Take absolute values of a and b. 2. Loop while b!=0: (a,b) = (b, a%b). 3. Return a.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc GCD(a, b int) int {\n\tif a &lt; 0 {\n\t\ta = -a\n\t}\n\tif b &lt; 0 {\n\t\tb = -b\n\t}\n\tfor b != 0 {\n\t\ta, b = b, a%b\n\t}\n\treturn a\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>GCD(12,8): a,b=12,8 -&gt; 8,4 -&gt; 4,0. b==0, return 4.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Normalize signs: <code>%</code> can be negative in Go, so take <code>abs</code> of the result (or of</li>\n</ul>\n<p>  the inputs).</p>\n<ul>\n<li><code>GCD(0,0)</code> is defined here as 0.</li>\n<li>Multiple assignment <code>a, b = b, a%b</code> evaluates the right side before assigning.</li>\n</ul>",
    "starter": "// Package gcd computes the greatest common divisor.\npackage gcd\n\n// GCD returns the greatest common divisor of a and b using the Euclidean\n// algorithm. GCD(0,0) is 0; results are non-negative.\n//\n// TODO(candidate): implement with the modulo-based Euclidean loop.\nfunc GCD(a, b int) int {\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "gcd.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/02-data-types/middle/hexencode": {
    "id": "01-language-basics/02-data-types/middle/hexencode",
    "title": "Hex Encode",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Hex Encode</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>Each byte becomes two hex chars: the high nibble <code>b&gt;&gt;4</code> and the low nibble</p>\n<p><code>b&amp;0x0f</code>, each mapped through <code>\"0123456789abcdef\"</code>.</p>\n<h2>Task</h2>\n<p>Implement <code>Encode(b)</code> (lowercase, two chars/byte). No <code>encoding/hex</code>.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Encode([]byte{0xFF})\nOutput: \"ff\"\n</code></pre>\n<p>_Explanation:_ high nibble f, low nibble f</p>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Encode([]byte{0x1a,0x2b})\nOutput: \"1a2b\"\n</code></pre>\n<p>_Explanation:_ two bytes -&gt; four hex chars</p>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Encode([]byte(\"Go\"))\nOutput: \"476f\"\n</code></pre>\n<p>_Explanation:_ 'G'=0x47, 'o'=0x6f</p>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Nibble split</span><span class=\"tchip\">Digit table</span><span class=\"tchip\">Builder</span></div></details>",
    "education": "<h1>Hex encoding</h1>\n<h2>Intuition</h2>\n<p>A byte is two 4-bit nibbles. Split them and map each through a digit table:</p>\n<pre class=\"md\"><code>\nconst hexd = \"0123456789abcdef\"\nhi := hexd[b&gt;&gt;4]\nlo := hexd[b&amp;0x0f]\n</code></pre>\n<h2>Approach</h2>\n<p>1. For each byte, take the high nibble (c&gt;&gt;4) and low nibble (c&amp;0x0f). 2. Index \"0123456789abcdef\" for each. 3. Append both chars.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Encode(b []byte) string {\n\tconst digits = \"0123456789abcdef\"\n\tout := make([]byte, 0, len(b)*2)\n\tfor _, c := range b {\n\t\tout = append(out, digits[c&gt;&gt;4], digits[c&amp;0x0f])\n\t}\n\treturn string(out)\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>Encode([0x1a]): 0x1a&gt;&gt;4=1-&gt;'1', 0x1a&amp;0xf=10-&gt;'a'. \"1a\".</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Mask the low nibble with <code>&amp; 0x0f</code>; <code>&gt;&gt;4</code> already drops the low bits for the</li>\n</ul>\n<p>  high one.</p>\n<ul>\n<li>Build with a <code>[]byte</code>/<code>strings.Builder</code> and convert once \u2014 not <code>+=</code> per byte.</li>\n<li>Output is fixed two chars per byte, including leading zeros (<code>0x00</code> \u2192 \"00\").</li>\n</ul>",
    "starter": "// Package hexencode encodes bytes as a lowercase hex string.\npackage hexencode\n\n// Encode returns the lowercase hex encoding of b (two chars per byte).\n//\n// TODO(candidate): implement without encoding/hex; map each nibble to 0-9a-f.\nfunc Encode(b []byte) string {\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "hexencode.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/02-data-types/middle/itoa": {
    "id": "01-language-basics/02-data-types/middle/itoa",
    "title": "Manual Itoa",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Manual Itoa</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>Formatting is the inverse of parsing: peel digits with <code>%10</code>, prepend <code>'0'+d</code>,</p>\n<p>divide by 10, reverse.</p>\n<h2>Task</h2>\n<p>Implement <code>Format(n)</code> (decimal, leading <code>-</code> for negatives). No <code>strconv</code>.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Format(42)\nOutput: \"42\"\n</code></pre>\n<p>_Explanation:_ digits extracted then reversed</p>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Format(-17)\nOutput: \"-17\"\n</code></pre>\n<p>_Explanation:_ negative gets a leading '-'</p>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Format(0)\nOutput: \"0\"\n</code></pre>\n<p>_Explanation:_ zero special case</p>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Digit extraction</span><span class=\"tchip\">Char mapping</span><span class=\"tchip\">Sign + reverse</span></div></details>",
    "education": "<h1>Formatting integers by hand</h1>\n<h2>Intuition</h2>\n<p>Extract digits from the right with <code>n%10</code>, map each to a character with</p>\n<p><code>byte('0'+d)</code>, divide by 10, and reverse:</p>\n<pre class=\"md\"><code>\nfor n &gt; 0 { buf = append(buf, byte('0'+n%10)); n /= 10 }\n// reverse\n</code></pre>\n<h2>Approach</h2>\n<p>1. n==0 -&gt; \"0\". 2. Track sign; work in the negative range (n = -n for positives) so math.MinInt won't overflow. 3. Extract digits via '0'-n%10 while n&lt;0. 4. Append '-' if negative, then reverse.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Format(n int) string {\n\tif n == 0 {\n\t\treturn \"0\"\n\t}\n\tneg := n &lt; 0\n\tif !neg {\n\t\tn = -n\n\t}\n\tvar buf []byte\n\tfor n &lt; 0 {\n\t\tbuf = append(buf, byte('0'-n%10))\n\t\tn /= 10\n\t}\n\tif neg {\n\t\tbuf = append(buf, '-')\n\t}\n\tfor i, j := 0, len(buf)-1; i &lt; j; i, j = i+1, j-1 {\n\t\tbuf[i], buf[j] = buf[j], buf[i]\n\t}\n\treturn string(buf)\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>Format(-17): neg. digits from -17: 7,1 -&gt; buf \"71\", append '-' -&gt; \"71-\", reverse -&gt; \"-17\".</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>Format(0)</code> must special-case to <code>\"0\"</code>, else the loop produces \"\".</li>\n<li>Taking <code>abs(n)</code> risks overflow for the most-negative int; a robust version</li>\n</ul>\n<p>  works in the negative domain instead.</p>\n<ul>\n<li>Digits come out reversed \u2014 remember to flip the buffer.</li>\n</ul>",
    "starter": "// Package itoa formats an int as a decimal string by hand.\npackage itoa\n\n// Format returns the base-10 string for n, including a leading '-' for\n// negatives. Format(0) is \"0\".\n//\n// TODO(candidate): implement without strconv.\nfunc Format(n int) string {\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "itoa.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/02-data-types/middle/lerp": {
    "id": "01-language-basics/02-data-types/middle/lerp",
    "title": "Linear Interpolation",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Linear Interpolation</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>Lerp blends two values by a factor <code>t</code>: at <code>t=0</code> you get <code>a</code>, at <code>t=1</code> you get</p>\n<p><code>b</code>.</p>\n<h2>Task</h2>\n<p>Implement <code>Lerp(a, b, t)</code> = <code>a + (b-a)*t</code>.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Lerp(0, 10, 0.5)\nOutput: 5\n</code></pre>\n<p>_Explanation:_ halfway between 0 and 10</p>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Lerp(2, 4, 0.25)\nOutput: 2.5\n</code></pre>\n<p>_Explanation:_ a quarter of the way from 2 to 4</p>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Lerp(0, 10, 1)\nOutput: 10\n</code></pre>\n<p>_Explanation:_ t=1 gives b</p>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Interpolation formula</span><span class=\"tchip\">Float arithmetic</span><span class=\"tchip\">Endpoints</span></div></details>",
    "education": "<h1>Linear interpolation</h1>\n<h2>Intuition</h2>\n<p><code>Lerp(a,b,t) = a + (b-a)*t</code> walks a straight line from <code>a</code> (at t=0) to <code>b</code> (at</p>\n<p>t=1). Values of <code>t</code> outside [0,1] extrapolate.</p>\n<h2>Approach</h2>\n<p>1. Return a + (b-a)*t.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Lerp(a, b, t float64) float64 {\n\treturn a + (b-a)*t\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>Lerp(2,4,0.25): 2 + (4-2)*0.25 = 2 + 0.5 = 2.5.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>a<em>(1-t) + b</em>t</code> is algebraically equal but can miss <code>b</code> at t=1 due to rounding.</li>\n<li>No clamping here: <code>t=2</code> extrapolates beyond <code>b</code>.</li>\n<li>Keep everything float64; integer operands would truncate.</li>\n</ul>",
    "starter": "// Package lerp does linear interpolation.\npackage lerp\n\n// Lerp returns the value t of the way from a to b (t in [0,1] typically):\n// a + (b-a)*t.\n//\n// TODO(candidate): implement.\nfunc Lerp(a, b, t float64) float64 {\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "lerp.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/02-data-types/middle/luhn": {
    "id": "01-language-basics/02-data-types/middle/luhn",
    "title": "Luhn Checksum",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Luhn Checksum</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>Payment forms pre-validate card numbers with the Luhn checksum before hitting the</p>\n<p>gateway \u2014 cheap client-side rejection of typos.</p>\n<h2>Task</h2>\n<p>Implement <code>Valid(s)</code>: double every second digit from the right (subtract 9 if</p>\n<p>&gt;9), sum all, and require the total <code>% 10 == 0</code>. Non-digits/empty \u2192 false.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Valid(\"79927398713\")\nOutput: true\n</code></pre>\n<p>_Explanation:_ classic valid Luhn number</p>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Valid(\"79927398710\")\nOutput: false\n</code></pre>\n<p>_Explanation:_ checksum not divisible by 10</p>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Valid(\"1234a\")\nOutput: false\n</code></pre>\n<p>_Explanation:_ non-digit -&gt; invalid</p>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Right-to-left scan</span><span class=\"tchip\">Digit value</span><span class=\"tchip\">Checksum mod 10</span></div></details>",
    "education": "<h1>The Luhn algorithm</h1>\n<h2>Intuition</h2>\n<p>Scanning right to left, leave the odd positions alone and double the even ones;</p>\n<p>if a doubled digit exceeds 9, subtract 9 (same as summing its digits). The number</p>\n<p>is valid when the grand total is divisible by 10.</p>\n<h2>Approach</h2>\n<p>1. Empty -&gt; false. 2. Walk right to left, doubling every second digit. 3. If a doubled digit exceeds 9, subtract 9. 4. Sum all digits. 5. Valid iff sum%10==0. Non-digit chars are invalid.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Valid(s string) bool {\n\tif s == \"\" {\n\t\treturn false\n\t}\n\tsum := 0\n\tdouble := false\n\tfor i := len(s) - 1; i &gt;= 0; i-- {\n\t\tc := s[i]\n\t\tif c &lt; '0' || c &gt; '9' {\n\t\t\treturn false\n\t\t}\n\t\td := int(c - '0')\n\t\tif double {\n\t\t\td *= 2\n\t\t\tif d &gt; 9 {\n\t\t\t\td -= 9\n\t\t\t}\n\t\t}\n\t\tsum += d\n\t\tdouble = !double\n\t}\n\treturn sum%10 == 0\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>Valid(\"79927398713\"): summing with alternating doubling gives 70; 70%10==0 -&gt; true.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Parity is by position <strong>from the right</strong>, so reverse or index carefully.</li>\n<li>\"Subtract 9\" is equivalent to summing the two digits of the doubled value.</li>\n<li>Reject non-digit characters up front; a stray space or letter is invalid.</li>\n</ul>",
    "starter": "// Package luhn validates a number string with the Luhn checksum.\npackage luhn\n\n// Valid reports whether the digit string s passes the Luhn check (used by\n// credit cards). Non-digit characters or empty input make it invalid.\n//\n// TODO(candidate): implement the Luhn algorithm.\nfunc Valid(s string) bool {\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "luhn.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/02-data-types/middle/moneyparse": {
    "id": "01-language-basics/02-data-types/middle/moneyparse",
    "title": "Parse Money to Cents",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Parse Money to Cents</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>A billing service must never store money as a float. You parse the user's</p>\n<p><code>\"12.34\"</code> straight into integer cents, exactly.</p>\n<h2>Task</h2>\n<p>Implement <code>Cents(s)</code> \u2192 integer cents; <code>\"7\"</code>\u2192700, <code>\"3.5\"</code>\u2192350, <code>\"12.34\"</code>\u21921234.</p>\n<p>Reject &gt;2 decimals or bad format.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Cents(\"12.34\")\nOutput: (1234, true)\n</code></pre>\n<p>_Explanation:_ 12 dollars 34 cents</p>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Cents(\"3.5\")\nOutput: (350, true)\n</code></pre>\n<p>_Explanation:_ one decimal padded to 50 cents</p>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Cents(\"7\")\nOutput: (700, true)\n</code></pre>\n<p>_Explanation:_ no decimals -&gt; whole dollars</p>\n<p><strong>Example 4:</strong></p>\n<pre class=\"md\"><code>\nInput:  Cents(\"1.234\")\nOutput: (0, false)\n</code></pre>\n<p>_Explanation:_ more than two decimals rejected</p>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Integer money</span><span class=\"tchip\">Optional fraction</span><span class=\"tchip\">Validation</span></div></details>",
    "education": "<h1>Money as integer minor units</h1>\n<h2>Intuition</h2>\n<p>Represent currency as an integer number of cents, never a float. Parse the</p>\n<p>dollars and the (optional) 1\u20132 fraction digits separately, pad the fraction to</p>\n<p>two places, and combine: <code>dollars*100 + cents</code>.</p>\n<h2>Approach</h2>\n<p>1. Empty -&gt; (0,false). 2. Scan digits into dollars until a '.' or end. 3. If a '.' follows, the fraction must be 1 or 2 digits (else false). 4. A single fractional digit is scaled <em>10. 5. All chars must be digits. 6. Return dollars</em>100 + cents.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Cents(s string) (int, bool) {\n\tif s == \"\" {\n\t\treturn 0, false\n\t}\n\ti := 0\n\tdollars := 0\n\tsawDigit := false\n\tfor ; i &lt; len(s) &amp;&amp; s[i] != '.'; i++ {\n\t\tc := s[i]\n\t\tif c &lt; '0' || c &gt; '9' {\n\t\t\treturn 0, false\n\t\t}\n\t\tdollars = dollars*10 + int(c-'0')\n\t\tsawDigit = true\n\t}\n\tcents := 0\n\tif i &lt; len(s) {\n\t\ti++\n\t\tfrac := s[i:]\n\t\tif len(frac) == 0 || len(frac) &gt; 2 {\n\t\t\treturn 0, false\n\t\t}\n\t\tfor j := 0; j &lt; len(frac); j++ {\n\t\t\tc := frac[j]\n\t\t\tif c &lt; '0' || c &gt; '9' {\n\t\t\t\treturn 0, false\n\t\t\t}\n\t\t\tcents = cents*10 + int(c-'0')\n\t\t}\n\t\tif len(frac) == 1 {\n\t\t\tcents *= 10\n\t\t}\n\t\tsawDigit = true\n\t}\n\tif !sawDigit {\n\t\treturn 0, false\n\t}\n\treturn dollars*100 + cents, true\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>Cents(\"3.5\"): dollars=3, frac=\"5\" len 1 -&gt; cents=5<em>10=50. 3</em>100+50=350 -&gt; (350,true).</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>\"3.5\"</code> means 350 cents \u2014 pad a single fraction digit to two.</li>\n<li>Reject 3+ fraction digits rather than silently rounding.</li>\n<li>No <code>.</code> at all is valid (\"7\" \u2192 700); a trailing <code>.</code> is not.</li>\n</ul>",
    "starter": "// Package moneyparse parses a dollar string into integer cents.\npackage moneyparse\n\n// Cents parses a string like \"12.34\" or \"0.05\" or \"7\" into the number of cents\n// (1234, 5, 700). Returns (0,false) on bad format or more than two decimals.\n//\n// TODO(candidate): implement by scanning; keep exact integer cents, no float.\nfunc Cents(s string) (int, bool) {\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "moneyparse.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/02-data-types/middle/parsehex": {
    "id": "01-language-basics/02-data-types/middle/parsehex",
    "title": "Parse Hex",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Parse Hex</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>Fold hex digits into a value: <code>n = n*16 + d</code>, where <code>d</code> comes from <code>'0'..'9'</code>,</p>\n<p><code>'a'..'f'</code>, or <code>'A'..'F'</code>.</p>\n<h2>Task</h2>\n<p>Implement <code>Parse(s)</code> (no <code>0x</code>), returning <code>(value, ok)</code>; false on non-hex/empty.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Parse(\"ff\")\nOutput: (255, true)\n</code></pre>\n<p>_Explanation:_ 15*16+15</p>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Parse(\"1A2B\")\nOutput: (6699, true)\n</code></pre>\n<p>_Explanation:_ uppercase accepted</p>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Parse(\"1g\")\nOutput: (0, false)\n</code></pre>\n<p>_Explanation:_ 'g' is not a hex digit</p>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Hex digit value</span><span class=\"tchip\">Horner fold</span><span class=\"tchip\">Case folding</span></div></details>",
    "education": "<h1>Parsing hex</h1>\n<h2>Intuition</h2>\n<p>Each hex digit contributes 4 bits: <code>n = n*16 + d</code>. The digit value <code>d</code> depends on</p>\n<p>which of three contiguous ranges the character falls in:</p>\n<pre class=\"md\"><code>\nswitch {\ncase c &gt;= '0' &amp;&amp; c &lt;= '9': d = int(c - '0')\ncase c &gt;= 'a' &amp;&amp; c &lt;= 'f': d = int(c-'a') + 10\ncase c &gt;= 'A' &amp;&amp; c &lt;= 'F': d = int(c-'A') + 10\ndefault: return 0, false\n}\n</code></pre>\n<h2>Approach</h2>\n<p>1. Empty -&gt; (0,false). 2. Map each char: '0'-'9' -&gt; 0-9, 'a'-'f'/'A'-'F' -&gt; 10-15, else invalid. 3. Accumulate n = n*16 + v.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Parse(s string) (int, bool) {\n\tif s == \"\" {\n\t\treturn 0, false\n\t}\n\tn := 0\n\tfor i := 0; i &lt; len(s); i++ {\n\t\tc := s[i]\n\t\tvar v int\n\t\tswitch {\n\t\tcase c &gt;= '0' &amp;&amp; c &lt;= '9':\n\t\t\tv = int(c - '0')\n\t\tcase c &gt;= 'a' &amp;&amp; c &lt;= 'f':\n\t\t\tv = int(c-'a') + 10\n\t\tcase c &gt;= 'A' &amp;&amp; c &lt;= 'F':\n\t\t\tv = int(c-'A') + 10\n\t\tdefault:\n\t\t\treturn 0, false\n\t\t}\n\t\tn = n*16 + v\n\t}\n\treturn n, true\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>Parse(\"ff\"): 'f'=15 -&gt; n=15; 'f'=15 -&gt; n=15*16+15=255. (255,true).</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Handle both letter cases.</li>\n<li><code>n*16</code> can overflow for long inputs; this simplified version ignores that.</li>\n<li>Reject empty input explicitly, or it returns <code>(0, true)</code>.</li>\n</ul>",
    "starter": "// Package parsehex parses a hex string into an int.\npackage parsehex\n\n// Parse converts a lowercase/uppercase hex string (no 0x prefix) to an int.\n// Returns (0, false) on any non-hex character or empty input.\n//\n// TODO(candidate): implement digit-by-digit (value 0-15 per char).\nfunc Parse(s string) (int, bool) {\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "parsehex.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/02-data-types/middle/popcount": {
    "id": "01-language-basics/02-data-types/middle/popcount",
    "title": "Population Count",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Population Count</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>Counting set bits (popcount) is a bit-manipulation staple. The trick</p>\n<p><code>x &amp;= x-1</code> clears the lowest set bit, so the loop runs once per 1-bit.</p>\n<h2>Task</h2>\n<p>Implement <code>Count(x)</code> returning the number of 1-bits.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Count(0b1011)\nOutput: 3\n</code></pre>\n<p>_Explanation:_ three set bits</p>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Count(0xFF)\nOutput: 8\n</code></pre>\n<p>_Explanation:_ all low 8 bits set</p>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Count(^uint64(0))\nOutput: 64\n</code></pre>\n<p>_Explanation:_ every bit set</p>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Bit AND</span><span class=\"tchip\">Loop bound</span><span class=\"tchip\">Unsigned shift</span></div></details>",
    "education": "<h1>Counting set bits</h1>\n<h2>Intuition</h2>\n<p><code>x &amp; (x-1)</code> clears the <strong>lowest</strong> set bit of <code>x</code>. Repeat until zero and you have</p>\n<p>counted exactly the set bits:</p>\n<pre class=\"md\"><code>\nfor x != 0 { x &amp;= x - 1; n++ }\n</code></pre>\n<p>Subtracting 1 flips the lowest 1 to 0 and all bits below it to 1; ANDing with</p>\n<p>the original keeps only the higher bits.</p>\n<h2>Approach</h2>\n<p>1. Loop while x!=0. 2. Clear the lowest set bit with x &amp;= x-1. 3. Count each clear. The loop runs once per set bit.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Count(x uint64) int {\n\tn := 0\n\tfor x != 0 {\n\t\tx &amp;= x - 1\n\t\tn++\n\t}\n\treturn n\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>Count(0b1011): 1011-&gt;1010-&gt;1000-&gt;0000, 3 iterations -&gt; 3.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Use an unsigned type so the right shift alternative is logical, not</li>\n</ul>\n<p>  arithmetic.</p>\n<ul>\n<li>The stdlib <code>math/bits.OnesCount64</code> does this in one instruction \u2014 hand-rolling</li>\n</ul>\n<p>  is for understanding.</p>\n<ul>\n<li><code>x-1</code> on <code>x==0</code> underflows to all-ones, but the loop guard <code>x != 0</code> prevents</li>\n</ul>\n<p>  entering with 0.</p>",
    "starter": "// Package popcount counts set bits.\npackage popcount\n\n// Count returns the number of 1-bits in x.\n//\n// TODO(candidate): implement (clear lowest set bit each step, or shift).\nfunc Count(x uint64) int {\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "popcount.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/02-data-types/middle/reverserunes": {
    "id": "01-language-basics/02-data-types/middle/reverserunes",
    "title": "Reverse by Runes",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Reverse by Runes</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>Reversing bytes corrupts multi-byte UTF-8 characters. Convert to <code>[]rune</code> first,</p>\n<p>reverse, convert back.</p>\n<h2>Task</h2>\n<p>Implement <code>Reverse(s)</code> reversing runes, keeping characters intact.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Reverse(\"hello\")\nOutput: \"olleh\"\n</code></pre>\n<p>_Explanation:_ ASCII reversal</p>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Reverse(\"caf\u00e9\")\nOutput: \"\u00e9fac\"\n</code></pre>\n<p>_Explanation:_ \u00e9 kept intact (rune-aware)</p>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Reverse(\"\u65e5\u672c\u8a9e\")\nOutput: \"\u8a9e\u672c\u65e5\"\n</code></pre>\n<p>_Explanation:_ multi-byte kanji reversed as whole runes</p>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">[]rune(s)</span><span class=\"tchip\">Two-pointer swap</span><span class=\"tchip\">string([]rune)</span></div></details>",
    "education": "<h1>Reversing Unicode text</h1>\n<h2>Intuition</h2>\n<p>Reversing the <strong>bytes</strong> of \"caf\u00e9\" splits the two-byte <code>\u00e9</code> and produces invalid</p>\n<p>UTF-8. Convert to a <code>[]rune</code> (one element per character), reverse that, and</p>\n<p>convert back:</p>\n<pre class=\"md\"><code>\nr := []rune(s)\nfor i, j := 0, len(r)-1; i &lt; j; i, j = i+1, j-1 { r[i], r[j] = r[j], r[i] }\nreturn string(r)\n</code></pre>\n<h2>Approach</h2>\n<p>1. Convert to []rune so multi-byte characters are single elements. 2. Two-pointer swap from ends toward middle. 3. Convert back to string.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Reverse(s string) string {\n\tr := []rune(s)\n\tfor i, j := 0, len(r)-1; i &lt; j; i, j = i+1, j-1 {\n\t\tr[i], r[j] = r[j], r[i]\n\t}\n\treturn string(r)\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>Reverse(\"caf\u00e9\"): runes [c,a,f,\u00e9] -&gt; swap to [\u00e9,f,a,c] -&gt; \"\u00e9fac\".</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>[]rune(s)</code> allocates and decodes; fine here, wasteful in tight loops.</li>\n<li>This reverses code points, not grapheme clusters \u2014 combining marks or emoji ZWJ</li>\n</ul>\n<p>  sequences can still reorder visibly (a deeper Unicode topic).</p>\n<ul>\n<li><code>len([]rune(s))</code> is the rune count, unlike <code>len(s)</code>.</li>\n</ul>",
    "starter": "// Package reverserunes reverses a string by runes (not bytes).\npackage reverserunes\n\n// Reverse returns s with its runes in reverse order, keeping multi-byte\n// characters intact.\n//\n// TODO(candidate): implement via []rune.\nfunc Reverse(s string) string {\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "reverserunes.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/02-data-types/middle/rotate": {
    "id": "01-language-basics/02-data-types/middle/rotate",
    "title": "Circular Bit Rotation",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Circular Bit Rotation</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>A rotate is a shift where the bits that fall off one end wrap to the other. For</p>\n<p>a byte, rotating by <code>n</code> is really rotating by <code>n % 8</code>.</p>\n<h2>Task</h2>\n<p>Implement <code>Left(b, n)</code> rotating the 8 bits of <code>b</code> left by <code>n</code> (n may be \u2265 8).</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Left(0b1000_0000, 1)\nOutput: 0b0000_0001\n</code></pre>\n<p>_Explanation:_ top bit wraps to bottom</p>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Left(0b1010_0000, 4)\nOutput: 0b0000_1010\n</code></pre>\n<p>_Explanation:_ nibbles swapped</p>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Left(0b0000_0001, 9)\nOutput: 0b0000_0010\n</code></pre>\n<p>_Explanation:_ n masked to 9&amp;7=1</p>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Shift + OR</span><span class=\"tchip\">Modulo width</span><span class=\"tchip\">byte width</span></div></details>",
    "education": "<h1>Bit rotation</h1>\n<h2>Intuition</h2>\n<p>Rotating left by <code>n</code> moves each bit up <code>n</code> places and wraps the top <code>n</code> bits to</p>\n<p>the bottom. For an 8-bit value:</p>\n<pre class=\"md\"><code>\nn &amp;= 7\nb&lt;&lt;n | b&gt;&gt;(8-n)\n</code></pre>\n<p>The <code>&lt;&lt;n</code> moves bits up (the high ones drop off, and <code>byte</code> truncates them); the</p>\n<p><code>&gt;&gt;(8-n)</code> brings those same high bits back in at the bottom.</p>\n<h2>Approach</h2>\n<p>1. Mask n to 0..7 with n&amp;=7. 2. If n==0 return b unchanged (avoids shifting by 8). 3. Otherwise b&lt;&lt;n | b&gt;&gt;(8-n) recombines the wrapped bits.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Left(b byte, n int) byte {\n\tn &amp;= 7\n\tif n == 0 {\n\t\treturn b\n\t}\n\treturn b&lt;&lt;n | b&gt;&gt;(8-n)\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>Left(0b1000_0000,1): n=1. b&lt;&lt;1=0 (top bit off), b&gt;&gt;7=1. 0|1 = 0b0000_0001.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Reduce <code>n</code> modulo the width first, or <code>8-n</code> goes wrong for <code>n&gt;=8</code>.</li>\n<li>With <code>n==0</code> after masking, <code>b&gt;&gt;8</code> is 0 in Go (shift \u2265 width yields 0 for</li>\n</ul>\n<p>  unsigned), so the formula still returns <code>b</code>.</p>\n<ul>\n<li>Use an unsigned type; rotating signed values sign-extends.</li>\n</ul>",
    "starter": "// Package rotate does circular bit rotation on a byte.\npackage rotate\n\n// Left rotates the 8 bits of b left by n positions (n may be >= 8). Bits shifted\n// off the top re-enter at the bottom.\n//\n// TODO(candidate): implement with shifts, masking n to 0..7.\nfunc Left(b byte, n int) byte {\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "rotate.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/02-data-types/middle/semverparse": {
    "id": "01-language-basics/02-data-types/middle/semverparse",
    "title": "Semver Parse",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Semver Parse</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>A deploy tool reads image tags like <code>1.4.10</code> and needs the three numeric parts</p>\n<p>to compare releases. You parse the string into integers, rejecting malformed</p>\n<p>tags.</p>\n<h2>Task</h2>\n<p>Implement <code>Parse(s)</code> returning <code>(major, minor, patch, ok)</code>; <code>ok=false</code> unless the</p>\n<p>string is exactly three dot-separated non-negative integers.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Parse(\"1.4.10\")\nOutput: (1, 4, 10, true)\n</code></pre>\n<p>_Explanation:_ three integer parts</p>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Parse(\"2.0\")\nOutput: (0, 0, 0, false)\n</code></pre>\n<p>_Explanation:_ only two parts</p>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Parse(\"1.x.0\")\nOutput: (0, 0, 0, false)\n</code></pre>\n<p>_Explanation:_ non-digit component</p>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Byte scanning</span><span class=\"tchip\">Digit fold</span><span class=\"tchip\">Strict validation</span></div></details>",
    "education": "<h1>Field-by-field numeric parsing</h1>\n<h2>Intuition</h2>\n<p>Parse a structured numeric string in one pass: accumulate digits with</p>\n<p><code>n = n*10 + (c-'0')</code>, and on a separator flush the field and move on. Validate</p>\n<p>strictly \u2014 count fields, forbid empty ones and stray characters.</p>\n<h2>Approach</h2>\n<p>1. Scan bytes, accumulating the current integer. 2. On '.', store the finished part, require at least one digit and fewer than 3 parts so far. 3. Non-digit chars are invalid. 4. At end require exactly two dots seen (idx==2) and a trailing digit.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Parse(s string) (major, minor, patch int, ok bool) {\n\tparts := [3]int{}\n\tidx := 0\n\tdigits := 0\n\tcur := 0\n\tfor i := 0; i &lt; len(s); i++ {\n\t\tc := s[i]\n\t\tif c == '.' {\n\t\t\tif digits == 0 || idx &gt;= 2 {\n\t\t\t\treturn 0, 0, 0, false\n\t\t\t}\n\t\t\tparts[idx] = cur\n\t\t\tidx++\n\t\t\tcur = 0\n\t\t\tdigits = 0\n\t\t\tcontinue\n\t\t}\n\t\tif c &lt; '0' || c &gt; '9' {\n\t\t\treturn 0, 0, 0, false\n\t\t}\n\t\tcur = cur*10 + int(c-'0')\n\t\tdigits++\n\t}\n\tif idx != 2 || digits == 0 {\n\t\treturn 0, 0, 0, false\n\t}\n\tparts[2] = cur\n\treturn parts[0], parts[1], parts[2], true\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>Parse(\"1.4.10\"): cur=1 at first '.' -&gt; parts[0]=1; cur=4 -&gt; parts[1]=4; cur=10 at end -&gt; parts[2]=10. (1,4,10,true).</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Reject an empty field (<code>1..2</code>) and a trailing dot.</li>\n<li>Enforce the exact field count; <code>1.2</code> and <code>1.2.3.4</code> are both invalid here.</li>\n<li>A leading <code>-</code> or letters must fail \u2014 only <code>'0'..'9'</code> and <code>.</code> are valid.</li>\n</ul>",
    "starter": "// Package semverparse parses a \"major.minor.patch\" version string.\npackage semverparse\n\n// Parse splits a version like \"1.4.10\" into its three integer components.\n// Returns (0,0,0,false) if the format is not exactly three dot-separated\n// non-negative integers.\n//\n// TODO(candidate): implement without strings.Split or strconv \u2014 scan bytes.\nfunc Parse(s string) (major, minor, patch int, ok bool) {\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "semverparse.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/02-data-types/middle/slugify": {
    "id": "01-language-basics/02-data-types/middle/slugify",
    "title": "URL Slug",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">URL Slug</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>A CMS turns article titles into URL slugs: lowercase, alphanumerics kept,</p>\n<p>everything else collapsed to single dashes, edges trimmed.</p>\n<h2>Task</h2>\n<p>Implement <code>Slug(s)</code> per the rules above.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Slug(\"Hello, World!\")\nOutput: \"hello-world\"\n</code></pre>\n<p>_Explanation:_ lowercased, punctuation run -&gt; single dash, trailing '!' trimmed</p>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Slug(\"  Go 1.26  \")\nOutput: \"go-1-26\"\n</code></pre>\n<p>_Explanation:_ leading/trailing spaces trimmed, '.' becomes dash</p>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Slug(\"a---b\")\nOutput: \"a-b\"\n</code></pre>\n<p>_Explanation:_ run of non-alnum collapses to one dash</p>\n<p><strong>Example 4:</strong></p>\n<pre class=\"md\"><code>\nInput:  Slug(\"!!!\")\nOutput: \"\"\n</code></pre>\n<p>_Explanation:_ nothing alphanumeric -&gt; empty</p>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Rune classification</span><span class=\"tchip\">Run collapsing</span><span class=\"tchip\">Trim edges</span></div></details>",
    "education": "<h1>Normalizing text to a slug</h1>\n<h2>Intuition</h2>\n<p>Scan runes, keep alphanumerics (lowercased), and collapse every run of other</p>\n<p>characters into a single <code>-</code>. A neat trick avoids trailing dashes: instead of</p>\n<p>appending a dash on each separator, set a <em>pending</em> flag and only emit the dash</p>\n<p>right before the next kept character.</p>\n<h2>Approach</h2>\n<p>1. Scan bytes. 2. Uppercase A-Z lowered and appended; a-z/0-9 appended as-is. 3. Any other char emits a single '-' only if output is non-empty and the previous char wasn't already a dash (collapses runs, drops leading). 4. Trim trailing '-'.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Slug(s string) string {\n\tvar out []byte\n\tprevDash := false\n\tfor i := 0; i &lt; len(s); i++ {\n\t\tc := s[i]\n\t\tswitch {\n\t\tcase c &gt;= 'A' &amp;&amp; c &lt;= 'Z':\n\t\t\tout = append(out, c+('a'-'A'))\n\t\t\tprevDash = false\n\t\tcase (c &gt;= 'a' &amp;&amp; c &lt;= 'z') || (c &gt;= '0' &amp;&amp; c &lt;= '9'):\n\t\t\tout = append(out, c)\n\t\t\tprevDash = false\n\t\tdefault:\n\t\t\tif len(out) &gt; 0 &amp;&amp; !prevDash {\n\t\t\t\tout = append(out, '-')\n\t\t\t\tprevDash = true\n\t\t\t}\n\t\t}\n\t}\n\tfor len(out) &gt; 0 &amp;&amp; out[len(out)-1] == '-' {\n\t\tout = out[:len(out)-1]\n\t}\n\treturn string(out)\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>Slug(\"a---b\"): 'a'-&gt;a; '-'x3 -&gt; one '-' (prevDash guards rest); 'b'-&gt;b -&gt; \"a-b\".</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Lowercase with <code>unicode.ToLower</code> (or ASCII arithmetic).</li>\n<li>Don't emit a leading dash: the pending flag naturally handles this if you only</li>\n</ul>\n<p>  flush before real output.</p>\n<ul>\n<li>Decide how to treat non-ASCII letters (this spec keeps only ASCII alnum).</li>\n</ul>",
    "starter": "// Package slugify turns a title into a URL slug.\npackage slugify\n\n// Slug lowercases s, converts each run of non-alphanumeric ASCII to a single\n// '-', and trims leading/trailing '-'.\n//\n// TODO(candidate): implement with rune scanning.\nfunc Slug(s string) string {\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "slugify.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/02-data-types/middle/titlecase": {
    "id": "01-language-basics/02-data-types/middle/titlecase",
    "title": "Title Case",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Title Case</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>Title-casing upper-cases each word's first letter and lower-cases the rest \u2014</p>\n<p>case conversion on ASCII letters via arithmetic or <code>unicode</code> helpers.</p>\n<h2>Task</h2>\n<p>Implement <code>Title(s)</code> for space-separated words.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Title(\"hello world\")\nOutput: \"Hello World\"\n</code></pre>\n<p>_Explanation:_ first letter of each word capitalized</p>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Title(\"GO is FUN\")\nOutput: \"Go Is Fun\"\n</code></pre>\n<p>_Explanation:_ rest of each word lowercased</p>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Title(\"a\")\nOutput: \"A\"\n</code></pre>\n<p>_Explanation:_ single letter uppercased</p>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Case conversion</span><span class=\"tchip\">Word boundary</span><span class=\"tchip\">Building output</span></div></details>",
    "education": "<h1>Case conversion</h1>\n<h2>Intuition</h2>\n<p>ASCII upper/lower differ by one bit (0x20). Use <code>unicode.ToUpper</code> / <code>ToLower</code> for</p>\n<p>clarity, and track whether you are at the start of a word:</p>\n<pre class=\"md\"><code>\nstart := true\nfor _, r := range s {\n\tif r == ' ' { start = true; ...append space; continue }\n\tif start { append ToUpper(r) } else { append ToLower(r) }\n\tstart = false\n}\n</code></pre>\n<h2>Approach</h2>\n<p>1. Track atStart, true at string start and after each space. 2. At a word start, uppercase a-z. 3. Elsewhere in a word, lowercase A-Z. 4. Spaces reset atStart.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Title(s string) string {\n\tout := []byte(s)\n\tatStart := true\n\tfor i := 0; i &lt; len(out); i++ {\n\t\tc := out[i]\n\t\tif c == ' ' {\n\t\t\tatStart = true\n\t\t\tcontinue\n\t\t}\n\t\tif atStart {\n\t\t\tif c &gt;= 'a' &amp;&amp; c &lt;= 'z' {\n\t\t\t\tc -= 'a' - 'A'\n\t\t\t}\n\t\t\tatStart = false\n\t\t} else {\n\t\t\tif c &gt;= 'A' &amp;&amp; c &lt;= 'Z' {\n\t\t\t\tc += 'a' - 'A'\n\t\t\t}\n\t\t}\n\t\tout[i] = c\n\t}\n\treturn string(out)\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>Title(\"GO is FUN\"): 'G' start-&gt;G; 'O' mid-&gt;o; space; 'i'-&gt;I; 's'-&gt;s; space; 'F'-&gt;F; 'U'-&gt;u; 'N'-&gt;n -&gt; \"Go Is Fun\".</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>strings.Title</code> is deprecated (Unicode-incorrect for many scripts); this ASCII</li>\n</ul>\n<p>  exercise avoids that dependency.</p>\n<ul>\n<li>Build output with <code>strings.Builder</code> or <code>[]rune</code>, not by <code>+=</code> in a loop.</li>\n<li>Multiple/leading spaces change word boundaries \u2014 decide the spec.</li>\n</ul>",
    "starter": "// Package titlecase capitalizes the first letter of each space-separated word.\npackage titlecase\n\n// Title upper-cases the first ASCII letter of each word (runs separated by\n// single spaces) and lower-cases the rest.\n//\n// TODO(candidate): implement.\nfunc Title(s string) string {\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "titlecase.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/02-data-types/middle/utf8truncate": {
    "id": "01-language-basics/02-data-types/middle/utf8truncate",
    "title": "Byte-Safe Truncate",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Byte-Safe Truncate</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>A database column is <code>VARCHAR(N)</code> counted in <strong>bytes</strong>. Truncating UTF-8 text to</p>\n<p>fit must not cut a multi-byte character in half, or you store invalid UTF-8.</p>\n<h2>Task</h2>\n<p>Implement <code>Truncate(s, max)</code> = longest prefix \u2264 <code>max</code> bytes, never splitting a</p>\n<p>rune.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Truncate(\"hello\", 3)\nOutput: \"hel\"\n</code></pre>\n<p>_Explanation:_ plain ASCII cut at 3 bytes</p>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Truncate(\"h\u00e9llo\", 2)\nOutput: \"h\"\n</code></pre>\n<p>_Explanation:_ \u00e9 is 2 bytes; including it would exceed 2 so stop at h</p>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Truncate(\"\u65e5\u672c\", 4)\nOutput: \"\u65e5\"\n</code></pre>\n<p>_Explanation:_ each kanji is 3 bytes; only one fits in 4</p>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Byte vs rune width</span><span class=\"tchip\">range byte index</span><span class=\"tchip\">Prefix cut</span></div></details>",
    "education": "<h1>Truncating UTF-8 safely</h1>\n<h2>Intuition</h2>\n<p><code>for i, r := range s</code> gives the <strong>byte index</strong> where each rune starts. A rune</p>\n<p>occupies <code>utf8.RuneLen(r)</code> bytes. Keep runes while the next one still fits under</p>\n<p>the byte budget; stop at the boundary:</p>\n<pre class=\"md\"><code>\nfor i, r := range s {\n\tif i+utf8.RuneLen(r) &gt; max { return s[:i] }\n}\nreturn s\n</code></pre>\n<h2>Approach</h2>\n<p>1. Range over s, which yields the starting byte index i of each rune. 2. Track the last rune boundary that is still &lt;= max. 3. When an index i exceeds max, return s[:last]. 4. If the whole string fits (len&lt;=max), return it all.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Truncate(s string, max int) string {\n\tlast := 0\n\tfor i := range s {\n\t\tif i &gt; max {\n\t\t\treturn s[:last]\n\t\t}\n\t\tlast = i\n\t}\n\tif len(s) &lt;= max {\n\t\treturn s\n\t}\n\treturn s[:last]\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>Truncate(\"h\u00e9llo\",2): boundaries i=0(h),1(\u00e9),3(l)... at i=3 &gt; 2 return s[:last] where last=1 -&gt; \"h\".</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>The range index is a byte offset, exactly the safe cut point.</li>\n<li><code>s[:i]</code> slices bytes without copying \u2014 cheap.</li>\n<li><code>max</code> larger than <code>len(s)</code> returns the whole string; <code>max==0</code> returns \"\".</li>\n</ul>",
    "starter": "// Package utf8truncate truncates a string to a byte budget without splitting a\n// rune.\npackage utf8truncate\n\n// Truncate returns the longest prefix of s whose length in bytes is <= max,\n// never cutting through the middle of a multi-byte rune.\n//\n// TODO(candidate): implement by ranging (byte index + rune width).\nfunc Truncate(s string, max int) string {\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "utf8truncate.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/02-data-types/senior/clampbyte": {
    "id": "01-language-basics/02-data-types/senior/clampbyte",
    "title": "Saturating Pixel Clamp",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Saturating Pixel Clamp</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p>Brightening an image adds to pixel values; a result of <code>300</code> must saturate to</p>\n<p><code>255</code>, not wrap to <code>44</code>. <code>byte(x)</code> wraps \u2014 it keeps only the low 8 bits.</p>\n<h2>Task</h2>\n<p>Fix the conversion between the markers in clampbyte.go to</p>\n<p>saturate to [0,255].</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  128\nOutput: 128\n</code></pre>\n<p>_Explanation:_ in range, unchanged</p>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  300\nOutput: 255\n</code></pre>\n<p>_Explanation:_ saturates to max byte</p>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  -20\nOutput: 0\n</code></pre>\n<p>_Explanation:_ saturates to min byte</p>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Conversion wraps</span><span class=\"tchip\">Saturation</span><span class=\"tchip\">Range guard</span></div></details>",
    "education": "<h1>Saturation vs wraparound</h1>\n<h2>Intuition</h2>\n<p>Converting an out-of-range <code>int</code> to <code>byte</code> keeps only the low 8 bits, so <code>300</code></p>\n<p>becomes <code>44</code> and <code>-20</code> becomes <code>236</code>. Image and audio math need <strong>saturation</strong> \u2014</p>\n<p>clamp to the range first, then convert:</p>\n<pre class=\"md\"><code>\nif x &lt; 0 { return 0 }\nif x &gt; 255 { return 255 }\nreturn byte(x)\n</code></pre>\n<h2>Approach</h2>\n<p>1. Bug: <code>return byte(x)</code> truncates to 8 bits, so 300 wraps to 44 and -20 wraps to 236.</p>\n<p>2. Fix: clamp before converting \u2014 if x&lt;0 return 0, if x&gt;255 return 255, else byte(x).</p>\n<p>3. This saturates out-of-range values instead of wrapping.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Clamp(x int) byte {\n\tif x &lt; 0 {\n\t\treturn 0\n\t}\n\tif x &gt; 255 {\n\t\treturn 255\n\t}\n\treturn byte(x)\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>Clamp(300): 300&gt;255 -&gt; return 255. Clamp(-20): -20&lt;0 -&gt; return 0. Clamp(128): in range -&gt; byte(128).</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Narrowing conversions wrap silently; they never clamp for you.</li>\n<li>Clamp in the wide type (<code>int</code>) before the conversion.</li>\n<li>Hardware SIMD has saturating adds for exactly this reason.</li>\n</ul>",
    "starter": "// Package clampbyte saturates an int to a byte (0..255) for pixel math.\n// A planted bug wraps instead of clamping.\npackage clampbyte\n\n// Clamp converts x to a byte, saturating out-of-range values to 0 or 255\n// (as image processing requires), never wrapping.\nfunc Clamp(x int) byte {\n\t// CHANGE CODE BELOW THIS LINE\n\treturn byte(x)\n\t// CHANGE CODE ABOVE THIS LINE\n}\n",
    "debug": "",
    "file": "clampbyte.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/02-data-types/senior/countwords": {
    "id": "01-language-basics/02-data-types/senior/countwords",
    "title": "Robust Word Count",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Robust Word Count</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p>An analytics job counts words with <code>strings.Split(s, \" \")</code>, which turns every</p>\n<p>run of spaces into empty strings \u2014 so <code>\"  a   b  \"</code> counts as 6, not 2.</p>\n<h2>Task</h2>\n<p>Fix the single line between the markers in countwords.go to</p>\n<p>count only non-empty words.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  \"hello world\"\nOutput: 2\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  \"  a   b  \"\nOutput: 2\n</code></pre>\n<p>_Explanation:_ runs of spaces are not words</p>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  \"   \"\nOutput: 0\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Split vs Fields</span><span class=\"tchip\">Empty tokens</span><span class=\"tchip\">Whitespace kinds</span></div></details>",
    "education": "<h1>Split vs Fields</h1>\n<h2>Intuition</h2>\n<p><code>strings.Split(s, \" \")</code> cuts on <strong>every</strong> single space, so consecutive spaces</p>\n<p>produce empty strings. <code>strings.Fields(s)</code> splits on runs of <em>any</em> whitespace and</p>\n<p>drops the empties:</p>\n<pre class=\"md\"><code>\nlen(strings.Fields(s)) // correct word count\n</code></pre>\n<h2>Approach</h2>\n<p>1. Bug: strings.Split(s,\" \") emits empty tokens for adjacent/leading/trailing spaces, inflating the count.</p>\n<p>2. Fix: use strings.Fields, which splits on runs of whitespace and drops empties.</p>\n<p>3. len(Fields) is the true word count.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nimport \"strings\"\n\nfunc Count(s string) int {\n\treturn len(strings.Fields(s))\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>\"  a   b  \" -&gt; Fields -&gt; [\"a\",\"b\"] -&gt; len 2. Split would give many \"\" tokens.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>Fields</code> treats tabs and newlines as separators too \u2014 usually what you want.</li>\n<li><code>Split(\"\", \" \")</code> returns <code>[\"\"]</code> (length 1); <code>Fields(\"\")</code> returns length 0.</li>\n<li>If you need a custom separator set, <code>strings.FieldsFunc</code> takes a predicate.</li>\n</ul>",
    "starter": "// Package countwords counts whitespace-separated words.\n// A planted bug counts empty tokens produced by runs of spaces.\npackage countwords\n\nimport \"strings\"\n\n// Count returns the number of words in s (maximal runs of non-space).\n// Multiple/leading/trailing spaces must not create empty words.\nfunc Count(s string) int {\n\t// CHANGE CODE BELOW THIS LINE\n\treturn len(strings.Split(s, \" \"))\n\t// CHANGE CODE ABOVE THIS LINE\n}\n",
    "debug": "",
    "file": "countwords.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/02-data-types/senior/csvsplit": {
    "id": "01-language-basics/02-data-types/senior/csvsplit",
    "title": "Quoted CSV Split",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Quoted CSV Split</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p>An importer splits CSV lines but breaks <code>a,\"b,c\",d</code> into four fields \u2014 it splits</p>\n<p>on the comma <em>inside</em> the quotes. A comma is only a separator when not inside a</p>\n<p>quoted field.</p>\n<h2>Task</h2>\n<p>Fix the <code>case c == ','</code> guard between the markers in csvsplit.go</p>\n<p>so commas inside quotes are literal.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  a,b,c\nOutput: [\"a\" \"b\" \"c\"]\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  a,\"b,c\",d\nOutput: [\"a\" \"b,c\" \"d\"]\n</code></pre>\n<p>_Explanation:_ comma inside quotes is literal</p>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  \"x\"\"y\",z\nOutput: [x\"y z]\n</code></pre>\n<p>_Explanation:_ doubled quote -&gt; literal quote</p>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Stateful scan</span><span class=\"tchip\">Context-sensitive delimiter</span><span class=\"tchip\">Quote escaping</span></div></details>",
    "education": "<h1>Context-sensitive delimiters</h1>\n<h2>Intuition</h2>\n<p>A CSV comma is a field separator only when you are <strong>not</strong> inside a quoted field.</p>\n<p>The parser carries an <code>inQuotes</code> flag; the split case must respect it:</p>\n<pre class=\"md\"><code>\ncase c == ',' &amp;&amp; !inQuotes:\n</code></pre>\n<h2>Approach</h2>\n<p>1. Bug: the comma case fires even inside a quoted field, wrongly splitting <code>\"b,c\"</code>.</p>\n<p>2. Fix: guard the comma case with <code>&amp;&amp; !inQuotes</code> so only unquoted commas separate fields.</p>\n<p>3. Quote toggling and \"\" un-escaping stay as-is.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Split(line string) []string {\n\tvar fields []string\n\tvar cur []byte\n\tinQuotes := false\n\tfor i := 0; i &lt; len(line); i++ {\n\t\tc := line[i]\n\t\tswitch {\n\t\tcase c == '\"':\n\t\t\tif inQuotes &amp;&amp; i+1 &lt; len(line) &amp;&amp; line[i+1] == '\"' {\n\t\t\t\tcur = append(cur, '\"')\n\t\t\t\ti++\n\t\t\t} else {\n\t\t\t\tinQuotes = !inQuotes\n\t\t\t}\n\t\tcase c == ',' &amp;&amp; !inQuotes:\n\t\t\tfields = append(fields, string(cur))\n\t\t\tcur = cur[:0]\n\t\tdefault:\n\t\t\tcur = append(cur, c)\n\t\t}\n\t}\n\tfields = append(fields, string(cur))\n\treturn fields\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>a,\"b,c\",d: inside quotes inQuotes=true so the middle comma appends to cur; only the two outer commas split.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Toggle <code>inQuotes</code> on a lone quote; a doubled <code>\"\"</code> inside quotes is a literal.</li>\n<li>The final field is flushed after the loop.</li>\n<li>Trailing/empty fields matter: <code>\"\"</code> line yields one empty field.</li>\n</ul>",
    "starter": "// Package csvsplit splits one CSV line into fields, honoring double quotes.\n// A planted bug splits on a comma that is inside a quoted field.\npackage csvsplit\n\n// Split parses a single RFC-4180-ish line: fields separated by commas, a field\n// may be wrapped in double quotes to contain commas. Quotes are not included in\n// the output (inner \"\" -> \").\nfunc Split(line string) []string {\n\tvar fields []string\n\tvar cur []byte\n\tinQuotes := false\n\tfor i := 0; i < len(line); i++ {\n\t\tc := line[i]\n\t\tswitch {\n\t\tcase c == '\"':\n\t\t\tif inQuotes && i+1 < len(line) && line[i+1] == '\"' {\n\t\t\t\tcur = append(cur, '\"')\n\t\t\t\ti++\n\t\t\t} else {\n\t\t\t\tinQuotes = !inQuotes\n\t\t\t}\n\t\t// CHANGE CODE BELOW THIS LINE\n\t\tcase c == ',':\n\t\t\t// CHANGE CODE ABOVE THIS LINE\n\t\t\tfields = append(fields, string(cur))\n\t\t\tcur = cur[:0]\n\t\tdefault:\n\t\t\tcur = append(cur, c)\n\t\t}\n\t}\n\tfields = append(fields, string(cur))\n\treturn fields\n}\n",
    "debug": "",
    "file": "csvsplit.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/02-data-types/senior/durationparse": {
    "id": "01-language-basics/02-data-types/senior/durationparse",
    "title": "Duration Minutes",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Duration Minutes</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p>A scheduler parses timeouts like <code>1h30m</code>. Minutes are converted with the wrong</p>\n<p>factor (\u00d76 instead of \u00d760), so every timeout with minutes is ten times too</p>\n<p>short.</p>\n<h2>Task</h2>\n<p>Fix the minute multiplier between the markers in</p>\n<p>durationparse.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  \"2m\"\nOutput: 120, true\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  \"1h30m\"\nOutput: 5400, true\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  \"10\"\nOutput: 0, false\n</code></pre>\n<p>_Explanation:_ trailing number without a unit</p>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Unit multipliers</span><span class=\"tchip\">Digit fold</span><span class=\"tchip\">Trailing unit</span></div></details>",
    "education": "<h1>Unit conversion factors</h1>\n<h2>Intuition</h2>\n<p>Parse a number, then multiply by its unit's factor in the common base (seconds):</p>\n<p>hours \u00d73600, minutes \u00d760, seconds \u00d71. A wrong factor silently rescales every</p>\n<p>value that uses that unit.</p>\n<h2>Approach</h2>\n<p>1. Bug: minutes used <code>num <em> 6</code> instead of <code>num </em> 60</code>.</p>\n<p>2. Fix: multiply minutes by 60 seconds.</p>\n<p>3. Hours (3600) and seconds (1) were already correct.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Seconds(s string) (int, bool) {\n\ttotal, num, seen := 0, 0, false\n\tfor i := 0; i &lt; len(s); i++ {\n\t\tc := s[i]\n\t\tswitch {\n\t\tcase c &gt;= '0' &amp;&amp; c &lt;= '9':\n\t\t\tnum = num*10 + int(c-'0')\n\t\t\tseen = true\n\t\tcase c == 'h':\n\t\t\tif !seen {\n\t\t\t\treturn 0, false\n\t\t\t}\n\t\t\ttotal += num * 3600\n\t\t\tnum, seen = 0, false\n\t\tcase c == 'm':\n\t\t\tif !seen {\n\t\t\t\treturn 0, false\n\t\t\t}\n\t\t\ttotal += num * 60\n\t\t\tnum, seen = 0, false\n\t\tcase c == 's':\n\t\t\tif !seen {\n\t\t\t\treturn 0, false\n\t\t\t}\n\t\t\ttotal += num\n\t\t\tnum, seen = 0, false\n\t\tdefault:\n\t\t\treturn 0, false\n\t\t}\n\t}\n\tif seen {\n\t\treturn 0, false // trailing number without a unit\n\t}\n\treturn total, true\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>\"1h30m\": 1<em>3600=3600, 30</em>60=1800, total 5400.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Keep the factors exact: 60 seconds per minute, 3600 per hour.</li>\n<li>Require a unit after each number; a dangling number is malformed.</li>\n<li>Empty input is a valid zero here \u2014 decide such edge cases explicitly.</li>\n</ul>",
    "starter": "// Package durationparse parses simple durations like \"1h30m45s\" into seconds.\n// A planted bug uses the wrong multiplier for minutes.\npackage durationparse\n\n// Seconds parses a duration of h/m/s parts (each an integer + unit, in order or\n// partial) into total seconds. Returns (0,false) on malformed input.\nfunc Seconds(s string) (int, bool) {\n\ttotal, num, seen := 0, 0, false\n\tfor i := 0; i < len(s); i++ {\n\t\tc := s[i]\n\t\tswitch {\n\t\tcase c >= '0' && c <= '9':\n\t\t\tnum = num*10 + int(c-'0')\n\t\t\tseen = true\n\t\tcase c == 'h':\n\t\t\tif !seen {\n\t\t\t\treturn 0, false\n\t\t\t}\n\t\t\ttotal += num * 3600\n\t\t\tnum, seen = 0, false\n\t\tcase c == 'm':\n\t\t\tif !seen {\n\t\t\t\treturn 0, false\n\t\t\t}\n\t\t\t// CHANGE CODE BELOW THIS LINE\n\t\t\ttotal += num * 6\n\t\t\t// CHANGE CODE ABOVE THIS LINE\n\t\t\tnum, seen = 0, false\n\t\tcase c == 's':\n\t\t\tif !seen {\n\t\t\t\treturn 0, false\n\t\t\t}\n\t\t\ttotal += num\n\t\t\tnum, seen = 0, false\n\t\tdefault:\n\t\t\treturn 0, false\n\t\t}\n\t}\n\tif seen {\n\t\treturn 0, false // trailing number without a unit\n\t}\n\treturn total, true\n}\n",
    "debug": "",
    "file": "durationparse.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/02-data-types/senior/fletcher16": {
    "id": "01-language-basics/02-data-types/senior/fletcher16",
    "title": "Fletcher-16 Modulus",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Fletcher-16 Modulus</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p>A firmware image uses a Fletcher-16 checksum. The running sums are reduced mod</p>\n<p><code>256</code> instead of the spec's <code>255</code>, so every checksum disagrees with the</p>\n<p>reference implementation and the bootloader rejects valid images.</p>\n<h2>Task</h2>\n<p>Fix the two modulus operations between the markers in</p>\n<p>fletcher16.go to use <code>255</code>.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  \"abcde\"\nOutput: 0xC8F0\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  \"\"\nOutput: 0x0000\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  \"a\"\nOutput: 0x6161\n</code></pre>\n<p>_Explanation:_ sum1=sum2=97</p>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Fletcher sums</span><span class=\"tchip\">Modulus 255</span><span class=\"tchip\">Combine</span></div></details>",
    "education": "<h1>Getting the modulus right</h1>\n<h2>Intuition</h2>\n<p>Fletcher-16 keeps two running sums: <code>sum1</code> accumulates the bytes, <code>sum2</code></p>\n<p>accumulates <code>sum1</code>. Both are reduced <strong>mod 255</strong> (not 256) each step, and the</p>\n<p>result is <code>(sum2 &lt;&lt; 8) | sum1</code>.</p>\n<h2>Approach</h2>\n<p>1. Bug: both running sums used <code>% 256</code> instead of the Fletcher-16 modulus <code>% 255</code>.</p>\n<p>2. Fix: change both moduli to 255.</p>\n<p>3. Combine as (sum2&lt;&lt;8)|sum1.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Checksum(data []byte) uint16 {\n\tvar sum1, sum2 uint16\n\tfor _, b := range data {\n\t\tsum1 = (sum1 + uint16(b)) % 255\n\t\tsum2 = (sum2 + sum1) % 255\n\t}\n\treturn sum2&lt;&lt;8 | sum1\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>\"abcde\": running sums mod 255 accumulate to sum2=0xC8, sum1=0xF0 -&gt; 0xC8F0.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>The modulus is 255 (<code>2^8 - 1</code>), a common surprise vs the \"natural\" 256.</li>\n<li>Reducing every step avoids overflow and matches the spec exactly.</li>\n<li>Constants like this must come from the specification, not intuition.</li>\n</ul>",
    "starter": "// Package fletcher16 computes the Fletcher-16 checksum.\n// A planted bug uses the wrong modulus.\npackage fletcher16\n\n// Checksum returns the Fletcher-16 checksum of data: two running sums mod 255,\n// combined as (sum2 << 8) | sum1.\nfunc Checksum(data []byte) uint16 {\n\tvar sum1, sum2 uint16\n\tfor _, b := range data {\n\t\t// CHANGE CODE BELOW THIS LINE\n\t\tsum1 = (sum1 + uint16(b)) % 256\n\t\tsum2 = (sum2 + sum1) % 256\n\t\t// CHANGE CODE ABOVE THIS LINE\n\t}\n\treturn sum2<<8 | sum1\n}\n",
    "debug": "",
    "file": "fletcher16.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/02-data-types/senior/hexdecode": {
    "id": "01-language-basics/02-data-types/senior/hexdecode",
    "title": "Hex Nibble Order",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Hex Nibble Order</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p>A wire-protocol decoder turns hex into bytes, but every byte comes out with its</p>\n<p>two nibbles swapped: <code>\"1a\"</code> decodes to <code>0xa1</code>. The high nibble is the <em>first</em></p>\n<p>character.</p>\n<h2>Task</h2>\n<p>Fix the single line between the markers in hexdecode.go so the</p>\n<p>first hex char is the high nibble.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  \"ff\"\nOutput: [0xff], true\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  \"476F\"\nOutput: \"Go\", true\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  \"abc\"\nOutput: nil, false\n</code></pre>\n<p>_Explanation:_ odd length</p>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Nibble position</span><span class=\"tchip\">Shift + OR</span><span class=\"tchip\">Even length</span></div></details>",
    "education": "<h1>Nibble ordering in hex</h1>\n<h2>Intuition</h2>\n<p>Each byte is two hex digits: the <strong>first</strong> character is the high nibble (bits</p>\n<p>4\u20137), the second the low nibble (bits 0\u20133):</p>\n<pre class=\"md\"><code>\nbyte(hi&lt;&lt;4 | lo)\n</code></pre>\n<p>Swapping them (<code>lo&lt;&lt;4 | hi</code>) reverses each byte's nibbles \u2014 <code>\"1a\"</code> \u2192 <code>0xa1</code>.</p>\n<h2>Approach</h2>\n<p>1. Bug: nibbles combined as <code>lo&lt;&lt;4|hi</code>, swapping high and low nibble.</p>\n<p>2. Fix: <code>hi&lt;&lt;4|lo</code> \u2014 first hex digit is the high nibble.</p>\n<p>3. Odd length and non-hex digits still return false.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc nib(c byte) (int, bool) {\n\tswitch {\n\tcase c &gt;= '0' &amp;&amp; c &lt;= '9':\n\t\treturn int(c - '0'), true\n\tcase c &gt;= 'a' &amp;&amp; c &lt;= 'f':\n\t\treturn int(c-'a') + 10, true\n\tcase c &gt;= 'A' &amp;&amp; c &lt;= 'F':\n\t\treturn int(c-'A') + 10, true\n\t}\n\treturn 0, false\n}\n\nfunc Decode(s string) ([]byte, bool) {\n\tif len(s)%2 != 0 {\n\t\treturn nil, false\n\t}\n\tout := make([]byte, 0, len(s)/2)\n\tfor i := 0; i &lt; len(s); i += 2 {\n\t\thi, ok1 := nib(s[i])\n\t\tlo, ok2 := nib(s[i+1])\n\t\tif !ok1 || !ok2 {\n\t\t\treturn nil, false\n\t\t}\n\t\tout = append(out, byte(hi&lt;&lt;4|lo))\n\t}\n\treturn out, true\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>\"476F\": hi=4,lo=7 -&gt; 0x47 'G'; hi=6,lo=F -&gt; 0x6F 'o' -&gt; \"Go\".</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>High nibble shifts left by 4; low nibble is unshifted.</li>\n<li>Reject odd-length input up front.</li>\n<li>Accept both letter cases when decoding.</li>\n</ul>",
    "starter": "// Package hexdecode decodes a lowercase/uppercase hex string to bytes.\n// A planted bug swaps the nibble order.\npackage hexdecode\n\nfunc nib(c byte) (int, bool) {\n\tswitch {\n\tcase c >= '0' && c <= '9':\n\t\treturn int(c - '0'), true\n\tcase c >= 'a' && c <= 'f':\n\t\treturn int(c-'a') + 10, true\n\tcase c >= 'A' && c <= 'F':\n\t\treturn int(c-'A') + 10, true\n\t}\n\treturn 0, false\n}\n\n// Decode parses hex (even length) into bytes. Returns (nil,false) on bad input.\nfunc Decode(s string) ([]byte, bool) {\n\tif len(s)%2 != 0 {\n\t\treturn nil, false\n\t}\n\tout := make([]byte, 0, len(s)/2)\n\tfor i := 0; i < len(s); i += 2 {\n\t\thi, ok1 := nib(s[i])\n\t\tlo, ok2 := nib(s[i+1])\n\t\tif !ok1 || !ok2 {\n\t\t\treturn nil, false\n\t\t}\n\t\t// CHANGE CODE BELOW THIS LINE\n\t\tout = append(out, byte(lo<<4|hi))\n\t\t// CHANGE CODE ABOVE THIS LINE\n\t}\n\treturn out, true\n}\n",
    "debug": "",
    "file": "hexdecode.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/02-data-types/senior/ipv4parse": {
    "id": "01-language-basics/02-data-types/senior/ipv4parse",
    "title": "IPv4 Octet Range",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">IPv4 Octet Range</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p>A firewall config loader parses <code>192.168.1.1</code> into four bytes. It accepts</p>\n<p><code>256.1.1.1</code> because the range check is wrong \u2014 and <code>byte(256)</code> silently wraps to</p>\n<p>0, corrupting the rule.</p>\n<h2>Task</h2>\n<p>Fix the single check between the markers in ipv4parse.go so each</p>\n<p>octet must be 0..255.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  \"192.168.1.1\"\nOutput: [192 168 1 1], true\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  \"256.1.1.1\"\nOutput: zero, false\n</code></pre>\n<p>_Explanation:_ octet &gt; 255</p>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  \"1.2.3\"\nOutput: zero, false\n</code></pre>\n<p>_Explanation:_ only 3 fields</p>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Octet range</span><span class=\"tchip\">Conversion wrap</span><span class=\"tchip\">Validate before convert</span></div></details>",
    "education": "<h1>Validate before narrowing</h1>\n<h2>Intuition</h2>\n<p>An IPv4 octet fits in a <code>byte</code> (0..255). Validation must happen <strong>before</strong> the</p>\n<p><code>byte(val)</code> conversion, because narrowing wraps: <code>byte(256) == 0</code>. A too-loose</p>\n<p>bound (<code>&gt; 999</code>) lets an invalid octet convert to a wrong byte.</p>\n<h2>Approach</h2>\n<p>1. Bug: octet range checked <code>val &gt; 999</code>, letting 256..999 through.</p>\n<p>2. Fix: reject <code>val &gt; 255</code>.</p>\n<p>3. Field count and digit checks unchanged.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Parse(s string) (out [4]byte, ok bool) {\n\tfield, seen, val := 0, 0, 0\n\tfor i := 0; i &lt;= len(s); i++ {\n\t\tif i == len(s) || s[i] == '.' {\n\t\t\tif seen == 0 {\n\t\t\t\treturn [4]byte{}, false\n\t\t\t}\n\t\t\tif val &gt; 255 {\n\t\t\t\treturn [4]byte{}, false\n\t\t\t}\n\t\t\tif field &gt; 3 {\n\t\t\t\treturn [4]byte{}, false\n\t\t\t}\n\t\t\tout[field] = byte(val)\n\t\t\tfield++\n\t\t\tval, seen = 0, 0\n\t\t\tcontinue\n\t\t}\n\t\tc := s[i]\n\t\tif c &lt; '0' || c &gt; '9' {\n\t\t\treturn [4]byte{}, false\n\t\t}\n\t\tval = val*10 + int(c-'0')\n\t\tseen++\n\t}\n\tif field != 4 {\n\t\treturn [4]byte{}, false\n\t}\n\treturn out, true\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>\"256.1.1.1\": first octet val=256 -&gt; 256&gt;255 -&gt; false.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>The bound is <code>&gt; 255</code>, exactly the byte maximum.</li>\n<li>Narrowing conversions never panic; they truncate silently.</li>\n<li>Also guard field count and empty fields, as this parser does.</li>\n</ul>",
    "starter": "// Package ipv4parse parses a dotted-decimal IPv4 address into 4 octets.\n// A planted bug lets an out-of-range octet through.\npackage ipv4parse\n\n// Parse converts \"a.b.c.d\" into four bytes. Each octet must be 0..255.\n// Returns (zero, false) on malformed input.\nfunc Parse(s string) (out [4]byte, ok bool) {\n\tfield, seen, val := 0, 0, 0\n\tfor i := 0; i <= len(s); i++ {\n\t\tif i == len(s) || s[i] == '.' {\n\t\t\tif seen == 0 {\n\t\t\t\treturn [4]byte{}, false\n\t\t\t}\n\t\t\t// CHANGE CODE BELOW THIS LINE\n\t\t\tif val > 999 {\n\t\t\t\t// CHANGE CODE ABOVE THIS LINE\n\t\t\t\treturn [4]byte{}, false\n\t\t\t}\n\t\t\tif field > 3 {\n\t\t\t\treturn [4]byte{}, false\n\t\t\t}\n\t\t\tout[field] = byte(val)\n\t\t\tfield++\n\t\t\tval, seen = 0, 0\n\t\t\tcontinue\n\t\t}\n\t\tc := s[i]\n\t\tif c < '0' || c > '9' {\n\t\t\treturn [4]byte{}, false\n\t\t}\n\t\tval = val*10 + int(c-'0')\n\t\tseen++\n\t}\n\tif field != 4 {\n\t\treturn [4]byte{}, false\n\t}\n\treturn out, true\n}\n",
    "debug": "",
    "file": "ipv4parse.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/02-data-types/senior/jsonescape": {
    "id": "01-language-basics/02-data-types/senior/jsonescape",
    "title": "JSON String Escaping",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">JSON String Escaping</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p>A hand-rolled JSON writer escapes quotes and control chars but forgets the</p>\n<p><strong>backslash</strong> \u2014 so <code>a\\b</code> is emitted as <code>a\\b</code>, producing invalid JSON that a</p>\n<p>strict parser rejects.</p>\n<h2>Task</h2>\n<p>Add the missing <code>'\\\\'</code> case between the markers in</p>\n<p>jsonescape.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  a\"b\nOutput: a\\\"b\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  a\\b\nOutput: a\\\\b\n</code></pre>\n<p>_Explanation:_ backslash must be escaped</p>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  line\\n\nOutput: line\\n\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Escape set</span><span class=\"tchip\">Backslash first</span><span class=\"tchip\">Raw literals</span></div></details>",
    "education": "<h1>Escaping the escape character</h1>\n<h2>Intuition</h2>\n<p>In JSON, the backslash introduces every escape, so a literal backslash must</p>\n<p>itself be written as <code>\\\\</code>. If you escape quotes and newlines but forget the</p>\n<p>backslash, any input containing <code>\\</code> yields invalid JSON:</p>\n<pre class=\"md\"><code>\ncase '\\\\':\n\tb.WriteString(`\\\\`) // raw literal: two backslash characters\n</code></pre>\n<h2>Approach</h2>\n<p>1. Bug: the backslash case was missing, so <code>\\</code> passed through unescaped (producing invalid JSON).</p>\n<p>2. Fix: add <code>case '\\\\': b.WriteString(\"\\\\\\\\\")</code> before the quote case.</p>\n<p>3. Quote, newline, tab, CR cases stay.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nimport \"strings\"\n\nfunc Escape(s string) string {\n\tvar b strings.Builder\n\tfor _, r := range s {\n\t\tswitch r {\n\t\tcase '\\\\':\n\t\t\tb.WriteString(`\\\\`)\n\t\tcase '\"':\n\t\t\tb.WriteString(`\\\"`)\n\t\tcase '\\n':\n\t\t\tb.WriteString(`\\n`)\n\t\tcase '\\t':\n\t\t\tb.WriteString(`\\t`)\n\t\tcase '\\r':\n\t\t\tb.WriteString(`\\r`)\n\t\tdefault:\n\t\t\tb.WriteRune(r)\n\t\t}\n\t}\n\treturn b.String()\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>a\\b: the \\ now matches the backslash case and emits \\\\ -&gt; a\\\\b.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Escape the backslash <em>before</em> other rules conceptually; order in a <code>switch</code></li>\n</ul>\n<p>  doesn't matter since each rune matches once.</p>\n<ul>\n<li>A raw literal `<code> </code>\\\\<code> </code><code> is two characters; </code>\"\\\\\\\\\"` is the interpreted</li>\n</ul>\n<p>  equivalent.</p>\n<ul>\n<li>Prefer <code>encoding/json</code> in production; hand-escaping is for understanding.</li>\n</ul>",
    "starter": "// Package jsonescape escapes a string for embedding in a JSON document.\n// A planted bug forgets to escape the backslash itself.\npackage jsonescape\n\nimport \"strings\"\n\n// Escape returns s with JSON-required escapes: backslash, double-quote, newline,\n// tab, and carriage return. (Other control chars are ignored for simplicity.)\nfunc Escape(s string) string {\n\tvar b strings.Builder\n\tfor _, r := range s {\n\t\tswitch r {\n\t\t// CHANGE CODE BELOW THIS LINE\n\t\tcase '\"':\n\t\t\tb.WriteString(`\\\"`)\n\t\t// CHANGE CODE ABOVE THIS LINE\n\t\tcase '\\n':\n\t\t\tb.WriteString(`\\n`)\n\t\tcase '\\t':\n\t\t\tb.WriteString(`\\t`)\n\t\tcase '\\r':\n\t\t\tb.WriteString(`\\r`)\n\t\tdefault:\n\t\t\tb.WriteRune(r)\n\t\t}\n\t}\n\treturn b.String()\n}\n",
    "debug": "",
    "file": "jsonescape.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/02-data-types/senior/maskcard": {
    "id": "01-language-basics/02-data-types/senior/maskcard",
    "title": "Mask All But Last 4",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Mask All But Last 4</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p>A receipt shows a card as <code>************3456</code>. The condition is inverted \u2014 it</p>\n<p>masks the last four and reveals the rest, leaking the sensitive part.</p>\n<h2>Task</h2>\n<p>Fix the condition between the markers in maskcard.go so only the</p>\n<p>last four are visible.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  \"1234567890123456\"\nOutput: \"************3456\"\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  \"4242\"\nOutput: \"4242\"\n</code></pre>\n<p>_Explanation:_ &lt;=4 chars unchanged</p>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  \"12345\"\nOutput: \"*2345\"\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Index window</span><span class=\"tchip\">Invert the mask</span><span class=\"tchip\">Runes</span></div></details>",
    "education": "<h1>Index windows and inverted conditions</h1>\n<h2>Intuition</h2>\n<p>The last four characters are indices <code>i &gt;= len-4</code>; everything before is</p>\n<p><code>i &lt; len-4</code>. To hide all but the last four, mask the <strong>front</strong>:</p>\n<pre class=\"md\"><code>\nif i &lt; len(r)-4 { out[i] = '*' } else { out[i] = r[i] }\n</code></pre>\n<h2>Approach</h2>\n<p>1. Bug: condition <code>i &gt;= len(r)-4</code> masked the LAST four and revealed the front.</p>\n<p>2. Fix: <code>i &lt; len(r)-4</code> masks everything except the last four.</p>\n<p>3. Strings of &lt;=4 runes return unchanged.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Mask(s string) string {\n\tr := []rune(s)\n\tif len(r) &lt;= 4 {\n\t\treturn s\n\t}\n\tout := make([]rune, len(r))\n\tfor i := range r {\n\t\tif i &lt; len(r)-4 {\n\t\t\tout[i] = '*'\n\t\t} else {\n\t\t\tout[i] = r[i]\n\t\t}\n\t}\n\treturn string(out)\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>\"12345\": len 5, mask indices &lt;1 (index 0) -&gt; '<em>', keep 2345 -&gt; \"</em>2345\".</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Operate on runes so multi-byte input isn't miscounted.</li>\n<li>Return short inputs unchanged, or you might reveal everything (define the</li>\n</ul>\n<p>  policy deliberately).</p>\n<ul>\n<li>The boundary is <code>len-4</code>; off-by-one reveals or hides one extra digit.</li>\n</ul>",
    "starter": "// Package maskcard masks all but the last four characters.\n// A planted bug reveals the wrong digits.\npackage maskcard\n\n// Mask replaces every character except the last four with '*'. If s has four or\n// fewer characters, it is returned unchanged (nothing to hide).\nfunc Mask(s string) string {\n\tr := []rune(s)\n\tif len(r) <= 4 {\n\t\treturn s\n\t}\n\tout := make([]rune, len(r))\n\tfor i := range r {\n\t\t// CHANGE CODE BELOW THIS LINE\n\t\tif i >= len(r)-4 {\n\t\t\t// CHANGE CODE ABOVE THIS LINE\n\t\t\tout[i] = '*'\n\t\t} else {\n\t\t\tout[i] = r[i]\n\t\t}\n\t}\n\treturn string(out)\n}\n",
    "debug": "",
    "file": "maskcard.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/02-data-types/senior/normspaces": {
    "id": "01-language-basics/02-data-types/senior/normspaces",
    "title": "Normalize Whitespace",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Normalize Whitespace</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p>A search indexer normalizes whitespace so <code>\"  hello   world  \"</code> becomes</p>\n<p><code>\"hello world\"</code>. The current code prepends a stray space, so keys never match.</p>\n<h2>Task</h2>\n<p>Fix the return between the markers in normspaces.go. Note</p>\n<p><code>strings.Fields</code> already dropped the empties \u2014 no manual space needed.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  \"  hello   world  \"\nOutput: \"hello world\"\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  \"a\\tb\\nc\"\nOutput: \"a b c\"\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  \"\"\nOutput: \"\"\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Fields + Join</span><span class=\"tchip\">No extra space</span><span class=\"tchip\">All whitespace kinds</span></div></details>",
    "education": "<h1>Fields + Join normalization</h1>\n<h2>Intuition</h2>\n<p><code>strings.Fields(s)</code> splits on runs of any whitespace and drops empties;</p>\n<p><code>strings.Join(..., \" \")</code> rejoins with single spaces. Together they collapse and</p>\n<p>trim in one step \u2014 no manual padding required:</p>\n<pre class=\"md\"><code>\nstrings.Join(strings.Fields(s), \" \")\n</code></pre>\n<h2>Approach</h2>\n<p>1. Bug: prepended a stray leading space: <code>return \" \" + collapsed</code>.</p>\n<p>2. Fix: return <code>collapsed</code> directly \u2014 Fields already trims and collapses.</p>\n<p>3. No leading/trailing whitespace remains.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nimport \"strings\"\n\nfunc Normalize(s string) string {\n\tcollapsed := strings.Join(strings.Fields(s), \" \")\n\treturn collapsed\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>\"  hello   world  \": Fields -&gt; [\"hello\",\"world\"] -&gt; Join -&gt; \"hello world\".</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>Fields</code> already handles leading/trailing and multiple spaces; don't re-add</li>\n</ul>\n<p>  any.</p>\n<ul>\n<li><code>Fields(\"\")</code> is empty, so <code>Join</code> yields <code>\"\"</code> \u2014 correct.</li>\n<li>Tabs and newlines are whitespace too and collapse to a single space.</li>\n</ul>",
    "starter": "// Package normspaces collapses internal whitespace and trims the ends.\n// A planted bug forgets to trim, leaving leading/trailing spaces.\npackage normspaces\n\nimport \"strings\"\n\n// Normalize collapses every run of whitespace in s to a single space and trims\n// leading/trailing whitespace.\nfunc Normalize(s string) string {\n\tcollapsed := strings.Join(strings.Fields(s), \" \")\n\t// CHANGE CODE BELOW THIS LINE\n\treturn \" \" + collapsed\n\t// CHANGE CODE ABOVE THIS LINE\n}\n",
    "debug": "",
    "file": "normspaces.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/02-data-types/senior/ordinal": {
    "id": "01-language-basics/02-data-types/senior/ordinal",
    "title": "English Ordinals",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">English Ordinals</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p>A UI prints ranks as <code>1st, 2nd, 3rd, 4th\u2026</code>. The code keys only on the last digit,</p>\n<p>so it wrongly prints <code>11st, 12nd, 13rd</code> \u2014 but 11\u201313 are always <code>th</code>.</p>\n<h2>Task</h2>\n<p>Fix the <code>switch</code> between the markers in ordinal.go so 11\u201313 (and</p>\n<p>111\u2013113, etc.) use <code>\"th\"</code>.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  21\nOutput: \"21st\"\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  11\nOutput: \"11th\"\n</code></pre>\n<p>_Explanation:_ teens exception</p>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  112\nOutput: \"112th\"\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Last-digit rule</span><span class=\"tchip\">Teens exception</span><span class=\"tchip\">Switch on two keys</span></div></details>",
    "education": "<h1>The teens exception</h1>\n<h2>Intuition</h2>\n<p>English ordinals key on the last digit (1\u2192st, 2\u2192nd, 3\u2192rd, else th) <strong>except</strong></p>\n<p>11, 12, 13, which are all \"th\". So check the last two digits first:</p>\n<pre class=\"md\"><code>\nif n%100 &gt;= 11 &amp;&amp; n%100 &lt;= 13 { return \"th\" }\nswitch n % 10 { case 1: \"st\"; case 2: \"nd\"; case 3: \"rd\"; default: \"th\" }\n</code></pre>\n<h2>Approach</h2>\n<p>1. Bug: <code>switch n % 10</code> ignored the 11-13 exception, giving 11-&gt;\"st\", 12-&gt;\"nd\", 13-&gt;\"rd\".</p>\n<p>2. Fix: first check <code>n % 100</code> for 11,12,13 and return \"th\", then fall through to the mod-10 switch.</p>\n<p>3. 21 -&gt; n%100=21 (no match) -&gt; n%10=1 -&gt; \"st\"; 11 -&gt; \"th\".</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nimport \"strconv\"\n\nfunc Suffix(n int) string {\n\tswitch n % 100 {\n\tcase 11, 12, 13:\n\t\treturn \"th\"\n\t}\n\tswitch n % 10 {\n\tcase 1:\n\t\treturn \"st\"\n\tcase 2:\n\t\treturn \"nd\"\n\tcase 3:\n\t\treturn \"rd\"\n\tdefault:\n\t\treturn \"th\"\n\t}\n}\n\nfunc Format(n int) string { return strconv.Itoa(n) + Suffix(n) }\n</code></pre>\n<h2>Walkthrough</h2>\n<p>Suffix(11): 11%100==11 -&gt; \"th\". Suffix(21): 21%100=21 skip, 21%10=1 -&gt; \"st\".</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>The exception is on <code>n%100</code>, catching 111, 112, 113 as well.</li>\n<li>Guard the teens <strong>before</strong> the last-digit switch.</li>\n<li>Negative inputs need their own policy; this version assumes non-negative.</li>\n</ul>",
    "starter": "// Package ordinal formats an integer as an English ordinal (1st, 2nd, 3rd...).\n// A planted bug mishandles the 11-13 exception.\npackage ordinal\n\nimport \"strconv\"\n\n// Suffix returns the ordinal suffix (\"st\",\"nd\",\"rd\",\"th\") for a non-negative n.\nfunc Suffix(n int) string {\n\t// CHANGE CODE BELOW THIS LINE\n\tswitch n % 10 {\n\t// CHANGE CODE ABOVE THIS LINE\n\tcase 1:\n\t\treturn \"st\"\n\tcase 2:\n\t\treturn \"nd\"\n\tcase 3:\n\t\treturn \"rd\"\n\tdefault:\n\t\treturn \"th\"\n\t}\n}\n\n// Format returns n with its ordinal suffix, e.g. \"21st\".\nfunc Format(n int) string { return strconv.Itoa(n) + Suffix(n) }\n",
    "debug": "",
    "file": "ordinal.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/02-data-types/senior/padleft": {
    "id": "01-language-basics/02-data-types/senior/padleft",
    "title": "Left Pad",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Left Pad</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p>A report aligns numbers by zero-padding on the <strong>left</strong> (<code>42</code> \u2192 <code>00042</code>). The</p>\n<p>code appends the pad on the right instead, so columns don't line up.</p>\n<h2>Task</h2>\n<p>Fix the return between the markers in padleft.go to pad on the</p>\n<p>left.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  \"42\",5,'0'\nOutput: \"00042\"\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  \"x\",4,'.'\nOutput: \"...x\"\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  \"toolong\",3,'0'\nOutput: \"toolong\"\n</code></pre>\n<p>_Explanation:_ already &gt;= width</p>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Pad side</span><span class=\"tchip\">Width in runes</span><span class=\"tchip\">No truncation</span></div></details>",
    "education": "<h1>Padding side and width unit</h1>\n<h2>Intuition</h2>\n<p>Left-padding <strong>prepends</strong> fill so the original text sits at the right edge:</p>\n<pre class=\"md\"><code>\nreturn pad + s\n</code></pre>\n<p>The pad count is <code>width - len([]rune(s))</code>, measured in runes so alignment is</p>\n<p>visual, not byte-based.</p>\n<h2>Approach</h2>\n<p>1. Bug: <code>return s + pad</code> appended the fill (right-pad).</p>\n<p>2. Fix: <code>return pad + s</code> to left-pad.</p>\n<p>3. Strings already &gt;= width return unchanged.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nimport \"strings\"\n\nfunc Pad(s string, width int, fill byte) string {\n\tn := width - len([]rune(s))\n\tif n &lt;= 0 {\n\t\treturn s\n\t}\n\tpad := strings.Repeat(string(fill), n)\n\treturn pad + s\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>Pad(\"42\",5,'0'): n=3, pad=\"000\", pad+s -&gt; \"00042\".</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Measure width in runes; byte width misaligns multi-byte text.</li>\n<li>Return unchanged when already at/over width \u2014 never truncate here.</li>\n<li><code>strings.Repeat</code> builds the fill run efficiently.</li>\n</ul>",
    "starter": "// Package padleft left-pads a string to a width with a fill byte.\n// A planted bug pads on the wrong side.\npackage padleft\n\nimport \"strings\"\n\n// Pad returns s left-padded with fill to at least width runes. If s is already\n// >= width, it is returned unchanged.\nfunc Pad(s string, width int, fill byte) string {\n\tn := width - len([]rune(s))\n\tif n <= 0 {\n\t\treturn s\n\t}\n\tpad := strings.Repeat(string(fill), n)\n\t// CHANGE CODE BELOW THIS LINE\n\treturn s + pad\n\t// CHANGE CODE ABOVE THIS LINE\n}\n",
    "debug": "",
    "file": "padleft.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/02-data-types/senior/parsebool": {
    "id": "01-language-basics/02-data-types/senior/parsebool",
    "title": "Config Bool Parsing",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Config Bool Parsing</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p>A config loader accepts <code>true/false</code>, <code>1/0</code>, <code>yes/no</code>, <code>on/off</code>. But <code>off</code> is</p>\n<p>not recognized \u2014 it falls through to \"unknown\" and the feature flag defaults</p>\n<p>wrong.</p>\n<h2>Task</h2>\n<p>Fix the falsey <code>case</code> between the markers in parsebool.go to</p>\n<p>include <code>\"off\"</code>.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  \"TRUE\"\nOutput: true, true\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  \"off\"\nOutput: false, true\n</code></pre>\n<p>_Explanation:_ must be recognized</p>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  \"maybe\"\nOutput: false, false\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Case list</span><span class=\"tchip\">Normalize input</span><span class=\"tchip\">(value, ok)</span></div></details>",
    "education": "<h1>Recognized vs default</h1>\n<h2>Intuition</h2>\n<p>Return two bools: the parsed value and whether the input was recognized. That</p>\n<p>lets the caller distinguish an explicit <code>false</code> from an unknown string that</p>\n<p>should keep a default. Missing an accepted form (<code>off</code>) silently turns it into</p>\n<p>\"unknown\".</p>\n<h2>Approach</h2>\n<p>1. Bug: the false case omitted \"off\", so \"off\" fell through to unrecognized.</p>\n<p>2. Fix: add \"off\" to the false case list.</p>\n<p>3. Input is lowercased and trimmed first.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nimport \"strings\"\n\nfunc Parse(s string) (bool, bool) {\n\tswitch strings.ToLower(strings.TrimSpace(s)) {\n\tcase \"true\", \"1\", \"yes\", \"on\":\n\t\treturn true, true\n\tcase \"false\", \"0\", \"no\", \"off\":\n\t\treturn false, true\n\t}\n\treturn false, false\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>\"off\": ToLower/Trim -&gt; \"off\" matches false case -&gt; (false,true).</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Normalize (<code>ToLower</code> + <code>TrimSpace</code>) before matching.</li>\n<li>Keep truthy and falsey sets symmetric (<code>on</code>/<code>off</code>, <code>yes</code>/<code>no</code>).</li>\n<li>Unknown input returns <code>ok=false</code>, not <code>false,true</code>.</li>\n</ul>",
    "starter": "// Package parsebool parses common truthy/falsey config strings.\n// A planted bug misclassifies one accepted form.\npackage parsebool\n\nimport \"strings\"\n\n// Parse accepts (case-insensitive): true/false, 1/0, yes/no, on/off.\n// Returns (value, true) if recognized, else (false, false).\nfunc Parse(s string) (bool, bool) {\n\tswitch strings.ToLower(strings.TrimSpace(s)) {\n\tcase \"true\", \"1\", \"yes\", \"on\":\n\t\treturn true, true\n\t// CHANGE CODE BELOW THIS LINE\n\tcase \"false\", \"0\", \"no\":\n\t\t// CHANGE CODE ABOVE THIS LINE\n\t\treturn false, true\n\t}\n\treturn false, false\n}\n",
    "debug": "",
    "file": "parsebool.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/02-data-types/senior/runeat": {
    "id": "01-language-basics/02-data-types/senior/runeat",
    "title": "Rune Index Bounds",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Rune Index Bounds</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p>A text editor addresses characters by index. The bounds check uses <code>len(s)</code> (the</p>\n<p><strong>byte</strong> length), so for multi-byte text it lets an out-of-range rune index</p>\n<p>through \u2014 <code>\"\u65e5\u672c\"</code> has 2 runes but <code>len</code> is 6.</p>\n<h2>Task</h2>\n<p>Fix the bounds check between the markers in runeat.go to use the</p>\n<p>rune count.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  \"h\u00e9llo\",2\nOutput: 'l', true\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  \"\u65e5\u672c\",1\nOutput: '\u672c', true\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  \"\u65e5\u672c\",2\nOutput: 0, false\n</code></pre>\n<p>_Explanation:_ index out of rune range</p>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Byte vs rune length</span><span class=\"tchip\">Bounds check</span><span class=\"tchip\">Consistency</span></div></details>",
    "education": "<h1>Index and bound must agree</h1>\n<h2>Intuition</h2>\n<p><code>rs := []rune(s)</code> gives rune-indexed access. The bounds check must use</p>\n<p><code>len(rs)</code> (rune count), not <code>len(s)</code> (byte count):</p>\n<pre class=\"md\"><code>\nif n &lt; 0 || n &gt;= len(rs) { return 0, false }\n</code></pre>\n<h2>Approach</h2>\n<p>1. Bug: bounds used <code>n &gt;= len(s)</code> (byte length), allowing indices past the rune count for multibyte strings.</p>\n<p>2. Fix: compare against <code>len(rs)</code>, the rune-slice length.</p>\n<p>3. rs[n] then returns the n-th rune.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc At(s string, n int) (rune, bool) {\n\trs := []rune(s)\n\tif n &lt; 0 || n &gt;= len(rs) {\n\t\treturn 0, false\n\t}\n\treturn rs[n], true\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>At(\"\u65e5\u672c\",2): rs has 2 runes, 2&gt;=len(rs)=2 -&gt; false.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>len(s)</code> \u2265 <code>len([]rune(s))</code>, equal only for pure ASCII.</li>\n<li>The conversion <code>[]rune(s)</code> allocates; fine for random access.</li>\n<li>Any time you index one representation, bound with the same one.</li>\n</ul>",
    "starter": "// Package runeat returns the n-th rune of a string.\n// A planted bug indexes bytes instead of runes.\npackage runeat\n\n// At returns the n-th rune (0-based) of s, counting characters not bytes.\n// Returns (0,false) if n is out of range.\nfunc At(s string, n int) (rune, bool) {\n\trs := []rune(s)\n\t// CHANGE CODE BELOW THIS LINE\n\tif n < 0 || n >= len(s) {\n\t\t// CHANGE CODE ABOVE THIS LINE\n\t\treturn 0, false\n\t}\n\treturn rs[n], true\n}\n",
    "debug": "",
    "file": "runeat.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/02-data-types/senior/runewidth": {
    "id": "01-language-basics/02-data-types/senior/runewidth",
    "title": "First Rune Byte Width",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">First Rune Byte Width</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p>A streaming UTF-8 decoder needs to advance by the width of the current rune, but</p>\n<p>the function returns <code>len(s)</code> \u2014 the whole remaining length \u2014 so the decoder</p>\n<p>jumps to the end instead of the next rune.</p>\n<h2>Task</h2>\n<p>Fix the return between the markers in runewidth.go to return the</p>\n<p>first rune's byte size.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  \"a\"\nOutput: 1\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  \"\u00e9\"\nOutput: 2\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  \"\u65e5\u672c\"\nOutput: 3\n</code></pre>\n<p>_Explanation:_ first rune only</p>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">utf8.DecodeRuneInString</span><span class=\"tchip\">Rune width</span><span class=\"tchip\">Advance step</span></div></details>",
    "education": "<h1>Advancing by rune width</h1>\n<h2>Intuition</h2>\n<p><code>utf8.DecodeRuneInString(s)</code> returns the first rune and the number of bytes it</p>\n<p>occupies. A decoder advances by that <code>size</code>, not by the whole string length:</p>\n<pre class=\"md\"><code>\n_, size := utf8.DecodeRuneInString(s)\nreturn size\n</code></pre>\n<h2>Approach</h2>\n<p>1. Bug: returned <code>len(s)</code>, the whole string's byte length.</p>\n<p>2. Fix: return <code>size</code> from utf8.DecodeRuneInString, the first rune's byte width.</p>\n<p>3. Empty string returns 0 early.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nimport \"unicode/utf8\"\n\nfunc FirstWidth(s string) int {\n\tif s == \"\" {\n\t\treturn 0\n\t}\n\t_, size := utf8.DecodeRuneInString(s)\n\t_ = size\n\treturn size\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>FirstWidth(\"\u65e5\u672c\"): DecodeRuneInString gives size=3 for \u65e5 -&gt; return 3.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>DecodeRuneInString</code> returns <code>(RuneError, 1)</code> for invalid bytes, so decoders</li>\n</ul>\n<p>  still make progress.</p>\n<ul>\n<li>Width is 1 for ASCII, up to 4 for astral-plane runes.</li>\n<li><code>for range</code> gives you this stepping for free; manual decode is for control.</li>\n</ul>",
    "starter": "// Package runewidth reports the byte width of the first rune in a string.\n// A planted bug returns the whole string's byte length.\npackage runewidth\n\nimport \"unicode/utf8\"\n\n// FirstWidth returns how many bytes the first rune of s occupies (1..4), or 0\n// for an empty string.\nfunc FirstWidth(s string) int {\n\tif s == \"\" {\n\t\treturn 0\n\t}\n\t_, size := utf8.DecodeRuneInString(s)\n\t_ = size\n\t// CHANGE CODE BELOW THIS LINE\n\treturn len(s)\n\t// CHANGE CODE ABOVE THIS LINE\n}\n",
    "debug": "",
    "file": "runewidth.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/02-data-types/senior/splitlines": {
    "id": "01-language-basics/02-data-types/senior/splitlines",
    "title": "CRLF-Safe Line Split",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">CRLF-Safe Line Split</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p>A log parser splits on <code>\\n</code> but forgets that Windows files use <code>\\r\\n</code>, so every</p>\n<p>line keeps a trailing <code>\\r</code>. Comparisons and trims downstream then mysteriously</p>\n<p>fail.</p>\n<h2>Task</h2>\n<p>Fix the loop body between the markers in splitlines.go to strip</p>\n<p>a trailing <code>\\r</code>.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  \"a\\nb\\nc\"\nOutput: [\"a\" \"b\" \"c\"]\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  \"a\\r\\nb\\r\\nc\"\nOutput: [\"a\" \"b\" \"c\"]\n</code></pre>\n<p>_Explanation:_ CRLF trimmed</p>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  \"\"\nOutput: [\"\"]\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Line endings</span><span class=\"tchip\">TrimSuffix</span><span class=\"tchip\">Idempotence</span></div></details>",
    "education": "<h1>Normalizing line endings</h1>\n<h2>Intuition</h2>\n<p>Split on <code>\\n</code>, then strip a trailing <code>\\r</code> from each piece so CRLF and LF inputs</p>\n<p>produce identical lines:</p>\n<pre class=\"md\"><code>\nparts[i] = strings.TrimSuffix(p, \"\\r\")\n</code></pre>\n<h2>Approach</h2>\n<p>1. Bug: <code>parts[i] = p</code> left the trailing '\\r' on CRLF lines.</p>\n<p>2. Fix: <code>parts[i] = strings.TrimSuffix(p, \"\\r\")</code>.</p>\n<p>3. LF and CRLF now yield identical results.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nimport \"strings\"\n\nfunc Lines(s string) []string {\n\tparts := strings.Split(s, \"\\n\")\n\tfor i, p := range parts {\n\t\tparts[i] = strings.TrimSuffix(p, \"\\r\")\n\t}\n\treturn parts\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>\"a\\r\\nb\": Split on \\n -&gt; [\"a\\r\",\"b\"]; TrimSuffix -&gt; [\"a\",\"b\"].</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>TrimSuffix</code> removes <code>\\r</code> only if present \u2014 safe for LF-only input.</li>\n<li>Don't <code>TrimSpace</code>; that would also drop meaningful leading/trailing spaces.</li>\n<li>Some files end with a trailing newline, yielding a final empty line \u2014 decide</li>\n</ul>\n<p>  whether to keep it.</p>",
    "starter": "// Package splitlines splits text into lines, handling both \\n and \\r\\n.\n// A planted bug leaves a trailing carriage return on CRLF input.\npackage splitlines\n\nimport \"strings\"\n\n// Lines splits s on '\\n' and strips a trailing '\\r' from each line so that\n// Windows (CRLF) and Unix (LF) input yield identical results.\nfunc Lines(s string) []string {\n\tparts := strings.Split(s, \"\\n\")\n\tfor i, p := range parts {\n\t\t// CHANGE CODE BELOW THIS LINE\n\t\tparts[i] = p\n\t\t// CHANGE CODE ABOVE THIS LINE\n\t}\n\treturn parts\n}\n",
    "debug": "",
    "file": "splitlines.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/02-data-types/senior/tolowerascii": {
    "id": "01-language-basics/02-data-types/senior/tolowerascii",
    "title": "ASCII Lowercase Range",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">ASCII Lowercase Range</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p>A header normalizer lowercases ASCII, but the range check only has a lower bound</p>\n<p>(<code>c &gt;= 'A'</code>), so <code>'['</code>, <code>'\\'</code>, <code>']'</code> \u2014 the bytes right after <code>'Z'</code> \u2014 get shifted</p>\n<p>too, corrupting the text.</p>\n<h2>Task</h2>\n<p>Fix the condition between the markers in tolowerascii.go to</p>\n<p>bound <code>A..Z</code>.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  \"Hello\"\nOutput: \"hello\"\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  \"a[b]\"\nOutput: \"a[b]\"\n</code></pre>\n<p>_Explanation:_ '[' just past 'Z' must stay</p>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  \"MixED_case\"\nOutput: \"mixed_case\"\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">ASCII ranges</span><span class=\"tchip\">Both bounds</span><span class=\"tchip\">Case offset</span></div></details>",
    "education": "<h1>Bounded character ranges</h1>\n<h2>Intuition</h2>\n<p>ASCII uppercase letters occupy 65..90. Lowercasing adds 32 \u2014 but only for that</p>\n<p>exact range. A one-sided check (<code>c &gt;= 'A'</code>) also matches <code>[</code>, <code>\\</code>, <code>]</code>, <code>^</code>, <code>_</code>,</p>\n<p>backtick, and every lowercase letter, corrupting them:</p>\n<pre class=\"md\"><code>\nif c &gt;= 'A' &amp;&amp; c &lt;= 'Z' { b[i] = c + 32 }\n</code></pre>\n<h2>Approach</h2>\n<p>1. Bug: condition <code>c &gt;= 'A'</code> also matched bytes above 'Z' ([, \\, ], _, and a-z), corrupting them.</p>\n<p>2. Fix: <code>c &gt;= 'A' &amp;&amp; c &lt;= 'Z'</code> restricts to uppercase letters.</p>\n<p>3. Only A-Z get +32.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Lower(s string) string {\n\tb := []byte(s)\n\tfor i, c := range b {\n\t\tif c &gt;= 'A' &amp;&amp; c &lt;= 'Z' {\n\t\t\tb[i] = c + 32\n\t\t}\n\t}\n\treturn string(b)\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>\"a[b]\": '['=91 &gt; 'Z'=90, now excluded, stays '[' -&gt; \"a[b]\".</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Always bound both ends of a character range.</li>\n<li><code>unicode.ToLower</code> handles non-ASCII correctly; this ASCII-only version is for</li>\n</ul>\n<p>  bytes.</p>\n<ul>\n<li>The <code>+32</code> offset is specific to ASCII letters.</li>\n</ul>",
    "starter": "// Package tolowerascii lowercases ASCII letters only.\n// A planted bug also alters characters just past 'Z'.\npackage tolowerascii\n\n// Lower returns s with A-Z mapped to a-z; every other byte is unchanged.\nfunc Lower(s string) string {\n\tb := []byte(s)\n\tfor i, c := range b {\n\t\t// CHANGE CODE BELOW THIS LINE\n\t\tif c >= 'A' {\n\t\t\t// CHANGE CODE ABOVE THIS LINE\n\t\t\tb[i] = c + 32\n\t\t}\n\t}\n\treturn string(b)\n}\n",
    "debug": "",
    "file": "tolowerascii.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/02-data-types/senior/wordwrap": {
    "id": "01-language-basics/02-data-types/senior/wordwrap",
    "title": "Word Wrap Width",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Word Wrap Width</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p>A terminal UI wraps text to a column width, but an off-by-one (<code>&lt;</code> instead of</p>\n<p><code>&lt;=</code>) breaks one character too early, so <code>\"aa bb\"</code> (exactly 5) is split at</p>\n<p>width 5.</p>\n<h2>Task</h2>\n<p>Fix the fit test between the markers in wordwrap.go so a line may</p>\n<p>use the full width.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  \"the quick brown fox\",10\nOutput: [\"the quick\" \"brown fox\"]\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  \"aa bb cc\",5\nOutput: [\"aa bb\" \"cc\"]\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  \"a b\",3\nOutput: [\"a b\"]\n</code></pre>\n<p>_Explanation:_ length 3 == width fits</p>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Fit condition</span><span class=\"tchip\">Account for space</span><span class=\"tchip\">Off-by-one</span></div></details>",
    "education": "<h1>Inclusive width boundaries</h1>\n<h2>Intuition</h2>\n<p>A word fits on the current line when the resulting length \u2014 existing text, one</p>\n<p>space, and the word \u2014 is <strong>at most</strong> the width. The boundary is inclusive:</p>\n<pre class=\"md\"><code>\nif len(line)+1+len(w) &lt;= width { line += \" \" + w }\n</code></pre>\n<p>Using <code>&lt;</code> rejects a line that exactly fills the width, wrapping one character too</p>\n<p>soon.</p>\n<h2>Approach</h2>\n<p>1. Bug: off-by-one <code>len(line)+1+len(w) &lt; width</code> broke a line that exactly fit the width.</p>\n<p>2. Fix: use <code>&lt;= width</code> so a line reaching exactly width is allowed.</p>\n<p>3. Words that would overflow start a new line.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nimport \"strings\"\n\nfunc Wrap(text string, width int) []string {\n\twords := strings.Fields(text)\n\tvar lines []string\n\tvar line string\n\tfor _, w := range words {\n\t\tif line == \"\" {\n\t\t\tline = w\n\t\t\tcontinue\n\t\t}\n\t\tif len(line)+1+len(w) &lt;= width {\n\t\t\tline += \" \" + w\n\t\t} else {\n\t\t\tlines = append(lines, line)\n\t\t\tline = w\n\t\t}\n\t}\n\tif line != \"\" {\n\t\tlines = append(lines, line)\n\t}\n\treturn lines\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>\"aa bb cc\" w5: \"aa\"+ \" bb\" len5 &lt;=5 ok -&gt; \"aa bb\"; adding \" cc\" len8 &gt;5 -&gt; new line \"cc\".</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Include the separator (<code>+1</code>) in the length math.</li>\n<li><code>&lt;=</code> vs <code>&lt;</code> is the whole bug \u2014 inclusive width.</li>\n<li>This simplified version counts bytes; real wrapping counts runes/display width.</li>\n</ul>",
    "starter": "// Package wordwrap breaks text into lines no longer than width runes.\n// A planted off-by-one lets a line exceed the width.\npackage wordwrap\n\nimport \"strings\"\n\n// Wrap joins words with spaces into lines whose length is at most width runes,\n// breaking before a word that would overflow. Assumes single-space input, no\n// word longer than width.\nfunc Wrap(text string, width int) []string {\n\twords := strings.Fields(text)\n\tvar lines []string\n\tvar line string\n\tfor _, w := range words {\n\t\tif line == \"\" {\n\t\t\tline = w\n\t\t\tcontinue\n\t\t}\n\t\t// CHANGE CODE BELOW THIS LINE\n\t\tif len(line)+1+len(w) < width {\n\t\t\t// CHANGE CODE ABOVE THIS LINE\n\t\t\tline += \" \" + w\n\t\t} else {\n\t\t\tlines = append(lines, line)\n\t\t\tline = w\n\t\t}\n\t}\n\tif line != \"\" {\n\t\tlines = append(lines, line)\n\t}\n\treturn lines\n}\n",
    "debug": "",
    "file": "wordwrap.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/02-data-types/staff/bankround": {
    "id": "01-language-basics/02-data-types/staff/bankround",
    "title": "Banker's Rounding",
    "level": "staff",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Banker's Rounding</h1><div class=\"p-meta\"><span class=\"pill lv-staff\">staff</span></div></div><h2>Context</h2>\n<p>A financial report must round half-to-even to avoid the upward bias of</p>\n<p>round-half-away. The code uses <code>math.Round</code>, which rounds half <strong>away</strong> from zero</p>\n<p>(<code>2.5 -&gt; 3</code>), skewing totals.</p>\n<h2>Task</h2>\n<p>Fix the body between the markers in bankround.go to round ties to</p>\n<p>the even neighbor. <code>math.RoundToEven</code> does exactly this.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  2.5\nOutput: 2\n</code></pre>\n<p>_Explanation:_ Tie rounds to the even neighbor 2, not 3.</p>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  3.5\nOutput: 4\n</code></pre>\n<p>_Explanation:_ Tie rounds to even 4.</p>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  -2.5\nOutput: -2\n</code></pre>\n<p>_Explanation:_ Tie rounds to even -2, not -3.</p>\n<p><strong>Example 4:</strong></p>\n<pre class=\"md\"><code>\nInput:  2.6\nOutput: 3\n</code></pre>\n<p>_Explanation:_ Non-tie rounds normally to nearest.</p>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Round modes</span><span class=\"tchip\">Bias</span><span class=\"tchip\">math.RoundToEven</span></div></details>",
    "education": "<h1>Rounding modes and bias</h1>\n<h2>Intuition</h2>\n<p><code>math.Round</code> rounds halves <strong>away from zero</strong> (2.5\u21923, 3.5\u21924), which biases sums</p>\n<p>upward when many values end in .5. Banker's rounding sends ties to the <strong>even</strong></p>\n<p>neighbor (2.5\u21922, 3.5\u21924), so the bias cancels over a data set:</p>\n<pre class=\"md\"><code>\nmath.RoundToEven(x)\n</code></pre>\n<h2>Approach</h2>\n<p>1. Bug: math.Round rounds half AWAY from zero (2.5-&gt;3, -2.5-&gt;-3).</p>\n<p>2. Bankers rounding sends ties to the even neighbor.</p>\n<p>3. Fix: use math.RoundToEven(x).</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nimport \"math\"\n\nfunc Round(x float64) float64 {\n\treturn math.RoundToEven(x)\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>2.5 -&gt; nearest evens 2 and 4, pick even 2. 3.5 -&gt; pick even 4. 2.6 not a tie -&gt; 3.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>math.Round</code> \u2260 <code>math.RoundToEven</code>; pick deliberately.</li>\n<li>Half-to-even is the IEEE-754 default rounding for float ops.</li>\n<li>Rounding to N decimals still needs scaling; the tie rule is the subtle part.</li>\n</ul>",
    "starter": "// Package bankround rounds half to even (\"banker's rounding\").\n// A planted bug rounds half away from zero instead.\npackage bankround\n\nimport \"math\"\n\n// Round returns x rounded to the nearest integer, with ties going to the even\n// neighbor (2.5 -> 2, 3.5 -> 4). Returned as float64.\nfunc Round(x float64) float64 {\n\t// CHANGE CODE BELOW THIS LINE\n\treturn math.Round(x)\n\t// CHANGE CODE ABOVE THIS LINE\n}\n",
    "debug": "",
    "file": "bankround.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/02-data-types/staff/base64val": {
    "id": "01-language-basics/02-data-types/staff/base64val",
    "title": "Base64 Digit Offset",
    "level": "staff",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Base64 Digit Offset</h1><div class=\"p-meta\"><span class=\"pill lv-staff\">staff</span></div></div><h2>Context</h2>\n<p>A base64 decoder maps characters to 6-bit values. Digits <code>0-9</code> are values</p>\n<p><strong>52-61</strong>, but the code adds 53, so every digit is off by one and decoded bytes</p>\n<p>are corrupt.</p>\n<h2>Task</h2>\n<p>Fix the digit offset between the markers in base64val.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  'A'\nOutput: 0, true\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  'z'\nOutput: 51, true\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  '0'\nOutput: 52, true\n</code></pre>\n<p>_Explanation:_ Digits start at value 52.</p>\n<p><strong>Example 4:</strong></p>\n<pre class=\"md\"><code>\nInput:  '='\nOutput: 0, false\n</code></pre>\n<p>_Explanation:_ Not a base64 char.</p>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Base64 alphabet</span><span class=\"tchip\">Range offsets</span><span class=\"tchip\">Off-by-one</span></div></details>",
    "education": "<h1>Contiguous alphabet offsets</h1>\n<h2>Intuition</h2>\n<p>The base64 value table concatenates four runs: 26 uppercase (0-25), 26 lowercase</p>\n<p>(26-51), 10 digits (52-61), then <code>+</code> (62) and <code>/</code> (63). Each run's base is the</p>\n<p>running total of the runs before it:</p>\n<pre class=\"md\"><code>\ncase c &gt;= '0' &amp;&amp; c &lt;= '9': return int(c-'0') + 52, true\n</code></pre>\n<h2>Approach</h2>\n<p>1. Bug: digit branch adds 53, so '0' yields 53 (off by one).</p>\n<p>2. Base64 alphabet: A-Z=0..25, a-z=26..51, 0-9=52..61.</p>\n<p>3. Fix: int(c-'0') + 52.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Value(c byte) (int, bool) {\n\tswitch {\n\tcase c &gt;= 'A' &amp;&amp; c &lt;= 'Z':\n\t\treturn int(c - 'A'), true\n\tcase c &gt;= 'a' &amp;&amp; c &lt;= 'z':\n\t\treturn int(c-'a') + 26, true\n\tcase c &gt;= '0' &amp;&amp; c &lt;= '9':\n\t\treturn int(c-'0') + 52, true\n\tcase c == '+':\n\t\treturn 62, true\n\tcase c == '/':\n\t\treturn 63, true\n\t}\n\treturn 0, false\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>'0'-'0'=0, +52 = 52. '9'-'0'=9, +52 = 61. Ranges now abut correctly.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>'a' starts at 26 (after 26 letters), '0' at 52 \u2014 count the runs.</li>\n<li>URL-safe base64 uses <code>-</code> and <code>_</code> instead of <code>+</code> and <code>/</code>.</li>\n<li>Reject non-alphabet bytes (including padding <code>=</code>) explicitly.</li>\n</ul>",
    "starter": "// Package base64val maps a standard base64 character to its 6-bit value.\n// A planted bug offsets the digit range by one.\npackage base64val\n\n// Value returns the 0..63 value of a base64 character (A-Z, a-z, 0-9, +, /),\n// or (0,false) for any other byte.\nfunc Value(c byte) (int, bool) {\n\tswitch {\n\tcase c >= 'A' && c <= 'Z':\n\t\treturn int(c - 'A'), true\n\tcase c >= 'a' && c <= 'z':\n\t\treturn int(c-'a') + 26, true\n\t// CHANGE CODE BELOW THIS LINE\n\tcase c >= '0' && c <= '9':\n\t\treturn int(c-'0') + 53, true\n\t// CHANGE CODE ABOVE THIS LINE\n\tcase c == '+':\n\t\treturn 62, true\n\tcase c == '/':\n\t\treturn 63, true\n\t}\n\treturn 0, false\n}\n",
    "debug": "",
    "file": "base64val.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/02-data-types/staff/checkedadd": {
    "id": "01-language-basics/02-data-types/staff/checkedadd",
    "title": "Checked Int64 Addition",
    "level": "staff",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Checked Int64 Addition</h1><div class=\"p-meta\"><span class=\"pill lv-staff\">staff</span></div></div><h2>Context</h2>\n<p>An accumulator must detect int64 overflow instead of silently wrapping. The</p>\n<p>positive-overflow test is <code>a &gt; math.MaxInt64</code>, which is <strong>never</strong> true (nothing</p>\n<p>exceeds the max), so <code>MaxInt64 + 1</code> wraps undetected.</p>\n<h2>Task</h2>\n<p>Fix the positive-overflow check between the markers in</p>\n<p>checkedadd.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  math.MaxInt64, 1\nOutput: 0, false\n</code></pre>\n<p>_Explanation:_ Positive overflow detected.</p>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  math.MaxInt64-1, 1\nOutput: math.MaxInt64, true\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  math.MinInt64, -1\nOutput: 0, false\n</code></pre>\n<p>_Explanation:_ Negative overflow.</p>\n<p><strong>Example 4:</strong></p>\n<pre class=\"md\"><code>\nInput:  -5, -5\nOutput: -10, true\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Overflow condition</span><span class=\"tchip\">Rearrange to avoid overflow</span><span class=\"tchip\">Symmetric negative case</span></div></details>",
    "education": "<h1>Detecting integer overflow before it happens</h1>\n<h2>Intuition</h2>\n<p>You cannot test overflow by computing <code>a+b</code> \u2014 that already wrapped. Rearrange so</p>\n<p>the check itself never overflows: for <code>b &gt; 0</code>, the sum overflows exactly when</p>\n<pre class=\"md\"><code>\na &gt; math.MaxInt64 - b\n</code></pre>\n<p>and symmetrically for <code>b &lt; 0</code>, when <code>a &lt; math.MinInt64 - b</code>.</p>\n<h2>Approach</h2>\n<p>1. Bug: <code>a &gt; math.MaxInt64</code> is never true (a is int64), so positive overflow is missed.</p>\n<p>2. Overflow when a+b &gt; MaxInt64, i.e. a &gt; MaxInt64 - b (no overflow in the check itself since b&gt;0).</p>\n<p>3. Fix: if b &gt; 0 &amp;&amp; a &gt; math.MaxInt64-b.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nimport \"math\"\n\nfunc Add(a, b int64) (int64, bool) {\n\tif b &gt; 0 &amp;&amp; a &gt; math.MaxInt64-b {\n\t\treturn 0, false\n\t}\n\tif b &lt; 0 &amp;&amp; a &lt; math.MinInt64-b {\n\t\treturn 0, false\n\t}\n\treturn a + b, true\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>MaxInt64 + 1: b=1&gt;0, a=MaxInt64 &gt; MaxInt64-1 -&gt; true -&gt; (0,false).</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Never test overflow using the overflowed result.</li>\n<li><code>MaxInt64 - b</code> is safe because <code>b &gt; 0</code>; <code>MinInt64 - b</code> is safe because <code>b &lt; 0</code>.</li>\n<li><code>math/bits.Add64</code> and Go 1.21's overflow-aware helpers exist for this.</li>\n</ul>",
    "starter": "// Package checkedadd adds int64 values with overflow detection.\n// A planted bug misses positive overflow.\npackage checkedadd\n\nimport \"math\"\n\n// Add returns a+b and ok=false if the mathematical sum overflows int64.\nfunc Add(a, b int64) (int64, bool) {\n\t// CHANGE CODE BELOW THIS LINE\n\tif b > 0 && a > math.MaxInt64 {\n\t\t// CHANGE CODE ABOVE THIS LINE\n\t\treturn 0, false\n\t}\n\tif b < 0 && a < math.MinInt64-b {\n\t\treturn 0, false\n\t}\n\treturn a + b, true\n}\n",
    "debug": "",
    "file": "checkedadd.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/02-data-types/staff/endianswap": {
    "id": "01-language-basics/02-data-types/staff/endianswap",
    "title": "Byte-Order Swap",
    "level": "staff",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Byte-Order Swap</h1><div class=\"p-meta\"><span class=\"pill lv-staff\">staff</span></div></div><h2>Context</h2>\n<p>A network layer converts a 32-bit field between host and network byte order. One</p>\n<p>lane uses the mask <code>0xFF00</code> where it should be <code>0xFF0000</code>, so byte 1 lands in the</p>\n<p>wrong place.</p>\n<h2>Task</h2>\n<p>Fix the masks between the markers in endianswap.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  0x11223344\nOutput: 0x44332211\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  0x00000001\nOutput: 0x01000000\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  0xDEADBEEF\nOutput: 0xEFBEADDE\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Byte lanes</span><span class=\"tchip\">Shift + mask</span><span class=\"tchip\">Symmetry</span></div></details>",
    "education": "<h1>Reversing byte order</h1>\n<h2>Intuition</h2>\n<p>Each byte moves to the mirrored lane, and the mask must match the <strong>destination</strong></p>\n<p>lane:</p>\n<pre class=\"md\"><code>\nx&lt;&lt;24                 // byte0 -&gt; byte3 (no mask; shifted out bits vanish)\n(x&lt;&lt;8)  &amp; 0xFF0000    // byte1 -&gt; byte2\n(x&gt;&gt;8)  &amp; 0xFF00      // byte2 -&gt; byte1\nx&gt;&gt;24                 // byte3 -&gt; byte0\n</code></pre>\n<h2>Approach</h2>\n<p>1. Bug: second lane mask is 0xFF00 instead of 0xFF0000, corrupting byte 2.</p>\n<p>2. Reversing 4 bytes needs lanes masked at 0xFF000000/0xFF0000/0xFF00/0xFF.</p>\n<p>3. Fix: (x&lt;&lt;8)&amp;0xFF0000.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Swap32(x uint32) uint32 {\n\treturn x&lt;&lt;24 | (x&lt;&lt;8)&amp;0xFF0000 | (x&gt;&gt;8)&amp;0xFF00 | x&gt;&gt;24\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>0x11223344: &lt;&lt;24=0x44..., (x&lt;&lt;8)&amp;0xFF0000=0x330000, (x&gt;&gt;8)&amp;0xFF00=0x2200, &gt;&gt;24=0x11 -&gt; 0x44332211.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>The mask names where a byte <strong>lands</strong>, after the shift.</li>\n<li>Outer lanes (<code>&lt;&lt;24</code>, <code>&gt;&gt;24</code>) need no mask; the shift discards the rest.</li>\n<li><code>math/bits.ReverseBytes32</code> does this in one instruction.</li>\n</ul>",
    "starter": "// Package endianswap reverses the byte order of a uint32.\n// A planted bug uses the wrong mask on one byte lane.\npackage endianswap\n\n// Swap32 returns x with its four bytes reversed (big-endian <-> little-endian).\nfunc Swap32(x uint32) uint32 {\n\t// CHANGE CODE BELOW THIS LINE\n\treturn x<<24 | (x<<8)&0xFF00 | (x>>8)&0xFF00 | x>>24\n\t// CHANGE CODE ABOVE THIS LINE\n}\n",
    "debug": "",
    "file": "endianswap.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/02-data-types/staff/floatbits": {
    "id": "01-language-basics/02-data-types/staff/floatbits",
    "title": "Float64 Exponent Bias",
    "level": "staff",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Float64 Exponent Bias</h1><div class=\"p-meta\"><span class=\"pill lv-staff\">staff</span></div></div><h2>Context</h2>\n<p>A numerics library reads the IEEE-754 exponent from the raw bits. The exponent</p>\n<p>field is stored <strong>biased by 1023</strong>, but the code subtracts 1024, so every</p>\n<p>exponent is off by one.</p>\n<h2>Task</h2>\n<p>Fix the bias between the markers in floatbits.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  1\nOutput: 0\n</code></pre>\n<p>_Explanation:_ 1 = 1.0 * 2^0.</p>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  2\nOutput: 1\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  0.5\nOutput: -1\n</code></pre>\n<p><strong>Example 4:</strong></p>\n<pre class=\"md\"><code>\nInput:  1024\nOutput: 10\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">IEEE-754 layout</span><span class=\"tchip\">Exponent bias</span><span class=\"tchip\">Bit extraction</span></div></details>",
    "education": "<h1>IEEE-754 double layout</h1>\n<h2>Intuition</h2>\n<p>A <code>float64</code> is 1 sign bit, 11 exponent bits, and 52 mantissa bits. The exponent</p>\n<p>is stored with a <strong>bias of 1023</strong>, so the real exponent is:</p>\n<pre class=\"md\"><code>\nraw := int((math.Float64bits(x) &gt;&gt; 52) &amp; 0x7FF)\nexp := raw - 1023\n</code></pre>\n<p><code>1.0</code> has raw exponent 1023 \u2192 unbiased 0; <code>2.0</code> \u2192 1024 \u2192 1.</p>\n<h2>Approach</h2>\n<p>1. Bug: subtracts bias 1024; float64 exponent bias is 1023.</p>\n<p>2. Raw 11-bit exponent minus 1023 gives the unbiased exponent.</p>\n<p>3. Fix: return raw - 1023.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nimport \"math\"\n\nfunc Exponent(x float64) int {\n\tbits := math.Float64bits(x)\n\traw := int((bits &gt;&gt; 52) &amp; 0x7FF)\n\treturn raw - 1023\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>x=1 has raw exponent 1023; 1023-1023=0. x=2 raw=1024 -&gt; 1.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Bias is <code>2^(11-1) - 1 = 1023</code> for float64; float32's bias is 127.</li>\n<li><code>raw == 0</code> is subnormal/zero; <code>raw == 0x7FF</code> is Inf/NaN \u2014 special cases.</li>\n<li>Mask the exponent with <code>0x7FF</code> (11 bits) after shifting right by 52.</li>\n</ul>",
    "starter": "// Package floatbits extracts the unbiased exponent of a float64.\n// A planted bug uses the wrong bias.\npackage floatbits\n\nimport \"math\"\n\n// Exponent returns the unbiased base-2 exponent of a normal, positive x\n// (x = mantissa * 2^exponent, 1 <= mantissa < 2). E.g. Exponent(1)=0,\n// Exponent(2)=1, Exponent(0.5)=-1.\nfunc Exponent(x float64) int {\n\tbits := math.Float64bits(x)\n\traw := int((bits >> 52) & 0x7FF)\n\t// CHANGE CODE BELOW THIS LINE\n\treturn raw - 1024\n\t// CHANGE CODE ABOVE THIS LINE\n}\n",
    "debug": "",
    "file": "floatbits.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/02-data-types/staff/fnv1a": {
    "id": "01-language-basics/02-data-types/staff/fnv1a",
    "title": "FNV-1a Hash Order",
    "level": "staff",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">FNV-1a Hash Order</h1><div class=\"p-meta\"><span class=\"pill lv-staff\">staff</span></div></div><h2>Context</h2>\n<p>A hash map uses FNV-1a for keys. The loop multiplies <strong>then</strong> XORs \u2014 that is</p>\n<p>FNV-1, a different hash \u2014 so results never match the reference vectors and</p>\n<p>cross-service hashing disagrees.</p>\n<h2>Task</h2>\n<p>Fix the order between the markers in fnv1a.go: FNV-1a is XOR first,</p>\n<p>then multiply.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  \"\"\nOutput: 0x811c9dc5\n</code></pre>\n<p>_Explanation:_ The FNV offset basis.</p>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  \"a\"\nOutput: 0xe40c292c\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  \"foobar\"\nOutput: 0xbf9cf968\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">FNV-1a step</span><span class=\"tchip\">FNV-1 vs 1a</span><span class=\"tchip\">Unsigned wrap</span></div></details>",
    "education": "<h1>FNV-1a vs FNV-1</h1>\n<h2>Intuition</h2>\n<p>FNV-1a hashes each byte as <strong>XOR then multiply</strong>:</p>\n<pre class=\"md\"><code>\nh = (h ^ uint32(b)) * prime32\n</code></pre>\n<p>FNV-1 does the reverse (multiply then XOR). Same constants, different avalanche</p>\n<p>behavior and different output \u2014 1a mixes low bits better.</p>\n<h2>Approach</h2>\n<p>1. Bug: does multiply-then-xor, which is FNV-1, not FNV-1a.</p>\n<p>2. FNV-1a XORs the byte first, then multiplies by the prime.</p>\n<p>3. Fix: h = (h ^ uint32(b)) * prime32.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nconst (\n\toffset32 = 2166136261\n\tprime32  = 16777619\n)\n\nfunc Hash(data []byte) uint32 {\n\th := uint32(offset32)\n\tfor _, b := range data {\n\t\th = (h ^ uint32(b)) * prime32\n\t}\n\treturn h\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>\"a\": h=offset, h^=0x61 then *prime32 -&gt; 0xe40c292c.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>The 32-bit offset basis is <code>0x811c9dc5</code>; the prime is <code>16777619</code>.</li>\n<li>The <code>uint32</code> multiply wraps mod 2\u00b3\u00b2 \u2014 that overflow is intended.</li>\n<li>Empty input hashes to the offset basis, a good sanity check.</li>\n</ul>",
    "starter": "// Package fnv1a computes the 32-bit FNV-1a hash.\n// A planted bug swaps the xor/multiply order (that is FNV-1, not FNV-1a).\npackage fnv1a\n\nconst (\n\toffset32 = 2166136261\n\tprime32  = 16777619\n)\n\n// Hash returns the FNV-1a 32-bit hash of data: for each byte, XOR then multiply.\nfunc Hash(data []byte) uint32 {\n\th := uint32(offset32)\n\tfor _, b := range data {\n\t\t// CHANGE CODE BELOW THIS LINE\n\t\th = (h * prime32) ^ uint32(b)\n\t\t// CHANGE CODE ABOVE THIS LINE\n\t}\n\treturn h\n}\n",
    "debug": "",
    "file": "fnv1a.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/02-data-types/staff/graycode": {
    "id": "01-language-basics/02-data-types/staff/graycode",
    "title": "Gray Code Encoding",
    "level": "staff",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Gray Code Encoding</h1><div class=\"p-meta\"><span class=\"pill lv-staff\">staff</span></div></div><h2>Context</h2>\n<p>A rotary encoder outputs Gray code so only one bit changes per step. The formula</p>\n<p>is <code>x ^ (x &gt;&gt; 1)</code>, but the code shifts <strong>left</strong>, breaking the single-bit-change</p>\n<p>property.</p>\n<h2>Task</h2>\n<p>Fix the shift between the markers in graycode.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  2\nOutput: 3\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  3\nOutput: 2\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  4\nOutput: 6\n</code></pre>\n<p><strong>Example 4:</strong></p>\n<pre class=\"md\"><code>\nInput:  7\nOutput: 4\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Gray code</span><span class=\"tchip\">Single-bit change</span><span class=\"tchip\">Shift direction</span></div></details>",
    "education": "<h1>Reflected Gray code</h1>\n<h2>Intuition</h2>\n<p>Binary-to-Gray is a single XOR with the value shifted <strong>right</strong> by one:</p>\n<pre class=\"md\"><code>\nx ^ (x &gt;&gt; 1)\n</code></pre>\n<p>This guarantees consecutive integers map to codes that differ in exactly one bit.</p>\n<h2>Approach</h2>\n<p>1. Bug: shifts left (x&lt;&lt;1); Gray code uses right shift.</p>\n<p>2. Gray = x XOR (x&gt;&gt;1).</p>\n<p>3. Fix: return x ^ (x &gt;&gt; 1).</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc ToGray(x uint32) uint32 {\n\treturn x ^ (x &gt;&gt; 1)\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>x=4 (100): x&gt;&gt;1=010, 100^010=110=6.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Encoding shifts right; decoding is a different loop (XOR of all higher bits).</li>\n<li>Left vs right shift is the whole bug \u2014 direction matters.</li>\n<li>Works per fixed width; the top bit passes through unchanged.</li>\n</ul>",
    "starter": "// Package graycode converts a binary number to reflected Gray code.\n// A planted bug shifts the wrong direction.\npackage graycode\n\n// ToGray returns the Gray-code encoding of x: x XOR (x >> 1).\nfunc ToGray(x uint32) uint32 {\n\t// CHANGE CODE BELOW THIS LINE\n\treturn x ^ (x << 1)\n\t// CHANGE CODE ABOVE THIS LINE\n}\n",
    "debug": "",
    "file": "graycode.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/02-data-types/staff/parity": {
    "id": "01-language-basics/02-data-types/staff/parity",
    "title": "Parity Bit",
    "level": "staff",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Parity Bit</h1><div class=\"p-meta\"><span class=\"pill lv-staff\">staff</span></div></div><h2>Context</h2>\n<p>A serial link appends a parity bit: 1 for an odd number of set bits. The code</p>\n<p><strong>sums</strong> the bits and returns the count, so it reports 3 instead of 1 for</p>\n<p><code>0b111</code> \u2014 not a parity bit at all.</p>\n<h2>Task</h2>\n<p>Fix the accumulation between the markers in parity.go to XOR.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  1\nOutput: 1\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  3\nOutput: 0\n</code></pre>\n<p>_Explanation:_ Two set bits -&gt; even parity.</p>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  0xFF\nOutput: 0\n</code></pre>\n<p>_Explanation:_ Eight set bits -&gt; even.</p>\n<p><strong>Example 4:</strong></p>\n<pre class=\"md\"><code>\nInput:  0x80000000\nOutput: 1\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Parity = XOR of bits</span><span class=\"tchip\">XOR accumulation</span><span class=\"tchip\">Sum vs parity</span></div></details>",
    "education": "<h1>Parity via XOR</h1>\n<h2>Intuition</h2>\n<p>The parity bit is 1 exactly when an odd number of bits are set. XOR is the parity</p>\n<p>of two bits, so folding every bit with XOR yields the parity of the whole word:</p>\n<pre class=\"md\"><code>\np ^= int(x &amp; 1)\n</code></pre>\n<h2>Approach</h2>\n<p>1. Bug: <code>p += bit</code> accumulates the popcount, not parity.</p>\n<p>2. Parity is the XOR of all bits, giving 0 or 1.</p>\n<p>3. Fix: p ^= int(x &amp; 1).</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Parity(x uint32) int {\n\tp := 0\n\tfor x != 0 {\n\t\tp ^= int(x &amp; 1)\n\t\tx &gt;&gt;= 1\n\t}\n\treturn p\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>x=3: bits 1,1 -&gt; 0^1^1 = 0.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>^=</code> folds to 0/1; <code>+=</code> accumulates the count.</li>\n<li><code>math/bits.OnesCount32(x) &amp; 1</code> is the same parity in one step.</li>\n<li>Parity detects an odd number of bit errors, not their location.</li>\n</ul>",
    "starter": "// Package parity computes the parity bit of a value.\n// A planted bug sums bits instead of XOR-ing them.\npackage parity\n\n// Parity returns 1 if x has an odd number of set bits, else 0.\nfunc Parity(x uint32) int {\n\tp := 0\n\tfor x != 0 {\n\t\t// CHANGE CODE BELOW THIS LINE\n\t\tp += int(x & 1)\n\t\t// CHANGE CODE ABOVE THIS LINE\n\t\tx >>= 1\n\t}\n\treturn p\n}\n",
    "debug": "",
    "file": "parity.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/02-data-types/staff/rune2decode": {
    "id": "01-language-basics/02-data-types/staff/rune2decode",
    "title": "Decode 2-Byte UTF-8",
    "level": "staff",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Decode 2-Byte UTF-8</h1><div class=\"p-meta\"><span class=\"pill lv-staff\">staff</span></div></div><h2>Context</h2>\n<p>A hand-written decoder combines a 2-byte sequence into a rune. It masks the lead</p>\n<p>byte with <code>0x0F</code> (4 bits), but a 2-byte lead carries <strong>5</strong> payload bits</p>\n<p>(<code>110xxxxx</code>), so any code point \u2265 U+0100 loses its top bit.</p>\n<h2>Task</h2>\n<p>Fix the lead-byte mask between the markers in rune2decode.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  0xC3, 0xA9\nOutput: '\u00e9' (U+00E9)\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  0xD0, 0x81\nOutput: '\u0401' (U+0401)\n</code></pre>\n<p>_Explanation:_ Needs the 5th lead payload bit.</p>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  0xD8, 0xA7\nOutput: '\u0627' (U+0627)\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Payload bits</span><span class=\"tchip\">Continuation</span><span class=\"tchip\">Assembly</span></div></details>",
    "education": "<h1>Assembling a code point from UTF-8</h1>\n<h2>Intuition</h2>\n<p>A 2-byte UTF-8 rune splits its bits across the two bytes:</p>\n<pre class=\"md\"><code>\nlead: 110xxxxx  (5 payload bits -&gt; mask 0x1F)\ncont: 10yyyyyy  (6 payload bits -&gt; mask 0x3F)\nrune = (x &lt;&lt; 6) | y\n</code></pre>\n<h2>Approach</h2>\n<p>1. Bug: lead masked with 0x0F keeps only 4 bits; 2-byte lead payload is 5 bits.</p>\n<p>2. Lead is 110xxxxx -&gt; low 5 bits (0x1F), shifted left 6, OR cont low 6 bits.</p>\n<p>3. Fix: rune(lead&amp;0x1F)&lt;&lt;6 | rune(cont&amp;0x3F).</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Decode2(lead, cont byte) rune {\n\treturn rune(lead&amp;0x1F)&lt;&lt;6 | rune(cont&amp;0x3F)\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>0xD0&amp;0x1F=0x10, &lt;&lt;6=0x400; 0x81&amp;0x3F=0x01 -&gt; 0x401 = U+0401.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>2-byte lead \u2192 5 bits (<code>0x1F</code>); 3-byte lead \u2192 4 bits (<code>0x0F</code>); the mask depends</li>\n</ul>\n<p>  on the length.</p>\n<ul>\n<li>Continuation bytes always contribute 6 bits (<code>0x3F</code>).</li>\n<li>Shift the lead payload left by 6 (the width of one continuation).</li>\n</ul>",
    "starter": "// Package rune2decode decodes a 2-byte UTF-8 sequence into a rune by hand.\n// A planted bug uses the wrong mask on the lead byte.\npackage rune2decode\n\n// Decode2 combines a 2-byte UTF-8 sequence (lead 110xxxxx, cont 10xxxxxx) into\n// its rune: (lead low 5 bits << 6) | (cont low 6 bits).\nfunc Decode2(lead, cont byte) rune {\n\t// CHANGE CODE BELOW THIS LINE\n\treturn rune(lead&0x0F)<<6 | rune(cont&0x3F)\n\t// CHANGE CODE ABOVE THIS LINE\n}\n",
    "debug": "",
    "file": "rune2decode.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/02-data-types/staff/utf8count": {
    "id": "01-language-basics/02-data-types/staff/utf8count",
    "title": "Rune Count by Lead Bytes",
    "level": "staff",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Rune Count by Lead Bytes</h1><div class=\"p-meta\"><span class=\"pill lv-staff\">staff</span></div></div><h2>Context</h2>\n<p>A metrics job counts characters without allocating a <code>[]rune</code>. The rule is \"count</p>\n<p>bytes that are not continuation bytes\", but the condition is inverted \u2014 it counts</p>\n<p>the continuation bytes, so ASCII counts as 0 and multi-byte text is undercounted.</p>\n<h2>Task</h2>\n<p>Fix the condition between the markers in utf8count.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  \"hello\"\nOutput: 5\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  \"h\u00e9llo\"\nOutput: 5\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  \"\u65e5\u672c\u8a9e\"\nOutput: 3\n</code></pre>\n<p><strong>Example 4:</strong></p>\n<pre class=\"md\"><code>\nInput:  \"a\u20acb\"\nOutput: 3\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Continuation byte</span><span class=\"tchip\">One lead per rune</span><span class=\"tchip\">No allocation</span></div></details>",
    "education": "<h1>Counting runes without decoding</h1>\n<h2>Intuition</h2>\n<p>Every rune has exactly one lead byte; continuation bytes are <code>10xxxxxx</code></p>\n<p>(<code>c&amp;0xC0 == 0x80</code>). So the rune count equals the number of <strong>non-continuation</strong></p>\n<p>bytes:</p>\n<pre class=\"md\"><code>\nif c&amp;0xC0 != 0x80 { n++ }\n</code></pre>\n<h2>Approach</h2>\n<p>1. Bug: counts bytes that ARE continuations (==0x80), inverting the logic.</p>\n<p>2. Each rune has exactly one non-continuation lead byte; count those.</p>\n<p>3. Fix: if c&amp;0xC0 != 0x80.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Count(b []byte) int {\n\tn := 0\n\tfor _, c := range b {\n\t\tif c&amp;0xC0 != 0x80 {\n\t\t\tn++\n\t\t}\n\t}\n\treturn n\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>\"\u65e5\u672c\u8a9e\": 9 bytes, 6 are continuations, 3 lead bytes -&gt; count 3.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>ASCII bytes (<code>0xxxxxxx</code>) are non-continuation and count as one rune each.</li>\n<li>This assumes valid UTF-8; invalid bytes need a real decoder.</li>\n<li><code>c&amp;0xC0</code> isolates the top two bits \u2014 the continuation signature is <code>10</code>.</li>\n</ul>",
    "starter": "// Package utf8count counts runes by scanning lead bytes.\n// A planted bug counts continuation bytes instead.\npackage utf8count\n\n// Count returns the number of runes in a valid UTF-8 byte slice by counting\n// every byte that is NOT a continuation byte (10xxxxxx).\nfunc Count(b []byte) int {\n\tn := 0\n\tfor _, c := range b {\n\t\t// CHANGE CODE BELOW THIS LINE\n\t\tif c&0xC0 == 0x80 {\n\t\t\t// CHANGE CODE ABOVE THIS LINE\n\t\t\tn++\n\t\t}\n\t}\n\treturn n\n}\n",
    "debug": "",
    "file": "utf8count.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/02-data-types/staff/utf8valid": {
    "id": "01-language-basics/02-data-types/staff/utf8valid",
    "title": "UTF-8 Lead Byte Mask",
    "level": "staff",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">UTF-8 Lead Byte Mask</h1><div class=\"p-meta\"><span class=\"pill lv-staff\">staff</span></div></div><h2>Context</h2>\n<p>A protocol layer validates incoming UTF-8. The 2-byte lead test uses</p>\n<p><code>c&amp;0xC0 == 0xC0</code>, which also matches 3- and 4-byte leads (0xE0, 0xF0), so those</p>\n<p>are decoded as 2-byte sequences and valid text is rejected.</p>\n<h2>Task</h2>\n<p>Fix the 2-byte lead mask between the markers in</p>\n<p>utf8valid.go to match only <code>110xxxxx</code>.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  \"\u00e9\" (C3 A9)\nOutput: true\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  \"\u20ac\" (E2 82 AC)\nOutput: true\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  [0xC3]\nOutput: false\n</code></pre>\n<p>_Explanation:_ Truncated 2-byte sequence.</p>\n<p><strong>Example 4:</strong></p>\n<pre class=\"md\"><code>\nInput:  [0x80]\nOutput: false\n</code></pre>\n<p>_Explanation:_ Lone continuation byte.</p>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">UTF-8 lead bytes</span><span class=\"tchip\">Prefix masks</span><span class=\"tchip\">Continuation</span></div></details>",
    "education": "<h1>UTF-8 lead-byte classification</h1>\n<h2>Intuition</h2>\n<p>UTF-8 encodes the byte count in the lead byte's high bits:</p>\n<p>| bytes | lead pattern | mask test |</p>\n<p>|-------|--------------|-----------|</p>\n<p>| 1 | <code>0xxxxxxx</code> | <code>c &lt; 0x80</code> |</p>\n<p>| 2 | <code>110xxxxx</code> | <code>c&amp;0xE0 == 0xC0</code> |</p>\n<p>| 3 | <code>1110xxxx</code> | <code>c&amp;0xF0 == 0xE0</code> |</p>\n<p>| 4 | <code>11110xxx</code> | <code>c&amp;0xF8 == 0xF0</code> |</p>\n<p>Continuation bytes are <code>10xxxxxx</code> (<code>c&amp;0xC0 == 0x80</code>).</p>\n<h2>Approach</h2>\n<p>1. Bug: 2-byte lead test <code>c&amp;0xC0==0xC0</code> also matches 3/4-byte leads, assigning them n=1.</p>\n<p>2. A 2-byte lead is 110xxxxx -&gt; mask 0xE0 must equal 0xC0.</p>\n<p>3. Fix: case c&amp;0xE0 == 0xC0.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Valid(b []byte) bool {\n\ti := 0\n\tfor i &lt; len(b) {\n\t\tc := b[i]\n\t\tvar n int\n\t\tswitch {\n\t\tcase c &lt; 0x80:\n\t\t\tn = 0\n\t\tcase c&amp;0xE0 == 0xC0:\n\t\t\tn = 1\n\t\tcase c&amp;0xF0 == 0xE0:\n\t\t\tn = 2\n\t\tcase c&amp;0xF8 == 0xF0:\n\t\t\tn = 3\n\t\tdefault:\n\t\t\treturn false\n\t\t}\n\t\tfor j := 1; j &lt;= n; j++ {\n\t\t\tif i+j &gt;= len(b) || b[i+j]&amp;0xC0 != 0x80 {\n\t\t\t\treturn false\n\t\t\t}\n\t\t}\n\t\ti += n + 1\n\t}\n\treturn true\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>0xE2 (3-byte lead): 0xE2&amp;0xE0=0xE0 != 0xC0, falls through to 0xF0-mask case -&gt; n=2 correct.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Each mask pairs a bit-width with the exact prefix value.</li>\n<li>Check that enough continuation bytes remain and each is <code>10xxxxxx</code>.</li>\n<li>Full validation also rejects overlong encodings and surrogate/range violations.</li>\n</ul>",
    "starter": "// Package utf8valid validates a UTF-8 byte sequence by hand.\n// A planted bug in the 2-byte lead mask misclassifies longer sequences.\npackage utf8valid\n\n// Valid reports whether b is well-formed UTF-8 (correct lead bytes and the right\n// number of 10xxxxxx continuation bytes). Overlong forms are not specifically\n// rejected here.\nfunc Valid(b []byte) bool {\n\ti := 0\n\tfor i < len(b) {\n\t\tc := b[i]\n\t\tvar n int\n\t\tswitch {\n\t\tcase c < 0x80:\n\t\t\tn = 0\n\t\t// CHANGE CODE BELOW THIS LINE\n\t\tcase c&0xC0 == 0xC0:\n\t\t\t// CHANGE CODE ABOVE THIS LINE\n\t\t\tn = 1\n\t\tcase c&0xF0 == 0xE0:\n\t\t\tn = 2\n\t\tcase c&0xF8 == 0xF0:\n\t\t\tn = 3\n\t\tdefault:\n\t\t\treturn false\n\t\t}\n\t\tfor j := 1; j <= n; j++ {\n\t\t\tif i+j >= len(b) || b[i+j]&0xC0 != 0x80 {\n\t\t\t\treturn false\n\t\t\t}\n\t\t}\n\t\ti += n + 1\n\t}\n\treturn true\n}\n",
    "debug": "",
    "file": "utf8valid.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/02-data-types/staff/varint": {
    "id": "01-language-basics/02-data-types/staff/varint",
    "title": "LEB128 Varint Shift",
    "level": "staff",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">LEB128 Varint Shift</h1><div class=\"p-meta\"><span class=\"pill lv-staff\">staff</span></div></div><h2>Context</h2>\n<p>A protobuf reader decodes varints. Each byte holds <strong>7</strong> value bits, but the</p>\n<p>decoder advances the shift by 8, so multi-byte values are wildly wrong (300</p>\n<p>decodes as 556).</p>\n<h2>Task</h2>\n<p>Fix the shift step between the markers in varint.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  [0xAC, 0x02]\nOutput: 300, 2\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  [0x80, 0x01]\nOutput: 128, 2\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  [0xFF, 0xFF, 0x03]\nOutput: 65535, 3\n</code></pre>\n<p><strong>Example 4:</strong></p>\n<pre class=\"md\"><code>\nInput:  [0x80]\nOutput: 0, 0\n</code></pre>\n<p>_Explanation:_ Truncated mid-varint.</p>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Base-128 groups</span><span class=\"tchip\">Continuation bit</span><span class=\"tchip\">Little-endian groups</span></div></details>",
    "education": "<h1>LEB128 varints</h1>\n<h2>Intuition</h2>\n<p>A varint stores 7 value bits per byte, low group first; the 8th bit signals</p>\n<p>whether more bytes follow. Each successive group shifts up by <strong>7</strong>, not 8:</p>\n<pre class=\"md\"><code>\nv |= uint64(c&amp;0x7F) &lt;&lt; shift\nif c&amp;0x80 == 0 { return v, i + 1 }\nshift += 7\n</code></pre>\n<h2>Approach</h2>\n<p>1. Bug: shift += 8; LEB128 packs 7 payload bits per byte.</p>\n<p>2. Each byte contributes 7 bits, so the shift must grow by 7.</p>\n<p>3. Fix: shift += 7.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Decode(b []byte) (uint64, int) {\n\tvar v uint64\n\tvar shift uint\n\tfor i := 0; i &lt; len(b); i++ {\n\t\tc := b[i]\n\t\tv |= uint64(c&amp;0x7F) &lt;&lt; shift\n\t\tif c&amp;0x80 == 0 {\n\t\t\treturn v, i + 1\n\t\t}\n\t\tshift += 7\n\t}\n\treturn 0, 0\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>[0xAC,0x02]: 0x2C=44 at shift0, 0x02=2 at shift7 -&gt; 44 | 256 = 300.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Mask the payload with <code>0x7F</code>; the top bit is the continuation flag, not data.</li>\n<li>Groups are little-endian: the first byte is the least significant 7 bits.</li>\n<li>Guard against a slice that ends with the continuation bit still set.</li>\n</ul>",
    "starter": "// Package varint decodes an unsigned LEB128 varint (as used by protobuf).\n// A planted bug advances the shift by 8 bits instead of 7.\npackage varint\n\n// Decode reads a base-128 varint from b, returning the value and the number of\n// bytes consumed. Each byte contributes 7 bits; the high bit means \"more\".\n// Returns (0,0) if the slice ends mid-varint.\nfunc Decode(b []byte) (uint64, int) {\n\tvar v uint64\n\tvar shift uint\n\tfor i := 0; i < len(b); i++ {\n\t\tc := b[i]\n\t\tv |= uint64(c&0x7F) << shift\n\t\tif c&0x80 == 0 {\n\t\t\treturn v, i + 1\n\t\t}\n\t\t// CHANGE CODE BELOW THIS LINE\n\t\tshift += 8\n\t\t// CHANGE CODE ABOVE THIS LINE\n\t}\n\treturn 0, 0\n}\n",
    "debug": "",
    "file": "varint.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/03-composite-types/junior/arrayval": {
    "id": "01-language-basics/03-composite-types/junior/arrayval",
    "title": "Array Value Semantics",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Array Value Semantics</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>Unlike a slice, a Go <strong>array</strong> is a value. Assigning it or passing it to a</p>\n<p>function copies the whole thing, so a helper can't change the caller's array in</p>\n<p>place \u2014 it has to return the new one.</p>\n<h2>Task</h2>\n<p>Implement <code>SetFirst</code> in arrayval.go so it returns a copy of <code>a</code></p>\n<p>with the first element set to <code>v</code>, leaving the caller's original array</p>\n<p>unchanged.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  a=[3]int{1,2,3}, v=9\nOutput: [3]int{9,2,3}\n</code></pre>\n<p>_Explanation:_ First element replaced; caller's array untouched because arrays are copied on pass.</p>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  a=[3]int{0,0,0}, v=5\nOutput: [3]int{5,0,0}\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  a=[3]int{7,8,9}, v=7\nOutput: [3]int{7,8,9}\n</code></pre>\n<p>_Explanation:_ Replacing with the same value leaves it identical.</p>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Arrays are values</span><span class=\"tchip\">Pass-by-copy</span><span class=\"tchip\">Array vs slice</span></div></details>",
    "education": "<h1>Arrays are values</h1>\n<h2>Intuition</h2>\n<p>A fixed-size array (<code>[N]T</code>) is a value type: its length is part of its type, and</p>\n<p>assigning or passing it <strong>copies</strong> every element. This differs from a slice,</p>\n<p>which shares a backing array.</p>\n<h2>Approach</h2>\n<p>1. The parameter a is already a copy \u2014 Go passes arrays by value.</p>\n<p>2. Assign a[0] = v to mutate the local copy.</p>\n<p>3. Return a. The caller's original is unaffected.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc SetFirst(a [3]int, v int) [3]int {\n\ta[0] = v\n\treturn a\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>SetFirst([3]int{1,2,3}, 9): the function receives its own copy {1,2,3}, sets index 0 to 9 giving {9,2,3}, and returns it; the caller still holds {1,2,3}.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>[3]int</code> and <code>[4]int</code> are different types.</li>\n<li>Arrays are comparable with <code>==</code> (element-wise); slices are not.</li>\n<li>Large arrays copied by value are expensive.</li>\n</ul>",
    "starter": "// Package arrayval \u2014 Gopher Workplace challenge.\npackage arrayval\n\n// SetFirst returns a copy of the array a with its first element replaced by v.\n// A Go array is a value: passing it to a function copies it, so the caller's\n// original array must stay unchanged \u2014 the new value comes back as the result.\n//\n// Examples:\n//\n//\tSetFirst([3]int{1, 2, 3}, 9) => [3]int{9, 2, 3}   // caller's array untouched\n//\tSetFirst([3]int{0, 0, 0}, 5) => [3]int{5, 0, 0}\nfunc SetFirst(a [3]int, v int) [3]int {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "arrayval.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/03-composite-types/junior/checkoutgrid": {
    "id": "01-language-basics/03-composite-types/junior/checkoutgrid",
    "title": "checkoutgrid \u2014 Seating Grid Marker",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">checkoutgrid \u2014 Seating Grid Marker</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>You are building a simple seat-hold service for a small theater. The seating arrangement is fixed: 7 rows (0\u20136) by 10 columns (0\u20139). When customers hold seats, you record them as a list of <code>{row, column}</code> coordinates. Your task is to mark those seats on a grid so customers can see which seats are taken.</p>\n<h2>Task</h2>\n<p>Implement <code>func SeatMap(taken [][2]int) [7][10]bool</code> to return a seating grid where:</p>\n<ul>\n<li>Each <code>true</code> value marks a taken seat</li>\n<li>Each <code>false</code> value marks an empty seat</li>\n<li>Coordinates outside the valid range <code>[0,7)\u00d7[0,10)</code> are ignored</li>\n<li>Do not modify the function signature or tests</li>\n</ul>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  taken=[][2]int{{0,0}}\nOutput: grid[0][0]=true, rest false\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  taken=[][2]int{{1,2},{6,9}}\nOutput: grid[1][2]=true, grid[6][9]=true\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  taken=[][2]int{{7,0},{0,10},{-1,0}}\nOutput: all false\n</code></pre>\n<p>_Explanation:_ Every coordinate is out of the 7x10 bounds, so all are ignored.</p>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">2</span></summary><div class=\"tchips\"><span class=\"tchip\">01-language-basics</span><span class=\"tchip\">03-composite-types</span></div></details>",
    "education": "<h1>2-D grids</h1>\n<h2>Intuition</h2>\n<p>A 2-D structure is a slice of slices, <code>[][]T</code>, indexed <code>grid[row][col]</code> in</p>\n<p>row-major order. Rows can be independent (and even ragged):</p>\n<pre class=\"md\"><code>\nfor r := range grid { for c := range grid[r] { use(grid[r][c]) } }\n</code></pre>\n<h2>Approach</h2>\n<p>1. Declare a zero-valued [7][10]bool (all false by default).</p>\n<p>2. Range over taken, reading r=seat[0], c=seat[1].</p>\n<p>3. Bounds-check 0&lt;=r&lt;7 and 0&lt;=c&lt;10; only then set grid[r][c]=true.</p>\n<p>4. Return the grid.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc SeatMap(taken [][2]int) [7][10]bool {\n\tvar grid [7][10]bool\n\tfor _, seat := range taken {\n\t\tr, c := seat[0], seat[1]\n\t\tif r &gt;= 0 &amp;&amp; r &lt; 7 &amp;&amp; c &gt;= 0 &amp;&amp; c &lt; 10 {\n\t\t\tgrid[r][c] = true\n\t\t}\n\t}\n\treturn grid\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>For {{1,2},{6,9}}: {1,2} is in range -&gt; grid[1][2]=true; {6,9} in range -&gt; grid[6][9]=true. All other cells stay at their false zero value.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Allocate each row with its own <code>make</code>; assigning one row to all aliases them.</li>\n<li><code>grid[row][col]</code> \u2014 outer is row, inner is column.</li>\n<li>Ragged rows are allowed; don't assume a fixed width.</li>\n</ul>",
    "starter": "// Package checkoutgrid \u2014 Gopher Workplace challenge.\npackage checkoutgrid\n\n// SeatMap returns a fixed 7-row by 10-column seating grid with every valid\n// {row,col} in taken marked true. Coordinates outside [0,7)\u00d7[0,10) are ignored.\n//\n// Examples:\n//\n//\tSeatMap([][2]int{{0,0}})        => grid with [0][0]=true\n//\tSeatMap([][2]int{{1,2},{6,9}})  => grid with [1][2],[6][9]=true\n//\tSeatMap([][2]int{{7,0}})        => all false (row 7 out of range)\nfunc SeatMap(taken [][2]int) [7][10]bool {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "checkoutgrid.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/03-composite-types/junior/concat": {
    "id": "01-language-basics/03-composite-types/junior/concat",
    "title": "Concatenate Slices",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Concatenate Slices</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>Flattening several slices into one with a variadic parameter and append-spread.</p>\n<h2>Task</h2>\n<p>Implement <code>Concat(slices ...[]int)</code>.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  []int{1,2}, []int{3}, nil, []int{4,5}\nOutput: []int{1,2,3,4,5}\n</code></pre>\n<p>_Explanation:_ nil slice contributes nothing when spread with append.</p>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  (no args)\nOutput: []int{}\n</code></pre>\n<p>_Explanation:_ empty variadic yields empty result.</p>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  []int{7}, []int{8,9}\nOutput: []int{7,8,9}\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Variadic of slices</span><span class=\"tchip\">append spread</span><span class=\"tchip\">nil handling</span></div></details>",
    "education": "<h1>Variadic parameters and append spread</h1>\n<h2>Intuition</h2>\n<p>A variadic <code>...[]int</code> parameter arrives as a <code>[][]int</code>. Flatten with the spread</p>\n<p>operator:</p>\n<pre class=\"md\"><code>\nout := []int{}\nfor _, s := range slices { out = append(out, s...) }\n</code></pre>\n<h2>Approach</h2>\n<p>1. Start with an empty result slice.</p>\n<p>2. Range over the variadic slices.</p>\n<p>3. append(result, s...) spreads each slice's elements in order (nil spreads to nothing).</p>\n<p>4. Return result.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Concat(slices ...[]int) []int {\n\tresult := []int{}\n\tfor _, s := range slices {\n\t\tresult = append(result, s...)\n\t}\n\treturn result\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>Concat([1,2],[3],nil,[4,5]): result grows [1,2] -&gt; [1,2,3] -&gt; unchanged by nil -&gt; [1,2,3,4,5].</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>append(out, s...)</code> spreads <code>s</code>; without <code>...</code> it's a type error.</li>\n<li>Appending a <code>nil</code> slice is a safe no-op.</li>\n<li>Pre-size with the total length to avoid regrowth.</li>\n</ul>",
    "starter": "// Package concat joins multiple slices into one.\npackage concat\n\n// Concat returns a single slice with all elements of the given slices in order.\n//\n// TODO(candidate): append each into a fresh result.\nfunc Concat(slices ...[]int) []int {\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "concat.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/03-composite-types/junior/contains": {
    "id": "01-language-basics/03-composite-types/junior/contains",
    "title": "Slice Contains",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Slice Contains</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>Checking membership in a small slice is a linear scan \u2014 return as soon as you</p>\n<p>find it.</p>\n<h2>Task</h2>\n<p>Implement <code>Contains(xs, target)</code>.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  xs=[\"a\",\"b\",\"c\"], target=\"a\"\nOutput: true\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  xs=[\"a\",\"b\",\"c\"], target=\"z\"\nOutput: false\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  xs=nil, target=\"x\"\nOutput: false\n</code></pre>\n<p>_Explanation:_ Ranging a nil slice never executes the loop body.</p>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Linear scan</span><span class=\"tchip\">Early return</span><span class=\"tchip\">Default false</span></div></details>",
    "education": "<h1>Membership by linear scan</h1>\n<h2>Intuition</h2>\n<p>For an unsorted slice, membership is a linear scan with an early return:</p>\n<pre class=\"md\"><code>\nfor _, x := range xs {\n\tif x == target { return true }\n}\nreturn false\n</code></pre>\n<h2>Approach</h2>\n<p>1. Range over xs.</p>\n<p>2. Compare each element to target; return true on the first match.</p>\n<p>3. If the loop finishes with no match, return false.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Contains(xs []string, target string) bool {\n\tfor _, x := range xs {\n\t\tif x == target {\n\t\t\treturn true\n\t\t}\n\t}\n\treturn false\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>Contains([\"a\",\"b\",\"c\"],\"c\"): \"a\"!=\"c\", \"b\"!=\"c\", \"c\"==\"c\" -&gt; return true.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Return <code>false</code> only after the whole loop \u2014 not inside it.</li>\n<li><code>==</code> must be defined for the element type (strings, numbers, comparable</li>\n</ul>\n<p>  structs).</p>\n<ul>\n<li>Ranging nil is safe and yields <code>false</code>.</li>\n</ul>",
    "starter": "// Package contains reports slice membership.\npackage contains\n\n// Contains reports whether target appears in xs.\n//\n// TODO(candidate): implement with a linear scan.\nfunc Contains(xs []string, target string) bool {\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "contains.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/03-composite-types/junior/copyslice": {
    "id": "01-language-basics/03-composite-types/junior/copyslice",
    "title": "Independent Slice Copy",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Independent Slice Copy</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>Assigning a slice shares its backing array. A real copy needs <code>make</code> + <code>copy</code>.</p>\n<h2>Task</h2>\n<p>Implement <code>Clone(xs)</code> \u2014 an independent copy; nil \u2192 non-nil empty.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  xs=[]int{1,2,3}\nOutput: []int{1,2,3} (independent copy)\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  clone then c[0]=99\nOutput: xs[0] still 1\n</code></pre>\n<p>_Explanation:_ No shared backing array.</p>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  xs=nil\nOutput: []int{} (non-nil, len 0)\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Shared backing</span><span class=\"tchip\">make + copy</span><span class=\"tchip\">Non-nil empty</span></div></details>",
    "education": "<h1>Slices share backing arrays</h1>\n<h2>Intuition</h2>\n<p>A slice is a header (pointer, length, capacity) over a backing array. Copying the</p>\n<p>header (<code>b := a</code>) shares the array. An independent copy allocates a new array:</p>\n<pre class=\"md\"><code>\nout := make([]int, len(xs))\ncopy(out, xs)\n</code></pre>\n<h2>Approach</h2>\n<p>1. Allocate make([]int, len(xs)) \u2014 a fresh backing array; for nil xs this is a non-nil empty slice.</p>\n<p>2. copy(result, xs) copies element-by-element.</p>\n<p>3. Return result, which shares nothing with xs.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Clone(xs []int) []int {\n\tresult := make([]int, len(xs))\n\tcopy(result, xs)\n\treturn result\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>Clone([1,2,3]): make len-3 slice, copy in 1,2,3. Writing result[0]=99 touches only the new array; xs[0] stays 1.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>copy</code> copies <code>min(len(dst), len(src))</code> elements.</li>\n<li><code>make([]int, len(xs))</code> is non-nil even when <code>len</code> is 0.</li>\n<li><code>append</code> to a shared slice may alias depending on capacity.</li>\n</ul>",
    "starter": "// Package copyslice makes an independent copy of a slice.\npackage copyslice\n\n// Clone returns a new slice with the same elements as xs, sharing no backing\n// array. Cloning nil returns an empty (non-nil) slice.\n//\n// TODO(candidate): use make + copy (or append).\nfunc Clone(xs []int) []int {\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "copyslice.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/03-composite-types/junior/counter": {
    "id": "01-language-basics/03-composite-types/junior/counter",
    "title": "Frequency Counter",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Frequency Counter</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>Counting occurrences relies on the map zero value: a missing key reads as 0, so</p>\n<p><code>m[k]++</code> just works.</p>\n<h2>Task</h2>\n<p>Implement <code>Count(xs)</code> returning element\u2192count.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  [\"a\",\"b\",\"a\",\"c\",\"a\",\"b\"]\nOutput: {\"a\":3,\"b\":2,\"c\":1}\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  nil\nOutput: {} (empty map)\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  [\"x\"]\nOutput: {\"x\":1}\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Map zero value</span><span class=\"tchip\">m[k]++</span><span class=\"tchip\">make first</span></div></details>",
    "education": "<h1>Counting with the map zero value</h1>\n<h2>Intuition</h2>\n<p>Reading a missing map key yields the value type's zero \u2014 0 for ints. So</p>\n<p>incrementing works without checking presence:</p>\n<pre class=\"md\"><code>\nm := make(map[string]int)\nfor _, x := range xs { m[x]++ }\n</code></pre>\n<p><code>m[x]++</code> reads (0 if absent), adds one, and stores.</p>\n<h2>Approach</h2>\n<p>1. make an empty map[string]int.</p>\n<p>2. Range over xs; result[x]++ relies on the int zero value 0 for first sightings.</p>\n<p>3. Return the map.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Count(xs []string) map[string]int {\n\tresult := make(map[string]int)\n\tfor _, x := range xs {\n\t\tresult[x]++\n\t}\n\treturn result\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>Count([\"a\",\"b\",\"a\"]): \"a\"-&gt;1, \"b\"-&gt;1, \"a\"-&gt;2. Result {\"a\":2,\"b\":1}.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>You must <code>make</code> the map first; <code>m[x]++</code> on a nil map panics (it's a write).</li>\n<li>Only counts are convenient; for non-zero defaults you still need comma-ok.</li>\n<li>Iteration order of the result is random.</li>\n</ul>",
    "starter": "// Package counter tallies occurrences.\npackage counter\n\n// Count returns a map from each element of xs to how many times it appears.\n//\n// TODO(candidate): use a map, incrementing per element (zero value helps).\nfunc Count(xs []string) map[string]int {\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "counter.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/03-composite-types/junior/dedupe": {
    "id": "01-language-basics/03-composite-types/junior/dedupe",
    "title": "Slice Dedupe",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Slice Dedupe</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>A data-ingestion step must drop duplicate integers from a slice while keeping</p>\n<p>the order in which each value first appeared \u2014 and it must leave the caller's</p>\n<p>slice untouched.</p>\n<h2>Task</h2>\n<p>Implement <code>Dedupe</code> in dedupe.go so that it:</p>\n<p>1. Returns elements in <strong>first-appearance order</strong>, duplicates removed.</p>\n<p>2. <strong>Does not mutate</strong> the input slice.</p>\n<p>3. Returns an empty (non-nil) slice for <code>nil</code> or empty input.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  []int{1,1,2,3,3,3}\nOutput: []int{1,2,3}\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  []int{5,4,5,4}\nOutput: []int{5,4}\n</code></pre>\n<p>_Explanation:_ Kept in first-appearance order.</p>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  nil\nOutput: []int{}\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">5</span></summary><div class=\"tchips\"><span class=\"tchip\">Slice header</span><span class=\"tchip\">Copy vs alias</span><span class=\"tchip\"><code>append</code> semantics</span><span class=\"tchip\">Map as a set</span><span class=\"tchip\">nil vs empty</span></div></details>",
    "education": "<h1>Deduplication</h1>\n<h2>Intuition</h2>\n<p>Remove duplicates by tracking what you've seen. A set (<code>map[T]struct{}</code>) gives</p>\n<p>O(1) membership and preserves first-seen order when you append on first sight:</p>\n<pre class=\"md\"><code>\nif _, ok := seen[x]; !ok { out = append(out, x); seen[x] = struct{}{} }\n</code></pre>\n<h2>Approach</h2>\n<p>1. Keep a seen map[int]bool and an empty result slice.</p>\n<p>2. Range the input; for each value not yet seen, mark it seen and append it.</p>\n<p>3. Never write to the input, so it stays unmutated.</p>\n<p>4. Return result.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Dedupe(in []int) []int {\n\tresult := []int{}\n\tseen := make(map[int]bool)\n\tfor _, v := range in {\n\t\tif !seen[v] {\n\t\t\tseen[v] = true\n\t\t\tresult = append(result, v)\n\t\t}\n\t}\n\treturn result\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>Dedupe([5,4,5,4]): 5 new -&gt; append(5); 4 new -&gt; append(4); 5 seen -&gt; skip; 4 seen -&gt; skip. Result [5,4].</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Emit on <strong>absence</strong>, then record.</li>\n<li>Order-preserving dedup differs from sort-then-compact.</li>\n<li>Element type must be comparable to be a set key.</li>\n</ul>",
    "starter": "// Package dedupe \u2014 Gopher Workplace challenge.\npackage dedupe\n\n// Dedupe returns the elements of in with duplicates removed, keeping the order\n// in which each value first appeared. The input slice must not be mutated, and\n// a nil or empty input returns an empty (non-nil) slice.\n//\n// Examples:\n//\n//\tDedupe([]int{1, 1, 2, 3, 3, 3}) => []int{1, 2, 3}\n//\tDedupe([]int{5, 4, 5, 4})       => []int{5, 4}   // first-appearance order\n//\tDedupe([]int{7, 7, 7})          => []int{7}\n//\tDedupe(nil)                     => []int{}\nfunc Dedupe(in []int) []int {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "dedupe.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/03-composite-types/junior/filterpos": {
    "id": "01-language-basics/03-composite-types/junior/filterpos",
    "title": "Filter Positives",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Filter Positives</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>Filtering builds a new slice with <code>append</code>, keeping only elements that pass a</p>\n<p>test.</p>\n<h2>Task</h2>\n<p>Implement <code>Positives(xs)</code> returning a new slice of the positive elements. Empty</p>\n<p>result must be non-nil.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  []int{1,-2,3,0,4}\nOutput: []int{1,3,4}\n</code></pre>\n<p>_Explanation:_ 0 is not positive.</p>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  []int{-1,-2}\nOutput: []int{}\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  []int{}\nOutput: []int{}\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">append</span><span class=\"tchip\">Non-nil empty</span><span class=\"tchip\">Predicate</span></div></details>",
    "education": "<h1>Building slices with append</h1>\n<h2>Intuition</h2>\n<p><code>append</code> adds to a slice and returns a (possibly relocated) new header \u2014 always</p>\n<p>assign the result back:</p>\n<pre class=\"md\"><code>\nout := []int{}\nfor _, x := range xs {\n\tif x &gt; 0 { out = append(out, x) }\n}\n</code></pre>\n<h2>Approach</h2>\n<p>1. Start with an empty (non-nil) result slice.</p>\n<p>2. Range xs; append only elements with x &gt; 0.</p>\n<p>3. Return result, preserving original order.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Positives(xs []int) []int {\n\tresult := []int{}\n\tfor _, x := range xs {\n\t\tif x &gt; 0 {\n\t\t\tresult = append(result, x)\n\t\t}\n\t}\n\treturn result\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>Positives([1,-2,3,0,4]): 1&gt;0 keep; -2 drop; 3 keep; 0 drop; 4 keep -&gt; [1,3,4].</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>append(s, x)</code> may reallocate; the returned slice is authoritative.</li>\n<li><code>[]int{}</code> is non-nil length 0; <code>var out []int</code> is nil \u2014 pick per the test/spec.</li>\n<li>Pre-sizing with <code>make([]int, 0, len(xs))</code> avoids regrowth in hot paths.</li>\n</ul>",
    "starter": "// Package filterpos keeps positive numbers.\npackage filterpos\n\n// Positives returns a new slice with only the positive elements of xs, order\n// preserved. Empty input yields an empty (non-nil) slice.\n//\n// TODO(candidate): build a result slice with append.\nfunc Positives(xs []int) []int {\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "filterpos.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/03-composite-types/junior/flatten": {
    "id": "01-language-basics/03-composite-types/junior/flatten",
    "title": "Flatten a Grid",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Flatten a Grid</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>Turning a 2-D grid into a flat list in row-major order.</p>\n<h2>Task</h2>\n<p>Implement <code>Flatten(grid)</code>.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  [][]int{{1,2},{3},{},{4,5}}\nOutput: []int{1,2,3,4,5}\n</code></pre>\n<p>_Explanation:_ Empty rows add nothing.</p>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  nil\nOutput: []int{}\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  [][]int{{9}}\nOutput: []int{9}\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Row-major</span><span class=\"tchip\">append spread</span><span class=\"tchip\">Ragged safe</span></div></details>",
    "education": "<h1>Row-major flattening</h1>\n<h2>Intuition</h2>\n<p>Flattening a slice of slices concatenates the rows in order:</p>\n<pre class=\"md\"><code>\nout := []int{}\nfor _, row := range grid { out = append(out, row...) }\n</code></pre>\n<h2>Approach</h2>\n<p>1. Start with an empty result slice.</p>\n<p>2. Range the rows in order (row-major).</p>\n<p>3. append(result, row...) spreads each row's elements.</p>\n<p>4. Return result.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Flatten(grid [][]int) []int {\n\tresult := []int{}\n\tfor _, row := range grid {\n\t\tresult = append(result, row...)\n\t}\n\treturn result\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>Flatten({{1,2},{3},{},{4,5}}): [1,2] -&gt; +3 -&gt; +nothing -&gt; +4,5 = [1,2,3,4,5].</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>append(out, row...)</code> spreads the row; without <code>...</code> it's a type error.</li>\n<li>Empty and nil rows contribute nothing.</li>\n<li>A single <code>[]int</code> indexed by <code>r*w+c</code> is more cache-friendly for fixed width.</li>\n</ul>",
    "starter": "// Package flatten flattens a 2-D grid into one slice, row by row.\npackage flatten\n\n// Flatten returns all elements of grid in row-major order.\n//\n// TODO(candidate): append each row.\nfunc Flatten(grid [][]int) []int {\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "flatten.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/03-composite-types/junior/getdefault": {
    "id": "01-language-basics/03-composite-types/junior/getdefault",
    "title": "Map Get With Default",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Map Get With Default</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>Reading a missing map key returns the zero value \u2014 indistinguishable from a real</p>\n<p>zero. The comma-ok idiom tells them apart.</p>\n<h2>Task</h2>\n<p>Implement <code>GetOr(m, key, def)</code> returning the value if present, else <code>def</code>.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  m={\"a\":1,\"zero\":0}, key=\"a\", def=99\nOutput: 1\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  key=\"zero\", def=99\nOutput: 0\n</code></pre>\n<p>_Explanation:_ Present with value 0 \u2014 the comma-ok distinguishes it from missing.</p>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  key=\"missing\", def=99\nOutput: 99\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Comma-ok</span><span class=\"tchip\">Zero vs absent</span><span class=\"tchip\">Nil map read</span></div></details>",
    "education": "<h1>The comma-ok idiom</h1>\n<h2>Intuition</h2>\n<p>Indexing a map always returns a value \u2014 the zero value if the key is absent. To</p>\n<p>distinguish \"present with value 0\" from \"missing\", use the two-result form:</p>\n<pre class=\"md\"><code>\nif v, ok := m[key]; ok { return v }\nreturn def\n</code></pre>\n<h2>Approach</h2>\n<p>1. Read v, ok := m[key] using the comma-ok form.</p>\n<p>2. If ok, the key exists \u2014 return v (even if it is 0).</p>\n<p>3. Otherwise return def.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc GetOr(m map[string]int, key string, def int) int {\n\tif v, ok := m[key]; ok {\n\t\treturn v\n\t}\n\treturn def\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>GetOr({\"zero\":0},\"zero\",99): comma-ok gives v=0, ok=true, so return 0 rather than the default.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Reading a <strong>nil</strong> map is safe and reports absent; only <em>writing</em> nil panics.</li>\n<li><code>ok</code> is the second result; ignoring it loses the distinction.</li>\n<li>The same idiom appears for type assertions and channel receives.</li>\n</ul>",
    "starter": "// Package getdefault reads a map with a fallback.\npackage getdefault\n\n// GetOr returns m[key] if present, else def, using the comma-ok idiom.\n//\n// TODO(candidate): implement.\nfunc GetOr(m map[string]int, key string, def int) int {\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "getdefault.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/03-composite-types/junior/gradebook": {
    "id": "01-language-basics/03-composite-types/junior/gradebook",
    "title": "Gradebook Averages",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Gradebook Averages</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>A map from student to their scores; produce each average. Students with no</p>\n<p>scores are skipped.</p>\n<h2>Task</h2>\n<p>Implement <code>Averages(book)</code> (integer average).</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  {\"ann\":[90,80,100],\"bob\":[70,75],\"cid\":[]}\nOutput: {\"ann\":90,\"bob\":72}\n</code></pre>\n<p>_Explanation:_ cid has no scores and is omitted; 145/2=72 via integer division.</p>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  {\"x\":[10,20]}\nOutput: {\"x\":15}\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  {}\nOutput: {}\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Map of slices</span><span class=\"tchip\">Guard empty</span><span class=\"tchip\">Build a map</span></div></details>",
    "education": "<h1>Maps of composite values</h1>\n<h2>Intuition</h2>\n<p>A map value can be any type, including a slice. <code>map[string][]int</code> associates</p>\n<p>each key with a list:</p>\n<pre class=\"md\"><code>\nfor name, scores := range book {\n\tif len(scores) == 0 { continue }\n\tsum := 0\n\tfor _, s := range scores { sum += s }\n\tout[name] = sum / len(scores)\n}\n</code></pre>\n<h2>Approach</h2>\n<p>1. make an empty result map.</p>\n<p>2. Range students; skip any whose score slice is empty (avoids divide-by-zero and omits them).</p>\n<p>3. Sum the slice, divide by its length with integer division, store under the name.</p>\n<p>4. Return the map.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Averages(book map[string][]int) map[string]int {\n\tresult := make(map[string]int)\n\tfor name, scores := range book {\n\t\tif len(scores) == 0 {\n\t\t\tcontinue\n\t\t}\n\t\tsum := 0\n\t\tfor _, s := range scores {\n\t\t\tsum += s\n\t\t}\n\t\tresult[name] = sum / len(scores)\n\t}\n\treturn result\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>Averages: ann sums to 270, 270/3=90; bob sums to 145, 145/2=72; cid empty -&gt; skipped. Result {\"ann\":90,\"bob\":72}.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Skip empty slices before dividing.</li>\n<li>A missing map key of slice type reads as <code>nil</code> (len 0, safe to range).</li>\n<li>Integer average truncates.</li>\n</ul>",
    "starter": "// Package gradebook averages scores per student.\npackage gradebook\n\n// Averages returns each student's average score (integer division) from a map of\n// name to their scores. A student with no scores is omitted.\n//\n// TODO(candidate): iterate the map, average each slice.\nfunc Averages(book map[string][]int) map[string]int {\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "gradebook.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/03-composite-types/junior/invert": {
    "id": "01-language-basics/03-composite-types/junior/invert",
    "title": "Invert a Map",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Invert a Map</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>Building a reverse lookup: given name\u2192id, produce id\u2192name.</p>\n<h2>Task</h2>\n<p>Implement <code>Invert(m)</code> returning value\u2192key (values unique).</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  {\"one\":1,\"two\":2,\"three\":3}\nOutput: {1:\"one\",2:\"two\",3:\"three\"}\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  {}\nOutput: {} (empty)\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  {\"a\":5}\nOutput: {5:\"a\"}\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">make with new types</span><span class=\"tchip\">Range k,v</span><span class=\"tchip\">Uniqueness</span></div></details>",
    "education": "<h1>Reverse lookups</h1>\n<h2>Intuition</h2>\n<p>Inverting a map swaps roles: each <code>key\u2192value</code> becomes <code>value\u2192key</code>. The result</p>\n<p>map's types are the originals reversed:</p>\n<pre class=\"md\"><code>\nout := make(map[int]string)\nfor k, v := range m { out[v] = k }\n</code></pre>\n<h2>Approach</h2>\n<p>1. make an empty map[int]string.</p>\n<p>2. Range the input as k, v.</p>\n<p>3. Store result[v] = k (values become keys).</p>\n<p>4. Return the inverted map.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Invert(m map[string]int) map[int]string {\n\tresult := make(map[int]string)\n\tfor k, v := range m {\n\t\tresult[v] = k\n\t}\n\treturn result\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>Invert({\"one\":1,\"two\":2}): store 1-&gt;\"one\", 2-&gt;\"two\". Result {1:\"one\",2:\"two\"}.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>If values are <strong>not</strong> unique, later pairs overwrite earlier ones \u2014 inversion</li>\n</ul>\n<p>  loses data.</p>\n<ul>\n<li>The value type must be a valid map key (comparable).</li>\n<li>Iteration order is random; the resulting map is unordered.</li>\n</ul>",
    "starter": "// Package invert swaps map keys and values.\npackage invert\n\n// Invert returns a new map mapping each value of m back to its key. Values are\n// assumed unique.\n//\n// TODO(candidate): build a new map with swapped pairs.\nfunc Invert(m map[string]int) map[int]string {\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "invert.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/03-composite-types/junior/keys": {
    "id": "01-language-basics/03-composite-types/junior/keys",
    "title": "Sorted Map Keys",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Sorted Map Keys</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>Map iteration order is random, so to print or compare deterministically you</p>\n<p>collect the keys and sort them.</p>\n<h2>Task</h2>\n<p>Implement <code>Sorted(m)</code> returning keys in ascending order.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  {\"banana\":1,\"apple\":2,\"cherry\":3}\nOutput: [\"apple\",\"banana\",\"cherry\"]\n</code></pre>\n<p>_Explanation:_ Sorted ascending regardless of map order.</p>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  {}\nOutput: [] (empty)\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  {\"b\":1,\"a\":2}\nOutput: [\"a\",\"b\"]\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Collect keys</span><span class=\"tchip\">sort.Strings</span><span class=\"tchip\">Random order</span></div></details>",
    "education": "<h1>Deterministic order from a map</h1>\n<h2>Intuition</h2>\n<p>Go randomizes map iteration order on purpose. To get a stable order, collect the</p>\n<p>keys into a slice and sort:</p>\n<pre class=\"md\"><code>\nout := make([]string, 0, len(m))\nfor k := range m { out = append(out, k) }\nsort.Strings(out)\n</code></pre>\n<h2>Approach</h2>\n<p>1. Allocate a slice with capacity len(m).</p>\n<p>2. Range the map, appending each key (map order is random).</p>\n<p>3. sort.Strings on the collected keys.</p>\n<p>4. Return the sorted slice.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nimport \"sort\"\n\nfunc Sorted(m map[string]int) []string {\n\tresult := make([]string, 0, len(m))\n\tfor k := range m {\n\t\tresult = append(result, k)\n\t}\n\tsort.Strings(result)\n\treturn result\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>Sorted({\"banana\",\"apple\",\"cherry\"}): collect in random order, sort.Strings -&gt; [\"apple\",\"banana\",\"cherry\"].</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Pre-size with <code>make(..., 0, len(m))</code> to avoid regrowth.</li>\n<li>Ranging a map with only <code>k</code> iterates keys.</li>\n<li><code>slices.Sorted(maps.Keys(m))</code> (Go 1.23+) does this in one line.</li>\n</ul>",
    "starter": "// Package keys returns a map's keys, sorted.\npackage keys\n\n// Sorted returns the keys of m in ascending order.\n//\n// TODO(candidate): collect keys, then sort them.\nfunc Sorted(m map[string]int) []string {\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "keys.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/03-composite-types/junior/lastn": {
    "id": "01-language-basics/03-composite-types/junior/lastn",
    "title": "Last N Elements",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Last N Elements</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>Tailing a log or history buffer: return the last N entries, clamping when N</p>\n<p>exceeds the length.</p>\n<h2>Task</h2>\n<p>Implement <code>Last(xs, n)</code> \u2014 last <code>n</code> in order; clamp <code>n</code> to <code>[0, len]</code>.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  xs=[1,2,3,4,5], n=2\nOutput: []int{4,5}\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  n=0\nOutput: []int{}\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  n=10\nOutput: []int{1,2,3,4,5}\n</code></pre>\n<p>_Explanation:_ n &gt;= len returns all of xs.</p>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Slice expression</span><span class=\"tchip\">Clamp bounds</span><span class=\"tchip\">Empty result</span></div></details>",
    "education": "<h1>Slice expressions</h1>\n<h2>Intuition</h2>\n<p><code>xs[a:b]</code> creates a slice sharing the same backing array, covering indices</p>\n<p><code>[a, b)</code>. A tail of <code>n</code> is <code>xs[len(xs)-n:]</code> \u2014 but only after clamping <code>n</code>:</p>\n<pre class=\"md\"><code>\nif n &gt; len(xs) { n = len(xs) }\nif n &lt; 0 { n = 0 }\nreturn xs[len(xs)-n:]\n</code></pre>\n<h2>Approach</h2>\n<p>1. If n &lt;= 0, return an empty slice.</p>\n<p>2. If n &gt;= len(xs), return all of xs.</p>\n<p>3. Otherwise return the slice expression xs[len(xs)-n:].</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Last(xs []int, n int) []int {\n\tif n &lt;= 0 {\n\t\treturn []int{}\n\t}\n\tif n &gt;= len(xs) {\n\t\treturn xs\n\t}\n\treturn xs[len(xs)-n:]\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>Last([1,2,3,4,5],2): n=2 is between 0 and 5, so return xs[5-2:] = xs[3:] = [4,5].</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>xs[len(xs)-n:]</code> panics if <code>n &gt; len(xs)</code> \u2014 clamp first.</li>\n<li>The result <strong>shares</strong> memory with <code>xs</code>; appending to it may overwrite.</li>\n<li><code>xs[i:]</code> is shorthand for <code>xs[i:len(xs)]</code>.</li>\n</ul>",
    "starter": "// Package lastn returns the last n elements of a slice.\npackage lastn\n\n// Last returns a slice of the last n elements of xs (in order). If n >= len(xs)\n// it returns all of xs; if n <= 0 it returns an empty slice.\n//\n// TODO(candidate): use slice expressions.\nfunc Last(xs []int, n int) []int {\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "lastn.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/03-composite-types/junior/lookup": {
    "id": "01-language-basics/03-composite-types/junior/lookup",
    "title": "Map Comma-Ok",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Map Comma-Ok</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>A feature-flag store maps names to counts. A plain <code>m[key]</code> returns <code>0</code> both</p>\n<p>when a key is missing <em>and</em> when it is present with value <code>0</code> \u2014 the code can't</p>\n<p>tell them apart. Go's comma-ok read solves this.</p>\n<h2>Task</h2>\n<p>Implement <code>Lookup</code> in lookup.go so it returns the value for <code>key</code></p>\n<p>and a boolean reporting whether the key was actually present. A key mapped to</p>\n<p><code>0</code> must report <code>true</code>.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  m={\"a\":5}, key=\"a\"\nOutput: 5, true\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  m={\"a\":5}, key=\"z\"\nOutput: 0, false\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  m={\"z\":0}, key=\"z\"\nOutput: 0, true\n</code></pre>\n<p>_Explanation:_ Present with zero value; comma-ok reports found.</p>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Comma-ok read</span><span class=\"tchip\">Zero value ambiguity</span><span class=\"tchip\">Nil map reads</span></div></details>",
    "education": "<h1>Map lookup</h1>\n<h2>Intuition</h2>\n<p>Indexing a map returns the value, or the value type's zero if the key is absent.</p>\n<p>The comma-ok form reports presence:</p>\n<pre class=\"md\"><code>\nv, ok := m[key]\n</code></pre>\n<h2>Approach</h2>\n<p>1. Read v, ok := m[key] \u2014 the comma-ok map access.</p>\n<p>2. Return v, ok directly. On a nil map the read yields 0, false without panicking.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Lookup(m map[string]int, key string) (int, bool) {\n\tv, ok := m[key]\n\treturn v, ok\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>Lookup({\"z\":0},\"z\"): comma-ok returns v=0, ok=true, so the caller learns the key exists despite the zero value.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Reading a nil map is safe (returns zero); writing panics.</li>\n<li>Iteration order is randomized.</li>\n<li>Keys must be comparable.</li>\n</ul>",
    "starter": "// Package lookup \u2014 Gopher Workplace challenge.\npackage lookup\n\n// Lookup reports the value stored for key in m and whether the key was present.\n// It must use the comma-ok form of a map read, so a key that is present but\n// maps to 0 is still reported as found \u2014 a plain `m[key]` cannot tell \"absent\"\n// from \"present with the zero value\".\n//\n// Examples:\n//\n//\tLookup(map[string]int{\"a\": 5}, \"a\") => 5, true\n//\tLookup(map[string]int{\"a\": 5}, \"z\") => 0, false\n//\tLookup(map[string]int{\"z\": 0}, \"z\") => 0, true   // present, value is 0\n//\tLookup(nil, \"a\")                    => 0, false\nfunc Lookup(m map[string]int, key string) (int, bool) {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "lookup.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/03-composite-types/junior/mapmerge": {
    "id": "01-language-basics/03-composite-types/junior/mapmerge",
    "title": "Merge Maps",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Merge Maps</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>Merging config layers: a base map plus overrides, where later values win.</p>\n<h2>Task</h2>\n<p>Implement <code>Merge(a, b)</code> \u2014 a new map, <code>b</code> overriding <code>a</code> on collisions, inputs</p>\n<p>untouched.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  a={\"x\":1,\"y\":2}, b={\"y\":20,\"z\":3}\nOutput: {\"x\":1,\"y\":20,\"z\":3}\n</code></pre>\n<p>_Explanation:_ b overrides a on key y.</p>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  inputs after merge\nOutput: a still {\"x\":1,\"y\":2}\n</code></pre>\n<p>_Explanation:_ Fresh map, inputs untouched.</p>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  a={}, b={\"k\":9}\nOutput: {\"k\":9}\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">make(map)</span><span class=\"tchip\">Range a map</span><span class=\"tchip\">Override order</span></div></details>",
    "education": "<h1>Copying and merging maps</h1>\n<h2>Intuition</h2>\n<p>Maps are reference types, so returning a fresh, independent map means allocating</p>\n<p>one and copying entries in override order:</p>\n<pre class=\"md\"><code>\nout := make(map[string]int)\nfor k, v := range a { out[k] = v }\nfor k, v := range b { out[k] = v } // b wins on collisions\n</code></pre>\n<h2>Approach</h2>\n<p>1. make a fresh result map.</p>\n<p>2. Copy all of a into it.</p>\n<p>3. Copy all of b, overwriting on collisions so b wins.</p>\n<p>4. Return result; a and b are never modified.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Merge(a, b map[string]int) map[string]int {\n\tresult := make(map[string]int)\n\tfor k, v := range a {\n\t\tresult[k] = v\n\t}\n\tfor k, v := range b {\n\t\tresult[k] = v\n\t}\n\treturn result\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>Merge({\"x\":1,\"y\":2},{\"y\":20,\"z\":3}): copy a -&gt; {x:1,y:2}; copy b -&gt; y overwritten to 20, z added -&gt; {x:1,y:20,z:3}.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Assigning one map to another copies the <strong>reference</strong>, not the data.</li>\n<li>Iteration order over a map is randomized \u2014 don't rely on it.</li>\n<li>Writing to a nil map panics; <code>make</code> first.</li>\n</ul>",
    "starter": "// Package mapmerge merges two maps.\npackage mapmerge\n\n// Merge returns a new map with all entries of a, then b overriding on key\n// collisions. Inputs are not modified.\n//\n// TODO(candidate): build a fresh map and copy both in.\nfunc Merge(a, b map[string]int) map[string]int {\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "mapmerge.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/03-composite-types/junior/minmax": {
    "id": "01-language-basics/03-composite-types/junior/minmax",
    "title": "Min and Max",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Min and Max</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>A single pass finds both extremes \u2014 seed from the first element, then compare.</p>\n<h2>Task</h2>\n<p>Implement <code>MinMax(xs)</code> returning min, max, and <code>ok=false</code> for empty.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  []int{3,1,4,1,5}\nOutput: 1, 5, true\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  []int{7}\nOutput: 7, 7, true\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  nil\nOutput: 0, 0, false\n</code></pre>\n<p>_Explanation:_ Empty input -&gt; ok is false.</p>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Seed from first</span><span class=\"tchip\">Single pass</span><span class=\"tchip\">Empty guard</span></div></details>",
    "education": "<h1>Seed-and-scan for extremes</h1>\n<h2>Intuition</h2>\n<p>Don't seed min/max with 0 or huge sentinels \u2014 seed with the <strong>first element</strong>,</p>\n<p>then scan the rest:</p>\n<pre class=\"md\"><code>\nif len(xs) == 0 { return 0, 0, false }\nmn, mx := xs[0], xs[0]\nfor _, x := range xs[1:] { if x &lt; mn { mn = x }; if x &gt; mx { mx = x } }\n</code></pre>\n<h2>Approach</h2>\n<p>1. If xs is empty, return 0, 0, false.</p>\n<p>2. Seed both min and max with xs[0].</p>\n<p>3. Scan xs[1:], lowering min or raising max as needed.</p>\n<p>4. Return min, max, true.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc MinMax(xs []int) (min, max int, ok bool) {\n\tif len(xs) == 0 {\n\t\treturn 0, 0, false\n\t}\n\tmin, max = xs[0], xs[0]\n\tfor _, x := range xs[1:] {\n\t\tif x &lt; min {\n\t\t\tmin = x\n\t\t}\n\t\tif x &gt; max {\n\t\t\tmax = x\n\t\t}\n\t}\n\treturn min, max, true\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>MinMax([3,1,4,1,5]): seed min=max=3; see 1 -&gt; min=1; 4 -&gt; max=4; 1 -&gt; no change; 5 -&gt; max=5. Return 1,5,true.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Guard empty before indexing <code>xs[0]</code>.</li>\n<li>Seeding with 0 is a classic bug for all-negative or all-large inputs.</li>\n<li><code>slices.Min</code>/<code>slices.Max</code> (Go 1.21+) panic on empty \u2014 the <code>ok</code> form is safer.</li>\n</ul>",
    "starter": "// Package minmax returns the smallest and largest element.\npackage minmax\n\n// MinMax returns the minimum and maximum of xs, and ok=false if xs is empty.\n//\n// TODO(candidate): scan once, tracking both.\nfunc MinMax(xs []int) (min, max int, ok bool) {\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "minmax.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/03-composite-types/junior/point": {
    "id": "01-language-basics/03-composite-types/junior/point",
    "title": "Struct Point",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Struct Point</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>Geometry code models a 2-D coordinate as a <code>struct</code>. Like arrays, a struct is a</p>\n<p>value: passing a <code>Point</code> copies its fields, so a translate helper returns a new</p>\n<p><code>Point</code> rather than mutating the caller's.</p>\n<h2>Task</h2>\n<p>Implement <code>Translate</code> in point.go so it returns a new <code>Point</code></p>\n<p>shifted by <code>(dx, dy)</code>, without changing the caller's <code>p</code>.</p>\n<p>Do <strong>not</strong> change the function signature, the <code>Point</code> type, or the tests.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  p=Point{1,2}, dx=3, dy=4\nOutput: Point{4,6}\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  p=Point{0,0}, dx=-1, dy=-1\nOutput: Point{-1,-1}\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  p=Point{5,5}, dx=0, dy=0\nOutput: Point{5,5}\n</code></pre>\n<p>_Explanation:_ Caller's p not mutated \u2014 a fresh Point is returned.</p>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">4</span></summary><div class=\"tchips\"><span class=\"tchip\">Struct types</span><span class=\"tchip\">Struct literals</span><span class=\"tchip\">Value semantics</span><span class=\"tchip\">Field access</span></div></details>",
    "education": "<h1>Structs</h1>\n<h2>Intuition</h2>\n<p>A struct groups named fields into one value:</p>\n<pre class=\"md\"><code>\ntype Point struct { X, Y int }\np := Point{X: 1, Y: 2}\n</code></pre>\n<p>Fields are accessed with dot notation; the whole struct copies on assignment.</p>\n<h2>Approach</h2>\n<p>1. Build a brand-new Point with X=p.X+dx and Y=p.Y+dy.</p>\n<p>2. Return it. Because Point is a value and we return a new one, the caller's p is unchanged.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\ntype Point struct {\n\tX int\n\tY int\n}\n\nfunc Translate(p Point, dx, dy int) Point {\n\treturn Point{X: p.X + dx, Y: p.Y + dy}\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>Translate(Point{1,2},3,4): construct Point{1+3, 2+4} = Point{4,6}; the argument copy is discarded, caller keeps {1,2}.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Struct assignment copies all fields (value semantics).</li>\n<li>Use a pointer receiver / pointer to mutate a struct in a function.</li>\n<li>Field order affects memory layout (padding), not equality.</li>\n</ul>",
    "starter": "// Package point \u2014 Gopher Workplace challenge.\npackage point\n\n// Point is a 2-D coordinate. It is a struct value: copying it copies its fields.\ntype Point struct {\n\tX int\n\tY int\n}\n\n// Translate returns a new Point shifted by (dx, dy). It must not modify the\n// caller's p \u2014 build and return a fresh Point.\n//\n// Examples:\n//\n//\tTranslate(Point{1, 2}, 3, 4)   => Point{4, 6}\n//\tTranslate(Point{0, 0}, -1, -1) => Point{-1, -1}\n//\tTranslate(Point{5, 5}, 0, 0)   => Point{5, 5}\nfunc Translate(p Point, dx, dy int) Point {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "point.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/03-composite-types/junior/rectarea": {
    "id": "01-language-basics/03-composite-types/junior/rectarea",
    "title": "Rectangle Struct",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Rectangle Struct</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>A geometry type with methods: area, and a scale that returns a new value without</p>\n<p>mutating the receiver.</p>\n<h2>Task</h2>\n<p>Implement <code>Area()</code> and <code>Scale(factor)</code> on <code>Rect</code>.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Rect{3,4}.Area()\nOutput: 12\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Rect{2,3}.Scale(2)\nOutput: Rect{4,6}\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  original after Scale\nOutput: Rect{2,3}\n</code></pre>\n<p>_Explanation:_ Value receiver returns a new Rect; original untouched.</p>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Value receiver</span><span class=\"tchip\">Return new struct</span><span class=\"tchip\">Struct equality</span></div></details>",
    "education": "<h1>Value-receiver methods</h1>\n<h2>Intuition</h2>\n<p>A method with a <strong>value receiver</strong> operates on a copy, so it can't mutate the</p>\n<p>caller \u2014 ideal for pure computations that return new values:</p>\n<pre class=\"md\"><code>\nfunc (r Rect) Area() int      { return r.W * r.H }\nfunc (r Rect) Scale(f int) Rect { return Rect{r.W*f, r.H*f} }\n</code></pre>\n<h2>Approach</h2>\n<p>1. Area: return r.W * r.H.</p>\n<p>2. Scale: return a new Rect{W: r.W<em>factor, H: r.H</em>factor}.</p>\n<p>3. Both use value receivers, so the caller's Rect is never mutated.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\ntype Rect struct {\n\tW, H int\n}\n\nfunc (r Rect) Area() int {\n\treturn r.W * r.H\n}\n\nfunc (r Rect) Scale(factor int) Rect {\n\treturn Rect{W: r.W * factor, H: r.H * factor}\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>Rect{2,3}.Scale(2): builds Rect{2<em>2, 3</em>2} = Rect{4,6}; the receiver r is a copy, so the original stays {2,3}.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>A value receiver copies the struct; mutations inside are lost. Use a pointer</li>\n</ul>\n<p>  receiver to mutate.</p>\n<ul>\n<li>Structs of comparable fields support <code>==</code> and can be map keys.</li>\n<li>Returning a new struct keeps the original untouched (no surprise mutation).</li>\n</ul>",
    "starter": "// Package rectarea computes rectangle geometry via a struct.\npackage rectarea\n\n// Rect is an axis-aligned rectangle by width and height.\ntype Rect struct {\n\tW, H int\n}\n\n// Area returns the rectangle's area.\n//\n// TODO(candidate): implement.\nfunc (r Rect) Area() int {\n\tpanic(\"not implemented\")\n}\n\n// Scale returns a new Rect scaled by factor (fields multiplied).\n//\n// TODO(candidate): implement, returning a new Rect.\nfunc (r Rect) Scale(factor int) Rect {\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "rectarea.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/03-composite-types/junior/removeindex": {
    "id": "01-language-basics/03-composite-types/junior/removeindex",
    "title": "Remove by Index",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Remove by Index</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>Deleting an element while keeping order uses the append-of-two-halves idiom.</p>\n<h2>Task</h2>\n<p>Implement <code>RemoveAt(xs, i)</code>; out-of-range <code>i</code> returns <code>xs</code> unchanged.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  xs=[1,2,3,4], i=1\nOutput: []int{1,3,4}\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  xs=[1,2,3], i=0\nOutput: []int{2,3}\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  xs=[1,2,3], i=5\nOutput: []int{1,2,3}\n</code></pre>\n<p>_Explanation:_ Out-of-range index returns xs unchanged.</p>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Two-half append</span><span class=\"tchip\">Bounds guard</span><span class=\"tchip\">Variadic spread</span></div></details>",
    "education": "<h1>Deleting from a slice, order-preserving</h1>\n<h2>Intuition</h2>\n<p>Removing index <code>i</code> while keeping order joins the parts before and after it:</p>\n<pre class=\"md\"><code>\nreturn append(xs[:i], xs[i+1:]...)\n</code></pre>\n<p>The <code>...</code> spreads the tail as individual arguments.</p>\n<h2>Approach</h2>\n<p>1. If i is out of [0, len(xs)), return xs unchanged.</p>\n<p>2. Otherwise append(xs[:i], xs[i+1:]...) to splice out element i, preserving order.</p>\n<p>3. Return the result.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc RemoveAt(xs []int, i int) []int {\n\tif i &lt; 0 || i &gt;= len(xs) {\n\t\treturn xs\n\t}\n\treturn append(xs[:i], xs[i+1:]...)\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>RemoveAt([1,2,3,4],1): xs[:1]=[1], spread xs[2:]=[3,4] -&gt; [1,3,4].</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>It <strong>overwrites</strong> <code>xs</code>'s backing array \u2014 copy first if the caller's slice</li>\n</ul>\n<p>  matters.</p>\n<ul>\n<li>Guard <code>i</code> in <code>[0, len)</code> or the slice expressions panic.</li>\n<li>The leftover tail element lingers in the backing array (a leak for pointers).</li>\n</ul>",
    "starter": "// Package removeindex deletes an element by index, preserving order.\npackage removeindex\n\n// RemoveAt returns xs with the element at index i removed, order preserved. If i\n// is out of range, xs is returned unchanged.\n//\n// TODO(candidate): use append with two slice expressions.\nfunc RemoveAt(xs []int, i int) []int {\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "removeindex.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/03-composite-types/junior/reverse": {
    "id": "01-language-basics/03-composite-types/junior/reverse",
    "title": "Reverse In Place",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Reverse In Place</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>A rendering step needs a slice flipped end-to-end without allocating a second</p>\n<p>slice. Because a slice is a view over a shared backing array, swapping elements</p>\n<p>through it changes the caller's data directly.</p>\n<h2>Task</h2>\n<p>Implement <code>Reverse</code> in reverse.go so it reverses <code>nums</code> **in</p>\n<p>place** (no new slice, no return value). Empty and single-element slices stay</p>\n<p>unchanged.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  []int{1,2,3}\nOutput: []int{3,2,1} (in place)\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  []int{1,2}\nOutput: []int{2,1}\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  []int{9}\nOutput: []int{9}\n</code></pre>\n<p>_Explanation:_ Single element: loop never runs.</p>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">4</span></summary><div class=\"tchips\"><span class=\"tchip\">Slices share backing</span><span class=\"tchip\">Two-pointer swap</span><span class=\"tchip\">Stop at the middle</span><span class=\"tchip\">Tuple swap</span></div></details>",
    "education": "<h1>Reversing a slice</h1>\n<h2>Intuition</h2>\n<p>Swap symmetric elements from the ends inward, stopping at the middle:</p>\n<pre class=\"md\"><code>\nfor i, j := 0, len(xs)-1; i &lt; j; i, j = i+1, j-1 { xs[i], xs[j] = xs[j], xs[i] }\n</code></pre>\n<h2>Approach</h2>\n<p>1. Set two indices i=0 and j=len(nums)-1.</p>\n<p>2. While i &lt; j, swap nums[i] and nums[j], then move i up and j down.</p>\n<p>3. Mutate the shared backing array in place; return nothing.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Reverse(nums []int) {\n\tfor i, j := 0, len(nums)-1; i &lt; j; i, j = i+1, j-1 {\n\t\tnums[i], nums[j] = nums[j], nums[i]\n\t}\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>Reverse([1,2,3]): i=0,j=2 swap -&gt; [3,2,1]; i=1,j=1 loop stops. Caller sees [3,2,1].</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>In-place reversal mutates the caller's slice (shared backing array).</li>\n<li>Multiple assignment swaps without a temp.</li>\n<li><code>slices.Reverse</code> does this generically (Go 1.21+).</li>\n</ul>",
    "starter": "// Package reverse \u2014 Gopher Workplace challenge.\npackage reverse\n\n// Reverse reverses nums in place: it rearranges the elements of the caller's\n// own slice, so the change is visible to the caller (a slice is a view over a\n// shared backing array). It returns nothing. Empty and single-element slices\n// are left unchanged.\n//\n// Examples:\n//\n//\ts := []int{1, 2, 3}; Reverse(s) // s is now []int{3, 2, 1}\n//\ts := []int{1, 2};    Reverse(s) // s is now []int{2, 1}\n//\ts := []int{9};       Reverse(s) // s is still []int{9}\nfunc Reverse(nums []int) {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "reverse.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/03-composite-types/junior/rowsum": {
    "id": "01-language-basics/03-composite-types/junior/rowsum",
    "title": "Row Sums",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Row Sums</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>A spreadsheet totals each row. A 2-D grid is a slice of slices, possibly ragged.</p>\n<h2>Task</h2>\n<p>Implement <code>RowSums(grid)</code> returning one sum per row.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  [][]int{{1,2,3},{4,5},{}}\nOutput: []int{6,9,0}\n</code></pre>\n<p>_Explanation:_ Empty row sums to 0.</p>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  nil\nOutput: []int{}\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  [][]int{{5}}\nOutput: []int{5}\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Slice of slices</span><span class=\"tchip\">Nested range</span><span class=\"tchip\">One output per row</span></div></details>",
    "education": "<h1>2-D data as slices of slices</h1>\n<h2>Intuition</h2>\n<p>Go's 2-D structures are slices of slices, and they can be <strong>ragged</strong>. Nested</p>\n<p>ranging handles that:</p>\n<pre class=\"md\"><code>\nfor _, row := range grid {\n\ts := 0\n\tfor _, v := range row { s += v }\n\tout = append(out, s)\n}\n</code></pre>\n<h2>Approach</h2>\n<p>1. Start with an empty result slice.</p>\n<p>2. Range each row; inner-range to accumulate its sum.</p>\n<p>3. Append each row's sum (0 for empty rows).</p>\n<p>4. Return result.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc RowSums(grid [][]int) []int {\n\tresult := []int{}\n\tfor _, row := range grid {\n\t\tsum := 0\n\t\tfor _, v := range row {\n\t\t\tsum += v\n\t\t}\n\t\tresult = append(result, sum)\n\t}\n\treturn result\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>RowSums({{1,2,3},{4,5},{}}): 1+2+3=6; 4+5=9; empty=0 -&gt; [6,9,0].</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Rows can differ in length; don't assume a fixed width.</li>\n<li>An empty row sums to 0.</li>\n<li>Rows are separate allocations and may be shared.</li>\n</ul>",
    "starter": "// Package rowsum sums each row of a 2-D grid.\npackage rowsum\n\n// RowSums returns a slice with the sum of each row of grid.\n//\n// TODO(candidate): iterate rows, sum each.\nfunc RowSums(grid [][]int) []int {\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "rowsum.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/03-composite-types/junior/structsum": {
    "id": "01-language-basics/03-composite-types/junior/structsum",
    "title": "Order Total",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Order Total</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>A cart totals line items: each <code>Order</code> has a price and quantity; sum</p>\n<p><code>Price*Qty</code>.</p>\n<h2>Task</h2>\n<p>Implement <code>Total(orders)</code>.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  [{pen,150,2},{pad,300,1},{ink,500,3}]\nOutput: 2100\n</code></pre>\n<p>_Explanation:_ 300+300+1500.</p>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  nil\nOutput: 0\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  [{a,100,1}]\nOutput: 100\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Struct fields</span><span class=\"tchip\">Slice of structs</span><span class=\"tchip\">Accumulate</span></div></details>",
    "education": "<h1>Slices of structs</h1>\n<h2>Intuition</h2>\n<p>A struct groups named fields; a slice of structs is a table of records. Range and</p>\n<p>read fields with dot notation:</p>\n<pre class=\"md\"><code>\nfor _, o := range orders { total += o.Price * o.Qty }\n</code></pre>\n<h2>Approach</h2>\n<p>1. Initialize total to 0.</p>\n<p>2. Range the orders, adding o.Price*o.Qty each iteration.</p>\n<p>3. Return total (0 for nil/empty).</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\ntype Order struct {\n\tItem  string\n\tPrice int // cents\n\tQty   int\n}\n\nfunc Total(orders []Order) int {\n\ttotal := 0\n\tfor _, o := range orders {\n\t\ttotal += o.Price * o.Qty\n\t}\n\treturn total\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>Total(orders): pen 150<em>2=300; pad 300</em>1=300; ink 500*3=1500; total 2100.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>The range variable is a <strong>copy</strong> of the struct; mutating <code>o</code> doesn't change the</li>\n</ul>\n<p>  slice. Use <code>orders[i].Field = ...</code> to mutate in place.</p>\n<ul>\n<li>Copying large structs each iteration has a cost; index if it matters.</li>\n<li>Keep money in integer cents to avoid float drift.</li>\n</ul>",
    "starter": "// Package structsum totals a field across a slice of structs.\npackage structsum\n\n// Order is a line item.\ntype Order struct {\n\tItem  string\n\tPrice int // cents\n\tQty   int\n}\n\n// Total returns the sum of Price*Qty over all orders.\n//\n// TODO(candidate): range the slice and accumulate.\nfunc Total(orders []Order) int {\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "structsum.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/03-composite-types/junior/structupdate": {
    "id": "01-language-basics/03-composite-types/junior/structupdate",
    "title": "Mutate via Pointer Receiver",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Mutate via Pointer Receiver</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>To change a struct's field from a method, the receiver must be a pointer.</p>\n<h2>Task</h2>\n<p>Implement <code>Deposit(amount)</code> on <code>*Account</code> to add to <code>Balance</code>.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  a=&amp;Account{100}; a.Deposit(50)\nOutput: a.Balance == 150\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  then a.Deposit(25)\nOutput: a.Balance == 175\n</code></pre>\n<p>_Explanation:_ Pointer receiver mutates in place across calls.</p>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  &amp;Account{0}; Deposit(10)\nOutput: Balance == 10\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Pointer receiver</span><span class=\"tchip\">Value vs pointer</span><span class=\"tchip\">Auto-address</span></div></details>",
    "education": "<h1>Pointer receivers mutate</h1>\n<h2>Intuition</h2>\n<p>A method with a <strong>pointer receiver</strong> operates on the original struct:</p>\n<pre class=\"md\"><code>\nfunc (a *Account) Deposit(amount int) { a.Balance += amount }\n</code></pre>\n<p>A value receiver would receive a copy and its changes would vanish.</p>\n<h2>Approach</h2>\n<p>1. Use the pointer receiver (a *Account) so the method mutates the caller's struct.</p>\n<p>2. a.Balance += amount adds to the stored balance in place.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\ntype Account struct {\n\tBalance int\n}\n\nfunc (a *Account) Deposit(amount int) {\n\ta.Balance += amount\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>a=&amp;Account{100}: Deposit(50) -&gt; 150; Deposit(25) -&gt; 175. Same underlying struct is updated each time.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Pointer-receiver calls need an <strong>addressable</strong> value; a map element is not.</li>\n<li>Value receiver + field write silently mutates a copy.</li>\n<li>Large structs are cheaper to pass by pointer regardless of mutation.</li>\n</ul>",
    "starter": "// Package structupdate mutates a struct through a pointer.\npackage structupdate\n\n// Account holds a balance in cents.\ntype Account struct {\n\tBalance int\n}\n\n// Deposit adds amount to the account's balance, mutating the receiver.\n//\n// TODO(candidate): use a pointer receiver to mutate in place.\nfunc (a *Account) Deposit(amount int) {\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "structupdate.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/03-composite-types/junior/subslice": {
    "id": "01-language-basics/03-composite-types/junior/subslice",
    "title": "Slice Copy Independence",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Slice Copy Independence</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>A cache hands out the first N items of an internal slice. Returning <code>s[:n]</code></p>\n<p>looked fine until callers mutated the result and silently corrupted the cache \u2014</p>\n<p>a sub-slice shares the same backing array. The result must be independent.</p>\n<h2>Task</h2>\n<p>Implement <code>Head</code> in subslice.go so it returns the first <code>n</code></p>\n<p>elements of <code>s</code> as an <strong>independent copy</strong>: writes to the result must not touch</p>\n<p><code>s</code>, and vice-versa. Clamp <code>n</code> to <code>len(s)</code>; treat <code>n &lt;= 0</code> as empty.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  s=[1,2,3,4], n=2\nOutput: []int{1,2} (independent copy)\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  s=[1,2], n=5\nOutput: []int{1,2}\n</code></pre>\n<p>_Explanation:_ n clamped to len(s).</p>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  s=[1,2,3], n=0\nOutput: []int{}\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">4</span></summary><div class=\"tchips\"><span class=\"tchip\">Slice expressions</span><span class=\"tchip\">Shared backing = shared writes</span><span class=\"tchip\"><code>make</code> + <code>copy</code></span><span class=\"tchip\">Bounds &amp; clamping</span></div></details>",
    "education": "<h1>Slice expressions</h1>\n<h2>Intuition</h2>\n<p><code>xs[low:high]</code> creates a slice over the same backing array covering <code>[low, high)</code>.</p>\n<p>It is O(1) and shares memory with the original:</p>\n<pre class=\"md\"><code>\nmid := xs[1:4] // elements 1,2,3\n</code></pre>\n<h2>Approach</h2>\n<p>1. Clamp n down to len(s) if it is larger.</p>\n<p>2. If n &lt;= 0, return an empty slice.</p>\n<p>3. make a fresh len-n slice and copy s[:n] into it \u2014 no shared backing array.</p>\n<p>4. Return the copy.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Head(s []int, n int) []int {\n\tif n &gt; len(s) {\n\t\tn = len(s)\n\t}\n\tif n &lt;= 0 {\n\t\treturn []int{}\n\t}\n\tresult := make([]int, n)\n\tcopy(result, s[:n])\n\treturn result\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>Head([1,2,3,4],2): n=2, make len-2 slice, copy [1,2]. Writing result[0]=99 leaves s[0]=1 because backing arrays differ.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>High index must be \u2264 <code>len</code> (or <code>cap</code> for the two-index form) or it panics.</li>\n<li>Sub-slices share the backing array; append may or may not alias depending on</li>\n</ul>\n<p>  capacity.</p>\n<ul>\n<li>The three-index form <code>xs[a:b:c]</code> caps capacity.</li>\n</ul>",
    "starter": "// Package subslice \u2014 Gopher Workplace challenge.\npackage subslice\n\n// Head returns the first n elements of s as an INDEPENDENT copy: later writes\n// to the result must not affect s, and writes to s must not affect the result.\n// A plain slice expression s[:n] would share s's backing array \u2014 this must not.\n// If n is greater than len(s), all of s is returned; n <= 0 gives an empty slice.\n//\n// Examples:\n//\n//\tHead([]int{1, 2, 3, 4}, 2) => []int{1, 2}   // independent of the input\n//\tHead([]int{1, 2}, 5)       => []int{1, 2}\n//\tHead([]int{1, 2, 3}, 0)    => []int{}\nfunc Head(s []int, n int) []int {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "subslice.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/03-composite-types/junior/sumints": {
    "id": "01-language-basics/03-composite-types/junior/sumints",
    "title": "Sum a Slice",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Sum a Slice</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>The most basic slice operation: walk it and accumulate.</p>\n<h2>Task</h2>\n<p>Implement <code>Sum(xs)</code> returning the total; empty/nil \u2192 0.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  []int{1,2,3}\nOutput: 6\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  nil\nOutput: 0\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  []int{-5,5}\nOutput: 0\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">for range</span><span class=\"tchip\">Nil vs empty</span><span class=\"tchip\">Accumulator</span></div></details>",
    "education": "<h1>Ranging over slices</h1>\n<h2>Intuition</h2>\n<p><code>for i, v := range xs</code> visits each element; use <code>_</code> for an index you don't need.</p>\n<p>Accumulate into a variable declared before the loop:</p>\n<pre class=\"md\"><code>\ntotal := 0\nfor _, x := range xs { total += x }\n</code></pre>\n<h2>Approach</h2>\n<p>1. Initialize total to 0.</p>\n<p>2. Range over xs, adding each element to total.</p>\n<p>3. Return total; empty and nil slices skip the loop and yield 0.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Sum(xs []int) int {\n\ttotal := 0\n\tfor _, x := range xs {\n\t\ttotal += x\n\t}\n\treturn total\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>Sum([-5,5]): total 0 -&gt; -5 -&gt; 0. Return 0.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>A <code>nil</code> slice is safe to range (zero iterations); no need to check.</li>\n<li>The range value is a <strong>copy</strong>; assigning to it doesn't change the slice.</li>\n<li><code>len(xs)</code> is O(1); indexing <code>xs[i]</code> is fine too, but range reads cleaner.</li>\n</ul>",
    "starter": "// Package sumints sums a slice of ints.\npackage sumints\n\n// Sum returns the total of all elements in xs. The sum of an empty slice is 0.\n//\n// TODO(candidate): implement by ranging over the slice.\nfunc Sum(xs []int) int {\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "sumints.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/03-composite-types/junior/uniquecount": {
    "id": "01-language-basics/03-composite-types/junior/uniquecount",
    "title": "Count Distinct",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Count Distinct</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>A set is a map whose keys are the members. Counting distinct values is inserting</p>\n<p>each into a set and taking its size.</p>\n<h2>Task</h2>\n<p>Implement <code>Distinct(xs)</code>.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  []int{1,2,2,3,3,3}\nOutput: 3\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  []int{5,5,5}\nOutput: 1\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  nil\nOutput: 0\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Set as map</span><span class=\"tchip\">Idempotent insert</span><span class=\"tchip\">Size = len</span></div></details>",
    "education": "<h1>Sets from maps</h1>\n<h2>Intuition</h2>\n<p>Go has no set type; you use a map whose keys are the members. The value carries</p>\n<p>no information, so <code>struct{}</code> (zero bytes) is idiomatic:</p>\n<pre class=\"md\"><code>\nseen := make(map[int]struct{})\nfor _, x := range xs { seen[x] = struct{}{} }\nreturn len(seen)\n</code></pre>\n<h2>Approach</h2>\n<p>1. make a set map[int]struct{} (or map[int]bool).</p>\n<p>2. Range xs, inserting each value; duplicates collapse to one key.</p>\n<p>3. Return len(seen), the count of distinct values.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Distinct(xs []int) int {\n\tseen := make(map[int]struct{})\n\tfor _, x := range xs {\n\t\tseen[x] = struct{}{}\n\t}\n\treturn len(seen)\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>Distinct([1,2,2,3,3,3]): set becomes {1,2,3}; len is 3.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>map[int]struct{}</code> uses no memory for values; <code>map[int]bool</code> also works but</li>\n</ul>\n<p>  stores a byte.</p>\n<ul>\n<li>Membership test is <code>_, ok := seen[x]</code>.</li>\n<li>The element type must be comparable to be a map key.</li>\n</ul>",
    "starter": "// Package uniquecount counts distinct elements using a set.\npackage uniquecount\n\n// Distinct returns the number of distinct values in xs, using a map as a set.\n//\n// TODO(candidate): implement with map[int]struct{} or map[int]bool.\nfunc Distinct(xs []int) int {\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "uniquecount.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/03-composite-types/junior/wordfreq": {
    "id": "01-language-basics/03-composite-types/junior/wordfreq",
    "title": "Word Frequency",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Word Frequency</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>A log analyzer needs a tally of how often each word shows up. A <code>map</code> from</p>\n<p>word to count is the natural tool \u2014 but a <code>nil</code> map must not be written to, and</p>\n<p>the empty case should still return a usable map.</p>\n<h2>Task</h2>\n<p>Implement <code>Count</code> in wordfreq.go so it returns a</p>\n<p><code>map[string]int</code> of each word's occurrence count. Return an empty (non-nil) map</p>\n<p>for <code>nil</code> or empty input.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  [\"a\",\"b\",\"a\"]\nOutput: {\"a\":2,\"b\":1}\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  [\"x\"]\nOutput: {\"x\":1}\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  nil\nOutput: {} (empty, non-nil)\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Map creation</span><span class=\"tchip\">Nil map writes panic</span><span class=\"tchip\">Zero value on read</span></div></details>",
    "education": "<h1>Frequency counting with maps</h1>\n<h2>Intuition</h2>\n<p>The map zero value makes counting a one-liner: a missing key reads as 0, so</p>\n<p><code>m[k]++</code> reads-modifies-writes without a presence check:</p>\n<pre class=\"md\"><code>\nm := make(map[string]int)\nfor _, w := range words { m[w]++ }\n</code></pre>\n<h2>Approach</h2>\n<p>1. make an empty map[string]int.</p>\n<p>2. Range words; result[w]++ leans on the int zero value for the first occurrence.</p>\n<p>3. Return the map (empty but non-nil for nil/empty input).</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Count(words []string) map[string]int {\n\tresult := make(map[string]int)\n\tfor _, w := range words {\n\t\tresult[w]++\n\t}\n\treturn result\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>Count([\"a\",\"b\",\"a\"]): \"a\"-&gt;1, \"b\"-&gt;1, \"a\"-&gt;2 -&gt; {\"a\":2,\"b\":1}.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>make</code> the map first; <code>m[k]++</code> on a nil map panics (it's a write).</li>\n<li>Iteration order is random \u2014 sort keys for deterministic output.</li>\n<li>For non-zero defaults, use comma-ok instead of the zero value.</li>\n</ul>",
    "starter": "// Package wordfreq \u2014 Gopher Workplace challenge.\npackage wordfreq\n\n// Count returns how many times each word appears in words, as a map from word\n// to its occurrence count. A nil or empty input returns an empty (non-nil) map.\n//\n// Examples:\n//\n//\tCount([]string{\"a\", \"b\", \"a\"}) => map[string]int{\"a\": 2, \"b\": 1}\n//\tCount([]string{\"x\"})           => map[string]int{\"x\": 1}\n//\tCount(nil)                     => map[string]int{}\nfunc Count(words []string) map[string]int {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "wordfreq.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/03-composite-types/middle/anagramgroup": {
    "id": "01-language-basics/03-composite-types/middle/anagramgroup",
    "title": "Count Anagram Groups",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Count Anagram Groups</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>Grouping anagrams by a canonical key \u2014 a real \"normalize then group\" task.</p>\n<h2>Task</h2>\n<p>Implement <code>Count(words)</code> \u2014 number of distinct anagram groups.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  [\"eat\",\"tea\",\"tan\",\"ate\",\"nat\",\"bat\"]\nOutput: 3\n</code></pre>\n<p>_Explanation:_ {eat,tea,ate}, {tan,nat}, {bat}</p>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  [\"a\",\"b\",\"c\"]\nOutput: 3\n</code></pre>\n<p>_Explanation:_ no anagrams, 3 distinct</p>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  [\"listen\",\"silent\"]\nOutput: 1\n</code></pre>\n<p>_Explanation:_ same sorted letters</p>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Canonical key</span><span class=\"tchip\">Set of keys</span><span class=\"tchip\">[]byte sort</span></div></details>",
    "education": "<h1>Canonical keys for grouping</h1>\n<h2>Intuition</h2>\n<p>Two anagrams share the same multiset of letters, so their <strong>sorted</strong> letters form</p>\n<p>a canonical key. Count distinct keys with a set:</p>\n<pre class=\"md\"><code>\nb := []byte(word)\nsort.Slice(b, func(i, j int) bool { return b[i] &lt; b[j] })\nkey := string(b)\nset[key] = struct{}{}\n</code></pre>\n<h2>Approach</h2>\n<p>1. For each word, sort its bytes to form a canonical key.</p>\n<p>2. Insert the key into a set (map[string]bool).</p>\n<p>3. Distinct anagram groups = number of keys in the set.</p>\n<p>4. Return len(set).</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nimport \"sort\"\n\nfunc Count(words []string) int {\n\tseen := map[string]bool{}\n\tfor _, w := range words {\n\t\tbs := []byte(w)\n\t\tsort.Slice(bs, func(i, j int) bool { return bs[i] &lt; bs[j] })\n\t\tseen[string(bs)] = true\n\t}\n\treturn len(seen)\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>\"eat\" and \"tea\" both sort to \"aet\" -&gt; one key; \"tan\",\"nat\" -&gt; \"ant\"; \"bat\" -&gt; \"abt\". Set = {aet,ant,abt}, len 3.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Convert the sorted <code>[]byte</code> back to a <code>string</code> to use as a map key.</li>\n<li>This keys by bytes; for full Unicode, normalize runes.</li>\n<li>Sorting each word is O(k log k); a letter-count key is O(k).</li>\n</ul>",
    "starter": "// Package anagramgroup groups words that are anagrams.\npackage anagramgroup\n\n// Count returns how many distinct anagram groups are in words. Two words are\n// anagrams if they contain the same letters with the same counts.\n//\n// TODO(candidate): key each word by its sorted letters, count distinct keys.\nfunc Count(words []string) int {\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "anagramgroup.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/03-composite-types/middle/chunk": {
    "id": "01-language-basics/03-composite-types/middle/chunk",
    "title": "Chunk a Slice",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Chunk a Slice</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>A batch processor sends records in groups of N. Split a slice into fixed-size</p>\n<p>chunks, the last possibly short.</p>\n<h2>Task</h2>\n<p>Implement <code>Chunk(xs, size)</code>; <code>size &lt;= 0</code> \u2192 empty.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  [1,2,3,4,5], size=2\nOutput: [[1,2],[3,4],[5]]\n</code></pre>\n<p>_Explanation:_ last chunk shorter</p>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  [1,2,3], size=5\nOutput: [[1,2,3]]\n</code></pre>\n<p>_Explanation:_ size exceeds len</p>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  [1], size=0\nOutput: []\n</code></pre>\n<p>_Explanation:_ size&lt;=0 gives empty</p>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Slice windows</span><span class=\"tchip\">Step by size</span><span class=\"tchip\">Clamp last</span></div></details>",
    "education": "<h1>Windowing a slice</h1>\n<h2>Intuition</h2>\n<p>Chunking steps an index by <code>size</code> and takes a sub-slice each time, clamping the</p>\n<p>end so the final (short) chunk fits:</p>\n<pre class=\"md\"><code>\nfor i := 0; i &lt; len(xs); i += size {\n\tend := i + size\n\tif end &gt; len(xs) { end = len(xs) }\n\tout = append(out, xs[i:end])\n}\n</code></pre>\n<h2>Approach</h2>\n<p>1. Start with an empty non-nil [][]int.</p>\n<p>2. If size&lt;=0 return it.</p>\n<p>3. Step i by size across xs.</p>\n<p>4. Clamp end to len(xs); append xs[i:end].</p>\n<p>5. Return result.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Chunk(xs []int, size int) [][]int {\n\tout := [][]int{}\n\tif size &lt;= 0 {\n\t\treturn out\n\t}\n\tfor i := 0; i &lt; len(xs); i += size {\n\t\tend := i + size\n\t\tif end &gt; len(xs) {\n\t\t\tend = len(xs)\n\t\t}\n\t\tout = append(out, xs[i:end])\n\t}\n\treturn out\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>xs=[1,2,3,4,5] size=2: i=0 append [1,2]; i=2 append [3,4]; i=4 end clamps to 5 append [5]. Result [[1,2],[3,4],[5]].</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>The chunks <strong>share</strong> <code>xs</code>'s backing array (sub-slices, not copies).</li>\n<li>Guard <code>size &lt;= 0</code> to avoid an infinite loop.</li>\n<li><code>min(i+size, len(xs))</code> (Go 1.21+) replaces the manual clamp.</li>\n</ul>",
    "starter": "// Package chunk splits a slice into fixed-size chunks.\npackage chunk\n\n// Chunk splits xs into consecutive slices of at most size elements. The last\n// chunk may be shorter. size <= 0 returns an empty result.\n//\n// TODO(candidate): implement with slice expressions.\nfunc Chunk(xs []int, size int) [][]int {\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "chunk.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/03-composite-types/middle/dedupesorted": {
    "id": "01-language-basics/03-composite-types/middle/dedupesorted",
    "title": "Dedupe Consecutive",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Dedupe Consecutive</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>Collapsing runs of equal values (e.g. deduping a sorted list) in one pass.</p>\n<h2>Task</h2>\n<p>Implement <code>Dedupe(xs)</code> collapsing consecutive duplicates.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  [1,1,2,3,3,3,4]\nOutput: [1,2,3,4]\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  [5,5,5]\nOutput: [5]\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  []\nOutput: []\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Track previous</span><span class=\"tchip\">Compare &amp; skip</span><span class=\"tchip\">First element</span></div></details>",
    "education": "<h1>Collapsing consecutive duplicates</h1>\n<h2>Intuition</h2>\n<p>Emit an element only when it differs from the previously emitted one:</p>\n<pre class=\"md\"><code>\nout := []int{}\nfor i, v := range xs {\n\tif i == 0 || v != xs[i-1] { out = append(out, v) }\n}\n</code></pre>\n<h2>Approach</h2>\n<p>1. Start with empty result.</p>\n<p>2. Walk each element with its index.</p>\n<p>3. Keep it if it is the first element or differs from the previous element.</p>\n<p>4. Return result.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Dedupe(xs []int) []int {\n\tout := []int{}\n\tfor i, v := range xs {\n\t\tif i == 0 || v != xs[i-1] {\n\t\t\tout = append(out, v)\n\t\t}\n\t}\n\treturn out\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>[1,1,2,...]: i0 keep 1; i1 v1==xs[0] skip; i2 v2!=1 keep; ... yields [1,2,3,4].</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>It only removes <strong>consecutive</strong> duplicates; unsorted input keeps distant repeats.</li>\n<li><code>slices.Compact</code> (Go 1.21+) does this in place.</li>\n<li>Comparing to <code>xs[i-1]</code> (source) vs the last kept value is equivalent here since</li>\n</ul>\n<p>  runs are contiguous.</p>",
    "starter": "// Package dedupesorted removes consecutive duplicates.\npackage dedupesorted\n\n// Dedupe returns a new slice with consecutive duplicate values collapsed to one.\n// (For sorted input this removes all duplicates.)\n//\n// TODO(candidate): compare each element to the previous kept one.\nfunc Dedupe(xs []int) []int {\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "dedupesorted.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/03-composite-types/middle/groupby": {
    "id": "01-language-basics/03-composite-types/middle/groupby",
    "title": "Group By First Letter",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Group By First Letter</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>Bucketing items by a key \u2014 the map-of-slices pattern.</p>\n<h2>Task</h2>\n<p>Implement <code>ByFirst(words)</code> grouping by first byte; skip empty words.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  [\"apple\",\"banana\",\"avocado\",\"cherry\",\"\"]\nOutput: {a:[apple,avocado], b:[banana], c:[cherry]}\n</code></pre>\n<p>_Explanation:_ empty word skipped</p>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  [\"dog\"]\nOutput: {d:[dog]}\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  [\"\"]\nOutput: {}\n</code></pre>\n<p>_Explanation:_ only empty -&gt; empty map</p>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">map[key][]T</span><span class=\"tchip\">append to map value</span><span class=\"tchip\">Nil-safe append</span></div></details>",
    "education": "<h1>Grouping with map-of-slices</h1>\n<h2>Intuition</h2>\n<p><code>append</code> to a map value works even when the key is absent, because a missing key</p>\n<p>reads as a nil slice and appending to nil is valid:</p>\n<pre class=\"md\"><code>\nm := make(map[byte][]string)\nfor _, w := range words {\n\tif w == \"\" { continue }\n\tm[w[0]] = append(m[w[0]], w)\n}\n</code></pre>\n<h2>Approach</h2>\n<p>1. Create map[byte][]string.</p>\n<p>2. For each word, skip if empty.</p>\n<p>3. Take first byte w[0] as key.</p>\n<p>4. Append the word to that key's slice.</p>\n<p>5. Return map.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc ByFirst(words []string) map[byte][]string {\n\tout := map[byte][]string{}\n\tfor _, w := range words {\n\t\tif w == \"\" {\n\t\t\tcontinue\n\t\t}\n\t\tk := w[0]\n\t\tout[k] = append(out[k], w)\n\t}\n\treturn out\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>\"apple\"-&gt;a:[apple]; \"banana\"-&gt;b:[banana]; \"avocado\"-&gt;a:[apple,avocado]; \"cherry\"-&gt;c:[cherry]; \"\" skipped.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>You must <code>make</code> the outer map; appending is a write to it.</li>\n<li>The <em>value</em> nil-append is safe; the outer map nil-write is not.</li>\n<li>Group order follows insertion order; map key order is random.</li>\n</ul>",
    "starter": "// Package groupby groups strings by their first letter.\npackage groupby\n\n// ByFirst groups words by their first byte (assumed ASCII). Order within each\n// group follows input order. Empty words are skipped.\n//\n// TODO(candidate): append each word to its group slice.\nfunc ByFirst(words []string) map[byte][]string {\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "groupby.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/03-composite-types/middle/groupstructs": {
    "id": "01-language-basics/03-composite-types/middle/groupstructs",
    "title": "Total by Customer",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Total by Customer</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>Aggregating records into per-key totals \u2014 a group-by-sum.</p>\n<h2>Task</h2>\n<p>Implement <code>TotalByCustomer(orders)</code>.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  [{ann,100},{bob,50},{ann,25},{bob,75}]\nOutput: {ann:125, bob:125}\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  [{ann,10}]\nOutput: {ann:10}\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  []\nOutput: {}\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Group-by-sum</span><span class=\"tchip\">Map zero value</span><span class=\"tchip\">Struct fields</span></div></details>",
    "education": "<h1>Group-by-sum</h1>\n<h2>Intuition</h2>\n<p>Accumulate a running total per key; the map zero value makes <code>+=</code> work for new</p>\n<p>keys:</p>\n<pre class=\"md\"><code>\nm := make(map[string]int)\nfor _, o := range orders { m[o.Customer] += o.Amount }\n</code></pre>\n<h2>Approach</h2>\n<p>1. Create map[string]int.</p>\n<p>2. For each order add Amount to map[Customer].</p>\n<p>3. The += on a missing key starts from zero.</p>\n<p>4. Return map.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\ntype Order struct {\n\tCustomer string\n\tAmount   int\n}\n\nfunc TotalByCustomer(orders []Order) map[string]int {\n\tout := map[string]int{}\n\tfor _, o := range orders {\n\t\tout[o.Customer] += o.Amount\n\t}\n\treturn out\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>ann+=100 (100); bob+=50 (50); ann+=25 (125); bob+=75 (125). {ann:125,bob:125}.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>make</code> the map first; <code>+=</code> is a write.</li>\n<li>The zero-value read is what lets a new key start at 0.</li>\n<li>For multiple aggregates, use a struct as the map value.</li>\n</ul>",
    "starter": "// Package groupstructs groups orders by customer.\npackage groupstructs\n\n// Order is a customer purchase.\ntype Order struct {\n\tCustomer string\n\tAmount   int\n}\n\n// TotalByCustomer returns each customer's total order amount.\n//\n// TODO(candidate): accumulate into a map.\nfunc TotalByCustomer(orders []Order) map[string]int {\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "groupstructs.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/03-composite-types/middle/histogram": {
    "id": "01-language-basics/03-composite-types/middle/histogram",
    "title": "Histogram Buckets",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Histogram Buckets</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>Binning values into a histogram \u2014 latency buckets, size distributions.</p>\n<h2>Task</h2>\n<p>Implement <code>Bucket(xs, size)</code>: count per <code>[i<em>size, (i+1)</em>size)</code> bin.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  [5,12,15,25,3], size=10\nOutput: [2,2,1]\n</code></pre>\n<p>_Explanation:_ bins [0,10)=2, [10,20)=2, [20,30)=1</p>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  nil, size=10\nOutput: []\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  [0], size=5\nOutput: [1]\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Bin index</span><span class=\"tchip\">Size the result</span><span class=\"tchip\">Allocate + count</span></div></details>",
    "education": "<h1>Bucketing into a histogram</h1>\n<h2>Intuition</h2>\n<p>The bin for a value is <code>v / size</code> (integer division). Size the result to hold the</p>\n<p>largest bin, then count:</p>\n<pre class=\"md\"><code>\nmaxBin := 0\nfor _, v := range xs { if v/size &gt; maxBin { maxBin = v / size } }\nbins := make([]int, maxBin+1)\nfor _, v := range xs { bins[v/size]++ }\n</code></pre>\n<h2>Approach</h2>\n<p>1. Empty input -&gt; empty slice.</p>\n<p>2. Compute max bin = max(v/size).</p>\n<p>3. Allocate counts of length maxBin+1.</p>\n<p>4. Increment counts[v/size] for each v.</p>\n<p>5. Return counts.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Bucket(xs []int, size int) []int {\n\tif len(xs) == 0 {\n\t\treturn []int{}\n\t}\n\tmaxBin := 0\n\tfor _, v := range xs {\n\t\tif b := v / size; b &gt; maxBin {\n\t\t\tmaxBin = b\n\t\t}\n\t}\n\tout := make([]int, maxBin+1)\n\tfor _, v := range xs {\n\t\tout[v/size]++\n\t}\n\treturn out\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>values/10: 5-&gt;0,12-&gt;1,15-&gt;1,25-&gt;2,3-&gt;0. maxBin=2, len 3. counts bin0=2,bin1=2,bin2=1 -&gt; [2,2,1].</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Guard <code>size &gt; 0</code> to avoid divide-by-zero.</li>\n<li>Allocate <code>maxBin+1</code> bins, or the highest value indexes out of range.</li>\n<li>This assumes non-negative values; negatives need an offset.</li>\n</ul>",
    "starter": "// Package histogram buckets values into fixed-width bins.\npackage histogram\n\n// Bucket returns counts per bin of width size for values in [0, +inf). Bin i\n// covers [i*size, (i+1)*size). The result length is enough to hold the max\n// value; empty input returns an empty slice. size must be > 0.\n//\n// TODO(candidate): find max bin, allocate, count.\nfunc Bucket(xs []int, size int) []int {\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "histogram.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/03-composite-types/middle/insertat": {
    "id": "01-language-basics/03-composite-types/middle/insertat",
    "title": "Insert at Index",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Insert at Index</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>Inserting into an ordered slice: split at the index and stitch the value in.</p>\n<h2>Task</h2>\n<p>Implement <code>InsertAt(xs, i, v)</code>, clamping <code>i</code> to <code>[0, len]</code>.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  [1,2,3], i=1, v=9\nOutput: [1,9,2,3]\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  [1,2,3], i=10, v=9\nOutput: [1,2,3,9]\n</code></pre>\n<p>_Explanation:_ i clamps to len</p>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  [], i=0, v=9\nOutput: [9]\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Split &amp; stitch</span><span class=\"tchip\">Aliasing hazard</span><span class=\"tchip\">Clamp index</span></div></details>",
    "education": "<h1>Inserting into a slice</h1>\n<h2>Intuition</h2>\n<p>Insertion stitches three pieces: the head, the new value, and the tail:</p>\n<pre class=\"md\"><code>\nout := append([]int{}, xs[:i]...)\nout = append(out, v)\nout = append(out, xs[i:]...)\n</code></pre>\n<h2>Approach</h2>\n<p>1. Clamp i to [0, len(xs)].</p>\n<p>2. Allocate result with cap len+1.</p>\n<p>3. Append xs[:i], then v, then xs[i:].</p>\n<p>4. Return result (input untouched).</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc InsertAt(xs []int, i, v int) []int {\n\tif i &lt; 0 {\n\t\ti = 0\n\t}\n\tif i &gt; len(xs) {\n\t\ti = len(xs)\n\t}\n\tout := make([]int, 0, len(xs)+1)\n\tout = append(out, xs[:i]...)\n\tout = append(out, v)\n\tout = append(out, xs[i:]...)\n\treturn out\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>[1,2,3] i=1 v=9: xs[:1]=[1], +9 -&gt; [1,9], +xs[1:]=[2,3] -&gt; [1,9,2,3].</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Building into a fresh slice avoids the aliasing clobber.</li>\n<li><code>slices.Insert</code> (Go 1.21+) handles this correctly and efficiently.</li>\n<li>Clamp <code>i</code>; <code>xs[:i]</code> panics for <code>i &gt; len</code>.</li>\n</ul>",
    "starter": "// Package insertat inserts an element at an index.\npackage insertat\n\n// InsertAt returns xs with v inserted at index i (0..len). Out-of-range i\n// clamps to the nearest end.\n//\n// TODO(candidate): build the result with append and slice expressions.\nfunc InsertAt(xs []int, i, v int) []int {\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "insertat.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/03-composite-types/middle/joinmanual": {
    "id": "01-language-basics/03-composite-types/middle/joinmanual",
    "title": "Join with Separator",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Join with Separator</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>The inverse of split: glue parts with a separator, but not before the first.</p>\n<h2>Task</h2>\n<p>Implement <code>Join(parts, sep)</code> without <code>strings.Join</code>.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  [\"a\",\"b\",\"c\"], sep=\",\"\nOutput: \"a,b,c\"\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  [\"x\"], sep=\",\"\nOutput: \"x\"\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  [], sep=\",\"\nOutput: \"\"\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">strings.Builder</span><span class=\"tchip\">Separator placement</span><span class=\"tchip\">Empty case</span></div></details>",
    "education": "<h1>Joining strings</h1>\n<h2>Intuition</h2>\n<p>Write the separator <strong>before</strong> each element except the first, building into a</p>\n<p><code>strings.Builder</code>:</p>\n<pre class=\"md\"><code>\nvar b strings.Builder\nfor i, p := range parts {\n\tif i &gt; 0 { b.WriteString(sep) }\n\tb.WriteString(p)\n}\nreturn b.String()\n</code></pre>\n<h2>Approach</h2>\n<p>1. Use a strings.Builder.</p>\n<p>2. Iterate parts with index.</p>\n<p>3. Write sep before every part except the first.</p>\n<p>4. Write the part.</p>\n<p>5. Return builder string.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nimport \"strings\"\n\nfunc Join(parts []string, sep string) string {\n\tvar b strings.Builder\n\tfor i, p := range parts {\n\t\tif i &gt; 0 {\n\t\t\tb.WriteString(sep)\n\t\t}\n\t\tb.WriteString(p)\n\t}\n\treturn b.String()\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>[\"a\",\"b\",\"c\"]: i0 write \"a\"; i1 write \",\" then \"b\"; i2 write \",\" then \"c\" -&gt; \"a,b,c\".</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Don't put the separator after the last element.</li>\n<li>Use <code>strings.Builder</code>, not repeated <code>+=</code>, for many parts.</li>\n<li>Empty slice yields \"\".</li>\n</ul>",
    "starter": "// Package joinmanual joins strings with a separator by hand.\npackage joinmanual\n\n// Join concatenates parts with sep between them, without strings.Join. Empty\n// input yields \"\".\n//\n// TODO(candidate): build with a Builder, sep before all but the first.\nfunc Join(parts []string, sep string) string {\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "joinmanual.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/03-composite-types/middle/matmul": {
    "id": "01-language-basics/03-composite-types/middle/matmul",
    "title": "Matrix Multiply",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Matrix Multiply</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>The classic triple-loop matrix product.</p>\n<h2>Task</h2>\n<p>Implement <code>Mul(a, b)</code>; return nil on dimension mismatch.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  [[1,2],[3,4]] * [[5,6],[7,8]]\nOutput: [[19,22],[43,50]]\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  [[1,2,3]] * [[1]]\nOutput: nil\n</code></pre>\n<p>_Explanation:_ inner dims 3 vs 1 mismatch</p>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  [[2]] * [[3]]\nOutput: [[6]]\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Dimensions</span><span class=\"tchip\">Dot product</span><span class=\"tchip\">Allocate result</span></div></details>",
    "education": "<h1>Matrix multiplication</h1>\n<h2>Intuition</h2>\n<p>Each output cell is a dot product of a row of <code>a</code> and a column of <code>b</code>:</p>\n<pre class=\"md\"><code>\nfor i := range a {\n\tfor j := range b[0] {\n\t\ts := 0\n\t\tfor k := range b { s += a[i][k] * b[k][j] }\n\t\tout[i][j] = s\n\t}\n}\n</code></pre>\n<h2>Approach</h2>\n<p>1. m=len(a); if 0 return nil.</p>\n<p>2. n=len(a[0]); if len(b)!=n return nil.</p>\n<p>3. p=len(b[0]).</p>\n<p>4. For each i,j sum over k of a[i][k]*b[k][j].</p>\n<p>5. Return m x p result.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Mul(a, b [][]int) [][]int {\n\tm := len(a)\n\tif m == 0 {\n\t\treturn nil\n\t}\n\tn := len(a[0])\n\tif len(b) != n {\n\t\treturn nil\n\t}\n\tp := 0\n\tif n &gt; 0 {\n\t\tp = len(b[0])\n\t}\n\tout := make([][]int, m)\n\tfor i := range out {\n\t\tout[i] = make([]int, p)\n\t\tfor j := 0; j &lt; p; j++ {\n\t\t\tsum := 0\n\t\t\tfor k := 0; k &lt; n; k++ {\n\t\t\t\tsum += a[i][k] * b[k][j]\n\t\t\t}\n\t\t\tout[i][j] = sum\n\t\t}\n\t}\n\treturn out\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>out[0][0]=1<em>5+2</em>7=19; out[0][1]=1<em>6+2</em>8=22; out[1][0]=3<em>5+4</em>7=43; out[1][1]=3<em>6+4</em>8=50.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Validate <code>len(a[0]) == len(b)</code> before multiplying.</li>\n<li>Pre-allocate the m\u00d7p result.</li>\n<li>Naive triple loop is O(m\u00b7n\u00b7p); fine for small matrices.</li>\n</ul>",
    "starter": "// Package matmul multiplies two matrices.\npackage matmul\n\n// Mul returns a*b for compatible matrices (a is m\u00d7n, b is n\u00d7p \u2192 m\u00d7p). If the\n// inner dimensions don't match, it returns nil.\n//\n// TODO(candidate): triple loop, dot products.\nfunc Mul(a, b [][]int) [][]int {\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "matmul.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/03-composite-types/middle/mergesorted": {
    "id": "01-language-basics/03-composite-types/middle/mergesorted",
    "title": "Merge Sorted Slices",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Merge Sorted Slices</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>The merge step of merge sort: combine two sorted runs into one, in O(n).</p>\n<h2>Task</h2>\n<p>Implement <code>Merge(a, b)</code> (ascending, duplicates kept).</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  [1,3,5],[2,4,6]\nOutput: [1,2,3,4,5,6]\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  [1,1,2],[1,3]\nOutput: [1,1,1,2,3]\n</code></pre>\n<p>_Explanation:_ duplicates kept</p>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  nil,nil\nOutput: []\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Two pointers</span><span class=\"tchip\">Drain remainder</span><span class=\"tchip\">Stable dupes</span></div></details>",
    "education": "<h1>The merge step</h1>\n<h2>Intuition</h2>\n<p>Two indices walk the sorted inputs; append the smaller front element, advance</p>\n<p>that side, and finally drain whatever remains:</p>\n<pre class=\"md\"><code>\ni, j := 0, 0\nfor i &lt; len(a) &amp;&amp; j &lt; len(b) {\n\tif a[i] &lt;= b[j] { out = append(out, a[i]); i++ } else { out = append(out, b[j]); j++ }\n}\nout = append(out, a[i:]...)\nout = append(out, b[j:]...)\n</code></pre>\n<h2>Approach</h2>\n<p>1. Two pointers i,j at heads of a,b.</p>\n<p>2. While both remain, append the smaller (a[i]&lt;=b[j] keeps stability).</p>\n<p>3. Append the remaining tail of whichever slice is left.</p>\n<p>4. Return merged slice.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Merge(a, b []int) []int {\n\tout := make([]int, 0, len(a)+len(b))\n\ti, j := 0, 0\n\tfor i &lt; len(a) &amp;&amp; j &lt; len(b) {\n\t\tif a[i] &lt;= b[j] {\n\t\t\tout = append(out, a[i])\n\t\t\ti++\n\t\t} else {\n\t\t\tout = append(out, b[j])\n\t\t\tj++\n\t\t}\n\t}\n\tout = append(out, a[i:]...)\n\tout = append(out, b[j:]...)\n\treturn out\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>a=[1,3,5] b=[2,4,6]: 1&lt;=2 take1; 3&gt;2 take2; 3&lt;=4 take3; 5&gt;4 take4; 5&lt;=6 take5; a done, append b tail [6] -&gt; [1,2,3,4,5,6].</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Use <code>&lt;=</code> (not <code>&lt;</code>) to keep the merge stable for equal keys.</li>\n<li>Remember to drain both tails \u2014 one will be empty.</li>\n<li>Pre-size the output to <code>len(a)+len(b)</code> to avoid regrowth.</li>\n</ul>",
    "starter": "// Package mergesorted merges two sorted slices.\npackage mergesorted\n\n// Merge returns the sorted merge of two ascending slices a and b (duplicates\n// kept).\n//\n// TODO(candidate): two-pointer merge.\nfunc Merge(a, b []int) []int {\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "mergesorted.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/03-composite-types/middle/mostcommon": {
    "id": "01-language-basics/03-composite-types/middle/mostcommon",
    "title": "Mode (Most Common)",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Mode (Most Common)</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>The statistical mode: the most frequent value, tie broken deterministically.</p>\n<h2>Task</h2>\n<p>Implement <code>Mode(xs)</code>; empty \u2192 ok=false; ties \u2192 smaller value.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  [1,2,2,3]\nOutput: (2,true)\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  [1,1,2,2]\nOutput: (1,true)\n</code></pre>\n<p>_Explanation:_ tie -&gt; smaller value</p>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  nil\nOutput: (0,false)\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Count map</span><span class=\"tchip\">Argmax</span><span class=\"tchip\">Deterministic ties</span></div></details>",
    "education": "<h1>Argmax over a count map</h1>\n<h2>Intuition</h2>\n<p>Count occurrences, then select the key with the highest count, breaking ties</p>\n<p>deterministically (here, the smaller value):</p>\n<pre class=\"md\"><code>\nbest, bestN, have := 0, 0, false\nfor v, n := range counts {\n\tif !have || n &gt; bestN || (n == bestN &amp;&amp; v &lt; best) { best, bestN, have = v, n, true }\n}\n</code></pre>\n<h2>Approach</h2>\n<p>1. Empty -&gt; (0,false).</p>\n<p>2. Count occurrences into a map.</p>\n<p>3. Track best value: higher count wins; equal count keeps the smaller value.</p>\n<p>4. Return (best,true).</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Mode(xs []int) (int, bool) {\n\tif len(xs) == 0 {\n\t\treturn 0, false\n\t}\n\tcounts := map[int]int{}\n\tfor _, v := range xs {\n\t\tcounts[v]++\n\t}\n\tbest, bestCount := 0, -1\n\tfor v, c := range counts {\n\t\tif c &gt; bestCount || (c == bestCount &amp;&amp; v &lt; best) {\n\t\t\tbest, bestCount = v, c\n\t\t}\n\t}\n\treturn best, true\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>[1,1,2,2] counts {1:2,2:2}. Both count 2; smaller value 1 wins -&gt; (1,true).</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Map iteration order is random \u2014 never rely on it; encode the tie-break.</li>\n<li>Return <code>ok=false</code> for empty rather than a fake value.</li>\n<li>Initialize the \"best\" carefully (use a <code>have</code> flag).</li>\n</ul>",
    "starter": "// Package mostcommon finds the most frequent element.\npackage mostcommon\n\n// Mode returns the most frequent value in xs and ok=false if xs is empty. Ties\n// are broken by the smallest value.\n//\n// TODO(candidate): count, then pick max count (min value on tie).\nfunc Mode(xs []int) (int, bool) {\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "mostcommon.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/03-composite-types/middle/movezeros": {
    "id": "01-language-basics/03-composite-types/middle/movezeros",
    "title": "Move Zeros",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Move Zeros</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>Compacting non-zero values to the front (stable), zeros to the back \u2014 a common</p>\n<p>array-shuffling task.</p>\n<h2>Task</h2>\n<p>Implement <code>MoveZeros(xs)</code> (stable order of non-zeros).</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  [0,1,0,3,12]\nOutput: [1,3,12,0,0]\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  [1,2,3]\nOutput: [1,2,3]\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  nil\nOutput: []\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Stable compaction</span><span class=\"tchip\">Two-phase build</span><span class=\"tchip\">Length preserved</span></div></details>",
    "education": "<h1>Stable compaction</h1>\n<h2>Intuition</h2>\n<p>Collect the non-zero elements in order, then pad the rest with zeros:</p>\n<pre class=\"md\"><code>\nout := []int{}\nfor _, x := range xs { if x != 0 { out = append(out, x) } }\nfor len(out) &lt; len(xs) { out = append(out, 0) }\n</code></pre>\n<h2>Approach</h2>\n<p>1. Walk xs; count zeros, append non-zeros in order.</p>\n<p>2. After the pass, append that many zeros.</p>\n<p>3. Return the new slice.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc MoveZeros(xs []int) []int {\n\tout := make([]int, 0, len(xs))\n\tzeros := 0\n\tfor _, v := range xs {\n\t\tif v == 0 {\n\t\t\tzeros++\n\t\t} else {\n\t\t\tout = append(out, v)\n\t\t}\n\t}\n\tfor ; zeros &gt; 0; zeros-- {\n\t\tout = append(out, 0)\n\t}\n\treturn out\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>[0,1,0,3,12]: non-zeros [1,3,12], zeros=2 -&gt; append 0,0 -&gt; [1,3,12,0,0].</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Preserve the <strong>order</strong> of non-zeros (stable) \u2014 a swap-based version wouldn't.</li>\n<li>Output length equals input length.</li>\n<li>An in-place two-pointer version avoids the second allocation.</li>\n</ul>",
    "starter": "// Package movezeros moves zeros to the end, keeping order of non-zeros.\npackage movezeros\n\n// MoveZeros returns a new slice with all non-zero elements first (in original\n// order) followed by the zeros.\n//\n// TODO(candidate): collect non-zeros, then pad with zeros.\nfunc MoveZeros(xs []int) []int {\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "movezeros.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/03-composite-types/middle/partition": {
    "id": "01-language-basics/03-composite-types/middle/partition",
    "title": "Partition by Predicate",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Partition by Predicate</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>Splitting a stream into two buckets (pass/fail, even/odd) in one pass.</p>\n<h2>Task</h2>\n<p>Implement <code>Partition(xs)</code> \u2192 (evens, odds), order preserved.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  [1,2,3,4,5,6]\nOutput: evens=[2,4,6], odds=[1,3,5]\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  nil\nOutput: evens=[], odds=[]\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  [2,4]\nOutput: evens=[2,4], odds=[]\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Two accumulators</span><span class=\"tchip\">Predicate</span><span class=\"tchip\">Multiple returns</span></div></details>",
    "education": "<h1>Partitioning into buckets</h1>\n<h2>Intuition</h2>\n<p>Route each element to one of two accumulators based on a predicate:</p>\n<pre class=\"md\"><code>\nfor _, x := range xs {\n\tif x%2 == 0 { evens = append(evens, x) } else { odds = append(odds, x) }\n}\n</code></pre>\n<h2>Approach</h2>\n<p>1. Iterate xs.</p>\n<p>2. v%2==0 -&gt; append to evens, else append to odds.</p>\n<p>3. Order preserved by append.</p>\n<p>4. Return both slices.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Partition(xs []int) (evens, odds []int) {\n\tfor _, v := range xs {\n\t\tif v%2 == 0 {\n\t\t\tevens = append(evens, v)\n\t\t} else {\n\t\t\todds = append(odds, v)\n\t\t}\n\t}\n\treturn evens, odds\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>[1..6]: 1 odd,2 even,3 odd,4 even,5 odd,6 even -&gt; evens=[2,4,6], odds=[1,3,5].</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Named returns start as <code>nil</code>; appending is fine, and the test accepts nil/empty.</li>\n<li>Generalizes to N buckets with a <code>map[key][]T</code>.</li>\n<li>Order is preserved only within each bucket, not across them.</li>\n</ul>",
    "starter": "// Package partition splits by a predicate.\npackage partition\n\n// Partition returns (evens, odds) preserving order within each group.\n//\n// TODO(candidate): append each element to the matching slice.\nfunc Partition(xs []int) (evens, odds []int) {\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "partition.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/03-composite-types/middle/reverseinplace": {
    "id": "01-language-basics/03-composite-types/middle/reverseinplace",
    "title": "Reverse In Place",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Reverse In Place</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>Reversing without allocating: swap the ends inward. Because slices share their</p>\n<p>backing array, this mutates the caller's data.</p>\n<h2>Task</h2>\n<p>Implement <code>Reverse(xs)</code> in place (also return it).</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  [1,2,3,4]\nOutput: [4,3,2,1]\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  [1,2,3]\nOutput: [3,2,1]\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  nil\nOutput: []\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Two-pointer swap</span><span class=\"tchip\">In-place mutation</span><span class=\"tchip\">Stop at middle</span></div></details>",
    "education": "<h1>In-place reversal</h1>\n<h2>Intuition</h2>\n<p>Swap symmetric pairs from the outside in, stopping at the middle:</p>\n<pre class=\"md\"><code>\nfor i, j := 0, len(xs)-1; i &lt; j; i, j = i+1, j-1 {\n\txs[i], xs[j] = xs[j], xs[i]\n}\n</code></pre>\n<h2>Approach</h2>\n<p>1. Two indices i=0, j=len-1.</p>\n<p>2. Swap xs[i],xs[j], move inward until i&gt;=j.</p>\n<p>3. Mutates the input in place.</p>\n<p>4. Return xs.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Reverse(xs []int) []int {\n\tfor i, j := 0, len(xs)-1; i &lt; j; i, j = i+1, j-1 {\n\t\txs[i], xs[j] = xs[j], xs[i]\n\t}\n\treturn xs\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>[1,2,3,4]: swap(0,3)-&gt;[4,2,3,1]; swap(1,2)-&gt;[4,3,2,1]; i&gt;=j stop.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>This <strong>mutates</strong> the caller's slice (shared backing array); copy first if the</li>\n</ul>\n<p>  original must survive.</p>\n<ul>\n<li>Multiple assignment swaps without a temp.</li>\n<li><code>slices.Reverse</code> (Go 1.21+) does this generically.</li>\n</ul>",
    "starter": "// Package reverseinplace reverses a slice in place.\npackage reverseinplace\n\n// Reverse reverses xs in place (mutating it) and returns it for convenience.\n//\n// TODO(candidate): swap ends moving inward.\nfunc Reverse(xs []int) []int {\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "reverseinplace.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/03-composite-types/middle/rotate": {
    "id": "01-language-basics/03-composite-types/middle/rotate",
    "title": "Rotate a Slice",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Rotate a Slice</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>A round-robin scheduler rotates the queue. Rotate left by k, wrapping, for any k.</p>\n<h2>Task</h2>\n<p>Implement <code>Left(xs, k)</code> returning a new rotated slice.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  [1,2,3,4,5], k=2\nOutput: [3,4,5,1,2]\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  [1,2,3], k=4\nOutput: [2,3,1]\n</code></pre>\n<p>_Explanation:_ k mod len</p>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  [1,2,3], k=-1\nOutput: [3,1,2]\n</code></pre>\n<p>_Explanation:_ negative normalized</p>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Normalize k</span><span class=\"tchip\">Reassemble</span><span class=\"tchip\">Fresh slice</span></div></details>",
    "education": "<h1>Rotation by slice reassembly</h1>\n<h2>Intuition</h2>\n<p>Rotating left by <code>k</code> moves the first <code>k</code> elements to the end. Normalize <code>k</code> into</p>\n<p><code>[0,n)</code> first (handles large and negative), then concatenate the two parts:</p>\n<pre class=\"md\"><code>\nk = ((k % n) + n) % n\nout := append([]int{}, xs[k:]...)\nout = append(out, xs[:k]...)\n</code></pre>\n<h2>Approach</h2>\n<p>1. n=len; if 0 return empty.</p>\n<p>2. Normalize k = ((k%n)+n)%n.</p>\n<p>3. Result = xs[k:] followed by xs[:k], copied into a new slice.</p>\n<p>4. Return it (input untouched).</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Left(xs []int, k int) []int {\n\tn := len(xs)\n\tif n == 0 {\n\t\treturn []int{}\n\t}\n\tk = ((k % n) + n) % n\n\tout := make([]int, 0, n)\n\tout = append(out, xs[k:]...)\n\tout = append(out, xs[:k]...)\n\treturn out\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>[1..5] k=2: xs[2:]=[3,4,5] + xs[:2]=[1,2] -&gt; [3,4,5,1,2]. k=-1,n=3 -&gt; ((-1%3)+3)%3=2 -&gt; xs[2:]+xs[:2]=[3]+[1,2]=[3,1,2].</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Guard <code>n == 0</code> before <code>k % n</code>.</li>\n<li><code>append([]int{}, xs[k:]...)</code> copies, so the result doesn't alias <code>xs</code>.</li>\n<li>An in-place rotate (three reversals) is O(1) space but mutates the input.</li>\n</ul>",
    "starter": "// Package rotate rotates a slice left by k positions.\npackage rotate\n\n// Left returns a new slice equal to xs rotated left by k (k may exceed len or be\n// negative). The input is not modified.\n//\n// TODO(candidate): normalize k, then reassemble with slice expressions.\nfunc Left(xs []int, k int) []int {\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "rotate.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/03-composite-types/middle/runlength": {
    "id": "01-language-basics/03-composite-types/middle/runlength",
    "title": "Run-Length Encode",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Run-Length Encode</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>A simple compression: replace runs of a repeated character with the character</p>\n<p>and a count.</p>\n<h2>Task</h2>\n<p>Implement <code>Encode(s)</code> producing <code>char + count</code> per run.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  \"aaab\"\nOutput: \"a3b1\"\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  \"abc\"\nOutput: \"a1b1c1\"\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  \"\"\nOutput: \"\"\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Run detection</span><span class=\"tchip\">Builder + Itoa</span><span class=\"tchip\">Empty input</span></div></details>",
    "education": "<h1>Run-length encoding</h1>\n<h2>Intuition</h2>\n<p>Scan consecutive equal bytes, counting the run; when the byte changes (or the</p>\n<p>string ends), emit the character and its count:</p>\n<pre class=\"md\"><code>\nfor i := 0; i &lt; len(s); {\n\tj := i\n\tfor j &lt; len(s) &amp;&amp; s[j] == s[i] { j++ }\n\tb.WriteByte(s[i]); b.WriteString(strconv.Itoa(j - i))\n\ti = j\n}\n</code></pre>\n<h2>Approach</h2>\n<p>1. Scan s with index i.</p>\n<p>2. Extend j while s[j]==s[i] to find run end.</p>\n<p>3. Write the char then the run length (strconv.Itoa).</p>\n<p>4. Set i=j and continue.</p>\n<p>5. Return builder string.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nimport (\n\t\"strconv\"\n\t\"strings\"\n)\n\nfunc Encode(s string) string {\n\tvar b strings.Builder\n\tfor i := 0; i &lt; len(s); {\n\t\tj := i\n\t\tfor j &lt; len(s) &amp;&amp; s[j] == s[i] {\n\t\t\tj++\n\t\t}\n\t\tb.WriteByte(s[i])\n\t\tb.WriteString(strconv.Itoa(j - i))\n\t\ti = j\n\t}\n\treturn b.String()\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>\"aaab\": i=0 run of a length 3 -&gt; \"a3\"; i=3 run of b length 1 -&gt; \"b1\"; result \"a3b1\".</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Build with a <code>Builder</code>, not <code>+=</code>.</li>\n<li>Decoding must handle multi-digit counts (<code>a12</code>).</li>\n<li>RLE only helps when runs are long; it can expand random data.</li>\n</ul>",
    "starter": "// Package runlength run-length-encodes a byte string.\npackage runlength\n\n// Encode returns the run-length encoding of s as pairs: for each run, the\n// character followed by its count as decimal. E.g. \"aaab\" -> \"a3b1\".\n//\n// TODO(candidate): scan runs, emit char + count.\nfunc Encode(s string) string {\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "runlength.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/03-composite-types/middle/setdiff": {
    "id": "01-language-basics/03-composite-types/middle/setdiff",
    "title": "Set Difference",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Set Difference</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>\"In A but not B\" \u2014 revoked items, missing keys.</p>\n<h2>Task</h2>\n<p>Implement <code>Diff(a, b)</code> \u2014 sorted, unique, in a not b.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  a=[1,2,3,3,4], b=[2,4]\nOutput: [1,3]\n</code></pre>\n<p>_Explanation:_ in a not in b, deduped+sorted</p>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  a=[1], b=[1]\nOutput: []\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  a=[5,5], b=[]\nOutput: [5]\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Exclusion set</span><span class=\"tchip\">Dedup</span><span class=\"tchip\">Sort</span></div></details>",
    "education": "<h1>Set difference</h1>\n<h2>Intuition</h2>\n<p>Build a set from <code>b</code>, then keep the elements of <code>a</code> absent from it, de-duping:</p>\n<pre class=\"md\"><code>\nrm := make(map[int]struct{})\nfor _, x := range b { rm[x] = struct{}{} }\nseen := make(map[int]struct{})\nfor _, x := range a {\n\tif _, bad := rm[x]; bad { continue }\n\tif _, dup := seen[x]; dup { continue }\n\tseen[x] = struct{}{}; out = append(out, x)\n}\nsort.Ints(out)\n</code></pre>\n<h2>Approach</h2>\n<p>1. Build set inB from b.</p>\n<p>2. For each a-value not in inB, add to a result set (dedupe).</p>\n<p>3. Collect keys, sort ascending.</p>\n<p>4. Return sorted slice.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nimport \"sort\"\n\nfunc Diff(a, b []int) []int {\n\tinB := map[int]bool{}\n\tfor _, v := range b {\n\t\tinB[v] = true\n\t}\n\tset := map[int]bool{}\n\tfor _, v := range a {\n\t\tif !inB[v] {\n\t\t\tset[v] = true\n\t\t}\n\t}\n\tout := make([]int, 0, len(set))\n\tfor v := range set {\n\t\tout = append(out, v)\n\t}\n\tsort.Ints(out)\n\treturn out\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>inB={2,4}. a: 1 keep,2 drop,3 keep,3 dup,4 drop -&gt; set{1,3} -&gt; sort -&gt; [1,3].</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>De-dup the output; <code>a</code> may repeat survivors.</li>\n<li>Sort for stable order.</li>\n<li>The exclusion set makes it O(n+m) instead of nested scans.</li>\n</ul>",
    "starter": "// Package setdiff computes the sorted difference a minus b.\npackage setdiff\n\n// Diff returns the sorted, de-duplicated values in a that are NOT in b.\n//\n// TODO(candidate): set of b, filter a.\nfunc Diff(a, b []int) []int {\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "setdiff.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/03-composite-types/middle/setintersect": {
    "id": "01-language-basics/03-composite-types/middle/setintersect",
    "title": "Set Intersection",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Set Intersection</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>Finding common elements (shared permissions, mutual friends).</p>\n<h2>Task</h2>\n<p>Implement <code>Intersect(a, b)</code> \u2014 sorted, unique, present in both.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  a=[1,2,3,4], b=[2,4,6,2]\nOutput: [2,4]\n</code></pre>\n<p>_Explanation:_ in both, deduped+sorted</p>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  a=[1], b=[2]\nOutput: []\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  a=[3,3], b=[3]\nOutput: [3]\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Set from a</span><span class=\"tchip\">Dedup result</span><span class=\"tchip\">Sort output</span></div></details>",
    "education": "<h1>Intersection with a lookup set</h1>\n<h2>Intuition</h2>\n<p>Put one side in a set, then filter the other by membership, de-duplicating the</p>\n<p>result:</p>\n<pre class=\"md\"><code>\nin := make(map[int]struct{})\nfor _, x := range a { in[x] = struct{}{} }\nseen := make(map[int]struct{})\nfor _, x := range b {\n\tif _, ok := in[x]; ok {\n\t\tif _, dup := seen[x]; !dup { out = append(out, x); seen[x] = struct{}{} }\n\t}\n}\nsort.Ints(out)\n</code></pre>\n<h2>Approach</h2>\n<p>1. Build set inA from a.</p>\n<p>2. For each b-value in inA, add to result set (dedupe).</p>\n<p>3. Collect keys, sort ascending.</p>\n<p>4. Return slice.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nimport \"sort\"\n\nfunc Intersect(a, b []int) []int {\n\tinA := map[int]bool{}\n\tfor _, v := range a {\n\t\tinA[v] = true\n\t}\n\tset := map[int]bool{}\n\tfor _, v := range b {\n\t\tif inA[v] {\n\t\t\tset[v] = true\n\t\t}\n\t}\n\tout := make([]int, 0, len(set))\n\tfor v := range set {\n\t\tout = append(out, v)\n\t}\n\tsort.Ints(out)\n\treturn out\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>inA={1,2,3,4}. b: 2 in -&gt;set{2}; 4 in -&gt;{2,4}; 6 no; 2 dup. sort -&gt; [2,4].</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>De-dup the output: <code>b</code> may repeat a common value.</li>\n<li>Membership is <code>_, ok := set[x]</code>.</li>\n<li>Sort for deterministic order.</li>\n</ul>",
    "starter": "// Package setintersect computes the sorted intersection of two int slices.\npackage setintersect\n\n// Intersect returns the sorted, de-duplicated values present in BOTH a and b.\n//\n// TODO(candidate): build a set from a, keep b's members that are in it.\nfunc Intersect(a, b []int) []int {\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "setintersect.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/03-composite-types/middle/setunion": {
    "id": "01-language-basics/03-composite-types/middle/setunion",
    "title": "Set Union",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Set Union</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>Combining two tag sets into one sorted, unique list.</p>\n<h2>Task</h2>\n<p>Implement <code>Union(a, b)</code> \u2014 sorted, de-duplicated.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  a=[3,1,2], b=[2,4,1]\nOutput: [1,2,3,4]\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  nil,nil\nOutput: []\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  a=[1,1], b=[1]\nOutput: [1]\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Set membership</span><span class=\"tchip\">Collect + sort</span><span class=\"tchip\">Dedup</span></div></details>",
    "education": "<h1>Set operations via maps</h1>\n<h2>Intuition</h2>\n<p>Insert both inputs into a set (a map), then collect and sort:</p>\n<pre class=\"md\"><code>\nset := make(map[int]struct{})\nfor _, x := range a { set[x] = struct{}{} }\nfor _, x := range b { set[x] = struct{}{} }\nout := make([]int, 0, len(set))\nfor x := range set { out = append(out, x) }\nsort.Ints(out)\n</code></pre>\n<h2>Approach</h2>\n<p>1. Add all of a then all of b into one set.</p>\n<p>2. Collect keys into a slice.</p>\n<p>3. sort.Ints ascending.</p>\n<p>4. Return slice.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nimport \"sort\"\n\nfunc Union(a, b []int) []int {\n\tset := map[int]bool{}\n\tfor _, v := range a {\n\t\tset[v] = true\n\t}\n\tfor _, v := range b {\n\t\tset[v] = true\n\t}\n\tout := make([]int, 0, len(set))\n\tfor v := range set {\n\t\tout = append(out, v)\n\t}\n\tsort.Ints(out)\n\treturn out\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>set from a={3,1,2}, add b -&gt; {1,2,3,4}; sort -&gt; [1,2,3,4].</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Map order is random \u2014 sort for stable output.</li>\n<li><code>struct{}</code> values use no memory.</li>\n<li>For huge sorted inputs, a merge is more cache-friendly than a map.</li>\n</ul>",
    "starter": "// Package setunion computes the sorted union of two int slices.\npackage setunion\n\n// Union returns the sorted, de-duplicated union of a and b.\n//\n// TODO(candidate): use a set, then sort.\nfunc Union(a, b []int) []int {\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "setunion.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/03-composite-types/middle/sortstructs": {
    "id": "01-language-basics/03-composite-types/middle/sortstructs",
    "title": "Sort Structs by Field",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Sort Structs by Field</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>Sorting records by a field with a stable tie-break \u2014 the everyday table sort.</p>\n<h2>Task</h2>\n<p>Implement <code>ByAge(people)</code> \u2014 sorted by Age, then Name; input untouched.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  [{bob,30},{amy,25},{cid,30}]\nOutput: [{amy,25},{bob,30},{cid,30}]\n</code></pre>\n<p>_Explanation:_ Age asc, Name asc on tie</p>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  [{x,1}]\nOutput: [{x,1}]\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  [{b,5},{a,5}]\nOutput: [{a,5},{b,5}]\n</code></pre>\n<p>_Explanation:_ tie broken by Name</p>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Copy first</span><span class=\"tchip\">sort.Slice</span><span class=\"tchip\">Compound key</span></div></details>",
    "education": "<h1>Sorting records</h1>\n<h2>Intuition</h2>\n<p><code>sort.Slice</code> takes a \"less\" function. For a stable, deterministic order, compare</p>\n<p>the primary field, then break ties on a secondary field:</p>\n<pre class=\"md\"><code>\nc := append([]Person{}, people...)\nsort.Slice(c, func(i, j int) bool {\n\tif c[i].Age != c[j].Age { return c[i].Age &lt; c[j].Age }\n\treturn c[i].Name &lt; c[j].Name\n})\n</code></pre>\n<h2>Approach</h2>\n<p>1. Copy input to a new slice (input must not mutate).</p>\n<p>2. sort.Slice with comparator: Age ascending; if Age equal, Name ascending.</p>\n<p>3. Return the copy.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nimport \"sort\"\n\ntype Person struct {\n\tName string\n\tAge  int\n}\n\nfunc ByAge(people []Person) []Person {\n\tout := make([]Person, len(people))\n\tcopy(out, people)\n\tsort.Slice(out, func(i, j int) bool {\n\t\tif out[i].Age != out[j].Age {\n\t\t\treturn out[i].Age &lt; out[j].Age\n\t\t}\n\t\treturn out[i].Name &lt; out[j].Name\n\t})\n\treturn out\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>bob(30),amy(25),cid(30): amy has lowest age; bob and cid tie at 30, bob&lt;cid by name -&gt; [amy,bob,cid].</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>sort.Slice</code> is <strong>not</strong> stable; encode tie-breaks in the comparator (or use</li>\n</ul>\n<p>  <code>sort.SliceStable</code>).</p>\n<ul>\n<li>Sort mutates in place \u2014 copy if the input must survive.</li>\n<li>The comparator must be a strict weak ordering (no <code>&lt;=</code>).</li>\n</ul>",
    "starter": "// Package sortstructs sorts records by a field.\npackage sortstructs\n\n// Person has a name and age.\ntype Person struct {\n\tName string\n\tAge  int\n}\n\n// ByAge returns a new slice sorted by Age ascending; ties keep Name ascending.\n// The input is not modified.\n//\n// TODO(candidate): copy, then sort.Slice with a compound comparator.\nfunc ByAge(people []Person) []Person {\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "sortstructs.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/03-composite-types/middle/splitmanual": {
    "id": "01-language-basics/03-composite-types/middle/splitmanual",
    "title": "Split by Byte",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Split by Byte</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>Understanding <code>strings.Split</code> by writing it: cut at each separator, emit the</p>\n<p>piece between cuts.</p>\n<h2>Task</h2>\n<p>Implement <code>Split(s, sep)</code> (single-byte separator), matching <code>strings.Split</code></p>\n<p>semantics.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  \"a,b,c\", sep=,\nOutput: [\"a\",\"b\",\"c\"]\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  \"a,,c\", sep=,\nOutput: [\"a\",\"\",\"c\"]\n</code></pre>\n<p>_Explanation:_ empty field between seps</p>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  \",\", sep=,\nOutput: [\"\",\"\"]\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Slice between cuts</span><span class=\"tchip\">Empty fields</span><span class=\"tchip\">Final field</span></div></details>",
    "education": "<h1>Splitting a string</h1>\n<h2>Intuition</h2>\n<p>Remember the start of the current field; when you hit a separator, emit</p>\n<p><code>s[start:i]</code> and move <code>start</code> past it. After the loop, emit the final field:</p>\n<pre class=\"md\"><code>\nstart := 0\nfor i := 0; i &lt; len(s); i++ {\n\tif s[i] == sep { out = append(out, s[start:i]); start = i + 1 }\n}\nout = append(out, s[start:])\n</code></pre>\n<h2>Approach</h2>\n<p>1. Track start index.</p>\n<p>2. Scan; at each sep append s[start:i] and set start=i+1.</p>\n<p>3. After the loop append the final segment s[start:].</p>\n<p>4. Result has seps+1 elements.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Split(s string, sep byte) []string {\n\tout := []string{}\n\tstart := 0\n\tfor i := 0; i &lt; len(s); i++ {\n\t\tif s[i] == sep {\n\t\t\tout = append(out, s[start:i])\n\t\t\tstart = i + 1\n\t\t}\n\t}\n\tout = append(out, s[start:])\n\treturn out\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>\"a,,c\": i1 sep append \"a\" start=2; i2 sep append \"\" (s[2:2]) start=3; end append \"c\" -&gt; [\"a\",\"\",\"c\"].</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>The result always has <code>count(sep)+1</code> elements \u2014 empty string gives <code>[\"\"]</code>.</li>\n<li>Fields are sub-strings sharing memory; no copying.</li>\n<li>This splits on a byte; a multi-byte or rune separator needs more care.</li>\n</ul>",
    "starter": "// Package splitmanual splits a string on a single-byte separator by hand.\npackage splitmanual\n\n// Split returns the fields of s separated by sep (a single byte), without using\n// strings.Split. Consecutive separators yield empty fields; result always has\n// (number of seps)+1 elements.\n//\n// TODO(candidate): scan, cutting at each sep.\nfunc Split(s string, sep byte) []string {\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "splitmanual.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/03-composite-types/middle/structfilter": {
    "id": "01-language-basics/03-composite-types/middle/structfilter",
    "title": "Filter and Project",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Filter and Project</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>Selecting records by a condition and pulling one field \u2014 filter + map.</p>\n<h2>Task</h2>\n<p>Implement <code>ActiveNames(users)</code> returning names of active users.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  [{ann,true},{bob,false},{cid,true}]\nOutput: [\"ann\",\"cid\"]\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  nil\nOutput: []\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  [{x,false}]\nOutput: []\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Filter</span><span class=\"tchip\">Project</span><span class=\"tchip\">append</span></div></details>",
    "education": "<h1>Filter then project</h1>\n<h2>Intuition</h2>\n<p>Combine two steps in one pass: skip records failing the predicate, and collect a</p>\n<p>projected field from the rest:</p>\n<pre class=\"md\"><code>\nfor _, u := range users {\n\tif u.Active { out = append(out, u.Name) }\n}\n</code></pre>\n<h2>Approach</h2>\n<p>1. Start with empty result.</p>\n<p>2. Iterate users in order.</p>\n<p>3. If Active, append Name.</p>\n<p>4. Return names.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\ntype User struct {\n\tName   string\n\tActive bool\n}\n\nfunc ActiveNames(users []User) []string {\n\tout := []string{}\n\tfor _, u := range users {\n\t\tif u.Active {\n\t\t\tout = append(out, u.Name)\n\t\t}\n\t}\n\treturn out\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>ann active -&gt; keep; bob inactive -&gt; skip; cid active -&gt; keep. [ann,cid].</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>The range value is a copy \u2014 fine for reading fields.</li>\n<li>Order is preserved.</li>\n<li>Pre-size with <code>make([]string, 0, len(users))</code> if most rows pass.</li>\n</ul>",
    "starter": "// Package structfilter filters records by a field.\npackage structfilter\n\n// User has a name and an active flag.\ntype User struct {\n\tName   string\n\tActive bool\n}\n\n// ActiveNames returns the names of active users, in order.\n//\n// TODO(candidate): filter and project.\nfunc ActiveNames(users []User) []string {\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "structfilter.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/03-composite-types/middle/takewhile": {
    "id": "01-language-basics/03-composite-types/middle/takewhile",
    "title": "Take While Positive",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Take While Positive</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>Reading a leading run until a condition breaks \u2014 like parsing a prefix.</p>\n<h2>Task</h2>\n<p>Implement <code>TakePositive(xs)</code> returning the longest all-positive prefix.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  [1,2,3,-1,4]\nOutput: [1,2,3]\n</code></pre>\n<p>_Explanation:_ stops at first &lt;=0</p>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  [-1,2]\nOutput: []\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  [1,2,3]\nOutput: [1,2,3]\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Prefix scan</span><span class=\"tchip\">Slice up to i</span><span class=\"tchip\">Empty result</span></div></details>",
    "education": "<h1>Prefix by predicate</h1>\n<h2>Intuition</h2>\n<p>Scan until the predicate fails, then slice up to that point:</p>\n<pre class=\"md\"><code>\ni := 0\nfor i &lt; len(xs) &amp;&amp; xs[i] &gt; 0 { i++ }\nreturn xs[:i]\n</code></pre>\n<h2>Approach</h2>\n<p>1. Start empty result.</p>\n<p>2. Walk xs; break at the first element &lt;=0.</p>\n<p>3. Otherwise append it.</p>\n<p>4. Return the leading positive run.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc TakePositive(xs []int) []int {\n\tout := []int{}\n\tfor _, v := range xs {\n\t\tif v &lt;= 0 {\n\t\t\tbreak\n\t\t}\n\t\tout = append(out, v)\n\t}\n\treturn out\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>[1,2,3,-1,4]: append 1,2,3; -1&lt;=0 break -&gt; [1,2,3].</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>xs[:i]</code> shares the backing array.</li>\n<li>Return a non-nil empty when the first element fails (<code>xs[:0]</code>).</li>\n<li>The dual, \"drop while\", slices from <code>i</code> onward.</li>\n</ul>",
    "starter": "// Package takewhile returns the leading run that satisfies a predicate.\npackage takewhile\n\n// TakePositive returns the longest prefix of xs whose elements are all > 0.\n//\n// TODO(candidate): stop at the first non-positive element.\nfunc TakePositive(xs []int) []int {\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "takewhile.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/03-composite-types/middle/topk": {
    "id": "01-language-basics/03-composite-types/middle/topk",
    "title": "Top K Frequent",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Top K Frequent</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>\"Top N\" analytics: count occurrences, then rank.</p>\n<h2>Task</h2>\n<p>Implement <code>TopK(xs, k)</code> \u2014 k most frequent, ties broken alphabetically.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  [\"a\",\"b\",\"a\",\"c\",\"b\",\"a\",\"d\"], k=2\nOutput: [\"a\",\"b\"]\n</code></pre>\n<p>_Explanation:_ a=3,b=2</p>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  same, k=4\nOutput: [\"a\",\"b\",\"c\",\"d\"]\n</code></pre>\n<p>_Explanation:_ c,d tie count1 -&gt; alphabetical</p>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  [\"z\",\"z\"], k=5\nOutput: [\"z\"]\n</code></pre>\n<p>_Explanation:_ k clamped to distinct count</p>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Count map</span><span class=\"tchip\">Custom sort</span><span class=\"tchip\">Clamp k</span></div></details>",
    "education": "<h1>Rank by frequency</h1>\n<h2>Intuition</h2>\n<p>Count with a map, then sort the distinct keys by a compound key \u2014 count</p>\n<p>descending, then the value ascending for stable tie-breaks:</p>\n<pre class=\"md\"><code>\nsort.Slice(keys, func(i, j int) bool {\n\tif m[keys[i]] != m[keys[j]] { return m[keys[i]] &gt; m[keys[j]] }\n\treturn keys[i] &lt; keys[j]\n})\n</code></pre>\n<h2>Approach</h2>\n<p>1. Count word frequencies into a map.</p>\n<p>2. Collect distinct words.</p>\n<p>3. Sort by count descending, then word ascending on ties.</p>\n<p>4. Clamp k to len(words).</p>\n<p>5. Return first k.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nimport \"sort\"\n\nfunc TopK(xs []string, k int) []string {\n\tcounts := map[string]int{}\n\tfor _, w := range xs {\n\t\tcounts[w]++\n\t}\n\twords := make([]string, 0, len(counts))\n\tfor w := range counts {\n\t\twords = append(words, w)\n\t}\n\tsort.Slice(words, func(i, j int) bool {\n\t\tif counts[words[i]] != counts[words[j]] {\n\t\t\treturn counts[words[i]] &gt; counts[words[j]]\n\t\t}\n\t\treturn words[i] &lt; words[j]\n\t})\n\tif k &gt; len(words) {\n\t\tk = len(words)\n\t}\n\tif k &lt; 0 {\n\t\tk = 0\n\t}\n\treturn words[:k]\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>counts a:3,b:2,c:1,d:1. sort -&gt; [a,b,c,d]. k=2 -&gt; [a,b]; k=4 ties c&lt;d alphabetically -&gt; [a,b,c,d].</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Map iteration is random; you must sort for a stable ranking.</li>\n<li>Break ties explicitly, or equal counts come out in arbitrary order.</li>\n<li>For large data, a heap of size k beats a full sort.</li>\n</ul>",
    "starter": "// Package topk returns the k most frequent strings.\npackage topk\n\n// TopK returns the k most frequent words in xs, most frequent first. Ties are\n// broken alphabetically. k is clamped to the number of distinct words.\n//\n// TODO(candidate): count, then sort by (count desc, word asc), take k.\nfunc TopK(xs []string, k int) []string {\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "topk.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/03-composite-types/middle/transpose": {
    "id": "01-language-basics/03-composite-types/middle/transpose",
    "title": "Matrix Transpose",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Matrix Transpose</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>Turning rows into columns \u2014 pivoting a table.</p>\n<h2>Task</h2>\n<p>Implement <code>Transpose(grid)</code> for a rectangular grid.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  [[1,2,3],[4,5,6]]\nOutput: [[1,4],[2,5],[3,6]]\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  nil\nOutput: []\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  [[1],[2]]\nOutput: [[1,2]]\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Dimensions swap</span><span class=\"tchip\">Allocate result</span><span class=\"tchip\">Index swap</span></div></details>",
    "education": "<h1>Transposing a matrix</h1>\n<h2>Intuition</h2>\n<p>Element <code>[i][j]</code> moves to <code>[j][i]</code>. Allocate the transposed shape, then copy:</p>\n<pre class=\"md\"><code>\nout := make([][]int, cols)\nfor j := range out { out[j] = make([]int, rows) }\nfor i := range grid {\n\tfor j := range grid[i] { out[j][i] = grid[i][j] }\n}\n</code></pre>\n<h2>Approach</h2>\n<p>1. Empty grid -&gt; empty grid.</p>\n<p>2. rows=len(grid), cols=len(grid[0]).</p>\n<p>3. Allocate cols x rows.</p>\n<p>4. out[j][i]=grid[i][j].</p>\n<p>5. Return transposed grid.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Transpose(grid [][]int) [][]int {\n\tif len(grid) == 0 || len(grid[0]) == 0 {\n\t\treturn [][]int{}\n\t}\n\trows, cols := len(grid), len(grid[0])\n\tout := make([][]int, cols)\n\tfor j := 0; j &lt; cols; j++ {\n\t\tout[j] = make([]int, rows)\n\t\tfor i := 0; i &lt; rows; i++ {\n\t\t\tout[j][i] = grid[i][j]\n\t\t}\n\t}\n\treturn out\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>2x3 grid: out[0]=[grid[0][0],grid[1][0]]=[1,4]; out[1]=[2,5]; out[2]=[3,6].</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Assumes rectangular input; ragged rows need care.</li>\n<li>Allocate each inner row with <code>make</code>, or you index into nil.</li>\n<li>Guard the empty grid before reading <code>grid[0]</code>.</li>\n</ul>",
    "starter": "// Package transpose transposes a rectangular matrix.\npackage transpose\n\n// Transpose returns the transpose of a rectangular grid (rows become columns).\n// An empty grid returns an empty grid.\n//\n// TODO(candidate): build a cols x rows result.\nfunc Transpose(grid [][]int) [][]int {\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "transpose.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/03-composite-types/middle/twosum": {
    "id": "01-language-basics/03-composite-types/middle/twosum",
    "title": "Two Sum",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Two Sum</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>Find two elements adding to a target \u2014 the canonical map-as-index-lookup.</p>\n<h2>Task</h2>\n<p>Implement <code>TwoSum(xs, target)</code> returning indices <code>i&lt;j</code>, or ok=false.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  [2,7,11,15], target=9\nOutput: (0,1,true)\n</code></pre>\n<p>_Explanation:_ 2+7=9</p>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  [3,2,4], target=6\nOutput: (1,2,true)\n</code></pre>\n<p>_Explanation:_ 2+4=6</p>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  [1,2], target=100\nOutput: (0,0,false)\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Complement lookup</span><span class=\"tchip\">Map value\u2192index</span><span class=\"tchip\">Single pass</span></div></details>",
    "education": "<h1>Map as an index</h1>\n<h2>Intuition</h2>\n<p>Walk once; for each element look up whether its complement was already seen. A</p>\n<p>map from value to index gives O(1) lookups:</p>\n<pre class=\"md\"><code>\nseen := make(map[int]int)\nfor i, x := range xs {\n\tif j, ok := seen[target-x]; ok { return j, i, true }\n\tseen[x] = i\n}\nreturn 0, 0, false\n</code></pre>\n<h2>Approach</h2>\n<p>1. Map seen value -&gt; earliest index.</p>\n<p>2. For each element, look up complement target-v.</p>\n<p>3. If found, return (storedIndex, currentIndex, true).</p>\n<p>4. Otherwise record v's index if unseen.</p>\n<p>5. Return false if no pair.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc TwoSum(xs []int, target int) (i, j int, ok bool) {\n\tseen := map[int]int{}\n\tfor idx, v := range xs {\n\t\tif p, found := seen[target-v]; found {\n\t\t\treturn p, idx, true\n\t\t}\n\t\tif _, dup := seen[v]; !dup {\n\t\t\tseen[v] = idx\n\t\t}\n\t}\n\treturn 0, 0, false\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>[3,2,4] t=6: 3 store; 2 store; 4 complement 2 found at index1 -&gt; (1,2,true).</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Check for the complement <strong>before</strong> inserting the current element, or you may</li>\n</ul>\n<p>  pair an element with itself.</p>\n<ul>\n<li>The map stores the earliest index of each value.</li>\n<li>Returns the first pair found; multiple pairs may exist.</li>\n</ul>",
    "starter": "// Package twosum finds indices of two numbers adding to a target.\npackage twosum\n\n// TwoSum returns the indices i<j such that xs[i]+xs[j]==target, or ok=false if\n// none. Uses a map for O(n).\n//\n// TODO(candidate): store seen value -> index, look for complement.\nfunc TwoSum(xs []int, target int) (i, j int, ok bool) {\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "twosum.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/03-composite-types/middle/window": {
    "id": "01-language-basics/03-composite-types/middle/window",
    "title": "Sliding Window Sums",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Sliding Window Sums</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>A metrics smoother reports the sum over each window of k samples.</p>\n<h2>Task</h2>\n<p>Implement <code>Sums(xs, k)</code>; invalid k \u2192 empty.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  [1,2,3,4], k=2\nOutput: [3,5,7]\n</code></pre>\n<p>_Explanation:_ 1+2,2+3,3+4</p>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  [1,2,3], k=3\nOutput: [6]\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  [1,2,3], k=5\nOutput: []\n</code></pre>\n<p>_Explanation:_ k&gt;len</p>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Window count</span><span class=\"tchip\">Rolling sum</span><span class=\"tchip\">Guard k</span></div></details>",
    "education": "<h1>Rolling window sums</h1>\n<h2>Intuition</h2>\n<p>Naively each window sum is O(k); rolling makes it O(1) per step by adding the new</p>\n<p>element and subtracting the one that left:</p>\n<pre class=\"md\"><code>\nsum := 0\nfor i := 0; i &lt; k; i++ { sum += xs[i] }\nout = append(out, sum)\nfor i := k; i &lt; len(xs); i++ { sum += xs[i] - xs[i-k]; out = append(out, sum) }\n</code></pre>\n<h2>Approach</h2>\n<p>1. If k&lt;=0 or k&gt;len return empty.</p>\n<p>2. Sum the first window.</p>\n<p>3. Slide: add xs[i], subtract xs[i-k] (rolling sum).</p>\n<p>4. Append each window sum.</p>\n<p>5. Return sums.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Sums(xs []int, k int) []int {\n\tout := []int{}\n\tif k &lt;= 0 || k &gt; len(xs) {\n\t\treturn out\n\t}\n\tsum := 0\n\tfor i := 0; i &lt; k; i++ {\n\t\tsum += xs[i]\n\t}\n\tout = append(out, sum)\n\tfor i := k; i &lt; len(xs); i++ {\n\t\tsum += xs[i] - xs[i-k]\n\t\tout = append(out, sum)\n\t}\n\treturn out\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>[1,2,3,4] k=2: first 1+2=3; +3-1=5; +4-2=7 -&gt; [3,5,7].</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>There are <code>len(xs)-k+1</code> windows; guard <code>k &gt; len</code> and <code>k &lt;= 0</code>.</li>\n<li>Rolling avoids recomputation but accumulates rounding for floats.</li>\n<li>Sub-slices would share memory; here you output computed sums.</li>\n</ul>",
    "starter": "// Package window computes sliding-window sums.\npackage window\n\n// Sums returns the sum of each contiguous window of length k in xs. If k > len\n// or k <= 0, it returns an empty slice.\n//\n// TODO(candidate): compute efficiently (rolling sum) or directly.\nfunc Sums(xs []int, k int) []int {\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "window.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/03-composite-types/middle/zip": {
    "id": "01-language-basics/03-composite-types/middle/zip",
    "title": "Zip to Map",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Zip to Map</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>Pairing parallel arrays (column names + values) into a map.</p>\n<h2>Task</h2>\n<p>Implement <code>Zip(keys, vals)</code> over the shorter length.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  keys=[\"a\",\"b\",\"c\"], vals=[1,2,3]\nOutput: {a:1,b:2,c:3}\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  keys=[\"a\",\"b\"], vals=[1,2,3]\nOutput: {a:1,b:2}\n</code></pre>\n<p>_Explanation:_ extra val ignored</p>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  keys=[], vals=[1]\nOutput: {}\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Parallel index</span><span class=\"tchip\">Min length</span><span class=\"tchip\">Build a map</span></div></details>",
    "education": "<h1>Zipping parallel slices</h1>\n<h2>Intuition</h2>\n<p>Iterate to the shorter length and pair by index:</p>\n<pre class=\"md\"><code>\nn := len(keys)\nif len(vals) &lt; n { n = len(vals) }\nfor i := 0; i &lt; n; i++ { m[keys[i]] = vals[i] }\n</code></pre>\n<h2>Approach</h2>\n<p>1. n = min(len(keys),len(vals)).</p>\n<p>2. For i in [0,n) set out[keys[i]]=vals[i].</p>\n<p>3. Extra elements of the longer slice are ignored.</p>\n<p>4. Return map.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Zip(keys []string, vals []int) map[string]int {\n\tout := map[string]int{}\n\tn := len(keys)\n\tif len(vals) &lt; n {\n\t\tn = len(vals)\n\t}\n\tfor i := 0; i &lt; n; i++ {\n\t\tout[keys[i]] = vals[i]\n\t}\n\treturn out\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>keys=[\"a\",\"b\"] vals=[1,2,3]: n=2 -&gt; a:1,b:2; vals[2] ignored.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Mismatched lengths: iterate <code>min</code>, don't assume equal.</li>\n<li>Duplicate keys: later pairs overwrite earlier ones.</li>\n<li><code>min</code> is a builtin in Go 1.21+.</li>\n</ul>",
    "starter": "// Package zip pairs two slices into a map.\npackage zip\n\n// Zip builds a map from keys[i] to vals[i] for the overlapping prefix (min\n// length). Extra elements of the longer slice are ignored.\n//\n// TODO(candidate): iterate to the shorter length.\nfunc Zip(keys []string, vals []int) map[string]int {\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "zip.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/03-composite-types/senior/appendcapshared": {
    "id": "01-language-basics/03-composite-types/senior/appendcapshared",
    "title": "Append Clobbers via Shared Capacity",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Append Clobbers via Shared Capacity</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p><code>xs[:2]</code> shares <code>xs</code>'s backing array <strong>and its spare capacity</strong>. Appending to it</p>\n<p>writes into <code>xs[2]</code>, corrupting the source. A full-slice expression caps the</p>\n<p>sub-slice so append must reallocate.</p>\n<h2>Task</h2>\n<p>Fix the sub-slice between the markers in</p>\n<p>appendcapshared.go to not share spare capacity.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  xs=[1,2,3], extra=9\nOutput: [1,2,9]\n</code></pre>\n<p>_Explanation:_ head is capped to len 2, so append allocates a new array; xs stays [1,2,3].</p>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  xs=[5,6,7,8], extra=0\nOutput: [5,6,0]\n</code></pre>\n<p>_Explanation:_ xs[2] must remain 7 afterwards.</p>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  xs=[1,2], extra=3\nOutput: [1,2,3]\n</code></pre>\n<p>_Explanation:_ No spare capacity so append reallocates anyway.</p>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Sub-slice capacity</span><span class=\"tchip\">Full-slice expr</span><span class=\"tchip\">Force realloc</span></div></details>",
    "education": "<h1>The three-index slice expression</h1>\n<h2>Intuition</h2>\n<p><code>xs[:2]</code> has length 2 but inherits <code>xs</code>'s <strong>capacity</strong>, so <code>append</code> writes into</p>\n<p>the shared tail (<code>xs[2]</code>). The three-index form <code>xs[low:high:max]</code> caps capacity,</p>\n<p>forcing <code>append</code> to allocate a fresh array:</p>\n<pre class=\"md\"><code>\nhead := xs[:2:2] // len 2, cap 2 -&gt; append reallocates\nreturn append(head, extra)\n</code></pre>\n<h2>Approach</h2>\n<p>1. Bug: head := xs[:2] shares xs's backing array AND its spare capacity, so append(head, extra) writes into xs[2] instead of a fresh array. 2. Fix: use a full slice expression head := xs[:2:2] which caps capacity at 2. 3. Now append sees no spare capacity and allocates a new backing array, leaving xs untouched.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc FirstTwoPlus(xs []int, extra int) []int {\n\thead := xs[:2:2]\n\treturn append(head, extra)\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>With xs=[1,2,3]: xs[:2] has len 2 cap 3. append writes extra at index 2 of the shared array -&gt; xs becomes [1,2,9]. With xs[:2:2] cap is 2, append must grow -&gt; new array [1,2,9], xs stays [1,2,3].</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Capacity, not length, decides whether append reuses memory.</li>\n<li>The bug only manifests when spare capacity exists \u2014 easy to miss in tests.</li>\n<li><code>slices.Clip</code> sets cap==len for the same effect.</li>\n</ul>",
    "starter": "// Package appendcapshared derives a prefix then appends to it. A planted bug\n// lets the append clobber the source's tail because they share capacity.\npackage appendcapshared\n\n// FirstTwoPlus returns the first two elements of xs followed by extra, WITHOUT\n// modifying xs. (xs has spare capacity, so a naive sub-slice append overwrites\n// xs[2].)\nfunc FirstTwoPlus(xs []int, extra int) []int {\n\t// CHANGE CODE BELOW THIS LINE\n\thead := xs[:2]\n\t// CHANGE CODE ABOVE THIS LINE\n\treturn append(head, extra)\n}\n",
    "debug": "",
    "file": "appendcapshared.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/03-composite-types/senior/appendnotcaptured": {
    "id": "01-language-basics/03-composite-types/senior/appendnotcaptured",
    "title": "append Result Ignored",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">append Result Ignored</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p><code>append</code> returns a new slice header (length grown, maybe relocated). The code</p>\n<p>discards that return (<code>_ = append(...)</code>), so <code>out</code> never grows.</p>\n<h2>Task</h2>\n<p>Fix the line between the markers in</p>\n<p>appendnotcaptured.go to capture <code>append</code>'s result.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  [1,2,3]\nOutput: [2,4,6]\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  []\nOutput: []\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  [-1,0,5]\nOutput: [-2,0,10]\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">append returns</span><span class=\"tchip\">Header, not array</span><span class=\"tchip\">Assign back</span></div></details>",
    "education": "<h1>append returns a new header</h1>\n<h2>Intuition</h2>\n<p>A slice header carries length and capacity by value. <code>append</code> may write into the</p>\n<p>existing backing array, but it always returns an updated header \u2014 you must assign</p>\n<p>it back:</p>\n<pre class=\"md\"><code>\nout = append(out, x*2)\n</code></pre>\n<h2>Approach</h2>\n<p>1. Bug: _ = append(out, x<em>2) discards append's return value, so out never grows. 2. append may return a new slice header (new pointer/len/cap) that must be reassigned. 3. Fix: out = append(out, x</em>2).</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Doubled(xs []int) []int {\n\tout := make([]int, 0, len(xs))\n\tfor _, x := range xs {\n\t\tout = append(out, x*2)\n\t}\n\treturn out\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>out starts len 0 cap 3. Each iteration append returns a header with len+1; without reassignment out.len stays 0, so final out is empty. Reassigning captures the growing length -&gt; [2,4,6].</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Always <code>s = append(s, ...)</code>.</li>\n<li><code>append</code> may reallocate; the old header is stale afterward.</li>\n<li>The compiler doesn't require using the result (it's just a value), so this</li>\n</ul>\n<p>  compiles.</p>",
    "starter": "// Package appendnotcaptured builds a slice. A planted bug ignores append's\n// return value.\npackage appendnotcaptured\n\n// Doubled returns a slice with each element of xs doubled.\nfunc Doubled(xs []int) []int {\n\tout := make([]int, 0, len(xs))\n\tfor _, x := range xs {\n\t\t// CHANGE CODE BELOW THIS LINE\n\t\t_ = append(out, x*2)\n\t\t// CHANGE CODE ABOVE THIS LINE\n\t}\n\treturn out\n}\n",
    "debug": "",
    "file": "appendnotcaptured.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/03-composite-types/senior/arrayvalue": {
    "id": "01-language-basics/03-composite-types/senior/arrayvalue",
    "title": "Arrays Are Values",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Arrays Are Values</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p><code>b := *a</code> copies the whole array (arrays are values). Doubling <code>b</code> mutates the</p>\n<p>copy; the caller's array via <code>a</code> is never touched.</p>\n<h2>Task</h2>\n<p>Fix the body between the markers in arrayvalue.go to mutate</p>\n<p>through the pointer.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  [1,2,3]\nOutput: [2,4,6]\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  [0,0,0]\nOutput: [0,0,0]\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  [-1,5,10]\nOutput: [-2,10,20]\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Array vs slice</span><span class=\"tchip\">Deref to mutate</span><span class=\"tchip\">Auto-index</span></div></details>",
    "education": "<h1>Arrays are value types</h1>\n<h2>Intuition</h2>\n<p>A Go array is a value: <code>b := *a</code> copies <strong>all</strong> elements. Mutating the copy</p>\n<p>leaves the original array (reachable via the pointer) unchanged. Mutate through</p>\n<p>the pointer directly:</p>\n<pre class=\"md\"><code>\nfor i := range a { a[i] *= 2 } // a is *[3]int; indexing auto-derefs\n</code></pre>\n<h2>Approach</h2>\n<p>1. Bug: b := <em>a dereferences and copies the whole array by value; mutating b never touches the caller's array. 2. Fix: b := a keeps the pointer (</em>[3]int). 3. Ranging over a pointer-to-array and b[i] *= 2 auto-dereferences, mutating the original in place.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Double(a *[3]int) {\n\tb := a\n\tfor i := range b {\n\t\tb[i] *= 2\n\t}\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><em>a copies [1,2,3] into b; doubling b leaves a=[1,2,3]. With b := a, b aliases a; b[i]</em>=2 writes through the pointer -&gt; a=[2,4,6].</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code><em>a</code> (or <code>b := </em>a</code>) copies the array; index <code>a[i]</code> to mutate in place.</li>\n<li>Large arrays copied by value are costly \u2014 pass pointers or use slices.</li>\n<li><code>[3]int</code> and <code>[4]int</code> are distinct types.</li>\n</ul>",
    "starter": "// Package arrayvalue doubles every element of a fixed array through a pointer.\n// A planted bug works on a copy of the array, so the caller's array is untouched.\npackage arrayvalue\n\n// Double doubles each element of the pointed-to 3-element array in place.\nfunc Double(a *[3]int) {\n\t// CHANGE CODE BELOW THIS LINE\n\tb := *a\n\tfor i := range b {\n\t\tb[i] *= 2\n\t}\n\t// CHANGE CODE ABOVE THIS LINE\n}\n",
    "debug": "",
    "file": "arrayvalue.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/03-composite-types/senior/commaokzero": {
    "id": "01-language-basics/03-composite-types/senior/commaokzero",
    "title": "Zero vs Missing",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Zero vs Missing</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p><code>GetOr</code> checks <code>v != 0</code> to decide presence, so a key stored with value <code>0</code></p>\n<p>wrongly returns the default. Only the comma-ok form distinguishes them.</p>\n<h2>Task</h2>\n<p>Fix the presence check between the markers in</p>\n<p>commaokzero.go to use comma-ok.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  m={a:0}, key=a, def=7\nOutput: 0\n</code></pre>\n<p>_Explanation:_ Key present with value 0 -&gt; return stored 0, not default.</p>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  m={a:5}, key=b, def=7\nOutput: 7\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  m={}, key=x, def=-1\nOutput: -1\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Comma-ok</span><span class=\"tchip\">Zero \u2260 absent</span><span class=\"tchip\">Presence flag</span></div></details>",
    "education": "<h1>Presence, not value</h1>\n<h2>Intuition</h2>\n<p>Indexing a map returns the zero value for a missing key, so testing <code>v != 0</code></p>\n<p>conflates \"absent\" with \"present but zero\". The comma-ok form separates them:</p>\n<pre class=\"md\"><code>\nif v, ok := m[key]; ok { return v }\nreturn def\n</code></pre>\n<h2>Approach</h2>\n<p>1. Bug: if v := m[key]; v != 0 treats a stored zero as missing, returning def. 2. A missing key and a stored 0 both yield v==0, so value alone cannot distinguish them. 3. Fix: use comma-ok: if v, ok := m[key]; ok { return v }.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc GetOr(m map[string]int, key string, def int) int {\n\tif v, ok := m[key]; ok {\n\t\treturn v\n\t}\n\treturn def\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>For m={a:0}, key=a: m[key] gives (0,true). Old code sees v==0 and falls through to def=7 (wrong). comma-ok sees ok=true and returns 0.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Branch on <code>ok</code>, never on the value, to detect presence.</li>\n<li>The same trap exists for <code>false</code> and <code>\"\"</code> values.</li>\n<li>Reading a nil map returns <code>(zero, false)</code> \u2014 safe.</li>\n</ul>",
    "starter": "// Package commaokzero looks up config with a default.\n// A planted bug treats a stored zero as \"missing\".\npackage commaokzero\n\n// GetOr returns m[key] if the key is present, else def.\nfunc GetOr(m map[string]int, key string, def int) int {\n\t// CHANGE CODE BELOW THIS LINE\n\tif v := m[key]; v != 0 {\n\t\treturn v\n\t}\n\t// CHANGE CODE ABOVE THIS LINE\n\treturn def\n}\n",
    "debug": "",
    "file": "commaokzero.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/03-composite-types/senior/copyemptydst": {
    "id": "01-language-basics/03-composite-types/senior/copyemptydst",
    "title": "copy Into a Length-0 Slice",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">copy Into a Length-0 Slice</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p><code>copy</code> copies <code>min(len(dst), len(src))</code> elements. The destination is made with</p>\n<p><strong>length 0</strong> (only capacity), so <code>copy</code> copies nothing and <code>Clone</code> returns empty.</p>\n<h2>Task</h2>\n<p>Fix the <code>make</code> between the markers in copyemptydst.go to give</p>\n<p>the destination the right length.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  [1,2,3]\nOutput: [1,2,3]\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  []\nOutput: []\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  [9]\nOutput: [9]\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">copy semantics</span><span class=\"tchip\">len vs cap</span><span class=\"tchip\">Size dst</span></div></details>",
    "education": "<h1>copy respects length, not capacity</h1>\n<h2>Intuition</h2>\n<p><code>copy(dst, src)</code> copies <code>min(len(dst), len(src))</code> elements. A slice made with</p>\n<p><code>make([]int, 0, n)</code> has <strong>length 0</strong>, so <code>copy</code> writes nothing \u2014 capacity is</p>\n<p>irrelevant:</p>\n<pre class=\"md\"><code>\ndst := make([]int, len(xs)) // length = len(xs)\ncopy(dst, xs)\n</code></pre>\n<h2>Approach</h2>\n<p>1. Bug: make([]int, 0, len(xs)) gives a destination of length 0; copy copies min(len(dst),len(src)) = 0 elements. 2. copy is bounded by the shorter length, not capacity. 3. Fix: make([]int, len(xs)) so dst has full length and copy fills it.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Clone(xs []int) []int {\n\tdst := make([]int, len(xs))\n\tcopy(dst, xs)\n\treturn dst\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>dst len 0, copy(dst,xs) returns 0, dst stays empty. With len(xs) length, copy transfers all elements -&gt; independent clone.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>To pre-allocate for <code>append</code>, use length 0 + capacity; to <code>copy</code>, use full</li>\n</ul>\n<p>  length.</p>\n<ul>\n<li><code>copy</code> returns the number copied \u2014 useful to assert.</li>\n<li><code>slices.Clone</code> avoids the footgun entirely.</li>\n</ul>",
    "starter": "// Package copyemptydst clones a slice. A planted bug sizes the destination\n// with length 0, so copy copies nothing.\npackage copyemptydst\n\n// Clone returns an independent copy of xs.\nfunc Clone(xs []int) []int {\n\t// CHANGE CODE BELOW THIS LINE\n\tdst := make([]int, 0, len(xs))\n\t// CHANGE CODE ABOVE THIS LINE\n\tcopy(dst, xs)\n\treturn dst\n}\n",
    "debug": "",
    "file": "copyemptydst.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/03-composite-types/senior/deduporder": {
    "id": "01-language-basics/03-composite-types/senior/deduporder",
    "title": "Dedupe Preserving Order",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Dedupe Preserving Order</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p>The condition is inverted: it appends only when the value was <strong>already seen</strong>, so</p>\n<p>the output is exactly the duplicates.</p>\n<h2>Task</h2>\n<p>Fix the condition between the markers in deduporder.go to keep</p>\n<p>first occurrences.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  [1,1,2,3,3]\nOutput: [1,2,3]\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  [5,5,5]\nOutput: [5]\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  [1,2,3]\nOutput: [1,2,3]\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Seen set</span><span class=\"tchip\">First occurrence</span><span class=\"tchip\">Order preserved</span></div></details>",
    "education": "<h1>Order-preserving dedup</h1>\n<h2>Intuition</h2>\n<p>Keep a set of already-emitted values; emit an element the <strong>first</strong> time you see</p>\n<p>it (when it is NOT in the set):</p>\n<pre class=\"md\"><code>\nif _, ok := seen[x]; !ok {\n\tout = append(out, x); seen[x] = struct{}{}\n}\n</code></pre>\n<h2>Approach</h2>\n<p>1. Bug: if ok { appends only when the value was ALREADY seen, inverting the logic (and it never records first sightings correctly). 2. Fix: if !ok { so we append on first occurrence and then mark it seen. 3. The seen map preserves first-seen order via the single append per new value.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Unique(xs []int) []int {\n\tseen := make(map[int]struct{})\n\tout := []int{}\n\tfor _, x := range xs {\n\t\t_, ok := seen[x]\n\t\tif !ok {\n\t\t\tout = append(out, x)\n\t\t\tseen[x] = struct{}{}\n\t\t}\n\t}\n\treturn out\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>[1,1,2]: x=1 ok=false -&gt; append 1, mark. x=1 ok=true -&gt; skip. x=2 ok=false -&gt; append 2. Result [1,2]. With the bug, first 1 (ok=false) is skipped, breaking output.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Emit on absence, then record; recording first would suppress everything.</li>\n<li>The map gives O(1) membership; a slice scan would be O(n\u00b2).</li>\n<li><code>slices.Compact</code> only removes <em>consecutive</em> dups \u2014 different semantics.</li>\n</ul>",
    "starter": "// Package deduporder removes duplicates preserving first-seen order. A planted\n// bug appends when the value was already seen.\npackage deduporder\n\n// Unique returns xs with duplicates removed, keeping the first occurrence order.\nfunc Unique(xs []int) []int {\n\tseen := make(map[int]struct{})\n\tout := []int{}\n\tfor _, x := range xs {\n\t\t_, ok := seen[x]\n\t\t// CHANGE CODE BELOW THIS LINE\n\t\tif ok {\n\t\t\t// CHANGE CODE ABOVE THIS LINE\n\t\t\tout = append(out, x)\n\t\t\tseen[x] = struct{}{}\n\t\t}\n\t}\n\treturn out\n}\n",
    "debug": "",
    "file": "deduporder.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/03-composite-types/senior/embeddingshadow": {
    "id": "01-language-basics/03-composite-types/senior/embeddingshadow",
    "title": "Embedded Field Shadowing",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Embedded Field Shadowing</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p><code>Entity</code> embeds <code>Base</code> (which has <code>ID</code>) but also declares its own <code>ID</code>. The outer</p>\n<p><code>ID</code> <strong>shadows</strong> the promoted one, so <code>e.ID</code> is the wrong field.</p>\n<h2>Task</h2>\n<p>Fix the return between the markers in</p>\n<p>embeddingshadow.go to read the embedded <code>Base.ID</code>.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Entity{Base:{ID:7}, ID:99}\nOutput: 7\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Entity{Base:{ID:1}, ID:0}\nOutput: 1\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Entity{Base:{ID:-3}, ID:5}\nOutput: -3\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Embedding</span><span class=\"tchip\">Shadowing</span><span class=\"tchip\">Qualify</span></div></details>",
    "education": "<h1>Promotion and shadowing</h1>\n<h2>Intuition</h2>\n<p>Embedding promotes a field: <code>e.ID</code> normally means <code>e.Base.ID</code>. But if the outer</p>\n<p>struct declares its <strong>own</strong> <code>ID</code>, that one wins \u2014 the promoted field is shadowed</p>\n<p>and must be qualified:</p>\n<pre class=\"md\"><code>\nreturn e.Base.ID // reach the embedded field explicitly\n</code></pre>\n<h2>Approach</h2>\n<p>1. Bug: e.ID resolves to the outer shadowing field, not the embedded Base.ID. 2. When an outer struct declares a field with the same name as an embedded field, the outer one wins for the unqualified selector. 3. Fix: qualify the path: return e.Base.ID.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\ntype Base struct {\n\tID int\n}\n\ntype Entity struct {\n\tBase\n\tID int // shadows Base.ID\n}\n\nfunc BaseID(e Entity) int {\n\treturn e.Base.ID\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>Entity{Base:{ID:7}, ID:99}: e.ID -&gt; 99 (outer). e.Base.ID -&gt; 7 (embedded). Task wants the embedded value, so use e.Base.ID.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>A shallower field shadows a deeper (promoted) one.</li>\n<li>Ambiguous promotions (two embeds with the same field) are a compile error until</li>\n</ul>\n<p>  qualified.</p>\n<ul>\n<li>Methods promote and shadow by the same rules.</li>\n</ul>",
    "starter": "// Package embeddingshadow uses struct embedding. A planted bug reads a shadowing\n// outer field instead of the embedded one.\npackage embeddingshadow\n\n// Base carries an ID.\ntype Base struct {\n\tID int\n}\n\n// Entity embeds Base but also (mistakenly) declares its own ID that shadows it.\ntype Entity struct {\n\tBase\n\tID int // shadows Base.ID\n}\n\n// BaseID must return the embedded Base's ID (set via the Base field), not the\n// outer shadow.\nfunc BaseID(e Entity) int {\n\t// CHANGE CODE BELOW THIS LINE\n\treturn e.ID\n\t// CHANGE CODE ABOVE THIS LINE\n}\n",
    "debug": "",
    "file": "embeddingshadow.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/03-composite-types/senior/emptystructset": {
    "id": "01-language-basics/03-composite-types/senior/emptystructset",
    "title": "Set Membership Value",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Set Membership Value</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p>The set uses <code>map[int]bool</code>, but members are stored as <code>false</code>. Membership is</p>\n<p>tested with <code>inB[x]</code> (the value), which is always false \u2014 so the intersection is</p>\n<p>empty.</p>\n<h2>Task</h2>\n<p>Fix the store between the markers in</p>\n<p>emptystructset.go so membership is true.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  a=[1,2,3], b=[2,3,4]\nOutput: [2,3]\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  a=[1,1,2], b=[1]\nOutput: [1]\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  a=[1,2], b=[3,4]\nOutput: []\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Bool set</span><span class=\"tchip\">Value vs presence</span><span class=\"tchip\">Alternative</span></div></details>",
    "education": "<h1>Bool sets store true</h1>\n<h2>Intuition</h2>\n<p>When a <code>map[T]bool</code> is used as a set and membership is tested by the value</p>\n<p>(<code>if inB[x]</code>), members must be stored as <strong>true</strong>:</p>\n<pre class=\"md\"><code>\ninB[x] = true\n</code></pre>\n<p>Storing <code>false</code> makes every lookup report \"absent\".</p>\n<h2>Approach</h2>\n<p>1. Bug: inB[x] = false stores false for every b element; then inB[x] reads false and membership always fails. 2. Presence in a map and the stored value are different; here the code relies on the value being true. 3. Fix: inB[x] = true.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Intersect(a, b []int) []int {\n\tinB := make(map[int]bool)\n\tfor _, x := range b {\n\t\tinB[x] = true\n\t}\n\tout := []int{}\n\tseen := make(map[int]bool)\n\tfor _, x := range a {\n\t\tif inB[x] &amp;&amp; !seen[x] {\n\t\t\tout = append(out, x)\n\t\t\tseen[x] = true\n\t\t}\n\t}\n\treturn out\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>b=[2,3,4] populates inB with 2,3,4 -&gt; true. For a's 2 and 3, inB[x]=true and unseen -&gt; appended. With false stored, the &amp;&amp;-guard is always false, output empty.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>With value-based tests, the stored bool must be <code>true</code>.</li>\n<li><code>map[T]struct{}</code> uses no value memory and tests via <code>ok</code>.</li>\n<li>Reading a missing key gives <code>false</code> \u2014 indistinguishable from a stored <code>false</code>.</li>\n</ul>",
    "starter": "// Package emptystructset builds a set. A planted bug tests membership by the\n// stored bool value instead of presence, but stores false.\npackage emptystructset\n\n// Intersect returns values present in both a and b (unique, order of a).\nfunc Intersect(a, b []int) []int {\n\tinB := make(map[int]bool)\n\tfor _, x := range b {\n\t\t// CHANGE CODE BELOW THIS LINE\n\t\tinB[x] = false\n\t\t// CHANGE CODE ABOVE THIS LINE\n\t}\n\tout := []int{}\n\tseen := make(map[int]bool)\n\tfor _, x := range a {\n\t\tif inB[x] && !seen[x] {\n\t\t\tout = append(out, x)\n\t\t\tseen[x] = true\n\t\t}\n\t}\n\treturn out\n}\n",
    "debug": "",
    "file": "emptystructset.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/03-composite-types/senior/filterreuse": {
    "id": "01-language-basics/03-composite-types/senior/filterreuse",
    "title": "Filter Corrupts Input",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Filter Corrupts Input</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p><code>out := xs[:0]</code> reuses <code>xs</code>'s backing array. Appending the kept elements</p>\n<p>overwrites <code>xs</code> from the front, so the input is corrupted mid-iteration.</p>\n<h2>Task</h2>\n<p>Fix the initialization between the markers in</p>\n<p>filterreuse.go to use a fresh slice.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  [1,2,3,4]\nOutput: [2,4]\n</code></pre>\n<p>_Explanation:_ input must stay [1,2,3,4].</p>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  [2,4,6]\nOutput: [2,4,6]\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  [1,3,5]\nOutput: []\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">xs[:0] aliases</span><span class=\"tchip\">In-place filter</span><span class=\"tchip\">Fresh slice</span></div></details>",
    "education": "<h1>In-place filtering aliases the input</h1>\n<h2>Intuition</h2>\n<p><code>xs[:0]</code> is a length-0 slice over <code>xs</code>'s <strong>same</strong> backing array. Appending to it</p>\n<p>writes over <code>xs[0], xs[1], ...</code>. That's the deliberate \"filter in place\" trick \u2014</p>\n<p>but only safe when you <em>want</em> to overwrite the input:</p>\n<pre class=\"md\"><code>\nout := []int{} // independent result; input untouched\n</code></pre>\n<h2>Approach</h2>\n<p>1. Bug: out := xs[:0] reuses xs's backing array; append(out, x) overwrites xs's leading elements as it filters, corrupting the input. 2. Fix: allocate a fresh slice out := []int{} (or make with cap). 3. Appending to an independent slice leaves xs intact.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Evens(xs []int) []int {\n\tout := []int{}\n\tfor _, x := range xs {\n\t\tif x%2 == 0 {\n\t\t\tout = append(out, x)\n\t\t}\n\t}\n\treturn out\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>xs=[1,2,3,4], out=xs[:0]. append 2 writes xs[0]=2, append 4 writes xs[1]=4 -&gt; xs corrupted to [2,4,3,4]. A fresh out collects [2,4] without touching xs.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>xs[:0]</code> shares memory; a fresh slice does not.</li>\n<li>The in-place version (used by <code>slices.DeleteFunc</code>) mutates and returns the</li>\n</ul>\n<p>  input.</p>\n<ul>\n<li>Reading <code>xs</code> after an in-place filter over it is undefined-ish \u2014 the front is</li>\n</ul>\n<p>  overwritten.</p>",
    "starter": "// Package filterreuse returns even numbers without modifying the input. A\n// planted bug filters in place over the same backing array, corrupting xs.\npackage filterreuse\n\n// Evens returns a new slice of the even elements of xs. xs must not be modified.\nfunc Evens(xs []int) []int {\n\t// CHANGE CODE BELOW THIS LINE\n\tout := xs[:0]\n\t// CHANGE CODE ABOVE THIS LINE\n\tfor _, x := range xs {\n\t\tif x%2 == 0 {\n\t\t\tout = append(out, x)\n\t\t}\n\t}\n\treturn out\n}\n",
    "debug": "",
    "file": "filterreuse.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/03-composite-types/senior/grid2dalias": {
    "id": "01-language-basics/03-composite-types/senior/grid2dalias",
    "title": "Shared Grid Rows",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Shared Grid Rows</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p><code>New</code> allocates <strong>one</strong> row slice and assigns it to every row, so all rows share</p>\n<p>the same backing array \u2014 writing <code>g[0][0]</code> changes every row.</p>\n<h2>Task</h2>\n<p>Fix the loop between the markers in grid2dalias.go to allocate</p>\n<p>a fresh row per line.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  rows=2, cols=2\nOutput: [[0,0],[0,0]] with independent rows\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  rows=3, cols=1\nOutput: [[0],[0],[0]]\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  rows=1, cols=3\nOutput: [[0,0,0]]\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Slices are references</span><span class=\"tchip\">Per-row make</span><span class=\"tchip\">2-D construction</span></div></details>",
    "education": "<h1>Independent rows in a 2-D grid</h1>\n<h2>Intuition</h2>\n<p>A slice value is a header pointing at a backing array. Assigning the <em>same</em> slice</p>\n<p>to every row makes them aliases. Allocate a new row each iteration:</p>\n<pre class=\"md\"><code>\nfor i := range grid { grid[i] = make([]int, cols) }\n</code></pre>\n<h2>Approach</h2>\n<p>1. Bug: one row slice is made once and assigned to every grid[i], so all rows alias the same backing array. 2. Writing grid[0][0] then shows up in every row. 3. Fix: allocate a fresh make([]int, cols) inside the loop per row.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc New(rows, cols int) [][]int {\n\tgrid := make([][]int, rows)\n\tfor i := range grid {\n\t\tgrid[i] = make([]int, cols)\n\t}\n\treturn grid\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>With the bug, grid[0] and grid[1] point at the same array; grid[0][0]=9 makes grid[1][0]==9 too. Per-row make gives distinct arrays so writes are isolated.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>One <code>make</code> outside the loop = shared row; one <code>make</code> per iteration = independent.</li>\n<li>The same trap hits maps of slices reused across keys.</li>\n<li>Ragged grids need per-row sizing anyway.</li>\n</ul>",
    "starter": "// Package grid2dalias builds a 2-D grid. A planted bug makes all rows share one\n// backing slice.\npackage grid2dalias\n\n// New builds a rows x cols grid of zeros. Writing one cell must not affect\n// others in different rows.\nfunc New(rows, cols int) [][]int {\n\tgrid := make([][]int, rows)\n\t// CHANGE CODE BELOW THIS LINE\n\trow := make([]int, cols)\n\tfor i := range grid {\n\t\tgrid[i] = row\n\t}\n\t// CHANGE CODE ABOVE THIS LINE\n\treturn grid\n}\n",
    "debug": "",
    "file": "grid2dalias.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/03-composite-types/senior/jsontag": {
    "id": "01-language-basics/03-composite-types/senior/jsontag",
    "title": "JSON Tag Mismatch",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">JSON Tag Mismatch</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p>The API contract is snake_case. <code>LastName</code> is tagged <code>lastName</code>, so the JSON key</p>\n<p>is wrong and clients break.</p>\n<h2>Task</h2>\n<p>Fix the struct tag between the markers in jsontag.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  User{First:Ada, Last:Lovelace}\nOutput: {\"first_name\":\"Ada\",\"last_name\":\"Lovelace\"}\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  User{First:Grace, Last:Hopper}\nOutput: {\"first_name\":\"Grace\",\"last_name\":\"Hopper\"}\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  User{}\nOutput: {\"first_name\":\"\",\"last_name\":\"\"}\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Struct tags</span><span class=\"tchip\">Exact key</span><span class=\"tchip\">encoding/json</span></div></details>",
    "education": "<h1>Struct tags and JSON</h1>\n<h2>Intuition</h2>\n<p>A struct tag is metadata read by libraries via reflection. <code>encoding/json</code> uses</p>\n<p>the <code>json:\"...\"</code> tag as the exact output key:</p>\n<pre class=\"md\"><code>\nLastName string `json:\"last_name\"`\n</code></pre>\n<h2>Approach</h2>\n<p>1. Bug: the struct tag json:\"lastName\" emits camelCase, but the required key is snake_case last_name. 2. json.Marshal reads the tag verbatim for the output key. 3. Fix: change the tag to json:\"last_name\".</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nimport \"encoding/json\"\n\ntype User struct {\n\tFirstName string `json:\"first_name\"`\n\tLastName string `json:\"last_name\"`\n}\n\nfunc Marshal(u User) (string, error) {\n\tb, err := json.Marshal(u)\n\treturn string(b), err\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>Marshaling produces \"lastName\":... with the buggy tag, failing the expected snake_case output. Correcting the tag yields \"last_name\":....</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>The tag string is literal; casing and spelling matter.</li>\n<li>Only <strong>exported</strong> (capitalized) fields are marshaled.</li>\n<li>Options like <code>,omitempty</code> follow the name: <code>json:\"x,omitempty\"</code>.</li>\n</ul>",
    "starter": "// Package jsontag serializes a user to JSON. A planted bug has the wrong tag, so\n// the field name is wrong in output.\npackage jsontag\n\nimport \"encoding/json\"\n\n// User is serialized to JSON with snake_case keys.\ntype User struct {\n\tFirstName string `json:\"first_name\"`\n\t// CHANGE CODE BELOW THIS LINE\n\tLastName string `json:\"lastName\"`\n\t// CHANGE CODE ABOVE THIS LINE\n}\n\n// Marshal returns the JSON encoding of u.\nfunc Marshal(u User) (string, error) {\n\tb, err := json.Marshal(u)\n\treturn string(b), err\n}\n",
    "debug": "",
    "file": "jsontag.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/03-composite-types/senior/mapstructupdate": {
    "id": "01-language-basics/03-composite-types/senior/mapstructupdate",
    "title": "Update Struct in Map",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Update Struct in Map</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p><code>m[key]</code> returns a <strong>copy</strong> of the struct. Incrementing <code>s.Hits</code> changes the copy;</p>\n<p>without writing it back, the map keeps the old value. (Go even forbids</p>\n<p><code>m[key].Hits++</code> directly.)</p>\n<h2>Task</h2>\n<p>Fix the line between the markers in</p>\n<p>mapstructupdate.go to store the updated struct.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Record(m,'a') once\nOutput: m['a'].Hits==1\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Record(m,'a') twice\nOutput: m['a'].Hits==2\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Record(m,'b') on empty m\nOutput: m['b'].Hits==1\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Map value copy</span><span class=\"tchip\">Read-modify-write</span><span class=\"tchip\">No <code>m[k].F++</code></span></div></details>",
    "education": "<h1>Map values are not addressable</h1>\n<h2>Intuition</h2>\n<p>A map returns a <strong>copy</strong> of its value, and map elements aren't addressable \u2014 so</p>\n<p><code>m[key].Hits++</code> won't even compile. The pattern is read-modify-write:</p>\n<pre class=\"md\"><code>\ns := m[key]\ns.Hits++\nm[key] = s\n</code></pre>\n<h2>Approach</h2>\n<p>1. Bug: s := m[key] copies the struct value; s.Hits++ mutates the copy and _ = s throws it away, so the map is unchanged. 2. Map values are not addressable, so you must write the modified struct back. 3. Fix: m[key] = s.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\ntype Stat struct {\n\tHits int\n}\n\nfunc Record(m map[string]Stat, key string) {\n\ts := m[key]\n\ts.Hits++\n\tm[key] = s\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>s=m[key] copies {Hits:0}; s.Hits++ -&gt; {Hits:1} in the copy; without write-back m[key] stays {Hits:0}. m[key]=s stores the incremented struct.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>m[k]</code> yields a copy; mutating it doesn't touch the map.</li>\n<li><code>m[k].Field = x</code> and <code>m[k].Field++</code> are compile errors (unaddressable).</li>\n<li>Pointer values (<code>map[K]*V</code>) are mutable in place.</li>\n</ul>",
    "starter": "// Package mapstructupdate updates a struct stored in a map. A planted bug mutates\n// a copy and never writes it back.\npackage mapstructupdate\n\n// Stat tracks hits.\ntype Stat struct {\n\tHits int\n}\n\n// Record increments the Hits of key in m (creating it if absent).\nfunc Record(m map[string]Stat, key string) {\n\ts := m[key]\n\ts.Hits++\n\t// CHANGE CODE BELOW THIS LINE\n\t_ = s\n\t// CHANGE CODE ABOVE THIS LINE\n}\n",
    "debug": "",
    "file": "mapstructupdate.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/03-composite-types/senior/mergedirection": {
    "id": "01-language-basics/03-composite-types/senior/mergedirection",
    "title": "Merge Override Order",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Merge Override Order</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p>Config layering: overrides must win over the base. The copies run base-last, so</p>\n<p>base overwrites the override on collisions.</p>\n<h2>Task</h2>\n<p>Fix the copy order between the markers in</p>\n<p>mergedirection.go so <code>over</code> wins.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  base={a:1,b:2}, over={b:9}\nOutput: {a:1,b:9}\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  base={x:1}, over={y:2}\nOutput: {x:1,y:2}\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  base={k:1}, over={k:1}\nOutput: {k:1}\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Last write wins</span><span class=\"tchip\">Layer order</span><span class=\"tchip\">Map copy</span></div></details>",
    "education": "<h1>Override order in a merge</h1>\n<h2>Intuition</h2>\n<p>When copying two maps into one, the <strong>last</strong> write to a key wins. For \"override</p>\n<p>on top of base\", copy the base first, then the overrides:</p>\n<pre class=\"md\"><code>\nfor k, v := range base { out[k] = v }\nfor k, v := range over { out[k] = v } // over wins\n</code></pre>\n<h2>Approach</h2>\n<p>1. Bug: over is copied first, then base overwrites it, so base wins on key collisions (wrong). 2. Later writes to the same key win in a map. 3. Fix: copy base first, then over on top, so over wins.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Merge(base, over map[string]int) map[string]int {\n\tout := make(map[string]int)\n\tfor k, v := range base {\n\t\tout[k] = v\n\t}\n\tfor k, v := range over {\n\t\tout[k] = v\n\t}\n\treturn out\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>base={b:2}, over={b:9}. Buggy order: out[b]=9 then out[b]=2 -&gt; 2. Correct order: out[b]=2 then out[b]=9 -&gt; 9.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Order of the two loops is the whole behavior.</li>\n<li>Within one map, iteration order is random but irrelevant here.</li>\n<li>Document precedence explicitly; it's easy to get backwards.</li>\n</ul>",
    "starter": "// Package mergedirection merges override config over base. A planted bug copies\n// in the wrong order, so base wins instead of the override.\npackage mergedirection\n\n// Merge returns base with over applied on top (over wins on collisions).\nfunc Merge(base, over map[string]int) map[string]int {\n\tout := make(map[string]int)\n\t// CHANGE CODE BELOW THIS LINE\n\tfor k, v := range over {\n\t\tout[k] = v\n\t}\n\tfor k, v := range base {\n\t\tout[k] = v\n\t}\n\t// CHANGE CODE ABOVE THIS LINE\n\treturn out\n}\n",
    "debug": "",
    "file": "mergedirection.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/03-composite-types/senior/nestedmapinit": {
    "id": "01-language-basics/03-composite-types/senior/nestedmapinit",
    "title": "Initialize the Inner Map",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Initialize the Inner Map</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p><code>m[o][i]++</code> writes into the inner map <code>m[o]</code>, but a missing outer key yields a</p>\n<p><strong>nil</strong> inner map \u2014 and writing to it panics. You must create the inner map first.</p>\n<h2>Task</h2>\n<p>Add the inner-map initialization between the markers in</p>\n<p>nestedmapinit.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  [[x,a],[x,a],[x,b]]\nOutput: {x:{a:2,b:1}}\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  [[p,q]]\nOutput: {p:{q:1}}\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  []\nOutput: {}\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Nested maps</span><span class=\"tchip\">Nil inner map</span><span class=\"tchip\">Lazy init</span></div></details>",
    "education": "<h1>Nested maps need inner initialization</h1>\n<h2>Intuition</h2>\n<p><code>map[string]map[string]int</code> has an inner map per outer key. A missing outer key</p>\n<p>reads as a <strong>nil</strong> inner map; writing <code>m[o][i]++</code> to nil panics. Lazily create it:</p>\n<pre class=\"md\"><code>\nif m[o] == nil { m[o] = make(map[string]int) }\nm[o][i]++\n</code></pre>\n<h2>Approach</h2>\n<p>1. Bug: m[o][i]++ writes into m[o] which is a nil inner map when o is first seen -&gt; panic (assignment to entry in nil map). 2. Fix: lazily initialize the inner map: if m[o]==nil { m[o]=make(map[string]int) }. 3. Then the ++ into the inner map is safe.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Tally(pairs [][2]string) map[string]map[string]int {\n\tm := make(map[string]map[string]int)\n\tfor _, p := range pairs {\n\t\to, i := p[0], p[1]\n\t\tif m[o] == nil {\n\t\t\tm[o] = make(map[string]int)\n\t\t}\n\t\tm[o][i]++\n\t}\n\treturn m\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>First pair (x,a): m[x] is nil, m[x][a]++ panics. After adding the init guard, m[x] becomes an empty map, then m[x][a] increments to 1.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Reading <code>m[o][i]</code> is safe (nil inner returns zero); <strong>writing</strong> is not.</li>\n<li>The check-and-create must precede the write.</li>\n<li>Alternatives: a <code>map[[2]string]int</code> keyed by the pair avoids nesting.</li>\n</ul>",
    "starter": "// Package nestedmapinit counts pairs in a nested map. A planted bug writes to an\n// uninitialized inner map.\npackage nestedmapinit\n\n// Tally counts occurrences of (outer, inner) pairs into a nested map.\nfunc Tally(pairs [][2]string) map[string]map[string]int {\n\tm := make(map[string]map[string]int)\n\tfor _, p := range pairs {\n\t\to, i := p[0], p[1]\n\t\t// CHANGE CODE BELOW THIS LINE\n\t\t// (inner map may not exist yet)\n\t\t// CHANGE CODE ABOVE THIS LINE\n\t\tm[o][i]++\n\t}\n\treturn m\n}\n",
    "debug": "",
    "file": "nestedmapinit.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/03-composite-types/senior/nilmap": {
    "id": "01-language-basics/03-composite-types/senior/nilmap",
    "title": "Nil Map Write",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Nil Map Write</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p><code>Count</code> declares the map with <code>var m map[int]int</code>, which is <strong>nil</strong>. Reading a</p>\n<p>nil map is fine, but the first <code>m[x]++</code> (a write) panics.</p>\n<h2>Task</h2>\n<p>Fix the declaration between the markers in nilmap.go to allocate the</p>\n<p>map.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  [1,1,2]\nOutput: {1:2, 2:1}\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  []\nOutput: {}\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  [5,5,5]\nOutput: {5:3}\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Nil map</span><span class=\"tchip\">Read vs write</span><span class=\"tchip\">make</span></div></details>",
    "education": "<h1>Nil maps: read yes, write no</h1>\n<h2>Intuition</h2>\n<p>A <code>var m map[K]V</code> is nil. You can read it (missing keys return the zero value)</p>\n<p>and range it (zero iterations), but <strong>writing</strong> to it panics:</p>\n<pre class=\"md\"><code>\nm := make(map[int]int) // must allocate before m[x]++\n</code></pre>\n<h2>Approach</h2>\n<p>1. Bug: var m map[int]int leaves m nil; m[x]++ panics with assignment to entry in nil map. 2. A nil map is readable but not writable. 3. Fix: m := make(map[int]int).</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Count(xs []int) map[int]int {\n\tm := make(map[int]int)\n\tfor _, x := range xs {\n\t\tm[x]++\n\t}\n\treturn m\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>Ranging over [1,1,2] the first m[1]++ panics on a nil map. After make, m[1]++ -&gt; 1, again -&gt; 2, m[2]++ -&gt; 1.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Struct fields of map type are nil until you <code>make</code> them.</li>\n<li>Returning a nil map is fine if the caller only reads it \u2014 but tests/consumers</li>\n</ul>\n<p>  may expect a non-nil empty map.</p>\n<ul>\n<li>Reading and <code>range</code> never panic on nil; only writes do.</li>\n</ul>",
    "starter": "// Package nilmap tallies counts. A planted bug writes to a nil map.\npackage nilmap\n\n// Count returns a map from each value to its occurrence count.\nfunc Count(xs []int) map[int]int {\n\t// CHANGE CODE BELOW THIS LINE\n\tvar m map[int]int\n\t// CHANGE CODE ABOVE THIS LINE\n\tfor _, x := range xs {\n\t\tm[x]++\n\t}\n\treturn m\n}\n",
    "debug": "",
    "file": "nilmap.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/03-composite-types/senior/nilvsempty": {
    "id": "01-language-basics/03-composite-types/senior/nilvsempty",
    "title": "Nil vs Empty Slice",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Nil vs Empty Slice</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p>A <code>nil</code> slice and an empty slice both have length 0, but they encode differently:</p>\n<p><code>nil</code> marshals to JSON <code>null</code>, an empty slice to <code>[]</code>. The function returns nil</p>\n<p>when nothing matches, breaking the API contract.</p>\n<h2>Task</h2>\n<p>Fix the declaration between the markers in nilvsempty.go to start</p>\n<p>from a non-nil empty slice.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  [a,,b]\nOutput: [a,b]\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  [,,]\nOutput: [] (non-nil)\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  []\nOutput: [] (non-nil)\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Nil vs empty</span><span class=\"tchip\">JSON encoding</span><span class=\"tchip\">Init non-nil</span></div></details>",
    "education": "<h1>Nil and empty slices differ at the boundary</h1>\n<h2>Intuition</h2>\n<p><code>var out []string</code> is <strong>nil</strong>; <code>[]string{}</code> is non-nil with length 0. They behave</p>\n<p>identically for <code>len</code>, <code>range</code>, and <code>append</code> \u2014 but not for identity or</p>\n<p>serialization:</p>\n<pre class=\"md\"><code>\nout := []string{} // marshals to [], not null\n</code></pre>\n<h2>Approach</h2>\n<p>1. Bug: var out []string is nil; when nothing matches it stays nil and JSON-encodes as null instead of []. 2. Fix: initialize out := []string{} \u2014 a non-nil empty slice. 3. Appends work identically; the difference is the empty case returns [] not null.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc NonEmpty(in []string) []string {\n\tout := []string{}\n\tfor _, s := range in {\n\t\tif s != \"\" {\n\t\t\tout = append(out, s)\n\t\t}\n\t}\n\treturn out\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>Input with no non-empty strings: buggy out is nil (encodes null). With []string{} out is empty-but-non-nil (encodes []).</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>out == nil</code> is the only way to tell them apart in Go.</li>\n<li><code>append</code> promotes nil to non-nil, so the distinction only matters when nothing</li>\n</ul>\n<p>  is appended.</p>\n<ul>\n<li>Prefer <code>[]T{}</code> when the emptiness must be observable.</li>\n</ul>",
    "starter": "// Package nilvsempty filters a list and must return a non-nil empty slice when\n// nothing matches. A planted bug returns nil.\npackage nilvsempty\n\n// NonEmpty returns the non-empty strings of in. When none match, it must return\n// an empty, non-nil slice (so JSON encodes [] not null).\nfunc NonEmpty(in []string) []string {\n\t// CHANGE CODE BELOW THIS LINE\n\tvar out []string\n\t// CHANGE CODE ABOVE THIS LINE\n\tfor _, s := range in {\n\t\tif s != \"\" {\n\t\t\tout = append(out, s)\n\t\t}\n\t}\n\treturn out\n}\n",
    "debug": "",
    "file": "nilvsempty.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/03-composite-types/senior/preallocindex": {
    "id": "01-language-basics/03-composite-types/senior/preallocindex",
    "title": "Length vs Capacity for Indexing",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Length vs Capacity for Indexing</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p><code>make([]int, 0, len(xs))</code> has capacity but <strong>length 0</strong>, so <code>out[i] = ...</code> panics</p>\n<p>(index out of range). Either index into a full-length slice, or <code>append</code>.</p>\n<h2>Task</h2>\n<p>Fix the build between the markers in</p>\n<p>preallocindex.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  [1,2,3]\nOutput: [2,4,6]\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  []\nOutput: []\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  [-1,4]\nOutput: [-2,8]\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">len vs cap</span><span class=\"tchip\">Two idioms</span><span class=\"tchip\">Panic</span></div></details>",
    "education": "<h1>Indexing needs length, appending needs capacity</h1>\n<h2>Intuition</h2>\n<p><code>make([]int, 0, n)</code> reserves capacity but has length 0 \u2014 indexing <code>out[i]</code> panics.</p>\n<p>Two valid patterns:</p>\n<pre class=\"md\"><code>\nout := make([]int, len(xs)); for i, x := range xs { out[i] = x * 2 } // index\nout := make([]int, 0, len(xs)); for _, x := range xs { out = append(out, x*2) } // append\n</code></pre>\n<h2>Approach</h2>\n<p>1. Bug: make([]int, 0, len(xs)) has length 0, so out[i] = x*2 indexes out of range and panics. 2. Fix: make([]int, len(xs)) so the slice has full length and index writes are valid. 3. Each out[i] then receives the doubled value.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Doubled(xs []int) []int {\n\tout := make([]int, len(xs))\n\tfor i, x := range xs {\n\t\tout[i] = x * 2\n\t}\n\treturn out\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>With length 0, out[0]=... panics (index out of range). With length len(xs), out[0..2] are valid slots filled with 2,4,6.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Capacity is spare room; only elements within length exist.</li>\n<li><code>append</code> grows length; indexing requires it to already be there.</li>\n<li><code>make([]T, n)</code> zero-fills n elements; <code>make([]T, 0, n)</code> fills none.</li>\n</ul>",
    "starter": "// Package preallocindex doubles a slice. A planted bug indexes into a length-0\n// slice instead of appending.\npackage preallocindex\n\n// Doubled returns a slice with each element of xs doubled.\nfunc Doubled(xs []int) []int {\n\t// CHANGE CODE BELOW THIS LINE\n\tout := make([]int, 0, len(xs))\n\tfor i, x := range xs {\n\t\tout[i] = x * 2\n\t}\n\t// CHANGE CODE ABOVE THIS LINE\n\treturn out\n}\n",
    "debug": "",
    "file": "preallocindex.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/03-composite-types/senior/rangecopymutate": {
    "id": "01-language-basics/03-composite-types/senior/rangecopymutate",
    "title": "Mutating the Range Copy",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Mutating the Range Copy</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p><code>for _, o := range orders</code> gives a <strong>copy</strong> of each struct. <code>o.Price -= ...</code></p>\n<p>changes the copy; the slice is untouched.</p>\n<h2>Task</h2>\n<p>Fix the loop between the markers in</p>\n<p>rangecopymutate.go to mutate the slice element.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  orders=[{100},{200}], pct=10\nOutput: [{90},{180}]\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  orders=[{50}], pct=0\nOutput: [{50}]\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  orders=[{100}], pct=100\nOutput: [{0}]\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Range copies</span><span class=\"tchip\">Index to mutate</span><span class=\"tchip\">Structs are values</span></div></details>",
    "education": "<h1>Range gives you a copy</h1>\n<h2>Intuition</h2>\n<p><code>for _, v := range s</code> binds <code>v</code> to a <strong>copy</strong> of each element. Writing to <code>v</code></p>\n<p>(or its fields, for a struct) doesn't touch the slice. Mutate through the index:</p>\n<pre class=\"md\"><code>\nfor i := range orders { orders[i].Price -= orders[i].Price * pct / 100 }\n</code></pre>\n<h2>Approach</h2>\n<p>1. Bug: for _, o := range orders copies each element into o; o.Price -= ... mutates the copy, so the slice is unchanged. 2. Fix: index the slice: for i := range orders { orders[i].Price -= orders[i].Price*pct/100 }. 3. Writing through orders[i] mutates the real element.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\ntype Order struct {\n\tPrice int\n}\n\nfunc Discount(orders []Order, pct int) {\n\tfor i := range orders {\n\t\torders[i].Price -= orders[i].Price * pct / 100\n\t}\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>o is a copy of {100}; discounting o leaves orders[0]={100}. Using orders[i], the write lands on the backing array -&gt; {90}.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Index (<code>s[i]</code>) to mutate; the range value is read-only in effect.</li>\n<li>For slices of pointers, mutating <code>*v</code> <strong>does</strong> work (the pointer is copied, not</li>\n</ul>\n<p>  the pointee).</p>\n<ul>\n<li>Large structs copied per iteration also cost performance.</li>\n</ul>",
    "starter": "// Package rangecopymutate applies a discount to every order. A planted bug\n// mutates the range copy, so nothing changes.\npackage rangecopymutate\n\n// Order has a price in cents.\ntype Order struct {\n\tPrice int\n}\n\n// Discount reduces every order's price by pct percent (integer), in place.\nfunc Discount(orders []Order, pct int) {\n\t// CHANGE CODE BELOW THIS LINE\n\tfor _, o := range orders {\n\t\to.Price -= o.Price * pct / 100\n\t}\n\t// CHANGE CODE ABOVE THIS LINE\n}\n",
    "debug": "",
    "file": "rangecopymutate.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/03-composite-types/senior/removeorder": {
    "id": "01-language-basics/03-composite-types/senior/removeorder",
    "title": "Delete Off-By-One",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Delete Off-By-One</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p>The delete idiom joins the parts before and after index <code>i</code>. The tail slice</p>\n<p>starts at <code>i</code> instead of <code>i+1</code>, so the element is copied back, not removed.</p>\n<h2>Task</h2>\n<p>Fix the tail expression between the markers in</p>\n<p>removeorder.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  xs=[1,2,3,4], i=1\nOutput: [1,3,4]\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  xs=[9], i=0\nOutput: []\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  xs=[1,2,3], i=2\nOutput: [1,2]\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Two-half join</span><span class=\"tchip\">Skip the target</span><span class=\"tchip\">Spread</span></div></details>",
    "education": "<h1>The delete idiom</h1>\n<h2>Intuition</h2>\n<p>Deleting index <code>i</code> keeps everything before it and everything <strong>after</strong> it:</p>\n<pre class=\"md\"><code>\nappend(xs[:i], xs[i+1:]...)\n</code></pre>\n<p>Using <code>xs[i:]</code> for the tail re-includes the element you meant to drop.</p>\n<h2>Approach</h2>\n<p>1. Bug: append(xs[:i], xs[i:]...) re-appends the element at index i (off-by-one), so nothing is removed. 2. Fix: skip the removed element with xs[i+1:]. 3. append(xs[:i], xs[i+1:]...) shifts the tail left over index i.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc RemoveAt(xs []int, i int) []int {\n\treturn append(xs[:i], xs[i+1:]...)\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>xs=[1,2,3,4], i=1. Buggy xs[i:]=[2,3,4] -&gt; result [1,2,3,4]. Correct xs[i+1:]=[3,4] -&gt; [1,3,4].</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Tail starts at <code>i+1</code>, not <code>i</code>.</li>\n<li>This overwrites the backing array \u2014 copy first if the caller's slice matters.</li>\n<li>Guard <code>i</code> in range before slicing.</li>\n</ul>",
    "starter": "// Package removeorder deletes a slice element by index. A planted off-by-one\n// keeps the removed element.\npackage removeorder\n\n// RemoveAt returns xs with the element at index i removed (0 <= i < len).\nfunc RemoveAt(xs []int, i int) []int {\n\t// CHANGE CODE BELOW THIS LINE\n\treturn append(xs[:i], xs[i:]...)\n\t// CHANGE CODE ABOVE THIS LINE\n}\n",
    "debug": "",
    "file": "removeorder.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/03-composite-types/senior/shallowclone": {
    "id": "01-language-basics/03-composite-types/senior/shallowclone",
    "title": "Struct Copy Shares Slice",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Struct Copy Shares Slice</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p>Returning <code>d</code> copies the struct \u2014 but the <code>Tags</code> <strong>slice header</strong> is copied, so</p>\n<p>both share the same backing array. Mutating the clone's tags corrupts the</p>\n<p>original.</p>\n<h2>Task</h2>\n<p>Fix <code>Clone</code> between the markers in shallowclone.go to copy the</p>\n<p><code>Tags</code> slice independently.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Doc{Title:A, Tags:[x,y]}\nOutput: independent copy; mutating copy.Tags[0] doesn't affect original\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Doc{Title:B, Tags:[]}\nOutput: Doc{Title:B, Tags:[]}\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Doc{Title:C, Tags:[t]}\nOutput: Doc{Title:C, Tags:[t]}\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Shallow struct copy</span><span class=\"tchip\">Slice field aliasing</span><span class=\"tchip\">Deep copy</span></div></details>",
    "education": "<h1>Shallow vs deep copies</h1>\n<h2>Intuition</h2>\n<p>Copying a struct by value duplicates each field. For a slice field, that copies</p>\n<p>the <strong>header</strong> (pointer/len/cap) but not the underlying array \u2014 so both structs</p>\n<p>share the same elements:</p>\n<pre class=\"md\"><code>\nd.Tags = append([]string{}, d.Tags...) // independent copy of the slice\nreturn d\n</code></pre>\n<h2>Approach</h2>\n<p>1. Bug: return d copies the struct but the Tags slice header still points at the same backing array, so the copy shares tags. 2. Fix: copy the struct, then replace Tags with a fresh slice: out.Tags = append([]string(nil), d.Tags...). 3. Now mutating one Doc's tags does not affect the other.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\ntype Doc struct {\n\tTitle string\n\tTags  []string\n}\n\nfunc Clone(d Doc) Doc {\n\tout := d\n\tout.Tags = append([]string(nil), d.Tags...)\n\treturn out\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>return d shares the Tags array: original.Tags[0]=z shows in the clone. Cloning Tags into a new array isolates the two.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Every slice/map/pointer field needs its own deep copy.</li>\n<li><code>slices.Clone</code>/<code>maps.Clone</code> handle one level; nested references need recursion.</li>\n<li>Value types (numbers, strings, arrays) copy fully and are safe.</li>\n</ul>",
    "starter": "// Package shallowclone copies a record. A planted bug shares the slice field, so\n// mutating the copy leaks into the original.\npackage shallowclone\n\n// Doc has a title and a list of tags.\ntype Doc struct {\n\tTitle string\n\tTags  []string\n}\n\n// Clone returns a deep-ish copy: the Tags slice must be independent.\nfunc Clone(d Doc) Doc {\n\t// CHANGE CODE BELOW THIS LINE\n\treturn d\n\t// CHANGE CODE ABOVE THIS LINE\n}\n",
    "debug": "",
    "file": "shallowclone.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/03-composite-types/senior/sliceequal": {
    "id": "01-language-basics/03-composite-types/senior/sliceequal",
    "title": "Slice Equality",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Slice Equality</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p>Slices can't be compared with <code>==</code> (that's a compile error), so equality must be</p>\n<p>element-wise. The code checks only the length, so different contents of equal</p>\n<p>length compare \"equal\".</p>\n<h2>Task</h2>\n<p>Fix the body between the markers in sliceequal.go to compare</p>\n<p>elements.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  a=[1,2,3], b=[1,2,3]\nOutput: true\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  a=[1,2,3], b=[1,9,3]\nOutput: false\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  a=[], b=[]\nOutput: true\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">No slice ==</span><span class=\"tchip\">Element-wise</span><span class=\"tchip\">Length first</span></div></details>",
    "education": "<h1>Comparing slices</h1>\n<h2>Intuition</h2>\n<p>Slices are not comparable with <code>==</code> (except to <code>nil</code>). Equality is length plus</p>\n<p>element-wise comparison:</p>\n<pre class=\"md\"><code>\nif len(a) != len(b) { return false }\nfor i := range a { if a[i] != b[i] { return false } }\nreturn true\n</code></pre>\n<h2>Approach</h2>\n<p>1. Bug: after the length check the function just returns true, so any two equal-length slices compare equal regardless of contents. 2. Fix: loop over the elements and return false on the first mismatch, true if all match. 3. Length equality is necessary but not sufficient.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Equal(a, b []int) bool {\n\tif len(a) != len(b) {\n\t\treturn false\n\t}\n\tfor i := range a {\n\t\tif a[i] != b[i] {\n\t\t\treturn false\n\t\t}\n\t}\n\treturn true\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>a=[1,2,3], b=[1,9,3] have equal length; buggy code returns true. The element loop finds a[1]!=b[1] and returns false.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>a == b</code> on slices is a compile error; don't reach for it.</li>\n<li><code>slices.Equal</code> (Go 1.21+) does exactly this.</li>\n<li><code>reflect.DeepEqual</code> also works but is slower and broader.</li>\n</ul>",
    "starter": "// Package sliceequal compares two int slices. A planted bug compares only\n// lengths.\npackage sliceequal\n\n// Equal reports whether a and b have the same length and equal elements in\n// order.\nfunc Equal(a, b []int) bool {\n\tif len(a) != len(b) {\n\t\treturn false\n\t}\n\t// CHANGE CODE BELOW THIS LINE\n\treturn true\n\t// CHANGE CODE ABOVE THIS LINE\n}\n",
    "debug": "",
    "file": "sliceequal.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/03-composite-types/senior/sortmutates": {
    "id": "01-language-basics/03-composite-types/senior/sortmutates",
    "title": "Sort Mutates Caller",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Sort Mutates Caller</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p><code>out := xs</code> copies the slice <strong>header</strong>, not the data, so <code>sort.Ints(out)</code> sorts</p>\n<p>the caller's backing array. \"SortedCopy\" corrupts its input.</p>\n<h2>Task</h2>\n<p>Fix the copy between the markers in sortmutates.go to duplicate</p>\n<p>the data first.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  [3,1,2]\nOutput: [1,2,3]; input stays [3,1,2]\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  [1]\nOutput: [1]\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  [5,4,3,2,1]\nOutput: [1,2,3,4,5]\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Header vs data</span><span class=\"tchip\">sort in place</span><span class=\"tchip\">Copy first</span></div></details>",
    "education": "<h1>\"Copy\" that isn't</h1>\n<h2>Intuition</h2>\n<p><code>out := xs</code> copies the slice header (pointer/len/cap) \u2014 both names point at the</p>\n<p>same backing array. <code>sort.Ints(out)</code> therefore sorts <code>xs</code> too. Duplicate the data</p>\n<p>to isolate:</p>\n<pre class=\"md\"><code>\nout := append([]int{}, xs...) // independent copy\nsort.Ints(out)\n</code></pre>\n<h2>Approach</h2>\n<p>1. Bug: out := xs aliases the caller's slice; sort.Ints(out) sorts in place, corrupting xs. 2. Fix: copy first: out := append([]int(nil), xs...). 3. Sorting the independent copy leaves the input untouched.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nimport \"sort\"\n\nfunc SortedCopy(xs []int) []int {\n\tout := append([]int(nil), xs...)\n\tsort.Ints(out)\n\treturn out\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>out=xs shares the array; sort.Ints sorts it so xs is reordered too. A copied out is sorted independently, xs preserved.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Assigning a slice never copies its elements.</li>\n<li><code>slices.Sorted</code>/<code>slices.Clone</code> avoid the footgun.</li>\n<li><code>sort.Ints</code>, <code>sort.Slice</code>, and <code>slices.Sort</code> all mutate in place.</li>\n</ul>",
    "starter": "// Package sortmutates returns a sorted copy. A planted bug sorts the input in\n// place, corrupting the caller's slice.\npackage sortmutates\n\nimport \"sort\"\n\n// SortedCopy returns a new sorted slice; the input xs must not be modified.\nfunc SortedCopy(xs []int) []int {\n\t// CHANGE CODE BELOW THIS LINE\n\tout := xs\n\t// CHANGE CODE ABOVE THIS LINE\n\tsort.Ints(out)\n\treturn out\n}\n",
    "debug": "",
    "file": "sortmutates.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/03-composite-types/senior/stackpop": {
    "id": "01-language-basics/03-composite-types/senior/stackpop",
    "title": "Stack Pop Shrink",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Stack Pop Shrink</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p><code>Pop</code> reads the top element but returns the stack unchanged, so it never shrinks \u2014</p>\n<p>popping repeatedly returns the same value.</p>\n<h2>Task</h2>\n<p>Fix the return between the markers in stackpop.go to drop the last</p>\n<p>element.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  [1,2,3]\nOutput: ([1,2], 3, true)\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  [9]\nOutput: ([], 9, true)\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  []\nOutput: ([], 0, false)\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Slice as stack</span><span class=\"tchip\">Shrink</span><span class=\"tchip\">Return new slice</span></div></details>",
    "education": "<h1>Slices as stacks</h1>\n<h2>Intuition</h2>\n<p>A slice makes a natural stack: push with <code>append</code>, pop by reading the last</p>\n<p>element and re-slicing to drop it:</p>\n<pre class=\"md\"><code>\ntop := s[len(s)-1]\nreturn s[:len(s)-1], top, true\n</code></pre>\n<h2>Approach</h2>\n<p>1. Bug: return s, top, true returns the popped value but the same full-length slice, so the stack never shrinks. 2. Fix: return the truncated slice s[:len(s)-1]. 3. That drops the last element while returning its value.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Pop(s []int) ([]int, int, bool) {\n\tif len(s) == 0 {\n\t\treturn s, 0, false\n\t}\n\ttop := s[len(s)-1]\n\treturn s[:len(s)-1], top, true\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>s=[1,2,3], top=3. Buggy return keeps s=[1,2,3]. Correct s[:2]=[1,2] plus top=3 and true.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>s[:len(s)-1]</code> keeps the popped element in the backing array; for pointer</li>\n</ul>\n<p>  elements, zero it to avoid a memory leak.</p>\n<ul>\n<li>Guard the empty case before indexing <code>len(s)-1</code>.</li>\n<li>The caller must use the returned slice (<code>s, v, ok = Pop(s)</code>).</li>\n</ul>",
    "starter": "// Package stackpop pops the top of a stack. A planted bug returns the element\n// but forgets to shrink the stack.\npackage stackpop\n\n// Pop removes and returns the last element of s, returning the shrunken stack\n// and the popped value. Popping an empty stack returns (s, 0, false).\nfunc Pop(s []int) ([]int, int, bool) {\n\tif len(s) == 0 {\n\t\treturn s, 0, false\n\t}\n\ttop := s[len(s)-1]\n\t// CHANGE CODE BELOW THIS LINE\n\treturn s, top, true\n\t// CHANGE CODE ABOVE THIS LINE\n}\n",
    "debug": "",
    "file": "stackpop.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/03-composite-types/senior/subsliceclamp": {
    "id": "01-language-basics/03-composite-types/senior/subsliceclamp",
    "title": "Clamp the Sub-slice End",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Clamp the Sub-slice End</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p><code>xs[:n]</code> panics when <code>n &gt; len(xs)</code>. A \"take up to n\" helper must clamp the end to</p>\n<p>the length.</p>\n<h2>Task</h2>\n<p>Fix the return between the markers in</p>\n<p>subsliceclamp.go to clamp <code>n</code>.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  xs=[1,2,3], n=2\nOutput: [1,2]\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  xs=[1,2,3], n=10\nOutput: [1,2,3]\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  xs=[1,2,3], n=0\nOutput: []\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Slice bounds</span><span class=\"tchip\">Clamp</span><span class=\"tchip\">Panic safety</span></div></details>",
    "education": "<h1>Slice bounds and clamping</h1>\n<h2>Intuition</h2>\n<p><code>xs[:n]</code> requires <code>n &lt;= len(xs)</code>; otherwise it panics at runtime. A \"take up to n\"</p>\n<p>operation clamps the end:</p>\n<pre class=\"md\"><code>\nif n &gt; len(xs) { n = len(xs) }\nreturn xs[:n]\n</code></pre>\n<h2>Approach</h2>\n<p>1. Bug: return xs[:n] panics when n &gt; len(xs) (slice bound out of range). 2. Fix: clamp n to len(xs) before slicing: if n &gt; len(xs) { n = len(xs) }. 3. Then xs[:n] is always in range.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Take(xs []int, n int) []int {\n\tif n &lt;= 0 {\n\t\treturn xs[:0]\n\t}\n\tif n &gt; len(xs) {\n\t\tn = len(xs)\n\t}\n\treturn xs[:n]\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>xs=[1,2,3], n=10: xs[:10] panics. After clamping n=3, xs[:3]=[1,2,3].</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Slicing bounds are checked at runtime; out-of-range panics.</li>\n<li><code>min(n, len(xs))</code> (Go 1.21+) expresses the clamp.</li>\n<li>Capacity allows <code>xs[:n]</code> up to cap for the two-index form \u2014 but that exposes</li>\n</ul>\n<p>  stale data; prefer length clamping.</p>",
    "starter": "// Package subsliceclamp returns a safe sub-slice. A planted bug omits the clamp,\n// so an out-of-range end panics.\npackage subsliceclamp\n\n// Take returns up to n elements from the front of xs. If n exceeds len(xs), it\n// returns all of xs (no panic). n <= 0 returns empty.\nfunc Take(xs []int, n int) []int {\n\tif n <= 0 {\n\t\treturn xs[:0]\n\t}\n\t// CHANGE CODE BELOW THIS LINE\n\treturn xs[:n]\n\t// CHANGE CODE ABOVE THIS LINE\n}\n",
    "debug": "",
    "file": "subsliceclamp.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/03-composite-types/senior/twodshallow": {
    "id": "01-language-basics/03-composite-types/senior/twodshallow",
    "title": "Deep-Copy a Grid",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Deep-Copy a Grid</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p><code>copy(out, grid)</code> copies the <strong>row slice headers</strong>, so both grids share the same</p>\n<p>rows. Writing a cell in the clone changes the original.</p>\n<h2>Task</h2>\n<p>Fix the copy between the markers in twodshallow.go to clone each</p>\n<p>row.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  [[1,2],[3,4]]\nOutput: independent copy; mutating out[0][0] doesn't affect input\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  [[9]]\nOutput: [[9]]\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  []\nOutput: []\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Shallow copy</span><span class=\"tchip\">Per-row clone</span><span class=\"tchip\">Nested references</span></div></details>",
    "education": "<h1>Deep-copying nested slices</h1>\n<h2>Intuition</h2>\n<p>A <code>[][]int</code> is a slice of slice-headers. <code>copy(out, grid)</code> (or a plain assignment)</p>\n<p>duplicates the outer headers but leaves every row pointing at the original backing</p>\n<p>arrays. Clone each row:</p>\n<pre class=\"md\"><code>\nfor i := range grid { out[i] = append([]int{}, grid[i]...) }\n</code></pre>\n<h2>Approach</h2>\n<p>1. Bug: copy(out, grid) copies only the row-slice headers, so out's rows alias grid's rows. 2. Fix: allocate a fresh row per index: out[i] = append([]int(nil), grid[i]...). 3. Each row is deep-copied, so writes to out don't touch grid.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Clone(grid [][]int) [][]int {\n\tout := make([][]int, len(grid))\n\tfor i := range grid {\n\t\tout[i] = append([]int(nil), grid[i]...)\n\t}\n\treturn out\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>copy(out,grid) makes out[0] share grid[0]'s array; out[0][0]=9 mutates grid too. Copying each row into a new array isolates them.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Each level of nesting needs its own copy.</li>\n<li><code>slices.Clone(grid)</code> is still shallow (rows shared) \u2014 clone rows explicitly.</li>\n<li>The cost is O(total elements), unavoidable for true independence.</li>\n</ul>",
    "starter": "// Package twodshallow clones a 2-D grid. A planted bug copies only the outer\n// slice, so rows stay shared.\npackage twodshallow\n\n// Clone returns an independent copy of a grid (rows must not be shared).\nfunc Clone(grid [][]int) [][]int {\n\tout := make([][]int, len(grid))\n\t// CHANGE CODE BELOW THIS LINE\n\tcopy(out, grid)\n\t// CHANGE CODE ABOVE THIS LINE\n\treturn out\n}\n",
    "debug": "",
    "file": "twodshallow.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/03-composite-types/senior/valuereceiver": {
    "id": "01-language-basics/03-composite-types/senior/valuereceiver",
    "title": "Receiver Doesn't Mutate",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Receiver Doesn't Mutate</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p><code>Credit</code> has a <strong>value</strong> receiver, so <code>w.Balance += amount</code> updates a copy that's</p>\n<p>discarded \u2014 the wallet never changes.</p>\n<h2>Task</h2>\n<p>Fix the receiver between the markers in</p>\n<p>valuereceiver.go to a pointer.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  w={0}; Credit(100)\nOutput: w.Balance==100\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  w={50}; Credit(25)\nOutput: w.Balance==75\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  w={10}; Credit(0)\nOutput: w.Balance==10\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Value receiver</span><span class=\"tchip\">Pointer receiver</span><span class=\"tchip\">Silent no-op</span></div></details>",
    "education": "<h1>Value vs pointer receivers</h1>\n<h2>Intuition</h2>\n<p>A value receiver copies the struct, so any field write inside the method affects</p>\n<p>only the copy:</p>\n<pre class=\"md\"><code>\nfunc (w *Wallet) Credit(amount int) { w.Balance += amount } // pointer: mutates\n</code></pre>\n<h2>Approach</h2>\n<p>1. Bug: func (w Wallet) Credit uses a value receiver, so w is a copy; w.Balance += amount mutates the copy and is lost. 2. Fix: use a pointer receiver func (w *Wallet) Credit. 3. Then the method mutates the caller's Wallet in place.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\ntype Wallet struct {\n\tBalance int\n}\n\nfunc (w *Wallet) Credit(amount int) {\n\tw.Balance += amount\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>With a value receiver, Credit(100) updates a copy; the caller's Balance stays 0. A pointer receiver writes through to the original -&gt; Balance==100.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>w.Credit(...)</code> auto-takes the address when <code>w</code> is addressable, hiding the</li>\n</ul>\n<p>  distinction.</p>\n<ul>\n<li>Keep a type's receivers consistent (all pointer if any mutate).</li>\n<li>Value receivers are right for small, immutable value types.</li>\n</ul>",
    "starter": "// Package valuereceiver credits an account. A planted value receiver drops the\n// mutation.\npackage valuereceiver\n\n// Wallet holds a balance in cents.\ntype Wallet struct {\n\tBalance int\n}\n\n// Credit adds amount to the wallet's balance.\n// CHANGE CODE BELOW THIS LINE\nfunc (w Wallet) Credit(amount int) {\n\t// CHANGE CODE ABOVE THIS LINE\n\tw.Balance += amount\n}\n",
    "debug": "",
    "file": "valuereceiver.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/03-composite-types/staff/appendnilreturn": {
    "id": "01-language-basics/03-composite-types/staff/appendnilreturn",
    "title": "Append to Nil",
    "level": "staff",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Append to Nil</h1><div class=\"p-meta\"><span class=\"pill lv-staff\">staff</span></div></div><h2>Context</h2>\n<p>A defensive nil-check returns early, but appending to a <strong>nil</strong> slice is perfectly</p>\n<p>valid \u2014 it allocates and returns a one-element slice. The guard drops the first</p>\n<p>element.</p>\n<h2>Task</h2>\n<p>Remove the unnecessary guard between the markers in</p>\n<p>appendnilreturn.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  s=nil, x=5\nOutput: [5]\n</code></pre>\n<p>_Explanation:_ append to a nil slice allocates a new one-element slice.</p>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  s=[1 2], x=3\nOutput: [1 2 3]\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  s=[], x=7\nOutput: [7]\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Nil is appendable</span><span class=\"tchip\">Zero value slice</span><span class=\"tchip\">Over-defensive code</span></div></details>",
    "education": "<h1>The nil slice is usable</h1>\n<h2>Intuition</h2>\n<p>A nil slice has length 0 and is safe to <code>range</code>, <code>len</code>, and \u2014 crucially \u2014</p>\n<p><code>append</code>:</p>\n<pre class=\"md\"><code>\nvar s []int      // nil\ns = append(s, 5) // [5], allocated on demand\n</code></pre>\n<p>So a <code>if s == nil { return nil }</code> guard before an append is not just unnecessary,</p>\n<p>it's a bug: it refuses to add the first element.</p>\n<h2>Approach</h2>\n<p>1. Bug: the guard <code>if s == nil { return nil }</code> short-circuits and returns nil for a nil input, dropping x entirely.</p>\n<p>2. Fix: delete the guard. <code>append(s, x)</code> already handles a nil s correctly by allocating a fresh backing array.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Add(s []int, x int) []int {\n\treturn append(s, x)\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>Call Add(nil, 5): with the bug, s==nil is true so it returns nil (wrong). After the fix the guard is gone, so append(nil, 5) allocates [5] and returns it.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Append to nil: fine. Write to nil map: panic.</li>\n<li><code>len(nil) == 0</code>, ranging nil is zero iterations.</li>\n<li>Distinguish nil from empty only when serialization/identity demands it.</li>\n</ul>",
    "starter": "// Package appendnilreturn appends to a slice. A planted bug bails out on a nil\n// slice instead of appending (append to nil is valid).\npackage appendnilreturn\n\n// Add appends x to s and returns the result. A nil s must become a one-element\n// slice.\nfunc Add(s []int, x int) []int {\n\t// CHANGE CODE BELOW THIS LINE\n\tif s == nil {\n\t\treturn nil\n\t}\n\t// CHANGE CODE ABOVE THIS LINE\n\treturn append(s, x)\n}\n",
    "debug": "",
    "file": "appendnilreturn.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/03-composite-types/staff/appendtwobranch": {
    "id": "01-language-basics/03-composite-types/staff/appendtwobranch",
    "title": "Two Appends Share Capacity",
    "level": "staff",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Two Appends Share Capacity</h1><div class=\"p-meta\"><span class=\"pill lv-staff\">staff</span></div></div><h2>Context</h2>\n<p><code>append(base, x)</code> and <code>append(base, y)</code> both write into <code>base</code>'s spare capacity at</p>\n<p>the same index, so the second overwrites the first \u2014 <code>b</code> ends up <code>[1 2 4]</code>.</p>\n<h2>Task</h2>\n<p>Fix the base between the markers in</p>\n<p>appendtwobranch.go so the two appends don't alias.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  a=[1 2] (cap 10), x=3, y=4\nOutput: b=[1 2 3], c=[1 2 4]\n</code></pre>\n<p>_Explanation:_ each branch must not reuse a's spare capacity, or the second append overwrites the first.</p>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  b after Branch\nOutput: [1 2 3]\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  c after Branch\nOutput: [1 2 4]\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Shared spare cap</span><span class=\"tchip\">Clip capacity</span><span class=\"tchip\">Independent results</span></div></details>",
    "education": "<h1>Appending twice to one base</h1>\n<h2>Intuition</h2>\n<p>If <code>base</code> has spare capacity, <code>append(base, x)</code> writes into that capacity without</p>\n<p>reallocating \u2014 and so does a second <code>append(base, y)</code>, into the <strong>same</strong> slot.</p>\n<p>The two results share memory and stomp each other. Clip first:</p>\n<pre class=\"md\"><code>\nbase := a[:len(a):len(a)] // cap == len -&gt; every append reallocates\n</code></pre>\n<h2>Approach</h2>\n<p>1. Bug: <code>base := a</code> keeps a's spare capacity, so append(base,x) and append(base,y) write to the SAME backing slot index 2; c's write clobbers b, giving b=[1 2 4].</p>\n<p>2. Fix: <code>base := a[:len(a):len(a)]</code> clips capacity to length, forcing each append to allocate its own array.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Branch(a []int, x, y int) ([]int, []int) {\n\tbase := a[:len(a):len(a)]\n\tb := append(base, x)\n\tc := append(base, y)\n\treturn b, c\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>a has len 2, cap 10. Unclipped, append(base,3) writes 3 at index 2 in place (b shares array); append(base,4) writes 4 at the same index 2, so b now reads [1 2 4]. Clipping cap to 2 makes both appends grow into fresh arrays -&gt; b=[1 2 3], c=[1 2 4].</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Only clipping (or copying) guarantees independence.</li>\n<li><code>append</code> reuses spare capacity; that's the whole hazard.</li>\n<li><code>slices.Clip</code> expresses <code>a[:len(a):len(a)]</code>.</li>\n</ul>",
    "starter": "// Package appendtwobranch appends two different tails to a base slice. A planted\n// bug lets both appends share the base's spare capacity, so the second clobbers\n// the first.\npackage appendtwobranch\n\n// Branch returns two independent slices: base+x and base+y.\nfunc Branch(a []int, x, y int) ([]int, []int) {\n\t// CHANGE CODE BELOW THIS LINE\n\tbase := a\n\t// CHANGE CODE ABOVE THIS LINE\n\tb := append(base, x)\n\tc := append(base, y)\n\treturn b, c\n}\n",
    "debug": "",
    "file": "appendtwobranch.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/03-composite-types/staff/bufconcat": {
    "id": "01-language-basics/03-composite-types/staff/bufconcat",
    "title": "Copy at the Right Offset",
    "level": "staff",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Copy at the Right Offset</h1><div class=\"p-meta\"><span class=\"pill lv-staff\">staff</span></div></div><h2>Context</h2>\n<p>The second <code>copy</code> writes <code>b</code> at offset 0, overwriting <code>a</code>. It must write at offset</p>\n<p><code>len(a)</code> so <code>b</code> lands after <code>a</code>.</p>\n<h2>Task</h2>\n<p>Fix the second copy between the markers in bufconcat.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  a=[1 2], b=[3 4]\nOutput: [1 2 3 4]\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  a=[], b=[9]\nOutput: [9]\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  a=[5], b=[]\nOutput: [5]\n</code></pre>\n<p>_Explanation:_ copy target offset must be len(a).</p>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Copy destination</span><span class=\"tchip\">Preallocated buffer</span><span class=\"tchip\">Offset math</span></div></details>",
    "education": "<h1>Writing into a buffer at an offset</h1>\n<h2>Intuition</h2>\n<p>Filling a preallocated buffer means copying each piece to its correct offset. The</p>\n<p>second slice starts where the first ends:</p>\n<pre class=\"md\"><code>\nout := make([]byte, len(a)+len(b))\ncopy(out, a)\ncopy(out[len(a):], b) // offset by len(a)\n</code></pre>\n<h2>Approach</h2>\n<p>1. Bug: <code>copy(out, b)</code> writes b at offset 0, overwriting the copy of a.</p>\n<p>2. Fix: <code>copy(out[len(a):], b)</code> writes b right after a.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Concat(a, b []byte) []byte {\n\tout := make([]byte, len(a)+len(b))\n\tcopy(out, a)\n\tcopy(out[len(a):], b)\n\treturn out\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>out is zeroed len 4. copy(out,a) -&gt; [1 2 0 0]. Bug copy(out,b) -&gt; [3 4 0 0]. Fixed copy(out[2:],b) -&gt; [1 2 3 4].</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>copy</code> returns the count; the destination sub-slice sets where it lands.</li>\n<li>Size the buffer exactly, or trailing zeros/overflows appear.</li>\n<li>For many pieces, <code>bytes.Buffer</code> or <code>append</code> is simpler than manual offsets.</li>\n</ul>",
    "starter": "// Package bufconcat concatenates two byte slices into a preallocated buffer.\n// A planted bug copies b over the start instead of after a.\npackage bufconcat\n\n// Concat returns a new buffer containing a followed by b.\nfunc Concat(a, b []byte) []byte {\n\tout := make([]byte, len(a)+len(b))\n\tcopy(out, a)\n\t// CHANGE CODE BELOW THIS LINE\n\tcopy(out, b)\n\t// CHANGE CODE ABOVE THIS LINE\n\treturn out\n}\n",
    "debug": "",
    "file": "bufconcat.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/03-composite-types/staff/clipcap": {
    "id": "01-language-basics/03-composite-types/staff/clipcap",
    "title": "Clip Capacity",
    "level": "staff",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Clip Capacity</h1><div class=\"p-meta\"><span class=\"pill lv-staff\">staff</span></div></div><h2>Context</h2>\n<p>Handing out a slice with spare capacity is dangerous: the receiver's <code>append</code> can</p>\n<p>overwrite memory you still use. <code>Clip</code> should cap capacity to length via a</p>\n<p>full-slice expression. The code returns <code>xs</code> unchanged.</p>\n<h2>Task</h2>\n<p>Fix the return between the markers in clipcap.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  xs=make([]int,3,10)\nOutput: len 3, cap 3\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  xs len 3\nOutput: len stays 3\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  cap 10 clipped\nOutput: cap 3\n</code></pre>\n<p>_Explanation:_ three-index slice caps future append growth.</p>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Three-index slice</span><span class=\"tchip\">Append safety</span><span class=\"tchip\">Defensive slicing</span></div></details>",
    "education": "<h1>Clipping capacity</h1>\n<h2>Intuition</h2>\n<p>The three-index slice expression <code>xs[low:high:max]</code> sets capacity to <code>max-low</code>.</p>\n<p><code>xs[:len(xs):len(xs)]</code> yields cap == len, so any later <code>append</code> must allocate a</p>\n<p>new array instead of writing into shared spare capacity:</p>\n<pre class=\"md\"><code>\nreturn xs[:len(xs):len(xs)]\n</code></pre>\n<h2>Approach</h2>\n<p>1. Bug: <code>return xs</code> leaves cap unchanged (10), so a later append can mutate shared memory.</p>\n<p>2. Fix: <code>return xs[:len(xs):len(xs)]</code> sets cap == len via the three-index slice.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Clip(xs []int) []int {\n\treturn xs[:len(xs):len(xs)]\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>xs: len 3 cap 10. xs[:3:3] yields the same elements but cap 3. Now append would reallocate rather than reuse shared backing.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Capacity, not length, governs append reuse.</li>\n<li>Clipping doesn't copy \u2014 it still shares the (now cap-limited) array.</li>\n<li>Pair with a copy when you also need to release the backing array.</li>\n</ul>",
    "starter": "// Package clipcap trims a slice's capacity to its length so future appends\n// can't touch shared memory. A planted bug leaves capacity unchanged.\npackage clipcap\n\n// Clip returns xs with capacity reduced to its length.\nfunc Clip(xs []int) []int {\n\t// CHANGE CODE BELOW THIS LINE\n\treturn xs\n\t// CHANGE CODE ABOVE THIS LINE\n}\n",
    "debug": "",
    "file": "clipcap.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/03-composite-types/staff/copyshiftleft": {
    "id": "01-language-basics/03-composite-types/staff/copyshiftleft",
    "title": "copy Direction for Shift",
    "level": "staff",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">copy Direction for Shift</h1><div class=\"p-meta\"><span class=\"pill lv-staff\">staff</span></div></div><h2>Context</h2>\n<p>Shifting left drops <code>xs[0]</code>: element <code>i+1</code> moves to <code>i</code>. The code does</p>\n<p><code>copy(xs[1:], xs)</code>, which shifts <strong>right</strong> (overlapping copy toward higher</p>\n<p>indices), duplicating the first element.</p>\n<h2>Task</h2>\n<p>Fix the copy between the markers in</p>\n<p>copyshiftleft.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  [1 2 3]\nOutput: [2 3 0]\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  [5]\nOutput: [0]\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  [] \nOutput: []\n</code></pre>\n<p>_Explanation:_ empty returns early.</p>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">copy on overlap</span><span class=\"tchip\">Source/dest</span><span class=\"tchip\">Fill vacated</span></div></details>",
    "education": "<h1>Overlapping copy and direction</h1>\n<h2>Intuition</h2>\n<p><code>copy(dst, src)</code> copies <code>min(len)</code> elements and handles overlap like <code>memmove</code>.</p>\n<p>To shift left (drop index 0), the destination is the earlier region:</p>\n<pre class=\"md\"><code>\ncopy(xs, xs[1:]) // xs[i] = xs[i+1]\nxs[len(xs)-1] = 0\n</code></pre>\n<p><code>copy(xs[1:], xs)</code> shifts the other way, replicating <code>xs[0]</code>.</p>\n<h2>Approach</h2>\n<p>1. Bug: <code>copy(xs[1:], xs)</code> copies forward into overlapping memory, duplicating xs[0].</p>\n<p>2. Fix: <code>copy(xs, xs[1:])</code> shifts every element left one; then clear the last slot.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc ShiftLeft(xs []int) {\n\tif len(xs) == 0 {\n\t\treturn\n\t}\n\tcopy(xs, xs[1:])\n\txs[len(xs)-1] = 0\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>[1 2 3]: copy(xs, xs[1:]) copies [2 3] onto positions 0,1 -&gt; [2 3 3], then xs[2]=0 -&gt; [2 3 0]. The buggy copy(xs[1:],xs) would give [1 1 2] before clearing -&gt; [1 1 0].</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Left shift: <code>copy(xs, xs[1:])</code>. Right shift: <code>copy(xs[1:], xs)</code>.</li>\n<li><code>copy</code> returns the count copied (<code>len-1</code> here).</li>\n<li>Remember to clear the vacated slot to avoid a stale/duplicated value.</li>\n</ul>",
    "starter": "// Package copyshiftleft shifts a slice left by one (dropping the first element).\n// A planted bug uses the wrong copy direction, duplicating data.\npackage copyshiftleft\n\n// ShiftLeft moves every element one position toward index 0 (xs[0] is lost) and\n// sets the last element to 0, in place.\nfunc ShiftLeft(xs []int) {\n\tif len(xs) == 0 {\n\t\treturn\n\t}\n\t// CHANGE CODE BELOW THIS LINE\n\tcopy(xs[1:], xs)\n\t// CHANGE CODE ABOVE THIS LINE\n\txs[len(xs)-1] = 0\n}\n",
    "debug": "",
    "file": "copyshiftleft.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/03-composite-types/staff/gridcolsum": {
    "id": "01-language-basics/03-composite-types/staff/gridcolsum",
    "title": "Column Sum Indexing",
    "level": "staff",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Column Sum Indexing</h1><div class=\"p-meta\"><span class=\"pill lv-staff\">staff</span></div></div><h2>Context</h2>\n<p>Summing column <code>c</code> means visiting <code>grid[r][c]</code> for every row <code>r</code>. The code writes</p>\n<p><code>grid[c][r]</code> \u2014 row and column swapped \u2014 which reads the wrong cells (and can panic</p>\n<p>on a non-square grid).</p>\n<h2>Task</h2>\n<p>Fix the index between the markers in gridcolsum.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  grid=[[1 2][3 4][5 6]], c=1\nOutput: 12\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  grid=[[1 2][3 4]], c=0\nOutput: 4\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  grid=[[7]], c=0\nOutput: 7\n</code></pre>\n<p>_Explanation:_ single cell.</p>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Row-major indexing</span><span class=\"tchip\">Column traversal</span><span class=\"tchip\">Shape safety</span></div></details>",
    "education": "<h1>Row-major indexing</h1>\n<h2>Intuition</h2>\n<p>Go's 2-D slices are row-major: <code>grid[row][col]</code>. To walk a column, hold the column</p>\n<p>fixed and vary the row:</p>\n<pre class=\"md\"><code>\nfor r := range grid { sum += grid[r][c] }\n</code></pre>\n<p>Swapping to <code>grid[c][r]</code> reads across the wrong axis.</p>\n<h2>Approach</h2>\n<p>1. Bug: <code>grid[c][r]</code> swaps row/column roles, indexing the wrong cells (and can panic on non-square grids).</p>\n<p>2. Fix: <code>grid[r][c]</code> walks each row r and sums the fixed column c.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc ColSum(grid [][]int, c int) int {\n\tsum := 0\n\tfor r := range grid {\n\t\tsum += grid[r][c]\n\t}\n\treturn sum\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>c=1, rows 0..2: grid[0][1]=2, grid[1][1]=4, grid[2][1]=6 -&gt; 12. Buggy grid[1][r] would read row 1 across columns instead.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Outer index is the row; inner is the column.</li>\n<li>Column access is cache-unfriendly (strided) but semantically fine.</li>\n<li>Validate rectangularity if the grid may be ragged.</li>\n</ul>",
    "starter": "// Package gridcolsum sums a column of a grid. A planted bug indexes with the\n// row/column roles swapped.\npackage gridcolsum\n\n// ColSum returns the sum of column c across all rows of a rectangular grid.\nfunc ColSum(grid [][]int, c int) int {\n\tsum := 0\n\tfor r := range grid {\n\t\t// CHANGE CODE BELOW THIS LINE\n\t\tsum += grid[c][r]\n\t\t// CHANGE CODE ABOVE THIS LINE\n\t}\n\treturn sum\n}\n",
    "debug": "",
    "file": "gridcolsum.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/03-composite-types/staff/growthstale": {
    "id": "01-language-basics/03-composite-types/staff/growthstale",
    "title": "Stale Pointer After Growth",
    "level": "staff",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Stale Pointer After Growth</h1><div class=\"p-meta\"><span class=\"pill lv-staff\">staff</span></div></div><h2>Context</h2>\n<p><code>append</code> to a full slice <strong>reallocates</strong> the backing array. The pointer <code>p</code>,</p>\n<p>taken before the append, still points at the old array \u2014 writing <code>*p = 99</code> updates</p>\n<p>freed-from-view memory, not <code>s[0]</code>.</p>\n<h2>Task</h2>\n<p>Fix the write between the markers in growthstale.go to update</p>\n<p>the current slice.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  val=7\nOutput: first=99, slice=[99 7]\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  val=0\nOutput: first=99, slice=[99 0]\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  val=-1\nOutput: first=99, slice=[99 -1]\n</code></pre>\n<p>_Explanation:_ write must target the new backing array, not the stale pointer.</p>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">append reallocates</span><span class=\"tchip\">Stale pointers</span><span class=\"tchip\">Re-address</span></div></details>",
    "education": "<h1>append can invalidate pointers</h1>\n<h2>Intuition</h2>\n<p>When <code>append</code> exceeds capacity, it allocates a <strong>new</strong> backing array and copies.</p>\n<p>Any pointer or sub-slice into the old array still references the old memory:</p>\n<pre class=\"md\"><code>\np := &amp;s[0]\ns = append(s, x) // may reallocate\ns[0] = 99        // correct: writes the current array (don't use *p)\n</code></pre>\n<h2>Approach</h2>\n<p>1. Bug: after append reallocates, <code>p</code> points into the OLD array; <code>*p = 99</code> writes to freed/detached memory, leaving s[0]=10.</p>\n<p>2. Fix: write through the live slice <code>s[0] = 99</code> so the current backing array is updated.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc BumpFirst(val int) (int, []int) {\n\ts := make([]int, 1, 1) // len 1, cap 1 (full)\n\ts[0] = 10\n\tp := &amp;s[0]\n\ts = append(s, val) // reallocates; p now points at the OLD array\n\t_ = p\n\ts[0] = 99\n\treturn s[0], s\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>s=[10] cap1. append(s,7) allocates a new array [10 7]; p still points at the old [10]. *p=99 mutates the old array (invisible). s[0]=99 mutates the new array -&gt; returns 99, [99 7].</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Growth is capacity-triggered; with spare cap, <code>p</code> would still be valid \u2014 making</li>\n</ul>\n<p>  the bug intermittent.</p>\n<ul>\n<li>Sub-slices taken before an append can likewise detach.</li>\n<li>Prefer indices over long-lived element pointers into growable slices.</li>\n</ul>",
    "starter": "// Package growthstale demonstrates that append can reallocate, invalidating a\n// pointer into the old backing array. A planted bug writes through the stale\n// pointer.\npackage growthstale\n\n// BumpFirst appends val to a full-capacity slice (forcing reallocation) and then\n// sets the first element to 99. Returns the first element and the grown slice.\nfunc BumpFirst(val int) (int, []int) {\n\ts := make([]int, 1, 1) // len 1, cap 1 (full)\n\ts[0] = 10\n\tp := &s[0]\n\ts = append(s, val) // reallocates; p now points at the OLD array\n\t// CHANGE CODE BELOW THIS LINE\n\t*p = 99\n\t// CHANGE CODE ABOVE THIS LINE\n\treturn s[0], s\n}\n",
    "debug": "",
    "file": "growthstale.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/03-composite-types/staff/insertshift": {
    "id": "01-language-basics/03-composite-types/staff/insertshift",
    "title": "Insert Shift Direction",
    "level": "staff",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Insert Shift Direction</h1><div class=\"p-meta\"><span class=\"pill lv-staff\">staff</span></div></div><h2>Context</h2>\n<p>To insert at <code>i</code>, the elements from <code>i</code> onward must shift <strong>right</strong> by one. The</p>\n<p>code does <code>copy(xs[i:], xs[i+1:])</code>, which shifts left, dropping the tail.</p>\n<h2>Task</h2>\n<p>Fix the copy between the markers in insertshift.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  xs=[1 2 3], i=1, v=9\nOutput: [1 9 2 3]\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  xs=[1 2 3], i=0, v=0\nOutput: [0 1 2 3]\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  xs=[1 2 3], i=3, v=4\nOutput: [1 2 3 4]\n</code></pre>\n<p>_Explanation:_ insert at end.</p>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Shift right</span><span class=\"tchip\">Overlap-safe</span><span class=\"tchip\">Grow then shift</span></div></details>",
    "education": "<h1>Opening a gap for insertion</h1>\n<h2>Intuition</h2>\n<p>In-place insertion at <code>i</code> first grows the slice by one, then shifts the tail one</p>\n<p>step <strong>right</strong> to open a gap:</p>\n<pre class=\"md\"><code>\nxs = append(xs, 0)\ncopy(xs[i+1:], xs[i:]) // dst starts one past src -&gt; shift right\nxs[i] = v\n</code></pre>\n<p><code>copy</code> is overlap-safe (memmove semantics), so the right-shift doesn't corrupt.</p>\n<h2>Approach</h2>\n<p>1. Bug: <code>copy(xs[i:], xs[i+1:])</code> shifts the tail LEFT, dropping elements instead of making room.</p>\n<p>2. Fix: <code>copy(xs[i+1:], xs[i:])</code> shifts the tail RIGHT by one so index i is free for v.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc InsertAt(xs []int, i, v int) []int {\n\txs = append(xs, 0) // make room at the end\n\tcopy(xs[i+1:], xs[i:])\n\txs[i] = v\n\treturn xs\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>xs=[1 2 3], append 0 -&gt; [1 2 3 0]. i=1: copy(xs[2:],xs[1:]) shifts [2 3] to positions 2,3 -&gt; [1 2 2 3], then xs[1]=9 -&gt; [1 9 2 3].</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Insert shifts right; delete shifts left.</li>\n<li>Grow the slice before shifting, or you write past the end.</li>\n<li><code>copy</code> copies <code>min(len(dst),len(src))</code> \u2014 the counts line up here.</li>\n</ul>",
    "starter": "// Package insertshift inserts into a slice by shifting the tail right. A planted\n// bug shifts the wrong direction, losing the tail.\npackage insertshift\n\n// InsertAt inserts v at index i (0..len) in xs, in place, growing by one.\nfunc InsertAt(xs []int, i, v int) []int {\n\txs = append(xs, 0) // make room at the end\n\t// CHANGE CODE BELOW THIS LINE\n\tcopy(xs[i:], xs[i+1:])\n\t// CHANGE CODE ABOVE THIS LINE\n\txs[i] = v\n\treturn xs\n}\n",
    "debug": "",
    "file": "insertshift.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/03-composite-types/staff/keyfromstruct": {
    "id": "01-language-basics/03-composite-types/staff/keyfromstruct",
    "title": "Struct Map Key Fields",
    "level": "staff",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Struct Map Key Fields</h1><div class=\"p-meta\"><span class=\"pill lv-staff\">staff</span></div></div><h2>Context</h2>\n<p>A struct of comparable fields is a valid map key. But the key is built with <code>X</code></p>\n<p>and <code>Y</code> swapped (<code>Point{p.Y, p.X}</code>), so <code>(1,2)</code> and <code>(2,1)</code> collide.</p>\n<h2>Task</h2>\n<p>Fix the key between the markers in keyfromstruct.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  pts=[{1,2},{1,2},{3,4}]\nOutput: { {1,2}:2, {3,4}:1 }\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  pts=[{0,1}]\nOutput: { {0,1}:1 }\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  pts=[{2,3},{3,2}]\nOutput: { {2,3}:1, {3,2}:1 }\n</code></pre>\n<p>_Explanation:_ order-sensitive keys stay distinct.</p>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Structs as keys</span><span class=\"tchip\">Field identity</span><span class=\"tchip\">No slice fields</span></div></details>",
    "education": "<h1>Structs as map keys</h1>\n<h2>Intuition</h2>\n<p>A struct whose fields are all comparable is itself comparable and can be a map</p>\n<p>key. Its identity is the tuple of field values, so order/assignment matters:</p>\n<pre class=\"md\"><code>\nm[Point{p.X, p.Y}]++ // or simply m[p]++\n</code></pre>\n<h2>Approach</h2>\n<p>1. Bug: <code>Point{p.Y, p.X}</code> swaps fields, so (1,2) is tallied under key (2,1).</p>\n<p>2. Fix: <code>Point{p.X, p.Y}</code> preserves the coordinate order.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\ntype Point struct {\n\tX, Y int\n}\n\nfunc Count(pts []Point) map[Point]int {\n\tm := make(map[Point]int)\n\tfor _, p := range pts {\n\t\tm[Point{p.X, p.Y}]++\n\t}\n\treturn m\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>For p={1,2}: buggy key {2,1}, correct key {1,2}. Two identical {1,2} inputs both map to {1,2} -&gt; count 2; {3,4} -&gt; count 1.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>A struct with a slice/map/func field is <strong>not</strong> comparable \u2014 compile error as a</li>\n</ul>\n<p>  key.</p>\n<ul>\n<li><code>m[p]</code> reuses the loop value directly, avoiding field mistakes.</li>\n<li>Keys are hashed/compared by all fields.</li>\n</ul>",
    "starter": "// Package keyfromstruct counts coordinates using a struct map key. A planted bug\n// swaps the fields when building the key.\npackage keyfromstruct\n\n// Point is a comparable struct usable as a map key.\ntype Point struct {\n\tX, Y int\n}\n\n// Count tallies how many times each point appears.\nfunc Count(pts []Point) map[Point]int {\n\tm := make(map[Point]int)\n\tfor _, p := range pts {\n\t\t// CHANGE CODE BELOW THIS LINE\n\t\tm[Point{p.Y, p.X}]++\n\t\t// CHANGE CODE ABOVE THIS LINE\n\t}\n\treturn m\n}\n",
    "debug": "",
    "file": "keyfromstruct.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/03-composite-types/staff/maparraykey": {
    "id": "01-language-basics/03-composite-types/staff/maparraykey",
    "title": "Array Key Coordinate Order",
    "level": "staff",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Array Key Coordinate Order</h1><div class=\"p-meta\"><span class=\"pill lv-staff\">staff</span></div></div><h2>Context</h2>\n<p>Arrays are comparable, so <code>[2]int</code> is a valid map key. But the key is built with</p>\n<p>the coordinates <strong>swapped</strong> (<code>{c[1], c[0]}</code>), so <code>(1,2)</code> and <code>(2,1)</code> are conflated.</p>\n<h2>Task</h2>\n<p>Fix the key between the markers in maparraykey.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  cells=[[0,1],[0,1],[2,3]]\nOutput: { [0,1]:2, [2,3]:1 }\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  cells=[[1,0]]\nOutput: { [1,0]:1 }\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  cells=[[2,3],[3,2]]\nOutput: { [2,3]:1, [3,2]:1 }\n</code></pre>\n<p>_Explanation:_ row/col order preserved.</p>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Arrays are comparable</span><span class=\"tchip\">Slices are not</span><span class=\"tchip\">Key identity</span></div></details>",
    "education": "<h1>Arrays as map keys</h1>\n<h2>Intuition</h2>\n<p>Unlike slices, <strong>arrays are comparable</strong>, so a <code>[2]int</code> (or <code>[N]T</code> of comparable</p>\n<p>T) is a valid map key \u2014 perfect for coordinate pairs:</p>\n<pre class=\"md\"><code>\nm[[2]int{c[0], c[1]}]++\n</code></pre>\n<p>The array's element order <em>is</em> its identity; swapping fields makes a different</p>\n<p>key.</p>\n<h2>Approach</h2>\n<p>1. Bug: <code>[2]int{c[1], c[0]}</code> swaps the coordinate order in the array key.</p>\n<p>2. Fix: <code>[2]int{c[0], c[1]}</code> keeps (row, col) order.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc CountCells(cells [][2]int) map[[2]int]int {\n\tm := make(map[[2]int]int)\n\tfor _, c := range cells {\n\t\tm[[2]int{c[0], c[1]}]++\n\t}\n\treturn m\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>cell [0,1]: buggy key [1,0], correct key [0,1]. Two [0,1] cells -&gt; [0,1]:2; [2,3] -&gt; 1.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Slices, maps, and funcs are <strong>not</strong> comparable \u2014 can't be keys.</li>\n<li>A struct of comparable fields is also a valid key.</li>\n<li>Array keys are compared and hashed by value (all elements).</li>\n</ul>",
    "starter": "// Package maparraykey counts grid cells using an array key. A planted bug swaps\n// the coordinate order in the key.\npackage maparraykey\n\n// CountCells tallies how many times each (row, col) coordinate appears, keyed by\n// a [2]int array.\nfunc CountCells(cells [][2]int) map[[2]int]int {\n\tm := make(map[[2]int]int)\n\tfor _, c := range cells {\n\t\t// CHANGE CODE BELOW THIS LINE\n\t\tm[[2]int{c[1], c[0]}]++\n\t\t// CHANGE CODE ABOVE THIS LINE\n\t}\n\treturn m\n}\n",
    "debug": "",
    "file": "maparraykey.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/03-composite-types/staff/mapclonesharedslice": {
    "id": "01-language-basics/03-composite-types/staff/mapclonesharedslice",
    "title": "Deep-Clone Map of Slices",
    "level": "staff",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Deep-Clone Map of Slices</h1><div class=\"p-meta\"><span class=\"pill lv-staff\">staff</span></div></div><h2>Context</h2>\n<p>Copying <code>out[k] = v</code> copies the slice <strong>header</strong>, so the clone's slices share</p>\n<p>backing arrays with the original. Mutating a cloned slice corrupts the source.</p>\n<p>(<code>maps.Clone</code> is shallow for the same reason.)</p>\n<h2>Task</h2>\n<p>Fix the assignment between the markers in</p>\n<p>mapclonesharedslice.go to copy each slice.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  m={a:[1 2], b:[3]}, then c[a][0]=99\nOutput: m[a][0] stays 1\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  len(clone)\nOutput: 2\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  clone[b][0]\nOutput: 3\n</code></pre>\n<p>_Explanation:_ values are independent copies.</p>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Shallow map copy</span><span class=\"tchip\">Deep copy</span><span class=\"tchip\">maps.Clone limit</span></div></details>",
    "education": "<h1>Deep-copying maps with reference values</h1>\n<h2>Intuition</h2>\n<p>A map copy that assigns each value copies <strong>references</strong> for reference-typed</p>\n<p>values (slices, maps, pointers). To fully separate, clone the values too:</p>\n<pre class=\"md\"><code>\nfor k, v := range m { out[k] = append([]int{}, v...) }\n</code></pre>\n<h2>Approach</h2>\n<p>1. Bug: <code>out[k] = v</code> copies the slice header, so clone and original share the same backing array; mutating the clone corrupts the source.</p>\n<p>2. Fix: <code>out[k] = append([]int(nil), v...)</code> allocates a fresh backing array per value.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Clone(m map[string][]int) map[string][]int {\n\tout := make(map[string][]int, len(m))\n\tfor k, v := range m {\n\t\tout[k] = append([]int(nil), v...)\n\t}\n\treturn out\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>Clone({a:[1 2]}): fix builds out[a] as a new [1 2] independent of m[a]. Then c[a][0]=99 changes only the clone; m[a][0] remains 1.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Every reference-typed value needs its own deep copy.</li>\n<li>Nested maps of maps need recursion.</li>\n<li>Value types (int, string, array) copy fully and are safe.</li>\n</ul>",
    "starter": "// Package mapclonesharedslice deep-copies a map of slices. A planted bug shares\n// the slice values, so mutating the clone corrupts the original.\npackage mapclonesharedslice\n\n// Clone returns an independent copy: both the map and each slice value must be\n// separate from the input.\nfunc Clone(m map[string][]int) map[string][]int {\n\tout := make(map[string][]int, len(m))\n\tfor k, v := range m {\n\t\t// CHANGE CODE BELOW THIS LINE\n\t\tout[k] = v\n\t\t// CHANGE CODE ABOVE THIS LINE\n\t}\n\treturn out\n}\n",
    "debug": "",
    "file": "mapclonesharedslice.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/03-composite-types/staff/mapniljson": {
    "id": "01-language-basics/03-composite-types/staff/mapniljson",
    "title": "Nil Map JSON",
    "level": "staff",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Nil Map JSON</h1><div class=\"p-meta\"><span class=\"pill lv-staff\">staff</span></div></div><h2>Context</h2>\n<p>A nil map marshals to JSON <code>null</code>; an empty map to <code>{}</code>. <code>Counts</code> starts with a</p>\n<p>nil map and only allocates inside the loop, so <strong>empty input</strong> returns nil \u2192 <code>null</code>,</p>\n<p>breaking clients expecting an object.</p>\n<h2>Task</h2>\n<p>Fix the declaration between the markers in mapniljson.go to</p>\n<p>allocate upfront.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  xs=[] (empty)\nOutput: non-nil empty map -&gt; JSON {}\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  xs=[a a b]\nOutput: {a:2, b:1}\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  xs=[x]\nOutput: {x:1}\n</code></pre>\n<p>_Explanation:_ empty input must not encode to null.</p>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Nil vs empty map</span><span class=\"tchip\">JSON encoding</span><span class=\"tchip\">Allocate upfront</span></div></details>",
    "education": "<h1>Nil and empty maps at the boundary</h1>\n<h2>Intuition</h2>\n<p>Like slices, a nil map and an empty map both have length 0 but serialize</p>\n<p>differently: <code>encoding/json</code> emits <code>null</code> for nil and <code>{}</code> for an allocated empty</p>\n<p>map:</p>\n<pre class=\"md\"><code>\nm := make(map[string]int) // marshals to {}, even when empty\n</code></pre>\n<h2>Approach</h2>\n<p>1. Bug: <code>var m map[string]int</code> starts nil; with empty input the loop never initializes it, so a nil map is returned and JSON-encodes to null.</p>\n<p>2. Fix: <code>m := map[string]int{}</code> starts non-nil so empty input encodes to {}.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Counts(xs []string) map[string]int {\n\tm := map[string]int{}\n\tfor _, x := range xs {\n\t\tif m == nil {\n\t\t\tm = map[string]int{}\n\t\t}\n\t\tm[x]++\n\t}\n\treturn m\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>xs=[]: fix returns an initialized empty map -&gt; json.Marshal gives {}. Buggy nil map -&gt; null. Non-empty inputs behave the same either way.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>m == nil</code> is the only way to tell nil from empty.</li>\n<li>Reading nil maps is safe; writing panics \u2014 allocate before the loop.</li>\n<li>Prefer <code>make</code> at the top when the emptiness must be observable.</li>\n</ul>",
    "starter": "// Package mapniljson returns per-key counts that must JSON-encode to {} when\n// empty. A planted bug returns a nil map, which encodes to null.\npackage mapniljson\n\n// Counts returns the occurrence count of each string. For empty input it must\n// return a non-nil (empty) map so JSON is {} and not null.\nfunc Counts(xs []string) map[string]int {\n\t// CHANGE CODE BELOW THIS LINE\n\tvar m map[string]int\n\t// CHANGE CODE ABOVE THIS LINE\n\tfor _, x := range xs {\n\t\tif m == nil {\n\t\t\tm = map[string]int{}\n\t\t}\n\t\tm[x]++\n\t}\n\treturn m\n}\n",
    "debug": "",
    "file": "mapniljson.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/03-composite-types/staff/ringbuffer": {
    "id": "01-language-basics/03-composite-types/staff/ringbuffer",
    "title": "Ring Buffer Wrap",
    "level": "staff",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Ring Buffer Wrap</h1><div class=\"p-meta\"><span class=\"pill lv-staff\">staff</span></div></div><h2>Context</h2>\n<p>A ring buffer maps logical index <code>i</code> to physical <code>(head+i) mod len</code>. The code</p>\n<p>omits the modulo, so once <code>head+i</code> reaches the end it indexes out of range (and</p>\n<p>never wraps).</p>\n<h2>Task</h2>\n<p>Fix the index between the markers in ringbuffer.go to wrap.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  buf=[10 20 30], head=1, i=0\nOutput: 20\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  buf=[10 20 30], head=1, i=2\nOutput: 10\n</code></pre>\n<p>_Explanation:_ (1+2)%3=0 wraps around.</p>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  buf=[10 20 30], head=0, i=4\nOutput: 20\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Modular index</span><span class=\"tchip\">Wraparound</span><span class=\"tchip\">Fixed storage</span></div></details>",
    "education": "<h1>Circular indexing</h1>\n<h2>Intuition</h2>\n<p>A ring buffer stores a logical sequence in a fixed physical array. Logical index</p>\n<p><code>i</code> lives at physical <code>(head + i) mod len</code>:</p>\n<pre class=\"md\"><code>\nreturn buf[(head+i)%len(buf)]\n</code></pre>\n<p>The modulo is what makes the buffer circular; without it, indices run off the end.</p>\n<h2>Approach</h2>\n<p>1. Bug: <code>buf[head+i]</code> omits the modulo, so head+i &gt;= len(buf) indexes out of range and panics.</p>\n<p>2. Fix: <code>buf[(head+i)%len(buf)]</code> wraps the logical index into physical bounds.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc At(buf []int, head, i int) int {\n\treturn buf[(head+i)%len(buf)]\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>head=1,i=2,len=3: (1+2)%3=0 -&gt; buf[0]=10. Without modulo, buf[3] panics.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>%</code> keeps the index in <code>[0, len)</code> for non-negative operands.</li>\n<li>Negative logical indices need the normalize form <code>((x%n)+n)%n</code>.</li>\n<li>Overwriting policy (drop-oldest) is separate from the index math.</li>\n</ul>",
    "starter": "// Package ringbuffer indexes a circular buffer. A planted bug omits the modulo,\n// so logical indices past the end go out of range.\npackage ringbuffer\n\n// At returns the element at logical position i in a ring buffer whose physical\n// storage is buf and whose logical start is head. i may be >= len(buf).\nfunc At(buf []int, head, i int) int {\n\t// CHANGE CODE BELOW THIS LINE\n\treturn buf[head+i]\n\t// CHANGE CODE ABOVE THIS LINE\n}\n",
    "debug": "",
    "file": "ringbuffer.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/03-composite-types/staff/rotate90": {
    "id": "01-language-basics/03-composite-types/staff/rotate90",
    "title": "Rotate Matrix 90\u00b0",
    "level": "staff",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Rotate Matrix 90\u00b0</h1><div class=\"p-meta\"><span class=\"pill lv-staff\">staff</span></div></div><h2>Context</h2>\n<p>A 90\u00b0 clockwise rotation is <strong>transpose, then reverse each row</strong>. The transpose is</p>\n<p>done; the row reversal step is missing, so the result is only transposed.</p>\n<h2>Task</h2>\n<p>Add the row-reversal between the markers in rotate90.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  [[1 2][3 4]]\nOutput: [[3 1][4 2]]\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  [[1 2 3][4 5 6][7 8 9]]\nOutput: [[7 4 1][8 5 2][9 6 3]]\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  [[5]]\nOutput: [[5]]\n</code></pre>\n<p>_Explanation:_ 1x1 is unchanged.</p>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Transpose</span><span class=\"tchip\">Reverse rows</span><span class=\"tchip\">Compose</span></div></details>",
    "education": "<h1>Rotation as transpose + reverse</h1>\n<h2>Intuition</h2>\n<p>A 90\u00b0 clockwise rotation of a square matrix decomposes into two simple steps:</p>\n<p>transpose (swap <code>[i][j]</code> with <code>[j][i]</code>), then reverse each row:</p>\n<pre class=\"md\"><code>\n// after transposing out:\nfor i := range out {\n\tfor a, b := 0, n-1; a &lt; b; a, b = a+1, b-1 {\n\t\tout[i][a], out[i][b] = out[i][b], out[i][a]\n\t}\n}\n</code></pre>\n<h2>Approach</h2>\n<p>1. Bug: only the transpose (out[i][j]=m[j][i]) runs; the row-reversal step is missing, so the matrix is transposed, not rotated.</p>\n<p>2. Fix: after transposing, reverse each row of out (swap ends inward).</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Rotate(m [][]int) [][]int {\n\tn := len(m)\n\tout := make([][]int, n)\n\tfor i := range out {\n\t\tout[i] = make([]int, n)\n\t\tfor j := 0; j &lt; n; j++ {\n\t\t\tout[i][j] = m[j][i] // transpose\n\t\t}\n\t}\n\tfor i := range out {\n\t\tfor l, r := 0, n-1; l &lt; r; l, r = l+1, r-1 {\n\t\t\tout[i][l], out[i][r] = out[i][r], out[i][l]\n\t\t}\n\t}\n\treturn out\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>[[1 2][3 4]] transposes to [[1 3][2 4]]; reversing each row gives [[3 1][4 2]], the 90-degrees-clockwise rotation.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Clockwise = transpose then reverse rows; counter-clockwise = transpose then</li>\n</ul>\n<p>  reverse columns.</p>\n<ul>\n<li>The two-pointer reverse stops at the middle.</li>\n<li>An in-place layer-by-layer rotation avoids the second matrix but is trickier.</li>\n</ul>",
    "starter": "// Package rotate90 rotates a square matrix 90 degrees clockwise. A planted bug\n// forgets to reverse rows after transposing.\npackage rotate90\n\n// Rotate returns an n\u00d7n matrix rotated 90\u00b0 clockwise. Rotation is transpose,\n// then reverse each row.\nfunc Rotate(m [][]int) [][]int {\n\tn := len(m)\n\tout := make([][]int, n)\n\tfor i := range out {\n\t\tout[i] = make([]int, n)\n\t\tfor j := 0; j < n; j++ {\n\t\t\tout[i][j] = m[j][i] // transpose\n\t\t}\n\t}\n\t// CHANGE CODE BELOW THIS LINE\n\t// (reverse each row of out here)\n\t// CHANGE CODE ABOVE THIS LINE\n\treturn out\n}\n",
    "debug": "",
    "file": "rotate90.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/03-composite-types/staff/sliceleak": {
    "id": "01-language-basics/03-composite-types/staff/sliceleak",
    "title": "Sub-slice Memory Leak",
    "level": "staff",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Sub-slice Memory Leak</h1><div class=\"p-meta\"><span class=\"pill lv-staff\">staff</span></div></div><h2>Context</h2>\n<p>Returning <code>xs[:3]</code> keeps a reference to the <strong>entire</strong> backing array \u2014 even if the</p>\n<p>source had a million-element capacity, none of it can be freed while the small</p>\n<p>head lives. Copy to release it.</p>\n<h2>Task</h2>\n<p>Fix the return between the markers in sliceleak.go to return an</p>\n<p>independent 3-element slice.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  xs=make([]int,3,100000) = [1 2 3]\nOutput: [1 2 3] with cap &lt;= 3\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  len(head)\nOutput: 3\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  cap(head)\nOutput: 3\n</code></pre>\n<p>_Explanation:_ must not retain the huge backing array.</p>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Sub-slice retains array</span><span class=\"tchip\">GC reachability</span><span class=\"tchip\">Copy to release</span></div></details>",
    "education": "<h1>Sub-slices pin the whole backing array</h1>\n<h2>Intuition</h2>\n<p>A slice keeps its backing array reachable for the GC \u2014 the <em>entire</em> array, not</p>\n<p>just the visible window. <code>xs[:3]</code> of a 1M-capacity slice keeps all 1M alive:</p>\n<pre class=\"md\"><code>\nreturn append([]int{}, xs[:3]...) // independent; source array can be freed\n</code></pre>\n<h2>Approach</h2>\n<p>1. Bug: <code>return xs[:3]</code> shares xs's backing array (cap 100000), keeping the whole array alive -&gt; memory leak.</p>\n<p>2. Fix: <code>return append([]int(nil), xs[:3]...)</code> copies the 3 elements into a small fresh slice (cap &lt;= 3).</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Head3(xs []int) []int {\n\treturn append([]int(nil), xs[:3]...)\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>xs cap 100000. xs[:3] has cap 100000 and pins the array. The append-copy yields a new slice holding [1 2 3] with cap 3, letting the big array be collected.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>cap</code>, not <code>len</code>, reveals how much is retained.</li>\n<li>Same leak with sub-strings of huge strings (copy with <code>strings.Clone</code>).</li>\n<li><code>slices.Clip</code> reduces capacity but still shares the array; a copy fully</li>\n</ul>\n<p>  releases it.</p>",
    "starter": "// Package sliceleak returns a small head of a large slice. A planted bug returns\n// a sub-slice that retains the entire backing array (a memory leak).\npackage sliceleak\n\n// Head3 returns the first 3 elements as an independent slice that does NOT keep\n// the (potentially huge) source backing array alive.\nfunc Head3(xs []int) []int {\n\t// CHANGE CODE BELOW THIS LINE\n\treturn xs[:3]\n\t// CHANGE CODE ABOVE THIS LINE\n}\n",
    "debug": "",
    "file": "sliceleak.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/03-composite-types/staff/slicetoarray": {
    "id": "01-language-basics/03-composite-types/staff/slicetoarray",
    "title": "Slice-to-Array Length Check",
    "level": "staff",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Slice-to-Array Length Check</h1><div class=\"p-meta\"><span class=\"pill lv-staff\">staff</span></div></div><h2>Context</h2>\n<p><code>[4]byte(b[:4])</code> (slice-to-array conversion) panics when <code>b</code> has fewer than 4</p>\n<p>bytes. A header parser must guard the length and report failure instead.</p>\n<h2>Task</h2>\n<p>Fix the body between the markers in</p>\n<p>slicetoarray.go to check length first.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  b=[1 2 3 4 5]\nOutput: [1 2 3 4], true\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  b=[1 2]\nOutput: [0 0 0 0], false\n</code></pre>\n<p>_Explanation:_ too short: return false, not panic.</p>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  b=[9 8 7 6]\nOutput: [9 8 7 6], true\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Slice\u2192array</span><span class=\"tchip\">Length requirement</span><span class=\"tchip\">Guard</span></div></details>",
    "education": "<h1>Slice-to-array conversion</h1>\n<h2>Intuition</h2>\n<p>Go 1.20+ allows converting a slice to an array or array pointer: <code>[4]byte(b)</code></p>\n<p>copies the first 4 bytes. It <strong>panics</strong> if the slice is shorter than the array,</p>\n<p>so guard first:</p>\n<pre class=\"md\"><code>\nif len(b) &lt; 4 { return [4]byte{}, false }\nreturn [4]byte(b[:4]), true\n</code></pre>\n<h2>Approach</h2>\n<p>1. Bug: <code>return [4]byte(b[:4]), true</code> slices b[:4] unconditionally; on input shorter than 4 the slice expression panics.</p>\n<p>2. Fix: guard <code>if len(b) &lt; 4 { return [4]byte{}, false }</code> before the conversion.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc First4(b []byte) ([4]byte, bool) {\n\tif len(b) &lt; 4 {\n\t\treturn [4]byte{}, false\n\t}\n\treturn [4]byte(b[:4]), true\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>b=[1 2]: len 2 &lt; 4, so the fix returns the zero array and false. b=[1 2 3 4 5]: len &gt;= 4, convert first 4 -&gt; [1 2 3 4], true.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>The slice length must be <code>&gt;=</code> the array length, or it panics.</li>\n<li><code>(*[4]byte)(b)</code> gives a pointer (no copy) with the same length rule.</li>\n<li>Arrays are comparable and copyable; slices are neither.</li>\n</ul>",
    "starter": "// Package slicetoarray reads a fixed 4-byte header. A planted bug converts\n// without checking length, so short input panics.\npackage slicetoarray\n\n// First4 returns the first 4 bytes of b as an array, and ok=false if b is too\n// short (it must not panic).\nfunc First4(b []byte) ([4]byte, bool) {\n\t// CHANGE CODE BELOW THIS LINE\n\treturn [4]byte(b[:4]), true\n\t// CHANGE CODE ABOVE THIS LINE\n}\n",
    "debug": "",
    "file": "slicetoarray.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/03-composite-types/staff/stringimmutable": {
    "id": "01-language-basics/03-composite-types/staff/stringimmutable",
    "title": "Strings Are Immutable",
    "level": "staff",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Strings Are Immutable</h1><div class=\"p-meta\"><span class=\"pill lv-staff\">staff</span></div></div><h2>Context</h2>\n<p>Strings are immutable, so the code copies to a <code>[]byte</code>, mutates that, then</p>\n<p>mistakenly returns the <strong>original</strong> <code>s</code> \u2014 the edits are lost.</p>\n<h2>Task</h2>\n<p>Fix the return between the markers in</p>\n<p>stringimmutable.go to return the modified bytes.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  \"abc\"\nOutput: \"ABC\"\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  \"Go1\"\nOutput: \"GO1\"\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  \"\"\nOutput: \"\"\n</code></pre>\n<p>_Explanation:_ digits/uppercase pass through unchanged.</p>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">String immutability</span><span class=\"tchip\">Mutate via []byte</span><span class=\"tchip\">Convert back</span></div></details>",
    "education": "<h1>Editing strings</h1>\n<h2>Intuition</h2>\n<p>A Go string is immutable \u2014 you cannot assign to <code>s[i]</code>. To transform it, copy to a</p>\n<p><code>[]byte</code>, edit that, and convert back:</p>\n<pre class=\"md\"><code>\nb := []byte(s)\n// mutate b...\nreturn string(b)\n</code></pre>\n<p>Returning the original <code>s</code> discards all the work.</p>\n<h2>Approach</h2>\n<p>1. Bug: the loop upper-cases the []byte copy b, but the function returns the original immutable string s, so the change is discarded.</p>\n<p>2. Fix: <code>return string(b)</code> returns the mutated bytes as a new string.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Upper(s string) string {\n\tb := []byte(s)\n\tfor i, c := range b {\n\t\tif c &gt;= 'a' &amp;&amp; c &lt;= 'z' {\n\t\t\tb[i] = c - 32\n\t\t}\n\t}\n\treturn string(b)\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>s=\"abc\": b becomes ['A','B','C'] but s is unchanged. Returning s gives \"abc\"; returning string(b) gives \"ABC\".</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>s[i] = x</code> is a compile error; strings are read-only.</li>\n<li>Each conversion (<code>[]byte(s)</code>, <code>string(b)</code>) copies.</li>\n<li>For rune-level edits, use <code>[]rune</code>; for ASCII, <code>[]byte</code> suffices.</li>\n</ul>",
    "starter": "// Package stringimmutable upper-cases ASCII. A planted bug mutates a []byte copy\n// but returns the original string, which is immutable and unchanged.\npackage stringimmutable\n\n// Upper returns s with ASCII lowercase letters upper-cased.\nfunc Upper(s string) string {\n\tb := []byte(s)\n\tfor i, c := range b {\n\t\tif c >= 'a' && c <= 'z' {\n\t\t\tb[i] = c - 32\n\t\t}\n\t}\n\t// CHANGE CODE BELOW THIS LINE\n\treturn s\n\t// CHANGE CODE ABOVE THIS LINE\n}\n",
    "debug": "",
    "file": "stringimmutable.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/03-composite-types/staff/structpadding": {
    "id": "01-language-basics/03-composite-types/staff/structpadding",
    "title": "Struct Field Padding",
    "level": "staff",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Struct Field Padding</h1><div class=\"p-meta\"><span class=\"pill lv-staff\">staff</span></div></div><h2>Context</h2>\n<p>Fields are laid out in declaration order with alignment padding. `bool, int64,</p>\n<p>bool<code> forces the </code>int64` to an 8-byte boundary and pads the trailing bool, giving</p>\n<p>24 bytes. Grouping the wide field first packs it to 16.</p>\n<h2>Task</h2>\n<p>Reorder the fields between the markers in</p>\n<p>structpadding.go to minimize size (16 bytes on 64-bit).</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  fields A bool, B int64, C bool\nOutput: size 24 (bloated)\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  fields B int64, A bool, C bool\nOutput: size 16 (minimal)\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  unsafe.Sizeof(Record{})\nOutput: 16\n</code></pre>\n<p>_Explanation:_ grouping the two bools after the int64 removes padding.</p>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Alignment</span><span class=\"tchip\">Padding</span><span class=\"tchip\">Field ordering</span></div></details>",
    "education": "<h1>Alignment and field ordering</h1>\n<h2>Intuition</h2>\n<p>Each field is aligned to its size; the compiler inserts padding to satisfy that,</p>\n<p>and the struct's size rounds up to its largest alignment. <code>bool,int64,bool</code> pads</p>\n<p>to <strong>24</strong> bytes; <code>int64,bool,bool</code> packs to <strong>16</strong>:</p>\n<pre class=\"md\"><code>\ntype Record struct { B int64; A bool; C bool } // 8 + 1 + 1 -&gt; padded to 16\n</code></pre>\n<h2>Approach</h2>\n<p>1. Bug: order A bool, B int64, C bool forces 7 bytes padding after A (to 8-align int64) plus tail padding -&gt; 24 bytes.</p>\n<p>2. Fix: put the 8-byte int64 first, then the two bools together: B int64, A bool, C bool -&gt; 8 + 1 + 1 + 6 tail padding = 16 bytes.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\ntype Record struct {\n\tB int64\n\tA bool\n\tC bool\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>On 64-bit, int64 needs 8-byte alignment. Bad layout: A(1)+pad(7)+B(8)+C(1)+pad(7)=24. Good layout: B(8)+A(1)+C(1)+pad(6)=16.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Alignment is platform-dependent (this targets 64-bit).</li>\n<li><code>unsafe.Sizeof</code> reveals the true size; <code>fieldalignment</code> (a vet tool) flags waste.</li>\n<li>Micro-optimize layout only for high-count structs; clarity first otherwise.</li>\n</ul>",
    "starter": "// Package structpadding lays out a record compactly. A planted bug orders fields\n// so padding bloats the struct.\npackage structpadding\n\n// Record should be laid out to minimize size via field ordering. On a 64-bit\n// platform the minimal size is 16 bytes.\ntype Record struct {\n\t// CHANGE CODE BELOW THIS LINE\n\tA bool\n\tB int64\n\tC bool\n\t// CHANGE CODE ABOVE THIS LINE\n}\n",
    "debug": "",
    "file": "structpadding.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/junior/absif": {
    "id": "01-language-basics/04-functions/junior/absif",
    "title": "Absolute Value",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Absolute Value</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>A single <code>if</code> flips the sign of negatives \u2014 the simplest branch there is.</p>\n<h2>Task</h2>\n<p>Implement <code>Abs</code> in absif.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Abs(-5)\nOutput: 5\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Abs(5)\nOutput: 5\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Abs(0)\nOutput: 0\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Single if</span><span class=\"tchip\">Return value</span><span class=\"tchip\">Zero</span></div></details>",
    "education": "<h1>The single-branch if</h1>\n<h2>Intuition</h2>\n<p>An <code>if</code> without <code>else</code> handles the exceptional case and falls through to the common return.</p>\n<h2>Approach</h2>\n<p>1. Negatives \u2192 negate.</p>\n<p>2. Otherwise return as-is.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Abs(n int) int {\n\tif n &lt; 0 {\n\t\treturn -n\n\t}\n\treturn n\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>Abs(-5)</code> returns <code>-(-5) = 5</code>.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>math.MinInt</code> has no positive counterpart; ignore that edge for this junior task.</li>\n<li>No <code>else</code> is needed after a <code>return</code>.</li>\n</ul>",
    "starter": "// Package absif \u2014 Gopher Workplace challenge.\npackage absif\n\n// Abs returns the absolute value of n.\nfunc Abs(n int) int {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "absif.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/junior/averagef": {
    "id": "01-language-basics/04-functions/junior/averagef",
    "title": "Variadic Average",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Variadic Average</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>Averaging combines a variadic parameter with a divide-by-count guard \u2014 the</p>\n<p>empty case must not divide by zero.</p>\n<h2>Task</h2>\n<p>Implement <code>Average</code> in averagef.go. Return <code>0, false</code> for no arguments.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Average(2, 4, 6)\nOutput: 4.0, true\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Average(5)\nOutput: 5.0, true\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Average()\nOutput: 0, false\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Variadic + guard</span><span class=\"tchip\">Integer vs float division</span><span class=\"tchip\">Comma-ok</span></div></details>",
    "education": "<h1>Guarding aggregation</h1>\n<h2>Intuition</h2>\n<p>An average needs at least one sample; returning a validity flag lets callers avoid a division-by-zero or a misleading 0.</p>\n<h2>Approach</h2>\n<p>1. Guard the empty case (<code>ok == false</code>).</p>\n<p>2. Sum then divide by <code>float64(len(nums))</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Average(nums ...float64) (float64, bool) {\n\tif len(nums) == 0 {\n\t\treturn 0, false\n\t}\n\tsum := 0.0\n\tfor _, v := range nums {\n\t\tsum += v\n\t}\n\treturn sum / float64(len(nums)), true\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>Average(2,4,6)</code>: sum 12 over 3 \u2192 4.0, true; no args returns 0, false.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Dividing by <code>len(nums)</code> as an int truncates; convert to <code>float64</code>.</li>\n<li>Guard the empty slice before the division.</li>\n</ul>",
    "starter": "// Package averagef \u2014 Gopher Workplace challenge.\npackage averagef\n\n// Average returns the mean of nums and ok=true, or 0, false for none.\nfunc Average(nums ...float64) (float64, bool) {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "averagef.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/junior/cascade": {
    "id": "01-language-basics/04-functions/junior/cascade",
    "title": "Cascading Permissions",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Cascading Permissions</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>A <code>switch</code> with <code>fallthrough</code> lets a matched case continue into the next, so higher access levels accumulate the permissions of lower ones.</p>\n<h2>Task</h2>\n<p>Implement <code>Access</code> in cascade.go so each level adds the permissions below it, comma-joined highest-first.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Access(3)\nOutput: \"admin,write,read\"\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Access(2)\nOutput: \"write,read\"\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Access(9)\nOutput: \"\"\n</code></pre>\n<p>_Explanation:_ No exact case matches, so nothing accumulates.</p>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">switch fallthrough</span><span class=\"tchip\">accumulation</span><span class=\"tchip\">default/empty</span></div></details>",
    "education": "<h1>switch fallthrough</h1>\n<h2>Intuition</h2>\n<p>Unlike C, Go <code>switch</code> cases stop by default; <code>fallthrough</code> opts into continuing so lower tiers accumulate.</p>\n<h2>Approach</h2>\n<p>1. Switch on the exact <code>level</code> (3, 2, 1).</p>\n<p>2. Each case adds its permission then <code>fallthrough</code> to the lower one.</p>\n<p>3. Unmatched levels return \"\".</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Access(level int) string {\n\tresult := \"\"\n\tadd := func(s string) {\n\t\tif result != \"\" {\n\t\t\tresult += \",\"\n\t\t}\n\t\tresult += s\n\t}\n\tswitch level {\n\tcase 3:\n\t\tadd(\"admin\")\n\t\tfallthrough\n\tcase 2:\n\t\tadd(\"write\")\n\t\tfallthrough\n\tcase 1:\n\t\tadd(\"read\")\n\t}\n\treturn result\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>Access(3)</code> matches case 3, adds \"admin\", falls through to add \"write\" and \"read\". <code>Access(9)</code> matches no case and returns empty.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>fallthrough</code> transfers to the next case unconditionally, ignoring its condition.</li>\n<li>The last case cannot <code>fallthrough</code>.</li>\n</ul>",
    "starter": "// Package cascade \u2014 Gopher Workplace challenge.\npackage cascade\n\n// Access returns the comma-separated permissions granted at an access level,\n// highest first, using switch fallthrough so each higher level accumulates the\n// lower ones:\n//\n//\t3 => \"admin,write,read\"\n//\t2 => \"write,read\"\n//\t1 => \"read\"\n//\tanything else => \"\"\n//\n// Examples:\n//\n//\tAccess(3) => \"admin,write,read\"\n//\tAccess(2) => \"write,read\"\n//\tAccess(1) => \"read\"\n//\tAccess(0) => \"\"\nfunc Access(level int) string {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "cascade.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/junior/clampf": {
    "id": "01-language-basics/04-functions/junior/clampf",
    "title": "Clamp to Range",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Clamp to Range</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>Clamping keeps a value inside bounds \u2014 below the floor it becomes the floor,</p>\n<p>above the ceiling it becomes the ceiling.</p>\n<h2>Task</h2>\n<p>Implement <code>Clamp</code> in clampf.go with an inclusive range.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Clamp(99, 0, 10)\nOutput: 10\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Clamp(-5, 0, 10)\nOutput: 0\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Clamp(4, 0, 10)\nOutput: 4\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Conditional logic</span><span class=\"tchip\">Inclusive bounds</span><span class=\"tchip\">Single return</span></div></details>",
    "education": "<h1>Bounding a value</h1>\n<h2>Intuition</h2>\n<p>Clamping composes a lower and upper limit into one operation used across UI, graphics, and numeric guards.</p>\n<h2>Approach</h2>\n<p>1. Below <code>lo</code> \u2192 return <code>lo</code>.</p>\n<p>2. Above <code>hi</code> \u2192 return <code>hi</code>.</p>\n<p>3. Otherwise return <code>v</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Clamp(v, lo, hi int) int {\n\tif v &lt; lo {\n\t\treturn lo\n\t}\n\tif v &gt; hi {\n\t\treturn hi\n\t}\n\treturn v\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>Clamp(99, 0, 10)</code>: 99 exceeds the ceiling, so 10 is returned.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>With <code>lo &gt; hi</code> the range is empty; the task assumes <code>lo &lt;= hi</code>.</li>\n<li>Use inclusive comparisons so the endpoints are reachable.</li>\n</ul>",
    "starter": "// Package clampf \u2014 Gopher Workplace challenge.\npackage clampf\n\n// Clamp constrains v to the inclusive range [lo, hi].\nfunc Clamp(v, lo, hi int) int {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "clampf.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/junior/clamploop": {
    "id": "01-language-basics/04-functions/junior/clamploop",
    "title": "Clamp Each Element",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Clamp Each Element</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>Mapping a transform over a slice into a fresh result keeps the caller's data</p>\n<p>intact while a <code>for range</code> applies the per-element rule.</p>\n<h2>Task</h2>\n<p>Implement <code>ClampAll</code> in clamploop.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  ClampAll([\u22121 5 99], 0, 10)\nOutput: [0 5 10]\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  ClampAll([3], 0, 10)\nOutput: [3]\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  ClampAll(nil, 0, 10)\nOutput: []\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Map over a slice</span><span class=\"tchip\">Per-element clamp</span><span class=\"tchip\">Non-destructive</span></div></details>",
    "education": "<h1>Mapping a pure transform</h1>\n<h2>Intuition</h2>\n<p>Producing a new slice from an old one, one transformed element at a time, is the functional map pattern in imperative Go.</p>\n<h2>Approach</h2>\n<p>1. Range the slice.</p>\n<p>2. Clamp each value to [lo, hi] and append.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc ClampAll(xs []int, lo, hi int) []int {\n\tout := make([]int, 0, len(xs))\n\tfor _, v := range xs {\n\t\tif v &lt; lo {\n\t\t\tv = lo\n\t\t}\n\t\tif v &gt; hi {\n\t\t\tv = hi\n\t\t}\n\t\tout = append(out, v)\n\t}\n\treturn out\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>For <code>[-1 5 99]</code> with bounds 0..10: -1\u21920, 5 stays, 99\u219210.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Preallocating with <code>make([]int, 0, len(xs))</code> avoids regrowth but isn't required.</li>\n<li>Clamp each value before appending; don't mutate <code>xs[i]</code>.</li>\n</ul>",
    "starter": "// Package clamploop \u2014 Gopher Workplace challenge.\npackage clamploop\n\n// ClampAll returns a NEW slice where each element is clamped to [lo, hi].\n// The input is not modified.\nfunc ClampAll(xs []int, lo, hi int) []int {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "clamploop.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/junior/countdown": {
    "id": "01-language-basics/04-functions/junior/countdown",
    "title": "Countdown Slice",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Countdown Slice</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>A classic <code>for</code> loop with a decrementing counter builds a descending sequence.</p>\n<h2>Task</h2>\n<p>Implement <code>Countdown</code> in countdown.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Countdown(3)\nOutput: [3 2 1]\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Countdown(1)\nOutput: [1]\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Countdown(0)\nOutput: []\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Three-clause for</span><span class=\"tchip\">append</span><span class=\"tchip\">Empty case</span></div></details>",
    "education": "<h1>The classic three-clause for loop</h1>\n<h2>Intuition</h2>\n<p><code>for init; cond; post</code> is Go's only loop keyword; it covers counting up, down, and while-style conditions.</p>\n<h2>Approach</h2>\n<p>1. Loop <code>i</code> from <code>n</code> down to 1.</p>\n<p>2. Append each to the result.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Countdown(n int) []int {\n\tvar out []int\n\tfor i := n; i &gt;= 1; i-- {\n\t\tout = append(out, i)\n\t}\n\treturn out\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>Countdown(3)</code> appends 3, 2, 1.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Off-by-one: use <code>i &gt;= 1</code> to include 1 and stop before 0.</li>\n<li>Return a non-nil empty slice for <code>n &lt;= 0</code> (len 0 is enough here).</li>\n</ul>",
    "starter": "// Package countdown \u2014 Gopher Workplace challenge.\npackage countdown\n\n// Countdown returns n, n-1, ... 1. For n <= 0 it returns an empty slice.\nfunc Countdown(n int) []int {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "countdown.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/junior/countword": {
    "id": "01-language-basics/04-functions/junior/countword",
    "title": "Count Matching",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Count Matching</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>Counting matches is a loop with a conditional increment \u2014 the smallest useful</p>\n<p>filter.</p>\n<h2>Task</h2>\n<p>Implement <code>CountEqual</code> in countword.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  CountEqual([1 2 2 3 2], 2)\nOutput: 3\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  CountEqual([1 2 3], 9)\nOutput: 0\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  CountEqual(nil, 1)\nOutput: 0\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Loop + condition</span><span class=\"tchip\">Equality</span><span class=\"tchip\">Counter</span></div></details>",
    "education": "<h1>Conditional counting</h1>\n<h2>Intuition</h2>\n<p>A running counter incremented under an <code>if</code> inside a loop is the basis of filters, histograms, and predicates.</p>\n<h2>Approach</h2>\n<p>1. Range the slice.</p>\n<p>2. Count elements equal to <code>target</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc CountEqual(xs []int, target int) int {\n\tcount := 0\n\tfor _, v := range xs {\n\t\tif v == target {\n\t\t\tcount++\n\t\t}\n\t}\n\treturn count\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>[1 2 2 3 2]</code> has three 2s \u2192 3.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Return 0 for no matches, never -1 or a sentinel.</li>\n<li>A nil slice ranges zero times.</li>\n</ul>",
    "starter": "// Package countword \u2014 Gopher Workplace challenge.\npackage countword\n\n// CountEqual returns how many elements of xs equal target.\nfunc CountEqual(xs []int, target int) int {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "countword.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/junior/dayname": {
    "id": "01-language-basics/04-functions/junior/dayname",
    "title": "Day Name via Switch",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Day Name via Switch</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>An expression <code>switch</code> compares one value against many case constants \u2014 the</p>\n<p>natural fit for a small lookup.</p>\n<h2>Task</h2>\n<p>Implement <code>DayName</code> in dayname.go with a value switch and a default.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  DayName(0)\nOutput: \"Sun\"\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  DayName(6)\nOutput: \"Sat\"\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  DayName(9)\nOutput: \"?\"\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Expression switch</span><span class=\"tchip\">default</span><span class=\"tchip\">Multiple constants</span></div></details>",
    "education": "<h1>Value switch for lookups</h1>\n<h2>Intuition</h2>\n<p><code>switch x { case a: }</code> compares <code>x</code> against each case value and runs the first equal one; <code>default</code> handles the rest.</p>\n<h2>Approach</h2>\n<p>1. <code>switch d</code> over 0..6.</p>\n<p>2. <code>default</code> returns \"?\" for out-of-range.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc DayName(d int) string {\n\tswitch d {\n\tcase 0:\n\t\treturn \"Sun\"\n\tcase 1:\n\t\treturn \"Mon\"\n\tcase 2:\n\t\treturn \"Tue\"\n\tcase 3:\n\t\treturn \"Wed\"\n\tcase 4:\n\t\treturn \"Thu\"\n\tcase 5:\n\t\treturn \"Fri\"\n\tcase 6:\n\t\treturn \"Sat\"\n\tdefault:\n\t\treturn \"?\"\n\t}\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>DayName(0)</code> is \"Sun\"; 9 has no case, so the default \"?\".</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Provide a <code>default</code> so unexpected input has a defined result.</li>\n<li>Cases can group values: <code>case 0, 6:</code> for weekend logic.</li>\n</ul>",
    "starter": "// Package dayname \u2014 Gopher Workplace challenge.\npackage dayname\n\n// DayName maps 0..6 to \"Sun\"..\"Sat\"; any other value returns \"?\".\nfunc DayName(d int) string {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "dayname.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/junior/divmod": {
    "id": "01-language-basics/04-functions/junior/divmod",
    "title": "Quotient and Remainder",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Quotient and Remainder</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>Integer division and remainder are separate operators (<code>/</code> and <code>%</code>).</p>\n<p>Returning both at once is a classic use of multiple return values.</p>\n<h2>Task</h2>\n<p>Implement <code>DivMod</code> in divmod.go returning quotient then remainder.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  DivMod(17, 5)\nOutput: 3, 2\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  DivMod(10, 2)\nOutput: 5, 0\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  DivMod(4, 5)\nOutput: 0, 4\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Integer <code>/</code> and <code>%</code></span><span class=\"tchip\">Multiple return</span><span class=\"tchip\">Order matters</span></div></details>",
    "education": "<h1>Integer division and modulo</h1>\n<h2>Intuition</h2>\n<p>For integers <code>/</code> yields the truncated quotient and <code>%</code> the remainder; together they satisfy <code>a == (a/b)*b + a%b</code>.</p>\n<h2>Approach</h2>\n<p>1. Integer division <code>a / b</code> gives the quotient.</p>\n<p>2. <code>a % b</code> gives the remainder.</p>\n<p>3. Return both.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc DivMod(a, b int) (q, r int) {\n\treturn a / b, a % b\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>DivMod(17, 5)</code>: <code>17/5</code> is 3, <code>17%5</code> is 2.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Division truncates toward zero, so <code>-7/2 == -3</code> and <code>-7%2 == -1</code>.</li>\n<li>Dividing by zero panics; the task guarantees <code>b != 0</code>.</li>\n</ul>",
    "starter": "// Package divmod \u2014 Gopher Workplace challenge.\npackage divmod\n\n// DivMod returns the quotient and remainder of a divided by b (b != 0).\nfunc DivMod(a, b int) (q, r int) {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "divmod.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/junior/doublecopy": {
    "id": "01-language-basics/04-functions/junior/doublecopy",
    "title": "Value Copy Doesn't Leak",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Value Copy Doesn't Leak</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>Go passes arguments by value: the function gets its own copy of <code>price</code>, so</p>\n<p>arithmetic inside never touches the caller's variable.</p>\n<h2>Task</h2>\n<p>Implement <code>AddTax</code> in doublecopy.go: return <code>price + price*rate/100</code>.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  AddTax(100, 20)\nOutput: 120\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  AddTax(50, 0)\nOutput: 50\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  AddTax(200, 10)\nOutput: 220\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Call by value</span><span class=\"tchip\">Integer percent</span><span class=\"tchip\">No side effects</span></div></details>",
    "education": "<h1>Call by value</h1>\n<h2>Intuition</h2>\n<p>Go copies each argument into the parameter; mutating a non-pointer parameter cannot affect the caller.</p>\n<h2>Approach</h2>\n<p>1. Tax is <code>price*rate/100</code>.</p>\n<p>2. Add it to the price.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc AddTax(price int, rate int) int {\n\treturn price + price*rate/100\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>AddTax(100, 20)</code>: 100 + 100*20/100 = 120.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Multiply before dividing (<code>price*rate/100</code>) to avoid truncating rate/100 to 0.</li>\n<li>Slices/maps/pointers are the exceptions that DO share underlying data.</li>\n</ul>",
    "starter": "// Package doublecopy \u2014 Gopher Workplace challenge.\npackage doublecopy\n\n// AddTax returns price plus rate percent, rounded down. The caller's price\n// value is a copy and must stay usable afterward (it always is in Go).\nfunc AddTax(price int, rate int) int {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "doublecopy.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/junior/firstlast": {
    "id": "01-language-basics/04-functions/junior/firstlast",
    "title": "First, Last, OK",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">First, Last, OK</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>Returning an <code>ok bool</code> alongside the data lets the caller distinguish a real</p>\n<p>result from the empty case without a sentinel value or panic.</p>\n<h2>Task</h2>\n<p>Implement <code>FirstLast</code> in firstlast.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  FirstLast([1 2 3])\nOutput: 1, 3, true\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  FirstLast([9])\nOutput: 9, 9, true\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  FirstLast(nil)\nOutput: 0, 0, false\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Comma-ok style return</span><span class=\"tchip\">Empty guard</span><span class=\"tchip\">Index math</span></div></details>",
    "education": "<h1>The comma-ok return pattern</h1>\n<h2>Intuition</h2>\n<p>A trailing boolean result reports whether the other values are meaningful, mirroring map lookups and type assertions.</p>\n<h2>Approach</h2>\n<p>1. Guard the empty slice, returning <code>ok == false</code>.</p>\n<p>2. Otherwise return <code>xs[0]</code>, <code>xs[len-1]</code>, true.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc FirstLast(xs []int) (first, last int, ok bool) {\n\tif len(xs) == 0 {\n\t\treturn 0, 0, false\n\t}\n\treturn xs[0], xs[len(xs)-1], true\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>[1 2 3]</code> returns first 1, last 3, ok true; an empty slice returns zeros and false.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Reading <code>xs[0]</code> before the length guard panics on an empty slice.</li>\n<li>Return the zero values (<code>0, 0</code>) with <code>false</code>, not garbage.</li>\n</ul>",
    "starter": "// Package firstlast \u2014 Gopher Workplace challenge.\npackage firstlast\n\n// FirstLast returns the first and last element of xs and ok=true. For an\n// empty slice it returns 0, 0, false.\nfunc FirstLast(xs []int) (first, last int, ok bool) {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "firstlast.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/junior/fizzbuzz": {
    "id": "01-language-basics/04-functions/junior/fizzbuzz",
    "title": "FizzBuzz Line",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">FizzBuzz Line</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>The order of divisibility checks matters: test the combined case (both 3 and 5)</p>\n<p>before the single ones.</p>\n<h2>Task</h2>\n<p>Implement <code>FizzBuzz</code> in fizzbuzz.go. Use <code>strconv.Itoa</code> for the numeric case.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  FizzBuzz(15)\nOutput: \"FizzBuzz\"\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  FizzBuzz(9)\nOutput: \"Fizz\"\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  FizzBuzz(7)\nOutput: \"7\"\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">if / else-if ladder</span><span class=\"tchip\">Modulo test</span><span class=\"tchip\">strconv.Itoa</span></div></details>",
    "education": "<h1>Ordering conditional branches</h1>\n<h2>Intuition</h2>\n<p>When cases overlap, the most specific condition must come first or a broader branch will shadow it.</p>\n<h2>Approach</h2>\n<p>1. Test <code>n%15</code> first (both), then <code>n%3</code>, then <code>n%5</code>.</p>\n<p>2. Otherwise return the number as a string.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nimport \"strconv\"\n\nvar _ = strconv.Itoa // keep import; use it in your solution\n\nfunc FizzBuzz(n int) string {\n\tswitch {\n\tcase n%15 == 0:\n\t\treturn \"FizzBuzz\"\n\tcase n%3 == 0:\n\t\treturn \"Fizz\"\n\tcase n%5 == 0:\n\t\treturn \"Buzz\"\n\tdefault:\n\t\treturn strconv.Itoa(n)\n\t}\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>FizzBuzz(15)</code> is divisible by both 3 and 5, so \"FizzBuzz\"; 7 divides neither, so \"7\".</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Checking <code>%3</code> before <code>%15</code> prints \"Fizz\" for 15 \u2014 wrong.</li>\n<li><code>strconv.Itoa</code> (not string(n)) converts the integer to its decimal text.</li>\n</ul>",
    "starter": "// Package fizzbuzz \u2014 Gopher Workplace challenge.\npackage fizzbuzz\n\nimport \"strconv\"\n\nvar _ = strconv.Itoa // keep import; use it in your solution\n\n// FizzBuzz returns \"Fizz\" if n divisible by 3, \"Buzz\" if by 5, \"FizzBuzz\"\n// if by both, otherwise the number as a decimal string.\nfunc FizzBuzz(n int) string {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "fizzbuzz.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/junior/gcdloop": {
    "id": "01-language-basics/04-functions/junior/gcdloop",
    "title": "Euclidean GCD",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Euclidean GCD</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>The Euclidean algorithm repeatedly replaces <code>(a, b)</code> with <code>(b, a % b)</code> until <code>b</code> is zero; then <code>a</code> is the greatest common divisor.</p>\n<h2>Task</h2>\n<p>Implement <code>GCD</code> in gcdloop.go using a loop.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  GCD(12, 8)\nOutput: 4\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  GCD(17, 5)\nOutput: 1\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  GCD(0, 9)\nOutput: 9\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">for loop</span><span class=\"tchip\">parallel assignment</span><span class=\"tchip\">modulo</span></div></details>",
    "education": "<h1>Euclidean algorithm</h1>\n<h2>Intuition</h2>\n<p>gcd(a,b) equals gcd(b, a mod b); iterating shrinks the pair until the remainder is 0.</p>\n<h2>Approach</h2>\n<p>1. Loop while <code>b != 0</code>.</p>\n<p>2. Replace <code>(a, b)</code> with <code>(b, a % b)</code>.</p>\n<p>3. Return <code>a</code> when <code>b</code> reaches 0.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc GCD(a, b int) int {\n\tfor b != 0 {\n\t\ta, b = b, a%b\n\t}\n\treturn a\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>GCD(12, 8)</code>: (12,8)\u2192(8,4)\u2192(4,0), so the answer is 4.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>gcd(0, n) is n \u2014 the loop handles it since b becomes 0 immediately.</li>\n<li>Parallel assignment avoids a temporary.</li>\n</ul>",
    "starter": "// Package gcdloop \u2014 Gopher Workplace challenge.\npackage gcdloop\n\n// GCD returns the greatest common divisor of a and b using the Euclidean\n// algorithm (a, b >= 0, not both zero).\nfunc GCD(a, b int) int {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "gcdloop.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/junior/grade": {
    "id": "01-language-basics/04-functions/junior/grade",
    "title": "Letter Grade Ladder",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Letter Grade Ladder</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>An if / else-if / else ladder maps a numeric score to a letter, testing the highest threshold first so boundaries stay inclusive.</p>\n<h2>Task</h2>\n<p>Implement <code>Grade</code> in grade.go mapping 0\u2013100 to A\u2013F.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Grade(95)\nOutput: \"A\"\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Grade(70)\nOutput: \"C\"\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Grade(59)\nOutput: \"F\"\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">if/else-if ladder</span><span class=\"tchip\">inclusive bounds</span><span class=\"tchip\">fallthrough default</span></div></details>",
    "education": "<h1>if/else-if ladder</h1>\n<h2>Intuition</h2>\n<p>Ordered threshold checks classify a value; testing highest first keeps each band correct.</p>\n<h2>Approach</h2>\n<p>1. Test the highest threshold first.</p>\n<p>2. Use <code>&gt;=</code> for inclusive boundaries.</p>\n<p>3. Fall to \"F\" below 60.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Grade(score int) string {\n\tswitch {\n\tcase score &gt;= 90:\n\t\treturn \"A\"\n\tcase score &gt;= 80:\n\t\treturn \"B\"\n\tcase score &gt;= 70:\n\t\treturn \"C\"\n\tcase score &gt;= 60:\n\t\treturn \"D\"\n\tdefault:\n\t\treturn \"F\"\n\t}\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>Grade(70)</code> fails <code>&gt;= 90</code> and <code>&gt;= 80</code> but passes <code>&gt;= 70</code> \u2192 \"C\".</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Order matters: a low threshold first would swallow higher scores.</li>\n<li>Use <code>&gt;=</code> for inclusive boundaries.</li>\n</ul>",
    "starter": "// Package grade \u2014 Gopher Workplace challenge.\npackage grade\n\n// Grade maps a 0\u2013100 score to a letter using an if / else-if / else ladder:\n// 90+ => \"A\", 80+ => \"B\", 70+ => \"C\", 60+ => \"D\", below 60 => \"F\". Boundaries\n// are inclusive, so exactly 90 is an \"A\".\n//\n// Examples:\n//\n//\tGrade(95) => \"A\"\n//\tGrade(90) => \"A\"\n//\tGrade(72) => \"C\"\n//\tGrade(59) => \"F\"\nfunc Grade(score int) string {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "grade.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/junior/gradeif": {
    "id": "01-language-basics/04-functions/junior/gradeif",
    "title": "Even/Odd Label",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Even/Odd Label</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>Testing evenness for negative numbers needs <code>== 0</code>, not <code>== 1</code>, since <code>-3 % 2</code></p>\n<p>is <code>-1</code> in Go.</p>\n<h2>Task</h2>\n<p>Implement <code>Parity</code> in gradeif.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Parity(4)\nOutput: \"even\"\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Parity(7)\nOutput: \"odd\"\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Parity(0)\nOutput: \"even\"\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">if / else</span><span class=\"tchip\">Signed modulo</span><span class=\"tchip\">Return strings</span></div></details>",
    "education": "<h1>Modulo with signed integers</h1>\n<h2>Intuition</h2>\n<p>Go's <code>%</code> keeps the sign of the dividend, so <code>-3 % 2 == -1</code>; test evenness with <code>== 0</code>, never <code>== 1</code>.</p>\n<h2>Approach</h2>\n<p>1. <code>n % 2 == 0</code> \u2192 even, else odd.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Parity(n int) string {\n\tif n%2 == 0 {\n\t\treturn \"even\"\n\t}\n\treturn \"odd\"\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>Parity(4)</code>: 4 mod 2 is 0 \u2192 \"even\".</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>-3 % 2</code> is <code>-1</code>, not <code>1</code>; only <code>== 0</code> is reliable for parity.</li>\n<li>0 is even.</li>\n</ul>",
    "starter": "// Package gradeif \u2014 Gopher Workplace challenge.\npackage gradeif\n\n// Parity returns \"even\" or \"odd\" for n (works for negatives too).\nfunc Parity(n int) string {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "gradeif.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/junior/gridsum": {
    "id": "01-language-basics/04-functions/junior/gridsum",
    "title": "2D Grid Sum",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">2D Grid Sum</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>Nested <code>for range</code> loops walk a grid row by row; rows may have different lengths.</p>\n<h2>Task</h2>\n<p>Implement <code>GridSum</code> in gridsum.go summing every cell.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  GridSum([[1 2],[3 4]])\nOutput: 10\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  GridSum([[5]])\nOutput: 5\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  GridSum(nil)\nOutput: 0\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Nested loops</span><span class=\"tchip\">Ragged rows</span><span class=\"tchip\">Accumulator</span></div></details>",
    "education": "<h1>Nested iteration over 2D data</h1>\n<h2>Intuition</h2>\n<p>A slice of slices is ragged; nested <code>range</code> visits each element without assuming a fixed width.</p>\n<h2>Approach</h2>\n<p>1. Range the rows, then range each row's cells.</p>\n<p>2. Accumulate every cell.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc GridSum(g [][]int) int {\n\ttotal := 0\n\tfor _, row := range g {\n\t\tfor _, c := range row {\n\t\t\ttotal += c\n\t\t}\n\t}\n\treturn total\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>[[1 2],[3 4]]</code> sums 1+2+3+4 = 10.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Don't assume all rows share a length; range each row independently.</li>\n<li>A nil or empty grid ranges zero times.</li>\n</ul>",
    "starter": "// Package gridsum \u2014 Gopher Workplace challenge.\npackage gridsum\n\n// GridSum returns the total of every cell in the 2D grid.\nfunc GridSum(g [][]int) int {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "gridsum.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/junior/ifinit": {
    "id": "01-language-basics/04-functions/junior/ifinit",
    "title": "If with Init Clause",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">If with Init Clause</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>An <code>if</code> init clause (<code>if r := n % 3; ...</code>) computes a value once and scopes it to the if/else chain.</p>\n<h2>Task</h2>\n<p>Implement <code>Bucket</code> in ifinit.go classifying <code>n</code> by <code>n % 3</code>.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Bucket(9)\nOutput: \"zero\"\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Bucket(10)\nOutput: \"one\"\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Bucket(11)\nOutput: \"two\"\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">if init clause</span><span class=\"tchip\">modulo</span><span class=\"tchip\">scoped variable</span></div></details>",
    "education": "<h1>if init statement</h1>\n<h2>Intuition</h2>\n<p>The init clause runs once and binds a variable scoped to the whole if/else chain, keeping helpers local.</p>\n<h2>Approach</h2>\n<p>1. Compute <code>r := n % 3</code> in the if init clause.</p>\n<p>2. Branch on <code>r</code> being 0, 1, or 2.</p>\n<p>3. <code>r</code> is scoped to the if/else chain.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Bucket(n int) string {\n\tif r := n % 3; r == 0 {\n\t\treturn \"zero\"\n\t} else if r == 1 {\n\t\treturn \"one\"\n\t} else {\n\t\treturn \"two\"\n\t}\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>Bucket(10)</code>: <code>r</code> is 1, so the middle branch returns \"one\".</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>r</code> is not visible after the if/else ends.</li>\n<li>The remainder is computed a single time.</li>\n</ul>",
    "starter": "// Package ifinit \u2014 Gopher Workplace challenge.\npackage ifinit\n\n// Bucket classifies a non-negative n by its remainder modulo 3, using an if\n// statement with an init clause (`if r := n % 3; ...`) so the remainder is\n// computed once and scoped to the if/else chain.\n//\n//\tremainder 0 => \"zero\"\n//\tremainder 1 => \"one\"\n//\tremainder 2 => \"two\"\n//\n// Examples:\n//\n//\tBucket(9)  => \"zero\"\n//\tBucket(10) => \"one\"\n//\tBucket(11) => \"two\"\n//\tBucket(0)  => \"zero\"\nfunc Bucket(n int) string {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "ifinit.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/junior/joinv": {
    "id": "01-language-basics/04-functions/junior/joinv",
    "title": "Variadic Join",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Variadic Join</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>A fixed leading parameter can precede a variadic one. Here <code>sep</code> is fixed and</p>\n<p><code>parts</code> collects the rest.</p>\n<h2>Task</h2>\n<p>Implement <code>Join</code> in joinv.go placing <code>sep</code> between consecutive parts only.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Join(\",\", \"a\", \"b\", \"c\")\nOutput: \"a,b,c\"\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Join(\"-\", \"x\")\nOutput: \"x\"\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Join(\",\")\nOutput: \"\"\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Fixed + variadic params</span><span class=\"tchip\">Separator placement</span><span class=\"tchip\">Empty and single cases</span></div></details>",
    "education": "<h1>Mixing fixed and variadic parameters</h1>\n<h2>Intuition</h2>\n<p>Only the last parameter may be variadic; fixed parameters bind positionally before it.</p>\n<h2>Approach</h2>\n<p>1. Add the separator before every part except the first.</p>\n<p>2. Concatenate into the result.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Join(sep string, parts ...string) string {\n\tresult := \"\"\n\tfor i, p := range parts {\n\t\tif i &gt; 0 {\n\t\t\tresult += sep\n\t\t}\n\t\tresult += p\n\t}\n\treturn result\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>Join(\",\", \"a\",\"b\",\"c\")</code> starts with \"a\", then \",b\", then \",c\" \u2192 \"a,b,c\".</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Prepending <code>sep</code> unconditionally leaks a leading separator.</li>\n<li>With zero parts the loop never runs and <code>\"\"</code> is correct.</li>\n</ul>",
    "starter": "// Package joinv \u2014 Gopher Workplace challenge.\npackage joinv\n\n// Join concatenates parts with sep between each. No arguments yields \"\".\nfunc Join(sep string, parts ...string) string {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "joinv.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/junior/leapyear": {
    "id": "01-language-basics/04-functions/junior/leapyear",
    "title": "Leap Year",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Leap Year</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>The leap-year rule is a layered condition: the 400 exception overrides the 100</p>\n<p>exception, which overrides the base 4 rule.</p>\n<h2>Task</h2>\n<p>Implement <code>IsLeap</code> in leapyear.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  IsLeap(2000)\nOutput: true\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  IsLeap(1900)\nOutput: false\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  IsLeap(2024)\nOutput: true\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Boolean composition</span><span class=\"tchip\">Rule precedence</span><span class=\"tchip\">Return a bool</span></div></details>",
    "education": "<h1>Composing layered rules</h1>\n<h2>Intuition</h2>\n<p>Nested exceptions collapse into a single boolean expression when you order the divisibility tests by precedence.</p>\n<h2>Approach</h2>\n<p>1. Divisible by 400 \u2192 leap.</p>\n<p>2. Divisible by 4 but not 100 \u2192 leap.</p>\n<p>3. Otherwise not.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc IsLeap(y int) bool {\n\treturn y%400 == 0 || (y%4 == 0 &amp;&amp; y%100 != 0)\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>2000 is divisible by 400 \u2192 leap; 1900 is divisible by 100 but not 400 \u2192 not leap.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>1900 is divisible by 4 and 100 but not 400, so it is NOT a leap year.</li>\n<li>Parenthesise to keep <code>&amp;&amp;</code> binding tighter than <code>||</code>.</li>\n</ul>",
    "starter": "// Package leapyear \u2014 Gopher Workplace challenge.\npackage leapyear\n\n// IsLeap reports whether y is a Gregorian leap year: divisible by 4, except\n// centuries which must be divisible by 400.\nfunc IsLeap(y int) bool {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "leapyear.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/junior/max3": {
    "id": "01-language-basics/04-functions/junior/max3",
    "title": "Max of Three",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Max of Three</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>Combining comparisons is the bread-and-butter of small helper functions.</p>\n<h2>Task</h2>\n<p>Implement <code>Max3</code> in max3.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Max3(1, 2, 3)\nOutput: 3\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Max3(9, 1, 5)\nOutput: 9\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Max3(4, 4, 4)\nOutput: 4\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Comparison chaining</span><span class=\"tchip\">Negatives</span><span class=\"tchip\">Single result</span></div></details>",
    "education": "<h1>Reducing several values</h1>\n<h2>Intuition</h2>\n<p>A running accumulator seeded from the first input generalises to any number of comparisons.</p>\n<h2>Approach</h2>\n<p>1. Start <code>m := a</code>.</p>\n<p>2. Raise <code>m</code> if <code>b</code> or <code>c</code> is larger.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Max3(a, b, c int) int {\n\tm := a\n\tif b &gt; m {\n\t\tm = b\n\t}\n\tif c &gt; m {\n\t\tm = c\n\t}\n\treturn m\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>Max3(1,2,3)</code>: m starts 1, becomes 2, then 3.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Seeding <code>m := 0</code> fails when every argument is negative.</li>\n<li>Use <code>&gt;</code> (or <code>&gt;=</code>) consistently.</li>\n</ul>",
    "starter": "// Package max3 \u2014 Gopher Workplace challenge.\npackage max3\n\n// Max3 returns the largest of its three arguments.\nfunc Max3(a, b, c int) int {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "max3.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/junior/minmax": {
    "id": "01-language-basics/04-functions/junior/minmax",
    "title": "Min and Max",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Min and Max</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>A Go function can return more than one value. Scanning a slice once and</p>\n<p>returning both extremes is cheaper and clearer than two passes.</p>\n<h2>Task</h2>\n<p>Implement <code>MinMax</code> in minmax.go so it returns the smallest and</p>\n<p>largest element of <code>xs</code> in one pass.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  MinMax([3 -1 7])\nOutput: -1, 7\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  MinMax([5])\nOutput: 5, 5\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  MinMax([2 2 2])\nOutput: 2, 2\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Multiple return values</span><span class=\"tchip\">Seed from first element</span><span class=\"tchip\">Single pass</span></div></details>",
    "education": "<h1>Multiple return values</h1>\n<h2>Intuition</h2>\n<p>A function's result list can hold several typed values, returned as a tuple and destructured at the call site.</p>\n<h2>Approach</h2>\n<p>1. Seed both <code>min</code> and <code>max</code> from <code>xs[0]</code>.</p>\n<p>2. Scan the rest, lowering <code>min</code> and raising <code>max</code>.</p>\n<p>3. Return the named results.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc MinMax(xs []int) (min, max int) {\n\tmin, max = xs[0], xs[0]\n\tfor _, v := range xs[1:] {\n\t\tif v &lt; min {\n\t\t\tmin = v\n\t\t}\n\t\tif v &gt; max {\n\t\t\tmax = v\n\t\t}\n\t}\n\treturn\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>For <code>[3 -1 7]</code>: seed 3,3; <code>-1</code> drops min to -1; <code>7</code> raises max to 7.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Seeding <code>min</code> to <code>0</code> breaks on all-positive slices.</li>\n<li>Named results are zero-initialised; you still must set them.</li>\n</ul>",
    "starter": "// Package minmax \u2014 Gopher Workplace challenge.\npackage minmax\n\n// MinMax returns the smallest and largest value in xs. xs is non-empty.\nfunc MinMax(xs []int) (min, max int) {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "minmax.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/junior/nocopysliceval": {
    "id": "01-language-basics/04-functions/junior/nocopysliceval",
    "title": "Sum Without Mutating",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Sum Without Mutating</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>A slice header is passed by value, but it points at a shared backing array \u2014</p>\n<p>so reading is safe, yet writing to elements would leak to the caller.</p>\n<h2>Task</h2>\n<p>Implement <code>SumKeep</code> in nocopysliceval.go: total the elements, changing none.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  SumKeep([1 2 3])\nOutput: 6\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  SumKeep(nil)\nOutput: 0\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  SumKeep([-2 2])\nOutput: 0\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Slice header copy</span><span class=\"tchip\">Read-only traversal</span><span class=\"tchip\">Named result</span></div></details>",
    "education": "<h1>Slices are references under a value header</h1>\n<h2>Intuition</h2>\n<p>Passing a slice copies its (ptr,len,cap) header but not the backing array, so element writes are visible to the caller \u2014 reads are always safe.</p>\n<h2>Approach</h2>\n<p>1. Range and add each value into the named <code>sum</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc SumKeep(xs []int) (sum int) {\n\tfor _, v := range xs {\n\t\tsum += v\n\t}\n\treturn\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>SumKeep([1 2 3])</code> accumulates to 6.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Don't sort or assign into <code>xs[i]</code> \u2014 that would mutate the caller's array.</li>\n<li>The named return <code>sum</code> starts at 0.</li>\n</ul>",
    "starter": "// Package nocopysliceval \u2014 Gopher Workplace challenge.\npackage nocopysliceval\n\n// SumKeep returns the total of xs without modifying the slice's elements.\nfunc SumKeep(xs []int) (sum int) {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "nocopysliceval.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/junior/reverseloop": {
    "id": "01-language-basics/04-functions/junior/reverseloop",
    "title": "Reverse via Index Loop",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Reverse via Index Loop</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>Walking an index from the end builds a reversed copy without touching the input.</p>\n<h2>Task</h2>\n<p>Implement <code>Reverse</code> in reverseloop.go returning a new slice.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Reverse([1 2 3])\nOutput: [3 2 1]\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Reverse([1])\nOutput: [1]\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Reverse(nil)\nOutput: []\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Descending index loop</span><span class=\"tchip\">Build new slice</span><span class=\"tchip\">Immutability</span></div></details>",
    "education": "<h1>Index-based reversal</h1>\n<h2>Intuition</h2>\n<p>Reading source back-to-front into a new slice keeps the original intact \u2014 a copy, not an in-place swap.</p>\n<h2>Approach</h2>\n<p>1. Loop from the last index down to 0.</p>\n<p>2. Append each element to a fresh slice.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Reverse(xs []int) []int {\n\tout := make([]int, 0, len(xs))\n\tfor i := len(xs) - 1; i &gt;= 0; i-- {\n\t\tout = append(out, xs[i])\n\t}\n\treturn out\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>Reverse([1 2 3])</code> appends 3, 2, 1 in that order.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Reversing in place would mutate the caller's slice (shared backing array).</li>\n<li>Start the index at <code>len(xs)-1</code>, end at <code>&gt;= 0</code>.</li>\n</ul>",
    "starter": "// Package reverseloop \u2014 Gopher Workplace challenge.\npackage reverseloop\n\n// Reverse returns a new slice with the elements of xs in reverse order.\n// The input is not modified.\nfunc Reverse(xs []int) []int {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "reverseloop.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/junior/signswitch": {
    "id": "01-language-basics/04-functions/junior/signswitch",
    "title": "Sign via Switch",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Sign via Switch</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>A tagless <code>switch</code> (no expression after <code>switch</code>) reads like an if-else ladder,</p>\n<p>matching the first true case.</p>\n<h2>Task</h2>\n<p>Implement <code>Sign</code> in signswitch.go using a switch.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Sign(-4)\nOutput: \"negative\"\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Sign(0)\nOutput: \"zero\"\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Sign(7)\nOutput: \"positive\"\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Tagless switch</span><span class=\"tchip\">First match wins</span><span class=\"tchip\">No fallthrough</span></div></details>",
    "education": "<h1>Tagless switch as an if-ladder</h1>\n<h2>Intuition</h2>\n<p><code>switch {}</code> with boolean case expressions is a clean alternative to long if/else-if chains; Go breaks after the first match automatically.</p>\n<h2>Approach</h2>\n<p>1. A tagless <code>switch</code> tests boolean cases in order.</p>\n<p>2. Negative, zero, else positive.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Sign(n int) string {\n\tswitch {\n\tcase n &lt; 0:\n\t\treturn \"negative\"\n\tcase n == 0:\n\t\treturn \"zero\"\n\tdefault:\n\t\treturn \"positive\"\n\t}\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>Sign(-4)</code> matches <code>n &lt; 0</code> \u2192 \"negative\".</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Cases evaluate top-down; put narrower conditions first if they overlap.</li>\n<li>Go does NOT fall through unless you write <code>fallthrough</code>.</li>\n</ul>",
    "starter": "// Package signswitch \u2014 Gopher Workplace challenge.\npackage signswitch\n\n// Sign returns \"negative\", \"zero\", or \"positive\".\nfunc Sign(n int) string {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "signswitch.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/junior/sumrange": {
    "id": "01-language-basics/04-functions/junior/sumrange",
    "title": "Sum with For-Range",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Sum with For-Range</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p><code>for range</code> walks a slice yielding index and value; ignore the index with <code>_</code></p>\n<p>when only the value matters.</p>\n<h2>Task</h2>\n<p>Implement <code>SumRange</code> in sumrange.go using a for-range loop.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  SumRange([1 2 3 4])\nOutput: 10\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  SumRange(nil)\nOutput: 0\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  SumRange([-1 1])\nOutput: 0\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">for-range</span><span class=\"tchip\">Blank identifier</span><span class=\"tchip\">Accumulator</span></div></details>",
    "education": "<h1>The for-range loop</h1>\n<h2>Intuition</h2>\n<p><code>range</code> over a slice returns <code>(index, value)</code> copies; discarding the index with <code>_</code> gives a clean value iteration.</p>\n<h2>Approach</h2>\n<p>1. Range and accumulate each element.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc SumRange(xs []int) int {\n\ttotal := 0\n\tfor _, v := range xs {\n\t\ttotal += v\n\t}\n\treturn total\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>SumRange([1 2 3 4])</code> adds to 10.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>v</code> is a copy; writing to it does not change the slice.</li>\n<li>A nil slice ranges zero times \u2014 no special case needed.</li>\n</ul>",
    "starter": "// Package sumrange \u2014 Gopher Workplace challenge.\npackage sumrange\n\n// SumRange returns the total of all elements using a for-range loop.\nfunc SumRange(xs []int) int {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "sumrange.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/junior/sumv": {
    "id": "01-language-basics/04-functions/junior/sumv",
    "title": "Variadic Sum",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Variadic Sum</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>A variadic parameter <code>nums ...int</code> collects any number of trailing arguments</p>\n<p>into a slice. A caller can also spread an existing slice with <code>xs...</code>.</p>\n<h2>Task</h2>\n<p>Implement <code>Sum</code> in sumv.go so it adds up every argument.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Sum(1, 2, 3, 4)\nOutput: 10\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Sum()\nOutput: 0\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Sum(-1, 1)\nOutput: 0\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Variadic parameter</span><span class=\"tchip\">Zero arguments</span><span class=\"tchip\">Spread operator</span></div></details>",
    "education": "<h1>Variadic functions</h1>\n<h2>Intuition</h2>\n<p>The final parameter written <code>...T</code> receives a <code>[]T</code> built from the trailing call arguments; passing <code>slice...</code> forwards an existing slice instead of copying element by element.</p>\n<h2>Approach</h2>\n<p>1. A variadic <code>nums ...int</code> arrives as a slice.</p>\n<p>2. Range and accumulate from 0.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Sum(nums ...int) int {\n\ttotal := 0\n\tfor _, v := range nums {\n\t\ttotal += v\n\t}\n\treturn total\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>Sum(1,2,3,4)</code> iterates the four values adding to a total of 10; no arguments yields 0.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Inside the function the parameter is a real slice; a nil/empty one is valid.</li>\n<li>You can pass either loose args OR one spread slice, never both.</li>\n</ul>",
    "starter": "// Package sumv \u2014 Gopher Workplace challenge.\npackage sumv\n\n// Sum returns the total of all arguments. Sum() with no arguments is 0.\nfunc Sum(nums ...int) int {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "sumv.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/junior/swap2": {
    "id": "01-language-basics/04-functions/junior/swap2",
    "title": "Swap Two",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Swap Two</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>Returning two values makes a swap a pure expression \u2014 no temporary variable,</p>\n<p>no mutation of the caller's data.</p>\n<h2>Task</h2>\n<p>Implement <code>Swap</code> in swap2.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Swap(1, 2)\nOutput: 2, 1\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Swap(5, 5)\nOutput: 5, 5\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Swap(-1, 9)\nOutput: 9, -1\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Multiple return</span><span class=\"tchip\">No temp needed</span><span class=\"tchip\">Value semantics</span></div></details>",
    "education": "<h1>Returning a tuple</h1>\n<h2>Intuition</h2>\n<p>A function returning <code>(int, int)</code> lets callers do <code>x, y = Swap(x, y)</code> \u2014 Go evaluates the right side fully before assigning.</p>\n<h2>Approach</h2>\n<p>1. Return the arguments in reverse order: <code>return b, a</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Swap(a, b int) (int, int) {\n\treturn b, a\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>Swap(1, 2)</code> hands back <code>2, 1</code> \u2014 multiple return values make this a one-liner.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>The function copies its arguments; it cannot mutate the caller's variables.</li>\n<li>Order in the <code>return</code> must match the signature.</li>\n</ul>",
    "starter": "// Package swap2 \u2014 Gopher Workplace challenge.\npackage swap2\n\n// Swap returns its two arguments in reversed order.\nfunc Swap(a, b int) (int, int) {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "swap2.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/junior/tagless": {
    "id": "01-language-basics/04-functions/junior/tagless",
    "title": "Tagless Switch Sign",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Tagless Switch Sign</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>A tagless <code>switch</code> (no expression) evaluates boolean cases in order \u2014 a clean if/else-if chain.</p>\n<h2>Task</h2>\n<p>Implement <code>Classify</code> in tagless.go returning the sign of <code>n</code>.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Classify(-5)\nOutput: \"negative\"\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Classify(0)\nOutput: \"zero\"\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Classify(7)\nOutput: \"positive\"\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">tagless switch</span><span class=\"tchip\">ordered cases</span><span class=\"tchip\">sign logic</span></div></details>",
    "education": "<h1>tagless switch</h1>\n<h2>Intuition</h2>\n<p>A switch with no tag tests each case's boolean condition in order, reading like an if/else-if ladder.</p>\n<h2>Approach</h2>\n<p>1. Use a <code>switch</code> with no tag.</p>\n<p>2. Each case is a boolean condition, tested in order.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Classify(n int) string {\n\tswitch {\n\tcase n &lt; 0:\n\t\treturn \"negative\"\n\tcase n == 0:\n\t\treturn \"zero\"\n\tdefault:\n\t\treturn \"positive\"\n\t}\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>Classify(-5)</code> matches <code>n &lt; 0</code> first \u2192 \"negative\".</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Cases are evaluated top to bottom; the first true one runs.</li>\n<li><code>default</code> covers the remaining case.</li>\n</ul>",
    "starter": "// Package tagless \u2014 Gopher Workplace challenge.\npackage tagless\n\n// Classify returns the sign of n using a tagless switch (a switch with no\n// expression, whose cases are boolean conditions \u2014 a clean if/else-if chain).\n//\n//\tn < 0  => \"negative\"\n//\tn == 0 => \"zero\"\n//\tn > 0  => \"positive\"\n//\n// Examples:\n//\n//\tClassify(-5) => \"negative\"\n//\tClassify(0)  => \"zero\"\n//\tClassify(7)  => \"positive\"\nfunc Classify(n int) string {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "tagless.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/junior/tally": {
    "id": "01-language-basics/04-functions/junior/tally",
    "title": "Count and Sum Evens",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Count and Sum Evens</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>A single pass can compute several aggregates at once, returned together.</p>\n<h2>Task</h2>\n<p>Implement <code>EvenStats</code> in tally.go: count the even arguments and sum them.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  EvenStats(1, 2, 3, 4)\nOutput: 2, 6\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  EvenStats(1, 3, 5)\nOutput: 0, 0\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  EvenStats()\nOutput: 0, 0\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Variadic input</span><span class=\"tchip\">Even test</span><span class=\"tchip\">Two accumulators</span></div></details>",
    "education": "<h1>Computing multiple aggregates in one pass</h1>\n<h2>Intuition</h2>\n<p>Accumulating several results in a single loop avoids re-scanning and keeps related outputs in sync.</p>\n<h2>Approach</h2>\n<p>1. Range the variadic args.</p>\n<p>2. For each even value, bump <code>count</code> and add to <code>total</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc EvenStats(xs ...int) (count, total int) {\n\tfor _, x := range xs {\n\t\tif x%2 == 0 {\n\t\t\tcount++\n\t\t\ttotal += x\n\t\t}\n\t}\n\treturn\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>EvenStats(1,2,3,4)</code>: 2 and 4 are even, so count 2 and total 6.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>n%2 != 0</code> for negative odds too (<code>-3%2 == -1</code>); evenness test still holds.</li>\n<li>Both accumulators start at 0.</li>\n</ul>",
    "starter": "// Package tally \u2014 Gopher Workplace challenge.\npackage tally\n\n// EvenStats returns how many arguments are even and their sum.\nfunc EvenStats(xs ...int) (count, total int) {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "tally.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/junior/weekday": {
    "id": "01-language-basics/04-functions/junior/weekday",
    "title": "Weekday Name",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Weekday Name</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>A <code>switch</code> on a value maps each case to a result, with <code>default</code> handling everything out of range.</p>\n<h2>Task</h2>\n<p>Implement <code>Weekday</code> in weekday.go mapping 1\u20137 to day names.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Weekday(1)\nOutput: \"Monday\"\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Weekday(7)\nOutput: \"Sunday\"\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Weekday(8)\nOutput: \"Unknown\"\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">expression switch</span><span class=\"tchip\">default case</span><span class=\"tchip\">value mapping</span></div></details>",
    "education": "<h1>expression switch</h1>\n<h2>Intuition</h2>\n<p>Switching on a value selects the matching case; <code>default</code> catches unmatched inputs cleanly.</p>\n<h2>Approach</h2>\n<p>1. Switch on <code>d</code> with a case per day 1\u20137.</p>\n<p>2. <code>default</code> returns \"Unknown\".</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Weekday(d int) string {\n\tswitch d {\n\tcase 1:\n\t\treturn \"Monday\"\n\tcase 2:\n\t\treturn \"Tuesday\"\n\tcase 3:\n\t\treturn \"Wednesday\"\n\tcase 4:\n\t\treturn \"Thursday\"\n\tcase 5:\n\t\treturn \"Friday\"\n\tcase 6:\n\t\treturn \"Saturday\"\n\tcase 7:\n\t\treturn \"Sunday\"\n\tdefault:\n\t\treturn \"Unknown\"\n\t}\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>Weekday(1)</code> matches case 1 \u2192 \"Monday\"; 8 has no case, so \"Unknown\".</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Go cases don't fall through by default.</li>\n<li><code>default</code> handles 0, 8, negatives.</li>\n</ul>",
    "starter": "// Package weekday \u2014 Gopher Workplace challenge.\npackage weekday\n\n// Weekday maps a day number 1\u20137 (Monday=1 \u2026 Sunday=7) to its name using a\n// switch. Any number outside 1\u20137 returns \"Unknown\".\n//\n// Examples:\n//\n//\tWeekday(1) => \"Monday\"\n//\tWeekday(7) => \"Sunday\"\n//\tWeekday(0) => \"Unknown\"\n//\tWeekday(8) => \"Unknown\"\nfunc Weekday(d int) string {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "weekday.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/middle/adder": {
    "id": "01-language-basics/04-functions/middle/adder",
    "title": "Adder Factory",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Adder Factory</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>A closure can capture a parameter of its factory, freezing it into the returned</p>\n<p>function \u2014 partial application.</p>\n<h2>Task</h2>\n<p>Implement <code>Adder</code> in adder.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  add5 := Adder(5); add5(3)\nOutput: 8\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Adder(0)(9)\nOutput: 9\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Adder(-2)(2)\nOutput: 0\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Capture a parameter</span><span class=\"tchip\">Partial application</span><span class=\"tchip\">Returned func type</span></div></details>",
    "education": "<h1>Partial application via closures</h1>\n<h2>Intuition</h2>\n<p>Capturing a factory parameter produces specialised functions without extra state types.</p>\n<h2>Approach</h2>\n<p>1. Capture <code>base</code> in a closure.</p>\n<p>2. Return <code>func(x int) int { return base + x }</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Adder(base int) func(int) int {\n\treturn func(x int) int {\n\t\treturn base + x\n\t}\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>Adder(5)</code> returns a function that always adds 5, so <code>add5(3)</code> is 8.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>base</code> is captured by reference but never mutated here, so each Adder is stable.</li>\n<li>The returned type must exactly match <code>func(int) int</code>.</li>\n</ul>",
    "starter": "// Package adder \u2014 Gopher Workplace challenge.\npackage adder\n\n// Adder returns a function that adds base to its argument.\nfunc Adder(base int) func(int) int {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "adder.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/middle/appendall": {
    "id": "01-language-basics/04-functions/middle/appendall",
    "title": "Forward Variadic to Append",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Forward Variadic to Append</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>Spreading a variadic slice into <code>append</code> (<code>append(dst, extra...)</code>) forwards all</p>\n<p>elements. To avoid mutating <code>base</code>, copy it first.</p>\n<h2>Task</h2>\n<p>Implement <code>Concat</code> in appendall.go without mutating <code>base</code>.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Concat([1 2], 3, 4)\nOutput: [1 2 3 4]; input [1 2] unchanged\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Concat(nil, 1)\nOutput: [1]\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Concat([9])\nOutput: [9]\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Variadic spread into append</span><span class=\"tchip\">Copy to avoid aliasing</span><span class=\"tchip\">Fresh result</span></div></details>",
    "education": "<h1>Forwarding variadic arguments</h1>\n<h2>Intuition</h2>\n<p><code>extra...</code> spreads a slice into another variadic/append call; appending to a caller's slice can clobber it if capacity allows, so copy when purity matters.</p>\n<h2>Approach</h2>\n<p>1. Copy <code>base</code> into a fresh slice so the caller's slice is not aliased.</p>\n<p>2. Append the variadic <code>extra</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Concat(base []int, extra ...int) []int {\n\tout := append([]int(nil), base...)\n\treturn append(out, extra...)\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>Copying <code>base</code> first means appending never overwrites the caller's <code>[1 2]</code>; the result is <code>[1 2 3 4]</code>.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>append(base, extra...)</code> can mutate base's backing array if it has spare cap.</li>\n<li>Copy into a nil-based slice first for a guaranteed-fresh result.</li>\n</ul>",
    "starter": "// Package appendall \u2014 Gopher Workplace challenge.\npackage appendall\n\n// Concat returns base with all extra values appended. base is not modified\n// (return a fresh slice).\nfunc Concat(base []int, extra ...int) []int {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "appendall.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/middle/bumpptr": {
    "id": "01-language-basics/04-functions/middle/bumpptr",
    "title": "Pointer Parameter Mutates",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Pointer Parameter Mutates</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>A value parameter is a copy; a pointer parameter aliases the caller's variable,</p>\n<p>so writing through it is visible outside.</p>\n<h2>Task</h2>\n<p>Implement <code>Bump</code> in bumpptr.go: increment <code>*p</code>.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  x := 5; Bump(&amp;x)\nOutput: x == 6\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  x := 0; Bump(&amp;x)\nOutput: x == 1\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  x := -1; Bump(&amp;x)\nOutput: x == 0\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Pointer parameter</span><span class=\"tchip\">Dereference to write</span><span class=\"tchip\">Value vs pointer</span></div></details>",
    "education": "<h1>Pointers as mutable references</h1>\n<h2>Intuition</h2>\n<p>Passing <code>&amp;x</code> lets a function modify the caller's variable through the pointer; a plain <code>int</code> parameter could not.</p>\n<h2>Approach</h2>\n<p>1. <code>p</code> aliases the caller's int.</p>\n<p>2. <code>*p++</code> increments it in place.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Bump(p *int) {\n\t*p++\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>Bump(&amp;x)</code> with <code>x = 5</code> writes 6 back through the pointer.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Forgetting the <code>*</code> writes to the pointer variable, not the pointee.</li>\n<li>A nil pointer dereference panics; callers must pass a valid address.</li>\n</ul>",
    "starter": "// Package bumpptr \u2014 Gopher Workplace challenge.\npackage bumpptr\n\n// Bump increments the int that p points to. Passing a pointer lets the callee\n// mutate the caller's variable, unlike a plain value parameter.\nfunc Bump(p *int) {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "bumpptr.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/middle/compose": {
    "id": "01-language-basics/04-functions/middle/compose",
    "title": "Compose Two Funcs",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Compose Two Funcs</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>Function composition builds a new function by chaining two \u2014 the returned</p>\n<p>closure captures both <code>f</code> and <code>g</code>.</p>\n<h2>Task</h2>\n<p>Implement <code>Compose</code> in compose.go so the result computes <code>f(g(x))</code>.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Compose(inc, double)(3)\nOutput: 7\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  applies g then f\nOutput: true\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Compose(double, inc)(3)\nOutput: 8\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Capture two funcs</span><span class=\"tchip\">Application order</span><span class=\"tchip\">Return a func</span></div></details>",
    "education": "<h1>Composing functions</h1>\n<h2>Intuition</h2>\n<p>A closure capturing two functions yields their composition; order matters \u2014 <code>f(g(x))</code> applies <code>g</code> first.</p>\n<h2>Approach</h2>\n<p>1. Return a closure computing <code>f(g(x))</code>.</p>\n<p>2. <code>g</code> runs first, then <code>f</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Compose(f, g func(int) int) func(int) int {\n\treturn func(x int) int {\n\t\treturn f(g(x))\n\t}\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>Compose(inc, double)(3)</code>: double(3)=6, inc(6)=7.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>Compose(f,g)</code> is f-after-g, not g-after-f.</li>\n<li>Both captured functions live as long as the returned closure.</li>\n</ul>",
    "starter": "// Package compose \u2014 Gopher Workplace challenge.\npackage compose\n\n// Compose returns a function computing f(g(x)).\nfunc Compose(f, g func(int) int) func(int) int {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "compose.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/middle/counter": {
    "id": "01-language-basics/04-functions/middle/counter",
    "title": "Closure Counter",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Closure Counter</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>A closure captures variables from its enclosing scope by reference. A counter</p>\n<p>keeps its state in a captured local that lives on after the outer function returns.</p>\n<h2>Task</h2>\n<p>Implement <code>MakeCounter</code> in counter.go so each call to the returned function yields the next integer starting at 1.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  c := MakeCounter(); c(); c()\nOutput: 1, then 2\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  two counters are independent\nOutput: true\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  c := MakeCounter(); c()\nOutput: 1\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Closures capture by reference</span><span class=\"tchip\">State outlives the call</span><span class=\"tchip\">Independent instances</span></div></details>",
    "education": "<h1>Closures capturing enclosing state</h1>\n<h2>Intuition</h2>\n<p>A function literal keeps a live reference to the variables it uses from the outer scope; those variables survive as long as the closure does.</p>\n<h2>Approach</h2>\n<p>1. Declare <code>n := 0</code> in the enclosing scope.</p>\n<p>2. Return a closure that increments and returns <code>n</code>.</p>\n<p>3. Each call to <code>MakeCounter</code> gets its own <code>n</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc MakeCounter() func() int {\n\tn := 0\n\treturn func() int {\n\t\tn++\n\t\treturn n\n\t}\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>The returned closure captures <code>n</code> by reference; successive calls yield 1, 2, 3. A fresh <code>MakeCounter</code> starts a new <code>n</code>.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Each call to the factory creates a NEW captured variable \u2014 instances don't share state.</li>\n<li>The captured variable escapes to the heap; that's expected.</li>\n</ul>",
    "starter": "// Package counter \u2014 Gopher Workplace challenge.\npackage counter\n\n// MakeCounter returns a function that returns 1, 2, 3, ... on successive\n// calls. Each returned counter is independent.\nfunc MakeCounter() func() int {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "counter.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/middle/curry": {
    "id": "01-language-basics/04-functions/middle/curry",
    "title": "Curried Add3",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Curried Add3</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>Currying nests closures: each call captures one argument and returns a function</p>\n<p>waiting for the next.</p>\n<h2>Task</h2>\n<p>Implement <code>Add3</code> in curry.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Add3()(1)(2)(3)\nOutput: 6\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Add3()(0)(0)(0)\nOutput: 0\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Add3()(10)(20)(30)\nOutput: 60\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Nested closures</span><span class=\"tchip\">Accumulated capture</span><span class=\"tchip\">Return function chain</span></div></details>",
    "education": "<h1>Currying with nested closures</h1>\n<h2>Intuition</h2>\n<p>Each closure captures its argument and returns the next; the innermost sees the whole accumulated environment.</p>\n<h2>Approach</h2>\n<p>1. Return nested closures, each capturing one argument.</p>\n<p>2. The innermost sums all three.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Add3() func(int) func(int) func(int) int {\n\treturn func(a int) func(int) func(int) int {\n\t\treturn func(b int) func(int) int {\n\t\t\treturn func(c int) int {\n\t\t\t\treturn a + b + c\n\t\t\t}\n\t\t}\n\t}\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>Add3()(1)(2)(3)</code> captures 1, then 2, then adds 3 \u2192 6.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Every inner closure captures its enclosing arguments by reference.</li>\n<li>The type signature nests exactly as the calls do.</li>\n</ul>",
    "starter": "// Package curry \u2014 Gopher Workplace challenge.\npackage curry\n\n// Add3 returns a curried adder: Add3()(a)(b)(c) == a+b+c.\nfunc Add3() func(int) func(int) func(int) int {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "curry.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/middle/deferbump": {
    "id": "01-language-basics/04-functions/middle/deferbump",
    "title": "Defer Adjusts Result",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Defer Adjusts Result</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>A deferred closure can read and rewrite a named return value after the</p>\n<p><code>return</code> statement has set it \u2014 the classic post-processing hook.</p>\n<h2>Task</h2>\n<p>Implement <code>Compute</code> in deferbump.go: set <code>n = 10</code>, defer a closure that does <code>n *= 2</code>, then <code>return</code>.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Compute()\nOutput: 20\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  deferred func mutates named return\nOutput: true\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  n set to 10 then doubled\nOutput: true\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Defer runs post-return</span><span class=\"tchip\">Mutating named result</span><span class=\"tchip\">Order</span></div></details>",
    "education": "<h1>Defer mutating the return value</h1>\n<h2>Intuition</h2>\n<p>Because deferred functions execute after the return value is assigned, a deferred closure over a named result can transform it (wrap errors, add context).</p>\n<h2>Approach</h2>\n<p>1. The named return <code>n</code> is set to 10.</p>\n<p>2. A deferred <code>n *= 2</code> runs after the body, before the caller sees the value.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Compute() (n int) {\n\tn = 10\n\tdefer func() { n *= 2 }()\n\treturn\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>The body sets <code>n = 10</code>; the defer doubles it to 20 during return.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Only NAMED returns are visible to the deferred closure; a bare <code>return x</code> on an unnamed result can't be adjusted.</li>\n<li>The mutation happens before the caller resumes.</li>\n</ul>",
    "starter": "// Package deferbump \u2014 Gopher Workplace challenge.\npackage deferbump\n\n// Compute sets n=10, then a deferred closure doubles n before return, so the\n// caller sees 20. Deferred functions run after the return value is set but\n// before control leaves.\nfunc Compute() (n int) {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "deferbump.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/middle/deferloop": {
    "id": "01-language-basics/04-functions/middle/deferloop",
    "title": "Defer Inside Loop",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Defer Inside Loop</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>Defers registered in a loop all fire at function exit, in reverse of</p>\n<p>registration \u2014 so a loop 0..n-1 unwinds n-1..0.</p>\n<h2>Task</h2>\n<p>Implement <code>CloseOrder</code> in deferloop.go: loop i from 0 to n-1, deferring an append of i to the named result.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  CloseOrder(3)\nOutput: [2 1 0]\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  defers fire LIFO\nOutput: true\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  CloseOrder(1)\nOutput: [0]\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Loop-registered defers</span><span class=\"tchip\">LIFO unwinding</span><span class=\"tchip\">Per-iteration capture (Go 1.22)</span></div></details>",
    "education": "<h1>Deferring within loops</h1>\n<h2>Intuition</h2>\n<p>Every <code>defer</code> in a loop is stacked; they all run at function return, not at each iteration's end \u2014 reverse order overall.</p>\n<h2>Approach</h2>\n<p>1. Each iteration defers a call capturing <code>i</code> as an argument.</p>\n<p>2. They fire in reverse order at function exit.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc CloseOrder(n int) (out []int) {\n\tfor i := 0; i &lt; n; i++ {\n\t\tdefer func(v int) { out = append(out, v) }(i)\n\t}\n\treturn\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>CloseOrder(3)</code> queues 0, 1, 2 and drains them 2, 1, 0.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Defers do NOT run at the end of each iteration \u2014 only at function return.</li>\n<li>For per-iteration cleanup, wrap the body in its own function.</li>\n</ul>",
    "starter": "// Package deferloop \u2014 Gopher Workplace challenge.\npackage deferloop\n\n// CloseOrder defers appending i for i=0..n-1, so out ends as n-1..0 (LIFO).\nfunc CloseOrder(n int) (out []int) {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "deferloop.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/middle/deferorder": {
    "id": "01-language-basics/04-functions/middle/deferorder",
    "title": "Defer LIFO Order",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Defer LIFO Order</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>Deferred calls stack: they execute in reverse order when the function returns.</p>\n<p>A named return value can be appended to from within deferred closures.</p>\n<h2>Task</h2>\n<p>Implement <code>Order</code> in deferorder.go using three <code>defer</code> statements that append 1, 2, then 3 to a named return slice.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Order()\nOutput: [3 2 1]\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  defers run LIFO\nOutput: true\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  last deferred runs first\nOutput: true\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">defer is LIFO</span><span class=\"tchip\">Named return + defer</span><span class=\"tchip\">Scheduling vs running</span></div></details>",
    "education": "<h1>Deferred call ordering</h1>\n<h2>Intuition</h2>\n<p>Each <code>defer</code> pushes onto a stack unwound in reverse at function exit; deferred closures see and can modify named return values.</p>\n<h2>Approach</h2>\n<p>1. Deferred calls run in <strong>last-in, first-out</strong> order.</p>\n<p>2. Defer appends 1, 2, 3; they fire 3, 2, 1.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Order() (out []int) {\n\tdefer func() { out = append(out, 1) }()\n\tdefer func() { out = append(out, 2) }()\n\tdefer func() { out = append(out, 3) }()\n\treturn\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>The three defers queue in order but execute in reverse, so <code>out</code> ends <code>[3 2 1]</code>.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Schedule order is source order; execution order is reversed.</li>\n<li>Append 1,2,3 in source \u2192 result [3 2 1].</li>\n</ul>",
    "starter": "// Package deferorder \u2014 Gopher Workplace challenge.\npackage deferorder\n\n// Order appends 1,2,3 to a slice via deferred calls, then returns it. Deferred\n// calls run last-in-first-out, so the result is [3 2 1].\nfunc Order() (out []int) {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "deferorder.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/middle/defersnapshot": {
    "id": "01-language-basics/04-functions/middle/defersnapshot",
    "title": "Defer Argument Snapshot",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Defer Argument Snapshot</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p><code>defer f(x)</code> evaluates <code>x</code> immediately, at the defer statement \u2014 not at call</p>\n<p>time. Later changes to <code>x</code> don't affect the deferred argument.</p>\n<h2>Task</h2>\n<p>Implement <code>Snapshot</code> in defersnapshot.go: set <code>x := 1</code>; use a named return <code>r</code>; <code>defer</code> a call passing <code>x</code> that assigns it into <code>r</code>; then set <code>x = 100</code>; return. Result must be 1.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Snapshot()\nOutput: 1\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  deferred arg captured before mutation\nOutput: true\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  later x=100 ignored\nOutput: true\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Argument evaluation time</span><span class=\"tchip\">Named return</span><span class=\"tchip\">Snapshot vs closure</span></div></details>",
    "education": "<h1>When defer evaluates its arguments</h1>\n<h2>Intuition</h2>\n<p>The deferred function's arguments are evaluated when <code>defer</code> executes; only the call is postponed.</p>\n<h2>Approach</h2>\n<p>1. A deferred argument is evaluated at the <code>defer</code> statement.</p>\n<p>2. <code>defer f(x)</code> with <code>x == 1</code> snapshots 1 before <code>x = 100</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Snapshot() (r int) {\n\tx := 1\n\tdefer func(v int) { r = v }(x)\n\tx = 100\n\treturn\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>x</code> is 1 when the defer registers, so the snapshot is 1; changing <code>x</code> to 100 afterward does not affect it.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>defer f(x)</code> snapshots <code>x</code>; <code>defer func(){ use(x) }()</code> reads it at return.</li>\n<li>Here the argument form gives 1.</li>\n</ul>",
    "starter": "// Package defersnapshot \u2014 Gopher Workplace challenge.\npackage defersnapshot\n\n// Snapshot sets x=1, defers a call that adds the CURRENT x to a result, then\n// sets x=100 before returning. Because defer evaluates its arguments when the\n// defer statement runs (x==1), the added value is 1, not 100.\nfunc Snapshot() (r int) {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "defersnapshot.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/middle/dropwhile": {
    "id": "01-language-basics/04-functions/middle/dropwhile",
    "title": "Drop While",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Drop While</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>DropWhile finds the first element failing the predicate and returns everything</p>\n<p>from there \u2014 the complement of TakeWhile.</p>\n<h2>Task</h2>\n<p>Implement <code>DropWhile</code> in dropwhile.go. Return a fresh slice (copy the tail).</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  DropWhile([2 4 5 6], even)\nOutput: [5 6]\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  DropWhile([1 2], even)\nOutput: [1 2]\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  DropWhile([2 4], even)\nOutput: []\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Find boundary</span><span class=\"tchip\">Copy the tail</span><span class=\"tchip\">Complements TakeWhile</span></div></details>",
    "education": "<h1>Skipping a matching prefix</h1>\n<h2>Intuition</h2>\n<p>DropWhile locates the first non-matching element and returns the remainder; copying the tail avoids sharing the caller's backing array.</p>\n<h2>Approach</h2>\n<p>1. Advance an index while the predicate holds.</p>\n<p>2. Return a copy of the remaining tail.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc DropWhile(xs []int, pred func(int) bool) []int {\n\ti := 0\n\tfor i &lt; len(xs) &amp;&amp; pred(xs[i]) {\n\t\ti++\n\t}\n\treturn append([]int(nil), xs[i:]...)\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>[2 4 5 6]</code> with even: skip 2 and 4, return <code>[5 6]</code>.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Returning <code>xs[i:]</code> directly would alias the caller's array; copy it.</li>\n<li>If everything matches, the result is empty.</li>\n</ul>",
    "starter": "// Package dropwhile \u2014 Gopher Workplace challenge.\npackage dropwhile\n\n// DropWhile skips the leading run where pred is true and returns the rest as a\n// new slice.\nfunc DropWhile(xs []int, pred func(int) bool) []int {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "dropwhile.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/middle/fallthru": {
    "id": "01-language-basics/04-functions/middle/fallthru",
    "title": "Switch Fallthrough",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Switch Fallthrough</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p><code>fallthrough</code> forces control into the next case even though its condition</p>\n<p>isn't tested \u2014 the opposite of Go's default break-after-case.</p>\n<h2>Task</h2>\n<p>Implement <code>Rank</code> in fallthru.go. Build the label from the highest tier down using fallthrough.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Rank(9)\nOutput: \"gold/silver/bronze\"\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Rank(6)\nOutput: \"silver/bronze\"\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Rank(1)\nOutput: \"\"\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">fallthrough keyword</span><span class=\"tchip\">Ordered tiers</span><span class=\"tchip\">Accumulate</span></div></details>",
    "education": "<h1>The fallthrough keyword</h1>\n<h2>Intuition</h2>\n<p>Unlike C, Go breaks after each case; <code>fallthrough</code> opts into entering the next case body (without checking its condition).</p>\n<h2>Approach</h2>\n<p>1. Use <code>fallthrough</code> to accumulate lower tiers.</p>\n<p>2. Join reached tiers with <code>/</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Rank(score int) string {\n\tresult := \"\"\n\tadd := func(s string) {\n\t\tif result != \"\" {\n\t\t\tresult += \"/\"\n\t\t}\n\t\tresult += s\n\t}\n\tswitch {\n\tcase score &gt;= 9:\n\t\tadd(\"gold\")\n\t\tfallthrough\n\tcase score &gt;= 6:\n\t\tadd(\"silver\")\n\t\tfallthrough\n\tcase score &gt;= 3:\n\t\tadd(\"bronze\")\n\t}\n\treturn result\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>Rank(9)</code> enters the gold case and falls through silver and bronze, joining all three.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>fallthrough</code> enters the NEXT case unconditionally \u2014 order your cases so that's correct.</li>\n<li>It must be the last statement in a case.</li>\n</ul>",
    "starter": "// Package fallthru \u2014 Gopher Workplace challenge.\npackage fallthru\n\n// Rank returns a cumulative label. Using fallthrough, a score of 3+ yields\n// \"bronze\", 6+ adds \"silver\", 9+ adds \"gold\" \u2014 accumulated as a slash-joined\n// string from the highest tier reached downward. Examples define it exactly.\nfunc Rank(score int) string {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "fallthru.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/middle/filterints": {
    "id": "01-language-basics/04-functions/middle/filterints",
    "title": "Filter by Predicate",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Filter by Predicate</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>A predicate function passed in lets one filter serve every condition.</p>\n<h2>Task</h2>\n<p>Implement <code>Filter</code> in filterints.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Filter([1 2 3 4], even)\nOutput: [2 4]\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Filter([1 3], even)\nOutput: []\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Filter(nil, even)\nOutput: []\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Predicate parameter</span><span class=\"tchip\">Conditional append</span><span class=\"tchip\">Fresh slice</span></div></details>",
    "education": "<h1>Predicate-driven filtering</h1>\n<h2>Intuition</h2>\n<p>Accepting a <code>func(T) bool</code> generalises selection so callers supply the rule, not a new function per condition.</p>\n<h2>Approach</h2>\n<p>1. Range the input.</p>\n<p>2. Keep elements where <code>keep(v)</code> is true.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Filter(xs []int, keep func(int) bool) []int {\n\tvar out []int\n\tfor _, v := range xs {\n\t\tif keep(v) {\n\t\t\tout = append(out, v)\n\t\t}\n\t}\n\treturn out\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>Filter([1 2 3 4], even)</code> keeps 2 and 4.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Return empty (not nil-panic) when nothing matches.</li>\n<li>Don't mutate the input slice.</li>\n</ul>",
    "starter": "// Package filterints \u2014 Gopher Workplace challenge.\npackage filterints\n\n// Filter returns a new slice of the elements for which keep returns true.\nfunc Filter(xs []int, keep func(int) bool) []int {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "filterints.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/middle/firstpair": {
    "id": "01-language-basics/04-functions/middle/firstpair",
    "title": "Labeled Break Search",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Labeled Break Search</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>A labeled <code>break</code> exits an outer loop from inside a nested one \u2014 cleaner than a</p>\n<p>found-flag checked at every level.</p>\n<h2>Task</h2>\n<p>Implement <code>FindPairSum</code> in firstpair.go using labeled break.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  FindPairSum([1 2 3 4], 7)\nOutput: 2, 3, true (xs[2]+xs[3])\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  FindPairSum([1 2], 9)\nOutput: 0, 0, false\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  FindPairSum([3 4], 7)\nOutput: 0, 1, true\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Labeled break</span><span class=\"tchip\">Nested search</span><span class=\"tchip\">Result flags</span></div></details>",
    "education": "<h1>Labeled break and continue</h1>\n<h2>Intuition</h2>\n<p>Labels let <code>break</code>/<code>continue</code> target an enclosing loop, avoiding sentinel flags in nested iteration.</p>\n<h2>Approach</h2>\n<p>1. Label the outer loop.</p>\n<p>2. On the first matching pair, set the returns and <code>break Outer</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc FindPairSum(xs []int, target int) (i, j int, ok bool) {\nOuter:\n\tfor i = 0; i &lt; len(xs); i++ {\n\t\tfor j = i + 1; j &lt; len(xs); j++ {\n\t\t\tif xs[i]+xs[j] == target {\n\t\t\t\tok = true\n\t\t\t\tbreak Outer\n\t\t\t}\n\t\t}\n\t}\n\treturn\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>For target 7 in <code>[1 2 3 4]</code>, indices 2 and 3 sum to 7; the labeled break exits both loops at once.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Only the FIRST matching pair should be returned; break immediately.</li>\n<li>Without the label, <code>break</code> exits just the inner loop.</li>\n</ul>",
    "starter": "// Package firstpair \u2014 Gopher Workplace challenge.\npackage firstpair\n\n// FindPairSum returns the first index pair i<j with xs[i]+xs[j]==target, using\n// a labeled break to exit both loops. Returns ok=false if none.\nfunc FindPairSum(xs []int, target int) (i, j int, ok bool) {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "firstpair.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/middle/flatten": {
    "id": "01-language-basics/04-functions/middle/flatten",
    "title": "Flatten Slices",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Flatten Slices</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>A variadic of slices (<code>...[]int</code>) accepts any number of groups; flattening</p>\n<p>appends each group's elements into one result.</p>\n<h2>Task</h2>\n<p>Implement <code>Flatten</code> in flatten.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Flatten([1 2], [3], [4 5])\nOutput: [1 2 3 4 5]\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Flatten()\nOutput: []\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Flatten(nil, [9])\nOutput: [9]\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Variadic of slices</span><span class=\"tchip\">Spread each group</span><span class=\"tchip\">Order preserved</span></div></details>",
    "education": "<h1>Variadic slice parameters</h1>\n<h2>Intuition</h2>\n<p><code>...[]int</code> collects trailing slice arguments; spreading each with <code>g...</code> builds a flat concatenation.</p>\n<h2>Approach</h2>\n<p>1. Range the variadic groups.</p>\n<p>2. Spread each into the output with <code>append(out, g...)</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Flatten(groups ...[]int) []int {\n\tvar out []int\n\tfor _, g := range groups {\n\t\tout = append(out, g...)\n\t}\n\treturn out\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>Flatten([1 2], [3], [4 5])</code> concatenates all groups into <code>[1 2 3 4 5]</code>.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>A nil group contributes nothing (spreads to zero elements).</li>\n<li>Return empty, not nil-panic, for no groups.</li>\n</ul>",
    "starter": "// Package flatten \u2014 Gopher Workplace challenge.\npackage flatten\n\n// Flatten concatenates all argument slices in order into one new slice.\nfunc Flatten(groups ...[]int) []int {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "flatten.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/middle/gotoretry": {
    "id": "01-language-basics/04-functions/middle/gotoretry",
    "title": "Retry with Goto",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Retry with Goto</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p><code>goto</code> jumps to a label in the same function. It can express a loop, though a</p>\n<p>normal <code>for</code> is clearer \u2014 this exercise makes the control flow explicit.</p>\n<h2>Task</h2>\n<p>Implement <code>SumUntil</code> in gotoretry.go using a <code>goto</code> label to repeat the add step.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  SumUntil([1 2 3 4], 5)\nOutput: 6, 3 (stops once sum &gt;= 5)\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  SumUntil(nil, 10)\nOutput: 0, 0\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  SumUntil([10], 5)\nOutput: 10, 1\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">goto and labels</span><span class=\"tchip\">Loop condition by hand</span><span class=\"tchip\">Two accumulators</span></div></details>",
    "education": "<h1>The goto statement</h1>\n<h2>Intuition</h2>\n<p><code>goto</code> transfers control to a label in the same function; it cannot jump into a block or over variable declarations.</p>\n<h2>Approach</h2>\n<p>1. Label a point and <code>goto</code> it to loop.</p>\n<p>2. Add elements while under the limit and within bounds.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc SumUntil(xs []int, limit int) (sum, used int) {\nloop:\n\tif used &lt; len(xs) &amp;&amp; sum &lt; limit {\n\t\tsum += xs[used]\n\t\tused++\n\t\tgoto loop\n\t}\n\treturn\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>For <code>[1 2 3 4]</code>, limit 5: add 1, 2, 3 (sum 6 \u2265 5), stop at used 3.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>goto</code> can't jump over a variable's declaration into its scope.</li>\n<li>Prefer <code>for</code>; use <code>goto</code> only when it genuinely reads better.</li>\n</ul>",
    "starter": "// Package gotoretry \u2014 Gopher Workplace challenge.\npackage gotoretry\n\n// SumUntil adds elements of xs one at a time; it uses a goto-based loop to\n// restart the accumulation step until the running sum reaches or exceeds limit\n// or xs is exhausted. Returns the sum and how many elements were used.\nfunc SumUntil(xs []int, limit int) (sum, used int) {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "gotoretry.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/middle/groupby": {
    "id": "01-language-basics/04-functions/middle/groupby",
    "title": "Group By Key",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Group By Key</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>A key function plus a map builds buckets; appending to <code>m[k]</code> works even when</p>\n<p>the key is new (nil slice appends fine).</p>\n<h2>Task</h2>\n<p>Implement <code>GroupBy</code> in groupby.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  GroupBy([1 2 3 4], mod2)\nOutput: {0:[2 4], 1:[1 3]}\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  GroupBy(nil, mod2)\nOutput: {}\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  GroupBy([2 4], mod2)\nOutput: {0:[2 4]}\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Key function</span><span class=\"tchip\">Map of slices</span><span class=\"tchip\">nil-slice append</span></div></details>",
    "education": "<h1>Bucketing with a key function</h1>\n<h2>Intuition</h2>\n<p><code>append(m[k], x)</code> relies on a missing key yielding a nil slice that append grows \u2014 no pre-init per key needed.</p>\n<h2>Approach</h2>\n<p>1. Init a <code>map[int][]int</code>.</p>\n<p>2. Append each element to the bucket for <code>key(x)</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc GroupBy(xs []int, key func(int) int) map[int][]int {\n\tm := map[int][]int{}\n\tfor _, x := range xs {\n\t\tk := key(x)\n\t\tm[k] = append(m[k], x)\n\t}\n\treturn m\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>With <code>mod2</code>, evens land in bucket 0 and odds in bucket 1.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>You must init the outer map; per-key slices need no init.</li>\n<li>Input order is preserved within each bucket by appending in order.</li>\n</ul>",
    "starter": "// Package groupby \u2014 Gopher Workplace challenge.\npackage groupby\n\n// GroupBy buckets elements of xs by key(x), preserving input order within each\n// bucket.\nfunc GroupBy(xs []int, key func(int) int) map[int][]int {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "groupby.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/middle/mapints": {
    "id": "01-language-basics/04-functions/middle/mapints",
    "title": "Map Over Slice",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Map Over Slice</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>Passing a function as an argument makes a transform generic: the caller decides</p>\n<p>the per-element operation.</p>\n<h2>Task</h2>\n<p>Implement <code>MapInts</code> in mapints.go returning a new slice.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  MapInts([1 2 3], square)\nOutput: [1 4 9]\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  MapInts(nil, square)\nOutput: []\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  MapInts([2], inc)\nOutput: [3]\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Function as a parameter</span><span class=\"tchip\">Build new slice</span><span class=\"tchip\">First-class functions</span></div></details>",
    "education": "<h1>First-class and higher-order functions</h1>\n<h2>Intuition</h2>\n<p>Functions are ordinary values in Go; a function that takes or returns a function is higher-order, enabling map/filter/reduce.</p>\n<h2>Approach</h2>\n<p>1. Range the input.</p>\n<p>2. Append <code>f(v)</code> to a fresh slice.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc MapInts(xs []int, f func(int) int) []int {\n\tout := make([]int, 0, len(xs))\n\tfor _, v := range xs {\n\t\tout = append(out, f(v))\n\t}\n\treturn out\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>MapInts([1 2 3], square)</code> applies square to each element \u2192 <code>[1 4 9]</code>.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Return a new slice; don't overwrite the caller's <code>xs</code>.</li>\n<li>Preallocating capacity is optional.</li>\n</ul>",
    "starter": "// Package mapints \u2014 Gopher Workplace challenge.\npackage mapints\n\n// MapInts returns a new slice with f applied to each element of xs.\nfunc MapInts(xs []int, f func(int) int) []int {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "mapints.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/middle/maxvar": {
    "id": "01-language-basics/04-functions/middle/maxvar",
    "title": "Variadic Max with OK",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Variadic Max with OK</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>Combining a variadic parameter with an ok flag handles the empty call safely.</p>\n<h2>Task</h2>\n<p>Implement <code>Max</code> in maxvar.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Max(3, 9, 1)\nOutput: 9, true\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Max()\nOutput: 0, false\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Max(5)\nOutput: 5, true\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Variadic slice</span><span class=\"tchip\">Empty guard</span><span class=\"tchip\">Seed from first</span></div></details>",
    "education": "<h1>Variadic with a validity flag</h1>\n<h2>Intuition</h2>\n<p>A <code>...int</code> plus an <code>ok bool</code> cleanly covers the no-argument case without panicking or returning a misleading zero.</p>\n<h2>Approach</h2>\n<p>1. Guard the empty case.</p>\n<p>2. Seed <code>m := nums[0]</code> and scan the rest.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Max(nums ...int) (int, bool) {\n\tif len(nums) == 0 {\n\t\treturn 0, false\n\t}\n\tm := nums[0]\n\tfor _, v := range nums[1:] {\n\t\tif v &gt; m {\n\t\t\tm = v\n\t\t}\n\t}\n\treturn m, true\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>Max(3, 9, 1)</code> seeds 3, raises to 9; no args returns 0, false.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Seeding from 0 breaks on all-negative inputs; seed from <code>nums[0]</code>.</li>\n<li>Guard before indexing.</li>\n</ul>",
    "starter": "// Package maxvar \u2014 Gopher Workplace challenge.\npackage maxvar\n\n// Max returns the largest argument and true, or 0,false when called with none.\nfunc Max(nums ...int) (int, bool) {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "maxvar.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/middle/memoize": {
    "id": "01-language-basics/04-functions/middle/memoize",
    "title": "Memoize a Function",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Memoize a Function</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>A closure can own a map as private state, turning any pure function into a</p>\n<p>cached one without changing its signature.</p>\n<h2>Task</h2>\n<p>Implement <code>Memoize</code> in memoize.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  m := Memoize(f); m(2); m(2)\nOutput: f called once for 2\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  cached result returned\nOutput: true\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  distinct inputs computed once each\nOutput: true\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Closure-owned map</span><span class=\"tchip\">comma-ok lookup</span><span class=\"tchip\">Compute-and-store</span></div></details>",
    "education": "<h1>Stateful closures for caching</h1>\n<h2>Intuition</h2>\n<p>A closure holding a map memoises across calls; the state is private and survives between invocations.</p>\n<h2>Approach</h2>\n<p>1. Capture a <code>cache</code> map.</p>\n<p>2. Return a closure that checks the cache before calling <code>f</code>, storing new results.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Memoize(f func(int) int) func(int) int {\n\tcache := map[int]int{}\n\treturn func(x int) int {\n\t\tif v, ok := cache[x]; ok {\n\t\t\treturn v\n\t\t}\n\t\tv := f(x)\n\t\tcache[x] = v\n\t\treturn v\n\t}\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>The first <code>m(2)</code> computes and caches; the second <code>m(2)</code> returns the cached value without calling <code>f</code> again.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Use comma-ok to distinguish a stored 0 from a missing key.</li>\n<li>Only correct for pure functions; side-effecting <code>f</code> breaks caching semantics.</li>\n</ul>",
    "starter": "// Package memoize \u2014 Gopher Workplace challenge.\npackage memoize\n\n// Memoize returns a caching wrapper: repeated calls with the same argument\n// return the stored result and DO NOT call f again.\nfunc Memoize(f func(int) int) func(int) int {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "memoize.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/middle/namedret": {
    "id": "01-language-basics/04-functions/middle/namedret",
    "title": "Named Return Adjust",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Named Return Adjust</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>Named results are pre-declared, zero-valued variables. A bare <code>return</code> sends</p>\n<p>back their current values \u2014 handy for guard-and-default patterns.</p>\n<h2>Task</h2>\n<p>Implement <code>SafeDiv</code> in namedret.go using named returns and a bare return for the zero-division case.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  SafeDiv(10, 2)\nOutput: 5, true\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  SafeDiv(1, 0)\nOutput: 0, false\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  SafeDiv(9, 3)\nOutput: 3, true\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Named results</span><span class=\"tchip\">Bare return</span><span class=\"tchip\">Guard clause</span></div></details>",
    "education": "<h1>Named return values</h1>\n<h2>Intuition</h2>\n<p>Declaring result names lets you assign them anywhere in the body and use a bare <code>return</code>; they default to zero values.</p>\n<h2>Approach</h2>\n<p>1. Guard division by zero: a bare <code>return</code> yields the zero-valued named returns.</p>\n<p>2. Otherwise set <code>q, ok = a/b, true</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc SafeDiv(a, b int) (q int, ok bool) {\n\tif b == 0 {\n\t\treturn\n\t}\n\tq, ok = a/b, true\n\treturn\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>SafeDiv(1, 0)</code> hits the guard and bare-returns <code>0, false</code>; a valid divisor returns the quotient and true.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>A bare <code>return</code> on the zero path yields (0,false) automatically.</li>\n<li>Overusing named returns hurts readability; reserve for defer/guard cases.</li>\n</ul>",
    "starter": "// Package namedret \u2014 Gopher Workplace challenge.\npackage namedret\n\n// SafeDiv returns a/b and ok=true, or 0,false when b==0. Use named returns.\nfunc SafeDiv(a, b int) (q int, ok bool) {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "namedret.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/middle/oncefn": {
    "id": "01-language-basics/04-functions/middle/oncefn",
    "title": "Run Once",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Run Once</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>A boolean captured in a closure gates a one-time action \u2014 the essence of</p>\n<p><code>sync.Once</code> without the concurrency.</p>\n<h2>Task</h2>\n<p>Implement <code>Once</code> in oncefn.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  o := Once(f); o(); o()\nOutput: f runs only the first time\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  second call is a no-op\nOutput: true\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  guarded by a captured flag\nOutput: true\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Captured flag</span><span class=\"tchip\">Guard the call</span><span class=\"tchip\">Closure state</span></div></details>",
    "education": "<h1>One-shot execution via captured state</h1>\n<h2>Intuition</h2>\n<p>A captured boolean lets a closure enforce run-once semantics across many calls.</p>\n<h2>Approach</h2>\n<p>1. Capture a <code>done</code> flag.</p>\n<p>2. Run <code>f</code> only when <code>!done</code>, then set <code>done = true</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Once(f func()) func() {\n\tdone := false\n\treturn func() {\n\t\tif !done {\n\t\t\tdone = true\n\t\t\tf()\n\t\t}\n\t}\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>The first call runs <code>f</code> and flips <code>done</code>; every later call sees <code>done</code> true and does nothing.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Set the flag BEFORE or after calling f consistently; here order doesn't matter (single-threaded).</li>\n<li>Not goroutine-safe; that's <code>sync.Once</code>'s job.</li>\n</ul>",
    "starter": "// Package oncefn \u2014 Gopher Workplace challenge.\npackage oncefn\n\n// Once returns a function that calls f only on its FIRST invocation; later\n// calls do nothing.\nfunc Once(f func()) func() {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "oncefn.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/middle/perindex": {
    "id": "01-language-basics/04-functions/middle/perindex",
    "title": "Closures Over Loop Variable",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Closures Over Loop Variable</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>Since Go 1.22 the loop variable is per-iteration, so closures created in the</p>\n<p>loop each capture their own copy \u2014 no more the classic shared-variable bug.</p>\n<h2>Task</h2>\n<p>Implement <code>Handlers</code> in perindex.go: build a slice of <code>n</code> closures where the i-th returns <code>i</code>.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Handlers(3)[1]()\nOutput: 1\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  each closure returns its index\nOutput: true\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Handlers(2)[0]()\nOutput: 0\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Per-iteration loop var (Go 1.22)</span><span class=\"tchip\">Capture in closure</span><span class=\"tchip\">Slice of funcs</span></div></details>",
    "education": "<h1>Loop-variable capture semantics</h1>\n<h2>Intuition</h2>\n<p>Go 1.22 gives each iteration its own loop variable, so closures capture distinct values; pre-1.22 they all shared the final value.</p>\n<h2>Approach</h2>\n<p>1. Go 1.22 gives each loop iteration its own <code>i</code>.</p>\n<p>2. Each closure captures its own index.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Handlers(n int) []func() int {\n\tvar out []func() int\n\tfor i := 0; i &lt; n; i++ {\n\t\tout = append(out, func() int { return i })\n\t}\n\treturn out\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>Handlers(3)[1]()</code> returns 1 because the closure at position 1 captured <code>i == 1</code>.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>On Go &lt;1.22 you would need <code>i := i</code> to shadow; on 1.26 it's automatic.</li>\n<li>Each closure returns its own captured index.</li>\n</ul>",
    "starter": "// Package perindex \u2014 Gopher Workplace challenge.\npackage perindex\n\n// Handlers returns n closures; the i-th returns i (0-based). In Go 1.22+ each\n// loop iteration has its own variable, so closures capture distinct values.\nfunc Handlers(n int) []func() int {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "perindex.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/middle/pipeline": {
    "id": "01-language-basics/04-functions/middle/pipeline",
    "title": "Function Pipeline",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Function Pipeline</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>A variadic of functions lets a caller build a processing pipeline; the value</p>\n<p>flows through each stage in order.</p>\n<h2>Task</h2>\n<p>Implement <code>Pipe</code> in pipeline.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Pipe(3, inc, double)\nOutput: 8\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Pipe(5)\nOutput: 5\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Pipe(2, double, double)\nOutput: 8\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Variadic of functions</span><span class=\"tchip\">Left-to-right application</span><span class=\"tchip\">Identity on empty</span></div></details>",
    "education": "<h1>Building pipelines from function slices</h1>\n<h2>Intuition</h2>\n<p>Threading a value through a slice of functions gives a data-flow pipeline with the order fixed by slice order.</p>\n<h2>Approach</h2>\n<p>1. Fold the value through each function left to right.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Pipe(x int, fns ...func(int) int) int {\n\tfor _, f := range fns {\n\t\tx = f(x)\n\t}\n\treturn x\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>Pipe(3, inc, double)</code>: inc(3)=4, double(4)=8.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Empty <code>fns</code> must return x unchanged (identity).</li>\n<li>Order is fns[0] first \u2014 opposite of mathematical Compose.</li>\n</ul>",
    "starter": "// Package pipeline \u2014 Gopher Workplace challenge.\npackage pipeline\n\n// Pipe applies fns to x left to right: fns[0] first, then fns[1], ...\nfunc Pipe(x int, fns ...func(int) int) int {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "pipeline.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/middle/reduceints": {
    "id": "01-language-basics/04-functions/middle/reduceints",
    "title": "Reduce/Fold",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Reduce/Fold</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>Reduce threads an accumulator through the slice, combining each element with a</p>\n<p>caller-supplied binary function.</p>\n<h2>Task</h2>\n<p>Implement <code>Reduce</code> in reduceints.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Reduce([1 2 3 4], 0, add)\nOutput: 10\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Reduce([1 2 3], 1, mul)\nOutput: 6\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Reduce(nil, 5, add)\nOutput: 5\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Accumulator threading</span><span class=\"tchip\">Seed value</span><span class=\"tchip\">Binary combiner</span></div></details>",
    "education": "<h1>Folding with an accumulator</h1>\n<h2>Intuition</h2>\n<p>Reduce expresses sum, product, max, and concatenation as one shape parameterised by the combining function and seed.</p>\n<h2>Approach</h2>\n<p>1. Start <code>acc := init</code>.</p>\n<p>2. Fold each element with <code>acc = f(acc, v)</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Reduce(xs []int, init int, f func(acc, x int) int) int {\n\tacc := init\n\tfor _, v := range xs {\n\t\tacc = f(acc, v)\n\t}\n\treturn acc\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>Reduce([1 2 3 4], 0, add)</code> accumulates 0+1+2+3+4 = 10.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>With an empty slice, the result is <code>init</code>.</li>\n<li>Argument order in <code>f(acc, x)</code> must match the signature.</li>\n</ul>",
    "starter": "// Package reduceints \u2014 Gopher Workplace challenge.\npackage reduceints\n\n// Reduce folds xs into a single value starting from init.\nfunc Reduce(xs []int, init int, f func(acc, x int) int) int {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "reduceints.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/middle/repeatfn": {
    "id": "01-language-basics/04-functions/middle/repeatfn",
    "title": "Apply N Times",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Apply N Times</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>Capturing both a function and a count produces a closure that self-iterates \u2014</p>\n<p>function exponentiation.</p>\n<h2>Task</h2>\n<p>Implement <code>Repeat</code> in repeatfn.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Repeat(inc, 3)(0)\nOutput: 3\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Repeat(double, 2)(1)\nOutput: 4\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Repeat(inc, 0)(5)\nOutput: 5\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Capture f and n</span><span class=\"tchip\">Loop application</span><span class=\"tchip\">Identity at 0</span></div></details>",
    "education": "<h1>Iterated application</h1>\n<h2>Intuition</h2>\n<p>A closure over a function and a count applies it repeatedly; zero applications is the identity.</p>\n<h2>Approach</h2>\n<p>1. Return a closure that applies <code>f</code> to <code>x</code> <code>n</code> times.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Repeat(f func(int) int, n int) func(int) int {\n\treturn func(x int) int {\n\t\tfor i := 0; i &lt; n; i++ {\n\t\t\tx = f(x)\n\t\t}\n\t\treturn x\n\t}\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>Repeat(inc, 3)(0)</code>: inc applied 3 times to 0 \u2192 3.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>n==0 must return the argument unchanged.</li>\n<li>The closure captures f and n by reference; don't mutate them after.</li>\n</ul>",
    "starter": "// Package repeatfn \u2014 Gopher Workplace challenge.\npackage repeatfn\n\n// Repeat returns a function applying f n times. n==0 yields the identity.\nfunc Repeat(f func(int) int, n int) func(int) int {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "repeatfn.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/middle/safecall": {
    "id": "01-language-basics/04-functions/middle/safecall",
    "title": "Recover From Panic",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Recover From Panic</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p><code>recover</code> inside a deferred function stops a panic from propagating. It only</p>\n<p>works when called directly from a deferred call.</p>\n<h2>Task</h2>\n<p>Implement <code>SafeInvoke</code> in safecall.go: defer a recover that sets the named result, then call <code>f</code>.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  SafeInvoke(func(){})\nOutput: false\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  SafeInvoke(func(){ panic(1) })\nOutput: true\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  panic is contained\nOutput: true\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">defer + recover</span><span class=\"tchip\">Named result set in defer</span><span class=\"tchip\">Panic unwinding</span></div></details>",
    "education": "<h1>Panic and recover</h1>\n<h2>Intuition</h2>\n<p>A panic unwinds the stack running deferred calls; <code>recover</code> in one of them captures the panic value and resumes normal return.</p>\n<h2>Approach</h2>\n<p>1. A deferred <code>recover()</code> catches a panic during unwinding.</p>\n<p>2. Set <code>recovered = true</code> when <code>recover()</code> is non-nil.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc SafeInvoke(f func()) (recovered bool) {\n\tdefer func() {\n\t\tif r := recover(); r != nil {\n\t\t\trecovered = true\n\t\t}\n\t}()\n\tf()\n\treturn\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>A panicking <code>f</code> triggers the deferred recover, setting <code>recovered = true</code>; a normal <code>f</code> leaves it false.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>recover</code> only works when called directly inside a deferred function.</li>\n<li>Don't use panic/recover for ordinary control flow.</li>\n</ul>",
    "starter": "// Package safecall \u2014 Gopher Workplace challenge.\npackage safecall\n\n// SafeInvoke runs f and returns true if f panicked (recovered), false if it\n// returned normally.\nfunc SafeInvoke(f func()) (recovered bool) {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "safecall.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/middle/takewhile": {
    "id": "01-language-basics/04-functions/middle/takewhile",
    "title": "Take While",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Take While</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>TakeWhile consumes a prefix, breaking out of the loop as soon as the predicate</p>\n<p>fails \u2014 order-sensitive, unlike Filter.</p>\n<h2>Task</h2>\n<p>Implement <code>TakeWhile</code> in takewhile.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  TakeWhile([2 4 5 6], even)\nOutput: [2 4]\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  TakeWhile([1 2], even)\nOutput: []\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  TakeWhile([2 4], even)\nOutput: [2 4]\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Break on first failure</span><span class=\"tchip\">Prefix semantics</span><span class=\"tchip\">Predicate arg</span></div></details>",
    "education": "<h1>Prefix-based higher-order operations</h1>\n<h2>Intuition</h2>\n<p>TakeWhile/DropWhile depend on order and stop at the boundary, unlike Filter which scans everything.</p>\n<h2>Approach</h2>\n<p>1. Range until the predicate first fails.</p>\n<p>2. Break at that point.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc TakeWhile(xs []int, pred func(int) bool) []int {\n\tvar out []int\n\tfor _, v := range xs {\n\t\tif !pred(v) {\n\t\t\tbreak\n\t\t}\n\t\tout = append(out, v)\n\t}\n\treturn out\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>[2 4 5 6]</code> with even: 2 and 4 pass, 5 fails and stops the take.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>break</code> on the first failure \u2014 do not continue scanning like Filter.</li>\n<li>Empty result when the first element already fails.</li>\n</ul>",
    "starter": "// Package takewhile \u2014 Gopher Workplace challenge.\npackage takewhile\n\n// TakeWhile returns the leading run of xs for which pred is true, stopping at\n// the first element that fails.\nfunc TakeWhile(xs []int, pred func(int) bool) []int {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "takewhile.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/middle/tracker": {
    "id": "01-language-basics/04-functions/middle/tracker",
    "title": "Closure Sharing State",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Closure Sharing State</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>Two closures created in the same scope share the same captured variable \u2014 a</p>\n<p>lightweight object with methods, built from functions.</p>\n<h2>Task</h2>\n<p>Implement <code>NewTracker</code> in tracker.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  add, total := NewTracker(); add(3); add(4); total()\nOutput: 7\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  both closures share sum\nOutput: true\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  total() before any add\nOutput: 0\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Shared capture</span><span class=\"tchip\">Multiple returns of funcs</span><span class=\"tchip\">Encapsulation</span></div></details>",
    "education": "<h1>Multiple closures over shared state</h1>\n<h2>Intuition</h2>\n<p>Closures defined in one scope share its variables, letting a factory return several functions that operate on the same hidden state.</p>\n<h2>Approach</h2>\n<p>1. Capture a shared <code>sum</code>.</p>\n<p>2. Return one closure that adds and one that reads.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc NewTracker() (add func(int), total func() int) {\n\tsum := 0\n\tadd = func(n int) { sum += n }\n\ttotal = func() int { return sum }\n\treturn\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>add(3)</code> and <code>add(4)</code> both mutate the shared <code>sum</code>, so <code>total()</code> returns 7.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Both returned functions capture the SAME <code>sum</code> by reference.</li>\n<li>The state is unreachable except through the returned closures.</li>\n</ul>",
    "starter": "// Package tracker \u2014 Gopher Workplace challenge.\npackage tracker\n\n// NewTracker returns two closures sharing one running sum: add(n) adds n, and\n// total() returns the current sum.\nfunc NewTracker() (add func(int), total func() int) {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "tracker.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/middle/zipwith": {
    "id": "01-language-basics/04-functions/middle/zipwith",
    "title": "Zip With",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Zip With</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>Zipping walks two slices in lockstep, stopping at the shorter one, applying a</p>\n<p>binary function to each pair.</p>\n<h2>Task</h2>\n<p>Implement <code>ZipWith</code> in zipwith.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  ZipWith([1 2 3], [10 20 30], add)\nOutput: [11 22 33]\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  ZipWith([1 2], [10], add)\nOutput: [11]\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  ZipWith(nil, [1], add)\nOutput: []\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Lockstep iteration</span><span class=\"tchip\">Binary combiner</span><span class=\"tchip\">Shorter wins</span></div></details>",
    "education": "<h1>Element-wise combination of two sequences</h1>\n<h2>Intuition</h2>\n<p>ZipWith iterates two inputs together, bounded by the shorter, applying a two-argument function.</p>\n<h2>Approach</h2>\n<p>1. Iterate up to the shorter length.</p>\n<p>2. Combine <code>a[i]</code> and <code>b[i]</code> with <code>f</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc ZipWith(a, b []int, f func(int, int) int) []int {\n\tn := len(a)\n\tif len(b) &lt; n {\n\t\tn = len(b)\n\t}\n\tout := make([]int, 0, n)\n\tfor i := 0; i &lt; n; i++ {\n\t\tout = append(out, f(a[i], b[i]))\n\t}\n\treturn out\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>ZipWith([1 2 3], [10 20 30], add)</code> pairs and sums \u2192 <code>[11 22 33]</code>; mismatched lengths stop at the shorter.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Stop at the shorter length; indexing past it panics.</li>\n<li>Result length equals the shorter input's length.</li>\n</ul>",
    "starter": "// Package zipwith \u2014 Gopher Workplace challenge.\npackage zipwith\n\n// ZipWith combines a and b elementwise with f, up to the shorter length.\nfunc ZipWith(a, b []int, f func(int, int) int) []int {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "zipwith.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/senior/aliasbug": {
    "id": "01-language-basics/04-functions/senior/aliasbug",
    "title": "Assignment Shares Backing Array",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Assignment Shares Backing Array</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p><code>cp := xs</code> copies the slice HEADER only; both share one backing array, so</p>\n<p><code>cp[0] = v</code> also changes <code>xs[0]</code>. A real copy needs <code>make</code>+<code>copy</code> or append.</p>\n<h2>Task</h2>\n<p>Fix aliasbug.go so the input is not mutated.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  WithFirst([1 2 3], 9)\nOutput: [9 2 3]; input stays [1 2 3]\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  original slice unchanged\nOutput: true\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  WithFirst([5], 0)\nOutput: [0]\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Slice assignment aliases</span><span class=\"tchip\">Deep copy</span><span class=\"tchip\">Header vs array</span></div></details>",
    "education": "<h1>Slice assignment copies only the header</h1>\n<h2>Intuition</h2>\n<p>Assigning a slice duplicates its (ptr,len,cap) header but not the backing array, so writes through either alias are shared.</p>\n<h2>Approach</h2>\n<p>1. <code>cp := xs</code> copies the header but shares the backing array.</p>\n<p>2. Copy the data: <code>cp := append([]int(nil), xs...)</code> before mutating.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc WithFirst(xs []int, v int) []int {\n\tcp := append([]int(nil), xs...)\n\tcp[0] = v\n\treturn cp\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>Writing <code>cp[0]</code> through the shared array also changes the caller's slice. Copying into a fresh array isolates the mutation.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>cp := xs</code> is NOT a copy of the elements.</li>\n<li>Use <code>append([]T(nil), xs...)</code> or <code>make</code>+<code>copy</code> for independence.</li>\n</ul>",
    "starter": "// Package aliasbug returns a modified copy of a slice. A planted bug assigns the\n// slice (copying only the header), so mutating the \"copy\" corrupts the input.\npackage aliasbug\n\n// WithFirst returns a copy of xs whose first element is v, leaving xs unchanged.\n// Precondition: len(xs) > 0.\nfunc WithFirst(xs []int, v int) []int {\n\t// CHANGE CODE BELOW THIS LINE\n\tcp := xs\n\t// CHANGE CODE ABOVE THIS LINE\n\tcp[0] = v\n\treturn cp\n}\n",
    "debug": "",
    "file": "aliasbug.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/senior/allpositive": {
    "id": "01-language-basics/04-functions/senior/allpositive",
    "title": "Early Return Placement",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Early Return Placement</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p>A universal claim (\"all positive\") is disproved by a single counterexample, so</p>\n<p>the loop must return false on the FIRST non-positive and only return true after</p>\n<p>checking everything. The bug returns true on the first positive \u2014 inverted logic.</p>\n<h2>Task</h2>\n<p>Fix allpositive.go so it checks every element.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  AllPositive([1 2 3])\nOutput: true\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  AllPositive([1 -2 3])\nOutput: false\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  AllPositive(nil)\nOutput: true\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Universal quantifier</span><span class=\"tchip\">Early return</span><span class=\"tchip\">Empty case</span></div></details>",
    "education": "<h1>Early return for all/any checks</h1>\n<h2>Intuition</h2>\n<p>\"All\" returns false at the first counterexample then true after the loop; \"any\" is the mirror. Returning true mid-loop answers the wrong question.</p>\n<h2>Approach</h2>\n<p>1. \"All\" fails fast on the first counterexample.</p>\n<p>2. The bug returns true on the first positive; instead return false on the first non-positive, and true after the loop.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc AllPositive(xs []int) bool {\n\tfor _, v := range xs {\n\t\tif v &lt;= 0 {\n\t\t\treturn false\n\t\t}\n\t}\n\treturn true\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>Returning true early accepts a slice after seeing one positive, ignoring later negatives. Failing on <code>v &lt;= 0</code> and returning true only at the end is correct; an empty slice is vacuously true.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>\"All P\" \u21d2 fail fast on <code>!P</code>, succeed after the loop.</li>\n<li>\"Any P\" \u21d2 succeed fast on <code>P</code>, fail after the loop.</li>\n</ul>",
    "starter": "// Package allpositive reports whether every element is positive. A planted bug\n// returns true from inside the loop on the first positive element, so it never\n// checks the rest.\npackage allpositive\n\n// AllPositive reports whether all elements of xs are > 0. Empty is true.\nfunc AllPositive(xs []int) bool {\n\tfor _, v := range xs {\n\t\t// CHANGE CODE BELOW THIS LINE\n\t\tif v > 0 {\n\t\t\treturn true\n\t\t}\n\t\t// CHANGE CODE ABOVE THIS LINE\n\t}\n\treturn true\n}\n",
    "debug": "",
    "file": "allpositive.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/senior/appendclobber": {
    "id": "01-language-basics/04-functions/senior/appendclobber",
    "title": "Append Clobbers Shared Cap",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Append Clobbers Shared Cap</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p>Because <code>base</code> has spare capacity, <code>append(base, x)</code> writes into base's backing</p>\n<p>array and returns a slice sharing it. The second <code>append(base, y)</code> writes to the</p>\n<p>same slot, clobbering the first result. Clip the base so each append reallocates.</p>\n<h2>Task</h2>\n<p>Fix appendclobber.go so <code>a</code> and <code>b</code> are independent.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  TwoTails([1 2](cap 8), 100, 200)\nOutput: a == [1 2 100], b == [1 2 200]\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  a and b independent\nOutput: true\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  a[2]\nOutput: 100 (not 200)\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Append into spare capacity</span><span class=\"tchip\">Three-index slice</span><span class=\"tchip\">Independent results</span></div></details>",
    "education": "<h1>Append and shared backing arrays</h1>\n<h2>Intuition</h2>\n<p>Appending to a slice with spare capacity mutates shared memory; a full-slice expression <code>s[:len:len]</code> sets cap=len so the next append copies.</p>\n<h2>Approach</h2>\n<p>1. Two appends onto the same base with spare capacity write into the <strong>same</strong> backing array.</p>\n<p>2. Clip base capacity first: <code>base[:len(base):len(base)]</code>, so each append reallocates.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc TwoTails(base []int, x, y int) (a, b []int) {\n\tclipped := base[:len(base):len(base)]\n\ta = append(clipped, x)\n\tb = append(clipped, y)\n\treturn a, b\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>Without clipping, <code>b</code>'s append overwrites the slot <code>a</code> just wrote, so <code>a[2]</code> becomes 200. The full-slice clip forces separate arrays.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>append</code> reuses capacity when available \u2014 two appends to the same base collide.</li>\n<li><code>base[:n:n]</code> forces the next append to allocate fresh.</li>\n</ul>",
    "starter": "// Package appendclobber derives two slices from a shared base with spare\n// capacity. A planted bug appends to the base directly, so the second append\n// overwrites the first result's tail.\npackage appendclobber\n\n// TwoTails returns base+ x and base+ y as two independent slices. base may have\n// spare capacity, so appending to it directly would make the two results share\n// (and clobber) memory.\nfunc TwoTails(base []int, x, y int) (a, b []int) {\n\t// CHANGE CODE BELOW THIS LINE\n\ta = append(base, x)\n\tb = append(base, y)\n\t// CHANGE CODE ABOVE THIS LINE\n\treturn a, b\n}\n",
    "debug": "",
    "file": "appendclobber.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/senior/appendignored": {
    "id": "01-language-basics/04-functions/senior/appendignored",
    "title": "Append Result Discarded",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Append Result Discarded</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p><code>append</code> may return a NEW slice header (new pointer/len/cap). Discarding its</p>\n<p>return value loses every element; you must assign it back to <code>out</code>.</p>\n<h2>Task</h2>\n<p>Fix appendignored.go so squares accumulate.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Squares(4)\nOutput: [1 4 9 16]\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Squares(1)\nOutput: [1]\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Squares(0)\nOutput: []\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">append returns a slice</span><span class=\"tchip\">Reassign</span><span class=\"tchip\">Header vs backing</span></div></details>",
    "education": "<h1>append returns a new header</h1>\n<h2>Intuition</h2>\n<p><code>append</code> can reallocate and always returns the (possibly new) slice header; ignoring the return keeps the old, shorter header.</p>\n<h2>Approach</h2>\n<p>1. <code>append</code> returns a new slice; the bug discards it with <code>_ =</code>.</p>\n<p>2. Assign back: <code>out = append(out, i*i)</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Squares(n int) []int {\n\tout := make([]int, 0)\n\tfor i := 1; i &lt;= n; i++ {\n\t\tout = append(out, i*i)\n\t}\n\treturn out\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>Ignoring <code>append</code>'s result leaves <code>out</code> empty. Reassigning <code>out</code> accumulates each square.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Always write <code>s = append(s, ...)</code>.</li>\n<li>The returned len/cap differ from the input whenever growth happens.</li>\n</ul>",
    "starter": "// Package appendignored builds a slice of squares. A planted bug ignores the\n// value append returns, so growth past capacity is lost.\npackage appendignored\n\n// Squares returns the squares 1..n.\nfunc Squares(n int) []int {\n\tout := make([]int, 0)\n\tfor i := 1; i <= n; i++ {\n\t\t// CHANGE CODE BELOW THIS LINE\n\t\t_ = append(out, i*i)\n\t\t// CHANGE CODE ABOVE THIS LINE\n\t}\n\treturn out\n}\n",
    "debug": "",
    "file": "appendignored.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/senior/bsearchbug": {
    "id": "01-language-basics/04-functions/senior/bsearchbug",
    "title": "Binary Search Bound",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Binary Search Bound</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p>Binary search must shrink the interval every step. When <code>xs[mid] &lt; target</code> the</p>\n<p>new lower bound is <code>mid+1</code>; setting <code>lo = mid</code> can leave the interval unchanged</p>\n<p>and spin (or time out).</p>\n<h2>Task</h2>\n<p>Fix the low-bound update in bsearchbug.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  IndexOf([1 3 5 7 9], 7)\nOutput: 3\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  IndexOf([1 3 5 7 9], 4)\nOutput: -1\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  IndexOf([1 3 5 7 9], 1)\nOutput: 0\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Interval invariant</span><span class=\"tchip\">Exclude mid</span><span class=\"tchip\">Termination</span></div></details>",
    "education": "<h1>Binary search boundary updates</h1>\n<h2>Intuition</h2>\n<p>After testing <code>mid</code>, the half that excludes it is <code>[mid+1, hi]</code> or <code>[lo, mid-1]</code>; reusing <code>mid</code> in a bound breaks the shrink invariant and can loop.</p>\n<h2>Approach</h2>\n<p>1. When the target is greater than <code>xs[mid]</code>, search the right half <strong>above</strong> mid.</p>\n<p>2. The bug sets <code>lo = mid</code> (can loop forever); use <code>lo = mid + 1</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc IndexOf(xs []int, target int) int {\n\tlo, hi := 0, len(xs)-1\n\tfor lo &lt;= hi {\n\t\tmid := (lo + hi) / 2\n\t\tswitch {\n\t\tcase xs[mid] == target:\n\t\t\treturn mid\n\t\tcase xs[mid] &lt; target:\n\t\t\tlo = mid + 1\n\t\tdefault:\n\t\t\thi = mid - 1\n\t\t}\n\t}\n\treturn -1\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>lo = mid</code> without advancing can stall when <code>lo == mid</code>. <code>lo = mid + 1</code> strictly shrinks the range, so the search terminates and finds index 3.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>xs[mid] &lt; target</code> \u21d2 <code>lo = mid + 1</code>, never <code>lo = mid</code>.</li>\n<li>Every iteration must reduce <code>hi - lo</code>.</li>\n</ul>",
    "starter": "// Package bsearchbug finds a target in a sorted slice. A planted bug moves the\n// low pointer to mid (not mid+1), so on a miss the search can loop or overshoot;\n// the tests catch a wrong/absent index.\npackage bsearchbug\n\n// IndexOf returns the index of target in the sorted slice xs, or -1 if absent.\nfunc IndexOf(xs []int, target int) int {\n\tlo, hi := 0, len(xs)-1\n\tfor lo <= hi {\n\t\tmid := (lo + hi) / 2\n\t\tswitch {\n\t\tcase xs[mid] == target:\n\t\t\treturn mid\n\t\tcase xs[mid] < target:\n\t\t\t// CHANGE CODE BELOW THIS LINE\n\t\t\tlo = mid\n\t\t\t// CHANGE CODE ABOVE THIS LINE\n\t\tdefault:\n\t\t\thi = mid - 1\n\t\t}\n\t}\n\treturn -1\n}\n",
    "debug": "",
    "file": "bsearchbug.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/senior/classifybug": {
    "id": "01-language-basics/04-functions/senior/classifybug",
    "title": "Missing Default Case",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Missing Default Case</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p>Every branch must produce a defined result. Falling out of the switch with an</p>\n<p>empty return string leaves 3xx/1xx codes unlabeled; a <code>default</code> (or a non-empty</p>\n<p>fallthrough return) fixes it.</p>\n<h2>Task</h2>\n<p>Fix classifybug.go so unknown classes return \"unknown\".</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Class(200)\nOutput: success\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Class(301)\nOutput: unknown\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Class(100)\nOutput: unknown\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Exhaustive branches</span><span class=\"tchip\">default case</span><span class=\"tchip\">Defined output</span></div></details>",
    "education": "<h1>Handling the default case</h1>\n<h2>Intuition</h2>\n<p>A switch without a default (or a catch-all return) silently produces the zero value for unlisted inputs.</p>\n<h2>Approach</h2>\n<p>1. A <code>switch</code> with no matching case and no <code>default</code> returns the zero value.</p>\n<p>2. Add a <code>default: return \"unknown\"</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Class(code int) string {\n\tswitch code / 100 {\n\tcase 2:\n\t\treturn \"success\"\n\tcase 4:\n\t\treturn \"client\"\n\tcase 5:\n\t\treturn \"server\"\n\tdefault:\n\t\treturn \"unknown\"\n\t}\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>Class(301)</code> matches no case and falls out returning \"\". A <code>default</code> branch supplies \"unknown\" for unmatched codes.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Always define behaviour for inputs outside the enumerated cases.</li>\n<li><code>default:</code> documents the catch-all intent.</li>\n</ul>",
    "starter": "// Package classifybug classifies an HTTP status code class. A planted bug omits\n// the default, so unknown codes return an empty string instead of \"unknown\".\npackage classifybug\n\n// Class returns \"success\" (2xx), \"client\" (4xx), \"server\" (5xx), or \"unknown\".\nfunc Class(code int) string {\n\tswitch code / 100 {\n\tcase 2:\n\t\treturn \"success\"\n\tcase 4:\n\t\treturn \"client\"\n\tcase 5:\n\t\treturn \"server\"\n\t// CHANGE CODE BELOW THIS LINE\n\t}\n\treturn \"\"\n\t// CHANGE CODE ABOVE THIS LINE\n}\n",
    "debug": "",
    "file": "classifybug.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/senior/cleanupcount": {
    "id": "01-language-basics/04-functions/senior/cleanupcount",
    "title": "Defers Fire at Function Exit",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Defers Fire at Function Exit</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p>Deferred cleanups do NOT run at the end of each loop iteration \u2014 they all run</p>\n<p>when the function returns. Decrementing eagerly inside the loop (the bug) drops</p>\n<p>the open count immediately, so the peak never rises above 1.</p>\n<h2>Task</h2>\n<p>Fix cleanupcount.go so the drain is deferred to function exit.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  PeakThenDrain(3)\nOutput: 3\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  PeakThenDrain(0)\nOutput: 0\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  PeakThenDrain(1)\nOutput: 1\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Defer timing</span><span class=\"tchip\">Peak before drain</span><span class=\"tchip\">Loop-scheduled defers</span></div></details>",
    "education": "<h1>Defer runs at function scope, not block scope</h1>\n<h2>Intuition</h2>\n<p>A <code>defer</code> inside a loop is deferred to the enclosing function's return, so resources accumulate until then \u2014 sometimes the intent, sometimes a leak.</p>\n<h2>Approach</h2>\n<p>1. The peak must be observed before the counter drains.</p>\n<p>2. The bug decrements inline during the loop; defer the decrement so draining happens after the peak is recorded.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc PeakThenDrain(n int) (peak int) {\n\topen := 0\n\tfor i := 0; i &lt; n; i++ {\n\t\topen++\n\t\tif open &gt; peak {\n\t\t\tpeak = open\n\t\t}\n\t\tdefer func() { open-- }()\n\t}\n\treturn peak\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>Decrementing <code>open</code> inline lowers the peak before it's captured. Deferring <code>open--</code> postpones draining until after the function records the maximum.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Defers do not run at each iteration's end.</li>\n<li>For per-iteration cleanup, extract the body into its own function.</li>\n</ul>",
    "starter": "// Package cleanupcount records how many resources are open at the moment a\n// deferred cleanup runs. Because defers all fire at function return, every\n// cleanup sees the FINAL open count \u2014 the test pins this behaviour, and a\n// planted bug decrements at schedule time instead of via the defer.\npackage cleanupcount\n\n// PeakThenDrain opens n resources (incrementing open), scheduling a deferred\n// decrement for each, and returns the peak open count observed before draining.\nfunc PeakThenDrain(n int) (peak int) {\n\topen := 0\n\tfor i := 0; i < n; i++ {\n\t\topen++\n\t\tif open > peak {\n\t\t\tpeak = open\n\t\t}\n\t\t// CHANGE CODE BELOW THIS LINE\n\t\topen--\n\t\t// CHANGE CODE ABOVE THIS LINE\n\t}\n\treturn peak\n}\n",
    "debug": "",
    "file": "cleanupcount.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/senior/copyshort": {
    "id": "01-language-basics/04-functions/senior/copyshort",
    "title": "Copy Bounded by Destination",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Copy Bounded by Destination</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p><code>copy(dst, src)</code> copies <code>min(len(dst), len(src))</code> elements. A destination of</p>\n<p>length 0 (even with capacity) copies nothing; the length, not the capacity, is</p>\n<p>what counts.</p>\n<h2>Task</h2>\n<p>Fix copyshort.go so the clone actually contains the data.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Clone([1 2 3])\nOutput: [1 2 3], independent\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  len(Clone([1 2 3]))\nOutput: 3\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Clone(nil)\nOutput: []\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">copy is length-bounded</span><span class=\"tchip\">length vs capacity</span><span class=\"tchip\">make length</span></div></details>",
    "education": "<h1>copy is bounded by length</h1>\n<h2>Intuition</h2>\n<p><code>copy</code> moves <code>min(len(dst), len(src))</code> elements; a zero-length destination copies nothing regardless of capacity.</p>\n<h2>Approach</h2>\n<p>1. <code>copy</code> transfers only <code>min(len(dst), len(src))</code> elements.</p>\n<p>2. The bug makes <code>dst</code> length 0, so nothing copies.</p>\n<p>3. Allocate <code>make([]int, len(xs))</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Clone(xs []int) []int {\n\tdst := make([]int, len(xs))\n\tcopy(dst, xs)\n\treturn dst\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>With a zero-length <code>dst</code>, <code>copy</code> writes nothing and returns an empty clone. Sizing <code>dst</code> to <code>len(xs)</code> copies all elements.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>copy</code> uses LENGTH, not capacity, of both slices.</li>\n<li>For a clone use <code>make([]T, len(src))</code> (or <code>append([]T(nil), src...)</code>).</li>\n</ul>",
    "starter": "// Package copyshort duplicates a slice. A planted bug allocates the destination\n// with length 0, so copy copies nothing (copy is bounded by the shorter slice).\npackage copyshort\n\n// Clone returns an independent copy of xs.\nfunc Clone(xs []int) []int {\n\t// CHANGE CODE BELOW THIS LINE\n\tdst := make([]int, 0, len(xs))\n\t// CHANGE CODE ABOVE THIS LINE\n\tcopy(dst, xs)\n\treturn dst\n}\n",
    "debug": "",
    "file": "copyshort.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/senior/counternilmap": {
    "id": "01-language-basics/04-functions/senior/counternilmap",
    "title": "Nil Map Write in Helper",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Nil Map Write in Helper</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p>Writing to a nil map panics. A map must be initialised with <code>make</code> (or a</p>\n<p>literal) before any assignment; reads from nil are fine, writes are not.</p>\n<h2>Task</h2>\n<p>Fix counternilmap.go so tallying works.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Tally([\"a\",\"b\",\"a\"])\nOutput: {a:2, b:1}\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Tally(nil)\nOutput: {} (empty, non-nil)\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Tally([\"x\"])\nOutput: {x:1}\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Nil vs made map</span><span class=\"tchip\">make</span><span class=\"tchip\">Increment on missing key</span></div></details>",
    "education": "<h1>Nil map writes panic</h1>\n<h2>Intuition</h2>\n<p>A nil map reads as empty but panics on write; construction via <code>make</code>/literal is mandatory before assignment.</p>\n<h2>Approach</h2>\n<p>1. A <code>var m map[string]int</code> is nil; writing to it panics.</p>\n<p>2. Initialize with <code>make(map[string]int)</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Tally(words []string) map[string]int {\n\tm := make(map[string]int)\n\tfor _, w := range words {\n\t\tm[w]++\n\t}\n\treturn m\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>The first <code>m[word]++</code> on a nil map panics. <code>make</code> gives an empty, writable map, so tallying works and <code>Tally(nil)</code> returns an empty map.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Reading <code>m[k]</code> on a nil map is safe; writing is not.</li>\n<li><code>make(map[K]V)</code> or <code>map[K]V{}</code> both work.</li>\n</ul>",
    "starter": "// Package counternilmap tallies word counts. A planted bug returns an unmade\n// (nil) map from the constructor, so the first write panics.\npackage counternilmap\n\n// Tally counts occurrences of each string in words.\nfunc Tally(words []string) map[string]int {\n\t// CHANGE CODE BELOW THIS LINE\n\tvar m map[string]int\n\t// CHANGE CODE ABOVE THIS LINE\n\tfor _, w := range words {\n\t\tm[w]++\n\t}\n\treturn m\n}\n",
    "debug": "",
    "file": "counternilmap.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/senior/deferargbug": {
    "id": "01-language-basics/04-functions/senior/deferargbug",
    "title": "Defer Argument Timing",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Defer Argument Timing</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p><code>defer f(c)</code> evaluates <code>c</code> at the defer statement (when c==0), not at exit. To</p>\n<p>read the final value, capture <code>c</code> in the closure body instead of passing it as</p>\n<p>an argument.</p>\n<h2>Task</h2>\n<p>Fix deferargbug.go so it records the FINAL counter value.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  FinalCount(5)\nOutput: 5\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  FinalCount(0)\nOutput: 0\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  FinalCount(3)\nOutput: 3\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Argument vs closure capture</span><span class=\"tchip\">Defer evaluation time</span><span class=\"tchip\">Named result</span></div></details>",
    "education": "<h1>Snapshot vs live capture in defer</h1>\n<h2>Intuition</h2>\n<p>Passing a value as a deferred argument freezes it at defer-time; referencing it in the closure body reads it at return-time.</p>\n<h2>Approach</h2>\n<p>1. Deferred <strong>arguments</strong> are evaluated at the <code>defer</code> statement, snapshotting the value.</p>\n<p>2. The bug captures <code>c</code> early, before later mutations.</p>\n<p>3. Close over <code>c</code> instead so the final value is read at return.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc FinalCount(n int) (recorded int) {\n\tc := 0\n\tdefer func() { recorded = c }()\n\tfor i := 0; i &lt; n; i++ {\n\t\tc++\n\t}\n\treturn\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>defer f(c)</code> freezes <code>c</code> at its value when defer runs. Closing over <code>c</code> in <code>defer func(){ recorded = c }()</code> reads it after all updates, so <code>FinalCount(5)</code> returns 5.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>defer f(c)</code> uses c's value NOW; <code>defer func(){ use(c) }()</code> uses it at return.</li>\n<li>For final values, capture in the body.</li>\n</ul>",
    "starter": "// Package deferargbug records the final value of a counter via defer. A planted\n// bug passes the counter as a defer ARGUMENT, snapshotting it early, so the\n// recorded value is the initial one, not the final.\npackage deferargbug\n\n// FinalCount increments a counter n times and records the counter's value into\n// a named result at function exit. It should record the FINAL value.\nfunc FinalCount(n int) (recorded int) {\n\tc := 0\n\t// CHANGE CODE BELOW THIS LINE\n\tdefer func(v int) { recorded = v }(c)\n\t// CHANGE CODE ABOVE THIS LINE\n\tfor i := 0; i < n; i++ {\n\t\tc++\n\t}\n\treturn\n}\n",
    "debug": "",
    "file": "deferargbug.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/senior/deferwipe": {
    "id": "01-language-basics/04-functions/senior/deferwipe",
    "title": "Defer Overwrites Result",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Defer Overwrites Result</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p>A deferred closure runs AFTER the return value is set and can overwrite it.</p>\n<p>Here the defer zeroes <code>result</code> just before the caller receives it. Remove (or</p>\n<p>correct) the deferred mutation.</p>\n<h2>Task</h2>\n<p>Fix deferwipe.go so <code>Compute</code> returns <code>a*b</code>.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Compute(6, 7)\nOutput: 42\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Compute(0, 5)\nOutput: 0\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Compute(3, 3)\nOutput: 9\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Defer runs post-return</span><span class=\"tchip\">Unintended mutation</span><span class=\"tchip\">Order of effects</span></div></details>",
    "education": "<h1>Deferred mutation of named returns</h1>\n<h2>Intuition</h2>\n<p>Because defers run after the return value is assigned, a deferred closure that writes the named result overrides whatever the body computed.</p>\n<h2>Approach</h2>\n<p>1. A deferred <code>result = 0</code> runs after the body and clobbers the named return.</p>\n<p>2. Remove the erroneous defer.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Compute(a, b int) (result int) {\n\tresult = a * b\n\treturn\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>The body computes 42, then the deferred assignment resets <code>result</code> to 0 just before return. Deleting the defer preserves the computed value.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Deferred writes to a named return WIN over the body's assignment.</li>\n<li>Only mutate the result in a defer when you intend to (e.g. wrapping errors).</li>\n</ul>",
    "starter": "// Package deferwipe should return a computed value, but a planted deferred\n// closure resets the named return to zero, wiping the result.\npackage deferwipe\n\n// Compute returns a*b, but a stray deferred closure zeroes the result.\nfunc Compute(a, b int) (result int) {\n\t// CHANGE CODE BELOW THIS LINE\n\tdefer func() { result = 0 }()\n\t// CHANGE CODE ABOVE THIS LINE\n\tresult = a * b\n\treturn\n}\n",
    "debug": "",
    "file": "deferwipe.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/senior/deletebug": {
    "id": "01-language-basics/04-functions/senior/deletebug",
    "title": "Ordered Slice Delete",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Ordered Slice Delete</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p>Deleting index i means keeping <code>xs[:i]</code> and everything AFTER i, i.e. <code>xs[i+1:]</code>.</p>\n<p>The bug appends <code>xs[i:]</code>, which re-includes the element meant to be removed.</p>\n<h2>Task</h2>\n<p>Fix the delete in deletebug.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  RemoveAt([10 20 30 40], 1)\nOutput: [10 30 40]\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  RemoveAt([10 20 30], 0)\nOutput: [20 30]\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  RemoveAt([10 20 30], 2)\nOutput: [10 20]\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Ordered delete idiom</span><span class=\"tchip\">Off-by-one slice bound</span><span class=\"tchip\">append with spread</span></div></details>",
    "education": "<h1>The ordered-delete idiom</h1>\n<h2>Intuition</h2>\n<p>Removing index i keeps the prefix <code>xs[:i]</code> and the suffix <code>xs[i+1:]</code>; using <code>xs[i:]</code> for the suffix keeps the element you meant to drop.</p>\n<h2>Approach</h2>\n<p>1. To delete index <code>i</code>, append the tail starting <strong>after</strong> it.</p>\n<p>2. The bug uses <code>xs[i:]</code> (keeps the element); use <code>xs[i+1:]</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc RemoveAt(xs []int, i int) []int {\n\treturn append(xs[:i], xs[i+1:]...)\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>append(xs[:i], xs[i:]...)</code> re-includes the element at <code>i</code>. Using <code>xs[i+1:]</code> skips it, removing index <code>i</code>.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Suffix after deletion starts at <code>i+1</code>, not <code>i</code>.</li>\n<li>This idiom mutates the backing array; copy first if the caller keeps xs.</li>\n</ul>",
    "starter": "// Package deletebug removes the element at index i, preserving order. A planted\n// bug shifts the wrong direction, dropping the wrong element.\npackage deletebug\n\n// RemoveAt returns xs with the element at index i removed, order preserved.\n// Precondition: 0 <= i < len(xs).\nfunc RemoveAt(xs []int, i int) []int {\n\t// CHANGE CODE BELOW THIS LINE\n\treturn append(xs[:i], xs[i:]...)\n\t// CHANGE CODE ABOVE THIS LINE\n}\n",
    "debug": "",
    "file": "deletebug.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/senior/factbug": {
    "id": "01-language-basics/04-functions/senior/factbug",
    "title": "Recursion Base Case",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Recursion Base Case</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p>Factorial's base case is <code>0! = 1</code>. Returning 0 makes the whole product</p>\n<p>collapse to 0 because every level multiplies by the base.</p>\n<h2>Task</h2>\n<p>Fix the base case in factbug.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Fact(0)\nOutput: 1\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Fact(5)\nOutput: 120\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Fact(6)\nOutput: 720\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Recursion base case</span><span class=\"tchip\">Multiplicative identity</span><span class=\"tchip\">Recursive product</span></div></details>",
    "education": "<h1>Base cases in recursion</h1>\n<h2>Intuition</h2>\n<p>A recursive definition needs a base that both terminates and returns the identity for the combining operation \u2014 1 for products, 0 for sums.</p>\n<h2>Approach</h2>\n<p>1. The recursion base case for factorial is <code>0! = 1</code>.</p>\n<p>2. The bug returns 0, zeroing every product.</p>\n<p>3. Return 1 at the base.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Fact(n int) int {\n\tif n == 0 {\n\t\treturn 1\n\t}\n\treturn n * Fact(n-1)\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>return 0</code> makes <code>Fact(5) = 5<em>4</em>3<em>2</em>1*0 = 0</code>. Returning 1 at <code>n == 0</code> gives the correct 120.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>0! == 1</code> by definition; the multiplicative identity is 1.</li>\n<li>Wrong base values are hard to spot because the recursion still terminates.</li>\n</ul>",
    "starter": "// Package factbug computes factorial recursively. A planted bug uses the wrong\n// base case (returns 0 at n==0), zeroing every result.\npackage factbug\n\n// Fact returns n! for n >= 0.\nfunc Fact(n int) int {\n\tif n == 0 {\n\t\t// CHANGE CODE BELOW THIS LINE\n\t\treturn 0\n\t\t// CHANGE CODE ABOVE THIS LINE\n\t}\n\treturn n * Fact(n-1)\n}\n",
    "debug": "",
    "file": "factbug.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/senior/handlerbug": {
    "id": "01-language-basics/04-functions/senior/handlerbug",
    "title": "Capture the Value, Not the Cursor",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Capture the Value, Not the Cursor</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p>Here <code>i</code> is declared ONCE outside the loop, so all closures share it; after the</p>\n<p>loop <code>i == len(names)</code> and every closure indexes out of range or returns the</p>\n<p>same element. Give each closure its own captured value.</p>\n<h2>Task</h2>\n<p>Fix handlerbug.go so closure k returns <code>names[k]</code>.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Labelers([\"a\",\"b\",\"c\"])[1]()\nOutput: b\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Labelers([\"a\",\"b\"])[0]()\nOutput: a\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  each closure returns its own name\nOutput: true\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Shared vs per-iteration variable</span><span class=\"tchip\">Capture by value</span><span class=\"tchip\">Go 1.22 range</span></div></details>",
    "education": "<h1>Capturing shared loop variables</h1>\n<h2>Intuition</h2>\n<p>A variable declared outside the loop is shared by every closure; only per-iteration variables (or an explicit inner copy) give each closure its own value.</p>\n<h2>Approach</h2>\n<p>1. A single hoisted <code>var i</code> is shared by every closure, so all capture the final <code>i</code>.</p>\n<p>2. Use <code>for i := range names</code> so each iteration has its own <code>i</code> (Go 1.22 per-iteration scope).</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Labelers(names []string) []func() string {\n\tvar out []func() string\n\tfor i := range names {\n\t\tout = append(out, func() string { return names[i] })\n\t}\n\treturn out\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>With the shared <code>i</code>, every closure reads the loop's end value and indexes the same element. A per-iteration <code>i</code> binds each closure to its own index.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>var i int; for i = 0; ...</code> shares one <code>i</code> across all closures.</li>\n<li>Use <code>for i := range</code> or copy into a fresh variable inside the loop.</li>\n</ul>",
    "starter": "// Package handlerbug builds label-printer closures from a slice. A planted bug\n// captures the shared index variable `i` (declared once, outside the range) so\n// every closure reads the same final index.\npackage handlerbug\n\n// Labelers returns one closure per element; the k-th returns names[k].\nfunc Labelers(names []string) []func() string {\n\tvar out []func() string\n\t// CHANGE CODE BELOW THIS LINE\n\tvar i int\n\tfor i = 0; i < len(names); i++ {\n\t\tout = append(out, func() string { return names[i] })\n\t}\n\t// CHANGE CODE ABOVE THIS LINE\n\treturn out\n}\n",
    "debug": "",
    "file": "handlerbug.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/senior/insertbug": {
    "id": "01-language-basics/04-functions/senior/insertbug",
    "title": "Insert Shifts the Wrong Way",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Insert Shifts the Wrong Way</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p>To open a gap at index i, the tail <code>xs[i:]</code> must shift RIGHT into <code>xs[i+1:]</code>.</p>\n<p>The bug copies <code>xs[i+1:]</code> back onto <code>xs[i:]</code>, shifting left and clobbering the</p>\n<p>insertion point.</p>\n<h2>Task</h2>\n<p>Fix the shift in insertbug.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  InsertAt([1 2 4], 2, 3)\nOutput: [1 2 3 4]\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  InsertAt([2 3], 0, 1)\nOutput: [1 2 3]\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  InsertAt([1 2], 2, 3)\nOutput: [1 2 3]\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Open a gap</span><span class=\"tchip\">copy direction</span><span class=\"tchip\">Overlapping copy</span></div></details>",
    "education": "<h1>Shifting to insert into a slice</h1>\n<h2>Intuition</h2>\n<p>Inserting at i needs the suffix <code>xs[i:]</code> moved into <code>xs[i+1:]</code> (rightward); copying the other way shifts left and overwrites.</p>\n<h2>Approach</h2>\n<p>1. To open a gap at <code>i</code>, shift the tail <strong>right</strong>: <code>copy(xs[i+1:], xs[i:])</code>.</p>\n<p>2. The bug copies in the wrong direction, overwriting elements.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc InsertAt(xs []int, i, v int) []int {\n\txs = append(xs, 0)\n\tcopy(xs[i+1:], xs[i:])\n\txs[i] = v\n\treturn xs\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>copy(xs[i:], xs[i+1:])</code> shifts left and loses data. Copying <code>xs[i:]</code> into <code>xs[i+1:]</code> makes room so the new value slots in at <code>i</code>.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Insert shifts RIGHT: <code>copy(xs[i+1:], xs[i:])</code>.</li>\n<li>Delete shifts LEFT: <code>copy(xs[i:], xs[i+1:])</code>.</li>\n</ul>",
    "starter": "// Package insertbug inserts a value at an index, shifting the tail right. A\n// planted bug copies in the wrong direction, duplicating an element and losing\n// the tail.\npackage insertbug\n\n// InsertAt returns xs with v inserted at index i (0 <= i <= len(xs)).\nfunc InsertAt(xs []int, i, v int) []int {\n\txs = append(xs, 0)\n\t// CHANGE CODE BELOW THIS LINE\n\tcopy(xs[i:], xs[i+1:])\n\t// CHANGE CODE ABOVE THIS LINE\n\txs[i] = v\n\treturn xs\n}\n",
    "debug": "",
    "file": "insertbug.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/senior/nestedmap": {
    "id": "01-language-basics/04-functions/senior/nestedmap",
    "title": "Nested Map Inner Init",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Nested Map Inner Init</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p><code>g[from]</code> is nil until the inner map is created. Assigning <code>g[from][to]</code> writes</p>\n<p>into that nil inner map and panics; you must lazily initialise the inner map.</p>\n<h2>Task</h2>\n<p>Fix nestedmap.go so edges can be added.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Add(g, \"a\", \"b\"); g[\"a\"][\"b\"]\nOutput: true\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Add on empty graph\nOutput: no panic\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Add(g, \"a\", \"c\") after \"a\",\"b\"\nOutput: both present\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Nested maps</span><span class=\"tchip\">Lazy init</span><span class=\"tchip\">comma-ok</span></div></details>",
    "education": "<h1>Initialising nested maps</h1>\n<h2>Intuition</h2>\n<p>Only the outer map exists after <code>map[K]map[..]{}</code>; each inner map is nil until explicitly created, and writing to a nil map panics.</p>\n<h2>Approach</h2>\n<p>1. Writing <code>g[from][to]</code> when the inner map is nil panics.</p>\n<p>2. Lazily create it: <code>if g[from] == nil { g[from] = map[string]bool{} }</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Add(g map[string]map[string]bool, from, to string) {\n\tif g[from] == nil {\n\t\tg[from] = map[string]bool{}\n\t}\n\tg[from][to] = true\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>The first edge from a new node finds a nil inner map and panics. Initializing it first makes the assignment safe.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>g[from]</code> is nil, not an empty map, until you assign one.</li>\n<li>Lazily create the inner map before writing to it.</li>\n</ul>",
    "starter": "// Package nestedmap records edges in an adjacency map. A planted bug writes to\n// an inner map that was never created, panicking on the first insert.\npackage nestedmap\n\n// Add records a directed edge from -> to in the adjacency structure.\nfunc Add(g map[string]map[string]bool, from, to string) {\n\t// CHANGE CODE BELOW THIS LINE\n\tg[from][to] = true\n\t// CHANGE CODE ABOVE THIS LINE\n}\n",
    "debug": "",
    "file": "nestedmap.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/senior/oddbug": {
    "id": "01-language-basics/04-functions/senior/oddbug",
    "title": "Parity Test for Negatives",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Parity Test for Negatives</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p>Go's <code>%</code> keeps the sign of the dividend, so <code>-3 % 2 == -1</code>, not 1. Testing</p>\n<p><code>== 1</code> misses negative odds; <code>!= 0</code> is the sign-safe parity test.</p>\n<h2>Task</h2>\n<p>Fix oddbug.go so negative odd numbers are counted.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  CountOdd([1 2 3 -3 -4 -5])\nOutput: 4\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  CountOdd([2 4 6])\nOutput: 0\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  CountOdd([-1])\nOutput: 1\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Signed modulo</span><span class=\"tchip\">Sign-safe parity</span><span class=\"tchip\">Boundary of the spec</span></div></details>",
    "education": "<h1>Signed modulo and parity</h1>\n<h2>Intuition</h2>\n<p>Because <code>%</code> follows the dividend's sign, <code>n%2</code> is -1 for negative odds; only <code>!= 0</code> reliably detects oddness.</p>\n<h2>Approach</h2>\n<p>1. In Go, <code>v % 2</code> is <code>-1</code> for negative odds, so <code>== 1</code> misses them.</p>\n<p>2. Test <code>v % 2 != 0</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc CountOdd(xs []int) int {\n\tc := 0\n\tfor _, v := range xs {\n\t\tif v%2 != 0 {\n\t\t\tc++\n\t\t}\n\t}\n\treturn c\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>-3 % 2</code> is <code>-1</code>, so <code>== 1</code> fails to count negative odds. <code>!= 0</code> correctly identifies all odd numbers.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>-3 % 2</code> is <code>-1</code>, so <code>== 1</code> fails.</li>\n<li>Prefer <code>v%2 != 0</code> for parity across all integers.</li>\n</ul>",
    "starter": "// Package oddbug counts odd numbers. A planted bug tests n%2 == 1, which is\n// false for negative odd numbers (e.g. -3 % 2 == -1 in Go).\npackage oddbug\n\n// CountOdd returns how many elements of xs are odd (including negatives).\nfunc CountOdd(xs []int) int {\n\tc := 0\n\tfor _, v := range xs {\n\t\t// CHANGE CODE BELOW THIS LINE\n\t\tif v%2 == 1 {\n\t\t\t// CHANGE CODE ABOVE THIS LINE\n\t\t\tc++\n\t\t}\n\t}\n\treturn c\n}\n",
    "debug": "",
    "file": "oddbug.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/senior/pairbug": {
    "id": "01-language-basics/04-functions/senior/pairbug",
    "title": "Adjacent-Pair Bound",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Adjacent-Pair Bound</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p>Reading <code>xs[i+1]</code> requires the loop to stop at <code>len(xs)-1</code>; iterating to</p>\n<p><code>len(xs)</code> reads one element past the slice and panics.</p>\n<h2>Task</h2>\n<p>Fix the loop bound in pairbug.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  SumDiffs([1 4 9])\nOutput: 8\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  SumDiffs([5])\nOutput: 0\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  SumDiffs([1 2 3 4])\nOutput: 3\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Look-ahead bound</span><span class=\"tchip\">Empty/single guard</span><span class=\"tchip\">Index safety</span></div></details>",
    "education": "<h1>Look-ahead loop bounds</h1>\n<h2>Intuition</h2>\n<p>When the body reads <code>xs[i+1]</code>, the loop must terminate at <code>len(xs)-1</code>; the classic <code>&lt; len(xs)</code> bound over-runs by one.</p>\n<h2>Approach</h2>\n<p>1. Summing adjacent differences accesses <code>xs[i+1]</code>, so <code>i</code> must stop at <code>len-2</code>.</p>\n<p>2. The bug loops to <code>len-1</code>, indexing out of range; use <code>i &lt; len(xs)-1</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc SumDiffs(xs []int) int {\n\ttotal := 0\n\tfor i := 0; i &lt; len(xs)-1; i++ {\n\t\ttotal += xs[i+1] - xs[i]\n\t}\n\treturn total\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>At the last index, <code>xs[i+1]</code> is out of bounds and panics. Stopping one early keeps every <code>xs[i+1]</code> valid.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Reading <code>xs[i+1]</code> \u21d2 stop at <code>len(xs)-1</code>.</li>\n<li>With 0 or 1 elements the loop must not run.</li>\n</ul>",
    "starter": "// Package pairbug sums adjacent differences. A planted bug loops i to len(xs)-1\n// inclusive and reads xs[i+1], running one past the end.\npackage pairbug\n\n// SumDiffs returns the sum of xs[i+1]-xs[i] for all adjacent pairs. For fewer\n// than two elements it returns 0.\nfunc SumDiffs(xs []int) int {\n\ttotal := 0\n\t// CHANGE CODE BELOW THIS LINE\n\tfor i := 0; i < len(xs); i++ {\n\t\t// CHANGE CODE ABOVE THIS LINE\n\t\ttotal += xs[i+1] - xs[i]\n\t}\n\treturn total\n}\n",
    "debug": "",
    "file": "pairbug.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/senior/prefixbug": {
    "id": "01-language-basics/04-functions/senior/prefixbug",
    "title": "Prefix-Sum Query",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Prefix-Sum Query</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p>A prefix array <code>pre</code> satisfies <code>sum(xs[l:r]) == pre[r] - pre[l]</code>. The bug</p>\n<p>returns only <code>pre[r]</code>, which is the sum from 0 to r \u2014 it omits subtracting the</p>\n<p>left prefix.</p>\n<h2>Task</h2>\n<p>Fix prefixbug.go so range sums are correct.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  RangeSum([1 2 3 4 5], 1, 4)\nOutput: 9\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  RangeSum([1 2 3 4 5], 0, 5)\nOutput: 15\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  RangeSum([1 2 3 4 5], 2, 3)\nOutput: 3\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Prefix sums</span><span class=\"tchip\">Range formula</span><span class=\"tchip\">Half-open interval</span></div></details>",
    "education": "<h1>Prefix-sum range queries</h1>\n<h2>Intuition</h2>\n<p>With <code>pre[i]</code> the sum of the first i elements, any range <code>[l:r)</code> is <code>pre[r] - pre[l]</code>; dropping the <code>pre[l]</code> term over-counts.</p>\n<h2>Approach</h2>\n<p>1. A prefix-sum range is <code>pre[r] - pre[l]</code>.</p>\n<p>2. The bug returns only <code>pre[r]</code>, ignoring the left offset.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc RangeSum(xs []int, l, r int) int {\n\tpre := make([]int, len(xs)+1)\n\tfor i, v := range xs {\n\t\tpre[i+1] = pre[i] + v\n\t}\n\treturn pre[r] - pre[l]\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>pre[r]</code> sums from index 0, not <code>l</code>. Subtracting <code>pre[l]</code> removes the prefix before <code>l</code>, giving the sub-range sum.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>pre[r]</code> alone is the sum from 0, not from l.</li>\n<li>Match the half-open convention: <code>pre[r] - pre[l]</code> for <code>xs[l:r]</code>.</li>\n</ul>",
    "starter": "// Package prefixbug answers range-sum queries using a prefix-sum array. A\n// planted bug forgets to subtract the prefix at l, over-counting the left part.\npackage prefixbug\n\n// RangeSum returns the sum of xs[l:r] (l inclusive, r exclusive) using a prefix\n// array. Precondition: 0 <= l <= r <= len(xs).\nfunc RangeSum(xs []int, l, r int) int {\n\tpre := make([]int, len(xs)+1)\n\tfor i, v := range xs {\n\t\tpre[i+1] = pre[i] + v\n\t}\n\t// CHANGE CODE BELOW THIS LINE\n\treturn pre[r]\n\t// CHANGE CODE ABOVE THIS LINE\n}\n",
    "debug": "",
    "file": "prefixbug.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/senior/rangemutate": {
    "id": "01-language-basics/04-functions/senior/rangemutate",
    "title": "Range Copy Doesn't Mutate",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Range Copy Doesn't Mutate</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p><code>for i, v := range xs</code> copies each element into <code>v</code>. Assigning to <code>v</code> changes</p>\n<p>only the copy; to mutate the slice you must write through the index <code>xs[i]</code>.</p>\n<h2>Task</h2>\n<p>Fix rangemutate.go so the slice is actually doubled.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  DoubleAll([1 2 3])\nOutput: [2 4 6]\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  DoubleAll([0])\nOutput: [0]\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  DoubleAll(nil)\nOutput: nil\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Range value is a copy</span><span class=\"tchip\">Index to mutate</span><span class=\"tchip\">In-place mutation</span></div></details>",
    "education": "<h1>Range value copies</h1>\n<h2>Intuition</h2>\n<p>The range loop's value variable is a per-iteration copy; mutating it never reaches the underlying element \u2014 only indexing does.</p>\n<h2>Approach</h2>\n<p>1. <code>for _, v := range xs</code> gives a <strong>copy</strong>; mutating <code>v</code> doesn't touch the slice.</p>\n<p>2. Write via index: <code>xs[i] = v * 2</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc DoubleAll(xs []int) {\n\tfor i, v := range xs {\n\t\t_ = i\n\t\txs[i] = v * 2\n\t}\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>The bug doubles a throwaway <code>v</code>, leaving the slice unchanged. Assigning <code>xs[i]</code> mutates the backing array in place.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>for _, v := range xs { v = ... }</code> changes nothing.</li>\n<li>Use <code>xs[i]</code> (or a pointer element) to mutate in place.</li>\n</ul>",
    "starter": "// Package rangemutate doubles every element in place. A planted bug writes to\n// the range VALUE variable, which is a copy, so the slice is unchanged.\npackage rangemutate\n\n// DoubleAll doubles each element of xs in place.\nfunc DoubleAll(xs []int) {\n\tfor i, v := range xs {\n\t\t_ = i\n\t\t// CHANGE CODE BELOW THIS LINE\n\t\tv = v * 2\n\t\t_ = v\n\t\t// CHANGE CODE ABOVE THIS LINE\n\t}\n}\n",
    "debug": "",
    "file": "rangemutate.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/senior/recoverdirect": {
    "id": "01-language-basics/04-functions/senior/recoverdirect",
    "title": "Recover Must Be Deferred",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Recover Must Be Deferred</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p><code>recover</code> only stops a panic when called from within a function that is running</p>\n<p>because of a <code>defer</code> during the panic unwind. Called directly at the top of the</p>\n<p>body, <code>recover</code> returns nil and the panic still crashes the program.</p>\n<h2>Task</h2>\n<p>Fix the recovery in recoverdirect.go so a panicking <code>f</code> is caught.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Guard(func(){})\nOutput: false\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Guard(func(){ panic(1) })\nOutput: true\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Guard(func(){ panic(\"x\") })\nOutput: true\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">recover placement</span><span class=\"tchip\">Panic unwinding</span><span class=\"tchip\">Named result in defer</span></div></details>",
    "education": "<h1>recover requires a deferred context</h1>\n<h2>Intuition</h2>\n<p>A direct <code>recover()</code> call returns nil; only a <code>recover()</code> inside a function invoked by <code>defer</code> during a panic captures the panic value.</p>\n<h2>Approach</h2>\n<p>1. <code>recover</code> only works inside a <strong>deferred</strong> function during unwinding.</p>\n<p>2. The bug calls <code>recover()</code> before <code>f()</code> runs, so it always sees nil.</p>\n<p>3. Wrap it in <code>defer func(){ ... }()</code> before calling <code>f</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Guard(f func()) (ok bool) {\n\tdefer func() {\n\t\tif r := recover(); r != nil {\n\t\t\tok = true\n\t\t}\n\t}()\n\tf()\n\treturn ok\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>Calling <code>recover()</code> up front returns nil and the panic from <code>f()</code> is never caught. A deferred recover fires while the stack unwinds, setting <code>ok = true</code>.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>recover</code> outside a deferred call always returns nil.</li>\n<li>Put <code>f()</code> after scheduling the deferred recover, not before it.</li>\n</ul>",
    "starter": "// Package recoverdirect guards a call that may panic. A planted bug calls\n// recover() directly in the body instead of from a deferred function, so it\n// never actually stops the panic.\npackage recoverdirect\n\n// Guard runs f and returns true if f panicked (and was recovered), false if it\n// returned normally.\nfunc Guard(f func()) (ok bool) {\n\t// CHANGE CODE BELOW THIS LINE\n\tif r := recover(); r != nil {\n\t\tok = true\n\t}\n\tf()\n\t// CHANGE CODE ABOVE THIS LINE\n\treturn ok\n}\n",
    "debug": "",
    "file": "recoverdirect.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/senior/reversebug": {
    "id": "01-language-basics/04-functions/senior/reversebug",
    "title": "Two-Pointer Reverse Bound",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Two-Pointer Reverse Bound</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p>The last valid index of a slice is <code>len(xs)-1</code>. Initialising the right pointer</p>\n<p>to <code>len(xs)</code> makes the first <code>xs[j]</code> read one past the end, panicking with an</p>\n<p>index-out-of-range error.</p>\n<h2>Task</h2>\n<p>Fix the right-pointer initialisation in reversebug.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Reverse([1 2 3 4])\nOutput: [4 3 2 1]\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Reverse([1 2 3])\nOutput: [3 2 1]\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Reverse([1])\nOutput: [1]\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Last index</span><span class=\"tchip\">Two-pointer technique</span><span class=\"tchip\">Index bounds</span></div></details>",
    "education": "<h1>Slice index bounds</h1>\n<h2>Intuition</h2>\n<p>Valid indices run <code>0 .. len(xs)-1</code>; seeding a pointer at <code>len(xs)</code> and dereferencing it panics immediately.</p>\n<h2>Approach</h2>\n<p>1. The right index starts at <code>len(xs)-1</code>, the last valid element.</p>\n<p>2. The bug seeds <code>j := len(xs)</code>, indexing out of range.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Reverse(xs []int) {\n\ti, j := 0, len(xs)-1\n\tfor i &lt; j {\n\t\txs[i], xs[j] = xs[j], xs[i]\n\t\ti++\n\t\tj--\n\t}\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>xs[len(xs)]</code> is out of bounds on the first swap. Seeding <code>j</code> at <code>len(xs)-1</code> swaps the true ends inward.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>The highest valid index is <code>len(xs)-1</code>, not <code>len(xs)</code>.</li>\n<li><code>xs[len(xs)]</code> always panics.</li>\n</ul>",
    "starter": "// Package reversebug reverses a slice in place with two pointers. A planted bug\n// initialises the right pointer to len(xs) instead of len(xs)-1, so the first\n// access xs[j] indexes one past the end and panics.\npackage reversebug\n\n// Reverse reverses xs in place.\nfunc Reverse(xs []int) {\n\t// CHANGE CODE BELOW THIS LINE\n\ti, j := 0, len(xs)\n\t// CHANGE CODE ABOVE THIS LINE\n\tfor i < j {\n\t\txs[i], xs[j] = xs[j], xs[i]\n\t\ti++\n\t\tj--\n\t}\n}\n",
    "debug": "",
    "file": "reversebug.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/senior/runecount": {
    "id": "01-language-basics/04-functions/senior/runecount",
    "title": "Range Over String Yields Runes",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Range Over String Yields Runes</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p><code>len(s)</code> on a string is its byte length; multibyte UTF-8 runes count as 2\u20134</p>\n<p>bytes. Ranging a string (or <code>utf8.RuneCountInString</code>) counts actual code points.</p>\n<h2>Task</h2>\n<p>Fix runecount.go to count runes, not bytes.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  CharCount(\"h\u00e9llo\")\nOutput: 5\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  CharCount(\"\u65e5\u672c\u8a9e\")\nOutput: 3\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  CharCount(\"abc\")\nOutput: 3\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Bytes vs runes</span><span class=\"tchip\">Range over string</span><span class=\"tchip\">utf8 helpers</span></div></details>",
    "education": "<h1>String length in bytes vs runes</h1>\n<h2>Intuition</h2>\n<p>A Go string is UTF-8 bytes; <code>len</code> returns bytes while ranging or <code>utf8.RuneCountInString</code> returns code points.</p>\n<h2>Approach</h2>\n<p>1. <code>len(s)</code> counts <strong>bytes</strong>, not characters.</p>\n<p>2. Range over the string (which yields runes) and count.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc CharCount(s string) int {\n\tn := 0\n\tfor range s {\n\t\tn++\n\t}\n\treturn n\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>\"h\u00e9llo\"</code> is 6 bytes but 5 runes; <code>len</code> returns 6. Ranging decodes UTF-8 and counts 5 runes.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>len(\"\u00e9\")</code> is 2, not 1.</li>\n<li>Range indices over a string are BYTE offsets, but the value is a rune.</li>\n</ul>",
    "starter": "// Package runecount counts characters (runes) in a UTF-8 string. A planted bug\n// uses len(s), which counts BYTES, over-counting multibyte characters.\npackage runecount\n\n// CharCount returns the number of Unicode code points in s.\nfunc CharCount(s string) int {\n\t// CHANGE CODE BELOW THIS LINE\n\treturn len(s)\n\t// CHANGE CODE ABOVE THIS LINE\n}\n",
    "debug": "",
    "file": "runecount.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/senior/shadowerr": {
    "id": "01-language-basics/04-functions/senior/shadowerr",
    "title": "Shadowed Named Return",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Shadowed Named Return</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p><code>:=</code> inside a block creates NEW variables, shadowing the outer named returns.</p>\n<p>Here the inner <code>err</code> never reaches the outer <code>err</code>, and the final <code>return n, err</code></p>\n<p>sends the zero-valued outer <code>err</code> (nil) even after a failure.</p>\n<h2>Task</h2>\n<p>Fix the shadowing bug between the markers in shadowerr.go so parse failures propagate.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Parse(\"42\")\nOutput: 42, nil\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Parse(\"nope\")\nOutput: 0, non-nil error\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Parse(\"-7\")\nOutput: -7, nil\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Variable shadowing</span><span class=\"tchip\">Named returns</span><span class=\"tchip\"><code>:=</code> vs <code>=</code></span></div></details>",
    "education": "<h1>Shadowing named return values</h1>\n<h2>Intuition</h2>\n<p>A short declaration inside a nested block shadows an outer variable of the same name; the outer named return keeps its zero value unless you assign to it with <code>=</code>.</p>\n<h2>Approach</h2>\n<p>1. The named return <code>err</code> must receive the failure.</p>\n<p>2. The bug's <code>:=</code> inside the <code>if</code> shadows <code>err</code> with a new local, so the outer <code>err</code> stays nil.</p>\n<p>3. Assign to the named returns directly: <code>n, err = strconv.Atoi(s)</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nimport \"strconv\"\n\nfunc Parse(s string) (n int, err error) {\n\tn, err = strconv.Atoi(s)\n\treturn\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>Parse(\"nope\")</code> should report an error, but the shadowed <code>err</code> is discarded at the block's end, leaving the outer <code>err</code> nil. Assigning without <code>:=</code> writes the real result.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>if x, err := f(); err != nil</code> shadows an outer <code>err</code>.</li>\n<li>Declare the extra variable separately, then assign the named return with <code>=</code>.</li>\n</ul>",
    "starter": "// Package shadowerr parses a decimal string into an int with a named error\n// return. A planted bug shadows the named err inside the if block, so a real\n// parse failure is reported as success.\npackage shadowerr\n\nimport \"strconv\"\n\n// Parse returns the parsed number and an error. On failure n is 0 and err is\n// non-nil.\nfunc Parse(s string) (n int, err error) {\n\t// CHANGE CODE BELOW THIS LINE\n\tif v, err := strconv.Atoi(s); err == nil {\n\t\tn = v\n\t}\n\t// CHANGE CODE ABOVE THIS LINE\n\treturn\n}\n",
    "debug": "",
    "file": "shadowerr.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/senior/skipbug": {
    "id": "01-language-basics/04-functions/senior/skipbug",
    "title": "Continue Skips Wrong Branch",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Continue Skips Wrong Branch</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p><code>continue</code> skips the rest of the current iteration. The bug continues on the</p>\n<p>positive elements \u2014 exactly the ones that should be summed \u2014 so only non-positive</p>\n<p>values are added. The condition is inverted.</p>\n<h2>Task</h2>\n<p>Fix skipbug.go so the POSITIVE elements are summed.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  SumPositive([1 -2 3 -4 5])\nOutput: 9\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  SumPositive([-1 -2])\nOutput: 0\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  SumPositive([1 2 3])\nOutput: 6\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">continue semantics</span><span class=\"tchip\">Guard polarity</span><span class=\"tchip\">Filter-by-continue</span></div></details>",
    "education": "<h1>continue as a filter guard</h1>\n<h2>Intuition</h2>\n<p><code>continue</code> skips to the next iteration; the guard must name the elements to SKIP, so a <code>continue</code> on the wanted elements silently drops them.</p>\n<h2>Approach</h2>\n<p>1. To sum positives, skip the non-positives.</p>\n<p>2. The bug <code>continue</code>s on <code>v &gt; 0</code>, skipping exactly the ones it should add.</p>\n<p>3. Invert to <code>if v &lt;= 0 { continue }</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc SumPositive(xs []int) int {\n\tsum := 0\n\tfor _, v := range xs {\n\t\tif v &lt;= 0 {\n\t\t\tcontinue\n\t\t}\n\t\tsum += v\n\t}\n\treturn sum\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>The inverted guard skips positives and sums negatives, giving a wrong total. Skipping <code>v &lt;= 0</code> accumulates only the positives \u2192 9.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>continue</code> should guard the unwanted case (<code>if !keep { continue }</code>).</li>\n<li>Continuing on the values you meant to process drops them.</li>\n</ul>",
    "starter": "// Package skipbug sums only the positive numbers using a range loop with\n// continue. A planted bug continues on the POSITIVE elements (skipping the ones\n// it should add) instead of skipping the non-positive ones.\npackage skipbug\n\n// SumPositive returns the sum of the positive elements of xs.\nfunc SumPositive(xs []int) int {\n\tsum := 0\n\tfor _, v := range xs {\n\t\t// CHANGE CODE BELOW THIS LINE\n\t\tif v > 0 {\n\t\t\tcontinue\n\t\t}\n\t\t// CHANGE CODE ABOVE THIS LINE\n\t\tsum += v\n\t}\n\treturn sum\n}\n",
    "debug": "",
    "file": "skipbug.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/senior/spreadmissing": {
    "id": "01-language-basics/04-functions/senior/spreadmissing",
    "title": "Forgotten Spread Operator",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Forgotten Spread Operator</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p>To pass a slice's elements to a variadic function you spread it with <code>xs...</code>.</p>\n<p>The bug passes <code>len(xs)</code> (a single count) instead, so the total is meaningless.</p>\n<h2>Task</h2>\n<p>Fix spreadmissing.go to forward the slice elements.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Total([1 2 3 4])\nOutput: 10\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Total(nil)\nOutput: 0\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Total([5])\nOutput: 5\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Spread operator</span><span class=\"tchip\">Variadic call forms</span><span class=\"tchip\">Not the length</span></div></details>",
    "education": "<h1>Spreading a slice into a variadic call</h1>\n<h2>Intuition</h2>\n<p><code>f(xs...)</code> forwards each element of <code>xs</code>; without the <code>...</code> you pass a single value (here the length), not the contents.</p>\n<h2>Approach</h2>\n<p>1. To pass a slice to a variadic function, spread it with <code>...</code>.</p>\n<p>2. The bug passes <code>len(xs)</code> (a single int); use <code>sum(xs...)</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc sum(nums ...int) int {\n\tt := 0\n\tfor _, n := range nums {\n\t\tt += n\n\t}\n\treturn t\n}\n\nfunc Total(xs []int) int {\n\treturn sum(xs...)\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>sum(len(xs))</code> sums the length, not the elements. <code>sum(xs...)</code> unpacks the slice into the variadic parameter.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>sum(xs...)</code> spreads; <code>sum(xs)</code> won't compile for <code>...int</code>, and <code>sum(len(xs))</code> compiles but is wrong.</li>\n<li>A nil slice spreads to zero arguments.</li>\n</ul>",
    "starter": "// Package spreadmissing sums a slice by delegating to a variadic helper. A\n// planted bug builds a nested slice instead of spreading, so it sums one value.\npackage spreadmissing\n\nfunc sum(nums ...int) int {\n\tt := 0\n\tfor _, n := range nums {\n\t\tt += n\n\t}\n\treturn t\n}\n\n// Total sums all elements of xs via the variadic sum helper.\nfunc Total(xs []int) int {\n\t// CHANGE CODE BELOW THIS LINE\n\treturn sum(len(xs))\n\t// CHANGE CODE ABOVE THIS LINE\n}\n",
    "debug": "",
    "file": "spreadmissing.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/senior/stalecap": {
    "id": "01-language-basics/04-functions/senior/stalecap",
    "title": "Append Invalidates Alias",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Append Invalidates Alias</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p>When append exceeds capacity it allocates a new array; pointers taken before</p>\n<p>the append still point at the OLD array. Writing through the stale pointer does</p>\n<p>not change the current slice.</p>\n<h2>Task</h2>\n<p>Fix stalecap.go so the write updates the current slice's element 0.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  FirstAfterGrow(7)\nOutput: 99\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  result reflects write through fresh pointer\nOutput: 99\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  element after grow\nOutput: 99\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Append reallocation</span><span class=\"tchip\">Stale element pointers</span><span class=\"tchip\">Re-address after growth</span></div></details>",
    "education": "<h1>Element pointers and reallocation</h1>\n<h2>Intuition</h2>\n<p><code>append</code> past capacity moves the backing array; any pointer/slice into the old array is now detached from the live slice.</p>\n<h2>Approach</h2>\n<p>1. After the slice grows (reallocates), an old element pointer is stale.</p>\n<p>2. Re-take <code>p = &amp;xs[0]</code> against the new backing array before writing.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc FirstAfterGrow(v int) int {\n\txs := make([]int, 1, 1) // len==cap==1, so append reallocates\n\txs[0] = 10\n\tp := &amp;xs[0]\n\txs = append(xs, v)\n\tp = &amp;xs[0]\n\t*p = 99\n\treturn xs[0]\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>append</code> may move the backing array, leaving <code>p</code> pointing at freed storage. Re-deriving <code>p</code> from <code>xs[0]</code> targets the live element so <code>*p = 99</code> sticks.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>After a reallocating append, old <code>&amp;xs[i]</code> is stale.</li>\n<li>Index the current slice (<code>xs[0]</code>) or re-take the address.</li>\n</ul>",
    "starter": "// Package stalecap builds a running-sum reporter. A planted bug keeps a pointer\n// into the slice taken BEFORE an append that reallocates, so later reads see a\n// stale backing array.\npackage stalecap\n\n// FirstAfterGrow appends v to xs (which is full), then returns the value now at\n// index 0 of the resulting slice. The append reallocates.\nfunc FirstAfterGrow(v int) int {\n\txs := make([]int, 1, 1) // len==cap==1, so append reallocates\n\txs[0] = 10\n\tp := &xs[0]\n\txs = append(xs, v)\n\t// CHANGE CODE BELOW THIS LINE\n\t*p = 99\n\t// CHANGE CODE ABOVE THIS LINE\n\treturn xs[0]\n}\n",
    "debug": "",
    "file": "stalecap.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/senior/thresholdbug": {
    "id": "01-language-basics/04-functions/senior/thresholdbug",
    "title": "Boundary Comparison",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Boundary Comparison</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p>\"Strictly greater\" excludes the boundary. Using <code>&gt;=</code> admits values equal to the</p>\n<p>threshold \u2014 an off-by-one in the comparison operator, not the index.</p>\n<h2>Task</h2>\n<p>Fix the comparison in thresholdbug.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  AboveThreshold([1 5 5 8 3], 5)\nOutput: [8]\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  AboveThreshold([1 2 3], 0)\nOutput: [1 2 3]\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  AboveThreshold([1], 5)\nOutput: []\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Strict vs inclusive comparison</span><span class=\"tchip\">Specification wording</span><span class=\"tchip\">Boundary values</span></div></details>",
    "education": "<h1>Comparison operator precision</h1>\n<h2>Intuition</h2>\n<p><code>&gt;</code> and <code>&gt;=</code> differ only at the boundary, but that single equal value is exactly where specifications are easy to misread.</p>\n<h2>Approach</h2>\n<p>1. \"Above\" means strictly greater.</p>\n<p>2. The bug uses <code>&gt;=</code>, including values equal to the threshold.</p>\n<p>3. Use <code>&gt; t</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc AboveThreshold(xs []int, t int) []int {\n\tvar out []int\n\tfor _, v := range xs {\n\t\tif v &gt; t {\n\t\t\tout = append(out, v)\n\t\t}\n\t}\n\treturn out\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>With <code>&gt;=</code>, the 5s are wrongly included. <code>&gt; t</code> keeps only 8, which is strictly above 5.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>\"strictly greater\" \u21d2 <code>&gt;</code>; \"at least\" \u21d2 <code>&gt;=</code>.</li>\n<li>Always test the exact boundary value.</li>\n</ul>",
    "starter": "// Package thresholdbug keeps elements strictly greater than a threshold. A\n// planted bug uses >= instead of >, wrongly including the boundary value.\npackage thresholdbug\n\n// AboveThreshold returns the elements of xs strictly greater than t.\nfunc AboveThreshold(xs []int, t int) []int {\n\tvar out []int\n\tfor _, v := range xs {\n\t\t// CHANGE CODE BELOW THIS LINE\n\t\tif v >= t {\n\t\t\t// CHANGE CODE ABOVE THIS LINE\n\t\t\tout = append(out, v)\n\t\t}\n\t}\n\treturn out\n}\n",
    "debug": "",
    "file": "thresholdbug.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/senior/tierbug": {
    "id": "01-language-basics/04-functions/senior/tierbug",
    "title": "Accidental Fallthrough",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Accidental Fallthrough</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p>In Go a case does NOT fall through by default; <code>fallthrough</code> forces entry into</p>\n<p>the next case unconditionally (ignoring its condition). Here it makes level 1</p>\n<p>leak into the level-2 body, so <code>Label(1)</code> returns \"mid\". Remove the stray keyword.</p>\n<h2>Task</h2>\n<p>Fix tierbug.go so case 1 yields \"low\".</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Label(1)\nOutput: low\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Label(2)\nOutput: mid\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Label(9)\nOutput: ?\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Go switch semantics</span><span class=\"tchip\">fallthrough keyword</span><span class=\"tchip\">Case isolation</span></div></details>",
    "education": "<h1>Explicit fallthrough in Go</h1>\n<h2>Intuition</h2>\n<p>Unlike C, Go breaks after each case; <code>fallthrough</code> opts in and ignores the next case's condition \u2014 misuse silently misroutes values.</p>\n<h2>Approach</h2>\n<p>1. Go <code>switch</code> cases do <strong>not</strong> fall through by default.</p>\n<p>2. The stray <code>fallthrough</code> after <code>low</code> leaks into the next case, mislabeling.</p>\n<p>3. Remove it.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Label(level int) string {\n\tlabel := \"?\"\n\tswitch level {\n\tcase 1:\n\t\tlabel = \"low\"\n\tcase 2:\n\t\tlabel = \"mid\"\n\tcase 3:\n\t\tlabel = \"high\"\n\t}\n\treturn label\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>With <code>fallthrough</code>, <code>Label(1)</code> continues into the <code>mid</code> branch and returns the wrong label. Dropping it makes each case independent.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Only add <code>fallthrough</code> when you truly want the next case body.</li>\n<li>Code after an unconditional <code>return</code> is dead.</li>\n</ul>",
    "starter": "// Package tierbug maps a numeric level to a label with a switch. A planted bug\n// adds a stray fallthrough, so level 1 leaks into the level-2 case and returns\n// the wrong label.\npackage tierbug\n\n// Label returns \"low\" for 1, \"mid\" for 2, \"high\" for 3, else \"?\".\nfunc Label(level int) string {\n\tlabel := \"?\"\n\tswitch level {\n\tcase 1:\n\t\t// CHANGE CODE BELOW THIS LINE\n\t\tlabel = \"low\"\n\t\tfallthrough\n\t\t// CHANGE CODE ABOVE THIS LINE\n\tcase 2:\n\t\tlabel = \"mid\"\n\tcase 3:\n\t\tlabel = \"high\"\n\t}\n\treturn label\n}\n",
    "debug": "",
    "file": "tierbug.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/senior/windowmax": {
    "id": "01-language-basics/04-functions/senior/windowmax",
    "title": "Sliding Window Bound",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Sliding Window Bound</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p>A sliding window of width k advances by adding the entering element <code>xs[i]</code> and</p>\n<p>removing the leaving one <code>xs[i-k]</code>. The planted bug removes <code>xs[i-k+1]</code> \u2014 off by</p>\n<p>one \u2014 so the running sum drifts.</p>\n<h2>Task</h2>\n<p>Fix the window update in windowmax.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  MaxWindow([1 2 3 4 5], 2)\nOutput: 9\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  MaxWindow([2 1 5 1 3 2], 3)\nOutput: 9\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  MaxWindow([5], 1)\nOutput: 5\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Sliding window</span><span class=\"tchip\">Index arithmetic</span><span class=\"tchip\">Off-by-one</span></div></details>",
    "education": "<h1>Sliding-window index arithmetic</h1>\n<h2>Intuition</h2>\n<p>When the window covers <code>[i-k+1 .. i]</code> after adding <code>xs[i]</code>, the element that just left is <code>xs[i-k]</code>; off-by-one here is a classic bug.</p>\n<h2>Approach</h2>\n<p>1. A sliding window of width <code>k</code> drops the element <code>k</code> positions back.</p>\n<p>2. The bug subtracts <code>xs[i-k+1]</code> (off by one); use <code>xs[i-k]</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc MaxWindow(xs []int, k int) int {\n\tbest := 0\n\tfor i := 0; i &lt; k; i++ {\n\t\tbest += xs[i]\n\t}\n\tcur := best\n\tfor i := k; i &lt; len(xs); i++ {\n\t\tcur += xs[i] - xs[i-k]\n\t\tif cur &gt; best {\n\t\t\tbest = cur\n\t\t}\n\t}\n\treturn best\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>When the window advances to index <code>i</code>, the element leaving is at <code>i-k</code>. Subtracting <code>i-k+1</code> keeps a stale element in the sum and corrupts the max.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Adding <code>xs[i]</code> means removing <code>xs[i-k]</code> to keep width k.</li>\n<li>Draw the indices for one step to check the boundary.</li>\n</ul>",
    "starter": "// Package windowmax returns the maximum sum of any k consecutive elements. A\n// planted bug in the loop bound reads one element past the window, corrupting\n// the result (and risking an out-of-range on the last window).\npackage windowmax\n\n// MaxWindow returns the largest sum of k consecutive elements of xs.\n// Precondition: 0 < k <= len(xs).\nfunc MaxWindow(xs []int, k int) int {\n\tbest := 0\n\tfor i := 0; i < k; i++ {\n\t\tbest += xs[i]\n\t}\n\tcur := best\n\tfor i := k; i < len(xs); i++ {\n\t\t// CHANGE CODE BELOW THIS LINE\n\t\tcur += xs[i] - xs[i-k+1]\n\t\t// CHANGE CODE ABOVE THIS LINE\n\t\tif cur > best {\n\t\t\tbest = cur\n\t\t}\n\t}\n\treturn best\n}\n",
    "debug": "",
    "file": "windowmax.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/staff/accreset": {
    "id": "01-language-basics/04-functions/staff/accreset",
    "title": "Accumulator Reset in Recursion",
    "level": "staff",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Accumulator Reset in Recursion</h1><div class=\"p-meta\"><span class=\"pill lv-staff\">staff</span></div></div><h2>Context</h2>\n<p>A tail-recursive accumulator must carry the running total forward: <code>acc + xs[0]</code>.</p>\n<p>The bug passes <code>xs[0]</code> alone, discarding everything accumulated so far.</p>\n<h2>Task</h2>\n<p>Fix the recursive step in accreset.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Sum([1 2 3 4])\nOutput: 10\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Sum(nil)\nOutput: 0\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Sum([5])\nOutput: 5\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Accumulator threading</span><span class=\"tchip\">Tail recursion</span><span class=\"tchip\">Base case returns acc</span></div></details>",
    "education": "<h1>Accumulator threading in recursion</h1>\n<h2>Intuition</h2>\n<p>Tail-recursive folds pass the running result forward each call; replacing the accumulator instead of adding to it loses all prior work.</p>\n<h2>Approach</h2>\n<p>1. The accumulator must carry forward across recursive calls.</p>\n<p>2. The bug passes <code>xs[0]</code> (resetting acc); pass <code>acc + xs[0]</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc sumAcc(xs []int, acc int) int {\n\tif len(xs) == 0 {\n\t\treturn acc\n\t}\n\treturn sumAcc(xs[1:], acc+xs[0])\n}\n\nfunc Sum(xs []int) int {\n\treturn sumAcc(xs, 0)\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>Passing only <code>xs[0]</code> discards the running total, so the result equals the last-but-one element logic, not the sum. Adding into <code>acc</code> accumulates correctly to 10.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>The recursive call must combine <code>acc</code> with the current element.</li>\n<li>Passing only <code>xs[0]</code> restarts the fold.</li>\n</ul>",
    "starter": "// Package accreset sums a slice with a tail-recursive helper threading an\n// accumulator. A planted bug ignores the passed accumulator and restarts from\n// zero at each call, so only the last element survives.\npackage accreset\n\nfunc sumAcc(xs []int, acc int) int {\n\tif len(xs) == 0 {\n\t\treturn acc\n\t}\n\t// CHANGE CODE BELOW THIS LINE\n\treturn sumAcc(xs[1:], xs[0])\n\t// CHANGE CODE ABOVE THIS LINE\n}\n\n// Sum returns the total of xs.\nfunc Sum(xs []int) int {\n\treturn sumAcc(xs, 0)\n}\n",
    "debug": "",
    "file": "accreset.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/staff/appendwhilerange": {
    "id": "01-language-basics/04-functions/staff/appendwhilerange",
    "title": "Append While Ranging",
    "level": "staff",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Append While Ranging</h1><div class=\"p-meta\"><span class=\"pill lv-staff\">staff</span></div></div><h2>Context</h2>\n<p>The loop bound must be over the INPUT length, <code>len(xs)</code>, not the growing output</p>\n<p><code>len(out)</code>. Bounding on <code>len(out)</code> (which starts at 0) skips the whole loop, and</p>\n<p>any self-referential growing bound is a footgun.</p>\n<h2>Task</h2>\n<p>Fix the loop bound in appendwhilerange.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  DupAll([3 5])\nOutput: [3 6 5 10]\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  DupAll([1])\nOutput: [1 2]\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  DupAll(nil)\nOutput: []\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Iterate the source length</span><span class=\"tchip\">Growing collections</span><span class=\"tchip\">Snapshot the length</span></div></details>",
    "education": "<h1>Bounding loops that grow their own target</h1>\n<h2>Intuition</h2>\n<p>A loop appending to <code>out</code> must not use <code>len(out)</code> as its bound; iterate the fixed source length instead to avoid skips or runaway growth.</p>\n<h2>Approach</h2>\n<p>1. Looping over <code>len(out)</code> while appending to <code>out</code> never terminates (it grows).</p>\n<p>2. Bound the loop by the fixed input <code>len(xs)</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc DupAll(xs []int) []int {\n\tout := make([]int, 0, len(xs)*2)\n\tfor i := 0; i &lt; len(xs); i++ {\n\t\tout = append(out, xs[i], xs[i]*2)\n\t}\n\treturn out\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>Ranging over the growing <code>out</code> keeps finding new elements forever. Iterating <code>len(xs)</code> visits each original element once.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Bound on the stable input (<code>len(xs)</code>), never the growing output.</li>\n<li><code>for i := range xs</code> snapshots the count safely.</li>\n</ul>",
    "starter": "// Package appendwhilerange duplicates each element into the SAME slice. A\n// planted bug ranges over the live slice while appending to it; range snapshots\n// the ORIGINAL length, but the bug instead loops on a manual index bound that\n// grows, never terminating conceptually \u2014 here it reads grown data and doubles\n// wrongly. Fix: range a snapshot / fixed length.\npackage appendwhilerange\n\n// DupAll returns a new slice containing each element of xs followed by its\n// double: [a, 2a, b, 2b, ...].\nfunc DupAll(xs []int) []int {\n\tout := make([]int, 0, len(xs)*2)\n\t// CHANGE CODE BELOW THIS LINE\n\tfor i := 0; i < len(out); i++ {\n\t\tout = append(out, xs[i], xs[i]*2)\n\t}\n\t// CHANGE CODE ABOVE THIS LINE\n\treturn out\n}\n",
    "debug": "",
    "file": "appendwhilerange.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/staff/bareoverwrite": {
    "id": "01-language-basics/04-functions/staff/bareoverwrite",
    "title": "Named Return Left Unset",
    "level": "staff",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Named Return Left Unset</h1><div class=\"p-meta\"><span class=\"pill lv-staff\">staff</span></div></div><h2>Context</h2>\n<p>A bare <code>return</code> sends the named result's CURRENT value. Since <code>result</code> was</p>\n<p>never assigned, it is 0; the deferred <code>result *= 2</code> then doubles 0. Assign</p>\n<p><code>result</code> (from <code>local</code>) before returning so the defer doubles x.</p>\n<h2>Task</h2>\n<p>Fix bareoverwrite.go so <code>Doubled(21)</code> returns 42.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Doubled(21)\nOutput: 42\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Doubled(0)\nOutput: 0\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Doubled(-5)\nOutput: -10\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Bare return uses current named values</span><span class=\"tchip\">Deferred adjust</span><span class=\"tchip\">Assign before returning</span></div></details>",
    "education": "<h1>Bare return with deferred adjustment</h1>\n<h2>Intuition</h2>\n<p>A bare <code>return</code> returns whatever the named results currently hold; a deferred closure then transforms them. Forgetting to assign leaves the defer operating on zero.</p>\n<h2>Approach</h2>\n<p>1. A bare <code>return</code> sends the current named return value \u2014 which was never set.</p>\n<p>2. Assign <code>result = local</code> before returning.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Doubled(x int) (result int) {\n\tdefer func() { result *= 2 }()\n\tlocal := x\n\t_ = local\n\tresult = local\n\treturn\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>The bug returns the zero-valued <code>result</code>. Assigning the computed <code>local</code> to <code>result</code> first makes the bare return hand back 42.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>A bare <code>return</code> on an unassigned named result yields its zero value.</li>\n<li>The deferred edit acts on that value \u2014 assign it first.</li>\n</ul>",
    "starter": "// Package bareoverwrite doubles a result via a deferred adjust. A planted bug\n// never assigns the named return before a bare `return`, so the deferred\n// doubling acts on zero.\npackage bareoverwrite\n\n// Doubled computes x*2 by assigning the named return then letting a deferred\n// closure double it. A planted bug forgets to assign result, so it returns 0.\nfunc Doubled(x int) (result int) {\n\tdefer func() { result *= 2 }()\n\tlocal := x\n\t_ = local\n\t// CHANGE CODE BELOW THIS LINE\n\treturn\n\t// CHANGE CODE ABOVE THIS LINE\n}\n",
    "debug": "",
    "file": "bareoverwrite.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/staff/bytesum": {
    "id": "01-language-basics/04-functions/staff/bytesum",
    "title": "Accumulator Type Overflow",
    "level": "staff",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Accumulator Type Overflow</h1><div class=\"p-meta\"><span class=\"pill lv-staff\">staff</span></div></div><h2>Context</h2>\n<p>A <code>uint8</code> accumulator wraps modulo 256, so 200+100+50 = 350 becomes 94. The</p>\n<p>accumulator's type must be wide enough for the total, not just for each element.</p>\n<h2>Task</h2>\n<p>Fix the accumulator type in bytesum.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Sum([]byte{200, 100, 50})\nOutput: 350\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Sum([]byte{255, 1})\nOutput: 256\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Sum(nil)\nOutput: 0\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Accumulator width</span><span class=\"tchip\">Unsigned wraparound</span><span class=\"tchip\">Type of the running total</span></div></details>",
    "education": "<h1>Choosing an accumulator type</h1>\n<h2>Intuition</h2>\n<p>Summation can exceed the element type's range; the accumulator must be sized for the aggregate, or unsigned/overflow wraps the result silently.</p>\n<h2>Approach</h2>\n<p>1. A <code>uint8</code> accumulator overflows past 255.</p>\n<p>2. Accumulate into an <code>int</code>, converting each byte: <code>total += int(b)</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Sum(bs []byte) int {\n\tvar total int\n\tfor _, b := range bs {\n\t\ttotal += int(b)\n\t}\n\treturn total\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>uint8</code> wraps: 200+100 already overflows. Summing into an <code>int</code> keeps the full total 350.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>uint8 += ...</code> wraps at 256; the sum of small bytes can still overflow.</li>\n<li>Accumulate in <code>int</code> (or <code>uint64</code>) and convert the element, not the total.</li>\n</ul>",
    "starter": "// Package bytesum sums byte values. A planted bug accumulates into a uint8,\n// which overflows (wraps mod 256) for totals above 255.\npackage bytesum\n\n// Sum returns the total of the byte values in bs.\nfunc Sum(bs []byte) int {\n\t// CHANGE CODE BELOW THIS LINE\n\tvar total uint8\n\tfor _, b := range bs {\n\t\ttotal += b\n\t}\n\treturn int(total)\n\t// CHANGE CODE ABOVE THIS LINE\n}\n",
    "debug": "",
    "file": "bytesum.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/staff/capretain": {
    "id": "01-language-basics/04-functions/staff/capretain",
    "title": "Sub-slice Retains Backing",
    "level": "staff",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Sub-slice Retains Backing</h1><div class=\"p-meta\"><span class=\"pill lv-staff\">staff</span></div></div><h2>Context</h2>\n<p><code>xs[:k]</code> shares the backing array and keeps its full capacity, so appending to</p>\n<p>the head writes into <code>xs[k]</code>. A full-slice expression <code>xs[:k:k]</code> caps capacity</p>\n<p>to k, forcing appends to allocate \u2014 and lets the rest of the array be freed.</p>\n<h2>Task</h2>\n<p>Fix capretain.go so appends to the head don't touch the original.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  h := Head([1 2 3 4 5], 2); append(h, 99)\nOutput: xs[2] stays 3\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  cap(Head(xs, 2))\nOutput: 2\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  append reallocates\nOutput: true\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Three-index slice</span><span class=\"tchip\">Capacity sharing</span><span class=\"tchip\">Append safety + GC</span></div></details>",
    "education": "<h1>Full-slice expressions and capacity</h1>\n<h2>Intuition</h2>\n<p><code>s[low:high:max]</code> sets capacity to <code>max-low</code>; using it to cap a sub-slice prevents appends from reaching the parent and lets the unused tail be garbage-collected.</p>\n<h2>Approach</h2>\n<p>1. <code>xs[:k]</code> retains the original capacity, so appending overwrites <code>xs[k]</code>.</p>\n<p>2. Use a full-slice bound <code>xs[:k:k]</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Head(xs []int, k int) []int {\n\treturn xs[:k:k]\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>xs[:2]</code> keeps capacity 5, so <code>append(h, 99)</code> writes into <code>xs[2]</code>. <code>xs[:2:2]</code> caps capacity, forcing a fresh array on append.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>xs[:k]</code> keeps cap == cap(xs); appends spill into the parent.</li>\n<li><code>xs[:k:k]</code> (or a copy) isolates the head.</li>\n</ul>",
    "starter": "// Package capretain extracts a small head from a large slice for long-term\n// keeping. A planted bug returns a plain re-slice, which retains the ENTIRE\n// backing array (a memory leak) AND lets an append into spare capacity clobber\n// data beyond the head. The test detects the clobber.\npackage capretain\n\n// Head returns the first k elements as an independent slice whose appends cannot\n// reach the original tail.\nfunc Head(xs []int, k int) []int {\n\t// CHANGE CODE BELOW THIS LINE\n\treturn xs[:k]\n\t// CHANGE CODE ABOVE THIS LINE\n}\n",
    "debug": "",
    "file": "capretain.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/staff/commaok": {
    "id": "01-language-basics/04-functions/staff/commaok",
    "title": "Ignoring comma-ok",
    "level": "staff",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Ignoring comma-ok</h1><div class=\"p-meta\"><span class=\"pill lv-staff\">staff</span></div></div><h2>Context</h2>\n<p>Indexing a map returns the zero value for a missing key, indistinguishable from</p>\n<p>a stored zero. The two-result form <code>v, ok := m[k]</code> reports presence; hardcoding</p>\n<p><code>ok = true</code> misclassifies absent keys.</p>\n<h2>Task</h2>\n<p>Fix commaok.go so absent keys report ok=false.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Lookup({a:0, b:5}, \"a\")\nOutput: 0, true\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Lookup({a:0, b:5}, \"z\")\nOutput: 0, false\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  present key with zero value\nOutput: 0, true\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">comma-ok map read</span><span class=\"tchip\">Zero vs absent</span><span class=\"tchip\">Propagate ok</span></div></details>",
    "education": "<h1>The comma-ok idiom for maps</h1>\n<h2>Intuition</h2>\n<p><code>v, ok := m[k]</code> is the only way to tell a stored zero from a missing key; the single-value form collapses them.</p>\n<h2>Approach</h2>\n<p>1. A plain map read can't distinguish a missing key from a zero value.</p>\n<p>2. Use the comma-ok form: <code>score, ok = scores[name]</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Lookup(scores map[string]int, name string) (score int, ok bool) {\n\tscore, ok = scores[name]\n\treturn\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>The bug always sets <code>ok = true</code>, so a missing key looks present. Comma-ok reports <code>false</code> for absent keys while still returning <code>0, true</code> for a real zero.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>m[k]</code> alone can't distinguish missing from zero-valued.</li>\n<li>Always use <code>v, ok := m[k]</code> when absence matters.</li>\n</ul>",
    "starter": "// Package commaok looks up a score, distinguishing \"absent\" from \"zero\". A\n// planted bug ignores the comma-ok boolean, so a missing key (zero value) is\n// reported as a real 0 score.\npackage commaok\n\n// Lookup returns the score for name and whether it was present.\nfunc Lookup(scores map[string]int, name string) (score int, ok bool) {\n\t// CHANGE CODE BELOW THIS LINE\n\tscore = scores[name]\n\tok = true\n\t// CHANGE CODE ABOVE THIS LINE\n\treturn\n}\n",
    "debug": "",
    "file": "commaok.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/staff/compactcopy": {
    "id": "01-language-basics/04-functions/staff/compactcopy",
    "title": "Overlapping Copy Shift",
    "level": "staff",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Overlapping Copy Shift</h1><div class=\"p-meta\"><span class=\"pill lv-staff\">staff</span></div></div><h2>Context</h2>\n<p>To drop index 0, the tail <code>xs[1:]</code> must move LEFT into <code>xs[:len-1]</code>, i.e.</p>\n<p><code>copy(xs, xs[1:])</code>. The bug copies <code>xs[:len-1]</code> into <code>xs[1:]</code>, shifting right and</p>\n<p>duplicating the first element.</p>\n<h2>Task</h2>\n<p>Fix the copy in compactcopy.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  DropFirst([10 20 30 40])\nOutput: [20 30 40]\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  DropFirst([1 2])\nOutput: [2]\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  DropFirst([9])\nOutput: []\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Compacting shift</span><span class=\"tchip\">copy overlap direction</span><span class=\"tchip\">Trim length</span></div></details>",
    "education": "<h1>Overlapping copy direction</h1>\n<h2>Intuition</h2>\n<p><code>copy</code> handles overlapping ranges as if through a temporary, but the direction you choose determines whether you compact or duplicate.</p>\n<h2>Approach</h2>\n<p>1. To drop the first element, shift the tail left: <code>copy(xs, xs[1:])</code>.</p>\n<p>2. The bug copies right-to-left with wrong bounds, duplicating data.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc DropFirst(xs []int) []int {\n\tif len(xs) == 0 {\n\t\treturn xs\n\t}\n\tcopy(xs, xs[1:])\n\treturn xs[:len(xs)-1]\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>copy(xs[1:], xs[:len-1])</code> shifts the wrong way. <code>copy(xs, xs[1:])</code> moves each element down one, then reslicing drops the last stale slot.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Drop-first shifts LEFT: <code>copy(xs, xs[1:])</code>.</li>\n<li>Insert shifts RIGHT: <code>copy(xs[i+1:], xs[i:])</code>.</li>\n</ul>",
    "starter": "// Package compactcopy removes the element at index 0 by shifting left with copy.\n// A planted bug copies in the wrong direction, so the shift duplicates rather\n// than compacts.\npackage compactcopy\n\n// DropFirst removes the first element by shifting the tail left, then trims the\n// length. Returns the compacted slice.\nfunc DropFirst(xs []int) []int {\n\tif len(xs) == 0 {\n\t\treturn xs\n\t}\n\t// CHANGE CODE BELOW THIS LINE\n\tcopy(xs[1:], xs[:len(xs)-1])\n\t// CHANGE CODE ABOVE THIS LINE\n\treturn xs[:len(xs)-1]\n}\n",
    "debug": "",
    "file": "compactcopy.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/staff/deferbeforeacq": {
    "id": "01-language-basics/04-functions/staff/deferbeforeacq",
    "title": "Defer Before Acquisition",
    "level": "staff",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Defer Before Acquisition</h1><div class=\"p-meta\"><span class=\"pill lv-staff\">staff</span></div></div><h2>Context</h2>\n<p>A cleanup <code>defer</code> must be scheduled AFTER the resource is successfully acquired.</p>\n<p>Scheduling it first means the early <code>return</code> on the failure path still runs the</p>\n<p>release, cleaning up something that was never created.</p>\n<h2>Task</h2>\n<p>Fix deferbeforeacq.go so nothing is released when nothing is acquired.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Use(false)\nOutput: []\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Use(true)\nOutput: [open close]\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  close only after open\nOutput: true\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Acquire-then-defer</span><span class=\"tchip\">Early return path</span><span class=\"tchip\">Resource discipline</span></div></details>",
    "education": "<h1>Ordering defer relative to acquisition</h1>\n<h2>Intuition</h2>\n<p>A <code>defer cleanup()</code> placed before the acquisition check runs on every exit path, including failures where nothing was acquired.</p>\n<h2>Approach</h2>\n<p>1. Register the cleanup <strong>after</strong> the resource is acquired.</p>\n<p>2. The bug defers <code>close</code> before the early return, logging a close with no open.</p>\n<p>3. Move the defer below the guard and the <code>open</code> step.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Use(ok bool) (log []string) {\n\tif !ok {\n\t\treturn\n\t}\n\tlog = append(log, \"open\")\n\tdefer func() { log = append(log, \"close\") }()\n\treturn\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>Deferring first makes <code>Use(false)</code> log <code>[close]</code> though nothing opened. Placing the defer after <code>open</code> pairs them correctly.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Put <code>defer resource.Close()</code> immediately AFTER a successful open, not before.</li>\n<li>A defer above an early error return still executes.</li>\n</ul>",
    "starter": "// Package deferbeforeacq models acquire/release logging. A planted bug schedules\n// the release defer BEFORE checking whether the resource was acquired, so a\n// release is logged even when nothing was opened.\npackage deferbeforeacq\n\n// Use logs \"open\" then \"close\" when ok is true, and logs NOTHING when ok is\n// false (the resource was never acquired, so it must not be released).\nfunc Use(ok bool) (log []string) {\n\t// CHANGE CODE BELOW THIS LINE\n\tdefer func() { log = append(log, \"close\") }()\n\tif !ok {\n\t\treturn\n\t}\n\tlog = append(log, \"open\")\n\t// CHANGE CODE ABOVE THIS LINE\n\treturn\n}\n",
    "debug": "",
    "file": "deferbeforeacq.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/staff/deferslicearg": {
    "id": "01-language-basics/04-functions/staff/deferslicearg",
    "title": "Defer Snapshots Slice Header",
    "level": "staff",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Defer Snapshots Slice Header</h1><div class=\"p-meta\"><span class=\"pill lv-staff\">staff</span></div></div><h2>Context</h2>\n<p>Passing <code>xs</code> as a deferred argument copies its slice HEADER (ptr,len,cap) at</p>\n<p>defer-time, when len is 0. Later appends reassign <code>xs</code> to a new header the</p>\n<p>snapshot never sees. Read <code>xs</code> in the closure body to get the final length.</p>\n<h2>Task</h2>\n<p>Fix deferslicearg.go so the reported length is final.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  BuildAndReport(4)\nOutput: 4\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  BuildAndReport(0)\nOutput: 0\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  reported equals final length\nOutput: true\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Slice header snapshot</span><span class=\"tchip\">Reassignment invisibility</span><span class=\"tchip\">Body capture</span></div></details>",
    "education": "<h1>Slice headers as deferred arguments</h1>\n<h2>Intuition</h2>\n<p>A deferred argument snapshots the slice header (including length) at defer-time; since append reassigns the variable, only a body capture sees the final slice.</p>\n<h2>Approach</h2>\n<p>1. A deferred argument snapshots the slice header at <code>defer</code> time.</p>\n<p>2. If <code>xs</code> grows afterward, the snapshot's length is stale.</p>\n<p>3. Close over <code>xs</code> instead of passing it.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc BuildAndReport(n int) (reported int) {\n\tvar xs []int\n\tdefer func() { reported = len(xs) }()\n\tfor i := 0; i &lt; n; i++ {\n\t\txs = append(xs, i)\n\t}\n\treturn\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>defer f(xs)</code> captures the length before the builds finish. A parameterless defer reading <code>len(xs)</code> at return sees the final length.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>defer f(xs)</code> freezes xs's header (len 0) before the appends.</li>\n<li>Reference <code>xs</code> in the closure body for the final value.</li>\n</ul>",
    "starter": "// Package deferslicearg logs the FINAL length of a slice via defer. A planted\n// bug passes the slice as a deferred argument, snapshotting its header (length 0)\n// before elements are appended, so the logged length is wrong.\npackage deferslicearg\n\n// BuildAndReport appends n items to a slice and returns the length recorded by a\n// deferred reporter. The reporter must see the FINAL length.\nfunc BuildAndReport(n int) (reported int) {\n\tvar xs []int\n\t// CHANGE CODE BELOW THIS LINE\n\tdefer func(s []int) { reported = len(s) }(xs)\n\t// CHANGE CODE ABOVE THIS LINE\n\tfor i := 0; i < n; i++ {\n\t\txs = append(xs, i)\n\t}\n\treturn\n}\n",
    "debug": "",
    "file": "deferslicearg.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/staff/elemptr": {
    "id": "01-language-basics/04-functions/staff/elemptr",
    "title": "Pointer to Element Before Grow",
    "level": "staff",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Pointer to Element Before Grow</h1><div class=\"p-meta\"><span class=\"pill lv-staff\">staff</span></div></div><h2>Context</h2>\n<p>A full slice reallocates on append; a pointer taken beforehand still points at</p>\n<p>the OLD array. Writing through it leaves the returned (new) slice unchanged.</p>\n<h2>Task</h2>\n<p>Fix elemptr.go so the result's first element is 42.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  BumpHead(9)\nOutput: [42 9]\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  write lands on element 0\nOutput: true\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  pointer re-taken after grow\nOutput: true\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Reallocation on growth</span><span class=\"tchip\">Stale element pointer</span><span class=\"tchip\">Re-address after append</span></div></details>",
    "education": "<h1>Element pointers across reallocation</h1>\n<h2>Intuition</h2>\n<p><code>append</code> beyond capacity detaches pre-existing element pointers from the live slice; you must re-take the address after growth.</p>\n<h2>Approach</h2>\n<p>1. After the slice grows, the earlier element pointer is stale.</p>\n<p>2. Re-take <code>p = &amp;xs[0]</code> on the current backing array before writing 42.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc BumpHead(v int) []int {\n\txs := make([]int, 1, 1)\n\txs[0] = 1\n\tp := &amp;xs[0]\n\txs = append(xs, v)\n\tp = &amp;xs[0]\n\t*p = 42\n\treturn xs\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>append</code> may relocate the array, so the old <code>p</code> points at freed memory. Re-deriving <code>p</code> from <code>xs[0]</code> writes 42 into the live head.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>After a reallocating append, <code>&amp;xs[i]</code> from before is stale.</li>\n<li>Re-take it, or index the current slice.</li>\n</ul>",
    "starter": "// Package elemptr returns a pointer into a slice and the grown slice. A planted\n// bug takes the element pointer, then grows the slice (reallocating), so the\n// pointer is stale and writing through it doesn't affect the returned slice.\npackage elemptr\n\n// BumpHead appends v (reallocating a full slice), then must make the first\n// element of the RESULT equal to 42 by writing through a pointer.\nfunc BumpHead(v int) []int {\n\txs := make([]int, 1, 1)\n\txs[0] = 1\n\tp := &xs[0]\n\txs = append(xs, v)\n\t// CHANGE CODE BELOW THIS LINE\n\t*p = 42\n\t// CHANGE CODE ABOVE THIS LINE\n\treturn xs\n}\n",
    "debug": "",
    "file": "elemptr.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/staff/iifeinit": {
    "id": "01-language-basics/04-functions/staff/iifeinit",
    "title": "Immediately-Invoked Init",
    "level": "staff",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Immediately-Invoked Init</h1><div class=\"p-meta\"><span class=\"pill lv-staff\">staff</span></div></div><h2>Context</h2>\n<p>An immediately-invoked function literal must be CALLED with a trailing <code>()</code> to</p>\n<p>produce its result. The bug stores the function itself and returns nil; add the</p>\n<p>call and return the built map.</p>\n<h2>Task</h2>\n<p>Fix iifeinit.go so the table is built and returned.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  BuildTable(4)\nOutput: {0:0, 1:1, 2:4, 3:9}\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  BuildTable(1)\nOutput: {0:0}\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  BuildTable(0)\nOutput: {}\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Immediately-invoked function</span><span class=\"tchip\">Function value vs call</span><span class=\"tchip\">Scoped initialisation</span></div></details>",
    "education": "<h1>Immediately-invoked function expressions</h1>\n<h2>Intuition</h2>\n<p>Appending <code>()</code> to a function literal runs it at once, yielding a value \u2014 handy for scoped, complex initialisation; omitting the call leaves you with the function itself.</p>\n<h2>Approach</h2>\n<p>1. The initializer closure must be <strong>called</strong> to produce the map.</p>\n<p>2. The bug assigns the function itself; return the immediately-invoked <code>func() map[int]int { ... }()</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc BuildTable(n int) map[int]int {\n\treturn func() map[int]int {\n\t\tm := map[int]int{}\n\t\tfor i := 0; i &lt; n; i++ {\n\t\t\tm[i] = i * i\n\t\t}\n\t\treturn m\n\t}()\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>Assigning the closure to <code>table</code> leaves a function, not a map. Invoking it with <code>()</code> runs the loop and yields the populated table.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>x := func(){...}</code> stores a func; <code>x := func(){...}()</code> stores its result.</li>\n<li>IIFEs keep setup logic local without a named helper.</li>\n</ul>",
    "starter": "// Package iifeinit initialises a lookup table via an immediately-invoked\n// function. A planted bug assigns the function VALUE instead of invoking it, so\n// the table field holds a func, and the builder returns an empty table.\npackage iifeinit\n\n// BuildTable returns a map from i to i*i for i in [0,n), built via an\n// immediately-invoked function literal.\nfunc BuildTable(n int) map[int]int {\n\t// CHANGE CODE BELOW THIS LINE\n\ttable := func() map[int]int {\n\t\tm := map[int]int{}\n\t\tfor i := 0; i < n; i++ {\n\t\t\tm[i] = i * i\n\t\t}\n\t\treturn m\n\t}\n\t_ = table\n\treturn nil\n\t// CHANGE CODE ABOVE THIS LINE\n}\n",
    "debug": "",
    "file": "iifeinit.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/staff/labeledcont": {
    "id": "01-language-basics/04-functions/staff/labeledcont",
    "title": "Labeled Continue",
    "level": "staff",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Labeled Continue</h1><div class=\"p-meta\"><span class=\"pill lv-staff\">staff</span></div></div><h2>Context</h2>\n<p><code>break Rows</code> exits the OUTER loop entirely, so scanning stops at the first row</p>\n<p>with a negative and all later rows go uncounted. The intent is to skip just the</p>\n<p>current row and keep going: <code>continue Rows</code>.</p>\n<h2>Task</h2>\n<p>Fix labeledcont.go so a negative skips only that row, not the rest.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  CleanRows([[1 2 3],[1 -1 2],[4 5]])\nOutput: 2\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  rows with a negative are skipped\nOutput: true\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  count of clean rows\nOutput: 2\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Labeled continue vs break</span><span class=\"tchip\">Nested-loop control</span><span class=\"tchip\">Skip past the increment</span></div></details>",
    "education": "<h1>Labeled break vs continue</h1>\n<h2>Intuition</h2>\n<p>A label lets <code>break</code>/<code>continue</code> act on an enclosing loop; <code>break Label</code> terminates it while <code>continue Label</code> moves to its next iteration \u2014 a one-word difference with opposite effects.</p>\n<h2>Approach</h2>\n<p>1. On a bad cell we should skip to the next row, not stop all rows.</p>\n<p>2. The bug <code>break Rows</code> exits the outer loop; use <code>continue Rows</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc CleanRows(grid [][]int) int {\n\tcount := 0\nRows:\n\tfor _, row := range grid {\n\t\tfor _, v := range row {\n\t\t\tif v &lt; 0 {\n\t\t\t\tcontinue Rows\n\t\t\t}\n\t\t}\n\t\tcount++\n\t}\n\treturn count\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>break Rows</code> abandons all remaining rows on the first negative. <code>continue Rows</code> skips only the offending row, so the count reaches 2.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>break Rows</code> ends the outer loop; <code>continue Rows</code> skips the current outer iteration.</li>\n<li>Pick the verb that matches 'skip this row' vs 'stop scanning'.</li>\n</ul>",
    "starter": "// Package labeledcont counts rows of a grid that contain NO negative number. A\n// planted bug uses a bare continue, which only skips to the next column instead\n// of abandoning the whole row, so rows with negatives are still counted.\npackage labeledcont\n\n// CleanRows returns how many rows contain no negative value.\nfunc CleanRows(grid [][]int) int {\n\tcount := 0\nRows:\n\tfor _, row := range grid {\n\t\tfor _, v := range row {\n\t\t\tif v < 0 {\n\t\t\t\t// CHANGE CODE BELOW THIS LINE\n\t\t\t\tbreak Rows\n\t\t\t\t// CHANGE CODE ABOVE THIS LINE\n\t\t\t}\n\t\t}\n\t\tcount++\n\t}\n\treturn count\n}\n",
    "debug": "",
    "file": "labeledcont.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/staff/maskpanic": {
    "id": "01-language-basics/04-functions/staff/maskpanic",
    "title": "Deferred Panic Masks Original",
    "level": "staff",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Deferred Panic Masks Original</h1><div class=\"p-meta\"><span class=\"pill lv-staff\">staff</span></div></div><h2>Context</h2>\n<p>If a deferred function panics during unwinding, its panic REPLACES the one in</p>\n<p>flight; the outer recover then sees the cleanup's value, not the original. A</p>\n<p>cleanup must not panic (or must recover its own).</p>\n<h2>Task</h2>\n<p>Fix maskpanic.go so the original panic value survives.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  FirstPanic(func(){ panic(\"original\") })\nOutput: \"original\"\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  deferred cleanup does not mask\nOutput: true\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  FirstPanic reports first panic\nOutput: original\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Panic during unwind</span><span class=\"tchip\">Cleanup discipline</span><span class=\"tchip\">recover sees the latest</span></div></details>",
    "education": "<h1>Panics during deferred unwinding</h1>\n<h2>Intuition</h2>\n<p>A panic in a deferred call replaces the panic currently unwinding; the last panic wins, so cleanup code that panics erases the original error.</p>\n<h2>Approach</h2>\n<p>1. A panic inside a deferred function replaces the original panic value.</p>\n<p>2. Remove the <code>panic(\"cleanup-failure\")</code> so the first panic survives.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc FirstPanic(f func()) (got any) {\n\tdefer func() {\n\t\tgot = recover()\n\t}()\n\tf()\n\treturn\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>The deferred <code>panic</code> overwrites the in-flight panic, masking \"original\". Dropping it lets the recover capture the real first panic.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>A second panic during unwind overwrites the first.</li>\n<li>Keep deferred cleanups panic-free, or recover inside them.</li>\n</ul>",
    "starter": "// Package maskpanic recovers the ORIGINAL panic value. A planted bug has a\n// second deferred function panic during unwind, replacing the original value\n// that the outer recover then sees.\npackage maskpanic\n\n// FirstPanic runs f (which panics), and returns the value of the ORIGINAL panic.\n// A cleanup step must not overwrite that value by panicking itself.\nfunc FirstPanic(f func()) (got any) {\n\tdefer func() {\n\t\tgot = recover()\n\t}()\n\t// CHANGE CODE BELOW THIS LINE\n\tdefer func() {\n\t\tpanic(\"cleanup-failure\")\n\t}()\n\t// CHANGE CODE ABOVE THIS LINE\n\tf()\n\treturn\n}\n",
    "debug": "",
    "file": "maskpanic.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/staff/methodval": {
    "id": "01-language-basics/04-functions/staff/methodval",
    "title": "Method Value Binds Receiver",
    "level": "staff",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Method Value Binds Receiver</h1><div class=\"p-meta\"><span class=\"pill lv-staff\">staff</span></div></div><h2>Context</h2>\n<p>A method value like <code>c.Get</code> copies the receiver at the moment it is created (for</p>\n<p>a value receiver). Re-binding <code>f = c.Get</code> after mutating <code>c</code> captures the NEW</p>\n<p>receiver state, defeating the early snapshot.</p>\n<h2>Task</h2>\n<p>Fix methodval.go so the returned function reports the early value.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  BoundEarly(7)()\nOutput: 7\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  method value binds receiver at creation\nOutput: true\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  later receiver change ignored\nOutput: true\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Method values</span><span class=\"tchip\">Value receiver copy</span><span class=\"tchip\">Re-binding</span></div></details>",
    "education": "<h1>Method values snapshot the receiver</h1>\n<h2>Intuition</h2>\n<p>Binding a value-receiver method (<code>c.Get</code>) copies the receiver then; the bound function is independent of later changes to <code>c</code> \u2014 unless you re-bind.</p>\n<h2>Approach</h2>\n<p>1. <code>c.Get</code> is a method <strong>value</strong>: it binds the receiver when created.</p>\n<p>2. The stray reassignment <code>f = c.Get</code> is redundant/incorrect; remove it so the intended binding stands.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\ntype counter struct{ n int }\n\nfunc (c counter) Get() int { return c.n }\n\nfunc BoundEarly(start int) func() int {\n\tc := counter{n: start}\n\tf := c.Get // method value: receiver copied now\n\tc.n = 999  // must NOT affect f (value receiver was copied)\n\treturn f\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>The method value already captures the receiver's state at bind time; the extra line re-binds to a stale copy. Removing it returns the correctly bound closure.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>f := c.Get</code> copies <code>c</code> now; mutating <code>c</code> afterwards doesn't change <code>f</code>.</li>\n<li>Re-binding after a mutation captures the newer receiver.</li>\n</ul>",
    "starter": "// Package methodval captures a bound method value. A planted bug rebinds the\n// method to a later receiver state, but a method VALUE snapshots the receiver\n// when created \u2014 the test pins that a value bound early sees the early state.\npackage methodval\n\ntype counter struct{ n int }\n\nfunc (c counter) Get() int { return c.n }\n\n// BoundEarly returns a function that reports the counter's value AS IT WAS when\n// the function was created (n = start), even though the local is later changed.\nfunc BoundEarly(start int) func() int {\n\tc := counter{n: start}\n\tf := c.Get // method value: receiver copied now\n\tc.n = 999  // must NOT affect f (value receiver was copied)\n\t// CHANGE CODE BELOW THIS LINE\n\tf = c.Get\n\t// CHANGE CODE ABOVE THIS LINE\n\treturn f\n}\n",
    "debug": "",
    "file": "methodval.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/staff/nilappend": {
    "id": "01-language-basics/04-functions/staff/nilappend",
    "title": "Append to Nil Is Valid",
    "level": "staff",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Append to Nil Is Valid</h1><div class=\"p-meta\"><span class=\"pill lv-staff\">staff</span></div></div><h2>Context</h2>\n<p><code>append</code> to a nil slice allocates and works exactly like appending to an empty</p>\n<p>one. Guarding against nil with an early return discards data unnecessarily.</p>\n<h2>Task</h2>\n<p>Fix nilappend.go so a nil starting slice still collects the extras.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Collect(nil, [1 2 3])\nOutput: [1 2 3]\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Collect([0], [9])\nOutput: [0 9]\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Collect(nil, nil)\nOutput: []\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">nil slice append</span><span class=\"tchip\">nil vs empty</span><span class=\"tchip\">Unnecessary guard</span></div></details>",
    "education": "<h1>nil slices are valid append targets</h1>\n<h2>Intuition</h2>\n<p>A nil slice has len 0 and appends normally; special-casing it is unnecessary and here actively wrong.</p>\n<h2>Approach</h2>\n<p>1. <code>append</code> to a nil slice is valid and returns a fresh slice.</p>\n<p>2. Remove the early <code>if xs == nil { return nil }</code> that drops the extras.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Collect(xs []int, extra []int) []int {\n\tfor _, v := range extra {\n\t\txs = append(xs, v)\n\t}\n\treturn xs\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>The guard returns nil before appending, losing the <code>extra</code> elements. Deleting it lets <code>append</code> build the combined slice from a nil base.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>append(nil, ...)</code> allocates a fresh slice \u2014 no pre-init needed.</li>\n<li>Ranging or <code>len</code> on a nil slice is also fine.</li>\n</ul>",
    "starter": "// Package nilappend collects results into a slice that starts nil. A planted bug\n// bails out early when the slice is nil, thinking it must be initialised first,\n// so it returns nothing.\npackage nilappend\n\n// Collect returns xs with each element of extra appended. xs may be nil, which\n// is a valid empty slice to append to.\nfunc Collect(xs []int, extra []int) []int {\n\t// CHANGE CODE BELOW THIS LINE\n\tif xs == nil {\n\t\treturn nil\n\t}\n\t// CHANGE CODE ABOVE THIS LINE\n\tfor _, v := range extra {\n\t\txs = append(xs, v)\n\t}\n\treturn xs\n}\n",
    "debug": "",
    "file": "nilappend.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/staff/nilcallback": {
    "id": "01-language-basics/04-functions/staff/nilcallback",
    "title": "Optional Callback Nil Check",
    "level": "staff",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Optional Callback Nil Check</h1><div class=\"p-meta\"><span class=\"pill lv-staff\">staff</span></div></div><h2>Context</h2>\n<p>A nil function value is callable syntactically but panics when invoked. Optional</p>\n<p>callbacks must be nil-checked before the call.</p>\n<h2>Task</h2>\n<p>Fix nilcallback.go so a nil hook is skipped (element added unchanged).</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Process([1 2 3], nil)\nOutput: 6\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Process([1 2 3], x*10)\nOutput: 60\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Process(nil, nil)\nOutput: 0\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Nil function values</span><span class=\"tchip\">Optional hooks</span><span class=\"tchip\">Default behaviour</span></div></details>",
    "education": "<h1>Nil function values</h1>\n<h2>Intuition</h2>\n<p>A func variable defaults to nil and panics on call; optional callbacks must be checked before invocation.</p>\n<h2>Approach</h2>\n<p>1. Calling a nil <code>hook</code> panics.</p>\n<p>2. Guard: use <code>hook(v)</code> when non-nil, else add <code>v</code> unchanged.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Process(xs []int, hook func(int) int) int {\n\ttotal := 0\n\tfor _, v := range xs {\n\t\tif hook != nil {\n\t\t\ttotal += hook(v)\n\t\t} else {\n\t\t\ttotal += v\n\t\t}\n\t}\n\treturn total\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>Process(xs, nil)</code> panics calling the nil function. The guard sums raw values when no hook is provided, giving 6.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>A nil func value is not callable \u2014 check <code>hook != nil</code> first.</li>\n<li>Provide a sensible default (identity) for the nil case.</li>\n</ul>",
    "starter": "// Package nilcallback invokes an optional hook. A planted bug calls the hook\n// without checking for nil, so a nil hook panics instead of being skipped.\npackage nilcallback\n\n// Process applies hook to each element if hook is non-nil, summing the results;\n// if hook is nil it sums the elements unchanged.\nfunc Process(xs []int, hook func(int) int) int {\n\ttotal := 0\n\tfor _, v := range xs {\n\t\t// CHANGE CODE BELOW THIS LINE\n\t\ttotal += hook(v)\n\t\t// CHANGE CODE ABOVE THIS LINE\n\t}\n\treturn total\n}\n",
    "debug": "",
    "file": "nilcallback.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/staff/prealloc": {
    "id": "01-language-basics/04-functions/staff/prealloc",
    "title": "Prealloc Length vs Append",
    "level": "staff",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Prealloc Length vs Append</h1><div class=\"p-meta\"><span class=\"pill lv-staff\">staff</span></div></div><h2>Context</h2>\n<p><code>make([]int, n)</code> creates a slice of LENGTH n (n zeros); appending then adds</p>\n<p>AFTER those zeros. To preallocate capacity without content, use</p>\n<p><code>make([]int, 0, n)</code>.</p>\n<h2>Task</h2>\n<p>Fix prealloc.go so the result has no leading zeros.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Doubles(3)\nOutput: [2 4 6]\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Doubles(0)\nOutput: []\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Doubles(1)\nOutput: [2]\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">length vs capacity in make</span><span class=\"tchip\">Append after prealloc</span><span class=\"tchip\">Zero-length, n-capacity</span></div></details>",
    "education": "<h1>make length vs capacity</h1>\n<h2>Intuition</h2>\n<p><code>make([]T, n)</code> fills n zero elements; <code>make([]T, 0, n)</code> reserves capacity but stays empty \u2014 mixing the two with append yields leading zeros.</p>\n<h2>Approach</h2>\n<p>1. <code>make([]int, n)</code> pre-fills <code>n</code> zeros; appending then adds after them.</p>\n<p>2. Use <code>make([]int, 0, n)</code> to preallocate capacity without length.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Doubles(n int) []int {\n\tout := make([]int, 0, n)\n\tfor i := 1; i &lt;= n; i++ {\n\t\tout = append(out, i*2)\n\t}\n\treturn out\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>With length <code>n</code>, appending yields <code>[0 0 0 2 4 6]</code>. Starting at length 0 with capacity <code>n</code> appends into the reserved space, giving <code>[2 4 6]</code>.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>make([]T, n)</code> + append = n zeros then your data.</li>\n<li>Use <code>make([]T, 0, n)</code> to reserve, or index into a length-n slice.</li>\n</ul>",
    "starter": "// Package prealloc builds a slice of doubles. A planted bug preallocates with a\n// LENGTH of n (not capacity), then appends, producing n leading zeros followed\n// by the real data.\npackage prealloc\n\n// Doubles returns [2,4,6,...,2n].\nfunc Doubles(n int) []int {\n\t// CHANGE CODE BELOW THIS LINE\n\tout := make([]int, n)\n\t// CHANGE CODE ABOVE THIS LINE\n\tfor i := 1; i <= n; i++ {\n\t\tout = append(out, i*2)\n\t}\n\treturn out\n}\n",
    "debug": "",
    "file": "prealloc.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/staff/recoverlost": {
    "id": "01-language-basics/04-functions/staff/recoverlost",
    "title": "Recovered Error Lost to Local",
    "level": "staff",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Recovered Error Lost to Local</h1><div class=\"p-meta\"><span class=\"pill lv-staff\">staff</span></div></div><h2>Context</h2>\n<p>Inside the deferred recover, assigning to a fresh local <code>e</code> does nothing for the</p>\n<p>caller; you must assign the NAMED return <code>err</code> so it propagates out.</p>\n<h2>Task</h2>\n<p>Fix recoverlost.go so the recovered panic becomes the returned error.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Safe(func(){ panic(\"boom\") })\nOutput: non-nil error\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Safe(func(){})\nOutput: nil\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  error message contains boom\nOutput: true\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Named return from defer</span><span class=\"tchip\">Local shadow</span><span class=\"tchip\">panic -&gt; error</span></div></details>",
    "education": "<h1>Setting named returns inside deferred recover</h1>\n<h2>Intuition</h2>\n<p>The panic-to-error idiom must write the NAMED result variable in the deferred closure; a local assignment is invisible to the caller.</p>\n<h2>Approach</h2>\n<p>1. The recovered error must be written to the <strong>named return</strong> <code>err</code>.</p>\n<p>2. The bug assigns to a local <code>e</code> and discards it; assign to <code>err</code> directly.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nimport \"fmt\"\n\nfunc Safe(f func()) (err error) {\n\tdefer func() {\n\t\tif r := recover(); r != nil {\n\t\t\terr = fmt.Errorf(\"panic: %v\", r)\n\t\t}\n\t}()\n\tf()\n\treturn nil\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>Building <code>e</code> and dropping it leaves <code>err</code> nil despite the panic. Writing <code>err = fmt.Errorf(...)</code> surfaces the recovered failure to the caller.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Deferred recover must set the named return, not a fresh local.</li>\n<li><code>err = ...</code> in the closure body reaches the caller.</li>\n</ul>",
    "starter": "// Package recoverlost converts a panic into a returned error. A planted bug\n// assigns the recovered value to a LOCAL variable instead of the named return,\n// so the caller receives nil.\npackage recoverlost\n\nimport \"fmt\"\n\n// Safe runs f; if f panics, Safe returns an error describing it, else nil.\nfunc Safe(f func()) (err error) {\n\tdefer func() {\n\t\tif r := recover(); r != nil {\n\t\t\t// CHANGE CODE BELOW THIS LINE\n\t\t\te := fmt.Errorf(\"panic: %v\", r)\n\t\t\t_ = e\n\t\t\t// CHANGE CODE ABOVE THIS LINE\n\t\t}\n\t}()\n\tf()\n\treturn nil\n}\n",
    "debug": "",
    "file": "recoverlost.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/staff/recovertype": {
    "id": "01-language-basics/04-functions/staff/recovertype",
    "title": "Typed Recover Value",
    "level": "staff",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Typed Recover Value</h1><div class=\"p-meta\"><span class=\"pill lv-staff\">staff</span></div></div><h2>Context</h2>\n<p>The recovered value has type <code>any</code>; to return it as an error you must assert</p>\n<p><code>r.(error)</code>, not <code>r.(string)</code>. Asserting the wrong type never matches an error</p>\n<p>panic, so <code>err</code> stays nil.</p>\n<h2>Task</h2>\n<p>Fix the type assertion in recovertype.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Call(func(){ panic(errBoom) })\nOutput: errBoom\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Call(func(){ panic(\"x\") })\nOutput: nil\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Call(func(){})\nOutput: nil\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Type assertion on recover</span><span class=\"tchip\">comma-ok assertion</span><span class=\"tchip\">Selective conversion</span></div></details>",
    "education": "<h1>Type-asserting a recovered value</h1>\n<h2>Intuition</h2>\n<p><code>recover()</code> yields <code>any</code>; extracting a specific type needs a (comma-ok) assertion \u2014 asserting the wrong type silently misses the value.</p>\n<h2>Approach</h2>\n<p>1. Only an <code>error</code> panic should be captured into <code>err</code>.</p>\n<p>2. The bug type-asserts to <code>string</code> and drops it; assert to <code>error</code> and assign.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Call(f func()) (err error) {\n\tdefer func() {\n\t\tr := recover()\n\t\tif e, ok := r.(error); ok {\n\t\t\terr = e\n\t\t}\n\t}()\n\tf()\n\treturn\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>Asserting <code>r.(string)</code> ignores an error panic, leaving <code>err</code> nil. Asserting <code>r.(error)</code> captures <code>errBoom</code>, while a non-error panic like \"x\" leaves <code>err</code> nil.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Recovered value is <code>any</code>; use <code>r.(error)</code> (comma-ok) to get an error.</li>\n<li>A plain assertion of a wrong type would panic \u2014 always use comma-ok here.</li>\n</ul>",
    "starter": "// Package recovertype recovers a panic and returns it as an error, only when the\n// panic value IS an error. A planted bug asserts the wrong type, dropping error\n// panics.\npackage recovertype\n\n// Call runs f. If f panics with an error value, Call returns it. If f panics\n// with a non-error, Call returns nil. Normal completion returns nil.\nfunc Call(f func()) (err error) {\n\tdefer func() {\n\t\tr := recover()\n\t\t// CHANGE CODE BELOW THIS LINE\n\t\tif s, ok := r.(string); ok {\n\t\t\t_ = s\n\t\t}\n\t\t// CHANGE CODE ABOVE THIS LINE\n\t}()\n\tf()\n\treturn\n}\n",
    "debug": "",
    "file": "recovertype.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/staff/repanic": {
    "id": "01-language-basics/04-functions/staff/repanic",
    "title": "Recover Then Re-panic",
    "level": "staff",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Recover Then Re-panic</h1><div class=\"p-meta\"><span class=\"pill lv-staff\">staff</span></div></div><h2>Context</h2>\n<p>A recover handler that absorbs EVERY panic hides bugs. It should absorb only</p>\n<p>the known sentinel and re-panic anything else so real failures still surface.</p>\n<h2>Task</h2>\n<p>Fix the recover handler in repanic.go to re-panic non-sentinel values.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Run(func(){})\nOutput: true\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Run(func(){ panic(ErrSentinel) })\nOutput: false\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Run(func(){ panic(\"x\") })\nOutput: re-panics \"x\"\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Selective recovery</span><span class=\"tchip\">Re-panic</span><span class=\"tchip\">Don't mask bugs</span></div></details>",
    "education": "<h1>Selective recover and re-panic</h1>\n<h2>Intuition</h2>\n<p>A recover that doesn't re-raise unexpected values silently swallows genuine panics; production guards absorb only known sentinels and re-panic the rest.</p>\n<h2>Approach</h2>\n<p>1. Only the sentinel panic should be swallowed; anything else must propagate.</p>\n<p>2. The bug recovers everything unconditionally.</p>\n<p>3. Compare <code>r == ErrSentinel</code>; otherwise re-<code>panic(r)</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nconst ErrSentinel = \"handled\"\n\nfunc Run(f func()) (normal bool) {\n\tdefer func() {\n\t\tif r := recover(); r != nil {\n\t\t\tif r == ErrSentinel {\n\t\t\t\tnormal = false\n\t\t\t} else {\n\t\t\t\tpanic(r)\n\t\t\t}\n\t\t}\n\t}()\n\tf()\n\treturn true\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>Swallowing every panic hides real failures. Checking the recovered value lets <code>ErrSentinel</code> set <code>normal = false</code> while an unexpected panic is re-raised.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Recover then inspect <code>r</code>; re-<code>panic(r)</code> what you don't recognise.</li>\n<li>Absorbing everything turns real bugs into silent wrong behaviour.</li>\n</ul>",
    "starter": "// Package repanic recovers only a known sentinel panic and re-panics anything\n// else. A planted bug swallows every panic, hiding unexpected failures.\npackage repanic\n\n// ErrSentinel is the only panic value this guard is allowed to absorb.\nconst ErrSentinel = \"handled\"\n\n// Run calls f. If f panics with ErrSentinel it returns false (absorbed). If f\n// panics with anything else, that panic must propagate. If f is normal it\n// returns true.\nfunc Run(f func()) (normal bool) {\n\tdefer func() {\n\t\tif r := recover(); r != nil {\n\t\t\t// CHANGE CODE BELOW THIS LINE\n\t\t\tnormal = false\n\t\t\t// CHANGE CODE ABOVE THIS LINE\n\t\t}\n\t}()\n\tf()\n\treturn true\n}\n",
    "debug": "",
    "file": "repanic.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/staff/reusedbuffer": {
    "id": "01-language-basics/04-functions/staff/reusedbuffer",
    "title": "Returning a Reused Buffer",
    "level": "staff",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Returning a Reused Buffer</h1><div class=\"p-meta\"><span class=\"pill lv-staff\">staff</span></div></div><h2>Context</h2>\n<p>The closure reuses one backing array (<code>buf[:0]</code> keeps the capacity). Returning</p>\n<p><code>buf</code> hands out a view into that shared array, so the NEXT call's <code>buf[:0]</code> +</p>\n<p>append overwrites the elements an earlier caller still holds. Return a copy.</p>\n<h2>Task</h2>\n<p>Fix reusedbuffer.go so earlier results survive later calls.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  r := Reader(); a := r(1,2); b := r(3,4)\nOutput: a stays [1 2]\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  a and b independent\nOutput: true\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  a after second call\nOutput: [1 2]\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Reused backing array</span><span class=\"tchip\">Returning a shared view</span><span class=\"tchip\">Defensive copy</span></div></details>",
    "education": "<h1>Returning reused buffers</h1>\n<h2>Intuition</h2>\n<p>Reusing a buffer with <code>buf[:0]</code> avoids allocations, but returning it aliases shared memory that the next call overwrites; hand out a copy when the caller keeps the result.</p>\n<h2>Approach</h2>\n<p>1. Returning the internal <code>buf</code> shares it across calls, so later calls overwrite earlier results.</p>\n<p>2. Return a copy: <code>append([]int(nil), buf...)</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Reader() func(vals ...int) []int {\n\tbuf := make([]int, 0, 16)\n\treturn func(vals ...int) []int {\n\t\tbuf = buf[:0]\n\t\tbuf = append(buf, vals...)\n\t\treturn append([]int(nil), buf...)\n\t}\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>The reused buffer means <code>b</code>'s call mutates the array <code>a</code> still references. Returning a fresh copy each call keeps results stable.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>buf[:0]</code> reuses the same array; returning it shares mutable memory.</li>\n<li>Copy with <code>append([]T(nil), buf...)</code> when the caller retains the slice.</li>\n</ul>",
    "starter": "// Package reusedbuffer builds a reader that fills a REUSED internal buffer each\n// call and returns it. A planted bug returns the buffer directly; because the\n// next call resets it with buf[:0] and re-appends into the SAME backing array,\n// a previously returned result is overwritten.\npackage reusedbuffer\n\n// Reader returns a function that, given values, returns them as a slice. Results\n// from earlier calls must remain valid after later calls.\nfunc Reader() func(vals ...int) []int {\n\tbuf := make([]int, 0, 16)\n\treturn func(vals ...int) []int {\n\t\tbuf = buf[:0]\n\t\tbuf = append(buf, vals...)\n\t\t// CHANGE CODE BELOW THIS LINE\n\t\treturn buf\n\t\t// CHANGE CODE ABOVE THIS LINE\n\t}\n}\n",
    "debug": "",
    "file": "reusedbuffer.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/staff/rotate3": {
    "id": "01-language-basics/04-functions/staff/rotate3",
    "title": "Simultaneous Assignment",
    "level": "staff",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Simultaneous Assignment</h1><div class=\"p-meta\"><span class=\"pill lv-staff\">staff</span></div></div><h2>Context</h2>\n<p>Sequential assignments overwrite <code>a</code> before its old value can reach <code>c</code>. Go's</p>\n<p>multiple assignment <code>a, b, c = b, c, a</code> evaluates the whole right-hand side</p>\n<p>first, so all moves happen simultaneously.</p>\n<h2>Task</h2>\n<p>Fix rotate3.go using simultaneous assignment.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  RotateLeft(1, 2, 3)\nOutput: 2, 3, 1\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  RotateLeft(5, 5, 5)\nOutput: 5, 5, 5\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  RotateLeft(0, 1, 2)\nOutput: 1, 2, 0\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Simultaneous assignment</span><span class=\"tchip\">Clobbering</span><span class=\"tchip\">No temp needed</span></div></details>",
    "education": "<h1>Multiple assignment evaluation order</h1>\n<h2>Intuition</h2>\n<p>Go evaluates all right-hand-side operands before assigning any left-hand target, enabling rotations and swaps without temporaries.</p>\n<h2>Approach</h2>\n<p>1. Sequential assignment clobbers values before they are used.</p>\n<p>2. Use parallel assignment <code>a, b, c = b, c, a</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc RotateLeft(a, b, c int) (int, int, int) {\n\ta, b, c = b, c, a\n\treturn a, b, c\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>The bug's <code>a = b; b = c; c = a</code> sets <code>c</code> from the already-overwritten <code>a</code>. Parallel assignment evaluates the whole right side first, rotating correctly.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>a, b, c = b, c, a</code> moves all three at once.</li>\n<li>Sequential <code>a = b; ...; c = a</code> uses the already-changed <code>a</code>.</li>\n</ul>",
    "starter": "// Package rotate3 rotates three variables left: (a,b,c) -> (b,c,a). A planted\n// bug uses sequential assignments that clobber values before they are moved.\npackage rotate3\n\n// RotateLeft returns a,b,c rotated left by one position.\nfunc RotateLeft(a, b, c int) (int, int, int) {\n\t// CHANGE CODE BELOW THIS LINE\n\ta = b\n\tb = c\n\tc = a\n\t// CHANGE CODE ABOVE THIS LINE\n\treturn a, b, c\n}\n",
    "debug": "",
    "file": "rotate3.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/staff/runeindex": {
    "id": "01-language-basics/04-functions/staff/runeindex",
    "title": "Index Into Runes, Not Bytes",
    "level": "staff",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Index Into Runes, Not Bytes</h1><div class=\"p-meta\"><span class=\"pill lv-staff\">staff</span></div></div><h2>Context</h2>\n<p><code>s[i]</code> indexes BYTES; for multibyte UTF-8 the i-th byte is not the i-th</p>\n<p>character. Convert to <code>[]rune</code> (or decode) to index by character position.</p>\n<h2>Task</h2>\n<p>Fix runeindex.go so it returns the i-th rune.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  CharAt(\"h\u00e9llo\", 2)\nOutput: 'l'\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  CharAt(\"\u65e5\u672c\u8a9e\", 1)\nOutput: '\u672c'\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  CharAt(\"abc\", 0)\nOutput: 'a'\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Byte vs rune indexing</span><span class=\"tchip\">[]rune conversion</span><span class=\"tchip\">UTF-8 width</span></div></details>",
    "education": "<h1>Indexing strings by rune</h1>\n<h2>Intuition</h2>\n<p>String indexing is byte-based; to address the i-th character convert to <code>[]rune</code> (O(n)) or decode sequentially.</p>\n<h2>Approach</h2>\n<p>1. <code>s[i]</code> indexes <strong>bytes</strong>, breaking on multi-byte runes.</p>\n<p>2. Convert to <code>[]rune</code> and index that.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc CharAt(s string, i int) rune {\n\treturn []rune(s)[i]\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>\"h\u00e9llo\"[2]</code> returns a byte in the middle of the multi-byte string, not the 3rd character. <code>[]rune(s)[2]</code> decodes and returns <code>'l'</code>.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>s[i]</code> yields a byte; <code>[]rune(s)[i]</code> yields the i-th character.</li>\n<li><code>[]rune(s)</code> costs a pass and allocation; fine for random access.</li>\n</ul>",
    "starter": "// Package runeindex returns the i-th CHARACTER (rune) of a string. A planted bug\n// indexes the string directly, which yields the i-th BYTE \u2014 wrong for multibyte\n// UTF-8 text.\npackage runeindex\n\n// CharAt returns the rune at character position i (0-based). Precondition: i is\n// within the rune count.\nfunc CharAt(s string, i int) rune {\n\t// CHANGE CODE BELOW THIS LINE\n\treturn rune(s[i])\n\t// CHANGE CODE ABOVE THIS LINE\n}\n",
    "debug": "",
    "file": "runeindex.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/staff/sharedcell": {
    "id": "01-language-basics/04-functions/staff/sharedcell",
    "title": "Closures Share One Cell",
    "level": "staff",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Closures Share One Cell</h1><div class=\"p-meta\"><span class=\"pill lv-staff\">staff</span></div></div><h2>Context</h2>\n<p>All closures created in the loop capture the SAME outer <code>c</code>, so they share and</p>\n<p>fight over one counter. Each closure needs its own captured variable, declared</p>\n<p>per iteration.</p>\n<h2>Task</h2>\n<p>Fix sharedcell.go so each counter is independent.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  cs := Counters(2); cs[0](); cs[0]()\nOutput: 1, 2\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  cs[1]()\nOutput: 1 (independent)\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  each closure has its own counter\nOutput: true\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Captured variable identity</span><span class=\"tchip\">Per-instance state</span><span class=\"tchip\">Closure environment</span></div></details>",
    "education": "<h1>Shared vs per-closure captured cells</h1>\n<h2>Intuition</h2>\n<p>Closures capture variables by identity; a variable declared outside the loop is one cell shared by all, while one declared inside gives each closure its own.</p>\n<h2>Approach</h2>\n<p>1. A single <code>c</code> declared outside the loop is shared by all closures.</p>\n<p>2. Declare <code>c := 0</code> <strong>inside</strong> the loop so each closure captures its own cell.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Counters(n int) []func() int {\n\tout := make([]func() int, 0, n)\n\tfor i := 0; i &lt; n; i++ {\n\t\tc := 0\n\t\tout = append(out, func() int { c++; return c })\n\t}\n\treturn out\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>With the shared <code>c</code>, incrementing one counter moves them all. A per-iteration <code>c</code> gives each closure independent state.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Hoisting <code>c</code> out of the loop makes every closure share it.</li>\n<li>Declare per-iteration state inside the loop body.</li>\n</ul>",
    "starter": "// Package sharedcell returns independent counters. A planted bug captures a\n// single shared variable across all counters, so they interfere.\npackage sharedcell\n\n// Counters returns n independent counter closures; each returns 1,2,3,... on its\n// own successive calls, independently of the others.\nfunc Counters(n int) []func() int {\n\tout := make([]func() int, 0, n)\n\t// CHANGE CODE BELOW THIS LINE\n\tc := 0\n\tfor i := 0; i < n; i++ {\n\t\tout = append(out, func() int { c++; return c })\n\t}\n\t// CHANGE CODE ABOVE THIS LINE\n\treturn out\n}\n",
    "debug": "",
    "file": "sharedcell.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/staff/shortcircuit": {
    "id": "01-language-basics/04-functions/staff/shortcircuit",
    "title": "Short-Circuit Nil Guard",
    "level": "staff",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Short-Circuit Nil Guard</h1><div class=\"p-meta\"><span class=\"pill lv-staff\">staff</span></div></div><h2>Context</h2>\n<p><code>&amp;&amp;</code> evaluates left to right and short-circuits, so the nil check must come</p>\n<p>FIRST: <code>p != nil &amp;&amp; <em>p &gt; 0</code>. With the operands swapped, <code></em>p</code> runs on a nil</p>\n<p>pointer before the guard, panicking.</p>\n<h2>Task</h2>\n<p>Fix the guard order in shortcircuit.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  ValueOr(nil, 5)\nOutput: 5\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  n := 7; ValueOr(&amp;n, 5)\nOutput: 7\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  n := -1; ValueOr(&amp;n, 5)\nOutput: 5\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Short-circuit evaluation</span><span class=\"tchip\">Guard ordering</span><span class=\"tchip\">Nil safety</span></div></details>",
    "education": "<h1>Short-circuit evaluation as a guard</h1>\n<h2>Intuition</h2>\n<p><code>&amp;&amp;</code> and <code>||</code> evaluate left to right and stop early; the nil/bounds guard must precede the operation it protects.</p>\n<h2>Approach</h2>\n<p>1. <code>&amp;&amp;</code> short-circuits left to right; the nil check must come <strong>first</strong>.</p>\n<p>2. The bug writes <code>*p &gt; 0 &amp;&amp; p != nil</code>, dereferencing before checking nil.</p>\n<p>3. Reorder to <code>p != nil &amp;&amp; *p &gt; 0</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc ValueOr(p *int, def int) int {\n\tif p != nil &amp;&amp; *p &gt; 0 {\n\t\treturn *p\n\t}\n\treturn def\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>ValueOr(nil, 5)</code> dereferences nil in the bug's first operand and panics. Checking <code>p != nil</code> first stops evaluation before the deref.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>p != nil &amp;&amp; *p &gt; 0</code> is safe; the reverse panics on nil.</li>\n<li>Rely on left-to-right short-circuit ordering deliberately.</li>\n</ul>",
    "starter": "// Package shortcircuit safely reads a value through a possibly-nil pointer. A\n// planted bug orders the && operands so the pointer is dereferenced before the\n// nil check, panicking on nil.\npackage shortcircuit\n\n// ValueOr returns *p if p is non-nil and *p is positive, else def.\nfunc ValueOr(p *int, def int) int {\n\t// CHANGE CODE BELOW THIS LINE\n\tif *p > 0 && p != nil {\n\t\t// CHANGE CODE ABOVE THIS LINE\n\t\treturn *p\n\t}\n\treturn def\n}\n",
    "debug": "",
    "file": "shortcircuit.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/staff/stackpush": {
    "id": "01-language-basics/04-functions/staff/stackpush",
    "title": "LIFO via Deferred Push",
    "level": "staff",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">LIFO via Deferred Push</h1><div class=\"p-meta\"><span class=\"pill lv-staff\">staff</span></div></div><h2>Context</h2>\n<p>The deferred closure always appends <code>xs[len(xs)-1]</code> \u2014 the same last element \u2014</p>\n<p>regardless of <code>i</code>. To reverse, each deferred push must record its OWN element,</p>\n<p>captured per iteration (as an argument or via the per-iteration <code>i</code>).</p>\n<h2>Task</h2>\n<p>Fix stackpush.go so it appends each element once, in reverse.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  ReverseInts([1 2 3])\nOutput: [3 2 1]\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  ReverseInts([1])\nOutput: [1]\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  ReverseInts(nil)\nOutput: []\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Per-iteration value</span><span class=\"tchip\">LIFO ordering</span><span class=\"tchip\">Index vs constant</span></div></details>",
    "education": "<h1>Recording per-iteration data in defers</h1>\n<h2>Intuition</h2>\n<p>A deferred closure that reads a fixed expression captures none of the loop's progression; snapshot <code>xs[i]</code> per iteration to record distinct values.</p>\n<h2>Approach</h2>\n<p>1. Deferred calls run LIFO, reversing the push order.</p>\n<p>2. The bug reads <code>xs[len-1]</code> at defer-run time (all the same); snapshot the value with a deferred argument <code>(xs[i])</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc ReverseInts(xs []int) (out []int) {\n\tfor i := 0; i &lt; len(xs); i++ {\n\t\tdefer func(v int) { out = append(out, v) }(xs[i])\n\t}\n\treturn\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>Closing over <code>xs[len(xs)-1]</code> makes every deferred call append the last element. Passing <code>xs[i]</code> as a deferred argument snapshots each element, so LIFO yields the reversal.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>xs[len(xs)-1]</code> is the same element every iteration.</li>\n<li>Snapshot <code>xs[i]</code> as an argument for a version-proof reverse.</li>\n</ul>",
    "starter": "// Package stackpush reverses via deferred pushes but a planted bug pushes with a\n// body-captured slice index expression that reads the final loop state.\npackage stackpush\n\n// ReverseInts returns xs reversed, built by deferring a push of each element.\nfunc ReverseInts(xs []int) (out []int) {\n\tfor i := 0; i < len(xs); i++ {\n\t\t// CHANGE CODE BELOW THIS LINE\n\t\tdefer func() { out = append(out, xs[len(xs)-1]) }()\n\t\t// CHANGE CODE ABOVE THIS LINE\n\t}\n\treturn\n}\n",
    "debug": "",
    "file": "stackpush.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/staff/switchinit": {
    "id": "01-language-basics/04-functions/staff/switchinit",
    "title": "Switch Init Statement",
    "level": "staff",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Switch Init Statement</h1><div class=\"p-meta\"><span class=\"pill lv-staff\">staff</span></div></div><h2>Context</h2>\n<p>Re-calling <code>compute(x)</code> in every case runs the expensive work multiple times. A</p>\n<p>switch INIT statement (<code>switch v := compute(x); { ... }</code>) evaluates it once and</p>\n<p>reuses <code>v</code> across the cases.</p>\n<h2>Task</h2>\n<p>Fix switchinit.go so compute runs exactly once.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Classify(5)\nOutput: pos, 1 call\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Classify(0)\nOutput: zero, 1 call\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Classify(-3)\nOutput: neg, 1 call\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Switch init statement</span><span class=\"tchip\">Compute once</span><span class=\"tchip\">Side-effect count</span></div></details>",
    "education": "<h1>The switch init statement</h1>\n<h2>Intuition</h2>\n<p><code>switch init; tag {}</code> (or <code>switch init; {}</code>) runs the init once and scopes its variables to the switch, avoiding repeated evaluation across cases.</p>\n<h2>Approach</h2>\n<p>1. Calling <code>compute(x)</code> in each case re-runs the expensive work.</p>\n<p>2. Use a switch init statement: <code>switch v := compute(x); {</code> and compare <code>v</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nvar calls int\n\nfunc compute(x int) int { calls++; return x }\n\nfunc Classify(x int) (label string, runs int) {\n\tcalls = 0\n\tswitch v := compute(x); {\n\tcase v &lt; 0:\n\t\tlabel = \"neg\"\n\tcase v == 0:\n\t\tlabel = \"zero\"\n\tdefault:\n\t\tlabel = \"pos\"\n\t}\n\treturn label, calls\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>The bug evaluates <code>compute(x)</code> once per case. The init form computes it once, binds <code>v</code>, and each case tests the cached value.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Cases in a tagless switch each re-evaluate their own expression.</li>\n<li>Hoist shared work into the switch init statement.</li>\n</ul>",
    "starter": "// Package switchinit classifies the sign of an expensive computation, counting\n// how many times the computation runs. A planted bug recomputes it in each case\n// instead of using a switch init statement, so the count is wrong (and the logic\n// duplicated). Use `switch v := f(); { ... }` to compute once.\npackage switchinit\n\n// classify returns the sign label of compute(x) and how many times compute ran.\nvar calls int\n\nfunc compute(x int) int { calls++; return x }\n\n// Classify returns the sign of compute(x) computing it EXACTLY once.\nfunc Classify(x int) (label string, runs int) {\n\tcalls = 0\n\t// CHANGE CODE BELOW THIS LINE\n\tswitch {\n\tcase compute(x) < 0:\n\t\tlabel = \"neg\"\n\tcase compute(x) == 0:\n\t\tlabel = \"zero\"\n\tdefault:\n\t\tlabel = \"pos\"\n\t}\n\t// CHANGE CODE ABOVE THIS LINE\n\treturn label, calls\n}\n",
    "debug": "",
    "file": "switchinit.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/staff/tracescope": {
    "id": "01-language-basics/04-functions/staff/tracescope",
    "title": "Defer Scope in a Loop",
    "level": "staff",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Defer Scope in a Loop</h1><div class=\"p-meta\"><span class=\"pill lv-staff\">staff</span></div></div><h2>Context</h2>\n<p>A <code>defer</code> runs when its ENCLOSING FUNCTION returns, not at the end of a loop</p>\n<p>iteration. Deferring inside the loop body schedules every \"end\" for function</p>\n<p>exit, producing [start0 start1 end1 end0]. Scope the defer to the iteration by</p>\n<p>wrapping the body in a function literal.</p>\n<h2>Task</h2>\n<p>Fix tracescope.go so each item's end is logged before the next start.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Trace(2)\nOutput: [start0 end0 start1 end1]\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  each iteration's defer fires at iteration end\nOutput: true\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Trace(1)\nOutput: [start0 end0]\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Defer is function-scoped</span><span class=\"tchip\">Per-iteration wrapper</span><span class=\"tchip\">LIFO at exit</span></div></details>",
    "education": "<h1>Scoping defer to a loop iteration</h1>\n<h2>Intuition</h2>\n<p>Deferred calls run at function return, so a <code>defer</code> in a loop body accumulates to the end; wrap each iteration in its own function literal to make the defer fire per iteration.</p>\n<h2>Approach</h2>\n<p>1. <code>defer</code> fires at <strong>function</strong> exit, not loop-iteration exit.</p>\n<p>2. Wrap each iteration's body in an immediately-invoked func so its defer runs per iteration.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nimport \"fmt\"\n\nfunc Trace(n int) (log []string) {\n\tfor i := 0; i &lt; n; i++ {\n\t\tfunc(k int) {\n\t\t\tlog = append(log, fmt.Sprintf(\"start%d\", k))\n\t\t\tdefer func() { log = append(log, fmt.Sprintf(\"end%d\", k)) }()\n\t\t}(i)\n\t}\n\treturn\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>The plain <code>defer</code> inside the loop queues all <code>end</code> logs to the function's end, giving <code>[start0 start1 end1 end0]</code>. An IIFE per iteration scopes the defer so <code>end</code> follows each <code>start</code>.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>A loop-body <code>defer</code> runs at function exit, not each iteration's end.</li>\n<li><code>func(){ ...; defer end() }()</code> per iteration gives interleaved order.</li>\n</ul>",
    "starter": "// Package tracescope logs a start and end marker per item; each item's \"end\"\n// must be logged before the next item's \"start\". A planted bug uses a\n// FUNCTION-level defer inside the loop, so all \"end\" markers run at function\n// return (in LIFO order) instead of at the end of each iteration.\npackage tracescope\n\nimport \"fmt\"\n\n// Trace returns the interleaved start/end log. For n=2 it must be\n// [start0 end0 start1 end1].\nfunc Trace(n int) (log []string) {\n\tfor i := 0; i < n; i++ {\n\t\t// CHANGE CODE BELOW THIS LINE\n\t\tlog = append(log, fmt.Sprintf(\"start%d\", i))\n\t\tdefer func(k int) { log = append(log, fmt.Sprintf(\"end%d\", k)) }(i)\n\t\t// CHANGE CODE ABOVE THIS LINE\n\t}\n\treturn\n}\n",
    "debug": "",
    "file": "tracescope.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/staff/twoappend": {
    "id": "01-language-basics/04-functions/staff/twoappend",
    "title": "Two Appends Share Spare Cap",
    "level": "staff",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Two Appends Share Spare Cap</h1><div class=\"p-meta\"><span class=\"pill lv-staff\">staff</span></div></div><h2>Context</h2>\n<p>Because <code>base</code> has spare capacity, both appends write into the same backing</p>\n<p>array at the same slot; the second clobbers the first. Clip the base so each</p>\n<p>append allocates its own array.</p>\n<h2>Task</h2>\n<p>Fix twoappend.go so the first variant keeps its value.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Fork([7](cap 4), 100, 200)\nOutput: x == [7 100], last == 100\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  x unaffected by second append\nOutput: true\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  x[1]\nOutput: 100 (not 200)\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Spare-capacity aliasing</span><span class=\"tchip\">Full-slice expression</span><span class=\"tchip\">Independent buffers</span></div></details>",
    "education": "<h1>Append aliasing through shared capacity</h1>\n<h2>Intuition</h2>\n<p>Two appends to a base with spare capacity write the same memory; a three-index slice (<code>s[:len:len]</code>) caps it so the next append copies.</p>\n<h2>Approach</h2>\n<p>1. Two appends sharing spare capacity write to the same array.</p>\n<p>2. Clip the base: <code>base[:len(base):len(base)]</code> so each append reallocates.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Fork(base []int, a, b int) ([]int, int) {\n\tbc := base[:len(base):len(base)]\n\tx := append(bc, a)\n\ty := append(bc, b)\n\t_ = y\n\treturn x, x[len(x)-1]\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>Without clipping, appending <code>b</code> overwrites the slot <code>x</code> holds, so <code>x[1]</code> becomes 200. The full-slice clip isolates the two forks.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Appending to a base with spare cap twice makes the results alias.</li>\n<li><code>base[:len:len]</code> guarantees the next append reallocates.</li>\n</ul>",
    "starter": "// Package twoappend forks a buffer into two variants. A planted bug appends to\n// the same base with spare capacity twice, so the second append overwrites the\n// first variant's last element.\npackage twoappend\n\n// Fork returns base+a and base+b as two independent slices.\nfunc Fork(base []int, a, b int) ([]int, int) {\n\t// CHANGE CODE BELOW THIS LINE\n\tx := append(base, a)\n\ty := append(base, b)\n\t// CHANGE CODE ABOVE THIS LINE\n\t_ = y\n\treturn x, x[len(x)-1]\n}\n",
    "debug": "",
    "file": "twoappend.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/04-functions/staff/wraperr": {
    "id": "01-language-basics/04-functions/staff/wraperr",
    "title": "Defer Wraps the Snapshot",
    "level": "staff",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Defer Wraps the Snapshot</h1><div class=\"p-meta\"><span class=\"pill lv-staff\">staff</span></div></div><h2>Context</h2>\n<p><code>defer f(err)</code> evaluates <code>err</code> at the defer statement \u2014 before the body assigns</p>\n<p>it \u2014 so the closure receives nil. To wrap the FINAL error, read the named return</p>\n<p><code>err</code> inside the closure body instead of taking it as an argument.</p>\n<h2>Task</h2>\n<p>Fix wraperr.go so the returned error is wrapped.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Do(true)\nOutput: error \"do: boom\"\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Do(false)\nOutput: nil\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  wrapped error unwraps to boom\nOutput: true\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Defer argument snapshot</span><span class=\"tchip\">Named-return wrapping</span><span class=\"tchip\">%w wrapping</span></div></details>",
    "education": "<h1>The defer-wrap-error idiom</h1>\n<h2>Intuition</h2>\n<p>The idiomatic error decorator captures the NAMED return in the closure body; passing it as a deferred argument freezes the pre-assignment (nil) value.</p>\n<h2>Approach</h2>\n<p>1. A deferred error-wrapper must read the <strong>named return</strong> at return time.</p>\n<p>2. The bug snapshots <code>err</code> as a deferred argument (nil when defer runs).</p>\n<p>3. Close over <code>err</code> with no parameter.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nimport (\n\t\"errors\"\n\t\"fmt\"\n)\n\nfunc Do(flag bool) (err error) {\n\tdefer func() {\n\t\tif err != nil {\n\t\t\terr = fmt.Errorf(\"do: %w\", err)\n\t\t}\n\t}()\n\tif flag {\n\t\terr = errors.New(\"boom\")\n\t}\n\treturn\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>defer func(e error){...}(err)</code> captures <code>err</code> while it is still nil, so nothing wraps. Reading the named <code>err</code> inside a parameterless defer sees the final error and wraps it.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>defer func(e error){...}(err)</code> snapshots err (nil) at defer-time.</li>\n<li>Read the named <code>err</code> in the body so you see the final value.</li>\n</ul>",
    "starter": "// Package wraperr adds context to a returned error using a deferred closure. A\n// planted bug passes err as a defer ARGUMENT, snapshotting its nil value before\n// the body sets it, so the wrap never happens.\npackage wraperr\n\nimport (\n\t\"errors\"\n\t\"fmt\"\n)\n\n// Do returns an error wrapped with context. It fails when flag is true.\nfunc Do(flag bool) (err error) {\n\t// CHANGE CODE BELOW THIS LINE\n\tdefer func(e error) {\n\t\tif e != nil {\n\t\t\terr = fmt.Errorf(\"do: %w\", e)\n\t\t}\n\t}(err)\n\t// CHANGE CODE ABOVE THIS LINE\n\tif flag {\n\t\terr = errors.New(\"boom\")\n\t}\n\treturn\n}\n",
    "debug": "",
    "file": "wraperr.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/junior/addvia": {
    "id": "01-language-basics/05-pointers/junior/addvia",
    "title": "Add Delta Through Pointer",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Add Delta Through Pointer</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>Accumulating into a caller's variable through a pointer is the building block of</p>\n<p>running totals and counters held elsewhere.</p>\n<h2>Task</h2>\n<p>Implement <code>Add</code> in addvia.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  x := 10; Add(&amp;x, 5)\nOutput: x == 15\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  x := 10; Add(&amp;x, -3)\nOutput: x == 7\n</code></pre>\n<p>_Explanation:_ Delta can be negative.</p>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  x := 0; Add(&amp;x, 0)\nOutput: x == 0\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Read-modify-write</span><span class=\"tchip\">Pointer aliasing</span><span class=\"tchip\">Signed delta</span></div></details>",
    "education": "<h1>Read-modify-write through a pointer</h1>\n<h2>Intuition</h2>\n<p><code>*p += d</code> loads, adds, and stores at the pointee in one expression.</p>\n<h2>Approach</h2>\n<p>1. <code>p</code> aliases the caller's int.</p>\n<p>2. <code>*p += delta</code> reads, adds, and stores back at the same address.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Add(p *int, delta int) {\n\t*p += delta\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>x := 10</code>, <code>Add(&amp;x, 5)</code>: <code>*p += 5</code> computes <code>10 + 5</code> and writes <code>15</code> back to <code>x</code>.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>*p += delta</code> mutates the caller's variable.</li>\n<li>Works for negative deltas too.</li>\n</ul>",
    "starter": "// Package addvia \u2014 Gopher Workplace challenge.\npackage addvia\n\n// Add adds delta to the int that p points to.\nfunc Add(p *int, delta int) {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "addvia.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/junior/applyptr": {
    "id": "01-language-basics/05-pointers/junior/applyptr",
    "title": "Apply Function In Place",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Apply Function In Place</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>Combining a pointer with a function value transforms the caller's variable in</p>\n<p>place with caller-chosen logic.</p>\n<h2>Task</h2>\n<p>Implement <code>Apply</code> in applyptr.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  x := 3; Apply(&amp;x, func(n int) int { return n*n })\nOutput: x == 9\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  x := 5; Apply(&amp;x, func(n int) int { return n+1 })\nOutput: x == 6\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  x := 4; Apply(&amp;x, func(n int) int { return n })\nOutput: x == 4\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Read, transform, write</span><span class=\"tchip\">Function parameter</span><span class=\"tchip\">In-place</span></div></details>",
    "education": "<h1>In-place transform via a callback</h1>\n<h2>Intuition</h2>\n<p>Reading <code>*p</code>, applying <code>f</code>, and storing back mutates the caller's variable with pluggable logic.</p>\n<h2>Approach</h2>\n<p>1. Read <code>*p</code>.</p>\n<p>2. Transform it with <code>f</code>.</p>\n<p>3. Store the result back: <code><em>p = f(</em>p)</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Apply(p *int, f func(int) int) {\n\t*p = f(*p)\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>Apply(&amp;x, square)</code> with <code>x = 3</code>: <code>f(3)</code> is <code>9</code>, written back into <code>x</code>.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>The pointee is both input and output.</li>\n<li><code>f</code> must be non-nil; calling a nil func panics.</li>\n</ul>",
    "starter": "// Package applyptr \u2014 Gopher Workplace challenge.\npackage applyptr\n\n// Apply replaces *p with f(*p).\nfunc Apply(p *int, f func(int) int) {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "applyptr.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/junior/bumpfield": {
    "id": "01-language-basics/05-pointers/junior/bumpfield",
    "title": "Mutate Struct Field via Pointer",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Mutate Struct Field via Pointer</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>Through a struct pointer you can update a field in place. Go lets you write</p>\n<p><code>c.Count</code> on a pointer \u2014 it auto-dereferences.</p>\n<h2>Task</h2>\n<p>Implement <code>Grow</code> in bumpfield.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  c := Cart{Count: 2}; Grow(&amp;c)\nOutput: c.Count == 3\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  c := Cart{Count: 0}; Grow(&amp;c)\nOutput: c.Count == 1\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  c := Cart{Count: -1}; Grow(&amp;c)\nOutput: c.Count == 0\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Struct pointer</span><span class=\"tchip\">Auto-deref field access</span><span class=\"tchip\">In-place field update</span></div></details>",
    "education": "<h1>Field access through struct pointers</h1>\n<h2>Intuition</h2>\n<p>Go implicitly dereferences a struct pointer for field selection, so <code>c.Count</code> reads/writes the pointee's field.</p>\n<h2>Approach</h2>\n<p>1. <code>c</code> is a <code>*Cart</code>.</p>\n<p>2. <code>c.Count++</code> \u2014 Go auto-dereferences the pointer for field access, so this mutates the caller's struct.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\ntype Cart struct{ Count int }\n\nfunc Grow(c *Cart) {\n\tc.Count++\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>Grow(&amp;c)</code> receives the address; <code>c.Count++</code> is shorthand for <code>(*c).Count++</code>, incrementing the caller's field.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>No need to write <code>(*c).Count</code>; <code>c.Count</code> is equivalent.</li>\n<li>A value receiver would edit a copy, not the caller's struct.</li>\n</ul>",
    "starter": "// Package bumpfield \u2014 Gopher Workplace challenge.\npackage bumpfield\n\ntype Cart struct{ Count int }\n\n// Grow increments the Count field of the Cart that c points to.\nfunc Grow(c *Cart) {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "bumpfield.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/junior/clampptr": {
    "id": "01-language-basics/05-pointers/junior/clampptr",
    "title": "Clamp In Place",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Clamp In Place</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>Clamping in place reads and conditionally rewrites the caller's variable through</p>\n<p>a pointer.</p>\n<h2>Task</h2>\n<p>Implement <code>Clamp</code> in clampptr.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  x := 99; Clamp(&amp;x, 0, 10)\nOutput: x == 10\n</code></pre>\n<p>_Explanation:_ Above the range \u2192 pulled down to <code>hi</code>.</p>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  x := -5; Clamp(&amp;x, 0, 10)\nOutput: x == 0\n</code></pre>\n<p>_Explanation:_ Below the range \u2192 raised to <code>lo</code>.</p>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  x := 4; Clamp(&amp;x, 0, 10)\nOutput: x == 4\n</code></pre>\n<p>_Explanation:_ Inside the range \u2192 unchanged.</p>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">In-place clamp</span><span class=\"tchip\">Inclusive bounds</span><span class=\"tchip\">Read then write</span></div></details>",
    "education": "<h1>Conditional in-place mutation</h1>\n<h2>Intuition</h2>\n<p>Reading <code>*p</code>, testing bounds, and writing back constrains the caller's value without a return.</p>\n<h2>Approach</h2>\n<p>1. If <code><em>p &lt; lo</code>, set <code></em>p = lo</code>.</p>\n<p>2. Else if <code><em>p &gt; hi</code>, set <code></em>p = hi</code>.</p>\n<p>3. Otherwise leave it alone.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Clamp(p *int, lo, hi int) {\n\tif *p &lt; lo {\n\t\t*p = lo\n\t} else if *p &gt; hi {\n\t\t*p = hi\n\t}\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>Clamp(&amp;x, 0, 10)</code> with <code>x = 99</code>: <code>99 &gt; 10</code>, so <code>*p = 10</code>.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Only assign when out of range (optional micro-optimisation).</li>\n<li>Endpoints are inclusive.</li>\n</ul>",
    "starter": "// Package clampptr \u2014 Gopher Workplace challenge.\npackage clampptr\n\n// Clamp constrains *p to the inclusive range [lo, hi], writing back in place.\nfunc Clamp(p *int, lo, hi int) {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "clampptr.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/junior/clearref": {
    "id": "01-language-basics/05-pointers/junior/clearref",
    "title": "Clear a Pointer Field",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Clear a Pointer Field</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>Setting a pointer field to nil drops the reference to whatever it pointed at,</p>\n<p>letting the garbage collector reclaim it if nothing else refers to it.</p>\n<h2>Task</h2>\n<p>Implement <code>Detach</code> in clearref.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  n := &amp;Node{Next: other}; Detach(n)\nOutput: n.Next == nil\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  n := &amp;Node{}; Detach(n)\nOutput: n.Next == nil\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Detach(&amp;Node{Value: 5, Next: x})\nOutput: Next == nil, Value == 5\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Nil a pointer field</span><span class=\"tchip\">Unlinking</span><span class=\"tchip\">GC hint</span></div></details>",
    "education": "<h1>Dropping references with nil</h1>\n<h2>Intuition</h2>\n<p>Assigning nil to a pointer field removes the link; if no other reference remains, the target becomes eligible for garbage collection.</p>\n<h2>Approach</h2>\n<p>1. <code>n</code> is a <code>*Node</code>.</p>\n<p>2. <code>n.Next = nil</code> drops the link; <code>Value</code> is left intact.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\ntype Node struct {\n\tVal  int\n\tNext *Node\n}\n\nfunc Detach(n *Node) {\n\tn.Next = nil\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>Detach(n)</code> writes <code>nil</code> into <code>n.Next</code>, so the node no longer references its successor.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Only the Next field changes; Val stays.</li>\n<li>Nil-ing references is how you let the GC reclaim memory.</li>\n</ul>",
    "starter": "// Package clearref \u2014 Gopher Workplace challenge.\npackage clearref\n\ntype Node struct {\n\tVal  int\n\tNext *Node\n}\n\n// Detach sets the node's Next to nil, unlinking the rest of the list.\nfunc Detach(n *Node) {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "clearref.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/junior/copyval": {
    "id": "01-language-basics/05-pointers/junior/copyval",
    "title": "Copy Value Between Pointers",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Copy Value Between Pointers</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>Assigning <code><em>dst = </em>src</code> copies the value; the two variables stay independent</p>\n<p>afterward.</p>\n<h2>Task</h2>\n<p>Implement <code>CopyInto</code> in copyval.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  a, b := 1, 9; CopyInto(&amp;a, &amp;b)\nOutput: a == 9, b == 9\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  a, b := 5, 5; CopyInto(&amp;a, &amp;b)\nOutput: a == 5, b == 5\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  a, b := 0, -2; CopyInto(&amp;a, &amp;b)\nOutput: a == -2, b == -2\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Deref both sides</span><span class=\"tchip\">Value copy</span><span class=\"tchip\">Direction</span></div></details>",
    "education": "<h1>Copying values via pointers</h1>\n<h2>Intuition</h2>\n<p><code><em>dst = </em>src</code> reads the source pointee and writes it to the destination pointee \u2014 a value copy, not an alias.</p>\n<h2>Approach</h2>\n<p>1. Read <code>*src</code>.</p>\n<p>2. Write it into <code>*dst</code>.</p>\n<p>3. <code>src</code> is untouched.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc CopyInto(dst, src *int) {\n\t*dst = *src\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>CopyInto(&amp;a, &amp;b)</code> with <code>a = 1</code>, <code>b = 9</code>: <code><em>dst = </em>src</code> copies <code>9</code> into <code>a</code>; <code>b</code> stays <code>9</code>.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>After the copy the two remain separate ints.</li>\n<li>Order matters: dst is the target.</li>\n</ul>",
    "starter": "// Package copyval \u2014 Gopher Workplace challenge.\npackage copyval\n\n// CopyInto sets *dst to the current value of *src.\nfunc CopyInto(dst, src *int) {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "copyval.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/junior/countnil": {
    "id": "01-language-basics/05-pointers/junior/countnil",
    "title": "Count Nil Pointers",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Count Nil Pointers</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>A pointer can be compared to <code>nil</code> to test whether it references anything.</p>\n<h2>Task</h2>\n<p>Implement <code>CountNil</code> in countnil.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  a := 1; CountNil([]*int{&amp;a, nil, nil})\nOutput: 2\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  CountNil([]*int{&amp;a, &amp;a})\nOutput: 0\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  CountNil(nil)\nOutput: 0\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">nil comparison</span><span class=\"tchip\">Iterate pointers</span><span class=\"tchip\">Counter</span></div></details>",
    "education": "<h1>Testing pointers for nil</h1>\n<h2>Intuition</h2>\n<p>The zero value of any pointer is nil; comparing with <code>== nil</code> detects unset references before use.</p>\n<h2>Approach</h2>\n<p>1. Range over the slice.</p>\n<p>2. Increment a counter when <code>p == nil</code>.</p>\n<p>3. Return the count.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc CountNil(ps []*int) int {\n\tc := 0\n\tfor _, p := range ps {\n\t\tif p == nil {\n\t\t\tc++\n\t\t}\n\t}\n\treturn c\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>[]*int{&amp;a, nil, nil}</code>: the loop sees one real pointer and two nils, counting <code>2</code>.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Only <code>== nil</code> distinguishes an unset pointer.</li>\n<li>Dereferencing the nil ones would panic.</li>\n</ul>",
    "starter": "// Package countnil \u2014 Gopher Workplace challenge.\npackage countnil\n\n// CountNil returns how many entries of ps are nil.\nfunc CountNil(ps []*int) int {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "countnil.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/junior/deposit": {
    "id": "01-language-basics/05-pointers/junior/deposit",
    "title": "Deposit Into Account",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Deposit Into Account</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>Mutating a struct's field through a pointer is exactly what a pointer-receiver</p>\n<p>method does under the hood.</p>\n<h2>Task</h2>\n<p>Implement <code>Deposit</code> in deposit.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  a := Account{Balance: 100}; Deposit(&amp;a, 50)\nOutput: a.Balance == 150\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  a := Account{}; Deposit(&amp;a, 20)\nOutput: a.Balance == 20\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  a := Account{Balance: 5}; Deposit(&amp;a, 0)\nOutput: a.Balance == 5\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Struct pointer</span><span class=\"tchip\">Field mutation</span><span class=\"tchip\">In-place update</span></div></details>",
    "education": "<h1>Pointer receivers by hand</h1>\n<h2>Intuition</h2>\n<p>A function taking <code>*Account</code> and updating a field is the mechanism behind pointer-receiver methods.</p>\n<h2>Approach</h2>\n<p>1. <code>a</code> is an <code>*Account</code>.</p>\n<p>2. <code>a.Balance += amount</code> mutates the caller's field through the pointer.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\ntype Account struct{ Balance int }\n\nfunc Deposit(a *Account, amount int) {\n\ta.Balance += amount\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>Deposit(&amp;a, 50)</code> with <code>Balance = 100</code>: the field becomes <code>150</code> in the caller's struct.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>A value parameter would update a copy, losing the change.</li>\n<li><code>a.Balance += amount</code> auto-dereferences the pointer.</li>\n</ul>",
    "starter": "// Package deposit \u2014 Gopher Workplace challenge.\npackage deposit\n\ntype Account struct{ Balance int }\n\n// Deposit adds amount to the Account's Balance in place.\nfunc Deposit(a *Account, amount int) {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "deposit.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/junior/doubleidx": {
    "id": "01-language-basics/05-pointers/junior/doubleidx",
    "title": "Double an Array Element",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Double an Array Element</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>An array is a value; to mutate the caller's array you pass a pointer to it.</p>\n<p>Indexing auto-dereferences.</p>\n<h2>Task</h2>\n<p>Implement <code>Double</code> in doubleidx.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  a := [4]int{1,2,3,4}; Double(&amp;a, 2)\nOutput: a[2] == 6\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  a := [4]int{1,2,3,4}; Double(&amp;a, 0)\nOutput: a[0] == 2\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  a := [4]int{0,0,0,0}; Double(&amp;a, 3)\nOutput: a[3] == 0\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Pointer to array</span><span class=\"tchip\">Auto-deref indexing</span><span class=\"tchip\">In-place element update</span></div></details>",
    "education": "<h1>Pointers to arrays</h1>\n<h2>Intuition</h2>\n<p>Arrays are values; a <code>*[N]T</code> lets a function mutate the caller's array, and indexing auto-dereferences the pointer.</p>\n<h2>Approach</h2>\n<p>1. <code>arr</code> is a <code>*[4]int</code>.</p>\n<p>2. <code>arr[i] *= 2</code> \u2014 Go auto-dereferences the array pointer for indexing, so the caller's array changes.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Double(arr *[4]int, i int) {\n\tarr[i] *= 2\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>Double(&amp;a, 2)</code> with <code>a[2] = 3</code>: <code>arr[2] *= 2</code> doubles it to <code>6</code> in the caller's array.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Passing <code>[4]int</code> by value copies; the caller wouldn't see changes.</li>\n<li><code>arr[i]</code> on a pointer is <code>(*arr)[i]</code>.</li>\n</ul>",
    "starter": "// Package doubleidx \u2014 Gopher Workplace challenge.\npackage doubleidx\n\n// Double doubles arr[i] in place through the array pointer. i is in range.\nfunc Double(arr *[4]int, i int) {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "doubleidx.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/junior/fillptr": {
    "id": "01-language-basics/05-pointers/junior/fillptr",
    "title": "Fill Two Outputs",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Fill Two Outputs</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>Output parameters via pointers let a function return several results by writing</p>\n<p>through caller-provided addresses.</p>\n<h2>Task</h2>\n<p>Implement <code>MinMax</code> in fillptr.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  MinMax([]int{3, -1, 7}, &amp;lo, &amp;hi)\nOutput: lo == -1, hi == 7\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  MinMax([]int{5}, &amp;lo, &amp;hi)\nOutput: lo == 5, hi == 5\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  MinMax([]int{2, 2, 2}, &amp;lo, &amp;hi)\nOutput: lo == 2, hi == 2\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Output pointers</span><span class=\"tchip\">Single pass</span><span class=\"tchip\">Write through</span></div></details>",
    "education": "<h1>Output parameters via pointers</h1>\n<h2>Intuition</h2>\n<p>Writing results through caller-supplied pointers is an alternative to multiple return values, common when interfacing with fixed signatures.</p>\n<h2>Approach</h2>\n<p>1. Seed both through the pointers: <code><em>min, </em>max = xs[0], xs[0]</code>.</p>\n<p>2. Scan the rest, updating <code><em>min</code>/<code></em>max</code> when a smaller/larger value appears.</p>\n<p>3. Results land in the caller's variables.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc MinMax(xs []int, min, max *int) {\n\t*min, *max = xs[0], xs[0]\n\tfor _, v := range xs {\n\t\tif v &lt; *min {\n\t\t\t*min = v\n\t\t}\n\t\tif v &gt; *max {\n\t\t\t*max = v\n\t\t}\n\t}\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>For <code>[3, -1, 7]</code>: seed <code>lo = hi = 3</code>; <code>-1</code> lowers <code>lo</code> to <code>-1</code>; <code>7</code> raises <code>hi</code> to <code>7</code>.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Dereference to write the results: <code>*min = v</code>.</li>\n<li>Go usually prefers multiple returns, but pointer outputs appear in stdlib.</li>\n</ul>",
    "starter": "// Package fillptr \u2014 Gopher Workplace challenge.\npackage fillptr\n\n// MinMax writes the smallest value to *min and the largest to *max. xs is\n// non-empty.\nfunc MinMax(xs []int, min, max *int) {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "fillptr.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/junior/incptr": {
    "id": "01-language-basics/05-pointers/junior/incptr",
    "title": "Increment Through Pointer",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Increment Through Pointer</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>A pointer parameter <code>*int</code> refers to the caller's variable. Writing through it</p>\n<p>with <code>*p</code> changes the original.</p>\n<h2>Task</h2>\n<p>Implement <code>Inc</code> in incptr.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  x := 41; Inc(&amp;x)\nOutput: x == 42\n</code></pre>\n<p>_Explanation:_ <code>Inc</code> follows the pointer to <code>x</code> and adds 1 in place.</p>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  x := 0; Inc(&amp;x)\nOutput: x == 1\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  x := -1; Inc(&amp;x)\nOutput: x == 0\n</code></pre>\n<p>_Explanation:_ Works for negatives too \u2014 dereference, add one, store back.</p>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Pointer parameter</span><span class=\"tchip\">Dereference to write</span><span class=\"tchip\">Address-of</span></div></details>",
    "education": "<h1>Pointers as references</h1>\n<h2>Intuition</h2>\n<p>A pointer holds the address of a value; dereferencing (<code>*p</code>) reaches the value so a function can mutate the caller's variable.</p>\n<h2>Approach</h2>\n<p>1. <code>p</code> has type <code>*int</code>: it points at the caller's variable.</p>\n<p>2. Dereference with <code>*p</code> to reach the underlying int.</p>\n<p>3. Add one <strong>in place</strong> so the store lands back at the same address.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Inc(p *int) {\n\t*p++\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>Take <code>x := 41</code> and call <code>Inc(&amp;x)</code>:</p>\n<ul>\n<li><code>p</code> receives the <strong>address</strong> of <code>x</code>.</li>\n<li><code>*p</code> reads the current value, <code>41</code>.</li>\n<li><code>*p++</code> writes <code>42</code> back to that address.</li>\n<li>Control returns; in the caller, <code>x</code> is now <code>42</code>.</li>\n</ul>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>p++</code> moves the pointer variable; <code>*p++</code> changes the pointee.</li>\n<li>A nil pointer dereference panics.</li>\n</ul>",
    "starter": "// Package incptr \u2014 Gopher Workplace challenge.\npackage incptr\n\n// Inc increments the int that p points to.\nfunc Inc(p *int) {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "incptr.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/junior/isempty": {
    "id": "01-language-basics/05-pointers/junior/isempty",
    "title": "Nil or Empty List",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Nil or Empty List</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>A nil head is the canonical empty-list representation; the check is a simple</p>\n<p>nil comparison.</p>\n<h2>Task</h2>\n<p>Implement <code>IsEmpty</code> in isempty.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  IsEmpty(nil)\nOutput: true\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  IsEmpty(&amp;Node{})\nOutput: false\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  IsEmpty(&amp;Node{Value: 0})\nOutput: false\n</code></pre>\n<p>_Explanation:_ A zero-value node is still a real node.</p>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">nil as empty</span><span class=\"tchip\">Nil comparison</span><span class=\"tchip\">No deref needed</span></div></details>",
    "education": "<h1>nil as the empty case</h1>\n<h2>Intuition</h2>\n<p>A nil pointer conventionally represents an empty linked structure; testing <code>== nil</code> needs no dereference.</p>\n<h2>Approach</h2>\n<p>1. An empty list is represented by a nil head.</p>\n<p>2. <code>return head == nil</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\ntype Node struct {\n\tVal  int\n\tNext *Node\n}\n\nfunc IsEmpty(head *Node) bool {\n\treturn head == nil\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>IsEmpty(nil)</code> \u2192 <code>true</code>. Any real <code>*Node</code>, even zero-valued, is non-nil \u2192 <code>false</code>.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Comparing to nil is always safe; dereferencing nil is not.</li>\n<li>Many recursive list algorithms base-case on nil.</li>\n</ul>",
    "starter": "// Package isempty \u2014 Gopher Workplace challenge.\npackage isempty\n\ntype Node struct {\n\tVal  int\n\tNext *Node\n}\n\n// IsEmpty reports whether the list is empty (head is nil).\nfunc IsEmpty(head *Node) bool {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "isempty.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/junior/listlen": {
    "id": "01-language-basics/05-pointers/junior/listlen",
    "title": "Linked List Length",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Linked List Length</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>Traversing a linked list follows <code>Next</code> pointers until nil.</p>\n<h2>Task</h2>\n<p>Implement <code>Length</code> in listlen.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Length(nil)\nOutput: 0\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Length(1 -&gt; 2 -&gt; 3)\nOutput: 3\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Length(&amp;Node{Value: 7})\nOutput: 1\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Pointer traversal</span><span class=\"tchip\">nil terminator</span><span class=\"tchip\">Counter</span></div></details>",
    "education": "<h1>Traversing pointer-linked structures</h1>\n<h2>Intuition</h2>\n<p>Following <code>Next</code> pointers to nil visits every node; nil marks the end.</p>\n<h2>Approach</h2>\n<p>1. Walk from <code>head</code>, following <code>.Next</code>.</p>\n<p>2. Count each non-nil node.</p>\n<p>3. Stop when the pointer is nil.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\ntype Node struct {\n\tVal  int\n\tNext *Node\n}\n\nfunc Length(head *Node) int {\n\tcount := 0\n\tfor n := head; n != nil; n = n.Next {\n\t\tcount++\n\t}\n\treturn count\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>For <code>1 -&gt; 2 -&gt; 3</code>: the loop visits three nodes before <code>n</code> becomes nil, returning <code>3</code>. An empty list starts nil, returning <code>0</code>.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Stop at nil, don't dereference it.</li>\n<li>An empty list (nil head) has length 0.</li>\n</ul>",
    "starter": "// Package listlen \u2014 Gopher Workplace challenge.\npackage listlen\n\ntype Node struct {\n\tVal  int\n\tNext *Node\n}\n\n// Length returns the number of nodes in the singly linked list starting at\n// head. nil head is length 0.\nfunc Length(head *Node) int {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "listlen.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/junior/mapbump": {
    "id": "01-language-basics/05-pointers/junior/mapbump",
    "title": "Mutate Through Map of Pointers",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Mutate Through Map of Pointers</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>Map values that are pointers let you mutate shared variables \u2014 the map stores</p>\n<p>addresses, not copies.</p>\n<h2>Task</h2>\n<p>Implement <code>BumpAll</code> in mapbump.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  a := 1; BumpAll(map[string]*int{\"a\": &amp;a})\nOutput: a == 2\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  a, b := 1, 2; BumpAll(m{\"a\":&amp;a,\"b\":&amp;b})\nOutput: a == 2, b == 3\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  BumpAll(map[string]*int{})\nOutput: no-op\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Map of pointers</span><span class=\"tchip\">Range values</span><span class=\"tchip\">Mutate through</span></div></details>",
    "education": "<h1>Pointer values in maps</h1>\n<h2>Intuition</h2>\n<p>Storing <code>*int</code> as map values lets updates flow to the underlying variables, unlike storing ints by value.</p>\n<h2>Approach</h2>\n<p>1. Range over the map's values (the pointers).</p>\n<p>2. <code>*p++</code> mutates each pointee.</p>\n<p>3. The map itself is unchanged \u2014 only the pointed-at ints move.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc BumpAll(m map[string]*int) {\n\tfor _, p := range m {\n\t\t*p++\n\t}\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>With <code>{\"a\": &amp;a}</code> and <code>a = 1</code>: the loop dereferences <code>&amp;a</code> and increments, so <code>a</code> becomes <code>2</code>.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Map iteration order is random but irrelevant here.</li>\n<li><code>*p++</code> changes the referenced variable.</li>\n</ul>",
    "starter": "// Package mapbump \u2014 Gopher Workplace challenge.\npackage mapbump\n\n// BumpAll increments every int pointed to by the map's values.\nfunc BumpAll(m map[string]*int) {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "mapbump.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/junior/maxptr": {
    "id": "01-language-basics/05-pointers/junior/maxptr",
    "title": "Return Pointer to Larger",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Return Pointer to Larger</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>Returning a pointer lets the caller keep mutating the chosen variable. Compare</p>\n<p>the pointed-to values, return the address.</p>\n<h2>Task</h2>\n<p>Implement <code>MaxPtr</code> in maxptr.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  a, b := 3, 8; MaxPtr(&amp;a, &amp;b)\nOutput: &amp;b (points at 8)\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  a, b := 8, 3; MaxPtr(&amp;a, &amp;b)\nOutput: &amp;a (points at 8)\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  a, b := 5, 5; MaxPtr(&amp;a, &amp;b)\nOutput: &amp;a\n</code></pre>\n<p>_Explanation:_ On a tie the first pointer is returned.</p>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Compare pointees</span><span class=\"tchip\">Return an address</span><span class=\"tchip\">Tie rule</span></div></details>",
    "education": "<h1>Returning a selected pointer</h1>\n<h2>Intuition</h2>\n<p>Comparing dereferenced values then returning one address hands the caller a live reference to the winner.</p>\n<h2>Approach</h2>\n<p>1. Compare the pointees: <code><em>a</code> vs <code></em>b</code>.</p>\n<p>2. Return the <strong>pointer</strong> to the larger, not the value.</p>\n<p>3. Default to <code>a</code> so ties return the first.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc MaxPtr(a, b *int) *int {\n\tif *b &gt; *a {\n\t\treturn b\n\t}\n\treturn a\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>MaxPtr(&amp;a, &amp;b)</code> with <code><em>a = 3</code>, <code></em>b = 8</code>: <code><em>b &gt; </em>a</code> is true, so <code>b</code> is returned \u2014 the caller can still mutate through it.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Compare <code><em>a</code>/<code></em>b</code>, but return <code>a</code>/<code>b</code>.</li>\n<li>The returned pointer still aliases the caller's variable.</li>\n</ul>",
    "starter": "// Package maxptr \u2014 Gopher Workplace challenge.\npackage maxptr\n\n// MaxPtr returns the pointer whose pointed-to value is larger (a on a tie).\nfunc MaxPtr(a, b *int) *int {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "maxptr.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/junior/negate": {
    "id": "01-language-basics/05-pointers/junior/negate",
    "title": "Negate In Place",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Negate In Place</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>Negation in place reads and rewrites through the pointer.</p>\n<h2>Task</h2>\n<p>Implement <code>Negate</code> in negate.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  x := 5; Negate(&amp;x)\nOutput: x == -5\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  x := -3; Negate(&amp;x)\nOutput: x == 3\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  x := 0; Negate(&amp;x)\nOutput: x == 0\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Write through pointer</span><span class=\"tchip\">Involution</span><span class=\"tchip\">In-place</span></div></details>",
    "education": "<h1>Unary mutation through a pointer</h1>\n<h2>Intuition</h2>\n<p><code><em>p = -</em>p</code> loads, negates, and stores back at the pointee.</p>\n<h2>Approach</h2>\n<p>1. Read the pointee <code>*p</code>.</p>\n<p>2. Store its negation: <code><em>p = -</em>p</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Negate(p *int) {\n\t*p = -*p\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>Negate(&amp;x)</code> with <code>x = 5</code>: <code>-*p</code> is <code>-5</code>, written back to <code>x</code>.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Negating twice is a no-op (<code>-(-x)==x</code>).</li>\n<li><code>-*p</code> reads first, then assign.</li>\n</ul>",
    "starter": "// Package negate \u2014 Gopher Workplace challenge.\npackage negate\n\n// Negate flips the sign of the int that p points to.\nfunc Negate(p *int) {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "negate.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/junior/newint": {
    "id": "01-language-basics/05-pointers/junior/newint",
    "title": "Allocate and Return Pointer",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Allocate and Return Pointer</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>Returning the address of a local is safe in Go \u2014 the value escapes to the heap</p>\n<p>and outlives the function. Each call yields a distinct pointer.</p>\n<h2>Task</h2>\n<p>Implement <code>Alloc</code> in newint.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  p := Alloc(7)\nOutput: *p == 7\n</code></pre>\n<p>_Explanation:_ A fresh int holding 7 is allocated and its address returned.</p>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  p := Alloc(0)\nOutput: p != nil, *p == 0\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  p, q := Alloc(7), Alloc(7)\nOutput: p != q\n</code></pre>\n<p>_Explanation:_ Each call allocates a distinct int \u2014 same value, different address.</p>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Address of a local</span><span class=\"tchip\">Escape to heap</span><span class=\"tchip\">Distinct allocations</span></div></details>",
    "education": "<h1>Returning pointers to locals</h1>\n<h2>Intuition</h2>\n<p>Unlike C, Go lets you return <code>&amp;local</code>; escape analysis moves it to the heap so it stays valid.</p>\n<h2>Approach</h2>\n<p>1. The parameter <code>v</code> already lives in fresh storage on each call.</p>\n<p>2. Return its address with <code>&amp;v</code>; Go moves it to the heap (escape analysis).</p>\n<p>3. Every call gets its own <code>v</code>, so the pointers differ.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Alloc(v int) *int {\n\treturn &amp;v\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>Alloc(7)</code> binds <code>v = 7</code>, returns <code>&amp;v</code>. Because <code>v</code> escapes, it survives the return; a second call has an independent <code>v</code>, so <code>p != q</code>.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Returning <code>&amp;v</code> is safe \u2014 no dangling pointer.</li>\n<li><code>new(int)</code> also allocates a zeroed int and returns its pointer.</li>\n</ul>",
    "starter": "// Package newint \u2014 Gopher Workplace challenge.\npackage newint\n\n// Alloc returns a pointer to a fresh int holding v.\nfunc Alloc(v int) *int {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "newint.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/junior/orzero": {
    "id": "01-language-basics/05-pointers/junior/orzero",
    "title": "Deref or Zero",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Deref or Zero</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>A very common helper: safely turn an optional pointer into a value, defaulting</p>\n<p>to the zero value on nil.</p>\n<h2>Task</h2>\n<p>Implement <code>DerefOrZero</code> in orzero.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  DerefOrZero(nil)\nOutput: 0\n</code></pre>\n<p>_Explanation:_ Nil \u2192 zero, no dereference.</p>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  n := 8; DerefOrZero(&amp;n)\nOutput: 8\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  n := 0; DerefOrZero(&amp;n)\nOutput: 0\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Nil guard</span><span class=\"tchip\">Zero default</span><span class=\"tchip\">Optional pointer</span></div></details>",
    "education": "<h1>Optional values as pointers</h1>\n<h2>Intuition</h2>\n<p>A <code>*int</code> doubles as an optional int; the nil case maps to the zero value.</p>\n<h2>Approach</h2>\n<p>1. Guard: <code>if p == nil { return 0 }</code>.</p>\n<p>2. Otherwise <code>return *p</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc DerefOrZero(p *int) int {\n\tif p == nil {\n\t\treturn 0\n\t}\n\treturn *p\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>DerefOrZero(nil)</code> returns <code>0</code> without touching memory; <code>DerefOrZero(&amp;n)</code> with <code>n = 8</code> dereferences to <code>8</code>.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Reading <code>*p</code> before the nil check panics.</li>\n<li>Returning 0 collapses nil and a real 0 \u2014 fine when that's intended.</li>\n</ul>",
    "starter": "// Package orzero \u2014 Gopher Workplace challenge.\npackage orzero\n\n// DerefOrZero returns *p, or 0 when p is nil.\nfunc DerefOrZero(p *int) int {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "orzero.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/junior/pushslice": {
    "id": "01-language-basics/05-pointers/junior/pushslice",
    "title": "Append Through Slice Pointer",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Append Through Slice Pointer</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>append may return a new slice header. To have the caller see growth, pass a</p>\n<p>pointer to the slice and assign the append result through it.</p>\n<h2>Task</h2>\n<p>Implement <code>Push</code> in pushslice.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  var xs []int; Push(&amp;xs, 1)\nOutput: xs == [1]\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  xs := []int{1}; Push(&amp;xs, 2)\nOutput: xs == [1 2]\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  var xs []int; Push(&amp;xs, 1); Push(&amp;xs, 2)\nOutput: xs == [1 2]\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Pointer to slice</span><span class=\"tchip\">Reassign through pointer</span><span class=\"tchip\">Header update</span></div></details>",
    "education": "<h1>Growing a caller's slice</h1>\n<h2>Intuition</h2>\n<p>Because append returns a possibly-new header, mutating the caller's slice requires assigning the result through a <code>*[]int</code>.</p>\n<h2>Approach</h2>\n<p>1. <code>sp</code> is a <code>*[]int</code> \u2014 a pointer to the caller's slice header.</p>\n<p>2. <code><em>sp = append(</em>sp, v)</code> writes the (possibly relocated) new header back.</p>\n<p>3. Without the pointer, <code>append</code>'s new header would be lost.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Push(sp *[]int, v int) {\n\t*sp = append(*sp, v)\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>Push(&amp;xs, 1)</code> on an empty slice: <code>append</code> returns a length-1 slice; storing it through <code>*sp</code> updates the caller's <code>xs</code>.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>A plain <code>[]int</code> parameter can't propagate a reallocated header.</li>\n<li><code><em>sp = append(</em>sp, v)</code> updates the caller.</li>\n</ul>",
    "starter": "// Package pushslice \u2014 Gopher Workplace challenge.\npackage pushslice\n\n// Push appends v to the slice that sp points to, updating the caller's slice.\nfunc Push(sp *[]int, v int) {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "pushslice.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/junior/reseat": {
    "id": "01-language-basics/05-pointers/junior/reseat",
    "title": "Reassign via Double Pointer",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Reassign via Double Pointer</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>To change which variable a pointer points to (as seen by the caller) you need a</p>\n<p>pointer TO the pointer: <code>**int</code>.</p>\n<h2>Task</h2>\n<p>Implement <code>Reseat</code> in reseat.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  a, b := 1, 2; p := &amp;a; Reseat(&amp;p, &amp;b)\nOutput: p == &amp;b\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  p := &amp;a; Reseat(&amp;p, nil)\nOutput: p == nil\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  p := &amp;a; Reseat(&amp;p, &amp;a)\nOutput: p == &amp;a (unchanged target)\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Pointer to pointer</span><span class=\"tchip\">int` aliases the caller's pointer. |\n| 2 |</span><span class=\"tchip\">Levels of indirection</span></div></details>",
    "education": "<h1>Double indirection</h1>\n<h2>Intuition</h2>\n<p>A <code>*<em>int</code> lets a callee reseat the caller's pointer variable; <code></em>pp = q</code> stores a new address into it.</p>\n<h2>Approach</h2>\n<p>1. <code>pp</code> is a <code>**int</code>: it points at the caller's pointer variable.</p>\n<p>2. <code>*pp = q</code> writes a new address into that pointer.</p>\n<p>3. This changes where <code>p</code> points, not the int it pointed at.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Reseat(pp **int, q *int) {\n\t*pp = q\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>Reseat(&amp;p, &amp;b)</code>: <code>*pp</code> refers to <code>p</code>; assigning <code>q</code> (which is <code>&amp;b</code>) makes <code>p</code> point at <code>b</code>.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Reassigning a plain <code>*int</code> parameter only changes the local copy.</li>\n<li><code>**int</code> reaches the caller's pointer.</li>\n</ul>",
    "starter": "// Package reseat \u2014 Gopher Workplace challenge.\npackage reseat\n\n// Reseat makes *pp point to q (reassigns the caller's pointer variable).\nfunc Reseat(pp **int, q *int) {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "reseat.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/junior/resetnode": {
    "id": "01-language-basics/05-pointers/junior/resetnode",
    "title": "Reset a Struct Pointer",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Reset a Struct Pointer</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>Through a struct pointer you can clear multiple fields, including pointer fields</p>\n<p>like <code>Next</code>.</p>\n<h2>Task</h2>\n<p>Implement <code>Reset</code> in resetnode.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  n := &amp;Node{Value: 5, Next: other}; Reset(n)\nOutput: n.Value == 0, n.Next == nil\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  n := &amp;Node{}; Reset(n)\nOutput: n.Value == 0, n.Next == nil\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Reset(&amp;Node{Value: -3})\nOutput: Value == 0, Next == nil\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Multiple field writes</span><span class=\"tchip\">Nil out a pointer field</span><span class=\"tchip\">Struct pointer receiver</span></div></details>",
    "education": "<h1>Clearing struct fields through a pointer</h1>\n<h2>Intuition</h2>\n<p>A struct pointer reaches every field, including nested pointer fields, letting a helper reset an object in place.</p>\n<h2>Approach</h2>\n<p>1. <code>n</code> is a <code>*Node</code>.</p>\n<p>2. Zero both fields through it: <code>n.Value = 0; n.Next = nil</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\ntype Node struct {\n\tValue int\n\tNext  *Node\n}\n\nfunc Reset(n *Node) {\n\tn.Value = 0\n\tn.Next = nil\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>Reset(n)</code> writes <code>0</code> into <code>n.Value</code> and <code>nil</code> into <code>n.Next</code>, clearing the caller's node.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Setting <code>n.Next = nil</code> drops the reference (helps GC).</li>\n<li>Value receiver would clear a copy only.</li>\n</ul>",
    "starter": "// Package resetnode \u2014 Gopher Workplace challenge.\npackage resetnode\n\ntype Node struct {\n\tValue int\n\tNext  *Node\n}\n\n// Reset sets the Node's Value to 0 and Next to nil.\nfunc Reset(n *Node) {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "resetnode.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/junior/samecart": {
    "id": "01-language-basics/05-pointers/junior/samecart",
    "title": "Same Struct Instance",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Same Struct Instance</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>Comparing struct POINTERS tests instance identity; two structs with equal</p>\n<p>fields are still different instances.</p>\n<h2>Task</h2>\n<p>Implement <code>Same</code> in samecart.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  c := &amp;Cart{}; Same(c, c)\nOutput: true\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Same(&amp;Cart{}, &amp;Cart{})\nOutput: false\n</code></pre>\n<p>_Explanation:_ Two separate allocations \u2192 different addresses.</p>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  c := &amp;Cart{}; d := c; Same(c, d)\nOutput: true\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Pointer identity</span><span class=\"tchip\">Instance vs value</span><span class=\"tchip\">Struct pointers</span></div></details>",
    "education": "<h1>Struct identity via pointers</h1>\n<h2>Intuition</h2>\n<p>Pointer <code>==</code> compares addresses, distinguishing two structurally-equal instances.</p>\n<h2>Approach</h2>\n<p>1. Compare the pointers with <code>==</code>.</p>\n<p>2. True only when both name the same struct in memory.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\ntype Cart struct{ Count int }\n\nfunc Same(a, b *Cart) bool {\n\treturn a == b\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>Same(c, c)</code> compares one address to itself \u2192 <code>true</code>. Two <code>&amp;Cart{}</code> literals allocate distinct structs \u2192 <code>false</code>.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code><em>a == </em>b</code> compares fields; <code>a == b</code> compares identity.</li>\n<li>Distinct <code>&amp;Cart{}</code> values are never equal pointers.</li>\n</ul>",
    "starter": "// Package samecart \u2014 Gopher Workplace challenge.\npackage samecart\n\ntype Cart struct{ Count int }\n\n// Same reports whether a and b are the SAME Cart instance (same address).\nfunc Same(a, b *Cart) bool {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "samecart.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/junior/sameptr": {
    "id": "01-language-basics/05-pointers/junior/sameptr",
    "title": "Pointer Identity",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Pointer Identity</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>Comparing pointers with <code>==</code> tests address identity, not the pointed-to values.</p>\n<p>Two variables with equal values still have different addresses.</p>\n<h2>Task</h2>\n<p>Implement <code>Same</code> in sameptr.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  x := 1; Same(&amp;x, &amp;x)\nOutput: true\n</code></pre>\n<p>_Explanation:_ Same variable \u2192 same address.</p>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  x, y := 1, 1; Same(&amp;x, &amp;y)\nOutput: false\n</code></pre>\n<p>_Explanation:_ Equal values but distinct variables \u2192 different addresses.</p>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  x := 1; p := &amp;x; Same(p, &amp;x)\nOutput: true\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Pointer equality</span><span class=\"tchip\">Identity vs value</span><span class=\"tchip\">Distinct variables</span></div></details>",
    "education": "<h1>Pointer equality is identity</h1>\n<h2>Intuition</h2>\n<p><code>==</code> on pointers compares the addresses they hold; it is true only when both refer to the same storage.</p>\n<h2>Approach</h2>\n<p>1. Pointer equality compares <strong>addresses</strong>, not pointee values.</p>\n<p>2. <code>return a == b</code> is true only when both point at the same storage.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Same(a, b *int) bool {\n\treturn a == b\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>Same(&amp;x, &amp;x)</code> compares identical addresses \u2192 <code>true</code>. <code>Same(&amp;x, &amp;y)</code> compares two distinct addresses \u2192 <code>false</code>, even when <code>x == y</code>.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Equal values do NOT imply equal pointers.</li>\n<li><code><em>a == </em>b</code> compares values; <code>a == b</code> compares addresses.</li>\n</ul>",
    "starter": "// Package sameptr \u2014 Gopher Workplace challenge.\npackage sameptr\n\n// Same reports whether a and b point to the SAME int (identical address).\nfunc Same(a, b *int) bool {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "sameptr.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/junior/scaleall": {
    "id": "01-language-basics/05-pointers/junior/scaleall",
    "title": "Mutate Through Slice of Pointers",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Mutate Through Slice of Pointers</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>A slice of pointers lets one call mutate many caller variables. Guard nil</p>\n<p>entries before dereferencing.</p>\n<h2>Task</h2>\n<p>Implement <code>ScaleAll</code> in scaleall.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  a, b := 2, 3; ScaleAll([]*int{&amp;a, &amp;b}, 10)\nOutput: a == 20, b == 30\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  a := 5; ScaleAll([]*int{&amp;a, nil}, 2)\nOutput: a == 10\n</code></pre>\n<p>_Explanation:_ Nil entries are skipped, no panic.</p>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  ScaleAll(nil, 10)\nOutput: no-op\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Slice of pointers</span><span class=\"tchip\">Nil-skip</span><span class=\"tchip\">Mutate through</span></div></details>",
    "education": "<h1>Collections of pointers</h1>\n<h2>Intuition</h2>\n<p>A <code>[]*int</code> holds addresses; iterating and dereferencing mutates each target, with nil entries needing a guard.</p>\n<h2>Approach</h2>\n<p>1. Range over the slice of pointers.</p>\n<p>2. Skip nil entries with <code>if p != nil</code>.</p>\n<p>3. Scale in place: <code><em>p </em>= k</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc ScaleAll(ps []*int, k int) {\n\tfor _, p := range ps {\n\t\tif p != nil {\n\t\t\t*p *= k\n\t\t}\n\t}\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>For <code>[]<em>int{&amp;a, &amp;b}</code> with <code>k = 10</code>: first <code></em>p *= 10</code> sets <code>a = 20</code>, then the second sets <code>b = 30</code>.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Dereferencing a nil element panics \u2014 skip it.</li>\n<li>Mutating <code>*p</code> changes the original variable.</li>\n</ul>",
    "starter": "// Package scaleall \u2014 Gopher Workplace challenge.\npackage scaleall\n\n// ScaleAll multiplies each pointed-to int by k. nil entries are skipped.\nfunc ScaleAll(ps []*int, k int) {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "scaleall.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/junior/setzero": {
    "id": "01-language-basics/05-pointers/junior/setzero",
    "title": "Zero Through Pointer",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Zero Through Pointer</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>Assigning through a pointer resets the caller's variable.</p>\n<h2>Task</h2>\n<p>Implement <code>Zero</code> in setzero.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  x := 99; Zero(&amp;x)\nOutput: x == 0\n</code></pre>\n<p>_Explanation:_ Assigning through the pointer clears the caller's variable.</p>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  x := 0; Zero(&amp;x)\nOutput: x == 0\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  x := -7; Zero(&amp;x)\nOutput: x == 0\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Write through pointer</span><span class=\"tchip\">Value reset</span><span class=\"tchip\">Pointer vs value</span></div></details>",
    "education": "<h1>Writing through a pointer</h1>\n<h2>Intuition</h2>\n<p><code>*p = v</code> stores v at the address p holds, mutating the caller's variable.</p>\n<h2>Approach</h2>\n<p>1. <code>p</code> points at the caller's int.</p>\n<p>2. <code>*p = 0</code> stores zero at that address.</p>\n<p>3. The change is visible to the caller because the storage is shared.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Zero(p *int) {\n\t*p = 0\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>x := 99</code>, <code>Zero(&amp;x)</code>: <code>*p = 0</code> writes <code>0</code> at the address of <code>x</code>, so the caller reads <code>0</code> afterwards.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Only <code>*p = ...</code> reaches the pointee.</li>\n<li>Passing the value by copy would not clear the original.</li>\n</ul>",
    "starter": "// Package setzero \u2014 Gopher Workplace challenge.\npackage setzero\n\n// Zero sets the int that p points to back to 0.\nfunc Zero(p *int) {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "setzero.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/junior/sumptrs": {
    "id": "01-language-basics/05-pointers/junior/sumptrs",
    "title": "Sum Through Pointers",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Sum Through Pointers</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>Reading through a slice of pointers requires skipping nil entries to avoid a</p>\n<p>panic.</p>\n<h2>Task</h2>\n<p>Implement <code>SumPtrs</code> in sumptrs.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  a, b := 1, 2; SumPtrs([]*int{&amp;a, nil, &amp;b})\nOutput: 3\n</code></pre>\n<p>_Explanation:_ Nil entries contribute nothing.</p>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  SumPtrs(nil)\nOutput: 0\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  a := 5; SumPtrs([]*int{&amp;a, &amp;a})\nOutput: 10\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Read through pointers</span><span class=\"tchip\">Nil skip</span><span class=\"tchip\">Accumulate</span></div></details>",
    "education": "<h1>Reading pointer collections safely</h1>\n<h2>Intuition</h2>\n<p>Dereferencing each non-nil pointer and summing avoids the nil-deref panic on empty slots.</p>\n<h2>Approach</h2>\n<p>1. Range the slice.</p>\n<p>2. Add <code>*p</code> only when <code>p != nil</code>.</p>\n<p>3. Return the running total.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc SumPtrs(ps []*int) int {\n\ttotal := 0\n\tfor _, p := range ps {\n\t\tif p != nil {\n\t\t\ttotal += *p\n\t\t}\n\t}\n\treturn total\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>[]*int{&amp;a, nil, &amp;b}</code> with <code>a = 1</code>, <code>b = 2</code>: add <code>1</code>, skip nil, add <code>2</code> \u2192 <code>3</code>.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Dereferencing a nil element panics.</li>\n<li>Skip nils, sum the rest.</li>\n</ul>",
    "starter": "// Package sumptrs \u2014 Gopher Workplace challenge.\npackage sumptrs\n\n// SumPtrs returns the total of the pointed-to ints, skipping nil entries.\nfunc SumPtrs(ps []*int) int {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "sumptrs.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/junior/swapcounts": {
    "id": "01-language-basics/05-pointers/junior/swapcounts",
    "title": "Swap Two Struct Fields",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Swap Two Struct Fields</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>Two struct pointers let you exchange a field between the caller's instances in</p>\n<p>place.</p>\n<h2>Task</h2>\n<p>Implement <code>SwapCounts</code> in swapcounts.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  a := Cart{Count:1}; b := Cart{Count:2}; SwapCounts(&amp;a, &amp;b)\nOutput: a.Count == 2, b.Count == 1\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  a := Cart{Count:5}; b := Cart{Count:5}; SwapCounts(&amp;a, &amp;b)\nOutput: a.Count == 5, b.Count == 5\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  a := Cart{Count:0}; b := Cart{Count:9}; SwapCounts(&amp;a, &amp;b)\nOutput: a.Count == 9, b.Count == 0\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Two struct pointers</span><span class=\"tchip\">Field-level swap</span><span class=\"tchip\">Parallel assignment</span></div></details>",
    "education": "<h1>Swapping fields across instances</h1>\n<h2>Intuition</h2>\n<p>Parallel assignment on two struct pointers exchanges a field without a temporary, mutating both callers' structs.</p>\n<h2>Approach</h2>\n<p>1. Access each field through its pointer.</p>\n<p>2. Parallel-assign <code>a.Count, b.Count = b.Count, a.Count</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\ntype Cart struct{ Count int }\n\nfunc SwapCounts(a, b *Cart) {\n\ta.Count, b.Count = b.Count, a.Count\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>SwapCounts(&amp;a, &amp;b)</code> with counts <code>1</code> and <code>2</code>: the right side reads <code>2, 1</code>, then both fields are stored at once.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Swapping the pointers (<code>a, b = b, a</code>) wouldn't affect the caller.</li>\n<li>Field-level swap touches the real instances.</li>\n</ul>",
    "starter": "// Package swapcounts \u2014 Gopher Workplace challenge.\npackage swapcounts\n\ntype Cart struct{ Count int }\n\n// SwapCounts exchanges the Count fields of the two carts.\nfunc SwapCounts(a, b *Cart) {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "swapcounts.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/junior/swapptr": {
    "id": "01-language-basics/05-pointers/junior/swapptr",
    "title": "Swap Through Pointers",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Swap Through Pointers</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>With two pointers you can swap the caller's variables in place \u2014 the classic</p>\n<p>demonstration that pointers alias real storage.</p>\n<h2>Task</h2>\n<p>Implement <code>Swap</code> in swapptr.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  x, y := 1, 2; Swap(&amp;x, &amp;y)\nOutput: x == 2, y == 1\n</code></pre>\n<p>_Explanation:_ Parallel assignment through both pointers exchanges the values.</p>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  x, y := 5, 5; Swap(&amp;x, &amp;y)\nOutput: x == 5, y == 5\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  x, y := -1, 9; Swap(&amp;x, &amp;y)\nOutput: x == 9, y == -1\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Two pointer params</span><span class=\"tchip\">Dereference both</span><span class=\"tchip\">Parallel assignment</span></div></details>",
    "education": "<h1>Swapping via pointers</h1>\n<h2>Intuition</h2>\n<p>Dereferencing two pointers and using parallel assignment exchanges the caller's values without a temporary.</p>\n<h2>Approach</h2>\n<p>1. <code>a</code> and <code>b</code> each alias a caller variable.</p>\n<p>2. Use parallel assignment <code><em>a, </em>b = <em>b, </em>a</code>.</p>\n<p>3. Go evaluates the whole right side first, so no temporary is needed.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Swap(a, b *int) {\n\t*a, *b = *b, *a\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>With <code>x, y := 1, 2</code> and <code>Swap(&amp;x, &amp;y)</code>:</p>\n<ul>\n<li>The right side <code><em>b, </em>a</code> reads <code>2, 1</code>.</li>\n<li>Both stores happen at once: <code><em>a = 2</code>, <code></em>b = 1</code>.</li>\n<li>Caller now sees <code>x == 2</code>, <code>y == 1</code>.</li>\n</ul>\n<h2>Pitfalls</h2>\n<ul>\n<li>Swapping the pointers themselves (<code>a, b = b, a</code>) does nothing to the caller.</li>\n<li>Dereference to touch the values.</li>\n</ul>",
    "starter": "// Package swapptr \u2014 Gopher Workplace challenge.\npackage swapptr\n\n// Swap exchanges the two ints that a and b point to.\nfunc Swap(a, b *int) {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "swapptr.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/junior/toggle": {
    "id": "01-language-basics/05-pointers/junior/toggle",
    "title": "Toggle a Bool Pointer",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Toggle a Bool Pointer</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>Pointers work for any type, including bool. Flip with <code>!</code>.</p>\n<h2>Task</h2>\n<p>Implement <code>Toggle</code> in toggle.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  b := false; Toggle(&amp;b)\nOutput: b == true\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  b := true; Toggle(&amp;b)\nOutput: b == false\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  b := false; Toggle(&amp;b); Toggle(&amp;b)\nOutput: b == false\n</code></pre>\n<p>_Explanation:_ Two toggles return to the start.</p>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Bool pointer</span><span class=\"tchip\">Logical not</span><span class=\"tchip\">Toggle</span></div></details>",
    "education": "<h1>Pointers to any type</h1>\n<h2>Intuition</h2>\n<p>A <code><em>bool</code> mutates a caller's flag just as <code></em>int</code> mutates an integer.</p>\n<h2>Approach</h2>\n<p>1. <code>p</code> points at a bool.</p>\n<p>2. <code><em>p = !</em>p</code> flips it in place.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Toggle(p *bool) {\n\t*p = !*p\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>Toggle(&amp;b)</code> with <code>b = false</code>: <code>!*p</code> is <code>true</code>, stored back into <code>b</code>.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>!*p</code> reads the current bool then negates.</li>\n<li>Applies to every type, not just numbers.</li>\n</ul>",
    "starter": "// Package toggle \u2014 Gopher Workplace challenge.\npackage toggle\n\n// Toggle flips the bool that p points to.\nfunc Toggle(p *bool) {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "toggle.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/junior/valueor": {
    "id": "01-language-basics/05-pointers/junior/valueor",
    "title": "Nil-Safe Dereference",
    "level": "junior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Nil-Safe Dereference</h1><div class=\"p-meta\"><span class=\"pill lv-junior\">junior</span></div></div><h2>Context</h2>\n<p>Dereferencing a nil pointer panics. Guard with a nil check before reading <code>*p</code>.</p>\n<h2>Task</h2>\n<p>Implement <code>ValueOr</code> in valueor.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  ValueOr(nil, 5)\nOutput: 5\n</code></pre>\n<p>_Explanation:_ Nil pointer \u2192 the default is returned, no dereference.</p>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  n := 9; ValueOr(&amp;n, 5)\nOutput: 9\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  n := 0; ValueOr(&amp;n, 5)\nOutput: 0\n</code></pre>\n<p>_Explanation:_ A real pointer to 0 still returns 0, not the default.</p>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Nil check</span><span class=\"tchip\">Safe dereference</span><span class=\"tchip\">Default fallback</span></div></details>",
    "education": "<h1>Guarding nil pointers</h1>\n<h2>Intuition</h2>\n<p>A nil <code><em>int</code> holds no address; reading <code></em>p</code> panics, so a nil check must precede the dereference.</p>\n<h2>Approach</h2>\n<p>1. Guard first: <code>if p == nil { return def }</code>.</p>\n<p>2. Only after the guard is it safe to <code>return *p</code>.</p>\n<p>3. A present pointer to zero is different from a missing pointer.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc ValueOr(p *int, def int) int {\n\tif p == nil {\n\t\treturn def\n\t}\n\treturn *p\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>ValueOr(nil, 5)</code>: guard catches nil, returns <code>5</code>. <code>ValueOr(&amp;n, 5)</code> with <code>n == 9</code>: guard passes, dereference yields <code>9</code>.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>*p</code> on a nil pointer panics with a nil-dereference.</li>\n<li>Check <code>p == nil</code> (or use short-circuit <code>p != nil &amp;&amp; ...</code>).</li>\n</ul>",
    "starter": "// Package valueor \u2014 Gopher Workplace challenge.\npackage valueor\n\n// ValueOr returns *p when p is non-nil, otherwise def.\nfunc ValueOr(p *int, def int) int {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "valueor.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/middle/appendtail": {
    "id": "01-language-basics/05-pointers/middle/appendtail",
    "title": "Append at Tail via Double Pointer",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Append at Tail via Double Pointer</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>Appending to the tail must handle the empty-list case by updating the caller's</p>\n<p>head pointer \u2014 hence <code>**Node</code>. Otherwise walk to the last node and link.</p>\n<h2>Task</h2>\n<p>Implement <code>Append</code> in appendtail.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  var h *Node; Append(&amp;h, 1); Append(&amp;h, 2)\nOutput: 1-&gt;2\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  h := &amp;Node{Val:1}; Append(&amp;h, 2)\nOutput: 1-&gt;2\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  var h *Node; Append(&amp;h, 9)\nOutput: 9\n</code></pre>\n<p>_Explanation:_ First append sets the head through the double pointer.</p>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Double pointer for empty case</span><span class=\"tchip\">Walk to tail</span><span class=\"tchip\">Link the new node</span></div></details>",
    "education": "<h1>Updating a head pointer via double indirection</h1>\n<h2>Intuition</h2>\n<p>Appending to an empty list must reseat the caller's head; a <code>**Node</code> lets the callee do that, while non-empty lists just link at the tail.</p>\n<h2>Approach</h2>\n<p>1. <code>head</code> is a <code>**Node</code> so the empty-list case can set the caller's head.</p>\n<p>2. If <code>*head == nil</code>, assign a new node and return.</p>\n<p>3. Otherwise walk to the tail and link the new node there.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\ntype Node struct {\n\tVal  int\n\tNext *Node\n}\n\nfunc Append(head **Node, v int) {\n\tif *head == nil {\n\t\t*head = &amp;Node{Val: v}\n\t\treturn\n\t}\n\tn := *head\n\tfor n.Next != nil {\n\t\tn = n.Next\n\t}\n\tn.Next = &amp;Node{Val: v}\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>On an empty list <code>Append(&amp;h, 1)</code> writes the head via <code>*head</code>. <code>Append(&amp;h, 2)</code> walks to node 1 and sets its <code>Next</code>, giving <code>1-&gt;2</code>.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Without <code>**Node</code>, an append to an empty list can't update the caller.</li>\n<li>Walk <code>n.Next</code> until nil to find the tail.</li>\n</ul>",
    "starter": "// Package appendtail \u2014 Gopher Workplace challenge.\npackage appendtail\n\ntype Node struct {\n\tVal  int\n\tNext *Node\n}\n\n// Append adds a node with value v to the END of the list, updating *head when\n// the list was empty.\nfunc Append(head **Node, v int) {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "appendtail.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/middle/boundmethod": {
    "id": "01-language-basics/05-pointers/middle/boundmethod",
    "title": "Bind a Pointer Method",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Bind a Pointer Method</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>A method value <code>c.Inc</code> bound on a pointer receiver captures the pointer, so the</p>\n<p>returned function keeps mutating the same counter.</p>\n<h2>Task</h2>\n<p>Implement <code>Bind</code> in boundmethod.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  inc := Bind(c); inc(); inc()\nOutput: c.N == 2\n</code></pre>\n<p>_Explanation:_ The method value captures the pointer receiver.</p>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  inc := Bind(c); inc()\nOutput: c.N == 1\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  c := &amp;Counter{N:5}; Bind(c)()\nOutput: c.N == 6\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Method value</span><span class=\"tchip\">Pointer capture</span><span class=\"tchip\">Shared state</span></div></details>",
    "education": "<h1>Method values on pointer receivers</h1>\n<h2>Intuition</h2>\n<p><code>c.Inc</code> (where Inc has a pointer receiver) yields a function value with the pointer bound, so calls mutate the shared counter.</p>\n<h2>Approach</h2>\n<p>1. <code>c.Inc</code> is a <strong>method value</strong>: it binds the receiver <code>c</code> now.</p>\n<p>2. Return it as a <code>func()</code>.</p>\n<p>3. Each call mutates the same counter through the captured pointer.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\ntype Counter struct{ N int }\n\nfunc (c *Counter) Inc() { c.N++ }\n\nfunc Bind(c *Counter) func() {\n\treturn c.Inc\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>Bind(c)</code> returns <code>c.Inc</code> with <code>c</code> fixed; calling the result twice increments <code>c.N</code> to 2.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>A pointer-receiver method value captures the pointer, not a copy.</li>\n<li>All invocations share the same underlying state.</li>\n</ul>",
    "starter": "// Package boundmethod \u2014 Gopher Workplace challenge.\npackage boundmethod\n\ntype Counter struct{ N int }\n\nfunc (c *Counter) Inc() { c.N++ }\n\n// Bind returns a function that, when called, increments c. It captures the\n// pointer so all invocations affect the same counter.\nfunc Bind(c *Counter) func() {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "boundmethod.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/middle/bstcontains": {
    "id": "01-language-basics/05-pointers/middle/bstcontains",
    "title": "BST Search",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">BST Search</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>BST search prunes half the tree at each step: go left when the target is</p>\n<p>smaller, right when larger.</p>\n<h2>Task</h2>\n<p>Implement <code>Contains</code> in bstcontains.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Contains(bst, 8)\nOutput: true\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Contains(bst, 4)\nOutput: false\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Contains(nil, 1)\nOutput: false\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Ordered descent</span><span class=\"tchip\">Prune</span><span class=\"tchip\">nil base</span></div></details>",
    "education": "<h1>Searching a BST</h1>\n<h2>Intuition</h2>\n<p>The ordering invariant lets search discard one subtree per step, giving O(height) lookups.</p>\n<h2>Approach</h2>\n<p>1. Nil \u2192 not found.</p>\n<p>2. Equal \u2192 found.</p>\n<p>3. Otherwise recurse into the side dictated by BST order.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\ntype Tree struct {\n\tVal         int\n\tLeft, Right *Tree\n}\n\nfunc Contains(root *Tree, v int) bool {\n\tif root == nil {\n\t\treturn false\n\t}\n\tif v == root.Val {\n\t\treturn true\n\t}\n\tif v &lt; root.Val {\n\t\treturn Contains(root.Left, v)\n\t}\n\treturn Contains(root.Right, v)\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>Contains(bst, 8)</code>: at each node compare and branch; the search reaches node 8 and returns true. A value not present bottoms out at nil \u2192 false.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Base-case nil returns false.</li>\n<li>Compare then recurse into exactly one child.</li>\n</ul>",
    "starter": "// Package bstcontains \u2014 Gopher Workplace challenge.\npackage bstcontains\n\ntype Tree struct {\n\tVal         int\n\tLeft, Right *Tree\n}\n\n// Contains reports whether v is in the BST.\nfunc Contains(root *Tree, v int) bool {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "bstcontains.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/middle/bstinsert": {
    "id": "01-language-basics/05-pointers/middle/bstinsert",
    "title": "BST Insert",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">BST Insert</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>BST insertion descends left for smaller values and right for larger/equal,</p>\n<p>creating a node when it reaches a nil slot. Returning the root handles the</p>\n<p>empty-tree case.</p>\n<h2>Task</h2>\n<p>Implement <code>Insert</code> in bstinsert.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Insert(nil, 5)\nOutput: tree rooted at 5\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Insert(root(5), 3)\nOutput: 3 becomes left child\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Insert(root(5), 8)\nOutput: 8 becomes right child\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">BST property</span><span class=\"tchip\">Recursive descent</span><span class=\"tchip\">Return the subtree</span></div></details>",
    "education": "<h1>Building a binary search tree</h1>\n<h2>Intuition</h2>\n<p>Insertion recurses to the correct nil slot and returns each subtree so parents can relink; the root return covers the empty tree.</p>\n<h2>Approach</h2>\n<p>1. Nil subtree \u2192 return a new node.</p>\n<p>2. Smaller values recurse left, larger recurse right.</p>\n<p>3. Reassign the child link and return <code>root</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\ntype Tree struct {\n\tVal         int\n\tLeft, Right *Tree\n}\n\nfunc Insert(root *Tree, v int) *Tree {\n\tif root == nil {\n\t\treturn &amp;Tree{Val: v}\n\t}\n\tif v &lt; root.Val {\n\t\troot.Left = Insert(root.Left, v)\n\t} else {\n\t\troot.Right = Insert(root.Right, v)\n\t}\n\treturn root\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>Insert(root(5), 3)</code>: <code>3 &lt; 5</code>, so recurse left into nil, which returns <code>&amp;Tree{Val:3}</code>, attached as <code>root.Left</code>.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Returning the subtree lets the parent reattach it.</li>\n<li>The nil slot is where the new node goes.</li>\n</ul>",
    "starter": "// Package bstinsert \u2014 Gopher Workplace challenge.\npackage bstinsert\n\ntype Tree struct {\n\tVal         int\n\tLeft, Right *Tree\n}\n\n// Insert adds v into the binary search tree and returns the (possibly new)\n// root. Duplicates go right.\nfunc Insert(root *Tree, v int) *Tree {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "bstinsert.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/middle/bucketadd": {
    "id": "01-language-basics/05-pointers/middle/bucketadd",
    "title": "Accumulate Into Bucket Pointers",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Accumulate Into Bucket Pointers</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>A map of struct POINTERS lets you mutate a bucket in place after fetching it \u2014</p>\n<p>unlike a map of struct values, which are not addressable.</p>\n<h2>Task</h2>\n<p>Implement <code>AddScore</code> in bucketadd.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  AddScore(m, \"a\", 3); AddScore(m, \"a\", 4)\nOutput: m[\"a\"].Total == 7\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  AddScore(m, \"b\", 5)\nOutput: m[\"b\"].Total == 5\n</code></pre>\n<p>_Explanation:_ First touch creates the bucket.</p>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  AddScore(m, \"a\", 0)\nOutput: bucket exists, Total unchanged\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Map of pointers</span><span class=\"tchip\">Lazy create</span><span class=\"tchip\">Mutate in place</span></div></details>",
    "education": "<h1>Mutable map values via pointers</h1>\n<h2>Intuition</h2>\n<p>Map struct VALUES aren't addressable (<code>m[k].F = x</code> is illegal), so storing <code>*Struct</code> lets you fetch and mutate the bucket in place.</p>\n<h2>Approach</h2>\n<p>1. Look up <code>m[key]</code>; a missing key yields a nil pointer.</p>\n<p>2. Lazily create <code>&amp;Bucket{}</code> on first use.</p>\n<p>3. Add through the pointer: <code>m[key].Total += pts</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\ntype Bucket struct{ Total int }\n\nfunc AddScore(m map[string]*Bucket, key string, pts int) {\n\tif m[key] == nil {\n\t\tm[key] = &amp;Bucket{}\n\t}\n\tm[key].Total += pts\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>First <code>AddScore(m, \"a\", 3)</code> allocates a bucket with Total 3; the second call finds it and adds 4, reaching 7.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>m[k].Total += x</code> on a map of struct VALUES won't compile.</li>\n<li>Store pointers and lazily create them.</li>\n</ul>",
    "starter": "// Package bucketadd \u2014 Gopher Workplace challenge.\npackage bucketadd\n\ntype Bucket struct{ Total int }\n\n// AddScore adds pts to m[key].Total, creating the bucket if absent.\nfunc AddScore(m map[string]*Bucket, key string, pts int) {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "bucketadd.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/middle/concatlist": {
    "id": "01-language-basics/05-pointers/middle/concatlist",
    "title": "Concatenate Two Lists",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Concatenate Two Lists</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>Concatenation links a's last node to b's head. If a is empty, the result is b.</p>\n<h2>Task</h2>\n<p>Implement <code>Concat</code> in concatlist.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Concat(1-&gt;2, 3-&gt;4)\nOutput: 1-&gt;2-&gt;3-&gt;4\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Concat(nil, 3-&gt;4)\nOutput: 3-&gt;4\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Concat(1-&gt;2, nil)\nOutput: 1-&gt;2\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Empty a</span><span class=\"tchip\">Find a's tail</span><span class=\"tchip\">Link tail to b</span></div></details>",
    "education": "<h1>Joining linked lists</h1>\n<h2>Intuition</h2>\n<p>Linking the first list's tail to the second's head concatenates them in O(len a); the empty first-list case returns the second directly.</p>\n<h2>Approach</h2>\n<p>1. If <code>a</code> is nil, <code>b</code> is the whole result.</p>\n<p>2. Walk to <code>a</code>'s tail and link <code>tail.Next = b</code>.</p>\n<p>3. Return <code>a</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\ntype Node struct {\n\tVal  int\n\tNext *Node\n}\n\nfunc Concat(a, b *Node) *Node {\n\tif a == nil {\n\t\treturn b\n\t}\n\tt := a\n\tfor t.Next != nil {\n\t\tt = t.Next\n\t}\n\tt.Next = b\n\treturn a\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>Concat(1-&gt;2, 3-&gt;4)</code>: the tail of <code>a</code> is node 2; setting <code>2.Next = b</code> joins the lists into <code>1-&gt;2-&gt;3-&gt;4</code>.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Handle a nil first list.</li>\n<li>Only a's tail's Next changes.</li>\n</ul>",
    "starter": "// Package concatlist \u2014 Gopher Workplace challenge.\npackage concatlist\n\ntype Node struct {\n\tVal  int\n\tNext *Node\n}\n\n// Concat appends list b after list a and returns the head. Either may be nil.\nfunc Concat(a, b *Node) *Node {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "concatlist.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/middle/copylist": {
    "id": "01-language-basics/05-pointers/middle/copylist",
    "title": "Deep Copy a List",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Deep Copy a List</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>A deep copy allocates a NEW node for each original node, so the two lists share</p>\n<p>no memory.</p>\n<h2>Task</h2>\n<p>Implement <code>Copy</code> in copylist.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  c := Copy(1-&gt;2-&gt;3)\nOutput: independent 1-&gt;2-&gt;3\n</code></pre>\n<p>_Explanation:_ Nodes are freshly allocated, not shared.</p>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Copy(nil)\nOutput: nil\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  c := Copy(orig); c.Val = 9\nOutput: orig.Val unchanged\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">New node per node</span><span class=\"tchip\">Copy the value</span><span class=\"tchip\">nil terminator</span></div></details>",
    "education": "<h1>Deep vs shallow copy</h1>\n<h2>Intuition</h2>\n<p>Copying the head pointer shares the whole list; a deep copy allocates a new node per element so mutations don't leak.</p>\n<h2>Approach</h2>\n<p>1. Base case: nil copies to nil.</p>\n<p>2. Allocate a new node with the same <code>Val</code> and recursively copy the tail.</p>\n<p>3. No node is shared with the original.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\ntype Node struct {\n\tVal  int\n\tNext *Node\n}\n\nfunc Copy(head *Node) *Node {\n\tif head == nil {\n\t\treturn nil\n\t}\n\treturn &amp;Node{Val: head.Val, Next: Copy(head.Next)}\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>Copy(1-&gt;2-&gt;3)</code> builds a new node 1 whose Next is a fresh copy of <code>2-&gt;3</code>, producing a fully independent list.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Returning <code>head</code> (shallow) shares every node.</li>\n<li>Allocate a new node for each; recurse on Next.</li>\n</ul>",
    "starter": "// Package copylist \u2014 Gopher Workplace challenge.\npackage copylist\n\ntype Node struct {\n\tVal  int\n\tNext *Node\n}\n\n// Copy returns an independent deep copy: mutating the copy must not affect the\n// original.\nfunc Copy(head *Node) *Node {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "copylist.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/middle/counterinc": {
    "id": "01-language-basics/05-pointers/middle/counterinc",
    "title": "Pointer Receiver Increment",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Pointer Receiver Increment</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>A method with a pointer receiver <code>(c *Counter)</code> can mutate the receiver; a value</p>\n<p>receiver would modify a copy and lose the change.</p>\n<h2>Task</h2>\n<p>Implement the <code>Inc</code> method in counterinc.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  c := &amp;Counter{}; c.Inc()\nOutput: c.N == 1\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  c := &amp;Counter{}; c.Inc(); c.Inc()\nOutput: c.N == 2\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  c := &amp;Counter{N: 5}; c.Inc()\nOutput: c.N == 6\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Pointer receiver</span><span class=\"tchip\">Field update</span><span class=\"tchip\">Value vs pointer receiver</span></div></details>",
    "education": "<h1>Pointer receivers mutate</h1>\n<h2>Intuition</h2>\n<p>A pointer-receiver method operates on the caller's value; a value-receiver method gets a copy and can't persist mutations.</p>\n<h2>Approach</h2>\n<p>1. The receiver is <code>*Counter</code>, so mutations persist.</p>\n<p>2. <code>c.N++</code> increments the caller's field.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\ntype Counter struct{ N int }\n\nfunc (c *Counter) Inc() {\n\tc.N++\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>c.Inc()</code> on <code>&amp;Counter{}</code> sets <code>N</code> to <code>1</code>; a pointer receiver is required or the increment would be lost on a copy.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>(c Counter)</code> (value) would increment a copy.</li>\n<li>Go auto-takes the address for <code>c.Inc()</code> when <code>c</code> is addressable.</li>\n</ul>",
    "starter": "// Package counterinc \u2014 Gopher Workplace challenge.\npackage counterinc\n\ntype Counter struct{ N int }\n\n// Inc increments the counter. It uses a POINTER receiver so the change sticks.\nfunc (c *Counter) Inc() {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "counterinc.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/middle/countif": {
    "id": "01-language-basics/05-pointers/middle/countif",
    "title": "Count Nodes by Predicate",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Count Nodes by Predicate</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>Combining pointer traversal with a predicate function counts matching nodes in</p>\n<p>one pass.</p>\n<h2>Task</h2>\n<p>Implement <code>CountIf</code> in countif.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  CountIf(1-&gt;2-&gt;3-&gt;4, even)\nOutput: 2\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  CountIf(nil, even)\nOutput: 0\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  CountIf(1-&gt;3-&gt;5, even)\nOutput: 0\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Traverse pointers</span><span class=\"tchip\">Predicate</span><span class=\"tchip\">Counter</span></div></details>",
    "education": "<h1>Predicate over a pointer structure</h1>\n<h2>Intuition</h2>\n<p>Threading a <code>func(int) bool</code> through a list traversal generalises counting/filtering to any condition.</p>\n<h2>Approach</h2>\n<p>1. Walk the list.</p>\n<p>2. Apply <code>pred</code> to each <code>Val</code>, counting the trues.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\ntype Node struct {\n\tVal  int\n\tNext *Node\n}\n\nfunc CountIf(head *Node, pred func(int) bool) int {\n\tc := 0\n\tfor n := head; n != nil; n = n.Next {\n\t\tif pred(n.Val) {\n\t\t\tc++\n\t\t}\n\t}\n\treturn c\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>CountIf(1-&gt;2-&gt;3-&gt;4, even)</code> tests each value; 2 and 4 pass, so the count is 2.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Stop at nil; test <code>n.Val</code> each step.</li>\n<li>The predicate decouples the condition from the walk.</li>\n</ul>",
    "starter": "// Package countif \u2014 Gopher Workplace challenge.\npackage countif\n\ntype Node struct {\n\tVal  int\n\tNext *Node\n}\n\n// CountIf returns how many node values satisfy pred.\nfunc CountIf(head *Node, pred func(int) bool) int {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "countif.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/middle/countleaves": {
    "id": "01-language-basics/05-pointers/middle/countleaves",
    "title": "Count Leaf Nodes",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Count Leaf Nodes</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>A leaf has no children. Count leaves by recursing and summing the leaf counts</p>\n<p>of both subtrees.</p>\n<h2>Task</h2>\n<p>Implement <code>CountLeaves</code> in countleaves.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  CountLeaves(nil)\nOutput: 0\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  CountLeaves(single node)\nOutput: 1\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  CountLeaves(root with 2 children)\nOutput: 2\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Leaf condition</span><span class=\"tchip\">Recursive sum</span><span class=\"tchip\">nil base</span></div></details>",
    "education": "<h1>Classifying nodes during traversal</h1>\n<h2>Intuition</h2>\n<p>A leaf test (both children nil) plus a recursive sum counts leaves; the same shape counts internal nodes or full nodes.</p>\n<h2>Approach</h2>\n<p>1. Nil \u2192 0.</p>\n<p>2. A node with no children is a leaf \u2192 1.</p>\n<p>3. Otherwise sum the leaves of both subtrees.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\ntype Tree struct {\n\tVal         int\n\tLeft, Right *Tree\n}\n\nfunc CountLeaves(t *Tree) int {\n\tif t == nil {\n\t\treturn 0\n\t}\n\tif t.Left == nil &amp;&amp; t.Right == nil {\n\t\treturn 1\n\t}\n\treturn CountLeaves(t.Left) + CountLeaves(t.Right)\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>For a root with two childless children, each child returns 1, so the root returns <code>1 + 1 = 2</code>.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Distinguish nil (0) from a leaf (1).</li>\n<li>Sum both subtrees for internal nodes.</li>\n</ul>",
    "starter": "// Package countleaves \u2014 Gopher Workplace challenge.\npackage countleaves\n\ntype Tree struct {\n\tVal         int\n\tLeft, Right *Tree\n}\n\n// CountLeaves returns the number of leaf nodes (no children).\nfunc CountLeaves(t *Tree) int {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "countleaves.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/middle/deduplist": {
    "id": "01-language-basics/05-pointers/middle/deduplist",
    "title": "Dedup Sorted List",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Dedup Sorted List</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>In a sorted list, duplicates are adjacent. Skip a node by pointing the current</p>\n<p>node's Next past any equal successor.</p>\n<h2>Task</h2>\n<p>Implement <code>Dedup</code> in deduplist.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Dedup(1-&gt;1-&gt;2-&gt;3-&gt;3)\nOutput: 1-&gt;2-&gt;3\n</code></pre>\n<p>_Explanation:_ Adjacent duplicates in a sorted list are removed.</p>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Dedup(nil)\nOutput: nil\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Dedup(1-&gt;2-&gt;3)\nOutput: 1-&gt;2-&gt;3\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Adjacent duplicates</span><span class=\"tchip\">Skip by relinking</span><span class=\"tchip\">Advance carefully</span></div></details>",
    "education": "<h1>In-place list compaction</h1>\n<h2>Intuition</h2>\n<p>Relinking past equal successors removes duplicates without allocation; advance only when you didn't skip.</p>\n<h2>Approach</h2>\n<p>1. Walk <code>cur</code>.</p>\n<p>2. While <code>cur.Next</code> has the same value, unlink it via <code>cur.Next = cur.Next.Next</code>.</p>\n<p>3. Otherwise advance <code>cur</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\ntype Node struct {\n\tVal  int\n\tNext *Node\n}\n\nfunc Dedup(head *Node) *Node {\n\tcur := head\n\tfor cur != nil &amp;&amp; cur.Next != nil {\n\t\tif cur.Val == cur.Next.Val {\n\t\t\tcur.Next = cur.Next.Next\n\t\t} else {\n\t\t\tcur = cur.Next\n\t\t}\n\t}\n\treturn head\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>Dedup(1-&gt;1-&gt;2-&gt;3-&gt;3)</code>: node 1 skips the duplicate 1; node 3 skips the duplicate 3, leaving <code>1-&gt;2-&gt;3</code>.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Don't advance <code>cur</code> when you just skipped \u2014 there may be more dups.</li>\n<li>Guard <code>cur.Next != nil</code> before comparing.</li>\n</ul>",
    "starter": "// Package deduplist \u2014 Gopher Workplace challenge.\npackage deduplist\n\ntype Node struct {\n\tVal  int\n\tNext *Node\n}\n\n// Dedup removes consecutive duplicate values from a SORTED list, in place, and\n// returns the head.\nfunc Dedup(head *Node) *Node {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "deduplist.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/middle/dellist": {
    "id": "01-language-basics/05-pointers/middle/dellist",
    "title": "Delete First Match",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Delete First Match</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>Deleting from a linked list means re-pointing the predecessor's Next past the</p>\n<p>target. Deleting the head returns a new head.</p>\n<h2>Task</h2>\n<p>Implement <code>Delete</code> in dellist.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Delete(1-&gt;2-&gt;3, 2)\nOutput: 1-&gt;3\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Delete(1-&gt;2-&gt;3, 1)\nOutput: 2-&gt;3\n</code></pre>\n<p>_Explanation:_ Deleting the head returns <code>head.Next</code>.</p>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Delete(1-&gt;2-&gt;3, 9)\nOutput: 1-&gt;2-&gt;3\n</code></pre>\n<p>_Explanation:_ Missing value leaves the list unchanged.</p>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Head special case</span><span class=\"tchip\">Predecessor relink</span><span class=\"tchip\">First match only</span></div></details>",
    "education": "<h1>Node deletion by relinking</h1>\n<h2>Intuition</h2>\n<p>Removing a node re-points its predecessor's Next to its successor; deleting the head yields a new head.</p>\n<h2>Approach</h2>\n<p>1. If the head matches, return <code>head.Next</code>.</p>\n<p>2. Else walk with a <code>prev</code>; when <code>prev.Next.Val == v</code>, splice it out via <code>prev.Next = prev.Next.Next</code>.</p>\n<p>3. Return the (possibly new) head.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\ntype Node struct {\n\tVal  int\n\tNext *Node\n}\n\nfunc Delete(head *Node, v int) *Node {\n\tif head == nil {\n\t\treturn nil\n\t}\n\tif head.Val == v {\n\t\treturn head.Next\n\t}\n\tprev := head\n\tfor prev.Next != nil {\n\t\tif prev.Next.Val == v {\n\t\t\tprev.Next = prev.Next.Next\n\t\t\tbreak\n\t\t}\n\t\tprev = prev.Next\n\t}\n\treturn head\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>Delete(1-&gt;2-&gt;3, 2)</code>: head is <code>1</code>, not a match; at node 1 the next is <code>2</code>, so <code>1.Next = 3</code>, yielding <code>1-&gt;3</code>.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Handle the head separately \u2014 it has no predecessor.</li>\n<li>Only the first match is removed.</li>\n</ul>",
    "starter": "// Package dellist \u2014 Gopher Workplace challenge.\npackage dellist\n\ntype Node struct {\n\tVal  int\n\tNext *Node\n}\n\n// Delete removes the FIRST node whose Val == v and returns the (possibly new)\n// head. If no match, the list is unchanged.\nfunc Delete(head *Node, v int) *Node {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "dellist.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/middle/elemptrs": {
    "id": "01-language-basics/05-pointers/middle/elemptrs",
    "title": "Pointers Into a Slice",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Pointers Into a Slice</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>Taking <code>&amp;xs[i]</code> yields a pointer that aliases the slice element; mutating</p>\n<p>through it changes the slice. (Go 1.22 per-iteration range vars make this safe.)</p>\n<h2>Task</h2>\n<p>Implement <code>Pointers</code> in elemptrs.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  xs := []int{1,2,3}; ps := Pointers(xs); *ps[1] = 99\nOutput: xs[1] == 99\n</code></pre>\n<p>_Explanation:_ Each pointer aliases a real slice element.</p>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Pointers([]int{})\nOutput: [] (empty)\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  ps := Pointers(xs); *ps[0]\nOutput: xs[0]\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Address of element</span><span class=\"tchip\">Slice of pointers</span><span class=\"tchip\">Aliasing</span></div></details>",
    "education": "<h1>Pointers into slice elements</h1>\n<h2>Intuition</h2>\n<p><code>&amp;xs[i]</code> addresses the actual backing-array slot, so a <code>[]*int</code> of these aliases the slice; writes through them mutate xs.</p>\n<h2>Approach</h2>\n<p>1. Range by <strong>index</strong>, not value, so each <code>&amp;xs[i]</code> addresses the element.</p>\n<p>2. Taking <code>&amp;v</code> of the loop copy would alias the copy, not the slice.</p>\n<p>3. Append each element address.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Pointers(xs []int) []*int {\n\tvar ps []*int\n\tfor i := range xs {\n\t\tps = append(ps, &amp;xs[i])\n\t}\n\treturn ps\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>Pointers(xs)</code> collects <code>&amp;xs[0], &amp;xs[1], &amp;xs[2]</code>; writing through <code>ps[1]</code> mutates <code>xs[1]</code> directly.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Use <code>&amp;xs[i]</code> (indexed), not <code>&amp;v</code> from a range value in a way that detaches \u2014 though Go 1.22 makes <code>&amp;v</code> per-iteration too.</li>\n<li>Appending to xs later may reallocate and detach these pointers.</li>\n</ul>",
    "starter": "// Package elemptrs \u2014 Gopher Workplace challenge.\npackage elemptrs\n\n// Pointers returns a slice of pointers to each element of xs (aliasing xs).\nfunc Pointers(xs []int) []*int {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "elemptrs.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/middle/hascycle": {
    "id": "01-language-basics/05-pointers/middle/hascycle",
    "title": "Detect a Cycle",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Detect a Cycle</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>Floyd's algorithm: a slow pointer (one step) and a fast pointer (two steps).</p>\n<p>If they ever meet, there is a cycle; if fast reaches nil, there isn't.</p>\n<h2>Task</h2>\n<p>Implement <code>HasCycle</code> in hascycle.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  HasCycle(1-&gt;2-&gt;3 acyclic)\nOutput: false\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  HasCycle(1-&gt;2-&gt;3-&gt;back to 1)\nOutput: true\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  HasCycle(nil)\nOutput: false\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Slow/fast pointers</span><span class=\"tchip\">Meeting means cycle</span><span class=\"tchip\">nil means acyclic</span></div></details>",
    "education": "<h1>Floyd's cycle detection</h1>\n<h2>Intuition</h2>\n<p>Two pointers at speeds 1 and 2 must meet inside a cycle and never meet in an acyclic list \u2014 O(1) space.</p>\n<h2>Approach</h2>\n<p>1. Move <code>slow</code> by 1 and <code>fast</code> by 2.</p>\n<p>2. If they ever meet, there is a cycle.</p>\n<p>3. If <code>fast</code> reaches nil, the list is acyclic.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\ntype Node struct {\n\tVal  int\n\tNext *Node\n}\n\nfunc HasCycle(head *Node) bool {\n\tslow, fast := head, head\n\tfor fast != nil &amp;&amp; fast.Next != nil {\n\t\tslow = slow.Next\n\t\tfast = fast.Next.Next\n\t\tif slow == fast {\n\t\t\treturn true\n\t\t}\n\t}\n\treturn false\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>On a cyclic list fast laps slow and they collide \u2192 true; on an acyclic list fast walks off the end \u2192 false.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Guard <code>fast</code> and <code>fast.Next</code> before the double step.</li>\n<li>Reaching nil proves no cycle.</li>\n</ul>",
    "starter": "// Package hascycle \u2014 Gopher Workplace challenge.\npackage hascycle\n\ntype Node struct {\n\tVal  int\n\tNext *Node\n}\n\n// HasCycle reports whether the list contains a cycle, using slow/fast pointers.\nfunc HasCycle(head *Node) bool {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "hascycle.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/middle/listslice": {
    "id": "01-language-basics/05-pointers/middle/listslice",
    "title": "List to Slice",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">List to Slice</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>Flattening a linked list into a slice is a straight traversal, appending each</p>\n<p>value.</p>\n<h2>Task</h2>\n<p>Implement <code>ToSlice</code> in listslice.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  ToSlice(1-&gt;2-&gt;3)\nOutput: [1 2 3]\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  ToSlice(nil)\nOutput: [] (empty)\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  ToSlice(9)\nOutput: [9]\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Traverse</span><span class=\"tchip\">Append values</span><span class=\"tchip\">Empty case</span></div></details>",
    "education": "<h1>Materialising a list</h1>\n<h2>Intuition</h2>\n<p>Traversing and appending converts a pointer chain to a contiguous slice.</p>\n<h2>Approach</h2>\n<p>1. Walk the list.</p>\n<p>2. Append each <code>Val</code> to a growing slice.</p>\n<p>3. Return it.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\ntype Node struct {\n\tVal  int\n\tNext *Node\n}\n\nfunc ToSlice(head *Node) []int {\n\tvar out []int\n\tfor n := head; n != nil; n = n.Next {\n\t\tout = append(out, n.Val)\n\t}\n\treturn out\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>ToSlice(1-&gt;2-&gt;3)</code> appends 1, 2, 3 in order, producing <code>[1 2 3]</code>.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Return empty (len 0) for nil head.</li>\n<li>One pass, append each value.</li>\n</ul>",
    "starter": "// Package listslice \u2014 Gopher Workplace challenge.\npackage listslice\n\ntype Node struct {\n\tVal  int\n\tNext *Node\n}\n\n// ToSlice returns the node values as a slice, in order. nil -> empty.\nfunc ToSlice(head *Node) []int {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "listslice.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/middle/makeuser": {
    "id": "01-language-basics/05-pointers/middle/makeuser",
    "title": "new vs Composite Literal",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">new vs Composite Literal</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p><code>&amp;User{...}</code> allocates and initialises a struct in one step, returning its</p>\n<p>address \u2014 the idiomatic constructor form (clearer than <code>new(User)</code> plus field</p>\n<p>assignments).</p>\n<h2>Task</h2>\n<p>Implement <code>NewUser</code> in makeuser.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  NewUser(\"ann\", 30)\nOutput: &amp;User{Name:\"ann\", Age:30}\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  u := NewUser(\"x\", 0); u != nil\nOutput: true\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  NewUser(\"\", -1)\nOutput: &amp;User{Name:\"\", Age:-1}\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Composite literal pointer</span><span class=\"tchip\">Allocation</span><span class=\"tchip\">Constructor idiom</span></div></details>",
    "education": "<h1>Constructing struct pointers</h1>\n<h2>Intuition</h2>\n<p><code>&amp;T{...}</code> allocates and initialises in one expression; it's the idiomatic constructor, equivalent to <code>new(T)</code> followed by field sets.</p>\n<h2>Approach</h2>\n<p>1. Build a composite literal <code>&amp;User{...}</code>.</p>\n<p>2. Return its address; the struct escapes to the heap and outlives the call.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\ntype User struct {\n\tName string\n\tAge  int\n}\n\nfunc NewUser(name string, age int) *User {\n\treturn &amp;User{Name: name, Age: age}\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>NewUser(\"ann\", 30)</code> allocates a <code>User</code>, fills both fields, and returns a pointer the caller keeps.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>&amp;User{...}</code> is clearer than <code>new(User)</code> + assignments.</li>\n<li>Each call allocates a distinct instance.</li>\n</ul>",
    "starter": "// Package makeuser \u2014 Gopher Workplace challenge.\npackage makeuser\n\ntype User struct {\n\tName string\n\tAge  int\n}\n\n// NewUser returns a pointer to a User with the given fields.\nfunc NewUser(name string, age int) *User {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "makeuser.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/middle/mergelists": {
    "id": "01-language-basics/05-pointers/middle/mergelists",
    "title": "Merge Two Sorted Lists",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Merge Two Sorted Lists</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>Merging uses a dummy head and a tail pointer, always attaching the smaller</p>\n<p>current node and advancing that list.</p>\n<h2>Task</h2>\n<p>Implement <code>Merge</code> in mergelists.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Merge(1-&gt;3-&gt;5, 2-&gt;4-&gt;6)\nOutput: 1-&gt;2-&gt;3-&gt;4-&gt;5-&gt;6\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Merge(nil, 2-&gt;4)\nOutput: 2-&gt;4\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Merge(1-&gt;2, nil)\nOutput: 1-&gt;2\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Dummy head</span><span class=\"tchip\">Tail pointer</span><span class=\"tchip\">Attach remainder</span></div></details>",
    "education": "<h1>Merging with a dummy head</h1>\n<h2>Intuition</h2>\n<p>A sentinel dummy node removes special-casing the first append; a tail pointer builds the merged list in order.</p>\n<h2>Approach</h2>\n<p>1. Use a <code>dummy</code> head and a <code>tail</code> cursor.</p>\n<p>2. While both lists are non-empty, attach the smaller node and advance it.</p>\n<p>3. Append whatever remains; return <code>dummy.Next</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\ntype Node struct {\n\tVal  int\n\tNext *Node\n}\n\nfunc Merge(a, b *Node) *Node {\n\tdummy := &amp;Node{}\n\ttail := dummy\n\tfor a != nil &amp;&amp; b != nil {\n\t\tif a.Val &lt;= b.Val {\n\t\t\ttail.Next = a\n\t\t\ta = a.Next\n\t\t} else {\n\t\t\ttail.Next = b\n\t\t\tb = b.Next\n\t\t}\n\t\ttail = tail.Next\n\t}\n\tif a != nil {\n\t\ttail.Next = a\n\t} else {\n\t\ttail.Next = b\n\t}\n\treturn dummy.Next\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>Merge(1-&gt;3-&gt;5, 2-&gt;4-&gt;6)</code>: pick 1, then 2, then 3... alternating by value, building the sorted <code>1-&gt;2-&gt;3-&gt;4-&gt;5-&gt;6</code>.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>The dummy head avoids branching on the first node.</li>\n<li>Attach the leftover list directly once one runs out.</li>\n</ul>",
    "starter": "// Package mergelists \u2014 Gopher Workplace challenge.\npackage mergelists\n\ntype Node struct {\n\tVal  int\n\tNext *Node\n}\n\n// Merge merges two sorted lists into one sorted list and returns the head.\nfunc Merge(a, b *Node) *Node {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "mergelists.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/middle/midnode": {
    "id": "01-language-basics/05-pointers/middle/midnode",
    "title": "Middle of List",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Middle of List</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>The slow/fast (tortoise/hare) technique finds the middle in one pass: advance</p>\n<p>slow by one and fast by two; when fast reaches the end, slow is at the middle.</p>\n<h2>Task</h2>\n<p>Implement <code>Middle</code> in midnode.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Middle(1-&gt;2-&gt;3-&gt;4-&gt;5)\nOutput: node 3\n</code></pre>\n<p>_Explanation:_ Odd length \u2192 exact middle.</p>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Middle(1-&gt;2-&gt;3-&gt;4)\nOutput: node 3\n</code></pre>\n<p>_Explanation:_ Even length \u2192 second of the two middles.</p>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Middle(1)\nOutput: node 1\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Slow/fast pointers</span><span class=\"tchip\">Termination</span><span class=\"tchip\">Even-length rule</span></div></details>",
    "education": "<h1>The tortoise-and-hare technique</h1>\n<h2>Intuition</h2>\n<p>Two pointers at different speeds locate the middle (or detect cycles) in a single pass with O(1) extra space.</p>\n<h2>Approach</h2>\n<p>1. Run <code>slow</code> by one and <code>fast</code> by two.</p>\n<p>2. When <code>fast</code> reaches the end, <code>slow</code> sits at the middle.</p>\n<p>3. One pass, no length count needed.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\ntype Node struct {\n\tVal  int\n\tNext *Node\n}\n\nfunc Middle(head *Node) *Node {\n\tslow, fast := head, head\n\tfor fast != nil &amp;&amp; fast.Next != nil {\n\t\tslow = slow.Next\n\t\tfast = fast.Next.Next\n\t}\n\treturn slow\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>For <code>1..5</code>: slow goes 1,2,3 while fast goes 1,3,5; fast stops, slow is at node 3.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Guard both <code>fast != nil</code> and <code>fast.Next != nil</code> before advancing.</li>\n<li>The even-length midpoint convention depends on the loop condition.</li>\n</ul>",
    "starter": "// Package midnode \u2014 Gopher Workplace challenge.\npackage midnode\n\ntype Node struct {\n\tVal  int\n\tNext *Node\n}\n\n// Middle returns the middle node using slow/fast pointers. For even length it\n// returns the second of the two middle nodes. nil for empty.\nfunc Middle(head *Node) *Node {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "midnode.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/middle/mirrortree": {
    "id": "01-language-basics/05-pointers/middle/mirrortree",
    "title": "Mirror a Tree",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Mirror a Tree</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>Mirroring swaps each node's children and recurses into both subtrees, mutating</p>\n<p>the tree in place.</p>\n<h2>Task</h2>\n<p>Implement <code>Mirror</code> in mirrortree.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Mirror(1 with children 2, 3)\nOutput: children become 3, 2\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Mirror(nil)\nOutput: no-op\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Mirror(single node)\nOutput: unchanged\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Swap children</span><span class=\"tchip\">Recurse both</span><span class=\"tchip\">nil base</span></div></details>",
    "education": "<h1>In-place tree transformation</h1>\n<h2>Intuition</h2>\n<p>Swapping children at every node and recursing mirrors the tree; the swap is a parallel assignment on pointer fields.</p>\n<h2>Approach</h2>\n<p>1. Nil \u2192 return.</p>\n<p>2. Swap <code>t.Left</code> and <code>t.Right</code>.</p>\n<p>3. Recurse into both subtrees.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\ntype Tree struct {\n\tVal         int\n\tLeft, Right *Tree\n}\n\nfunc Mirror(t *Tree) {\n\tif t == nil {\n\t\treturn\n\t}\n\tt.Left, t.Right = t.Right, t.Left\n\tMirror(t.Left)\n\tMirror(t.Right)\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>Mirror</code> swaps each node's children top-down; a root with children 2,3 ends with children 3,2, and the swap propagates to every subtree.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Swap before or after recursing \u2014 both work since the swap is local.</li>\n<li>Base-case nil returns immediately.</li>\n</ul>",
    "starter": "// Package mirrortree \u2014 Gopher Workplace challenge.\npackage mirrortree\n\ntype Tree struct {\n\tVal         int\n\tLeft, Right *Tree\n}\n\n// Mirror flips the tree in place: every node's Left and Right are swapped.\nfunc Mirror(t *Tree) {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "mirrortree.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/middle/nthfromend": {
    "id": "01-language-basics/05-pointers/middle/nthfromend",
    "title": "Nth Node From End",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Nth Node From End</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>The two-pointer gap technique: advance a lead pointer n nodes ahead, then move</p>\n<p>both until lead reaches the end; the trailing pointer is at the target.</p>\n<h2>Task</h2>\n<p>Implement <code>NthFromEnd</code> in nthfromend.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  NthFromEnd(1-&gt;2-&gt;3-&gt;4-&gt;5, 1)\nOutput: node 5\n</code></pre>\n<p>_Explanation:_ 1st from the end is the last node.</p>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  NthFromEnd(1-&gt;2-&gt;3-&gt;4-&gt;5, 3)\nOutput: node 3\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  NthFromEnd(1-&gt;2, 5)\nOutput: nil\n</code></pre>\n<p>_Explanation:_ n larger than the length \u2192 nil.</p>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Fixed gap</span><span class=\"tchip\">Move together</span><span class=\"tchip\">Bounds</span></div></details>",
    "education": "<h1>The two-pointer gap technique</h1>\n<h2>Intuition</h2>\n<p>Keeping a fixed n-node gap between two pointers locates the n-th-from-end in one pass without counting length first.</p>\n<h2>Approach</h2>\n<p>1. Advance a <code>lead</code> pointer <code>n</code> steps (nil if it overruns).</p>\n<p>2. Move <code>lead</code> and a <code>trail</code> together until <code>lead</code> hits the end.</p>\n<p>3. <code>trail</code> is then n-from-end.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\ntype Node struct {\n\tVal  int\n\tNext *Node\n}\n\nfunc NthFromEnd(head *Node, n int) *Node {\n\tlead := head\n\tfor i := 0; i &lt; n; i++ {\n\t\tif lead == nil {\n\t\t\treturn nil\n\t\t}\n\t\tlead = lead.Next\n\t}\n\ttrail := head\n\tfor lead != nil {\n\t\tlead = lead.Next\n\t\ttrail = trail.Next\n\t}\n\treturn trail\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>For <code>1..5, n=1</code>: lead advances 1 step to node 2, then lead and trail move together until lead is nil; trail lands on node 5.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>If advancing the lead n steps runs past the end, the list is too short.</li>\n<li>Both pointers move in lockstep after the gap is set.</li>\n</ul>",
    "starter": "// Package nthfromend \u2014 Gopher Workplace challenge.\npackage nthfromend\n\ntype Node struct {\n\tVal  int\n\tNext *Node\n}\n\n// NthFromEnd returns the n-th node from the end (1-based: n=1 is the last), or\n// nil if the list is shorter than n.\nfunc NthFromEnd(head *Node, n int) *Node {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "nthfromend.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/middle/ptrstack": {
    "id": "01-language-basics/05-pointers/middle/ptrstack",
    "title": "Stack Push/Pop",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Stack Push/Pop</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>A stack Push mutates the receiver's slice, so it needs a pointer receiver and</p>\n<p>must assign the append result back.</p>\n<h2>Task</h2>\n<p>Implement the <code>Push</code> method in ptrstack.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  s := &amp;Stack{}; s.Push(1); s.Push(2); s.Len()\nOutput: 2\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  s := &amp;Stack{}; s.Len()\nOutput: 0\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  s.Push(1); s.Pop(); s.Len()\nOutput: 0\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Pointer receiver</span><span class=\"tchip\">Append to field</span><span class=\"tchip\">Encapsulated state</span></div></details>",
    "education": "<h1>Mutating a struct's slice field</h1>\n<h2>Intuition</h2>\n<p>A pointer-receiver method appends to and reassigns the struct's slice field, keeping the growth visible to callers.</p>\n<h2>Approach</h2>\n<p>1. The receiver is <code>*Stack</code> so <code>data</code> grows persistently.</p>\n<p>2. <code>Push</code> appends; <code>Len</code> returns <code>len(s.data)</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\ntype Stack struct{ data []int }\n\nfunc (s *Stack) Len() int { return len(s.data) }\n\nfunc (s *Stack) Pop() (int, bool) {\n\tif len(s.data) == 0 {\n\t\treturn 0, false\n\t}\n\tv := s.data[len(s.data)-1]\n\ts.data = s.data[:len(s.data)-1]\n\treturn v, true\n}\n\nfunc (s *Stack) Push(v int) {\n\ts.data = append(s.data, v)\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>After two <code>Push</code> calls the backing slice holds two elements, so <code>Len()</code> returns 2. A pointer receiver keeps the appended header.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>A value receiver would append to a copy's field and lose it.</li>\n<li>Reassign the field with the append result.</li>\n</ul>",
    "starter": "// Package ptrstack \u2014 Gopher Workplace challenge.\npackage ptrstack\n\ntype Stack struct{ data []int }\n\nfunc (s *Stack) Len() int { return len(s.data) }\n\nfunc (s *Stack) Pop() (int, bool) {\n\tif len(s.data) == 0 {\n\t\treturn 0, false\n\t}\n\tv := s.data[len(s.data)-1]\n\ts.data = s.data[:len(s.data)-1]\n\treturn v, true\n}\n\n// Push pushes v onto the stack (pointer receiver so it mutates).\nfunc (s *Stack) Push(v int) {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "ptrstack.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/middle/pushfront": {
    "id": "01-language-basics/05-pointers/middle/pushfront",
    "title": "Prepend to List",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Prepend to List</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>Prepending to a singly linked list creates a new node whose Next is the old</p>\n<p>head, and returns it as the new head.</p>\n<h2>Task</h2>\n<p>Implement <code>PushFront</code> in pushfront.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  PushFront(1-&gt;2, 0)\nOutput: 0-&gt;1-&gt;2\n</code></pre>\n<p>_Explanation:_ A new node points at the old head, becoming the new head.</p>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  PushFront(nil, 5)\nOutput: 5\n</code></pre>\n<p>_Explanation:_ Prepending to an empty list yields a single node.</p>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  PushFront(9, 7)\nOutput: 7-&gt;9\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Node construction</span><span class=\"tchip\">New head returned</span><span class=\"tchip\">nil-safe</span></div></details>",
    "education": "<h1>Prepending to a linked list</h1>\n<h2>Intuition</h2>\n<p>A new node pointing at the current head becomes the new head \u2014 an O(1) push front.</p>\n<h2>Approach</h2>\n<p>1. Allocate a node whose <code>Next</code> is the current <code>head</code>.</p>\n<p>2. Return it as the new head.</p>\n<p>3. The old list is reused untouched.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\ntype Node struct {\n\tVal  int\n\tNext *Node\n}\n\nfunc PushFront(head *Node, v int) *Node {\n\treturn &amp;Node{Val: v, Next: head}\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>PushFront(1-&gt;2, 0)</code> builds <code>&amp;Node{Val:0, Next: head}</code>; that node links to <code>1-&gt;2</code> and is returned, giving <code>0-&gt;1-&gt;2</code>.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Return the new node; the old head is now second.</li>\n<li>Works with a nil head (empty list).</li>\n</ul>",
    "starter": "// Package pushfront \u2014 Gopher Workplace challenge.\npackage pushfront\n\ntype Node struct {\n\tVal  int\n\tNext *Node\n}\n\n// PushFront returns a new head with v prepended to the list.\nfunc PushFront(head *Node, v int) *Node {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "pushfront.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/middle/revlist": {
    "id": "01-language-basics/05-pointers/middle/revlist",
    "title": "Reverse a Linked List",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Reverse a Linked List</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>Reversing a list re-points each node's Next to its predecessor, tracking the</p>\n<p>previous node as you walk.</p>\n<h2>Task</h2>\n<p>Implement <code>Reverse</code> in revlist.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Reverse(1-&gt;2-&gt;3)\nOutput: 3-&gt;2-&gt;1\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Reverse(nil)\nOutput: nil\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Reverse(5)\nOutput: 5\n</code></pre>\n<p>_Explanation:_ A single node reverses to itself.</p>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Three pointers</span><span class=\"tchip\">Re-point Next</span><span class=\"tchip\">Advance</span></div></details>",
    "education": "<h1>Iterative list reversal</h1>\n<h2>Intuition</h2>\n<p>Walking the list while re-pointing each node's Next to the previous node reverses it in O(n) time and O(1) space.</p>\n<h2>Approach</h2>\n<p>1. Keep a <code>prev</code>, initially nil.</p>\n<p>2. For each node, save <code>next</code>, point <code>cur.Next</code> back to <code>prev</code>, advance both.</p>\n<p>3. When <code>cur</code> is nil, <code>prev</code> is the new head.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\ntype Node struct {\n\tVal  int\n\tNext *Node\n}\n\nfunc Reverse(head *Node) *Node {\n\tvar prev *Node\n\tcur := head\n\tfor cur != nil {\n\t\tnext := cur.Next\n\t\tcur.Next = prev\n\t\tprev = cur\n\t\tcur = next\n\t}\n\treturn prev\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>For <code>1-&gt;2-&gt;3</code>: node 1's Next flips to nil, node 2's to 1, node 3's to 2; the loop ends with <code>prev == node 3</code>, so the head is <code>3-&gt;2-&gt;1</code>.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Save <code>cur.Next</code> before overwriting it, or you lose the rest.</li>\n<li>The final <code>prev</code> is the new head.</li>\n</ul>",
    "starter": "// Package revlist \u2014 Gopher Workplace challenge.\npackage revlist\n\ntype Node struct {\n\tVal  int\n\tNext *Node\n}\n\n// Reverse reverses the singly linked list and returns the new head.\nfunc Reverse(head *Node) *Node {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "revlist.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/middle/rotatelist": {
    "id": "01-language-basics/05-pointers/middle/rotatelist",
    "title": "Rotate List Right",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Rotate List Right</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>Rotating right by k: form a ring, then break it k%len nodes before the end.</p>\n<p>Handle k &gt;= len with the modulo.</p>\n<h2>Task</h2>\n<p>Implement <code>Rotate</code> in rotatelist.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Rotate(1-&gt;2-&gt;3-&gt;4-&gt;5, 2)\nOutput: 4-&gt;5-&gt;1-&gt;2-&gt;3\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Rotate(1-&gt;2-&gt;3, 0)\nOutput: 1-&gt;2-&gt;3\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Rotate(1-&gt;2-&gt;3, 3)\nOutput: 1-&gt;2-&gt;3\n</code></pre>\n<p>_Explanation:_ k equal to the length is a no-op.</p>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Length and modulo</span><span class=\"tchip\">Close the ring</span><span class=\"tchip\">Break at the right spot</span></div></details>",
    "education": "<h1>Ring-based list rotation</h1>\n<h2>Intuition</h2>\n<p>Closing the list into a ring and breaking it at the right offset rotates in O(n); the modulo handles k larger than the length.</p>\n<h2>Approach</h2>\n<p>1. Find the length and link the tail to the head to form a ring.</p>\n<p>2. Walk <code>len - k%len</code> steps to the new tail.</p>\n<p>3. Break the ring there; the next node is the new head.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\ntype Node struct {\n\tVal  int\n\tNext *Node\n}\n\nfunc Rotate(head *Node, k int) *Node {\n\tif head == nil || head.Next == nil {\n\t\treturn head\n\t}\n\tn := 1\n\ttail := head\n\tfor tail.Next != nil {\n\t\ttail = tail.Next\n\t\tn++\n\t}\n\tk = k % n\n\tif k == 0 {\n\t\treturn head\n\t}\n\ttail.Next = head\n\tsteps := n - k\n\tnewTail := head\n\tfor i := 1; i &lt; steps; i++ {\n\t\tnewTail = newTail.Next\n\t}\n\tnewHead := newTail.Next\n\tnewTail.Next = nil\n\treturn newHead\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>Rotate(1..5, 2)</code>: form a ring, step <code>5-2=3</code> nodes to node 3 (new tail), break after it \u2192 head becomes node 4: <code>4-&gt;5-&gt;1-&gt;2-&gt;3</code>.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Reduce k with <code>k % len</code> (guard len 0).</li>\n<li>Breaking the ring one node early sets the new head.</li>\n</ul>",
    "starter": "// Package rotatelist \u2014 Gopher Workplace challenge.\npackage rotatelist\n\ntype Node struct {\n\tVal  int\n\tNext *Node\n}\n\n// Rotate rotates the list to the right by k places and returns the new head.\n// k may exceed the length.\nfunc Rotate(head *Node, k int) *Node {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "rotatelist.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/middle/setfield": {
    "id": "01-language-basics/05-pointers/middle/setfield",
    "title": "Update Through Fetched Pointer",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Update Through Fetched Pointer</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>Fetching a pointer from a map with comma-ok lets you mutate the pointee and</p>\n<p>report presence.</p>\n<h2>Task</h2>\n<p>Implement <code>SetName</code> in setfield.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  SetName(m, 1, \"new\")\nOutput: m[1].Name == \"new\", returns true\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  SetName(m, 99, \"x\")\nOutput: false\n</code></pre>\n<p>_Explanation:_ Missing key \u2192 no write, report failure.</p>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  SetName(m, 2, \"\")\nOutput: m[2].Name == \"\", true\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">comma-ok fetch</span><span class=\"tchip\">Mutate pointee</span><span class=\"tchip\">Report presence</span></div></details>",
    "education": "<h1>Fetch-then-mutate on pointer maps</h1>\n<h2>Intuition</h2>\n<p>The comma-ok read distinguishes absence; the fetched pointer allows in-place mutation of the stored object.</p>\n<h2>Approach</h2>\n<p>1. Use comma-ok: <code>u, ok := m[id]</code>.</p>\n<p>2. If <code>!ok</code>, return false without writing.</p>\n<p>3. Otherwise mutate through the pointer and return true.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\ntype User struct{ Name string }\n\nfunc SetName(m map[int]*User, id int, name string) bool {\n\tu, ok := m[id]\n\tif !ok {\n\t\treturn false\n\t}\n\tu.Name = name\n\treturn true\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>SetName(m, 1, \"new\")</code> finds the user, sets <code>u.Name</code>, returns true; an unknown id short-circuits to false.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Guard the missing key before dereferencing.</li>\n<li>The fetched pointer aliases the stored object.</li>\n</ul>",
    "starter": "// Package setfield \u2014 Gopher Workplace challenge.\npackage setfield\n\ntype User struct{ Name string }\n\n// SetName sets m[id].Name and returns true, or returns false if id is absent.\nfunc SetName(m map[int]*User, id int, name string) bool {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "setfield.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/middle/sumlist": {
    "id": "01-language-basics/05-pointers/middle/sumlist",
    "title": "Recursive List Sum",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Recursive List Sum</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>A recursive list walk bottoms out at nil (sum 0) and adds the current value to</p>\n<p>the sum of the rest.</p>\n<h2>Task</h2>\n<p>Implement <code>SumList</code> in sumlist.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  SumList(1-&gt;2-&gt;3-&gt;4)\nOutput: 10\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  SumList(nil)\nOutput: 0\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  SumList(5)\nOutput: 5\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Recursive base case</span><span class=\"tchip\">Recursive step</span><span class=\"tchip\">Pointer recursion</span></div></details>",
    "education": "<h1>Recursion over pointer structures</h1>\n<h2>Intuition</h2>\n<p>Linked lists and trees recurse naturally: base-case on nil, combine the current node with the recursive result.</p>\n<h2>Approach</h2>\n<p>1. Base case: a nil list sums to 0.</p>\n<p>2. Otherwise return <code>head.Val + SumList(head.Next)</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\ntype Node struct {\n\tVal  int\n\tNext *Node\n}\n\nfunc SumList(head *Node) int {\n\tif head == nil {\n\t\treturn 0\n\t}\n\treturn head.Val + SumList(head.Next)\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>SumList(1-&gt;2-&gt;3-&gt;4)</code> unfolds to <code>1 + (2 + (3 + (4 + 0)))</code> = <code>10</code>.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Base-case nil BEFORE dereferencing.</li>\n<li>Each call handles one node plus the rest.</li>\n</ul>",
    "starter": "// Package sumlist \u2014 Gopher Workplace challenge.\npackage sumlist\n\ntype Node struct {\n\tVal  int\n\tNext *Node\n}\n\n// SumList returns the sum of all node values, recursively. nil is 0.\nfunc SumList(head *Node) int {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "sumlist.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/middle/sumtree": {
    "id": "01-language-basics/05-pointers/middle/sumtree",
    "title": "Sum a Tree",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Sum a Tree</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>Summing a tree adds the node value to the sums of both subtrees.</p>\n<h2>Task</h2>\n<p>Implement <code>SumTree</code> in sumtree.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  SumTree(1 with children 2, 3)\nOutput: 6\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  SumTree(nil)\nOutput: 0\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  SumTree(single node 5)\nOutput: 5\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Recursive combine</span><span class=\"tchip\">nil base</span><span class=\"tchip\">Post-order shape</span></div></details>",
    "education": "<h1>Aggregating over a tree</h1>\n<h2>Intuition</h2>\n<p>A post-order recursion combines the current value with both subtree sums.</p>\n<h2>Approach</h2>\n<p>1. Nil \u2192 0.</p>\n<p>2. Return <code>t.Val + SumTree(left) + SumTree(right)</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\ntype Tree struct {\n\tVal         int\n\tLeft, Right *Tree\n}\n\nfunc SumTree(t *Tree) int {\n\tif t == nil {\n\t\treturn 0\n\t}\n\treturn t.Val + SumTree(t.Left) + SumTree(t.Right)\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>For root 1 with children 2 and 3: <code>1 + 2 + 3 = 6</code>.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Base-case nil returns the identity (0 for sums).</li>\n<li>Recurse both children.</li>\n</ul>",
    "starter": "// Package sumtree \u2014 Gopher Workplace challenge.\npackage sumtree\n\ntype Tree struct {\n\tVal         int\n\tLeft, Right *Tree\n}\n\n// SumTree returns the sum of all node values.\nfunc SumTree(t *Tree) int {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "sumtree.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/middle/swappairs": {
    "id": "01-language-basics/05-pointers/middle/swappairs",
    "title": "Swap First Two Nodes",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Swap First Two Nodes</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>Swapping the first two nodes rewires three pointers and returns the second node</p>\n<p>as the new head \u2014 a pointer-surgery exercise.</p>\n<h2>Task</h2>\n<p>Implement <code>SwapHead</code> in swappairs.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  SwapHead(1-&gt;2-&gt;3)\nOutput: 2-&gt;1-&gt;3\n</code></pre>\n<p>_Explanation:_ The first two nodes swap; the tail is untouched.</p>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  SwapHead(1)\nOutput: 1\n</code></pre>\n<p>_Explanation:_ Fewer than two nodes \u2192 unchanged.</p>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  SwapHead(nil)\nOutput: nil\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Guard short lists</span><span class=\"tchip\">Rewire pointers</span><span class=\"tchip\">New head</span></div></details>",
    "education": "<h1>Pointer surgery on adjacent nodes</h1>\n<h2>Intuition</h2>\n<p>Swapping nodes rewires their Next links and promotes the second node to head \u2014 order the assignments so no link is lost.</p>\n<h2>Approach</h2>\n<p>1. Guard lists with 0 or 1 node.</p>\n<p>2. Let <code>second = head.Next</code>; relink <code>head.Next = second.Next</code> and <code>second.Next = head</code>.</p>\n<p>3. Return <code>second</code> as the new head.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\ntype Node struct {\n\tVal  int\n\tNext *Node\n}\n\nfunc SwapHead(head *Node) *Node {\n\tif head == nil || head.Next == nil {\n\t\treturn head\n\t}\n\tsecond := head.Next\n\thead.Next = second.Next\n\tsecond.Next = head\n\treturn second\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>SwapHead(1-&gt;2-&gt;3)</code>: second is <code>2</code>; <code>1.Next</code> becomes <code>3</code>, <code>2.Next</code> becomes <code>1</code>, and <code>2</code> is returned \u2192 <code>2-&gt;1-&gt;3</code>.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Save <code>second.Next</code> before overwriting.</li>\n<li>Return the new head (the old second node).</li>\n</ul>",
    "starter": "// Package swappairs \u2014 Gopher Workplace challenge.\npackage swappairs\n\ntype Node struct {\n\tVal  int\n\tNext *Node\n}\n\n// SwapHead swaps the first two nodes and returns the new head. Lists shorter\n// than two are returned unchanged.\nfunc SwapHead(head *Node) *Node {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "swappairs.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/middle/treeheight": {
    "id": "01-language-basics/05-pointers/middle/treeheight",
    "title": "Tree Height",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Tree Height</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>Tree height is 1 plus the taller of the two subtrees; nil subtrees contribute 0.</p>\n<h2>Task</h2>\n<p>Implement <code>Height</code> in treeheight.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Height(nil)\nOutput: 0\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Height(single node)\nOutput: 1\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Height(balanced 3 levels)\nOutput: 3\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Recursive over children</span><span class=\"tchip\">Combine</span><span class=\"tchip\">nil base</span></div></details>",
    "education": "<h1>Recursion over binary trees</h1>\n<h2>Intuition</h2>\n<p>Tree algorithms recurse on Left and Right, base-casing on nil; height combines the deeper subtree.</p>\n<h2>Approach</h2>\n<p>1. Base: nil has height 0.</p>\n<p>2. Recurse into both children.</p>\n<p>3. Return <code>1 + max(left, right)</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\ntype Tree struct {\n\tVal         int\n\tLeft, Right *Tree\n}\n\nfunc Height(t *Tree) int {\n\tif t == nil {\n\t\treturn 0\n\t}\n\tl, r := Height(t.Left), Height(t.Right)\n\tif l &gt; r {\n\t\treturn 1 + l\n\t}\n\treturn 1 + r\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>For a balanced 3-level tree each side returns 2, so the root returns <code>1 + 2 = 3</code>.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Base-case nil before dereferencing.</li>\n<li>Height uses max of subtrees; size would use sum.</li>\n</ul>",
    "starter": "// Package treeheight \u2014 Gopher Workplace challenge.\npackage treeheight\n\ntype Tree struct {\n\tVal         int\n\tLeft, Right *Tree\n}\n\n// Height returns the height of the binary tree (nil = 0, single node = 1).\nfunc Height(t *Tree) int {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "treeheight.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/middle/vscale": {
    "id": "01-language-basics/05-pointers/middle/vscale",
    "title": "Value Receiver Returns Copy",
    "level": "middle",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Value Receiver Returns Copy</h1><div class=\"p-meta\"><span class=\"pill lv-middle\">middle</span></div></div><h2>Context</h2>\n<p>A value receiver is a copy; returning a new value (rather than mutating) is the</p>\n<p>idiomatic immutable style for small structs like points.</p>\n<h2>Task</h2>\n<p>Implement the <code>Scaled</code> method in vscale.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Point{1, 2}.Scaled(3)\nOutput: {3, 6}\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Point{0, 0}.Scaled(9)\nOutput: {0, 0}\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Point{-1, 2}.Scaled(2)\nOutput: {-2, 4}\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Value receiver</span><span class=\"tchip\">Return new value</span><span class=\"tchip\">Immutability</span></div></details>",
    "education": "<h1>Value receivers and immutability</h1>\n<h2>Intuition</h2>\n<p>A value-receiver method works on a copy; returning a new value keeps the original immutable \u2014 natural for small value types.</p>\n<h2>Approach</h2>\n<p>1. A value receiver is fine: we return a new <code>Point</code>, not mutate.</p>\n<p>2. Multiply each field by <code>k</code> and construct the result.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\ntype Point struct{ X, Y int }\n\nfunc (p Point) Scaled(k int) Point {\n\treturn Point{X: p.X * k, Y: p.Y * k}\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>Point{1,2}.Scaled(3)</code> returns <code>Point{3, 6}</code>; the original is unchanged because the receiver is a copy.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Mutating <code>p</code> inside a value receiver changes only the copy.</li>\n<li>Return a new struct to express a transform.</li>\n</ul>",
    "starter": "// Package vscale \u2014 Gopher Workplace challenge.\npackage vscale\n\ntype Point struct{ X, Y int }\n\n// Scaled returns a NEW Point scaled by k, leaving the receiver unchanged (value\n// receiver).\nfunc (p Point) Scaled(k int) Point {\n\t// TODO(candidate): implement this from scratch so all tests pass.\n\tpanic(\"not implemented\")\n}\n",
    "debug": "",
    "file": "vscale.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/senior/applyinplace": {
    "id": "01-language-basics/05-pointers/senior/applyinplace",
    "title": "Apply Discards the Result",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Apply Discards the Result</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p><code>f(*p)</code> computes the new value but throws it away. To update the caller's</p>\n<p>variable you must store the result back: <code><em>p = f(</em>p)</code>.</p>\n<h2>Task</h2>\n<p>Fix applyinplace.go so the transform is stored.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  x := 3; Apply(&amp;x, square)\nOutput: x == 9\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  x := 5; Apply(&amp;x, inc)\nOutput: x == 6\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  x := 4; Apply(&amp;x, identity)\nOutput: x == 4\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Store the result</span><span class=\"tchip\">Discarded computation</span><span class=\"tchip\">Write through the pointer</span></div></details>",
    "education": "<h1>Storing computed results through a pointer</h1>\n<h2>Intuition</h2>\n<p>Calling a pure function without assigning its result is a no-op; the transformed value must be written back through the pointer.</p>\n<h2>Approach</h2>\n<p>1. The bug calls <code>f(*p)</code> and drops the result.</p>\n<p>2. Store it: <code><em>p = f(</em>p)</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Apply(p *int, f func(int) int) {\n\t*p = f(*p)\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>f(<em>p)</code> computes the new value but never writes it, so <code>x</code> is unchanged. Assigning through <code></em>p</code> records the transformation.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>f(*p)</code> returns a value nobody keeps.</li>\n<li><code><em>p = f(</em>p)</code> updates the caller's variable.</li>\n</ul>",
    "starter": "// Package applyinplace transforms the int a pointer references. A planted bug\n// calls f but discards its result instead of storing it back through the pointer.\npackage applyinplace\n\n// Apply replaces *p with f(*p).\nfunc Apply(p *int, f func(int) int) {\n\t// CHANGE CODE BELOW THIS LINE\n\tf(*p)\n\t// CHANGE CODE ABOVE THIS LINE\n}\n",
    "debug": "",
    "file": "applyinplace.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/senior/clonesharetail": {
    "id": "01-language-basics/05-pointers/senior/clonesharetail",
    "title": "List Copy Shares the Tail",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">List Copy Shares the Tail</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p>Reusing <code>head.Next</code> shares the entire tail. A deep copy must recursively copy</p>\n<p>the rest of the list.</p>\n<h2>Task</h2>\n<p>Fix clonesharetail.go to deep-copy the tail.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  c := Copy(1-&gt;2-&gt;3); mutate c\nOutput: original unchanged\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Copy(nil)\nOutput: nil\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Copy(5)\nOutput: independent 5\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Shallow vs deep</span><span class=\"tchip\">Recursive copy</span><span class=\"tchip\">Independence</span></div></details>",
    "education": "<h1>Deep-copying a linked list</h1>\n<h2>Intuition</h2>\n<p>Copying only the head while reusing Next aliases the whole tail; recurse to allocate a fresh node per element.</p>\n<h2>Approach</h2>\n<p>1. <code>Next: head.Next</code> shares the rest of the list with the original.</p>\n<p>2. Recurse: <code>Next: Copy(head.Next)</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\ntype Node struct {\n\tVal  int\n\tNext *Node\n}\n\nfunc Copy(head *Node) *Node {\n\tif head == nil {\n\t\treturn nil\n\t}\n\treturn &amp;Node{Val: head.Val, Next: Copy(head.Next)}\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>The bug copies only the head and reuses the tail, so the lists alias from node 2 on. Recursively copying <code>Next</code> makes the copy independent.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>Next: head.Next</code> shares the rest of the list.</li>\n<li>Use <code>Next: Copy(head.Next)</code> for a deep copy.</li>\n</ul>",
    "starter": "// Package clonesharetail copies a list. A planted bug copies only the head node\n// and reuses the original tail, so mutating the copy's tail corrupts the source.\npackage clonesharetail\n\ntype Node struct {\n\tVal  int\n\tNext *Node\n}\n\n// Copy returns a deep copy of the list.\nfunc Copy(head *Node) *Node {\n\tif head == nil {\n\t\treturn nil\n\t}\n\t// CHANGE CODE BELOW THIS LINE\n\treturn &Node{Val: head.Val, Next: head.Next}\n\t// CHANGE CODE ABOVE THIS LINE\n}\n",
    "debug": "",
    "file": "clonesharetail.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/senior/cyclenoguard": {
    "id": "01-language-basics/05-pointers/senior/cyclenoguard",
    "title": "Fast Pointer Missing Guard",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Fast Pointer Missing Guard</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p>Advancing <code>fast = fast.Next.Next</code> requires BOTH <code>fast != nil</code> AND</p>\n<p><code>fast.Next != nil</code>; without the second guard, an acyclic list dereferences a nil</p>\n<p>fast.Next and panics.</p>\n<h2>Task</h2>\n<p>Fix the loop condition in cyclenoguard.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  HasCycle(acyclic list)\nOutput: false, no panic\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  HasCycle(cyclic list)\nOutput: true\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  HasCycle(nil)\nOutput: false\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Two-step guard</span><span class=\"tchip\">Nil dereference</span><span class=\"tchip\">Loop safety</span></div></details>",
    "education": "<h1>Guarding multi-step pointer advances</h1>\n<h2>Intuition</h2>\n<p>Moving two steps requires both the current and next pointers to be non-nil; a single guard leaves the second hop unsafe.</p>\n<h2>Approach</h2>\n<p>1. <code>fast = fast.Next.Next</code> needs both <code>fast</code> and <code>fast.Next</code> non-nil.</p>\n<p>2. The bug checks only <code>fast != nil</code>, so <code>fast.Next.Next</code> panics at the end of an odd list.</p>\n<p>3. Guard with <code>fast != nil &amp;&amp; fast.Next != nil</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\ntype Node struct {\n\tVal  int\n\tNext *Node\n}\n\nfunc HasCycle(head *Node) bool {\n\tslow, fast := head, head\n\tfor fast != nil &amp;&amp; fast.Next != nil {\n\t\tslow = slow.Next\n\t\tfast = fast.Next.Next\n\t\tif slow == fast {\n\t\t\treturn true\n\t\t}\n\t}\n\treturn false\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>On an acyclic odd-length list, <code>fast</code> reaches the last node where <code>fast.Next</code> is nil; the full guard stops the loop instead of dereferencing nil.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>fast.Next.Next</code> needs <code>fast.Next != nil</code> too.</li>\n<li>Check every dereference in a compound advance.</li>\n</ul>",
    "starter": "// Package cyclenoguard detects a cycle with slow/fast pointers. A planted bug\n// omits the fast.Next nil check, so an acyclic list panics on nil dereference.\npackage cyclenoguard\n\ntype Node struct {\n\tVal  int\n\tNext *Node\n}\n\n// HasCycle reports whether the list has a cycle.\nfunc HasCycle(head *Node) bool {\n\tslow, fast := head, head\n\t// CHANGE CODE BELOW THIS LINE\n\tfor fast != nil {\n\t\t// CHANGE CODE ABOVE THIS LINE\n\t\tslow = slow.Next\n\t\tfast = fast.Next.Next\n\t\tif slow == fast {\n\t\t\treturn true\n\t\t}\n\t}\n\treturn false\n}\n",
    "debug": "",
    "file": "cyclenoguard.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/senior/dblappend": {
    "id": "01-language-basics/05-pointers/senior/dblappend",
    "title": "Append via Slice Pointer",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Append via Slice Pointer</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p>Appending to a local copy <code>s</code> and discarding it leaves the caller's slice</p>\n<p>unchanged. Assign the append result back through the pointer: <code><em>sp = append(</em>sp, vs...)</code>.</p>\n<h2>Task</h2>\n<p>Fix dblappend.go so the caller's slice grows.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  var xs []int; Extend(&amp;xs, 1, 2, 3)\nOutput: xs == [1 2 3]\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  xs := []int{0}; Extend(&amp;xs, 1)\nOutput: xs == [0 1]\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Extend(&amp;xs)\nOutput: xs unchanged\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Assign through the pointer</span><span class=\"tchip\">Local copy lost</span><span class=\"tchip\">Slice header propagation</span></div></details>",
    "education": "<h1>Growing a slice via a slice pointer</h1>\n<h2>Intuition</h2>\n<p>Appending to a local dereference and dropping it leaves <code>*sp</code> stale; assign the result through the pointer.</p>\n<h2>Approach</h2>\n<p>1. <code>s := *sp; s = append(...)</code> updates a local header only.</p>\n<p>2. Write back through the pointer: <code><em>sp = append(</em>sp, vs...)</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Extend(sp *[]int, vs ...int) {\n\t*sp = append(*sp, vs...)\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>The bug appends to a local copy of the header and discards it, so the caller's slice stays empty. Assigning through <code>*sp</code> publishes the grown slice.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>s := *sp; s = append(...)</code> updates only the local.</li>\n<li><code><em>sp = append(</em>sp, vs...)</code> reaches the caller.</li>\n</ul>",
    "starter": "// Package dblappend appends through a *[]int. A planted bug appends to a local\n// dereference and discards it, so the caller's slice never grows.\npackage dblappend\n\n// Extend appends all of vs to the slice pointed to by sp.\nfunc Extend(sp *[]int, vs ...int) {\n\t// CHANGE CODE BELOW THIS LINE\n\ts := *sp\n\ts = append(s, vs...)\n\t_ = s\n\t// CHANGE CODE ABOVE THIS LINE\n}\n",
    "debug": "",
    "file": "dblappend.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/senior/delhead": {
    "id": "01-language-basics/05-pointers/senior/delhead",
    "title": "Delete Head Ignores New Head",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Delete Head Ignores New Head</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p>Removing the head means returning <code>head.Next</code> as the new head. Returning the</p>\n<p>original <code>head</code> keeps the deleted node.</p>\n<h2>Task</h2>\n<p>Fix delhead.go so the first node is actually removed.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  RemoveFirst(1-&gt;2-&gt;3)\nOutput: 2-&gt;3\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  RemoveFirst(1)\nOutput: nil\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  RemoveFirst(1-&gt;2)\nOutput: 2\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">New head on head-delete</span><span class=\"tchip\">Caller reassigns</span><span class=\"tchip\">Single node</span></div></details>",
    "education": "<h1>Returning the new head</h1>\n<h2>Intuition</h2>\n<p>Deleting the head has no predecessor to relink, so the function must return the successor for the caller to adopt.</p>\n<h2>Approach</h2>\n<p>1. Removing the first node means the new head is <code>head.Next</code>.</p>\n<p>2. The bug returns <code>head</code> (the node meant to be removed).</p>\n<p>3. Return <code>head.Next</code> instead.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\ntype Node struct {\n\tVal  int\n\tNext *Node\n}\n\nfunc RemoveFirst(head *Node) *Node {\n\tif head == nil {\n\t\treturn nil\n\t}\n\treturn head.Next\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>RemoveFirst(1-&gt;2-&gt;3)</code> should drop node 1 and return node 2. Returning <code>head</code> keeps node 1; <code>head.Next</code> gives <code>2-&gt;3</code>.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Returning the original <code>head</code> keeps the deleted node; RETURN head.Next.</li>\n<li>The caller must use the returned head.</li>\n</ul>",
    "starter": "// Package delhead removes the first node. A planted bug always returns the\n// original head, so deleting the head node doesn't actually drop it.\npackage delhead\n\ntype Node struct {\n\tVal  int\n\tNext *Node\n}\n\n// RemoveFirst removes the first node and returns the new head.\nfunc RemoveFirst(head *Node) *Node {\n\tif head == nil {\n\t\treturn nil\n\t}\n\t// CHANGE CODE BELOW THIS LINE\n\treturn head\n\t// CHANGE CODE ABOVE THIS LINE\n}\n",
    "debug": "",
    "file": "delhead.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/senior/derefearly": {
    "id": "01-language-basics/05-pointers/senior/derefearly",
    "title": "Dereference Before Nil Check",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Dereference Before Nil Check</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p>The nil check must come BEFORE any dereference. Reading <code>head.Val</code> first panics</p>\n<p>when head is nil, no matter what the later check does.</p>\n<h2>Task</h2>\n<p>Fix derefearly.go so a nil head returns the default without panicking.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  FirstOr(nil, 7)\nOutput: 7\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  FirstOr(&amp;Node{Val: 3}, 7)\nOutput: 3\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  FirstOr(&amp;Node{Val: 0}, 7)\nOutput: 0\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Guard before deref</span><span class=\"tchip\">Order of operations</span><span class=\"tchip\">Early return</span></div></details>",
    "education": "<h1>Nil checks precede dereferences</h1>\n<h2>Intuition</h2>\n<p>A dereference evaluates immediately; guarding after it is too late. The nil test must run before touching the pointee.</p>\n<h2>Approach</h2>\n<p>1. The bug reads <code>head.Val</code> <strong>before</strong> the nil check, panicking on nil.</p>\n<p>2. Move the nil check first, then dereference.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\ntype Node struct {\n\tVal  int\n\tNext *Node\n}\n\nfunc FirstOr(head *Node, def int) int {\n\tif head == nil {\n\t\treturn def\n\t}\n\treturn head.Val\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>FirstOr(nil, 7)</code> should return 7, but reading <code>head.Val</code> up front dereferences nil and panics. Checking <code>head == nil</code> first is safe.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>head.Val</code> panics on nil regardless of a later check.</li>\n<li>Put <code>if head == nil</code> first.</li>\n</ul>",
    "starter": "// Package derefearly reads a node's value safely. A planted bug reads head.Val\n// before checking head for nil, panicking on an empty list.\npackage derefearly\n\ntype Node struct {\n\tVal  int\n\tNext *Node\n}\n\n// FirstOr returns head.Val, or def when head is nil.\nfunc FirstOr(head *Node, def int) int {\n\t// CHANGE CODE BELOW THIS LINE\n\tv := head.Val\n\tif head == nil {\n\t\treturn def\n\t}\n\treturn v\n\t// CHANGE CODE ABOVE THIS LINE\n}\n",
    "debug": "",
    "file": "derefearly.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/senior/elemstale": {
    "id": "01-language-basics/05-pointers/senior/elemstale",
    "title": "Element Pointer After Append",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Element Pointer After Append</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p>The append reallocates (len == cap), so <code>p</code> (taken before) points at the OLD</p>\n<p>array. Writing through it doesn't touch the returned slice; re-take the address</p>\n<p>or index directly after the append.</p>\n<h2>Task</h2>\n<p>Fix elemstale.go so the first element of the result is 42.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  FirstOf([1 2])\nOutput: [42 2 0]\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  result length\nOutput: 3\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  s[0] after call\nOutput: 42\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Reallocation on append</span><span class=\"tchip\">Stale element pointer</span><span class=\"tchip\">Re-address</span></div></details>",
    "education": "<h1>Element pointers invalidated by append</h1>\n<h2>Intuition</h2>\n<p>A reallocating append detaches pointers taken beforehand; use the current slice's index or re-take the address afterward.</p>\n<h2>Approach</h2>\n<p>1. After the slice header is reassigned/reallocated, an old element pointer <code>p</code> is stale.</p>\n<p>2. Re-take <code>p = &amp;s[0]</code> against the current backing array before writing.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc FirstOf(xs []int) []int {\n\ts := make([]int, len(xs), len(xs)) // len == cap, so append reallocates\n\tcopy(s, xs)\n\tp := &amp;s[0]\n\ts = append(s, 0)\n\tp = &amp;s[0]\n\t*p = 42\n\treturn s\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>Writing through the stale <code>p</code> misses the live element; re-deriving <code>p</code> from <code>s[0]</code> targets the current storage so <code>*p = 42</code> lands.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>After a reallocating append, <code>&amp;s[0]</code> from before is stale.</li>\n<li>Index the current slice (<code>s[0]</code>) or re-take the pointer.</li>\n</ul>",
    "starter": "// Package elemstale returns a pointer into a slice, then appends. A planted bug\n// keeps the pointer taken before a reallocating append, so writes through it\n// don't affect the returned slice.\npackage elemstale\n\n// FirstOf returns a slice built from xs plus a trailing 0, with its first\n// element set to 42 through a pointer.\nfunc FirstOf(xs []int) []int {\n\ts := make([]int, len(xs), len(xs)) // len == cap, so append reallocates\n\tcopy(s, xs)\n\tp := &s[0]\n\ts = append(s, 0)\n\t// CHANGE CODE BELOW THIS LINE\n\t*p = 42\n\t// CHANGE CODE ABOVE THIS LINE\n\treturn s\n}\n",
    "debug": "",
    "file": "elemstale.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/senior/eqconfuse": {
    "id": "01-language-basics/05-pointers/senior/eqconfuse",
    "title": "Identity vs Value Confusion",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Identity vs Value Confusion</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p><code><em>a == </em>b</code> compares the VALUES, which can be equal for different variables.</p>\n<p>Identity is <code>a == b</code> \u2014 comparing the addresses.</p>\n<h2>Task</h2>\n<p>Fix eqconfuse.go to compare identity.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  x := 1; Same(&amp;x, &amp;x)\nOutput: true\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  x, y := 1, 1; Same(&amp;x, &amp;y)\nOutput: false\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  p := &amp;x; Same(p, &amp;x)\nOutput: true\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Identity vs value</span><span class=\"tchip\">Distinct storage</span><span class=\"tchip\">Pointer comparison</span></div></details>",
    "education": "<h1>Pointer identity vs pointed-to equality</h1>\n<h2>Intuition</h2>\n<p><code>a == b</code> tests whether two pointers share an address; <code><em>a == </em>b</code> tests whether their values match \u2014 different questions.</p>\n<h2>Approach</h2>\n<p>1. Identity means same <strong>address</strong>, not equal values.</p>\n<p>2. The bug compares <code><em>a == </em>b</code> (values). Compare the pointers: <code>a == b</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Same(a, b *int) bool {\n\treturn a == b\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>Same(&amp;x, &amp;y)</code> with equal values returns true under the bug (<code><em>a == </em>b</code>), but they are distinct variables. Comparing <code>a == b</code> reports identity correctly.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code><em>a == </em>b</code> is true for two different vars holding 5.</li>\n<li>Use <code>a == b</code> for identity.</li>\n</ul>",
    "starter": "// Package eqconfuse checks whether two pointers refer to the same variable. A\n// planted bug compares the pointed-to VALUES, so two distinct variables holding\n// equal values are wrongly reported as the same.\npackage eqconfuse\n\n// Same reports whether a and b point to the SAME int (same address).\nfunc Same(a, b *int) bool {\n\t// CHANGE CODE BELOW THIS LINE\n\treturn *a == *b\n\t// CHANGE CODE ABOVE THIS LINE\n}\n",
    "debug": "",
    "file": "eqconfuse.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/senior/hoistedaddr": {
    "id": "01-language-basics/05-pointers/senior/hoistedaddr",
    "title": "Address of a Hoisted Loop Var",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Address of a Hoisted Loop Var</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p><code>v</code> is declared ONCE outside the loop, so <code>&amp;v</code> is the same address every</p>\n<p>iteration; all pointers see the final value. Take the address of a fresh</p>\n<p>per-iteration variable instead.</p>\n<h2>Task</h2>\n<p>Fix hoistedaddr.go so each pointer holds a distinct value.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Pointers([10 20 30])\nOutput: pointers to 10, 20, 30\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  deref each result\nOutput: 10, 20, 30 (distinct)\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Pointers([])\nOutput: empty\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Hoisted variable address</span><span class=\"tchip\">Per-iteration copy</span><span class=\"tchip\">Distinct pointers</span></div></details>",
    "education": "<h1>Addresses of reused variables</h1>\n<h2>Intuition</h2>\n<p>A variable declared outside the loop has one address; taking <code>&amp;v</code> each iteration stores the same pointer. A per-iteration declaration gives distinct addresses.</p>\n<h2>Approach</h2>\n<p>1. A single hoisted <code>var v</code> is reused every iteration, so all pointers alias it.</p>\n<p>2. Declare <code>v := xs[i]</code> <strong>inside</strong> the loop to get fresh storage each time.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Pointers(xs []int) []*int {\n\tvar out []*int\n\tfor i := 0; i &lt; len(xs); i++ {\n\t\tv := xs[i]\n\t\tout = append(out, &amp;v)\n\t}\n\treturn out\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>With the shared <code>v</code>, every <code>&amp;v</code> points at the same cell holding the last value. A per-iteration <code>v</code> gives each pointer its own element value.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>&amp;v</code> of a hoisted <code>v</code> aliases one storage cell.</li>\n<li>Declare the variable inside the loop for distinct addresses.</li>\n</ul>",
    "starter": "// Package hoistedaddr collects pointers to each value. A planted bug takes the\n// address of a single hoisted variable reused across iterations, so every\n// pointer ends up aliasing the same (final) value.\npackage hoistedaddr\n\n// Pointers returns a pointer to a copy of each element. Each pointer must hold a\n// DISTINCT value.\nfunc Pointers(xs []int) []*int {\n\tvar out []*int\n\tvar v int\n\tfor i := 0; i < len(xs); i++ {\n\t\tv = xs[i]\n\t\t// CHANGE CODE BELOW THIS LINE\n\t\tout = append(out, &v)\n\t\t// CHANGE CODE ABOVE THIS LINE\n\t}\n\treturn out\n}\n",
    "debug": "",
    "file": "hoistedaddr.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/senior/insertnoreattach": {
    "id": "01-language-basics/05-pointers/senior/insertnoreattach",
    "title": "Insert Result Not Reattached",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Insert Result Not Reattached</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p>When a subtree is nil, <code>Insert</code> returns a NEW node that the parent must reattach</p>\n<p>via <code>root.Left = Insert(root.Left, v)</code>. Discarding the return drops the new node.</p>\n<h2>Task</h2>\n<p>Fix insertnoreattach.go so left-subtree inserts persist.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Insert(nil, 5)\nOutput: tree rooted at 5\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Insert(root(5), 3)\nOutput: 3 attached as left child\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Insert(root(5), 8)\nOutput: 8 attached as right child\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Reattach the subtree</span><span class=\"tchip\">New nodes returned</span><span class=\"tchip\">Symmetry</span></div></details>",
    "education": "<h1>Reattaching returned subtrees</h1>\n<h2>Intuition</h2>\n<p>Recursive tree mutators return the (possibly new) subtree; the parent must reassign it, or newly created nodes are lost.</p>\n<h2>Approach</h2>\n<p>1. <code>Insert</code> returns the (possibly new) subtree root.</p>\n<p>2. The bug discards that return, so a new leaf is never linked in.</p>\n<p>3. Reassign: <code>root.Left = Insert(root.Left, v)</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\ntype Tree struct {\n\tVal         int\n\tLeft, Right *Tree\n}\n\nfunc Insert(root *Tree, v int) *Tree {\n\tif root == nil {\n\t\treturn &amp;Tree{Val: v}\n\t}\n\tif v &lt; root.Val {\n\t\troot.Left = Insert(root.Left, v)\n\t} else {\n\t\troot.Right = Insert(root.Right, v)\n\t}\n\treturn root\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>Inserting into an empty child, <code>Insert(root.Left, v)</code> builds a node but throws it away. Assigning the result back attaches it to <code>root.Left</code>.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>Insert(root.Left, v)</code> alone drops the result for empty subtrees.</li>\n<li>Always write <code>root.Left = Insert(root.Left, v)</code>.</li>\n</ul>",
    "starter": "// Package insertnoreattach inserts into a BST. A planted bug calls Insert on a\n// child but discards the returned subtree, so inserts into empty subtrees are\n// lost.\npackage insertnoreattach\n\ntype Tree struct {\n\tVal         int\n\tLeft, Right *Tree\n}\n\n// Insert adds v into the BST and returns the (possibly new) root.\nfunc Insert(root *Tree, v int) *Tree {\n\tif root == nil {\n\t\treturn &Tree{Val: v}\n\t}\n\tif v < root.Val {\n\t\t// CHANGE CODE BELOW THIS LINE\n\t\tInsert(root.Left, v)\n\t\t// CHANGE CODE ABOVE THIS LINE\n\t} else {\n\t\troot.Right = Insert(root.Right, v)\n\t}\n\treturn root\n}\n",
    "debug": "",
    "file": "insertnoreattach.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/senior/insertoverwrite": {
    "id": "01-language-basics/05-pointers/senior/insertoverwrite",
    "title": "Insert Overwrites the Root",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Insert Overwrites the Root</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p>Reassigning <code>root = &amp;Tree{Val: v}</code> throws away the existing tree each call. A</p>\n<p>real insert descends to a nil slot and creates a node there, leaving the rest</p>\n<p>intact.</p>\n<h2>Task</h2>\n<p>Fix insertoverwrite.go to insert without destroying the tree.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Insert 5, then 3, then 8\nOutput: 3-node BST\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Insert(nil, 5)\nOutput: root 5\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Insert(root(5), 3)\nOutput: 3 as left child\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Preserve the tree</span><span class=\"tchip\">Recursive descent</span><span class=\"tchip\">Return the root</span></div></details>",
    "education": "<h1>Non-destructive BST insertion</h1>\n<h2>Intuition</h2>\n<p>Insertion must recurse to a nil slot and add a node there; overwriting the root each call collapses the tree to one node.</p>\n<h2>Approach</h2>\n<p>1. The bug replaces the whole tree with a new root on every call.</p>\n<p>2. Recurse into the correct subtree and only allocate on nil: <code>if root == nil { return &amp;Tree{Val: v} }</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\ntype Tree struct {\n\tVal         int\n\tLeft, Right *Tree\n}\n\nfunc Insert(root *Tree, v int) *Tree {\n\tif root == nil {\n\t\treturn &amp;Tree{Val: v}\n\t}\n\tif v &lt; root.Val {\n\t\troot.Left = Insert(root.Left, v)\n\t} else {\n\t\troot.Right = Insert(root.Right, v)\n\t}\n\treturn root\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>root = &amp;Tree{Val: v}</code> discards the existing tree, so only the last insert survives. Recursing left/right by BST order preserves prior nodes.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>root = &amp;Tree{Val: v}</code> discards everything already inserted.</li>\n<li>Recurse to the correct empty child instead.</li>\n</ul>",
    "starter": "// Package insertoverwrite inserts into a BST. A planted bug reassigns root to the\n// new node on every call, discarding the existing tree.\npackage insertoverwrite\n\ntype Tree struct {\n\tVal         int\n\tLeft, Right *Tree\n}\n\n// Insert adds v into the BST and returns the root.\nfunc Insert(root *Tree, v int) *Tree {\n\t// CHANGE CODE BELOW THIS LINE\n\troot = &Tree{Val: v}\n\treturn root\n\t// CHANGE CODE ABOVE THIS LINE\n}\n",
    "debug": "",
    "file": "insertoverwrite.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/senior/lenoffbyone": {
    "id": "01-language-basics/05-pointers/senior/lenoffbyone",
    "title": "List Length Counts a Phantom",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">List Length Counts a Phantom</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p>The counter must start at 0; the loop already visits every node. Starting at 1</p>\n<p>counts a node that isn't there (and makes nil report 1).</p>\n<h2>Task</h2>\n<p>Fix the counter initialisation in lenoffbyone.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Length(nil)\nOutput: 0\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Length(1-&gt;2-&gt;3)\nOutput: 3\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Length(5)\nOutput: 1\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Counter start</span><span class=\"tchip\">Loop visits all</span><span class=\"tchip\">Empty case</span></div></details>",
    "education": "<h1>Initialising accumulators</h1>\n<h2>Intuition</h2>\n<p>The loop counts every node, so the counter must start at 0; a start of 1 double-counts and breaks the empty case.</p>\n<h2>Approach</h2>\n<p>1. The counter must start at 0; the bug seeds it at 1.</p>\n<p>2. <code>count := 0</code> then increment per node.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\ntype Node struct {\n\tVal  int\n\tNext *Node\n}\n\nfunc Length(head *Node) int {\n\tcount := 0\n\tfor n := head; n != nil; n = n.Next {\n\t\tcount++\n\t}\n\treturn count\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>Seeding <code>count := 1</code> overcounts by one and returns 1 for an empty list. Starting at 0 gives the true length.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>count := 1</code> makes an empty list report length 1.</li>\n<li>Let the loop do all the counting from 0.</li>\n</ul>",
    "starter": "// Package lenoffbyone counts nodes. A planted bug starts the counter at 1 even\n// for an empty list, over-counting by one.\npackage lenoffbyone\n\ntype Node struct {\n\tVal  int\n\tNext *Node\n}\n\n// Length returns the number of nodes. nil is 0.\nfunc Length(head *Node) int {\n\t// CHANGE CODE BELOW THIS LINE\n\tcount := 1\n\t// CHANGE CODE ABOVE THIS LINE\n\tfor n := head; n != nil; n = n.Next {\n\t\tcount++\n\t}\n\treturn count\n}\n",
    "debug": "",
    "file": "lenoffbyone.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/senior/mapptrupdate": {
    "id": "01-language-basics/05-pointers/senior/mapptrupdate",
    "title": "Update Map Struct via Pointer",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Update Map Struct via Pointer</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p><code>acc := *a</code> copies the account; mutating <code>acc</code> changes the copy, not the stored</p>\n<p>object. Mutate through the pointer: <code>a.Balance += amt</code>.</p>\n<h2>Task</h2>\n<p>Fix mapptrupdate.go so the stored account is credited.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Credit(m, 1, 50)\nOutput: m[1].Balance += 50\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  m[1].Balance == 100; Credit(m, 1, 50)\nOutput: 150\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Credit(m, 1, 0)\nOutput: unchanged\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Copy vs pointer mutation</span><span class=\"tchip\">Mutate through the pointer</span><span class=\"tchip\">Stored object</span></div></details>",
    "education": "<h1>Mutating pointees fetched from maps</h1>\n<h2>Intuition</h2>\n<p>Dereferencing into a local copies the struct; to change the stored object, write through the fetched pointer.</p>\n<h2>Approach</h2>\n<p>1. <code>acc := *a</code> copies the struct; mutating <code>acc</code> doesn't touch the map's value.</p>\n<p>2. Mutate through the pointer: <code>a.Balance += amt</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\ntype Account struct{ Balance int }\n\nfunc Credit(m map[int]*Account, id, amt int) bool {\n\ta, ok := m[id]\n\tif !ok {\n\t\treturn false\n\t}\n\ta.Balance += amt\n\treturn true\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>The bug adds to a local copy that is thrown away. Writing <code>a.Balance += amt</code> updates the account the map points at.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>acc := *a; acc.Balance += amt</code> edits a throwaway copy.</li>\n<li><code>a.Balance += amt</code> edits the stored account.</li>\n</ul>",
    "starter": "// Package mapptrupdate stores accounts in a map and credits one. A planted bug\n// fetches a COPY of the pointer's pointee, mutates the copy, and stores nothing,\n// so the update is lost. Mutate through the fetched pointer instead.\npackage mapptrupdate\n\ntype Account struct{ Balance int }\n\n// Credit adds amt to the account stored at id. Returns false if id is absent.\nfunc Credit(m map[int]*Account, id, amt int) bool {\n\ta, ok := m[id]\n\tif !ok {\n\t\treturn false\n\t}\n\t// CHANGE CODE BELOW THIS LINE\n\tacc := *a\n\tacc.Balance += amt\n\t// CHANGE CODE ABOVE THIS LINE\n\treturn true\n}\n",
    "debug": "",
    "file": "mapptrupdate.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/senior/mergelost": {
    "id": "01-language-basics/05-pointers/senior/mergelost",
    "title": "Merge Drops the Remainder",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Merge Drops the Remainder</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p>When one list is exhausted, the other may still have nodes. The merge must link</p>\n<p>the remaining list to the tail, or those nodes are lost.</p>\n<h2>Task</h2>\n<p>Fix mergelost.go by attaching the leftover list.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Merge(1-&gt;2, 3-&gt;4-&gt;5)\nOutput: 1-&gt;2-&gt;3-&gt;4-&gt;5\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Merge(nil, 3-&gt;4)\nOutput: 3-&gt;4\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Merge(1-&gt;2, nil)\nOutput: 1-&gt;2\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Leftover handling</span><span class=\"tchip\">Attach remainder</span><span class=\"tchip\">Loop exit</span></div></details>",
    "education": "<h1>Handling leftovers in a merge</h1>\n<h2>Intuition</h2>\n<p>The merge loop stops when either list empties; the non-empty remainder must be linked to the tail to avoid dropping nodes.</p>\n<h2>Approach</h2>\n<p>1. After the main loop one list still has nodes.</p>\n<p>2. The bug drops that remainder; attach whichever of <code>a</code>/<code>b</code> is non-nil to <code>tail.Next</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\ntype Node struct {\n\tVal  int\n\tNext *Node\n}\n\nfunc Merge(a, b *Node) *Node {\n\tdummy := &amp;Node{}\n\ttail := dummy\n\tfor a != nil &amp;&amp; b != nil {\n\t\tif a.Val &lt;= b.Val {\n\t\t\ttail.Next = a\n\t\t\ta = a.Next\n\t\t} else {\n\t\t\ttail.Next = b\n\t\t\tb = b.Next\n\t\t}\n\t\ttail = tail.Next\n\t}\n\tif a != nil {\n\t\ttail.Next = a\n\t} else {\n\t\ttail.Next = b\n\t}\n\treturn dummy.Next\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>When <code>a</code> runs out with <code>3-&gt;4-&gt;5</code> left in <code>b</code>, the tail must link to <code>b</code>. Omitting this truncates the merged list.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>The loop condition <code>a != nil &amp;&amp; b != nil</code> leaves one list non-empty.</li>\n<li>Link the leftover directly; it's already sorted.</li>\n</ul>",
    "starter": "// Package mergelost merges two sorted lists. A planted bug forgets to attach the\n// leftover of the non-empty list after one runs out, truncating the result.\npackage mergelost\n\ntype Node struct {\n\tVal  int\n\tNext *Node\n}\n\n// Merge merges two sorted lists and returns the head.\nfunc Merge(a, b *Node) *Node {\n\tdummy := &Node{}\n\ttail := dummy\n\tfor a != nil && b != nil {\n\t\tif a.Val <= b.Val {\n\t\t\ttail.Next = a\n\t\t\ta = a.Next\n\t\t} else {\n\t\t\ttail.Next = b\n\t\t\tb = b.Next\n\t\t}\n\t\ttail = tail.Next\n\t}\n\t// CHANGE CODE BELOW THIS LINE\n\n\t// CHANGE CODE ABOVE THIS LINE\n\treturn dummy.Next\n}\n",
    "debug": "",
    "file": "mergelost.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/senior/methodcopy": {
    "id": "01-language-basics/05-pointers/senior/methodcopy",
    "title": "Method Value Captures a Copy",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Method Value Captures a Copy</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p><code>(*a).Get</code> dereferences to a value first, binding the method value to a COPY of</p>\n<p>the account; later changes to <code>*a</code> aren't seen. Bind on the pointer (<code>a.Get</code>) to</p>\n<p>capture the live object.</p>\n<h2>Task</h2>\n<p>Fix methodcopy.go so the getter reflects later balance changes.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  get := Getter(a); a.Balance = 250; get()\nOutput: 250\n</code></pre>\n<p>_Explanation:_ The closure must observe later mutations.</p>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  a := &amp;Account{Balance: 100}; Getter(a)()\nOutput: 100\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  get := Getter(a); a.Balance = 0; get()\nOutput: 0\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Method value binding</span><span class=\"tchip\">Pointer binding</span><span class=\"tchip\">Live vs snapshot</span></div></details>",
    "education": "<h1>Binding method values on pointers vs values</h1>\n<h2>Intuition</h2>\n<p><code>a.Get</code> (pointer) binds the live object; <code>(*a).Get</code> binds a copy of the value, freezing its state at bind time.</p>\n<h2>Approach</h2>\n<p>1. <code>tmp := *a</code> copies the struct, so <code>tmp.Get</code> is bound to a stale snapshot.</p>\n<p>2. Bind the method value to the live pointer: <code>return a.Get</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\ntype Account struct{ Balance int }\n\nfunc (a *Account) Get() int { return a.Balance }\n\nfunc Getter(a *Account) func() int {\n\treturn a.Get\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>The bug's <code>tmp.Get</code> captures a copy taken at bind time; later writes to <code>*a</code> are invisible. Binding <code>a.Get</code> keeps the pointer, so <code>get()</code> sees <code>250</code>.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>(*a).Get</code> snapshots the value; <code>a.Get</code> tracks the pointer.</li>\n<li>Prefer binding the method value on the pointer for live views.</li>\n</ul>",
    "starter": "// Package methodcopy returns a getter bound to an account. A planted bug binds\n// the method value on a COPY (dereferenced value), so later deposits are not\n// seen. Binding on the pointer captures the live object.\npackage methodcopy\n\ntype Account struct{ Balance int }\n\nfunc (a *Account) Get() int { return a.Balance }\n\n// Getter returns a function that reports a's CURRENT balance, reflecting later\n// deposits.\nfunc Getter(a *Account) func() int {\n\t// CHANGE CODE BELOW THIS LINE\n\ttmp := *a\n\treturn tmp.Get\n\t// CHANGE CODE ABOVE THIS LINE\n}\n",
    "debug": "",
    "file": "methodcopy.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/senior/minseed": {
    "id": "01-language-basics/05-pointers/senior/minseed",
    "title": "Min Pointer Bad Seed",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Min Pointer Bad Seed</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p>Seeding min with the LAST element breaks the earliest-tie rule: when the minimum</p>\n<p>appears earlier, the strict <code>&lt;</code> comparison won't replace an equal last element.</p>\n<p>Seed from the first element instead.</p>\n<h2>Task</h2>\n<p>Fix the seed in minseed.go so ties return the earliest minimum.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  MinPtr([3 1 4 1 5])\nOutput: &amp;xs[1]\n</code></pre>\n<p>_Explanation:_ First index of the minimum.</p>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  MinPtr([5])\nOutput: &amp;xs[0]\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  MinPtr([2 2 2])\nOutput: &amp;xs[0]\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Seed from the front</span><span class=\"tchip\">Strict comparison + tie rule</span><span class=\"tchip\">Consistent scan</span></div></details>",
    "education": "<h1>Seeding a running extremum</h1>\n<h2>Intuition</h2>\n<p>Seeding from the last element with a strict <code>&lt;</code> comparison can return a later tie; seed from the first element so the earliest minimum wins.</p>\n<h2>Approach</h2>\n<p>1. Seed the running min at <code>&amp;xs[0]</code>, then scan.</p>\n<p>2. The bug seeds at the last element, biasing ties and starting the scan wrong.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc MinPtr(xs []int) *int {\n\tmin := &amp;xs[0]\n\tfor i := range xs {\n\t\tif xs[i] &lt; *min {\n\t\t\tmin = &amp;xs[i]\n\t\t}\n\t}\n\treturn min\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>Seeding at <code>&amp;xs[len-1]</code> makes the last element the tentative min; a leading true minimum is still found by value, but the seed should be <code>&amp;xs[0]</code> for a correct left-most result.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Seeding <code>&amp;xs[len-1]</code> with <code>&lt;</code> returns the last min on ties.</li>\n<li>Seed <code>&amp;xs[0]</code> to honour the earliest-tie rule.</li>\n</ul>",
    "starter": "// Package minseed returns a pointer to the smallest element. A planted bug seeds\n// the min with the last element's address but compares from index 0, or rather\n// seeds with a nil/zero assumption; here it starts min at &xs[0] but the loop\n// skips index 0 incorrectly by starting comparisons that overwrite wrongly. The\n// concrete bug: it seeds min = &xs[len-1] instead of &xs[0].\npackage minseed\n\n// MinPtr returns a pointer to the smallest element of xs (xs non-empty). On a\n// tie it returns the earliest.\nfunc MinPtr(xs []int) *int {\n\t// CHANGE CODE BELOW THIS LINE\n\tmin := &xs[len(xs)-1]\n\t// CHANGE CODE ABOVE THIS LINE\n\tfor i := range xs {\n\t\tif xs[i] < *min {\n\t\t\tmin = &xs[i]\n\t\t}\n\t}\n\treturn min\n}\n",
    "debug": "",
    "file": "minseed.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/senior/nestptr": {
    "id": "01-language-basics/05-pointers/senior/nestptr",
    "title": "Nested Pointer Map Init",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Nested Pointer Map Init</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p>The inner map <code>m[group]</code> is nil until created; writing <code>m[group][key]</code> panics.</p>\n<p>Lazily initialise the inner map before the write.</p>\n<h2>Task</h2>\n<p>Fix nestptr.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Add(m, \"g\", \"a\"); Add(m, \"g\", \"a\")\nOutput: m[\"g\"][\"a\"] == 2\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Add(m, \"g\", \"b\")\nOutput: m[\"g\"][\"b\"] == 1\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  first Add on empty map\nOutput: no panic\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Nested maps</span><span class=\"tchip\">Lazy init</span><span class=\"tchip\">Nil map write</span></div></details>",
    "education": "<h1>Initialising nested maps</h1>\n<h2>Intuition</h2>\n<p>Only the outer map exists after construction; each inner map is nil until assigned, and writing to nil panics.</p>\n<h2>Approach</h2>\n<p>1. Writing <code>m[group][key]++</code> on a missing inner map assigns to a nil map \u2192 panic.</p>\n<p>2. Lazily initialize: <code>if m[group] == nil { m[group] = map[string]int{} }</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Add(m map[string]map[string]int, group, key string) {\n\tif m[group] == nil {\n\t\tm[group] = map[string]int{}\n\t}\n\tm[group][key]++\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>The first <code>Add</code> finds no inner map and the bug panics writing to nil. Creating the inner map first makes the increment safe.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>m[group]</code> is nil, not empty, until you assign a map.</li>\n<li>Create the inner map before incrementing.</li>\n</ul>",
    "starter": "// Package nestptr records counts per (group, key). A planted bug writes to an\n// inner map that was never created, panicking on the first insert.\npackage nestptr\n\n// Add increments m[group][key], creating the inner map when needed.\nfunc Add(m map[string]map[string]int, group, key string) {\n\t// CHANGE CODE BELOW THIS LINE\n\tm[group][key]++\n\t// CHANGE CODE ABOVE THIS LINE\n}\n",
    "debug": "",
    "file": "nestptr.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/senior/nthoff": {
    "id": "01-language-basics/05-pointers/senior/nthoff",
    "title": "Gap Off-by-One",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Gap Off-by-One</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p>The gap between lead and trail must be exactly n. Advancing lead <code>n+1</code> times</p>\n<p>(with <code>&lt;= n</code>) makes the gap too large, returning the (n+1)-th from the end.</p>\n<h2>Task</h2>\n<p>Fix the lead-advance loop in nthoff.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  NthFromEnd(1-&gt;2-&gt;3-&gt;4-&gt;5, 1)\nOutput: node 5\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  NthFromEnd(1-&gt;2-&gt;3-&gt;4-&gt;5, 2)\nOutput: node 4\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  NthFromEnd(1-&gt;2-&gt;3-&gt;4-&gt;5, 5)\nOutput: node 1\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Exact gap</span><span class=\"tchip\">Loop bound</span><span class=\"tchip\">Two-pointer window</span></div></details>",
    "education": "<h1>Gap size in two-pointer windows</h1>\n<h2>Intuition</h2>\n<p>The lead pointer must be exactly n ahead; an inclusive bound makes it n+1 ahead and shifts the result.</p>\n<h2>Approach</h2>\n<p>1. The lead pointer must advance exactly <code>n</code> steps, not <code>n+1</code>.</p>\n<p>2. The bug's <code>i &lt;= n</code> overshoots by one; use <code>i &lt; n</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\ntype Node struct {\n\tVal  int\n\tNext *Node\n}\n\nfunc NthFromEnd(head *Node, n int) *Node {\n\tlead := head\n\tfor i := 0; i &lt; n; i++ {\n\t\tlead = lead.Next\n\t}\n\ttrail := head\n\tfor lead != nil {\n\t\tlead = lead.Next\n\t\ttrail = trail.Next\n\t}\n\treturn trail\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>For <code>n = 1</code>, <code>i &lt;= n</code> moves lead 2 steps, landing the trailing pointer one node too early. <code>i &lt; n</code> moves it exactly 1 step, so trail ends on the last node.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>i &lt;= n</code> advances n+1 times.</li>\n<li>The gap equals the number of lead-advance steps.</li>\n</ul>",
    "starter": "// Package nthoff returns the n-th node from the end. A planted bug advances the\n// lead pointer n+1 steps (or the wrong count), returning the wrong node.\npackage nthoff\n\ntype Node struct {\n\tVal  int\n\tNext *Node\n}\n\n// NthFromEnd returns the n-th node from the end (1-based). Assume n <= length.\nfunc NthFromEnd(head *Node, n int) *Node {\n\tlead := head\n\t// CHANGE CODE BELOW THIS LINE\n\tfor i := 0; i <= n; i++ {\n\t\t// CHANGE CODE ABOVE THIS LINE\n\t\tlead = lead.Next\n\t}\n\ttrail := head\n\tfor lead != nil {\n\t\tlead = lead.Next\n\t\ttrail = trail.Next\n\t}\n\treturn trail\n}\n",
    "debug": "",
    "file": "nthoff.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/senior/popnoguard": {
    "id": "01-language-basics/05-pointers/senior/popnoguard",
    "title": "Pop Without Empty Guard",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Pop Without Empty Guard</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p>Popping from an empty queue dereferences a nil head. Guard <code>q.head == nil</code> and</p>\n<p>return the not-ok result before touching the node.</p>\n<h2>Task</h2>\n<p>Fix popnoguard.go so popping an empty queue returns false without panicking.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  (&amp;Queue{}).Pop()\nOutput: 0, false\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  q with head 5; q.Pop()\nOutput: 5, true\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  pop until empty then Pop()\nOutput: 0, false, no panic\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Empty guard</span><span class=\"tchip\">Nil dereference</span><span class=\"tchip\">Not-ok result</span></div></details>",
    "education": "<h1>Guarding container operations</h1>\n<h2>Intuition</h2>\n<p>Removing from an empty pointer-backed container must check for nil before dereferencing the head.</p>\n<h2>Approach</h2>\n<p>1. Popping an empty queue dereferences a nil head \u2192 panic.</p>\n<p>2. Guard: <code>if q.head == nil { return 0, false }</code> before reading.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\ntype Node struct {\n\tVal  int\n\tNext *Node\n}\n\ntype Queue struct{ head *Node }\n\nfunc (q *Queue) Pop() (int, bool) {\n\tif q.head == nil {\n\t\treturn 0, false\n\t}\n\tv := q.head.Val\n\tq.head = q.head.Next\n\treturn v, true\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>With no guard, <code>q.head.Val</code> on an empty queue panics. The nil check returns the zero value and <code>false</code> safely.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>q.head.Val</code> on an empty queue panics.</li>\n<li>Return the zero/not-ok pair before dereferencing.</li>\n</ul>",
    "starter": "// Package popnoguard pops the head of a list-backed queue. A planted bug omits\n// the empty check, dereferencing a nil head.\npackage popnoguard\n\ntype Node struct {\n\tVal  int\n\tNext *Node\n}\n\ntype Queue struct{ head *Node }\n\n// Pop removes and returns the front value and true, or 0,false when empty.\nfunc (q *Queue) Pop() (int, bool) {\n\t// CHANGE CODE BELOW THIS LINE\n\tv := q.head.Val\n\tq.head = q.head.Next\n\treturn v, true\n\t// CHANGE CODE ABOVE THIS LINE\n}\n",
    "debug": "",
    "file": "popnoguard.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/senior/pushnoassign": {
    "id": "01-language-basics/05-pointers/senior/pushnoassign",
    "title": "Push Discards Append Result",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Push Discards Append Result</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p><code>append</code> returns a possibly-new slice header; discarding it leaves <code>s.data</code></p>\n<p>unchanged. Assign the result back to the field.</p>\n<h2>Task</h2>\n<p>Fix pushnoassign.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  s.Push(1); s.Push(2)\nOutput: len == 2\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  s.Push(1)\nOutput: len == 1\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  empty stack len\nOutput: 0\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">append returns a header</span><span class=\"tchip\">Reassign the field</span><span class=\"tchip\">Pointer receiver</span></div></details>",
    "education": "<h1>Reassigning appended slice fields</h1>\n<h2>Intuition</h2>\n<p>A struct's slice field must be updated with the append result; ignoring it keeps the old, shorter header.</p>\n<h2>Approach</h2>\n<p>1. <code>append</code> returns a new slice header; the bug discards it with <code>_ =</code>.</p>\n<p>2. Store it back: <code>s.data = append(s.data, v)</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\ntype Stack struct{ data []int }\n\nfunc (s *Stack) Push(v int) {\n\ts.data = append(s.data, v)\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>Discarding <code>append</code>'s result leaves <code>s.data</code> empty despite pushes. Assigning it keeps the grown header, so <code>len</code> reflects the pushes.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>_ = append(s.data, v)</code> builds nothing observable.</li>\n<li>Write <code>s.data = append(s.data, v)</code>.</li>\n</ul>",
    "starter": "// Package pushnoassign pushes onto a slice-backed stack. A planted bug discards\n// the append result, so growth past capacity is lost.\npackage pushnoassign\n\ntype Stack struct{ data []int }\n\n// Push appends v to the stack.\nfunc (s *Stack) Push(v int) {\n\t// CHANGE CODE BELOW THIS LINE\n\t_ = append(s.data, v)\n\t// CHANGE CODE ABOVE THIS LINE\n}\n",
    "debug": "",
    "file": "pushnoassign.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/senior/removeatlist": {
    "id": "01-language-basics/05-pointers/senior/removeatlist",
    "title": "Remove Nth Node Off-by-One",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Remove Nth Node Off-by-One</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p>To remove index i you need its PREDECESSOR (index i-1) so you can relink past</p>\n<p>it. Walking i steps lands on the target itself; walk i-1 steps.</p>\n<h2>Task</h2>\n<p>Fix the walk in removeatlist.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  RemoveAt(10-&gt;20-&gt;30-&gt;40, 2)\nOutput: 10-&gt;20-&gt;40\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  RemoveAt(10-&gt;20-&gt;30, 0)\nOutput: 20-&gt;30\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  RemoveAt(10-&gt;20-&gt;30, 1)\nOutput: 10-&gt;30\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Predecessor needed</span><span class=\"tchip\">Relink past target</span><span class=\"tchip\">Off-by-one walk</span></div></details>",
    "education": "<h1>Reaching the predecessor for deletion</h1>\n<h2>Intuition</h2>\n<p>Unlinking a node needs a handle on the node before it; walking all the way to the target leaves nothing to relink.</p>\n<h2>Approach</h2>\n<p>1. To delete index <code>i</code> you must stop at index <code>i-1</code> (the node before it).</p>\n<p>2. The bug walks to index <code>i</code>; loop to <code>i-1</code> instead.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\ntype Node struct {\n\tVal  int\n\tNext *Node\n}\n\nfunc RemoveAt(head *Node, i int) *Node {\n\tif i == 0 {\n\t\treturn head.Next\n\t}\n\tprev := head\n\tfor k := 0; k &lt; i-1; k++ {\n\t\tprev = prev.Next\n\t}\n\tprev.Next = prev.Next.Next\n\treturn head\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>Walking <code>k &lt; i</code> lands <code>prev</code> on the target itself, so the splice skips the wrong node. <code>k &lt; i-1</code> stops one before, and <code>prev.Next = prev.Next.Next</code> removes index <code>i</code>.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>To delete index i, stop <code>prev</code> at i-1.</li>\n<li>Then <code>prev.Next = prev.Next.Next</code> skips the target.</li>\n</ul>",
    "starter": "// Package removeatlist removes the node at index i (0-based). A planted bug walks\n// to the target node itself instead of its predecessor, so it can't relink.\npackage removeatlist\n\ntype Node struct {\n\tVal  int\n\tNext *Node\n}\n\n// RemoveAt removes the node at index i (0 <= i < length) and returns the head.\nfunc RemoveAt(head *Node, i int) *Node {\n\tif i == 0 {\n\t\treturn head.Next\n\t}\n\tprev := head\n\t// CHANGE CODE BELOW THIS LINE\n\tfor k := 0; k < i; k++ {\n\t\tprev = prev.Next\n\t}\n\t// CHANGE CODE ABOVE THIS LINE\n\tprev.Next = prev.Next.Next\n\treturn head\n}\n",
    "debug": "",
    "file": "removeatlist.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/senior/reseatlocal": {
    "id": "01-language-basics/05-pointers/senior/reseatlocal",
    "title": "Reassigning a Local Pointer",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Reassigning a Local Pointer</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p>Assigning to <code>pp</code> changes the local copy of the double pointer. To reseat the</p>\n<p>caller's pointer you must dereference once: <code>*pp = q</code>.</p>\n<h2>Task</h2>\n<p>Fix reseatlocal.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  a, b := 1, 2; p := &amp;a; Reseat(&amp;p, &amp;b)\nOutput: p == &amp;b\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  p := &amp;a; Reseat(&amp;p, nil)\nOutput: p == nil\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  p := &amp;a; Reseat(&amp;p, &amp;a)\nOutput: p == &amp;a\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Levels of indirection</span><span class=\"tchip\">Local copy</span><span class=\"tchip\">Dereference once</span></div></details>",
    "education": "<h1>Double-pointer reassignment</h1>\n<h2>Intuition</h2>\n<p><code>pp</code> is a copy of the address of the caller's pointer; only <code>*pp = q</code> writes through to reseat it.</p>\n<h2>Approach</h2>\n<p>1. <code>pp</code> is a <code>**int</code>. The bug writes <code>pp = &amp;q</code>, rebinding the local parameter only.</p>\n<p>2. <code>*pp = q</code> writes through to the caller's pointer.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Reseat(pp **int, q *int) {\n\t*pp = q\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>pp = &amp;q</code> changes where <code>pp</code> itself points inside the function; the caller's <code>p</code> is untouched. <code>*pp = q</code> stores <code>q</code> into <code>p</code>.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>pp = &amp;q</code> changes a local copy.</li>\n<li><code>*pp = q</code> reseats the caller's pointer.</li>\n</ul>",
    "starter": "// Package reseatlocal reseats a caller's pointer via a double pointer. A planted\n// bug assigns to the local parameter instead of dereferencing, so the caller's\n// pointer never changes.\npackage reseatlocal\n\n// Reseat should make the caller's pointer (*pp) point to q.\nfunc Reseat(pp **int, q *int) {\n\t// CHANGE CODE BELOW THIS LINE\n\tpp = &q\n\t// CHANGE CODE ABOVE THIS LINE\n}\n",
    "debug": "",
    "file": "reseatlocal.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/senior/reverselose": {
    "id": "01-language-basics/05-pointers/senior/reverselose",
    "title": "Reverse Loses the Rest",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Reverse Loses the Rest</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p>You must save <code>cur.Next</code> BEFORE overwriting it. The bug sets <code>cur.Next = prev</code></p>\n<p>first, so the following <code>next := cur.Next</code> reads <code>prev</code>, losing the remainder.</p>\n<h2>Task</h2>\n<p>Fix the statement order in reverselose.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Reverse(1-&gt;2-&gt;3)\nOutput: 3-&gt;2-&gt;1\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Reverse(nil)\nOutput: nil\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Reverse(5)\nOutput: 5\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Save before overwrite</span><span class=\"tchip\">Order matters</span><span class=\"tchip\">Three-pointer reversal</span></div></details>",
    "education": "<h1>Statement ordering in pointer surgery</h1>\n<h2>Intuition</h2>\n<p>Overwriting <code>cur.Next</code> before saving it loses the rest of the list; capture the successor first.</p>\n<h2>Approach</h2>\n<p>1. You must save <code>next := cur.Next</code> <strong>before</strong> overwriting <code>cur.Next</code>.</p>\n<p>2. The bug reverses the order, so after <code>cur.Next = prev</code> the saved <code>next</code> is already <code>prev</code> \u2014 the rest of the list is lost.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\ntype Node struct {\n\tVal  int\n\tNext *Node\n}\n\nfunc Reverse(head *Node) *Node {\n\tvar prev *Node\n\tcur := head\n\tfor cur != nil {\n\t\tnext := cur.Next\n\t\tcur.Next = prev\n\t\tprev = cur\n\t\tcur = next\n\t}\n\treturn prev\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>With the lines swapped, <code>cur.Next</code> is set to <code>prev</code> and only then read into <code>next</code>, so the traversal loops back instead of advancing. Saving <code>next</code> first keeps the walk moving forward.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Read <code>next := cur.Next</code> before <code>cur.Next = prev</code>.</li>\n<li>Reversed order truncates the list after the first node.</li>\n</ul>",
    "starter": "// Package reverselose reverses a list. A planted bug overwrites cur.Next before\n// saving it, cutting off the rest of the list after one node.\npackage reverselose\n\ntype Node struct {\n\tVal  int\n\tNext *Node\n}\n\n// Reverse reverses the list and returns the new head.\nfunc Reverse(head *Node) *Node {\n\tvar prev *Node\n\tcur := head\n\tfor cur != nil {\n\t\t// CHANGE CODE BELOW THIS LINE\n\t\tcur.Next = prev\n\t\tnext := cur.Next\n\t\t// CHANGE CODE ABOVE THIS LINE\n\t\tprev = cur\n\t\tcur = next\n\t}\n\treturn prev\n}\n",
    "debug": "",
    "file": "reverselose.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/senior/revreturnhead": {
    "id": "01-language-basics/05-pointers/senior/revreturnhead",
    "title": "Reverse Returns Old Head",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Reverse Returns Old Head</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p>After reversal, the original <code>head</code> is the LAST node (its Next is nil). The new</p>\n<p>head is <code>prev</code>. Returning <code>head</code> yields a one-element list.</p>\n<h2>Task</h2>\n<p>Fix the return in revreturnhead.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Reverse(1-&gt;2-&gt;3)\nOutput: 3-&gt;2-&gt;1\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Reverse(nil)\nOutput: nil\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Reverse(5)\nOutput: 5\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">New head is prev</span><span class=\"tchip\">Old head becomes tail</span><span class=\"tchip\">Return the right node</span></div></details>",
    "education": "<h1>Tracking the new head after reversal</h1>\n<h2>Intuition</h2>\n<p>The loop's final <code>prev</code> is the reversed list's head; the original head has become the tail with a nil Next.</p>\n<h2>Approach</h2>\n<p>1. After the loop, <code>prev</code> is the new head; <code>head</code> is now the tail.</p>\n<p>2. The bug returns <code>head</code> (old head, now last). Return <code>prev</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\ntype Node struct {\n\tVal  int\n\tNext *Node\n}\n\nfunc Reverse(head *Node) *Node {\n\tvar prev *Node\n\tcur := head\n\tfor cur != nil {\n\t\tnext := cur.Next\n\t\tcur.Next = prev\n\t\tprev = cur\n\t\tcur = next\n\t}\n\treturn prev\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>The reversal is correct but returning <code>head</code> hands back the original first node \u2014 now the tail. <code>prev</code> is the new front, <code>3-&gt;2-&gt;1</code>.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>After reversal, <code>head.Next == nil</code> (it's the tail).</li>\n<li>Return <code>prev</code>, the last node advanced.</li>\n</ul>",
    "starter": "// Package revreturnhead reverses a list. A planted bug returns the original head\n// instead of the new head (prev), so the caller only sees the (now last) node.\npackage revreturnhead\n\ntype Node struct {\n\tVal  int\n\tNext *Node\n}\n\n// Reverse reverses the list and returns the new head.\nfunc Reverse(head *Node) *Node {\n\tvar prev *Node\n\tcur := head\n\tfor cur != nil {\n\t\tnext := cur.Next\n\t\tcur.Next = prev\n\t\tprev = cur\n\t\tcur = next\n\t}\n\t// CHANGE CODE BELOW THIS LINE\n\treturn head\n\t// CHANGE CODE ABOVE THIS LINE\n}\n",
    "debug": "",
    "file": "revreturnhead.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/senior/shallowclone": {
    "id": "01-language-basics/05-pointers/senior/shallowclone",
    "title": "Struct Clone Shares Pointer Field",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Struct Clone Shares Pointer Field</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p>Copying <code>*d</code> duplicates the struct's fields, but a slice field is just a header</p>\n<p>sharing the same backing array. A deep clone must copy the slice too.</p>\n<h2>Task</h2>\n<p>Fix shallowclone.go so the clone's Tags are independent.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  d := Clone(src); c.Tags[0] = \"X\"\nOutput: src.Tags unchanged\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Clone(&amp;Doc{})\nOutput: independent empty Doc\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  len(Clone(d).Tags)\nOutput: len(d.Tags)\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Shallow struct copy</span><span class=\"tchip\">Copy the slice</span><span class=\"tchip\">Deep clone</span></div></details>",
    "education": "<h1>Shallow vs deep struct copies</h1>\n<h2>Intuition</h2>\n<p>Copying a struct duplicates value fields but shares slices/maps/pointers; a deep clone must copy those reference fields explicitly.</p>\n<h2>Approach</h2>\n<p>1. <code>cp := *d</code> copies the struct but the <code>Tags</code> slice header still shares the backing array.</p>\n<p>2. Deep-copy the slice: <code>cp.Tags = append([]string(nil), d.Tags...)</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\ntype Doc struct {\n\tTags []string\n}\n\nfunc Clone(d *Doc) *Doc {\n\tcp := *d\n\tcp.Tags = append([]string(nil), d.Tags...)\n\treturn &amp;cp\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>The shallow copy shares <code>Tags</code>, so mutating the clone's tags mutates the source. Cloning the slice into a fresh array breaks the aliasing.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>*d</code> copies the header, not the slice's elements.</li>\n<li>Deep-copy each reference-typed field.</li>\n</ul>",
    "starter": "// Package shallowclone clones a struct that holds a slice. A planted bug copies\n// the struct by value, which shares the slice header (backing array), so editing\n// the clone's slice corrupts the original.\npackage shallowclone\n\ntype Doc struct {\n\tTags []string\n}\n\n// Clone returns an independent copy of d: editing the clone's Tags must not\n// affect the original.\nfunc Clone(d *Doc) *Doc {\n\t// CHANGE CODE BELOW THIS LINE\n\tcp := *d\n\treturn &cp\n\t// CHANGE CODE ABOVE THIS LINE\n}\n",
    "debug": "",
    "file": "shallowclone.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/senior/shallowcopy": {
    "id": "01-language-basics/05-pointers/senior/shallowcopy",
    "title": "Shallow Tree Copy Shares Nodes",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Shallow Tree Copy Shares Nodes</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p>Reusing <code>t.Left</code>/<code>t.Right</code> shares the original subtrees; a deep copy must</p>\n<p>recursively copy the children so the two trees share no nodes.</p>\n<h2>Task</h2>\n<p>Fix shallowcopy.go to deep-copy the children.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  c := Copy(tree); mutate c.Left\nOutput: original tree unchanged\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Copy(nil)\nOutput: nil\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Copy(single node)\nOutput: independent single node\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Deep vs shallow</span><span class=\"tchip\">Recursive copy</span><span class=\"tchip\">Independence</span></div></details>",
    "education": "<h1>Deep-copying tree structures</h1>\n<h2>Intuition</h2>\n<p>Copying only the root while reusing child pointers aliases the subtrees; a true deep copy recurses on both children.</p>\n<h2>Approach</h2>\n<p>1. Copying <code>Left</code>/<code>Right</code> by reference shares subtrees with the original.</p>\n<p>2. Recurse: <code>Left: Copy(t.Left), Right: Copy(t.Right)</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\ntype Tree struct {\n\tVal         int\n\tLeft, Right *Tree\n}\n\nfunc Copy(t *Tree) *Tree {\n\tif t == nil {\n\t\treturn nil\n\t}\n\treturn &amp;Tree{Val: t.Val, Left: Copy(t.Left), Right: Copy(t.Right)}\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>The bug reuses the original child pointers, so mutating the copy mutates the source. Recursively copying children makes the trees independent.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>Left: t.Left</code> shares the original subtree.</li>\n<li>Recurse with <code>Copy(t.Left)</code> for independence.</li>\n</ul>",
    "starter": "// Package shallowcopy copies a tree. A planted bug copies only the root node and\n// reuses the original children, so mutating the copy's subtree corrupts the\n// original.\npackage shallowcopy\n\ntype Tree struct {\n\tVal         int\n\tLeft, Right *Tree\n}\n\n// Copy returns a deep, independent copy of t.\nfunc Copy(t *Tree) *Tree {\n\tif t == nil {\n\t\treturn nil\n\t}\n\t// CHANGE CODE BELOW THIS LINE\n\treturn &Tree{Val: t.Val, Left: t.Left, Right: t.Right}\n\t// CHANGE CODE ABOVE THIS LINE\n}\n",
    "debug": "",
    "file": "shallowcopy.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/senior/sumtreebug": {
    "id": "01-language-basics/05-pointers/senior/sumtreebug",
    "title": "Tree Sum Skips Subtrees",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Tree Sum Skips Subtrees</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p>Summing a tree must add the two subtree sums to the node value. Returning only</p>\n<p><code>t.Val</code> ignores every descendant.</p>\n<h2>Task</h2>\n<p>Fix sumtreebug.go to recurse into both children.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  SumTree(tree of 1,2,3,4)\nOutput: 10\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  SumTree(nil)\nOutput: 0\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  SumTree(single node 5)\nOutput: 5\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Recursive combine</span><span class=\"tchip\">Both children</span><span class=\"tchip\">Post-order</span></div></details>",
    "education": "<h1>Recursing into all children</h1>\n<h2>Intuition</h2>\n<p>A tree aggregation must combine the node with BOTH subtree results; returning only the node value discards the rest.</p>\n<h2>Approach</h2>\n<p>1. The bug returns only <code>t.Val</code>, ignoring the subtrees.</p>\n<p>2. Return <code>t.Val + SumTree(t.Left) + SumTree(t.Right)</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\ntype Tree struct {\n\tVal         int\n\tLeft, Right *Tree\n}\n\nfunc SumTree(t *Tree) int {\n\tif t == nil {\n\t\treturn 0\n\t}\n\treturn t.Val + SumTree(t.Left) + SumTree(t.Right)\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>Returning just <code>t.Val</code> sums only the root. Adding both recursive subtree sums accumulates the whole tree to 10.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>return t.Val</code> visits one node only.</li>\n<li>Add <code>SumTree(t.Left) + SumTree(t.Right)</code>.</li>\n</ul>",
    "starter": "// Package sumtreebug sums a tree. A planted bug returns only the root value,\n// never recursing into the children.\npackage sumtreebug\n\ntype Tree struct {\n\tVal         int\n\tLeft, Right *Tree\n}\n\n// SumTree returns the sum of all node values.\nfunc SumTree(t *Tree) int {\n\tif t == nil {\n\t\treturn 0\n\t}\n\t// CHANGE CODE BELOW THIS LINE\n\treturn t.Val\n\t// CHANGE CODE ABOVE THIS LINE\n}\n",
    "debug": "",
    "file": "sumtreebug.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/senior/swapwrong": {
    "id": "01-language-basics/05-pointers/senior/swapwrong",
    "title": "Swapping Pointers, Not Values",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Swapping Pointers, Not Values</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p><code>a, b = b, a</code> swaps the local pointer parameters, which are copies; the caller's</p>\n<p>variables are untouched. Swap the POINTED-TO values with <code><em>a, </em>b = <em>b, </em>a</code>.</p>\n<h2>Task</h2>\n<p>Fix swapwrong.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  x, y := 1, 2; Swap(&amp;x, &amp;y)\nOutput: x == 2, y == 1\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  x, y := 5, 5; Swap(&amp;x, &amp;y)\nOutput: x == 5, y == 5\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  x, y := -1, 9; Swap(&amp;x, &amp;y)\nOutput: x == 9, y == -1\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Pointer params are copies</span><span class=\"tchip\">Dereference to swap values</span><span class=\"tchip\">Caller visibility</span></div></details>",
    "education": "<h1>Swapping through pointers</h1>\n<h2>Intuition</h2>\n<p>Reassigning pointer parameters only changes local copies; exchanging the caller's data requires dereferencing both.</p>\n<h2>Approach</h2>\n<p>1. <code>a, b = b, a</code> swaps the local <strong>pointer</strong> variables \u2014 invisible to the caller.</p>\n<p>2. Dereference: <code><em>a, </em>b = <em>b, </em>a</code> swaps the pointed-at values.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Swap(a, b *int) {\n\t*a, *b = *b, *a\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>The buggy line reorders <code>a</code> and <code>b</code> inside the function only; on return <code>x</code> and <code>y</code> are unchanged. Writing through <code><em>a</code>/<code></em>b</code> swaps the real storage.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>a, b = b, a</code> swaps copies of the addresses.</li>\n<li><code><em>a, </em>b = <em>b, </em>a</code> swaps the values the caller holds.</li>\n</ul>",
    "starter": "// Package swapwrong swaps two ints through pointers. A planted bug swaps the\n// local pointer variables instead of the pointed-to values, so the caller sees\n// no change.\npackage swapwrong\n\n// Swap should exchange the values that a and b point to.\nfunc Swap(a, b *int) {\n\t// CHANGE CODE BELOW THIS LINE\n\ta, b = b, a\n\t// CHANGE CODE ABOVE THIS LINE\n}\n",
    "debug": "",
    "file": "swapwrong.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/senior/tailnoupdate": {
    "id": "01-language-basics/05-pointers/senior/tailnoupdate",
    "title": "Append Can't Grow Empty List",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Append Can't Grow Empty List</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p>When the list is empty there is no tail to link to; the function must CREATE and</p>\n<p>return the first node. Returning nil drops the appended value.</p>\n<h2>Task</h2>\n<p>Fix tailnoupdate.go so appending to an empty list works.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Append(nil, 1)\nOutput: 1\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Append(1, 2)\nOutput: 1-&gt;2\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Append(nil, 9)\nOutput: 9\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Empty-list case</span><span class=\"tchip\">Return the new head</span><span class=\"tchip\">Non-empty case</span></div></details>",
    "education": "<h1>Growing an empty list</h1>\n<h2>Intuition</h2>\n<p>With a <code>*Node</code> signature the empty case must return the new head; returning nil loses the value.</p>\n<h2>Approach</h2>\n<p>1. Appending to an empty list must create the first node.</p>\n<p>2. The bug returns nil, dropping the value.</p>\n<p>3. Return <code>&amp;Node{Val: v}</code> in the empty case.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\ntype Node struct {\n\tVal  int\n\tNext *Node\n}\n\nfunc Append(head *Node, v int) *Node {\n\tif head == nil {\n\t\treturn &amp;Node{Val: v}\n\t}\n\tn := head\n\tfor n.Next != nil {\n\t\tn = n.Next\n\t}\n\tn.Next = &amp;Node{Val: v}\n\treturn head\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>Append(nil, 1)</code> should yield a one-node list; returning nil loses the value. Allocating a node fixes the base case.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>An empty list has no tail \u2014 build the head instead.</li>\n<li>Return it so the caller can adopt the new head.</li>\n</ul>",
    "starter": "// Package tailnoupdate appends to a list's tail. A planted bug takes *Node (not\n// **Node) and returns nothing, so appending to an empty list can't tell the\n// caller about the new head. It is fixed by returning the head.\npackage tailnoupdate\n\ntype Node struct {\n\tVal  int\n\tNext *Node\n}\n\n// Append adds v at the tail and returns the (possibly new) head.\nfunc Append(head *Node, v int) *Node {\n\tif head == nil {\n\t\t// CHANGE CODE BELOW THIS LINE\n\t\treturn nil\n\t\t// CHANGE CODE ABOVE THIS LINE\n\t}\n\tn := head\n\tfor n.Next != nil {\n\t\tn = n.Next\n\t}\n\tn.Next = &Node{Val: v}\n\treturn head\n}\n",
    "debug": "",
    "file": "tailnoupdate.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/senior/valuerecv": {
    "id": "01-language-basics/05-pointers/senior/valuerecv",
    "title": "Value Receiver Loses Mutation",
    "level": "senior",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Value Receiver Loses Mutation</h1><div class=\"p-meta\"><span class=\"pill lv-senior\">senior</span></div></div><h2>Context</h2>\n<p>A value receiver <code>(c Counter)</code> operates on a COPY; <code>c.N++</code> changes the copy and</p>\n<p>vanishes when the method returns. Mutating methods need a pointer receiver.</p>\n<h2>Task</h2>\n<p>Fix the receiver in valuerecv.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  c := &amp;Counter{}; c.Inc(); c.Inc()\nOutput: c.N == 2\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  c := &amp;Counter{N: 5}; c.Inc()\nOutput: c.N == 6\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  c := &amp;Counter{}; c.Inc()\nOutput: c.N == 1\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Value vs pointer receiver</span><span class=\"tchip\">Persisting mutation</span><span class=\"tchip\">Method sets</span></div></details>",
    "education": "<h1>Pointer vs value receivers</h1>\n<h2>Intuition</h2>\n<p>A value receiver copies the struct; mutations don't reach the caller. Any state-changing method must take a pointer receiver.</p>\n<h2>Approach</h2>\n<p>1. A <strong>value</strong> receiver <code>(c Counter)</code> mutates a copy, so the caller never sees <code>c.N</code> change.</p>\n<p>2. Switch to a <strong>pointer</strong> receiver <code>(c *Counter)</code> so <code>c.N++</code> writes back to the caller's struct.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\ntype Counter struct{ N int }\n\nfunc (c *Counter) Inc() {\n\tc.N++\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>With the value receiver, each <code>Inc</code> bumps a throwaway copy and <code>c.N</code> stays 0. The pointer receiver shares storage, so two calls leave <code>c.N == 2</code>.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>(c Counter)</code> increments a copy; <code>(c *Counter)</code> increments the caller's value.</li>\n<li>Keep receiver types consistent across a type's methods.</li>\n</ul>",
    "starter": "// Package valuerecv increments a counter via a method. A planted bug uses a\n// VALUE receiver, so the increment mutates a copy and is lost.\npackage valuerecv\n\ntype Counter struct{ N int }\n\n// Inc should increment the counter so the change persists on the caller.\n// CHANGE CODE BELOW THIS LINE\nfunc (c Counter) Inc() {\n\t// CHANGE CODE ABOVE THIS LINE\n\tc.N++\n}\n",
    "debug": "",
    "file": "valuerecv.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/staff/alignof": {
    "id": "01-language-basics/05-pointers/staff/alignof",
    "title": "Alignment of a Type",
    "level": "staff",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Alignment of a Type</h1><div class=\"p-meta\"><span class=\"pill lv-staff\">staff</span></div></div><h2>Context</h2>\n<p>Alignment and size are distinct: <code>unsafe.Alignof</code> gives the required address</p>\n<p>alignment, <code>unsafe.Sizeof</code> the byte width. Use Alignof for alignment.</p>\n<h2>Task</h2>\n<p>Fix alignof.go to return the alignment.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  FieldAlign(&amp;S{})\nOutput: unsafe.Alignof(int64) (8)\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  alignment of int64 field\nOutput: 8\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  result is an alignment, not a size\nOutput: 8, not Sizeof\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Alignof vs Sizeof</span><span class=\"tchip\">Field alignment</span><span class=\"tchip\">Correct API</span></div></details>",
    "education": "<h1>Alignment vs size</h1>\n<h2>Intuition</h2>\n<p><code>Alignof</code> reports the address boundary a type must sit on; <code>Sizeof</code> reports its width. They match for basic types but the concepts (and APIs) are distinct.</p>\n<h2>Approach</h2>\n<p>1. Alignment and size are different properties.</p>\n<p>2. The bug returns <code>Sizeof(s.B)</code>; use <code>unsafe.Alignof(s.B)</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nimport \"unsafe\"\n\ntype S struct {\n\tA bool\n\tB [3]int32\n}\n\nfunc FieldAlign(s *S) uintptr {\n\treturn unsafe.Alignof(s.B)\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>For an int64 both happen to be 8, but the question asks for alignment. <code>Alignof</code> is the correct operator and stays right for types where size != alignment.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Use <code>Alignof</code> for alignment, <code>Sizeof</code> for width.</li>\n<li>Struct padding is driven by field alignment.</li>\n</ul>",
    "starter": "// Package alignof reports the alignment of int64 on the platform. A planted bug\n// returns the SIZE instead of the ALIGNMENT (they can differ for composite\n// types; the test pins the int64 case where both are 8 but the API must be\n// Alignof).\npackage alignof\n\nimport \"unsafe\"\n\ntype S struct {\n\tA bool\n\tB [3]int32\n}\n\n// FieldAlign returns the alignment requirement of field B.\nfunc FieldAlign(s *S) uintptr {\n\t// CHANGE CODE BELOW THIS LINE\n\treturn unsafe.Sizeof(s.B)\n\t// CHANGE CODE ABOVE THIS LINE\n}\n",
    "debug": "",
    "file": "alignof.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/staff/arraysharing": {
    "id": "01-language-basics/05-pointers/staff/arraysharing",
    "title": "Two Views Share the Array",
    "level": "staff",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Two Views Share the Array</h1><div class=\"p-meta\"><span class=\"pill lv-staff\">staff</span></div></div><h2>Context</h2>\n<p><code>append([]int(nil), p[:]...)</code> COPIES the data into a new array, so the second</p>\n<p>view is independent. To alias, both must slice the same array: <code>b := p[:]</code>.</p>\n<h2>Task</h2>\n<p>Fix arraysharing.go so both views alias the array.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  a, b := Views(&amp;arr); a[0] = 42\nOutput: b[0] == 42\n</code></pre>\n<p>_Explanation:_ Both views must alias the same array.</p>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  a and b share backing\nOutput: true\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  mutate a, observe b\nOutput: reflected\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Aliasing views</span><span class=\"tchip\">Copy breaks aliasing</span><span class=\"tchip\">Shared storage</span></div></details>",
    "education": "<h1>Aliasing vs copying slice views</h1>\n<h2>Intuition</h2>\n<p>Multiple slices of the same array share storage; copying into a new slice breaks that aliasing.</p>\n<h2>Approach</h2>\n<p>1. <code>append([]int(nil), p[:]...)</code> copies into a new array \u2014 no aliasing.</p>\n<p>2. Slice the array directly: <code>b := p[:]</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Views(p *[3]int) ([]int, []int) {\n\ta := p[:]\n\tb := p[:]\n\treturn a, b\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>The bug's copy makes <code>b</code> independent, so <code>a[0] = 42</code> doesn't reach it. Slicing <code>p[:]</code> shares the backing array.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>p[:]</code> aliases the array; <code>append([]int(nil), p[:]...)</code> copies it.</li>\n<li>Aliased views see each other's writes.</li>\n</ul>",
    "starter": "// Package arraysharing returns two slice views over the same array and expects\n// them to alias. A planted bug copies the array into a fresh slice for the second\n// view, so writes through one are not seen by the other.\npackage arraysharing\n\n// Views returns two slices that both view the array p points to (they must\n// alias: a write through one is visible in the other).\nfunc Views(p *[3]int) ([]int, []int) {\n\ta := p[:]\n\t// CHANGE CODE BELOW THIS LINE\n\tb := append([]int(nil), p[:]...)\n\t// CHANGE CODE ABOVE THIS LINE\n\treturn a, b\n}\n",
    "debug": "",
    "file": "arraysharing.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/staff/arrtoslice": {
    "id": "01-language-basics/05-pointers/staff/arrtoslice",
    "title": "Array Pointer to Slice",
    "level": "staff",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Array Pointer to Slice</h1><div class=\"p-meta\"><span class=\"pill lv-staff\">staff</span></div></div><h2>Context</h2>\n<p>Slicing an array pointer <code>p[:]</code> yields a slice over ALL elements, aliasing the</p>\n<p>array. <code>p[:2]</code> drops the last two.</p>\n<h2>Task</h2>\n<p>Fix arrtoslice.go to view the whole array.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  AsSlice(&amp;[4]int{1,2,3,4})\nOutput: len 4, aliases the array\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  len(AsSlice(&amp;[4]int{}))\nOutput: 4\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  AsSlice(&amp;a)[0] = 9\nOutput: a[0] == 9\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Array pointer slicing</span><span class=\"tchip\">Aliasing</span><span class=\"tchip\">Full length</span></div></details>",
    "education": "<h1>Slicing array pointers</h1>\n<h2>Intuition</h2>\n<p><code>(&amp;arr)[:]</code> produces a slice over the entire array, aliasing it; a shorter slice expression drops elements.</p>\n<h2>Approach</h2>\n<p>1. <code>p[:2]</code> only views the first two elements.</p>\n<p>2. <code>p[:]</code> slices the whole array, still aliasing it.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc AsSlice(p *[4]int) []int {\n\treturn p[:]\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>The bug truncates to length 2. <code>p[:]</code> yields a length-4 slice backed by the same array, so writes propagate.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>p[:]</code> gives all 4 elements; <code>p[:2]</code> only the first two.</li>\n<li>The resulting slice aliases the array's memory.</li>\n</ul>",
    "starter": "// Package arrtoslice converts a *[4]int into a full []int view. A planted bug\n// slices with a fixed wrong length, dropping elements.\npackage arrtoslice\n\n// AsSlice returns a slice viewing all elements of the array p points to.\nfunc AsSlice(p *[4]int) []int {\n\t// CHANGE CODE BELOW THIS LINE\n\treturn p[:2]\n\t// CHANGE CODE ABOVE THIS LINE\n}\n",
    "debug": "",
    "file": "arrtoslice.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/staff/clearptrs": {
    "id": "01-language-basics/05-pointers/staff/clearptrs",
    "title": "Clear a Pointer Slice",
    "level": "staff",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Clear a Pointer Slice</h1><div class=\"p-meta\"><span class=\"pill lv-staff\">staff</span></div></div><h2>Context</h2>\n<p><code>s[:0]</code> sets length to 0 but the backing array still holds the pointers, keeping</p>\n<p>the objects alive. Nil each element first (the <code>clear</code> built-in does this), then</p>\n<p>re-slice.</p>\n<h2>Task</h2>\n<p>Fix clearptrs.go to drop the references before emptying.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Clear([]*int{&amp;a, &amp;b})\nOutput: len 0, backing array nilled\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  len after Clear\nOutput: 0\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  backing slots\nOutput: all nil\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Retention via backing array</span><span class=\"tchip\">Nil the elements</span><span class=\"tchip\">GC eligibility</span></div></details>",
    "education": "<h1>Clearing pointer slices without leaking</h1>\n<h2>Intuition</h2>\n<p>Reducing length leaves element pointers in the backing array; for pointer/interface elements you must nil them (or use <code>clear</code>) so the referents can be collected.</p>\n<h2>Approach</h2>\n<p>1. <code>s[:0]</code> alone leaves the pointers live in the backing array, leaking them.</p>\n<p>2. Nil each slot first, then reslice to length 0.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Clear(s []*int) []*int {\n\tfor i := range s {\n\t\ts[i] = nil\n\t}\n\treturn s[:0]\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>Reslicing keeps the element pointers reachable via capacity. Setting each to nil before <code>s[:0]</code> lets them be collected.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>s[:0]</code> keeps pointers in the array's spare capacity.</li>\n<li><code>clear(s)</code> (Go 1.21+) zeroes elements; then re-slice.</li>\n</ul>",
    "starter": "// Package clearptrs clears a slice of pointers so referenced objects can be\n// freed. A planted bug re-slices to length 0 but leaves the pointers in the\n// backing array, retaining the objects.\npackage clearptrs\n\n// Clear empties the slice AND drops its references so the pointees can be GC'd.\n// It returns the emptied slice (len 0).\nfunc Clear(s []*int) []*int {\n\t// CHANGE CODE BELOW THIS LINE\n\treturn s[:0]\n\t// CHANGE CODE ABOVE THIS LINE\n}\n",
    "debug": "",
    "file": "clearptrs.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/staff/deepptrfield": {
    "id": "01-language-basics/05-pointers/staff/deepptrfield",
    "title": "Copy Shares Pointer Field",
    "level": "staff",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Copy Shares Pointer Field</h1><div class=\"p-meta\"><span class=\"pill lv-staff\">staff</span></div></div><h2>Context</h2>\n<p>Copying <code>*b</code> duplicates the pointer field's VALUE (the address), so both boxes</p>\n<p>share one pointee. A deep clone must allocate a new int and copy the value.</p>\n<h2>Task</h2>\n<p>Fix deepptrfield.go so the clone's pointer is independent.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  c := Clone(b); *c.P = 99\nOutput: *b.P unchanged\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Clone(&amp;Box{P: &amp;x})\nOutput: independent pointee\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  c.P != b.P\nOutput: true\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Shallow copy shares pointers</span><span class=\"tchip\">Deep copy the pointee</span><span class=\"tchip\">Independence</span></div></details>",
    "education": "<h1>Deep-copying pointer fields</h1>\n<h2>Intuition</h2>\n<p>Struct copy duplicates a pointer field's address, sharing the pointee; a deep clone allocates and copies what the pointer references.</p>\n<h2>Approach</h2>\n<p>1. <code>cp := *b</code> copies the struct but shares the <code>P</code> pointer.</p>\n<p>2. Deep-copy the pointee: <code>v := *b.P; cp.P = &amp;v</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\ntype Box struct {\n\tP *int\n}\n\nfunc Clone(b *Box) *Box {\n\tcp := *b\n\tv := *b.P\n\tcp.P = &amp;v\n\treturn &amp;cp\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>The shallow copy shares <code>P</code>, so writing <code><em>c.P</code> also changes <code></em>b.P</code>. Allocating a fresh pointee for the clone breaks the sharing.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>*b</code> copies the pointer value, not the pointee.</li>\n<li>Allocate a new pointee and copy the value into it.</li>\n</ul>",
    "starter": "// Package deepptrfield clones a struct holding a *int. A planted bug copies the\n// struct by value, sharing the SAME *int, so editing the clone's pointee changes\n// the original.\npackage deepptrfield\n\ntype Box struct {\n\tP *int\n}\n\n// Clone returns an independent copy: writing through the clone's P must not\n// affect the original.\nfunc Clone(b *Box) *Box {\n\t// CHANGE CODE BELOW THIS LINE\n\tcp := *b\n\treturn &cp\n\t// CHANGE CODE ABOVE THIS LINE\n}\n",
    "debug": "",
    "file": "deepptrfield.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/staff/escapereturn": {
    "id": "01-language-basics/05-pointers/staff/escapereturn",
    "title": "Return Pointer to the Element",
    "level": "staff",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Return Pointer to the Element</h1><div class=\"p-meta\"><span class=\"pill lv-staff\">staff</span></div></div><h2>Context</h2>\n<p><code>&amp;v</code> addresses a local copy of the element (which escapes to the heap but is</p>\n<p>detached from the slice). To return an aliasing pointer, take the element's</p>\n<p>address directly: <code>&amp;xs[best]</code>.</p>\n<h2>Task</h2>\n<p>Fix escapereturn.go so the returned pointer aliases the slice.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  p := MaxPtr(xs); *p = 0\nOutput: xs's max element becomes 0\n</code></pre>\n<p>_Explanation:_ The returned pointer must alias the slice element.</p>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  MaxPtr([3 9 1])\nOutput: &amp;xs[1]\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  MaxPtr([5])\nOutput: &amp;xs[0]\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Address of a copy</span><span class=\"tchip\">Address of the element</span><span class=\"tchip\">Escape analysis</span></div></details>",
    "education": "<h1>Aliasing vs escaping copies</h1>\n<h2>Intuition</h2>\n<p><code>&amp;localCopy</code> escapes to the heap but is independent of the slice; <code>&amp;xs[i]</code> addresses the backing array so writes are visible in the slice.</p>\n<h2>Approach</h2>\n<p>1. <code>v := xs[best]; return &amp;v</code> returns the address of a <strong>copy</strong>.</p>\n<p>2. Return <code>&amp;xs[best]</code> to alias the real element.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc MaxPtr(xs []int) *int {\n\tbest := 0\n\tfor i := range xs {\n\t\tif xs[i] &gt; xs[best] {\n\t\t\tbest = i\n\t\t}\n\t}\n\treturn &amp;xs[best]\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>Writing through the bug's pointer changes a dead local, not the slice. <code>&amp;xs[best]</code> lets the caller mutate the actual max element.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>v := xs[i]; &amp;v</code> gives an independent pointer.</li>\n<li><code>&amp;xs[i]</code> gives an aliasing pointer into the slice.</li>\n</ul>",
    "starter": "// Package escapereturn returns a pointer to the max element of a slice. A planted\n// bug returns the address of a local COPY (loop value), which aliases nothing in\n// the caller's slice, so mutating through it doesn't change the slice.\npackage escapereturn\n\n// MaxPtr returns a pointer to the largest element of xs (xs non-empty), aliasing\n// the slice so writes through it change xs.\nfunc MaxPtr(xs []int) *int {\n\tbest := 0\n\tfor i := range xs {\n\t\tif xs[i] > xs[best] {\n\t\t\tbest = i\n\t\t}\n\t}\n\t// CHANGE CODE BELOW THIS LINE\n\tv := xs[best]\n\treturn &v\n\t// CHANGE CODE ABOVE THIS LINE\n}\n",
    "debug": "",
    "file": "escapereturn.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/staff/escapeslice": {
    "id": "01-language-basics/05-pointers/staff/escapeslice",
    "title": "Store Distinct Struct Pointers",
    "level": "staff",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Store Distinct Struct Pointers</h1><div class=\"p-meta\"><span class=\"pill lv-staff\">staff</span></div></div><h2>Context</h2>\n<p><code>it</code> is declared ONCE, so <code>&amp;it</code> is the same address every iteration; all</p>\n<p>pointers see the final value. Declare a fresh struct per iteration so each</p>\n<p>pointer escapes to its own allocation.</p>\n<h2>Task</h2>\n<p>Fix escapeslice.go so each pointer holds a distinct value.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Items([1 2 3])\nOutput: pointers to V=1, V=2, V=3\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  deref results\nOutput: 1, 2, 3 (distinct)\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Items([])\nOutput: empty\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Hoisted struct address</span><span class=\"tchip\">Per-iteration allocation</span><span class=\"tchip\">Escape analysis</span></div></details>",
    "education": "<h1>Distinct allocations per iteration</h1>\n<h2>Intuition</h2>\n<p>Taking the address of a single hoisted variable shares one allocation; declaring the variable inside the loop gives each pointer its own heap object.</p>\n<h2>Approach</h2>\n<p>1. A single hoisted <code>it</code> is reused, so all pointers alias the last value.</p>\n<p>2. Declare <code>it := Item{V: v}</code> inside the loop for fresh storage.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\ntype Item struct{ V int }\n\nfunc Items(vs []int) []*Item {\n\tvar out []*Item\n\tfor _, v := range vs {\n\t\tit := Item{V: v}\n\t\tout = append(out, &amp;it)\n\t}\n\treturn out\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>With the shared <code>it</code>, every <code>&amp;it</code> points at one struct holding the final value. A per-iteration <code>it</code> gives each pointer its own item.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>&amp;it</code> of a hoisted <code>it</code> aliases one struct.</li>\n<li>Declare <code>it := Item{...}</code> inside the loop for distinct pointers.</li>\n</ul>",
    "starter": "// Package escapeslice builds a slice of pointers to per-element structs. A\n// planted bug appends the address of a single hoisted struct reused across\n// iterations, so all pointers alias the final value.\npackage escapeslice\n\ntype Item struct{ V int }\n\n// Items returns one *Item per input value; each pointer must hold a DISTINCT V.\nfunc Items(vs []int) []*Item {\n\tvar out []*Item\n\tvar it Item\n\tfor _, v := range vs {\n\t\tit.V = v\n\t\t// CHANGE CODE BELOW THIS LINE\n\t\tout = append(out, &it)\n\t\t// CHANGE CODE ABOVE THIS LINE\n\t}\n\treturn out\n}\n",
    "debug": "",
    "file": "escapeslice.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/staff/fieldoffset": {
    "id": "01-language-basics/05-pointers/staff/fieldoffset",
    "title": "Reach a Field by Offset",
    "level": "staff",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Reach a Field by Offset</h1><div class=\"p-meta\"><span class=\"pill lv-staff\">staff</span></div></div><h2>Context</h2>\n<p>The offset of field B within the struct is <code>unsafe.Offsetof(p.B)</code>, not</p>\n<p><code>unsafe.Sizeof(p.B)</code>. Sizeof gives the field's width, not its position.</p>\n<h2>Task</h2>\n<p>Fix fieldoffset.go to use the field offset.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  SecondField(&amp;Pair{1, 2})\nOutput: 2\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  SecondField(&amp;Pair{10, 20})\nOutput: 20\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  SecondField(&amp;Pair{0, -5})\nOutput: -5\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Offsetof vs Sizeof</span><span class=\"tchip\">Field position</span><span class=\"tchip\">unsafe.Add</span></div></details>",
    "education": "<h1>Locating fields with unsafe.Offsetof</h1>\n<h2>Intuition</h2>\n<p><code>Offsetof</code> returns a field's byte position within its struct; <code>Sizeof</code> returns a type's width \u2014 different quantities.</p>\n<h2>Approach</h2>\n<p>1. To reach field B you need its <strong>offset</strong>, not the size of B.</p>\n<p>2. Use <code>unsafe.Offsetof(p.B)</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nimport \"unsafe\"\n\ntype Pair struct {\n\tA int64\n\tB int32\n}\n\nfunc SecondField(p *Pair) int32 {\n\tbase := unsafe.Pointer(p)\n\toff := unsafe.Offsetof(p.B)\n\treturn *(*int32)(unsafe.Add(base, off))\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>Sizeof(p.B)</code> happens to equal the offset only by luck of layout; <code>Offsetof(p.B)</code> is the correct byte distance from the struct start to B.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>Sizeof(p.B)</code> is 4 (width), <code>Offsetof(p.B)</code> is 4 (position) \u2014 they coincide here by luck of layout but mean different things; with a wider first field they diverge.</li>\n<li>Always use Offsetof for a field's position.</li>\n</ul>",
    "starter": "// Package fieldoffset reads a struct's second field through unsafe pointer\n// arithmetic. A planted bug uses Sizeof (position after the field) instead of\n// Offsetof (position of the field).\npackage fieldoffset\n\nimport \"unsafe\"\n\ntype Pair struct {\n\tA int64\n\tB int32\n}\n\n// SecondField returns p.B by computing the field offset with unsafe.\nfunc SecondField(p *Pair) int32 {\n\tbase := unsafe.Pointer(p)\n\t// CHANGE CODE BELOW THIS LINE\n\toff := unsafe.Sizeof(p.B)\n\t// CHANGE CODE ABOVE THIS LINE\n\treturn *(*int32)(unsafe.Add(base, off))\n}\n",
    "debug": "",
    "file": "fieldoffset.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/staff/floatbits": {
    "id": "01-language-basics/05-pointers/staff/floatbits",
    "title": "Reinterpret Float Bits",
    "level": "staff",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Reinterpret Float Bits</h1><div class=\"p-meta\"><span class=\"pill lv-staff\">staff</span></div></div><h2>Context</h2>\n<p>The bug narrows to float32 first (losing precision) and reads 32 bits. Read the</p>\n<p>full float64 pattern: reinterpret <code>&amp;f</code> as <code>*uint64</code>.</p>\n<h2>Task</h2>\n<p>Fix floatbits.go to return the bit pattern.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Bits(1.5)\nOutput: 0x3ff8000000000000\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Bits(0.0)\nOutput: 0x0\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Bits(-2.0)\nOutput: 0xc000000000000000\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Value cast vs reinterpret</span><span class=\"tchip\">unsafe.Pointer reinterpret</span><span class=\"tchip\">Same-size types</span></div></details>",
    "education": "<h1>Reinterpreting memory with unsafe.Pointer</h1>\n<h2>Intuition</h2>\n<p><code><em>(</em>T)(unsafe.Pointer(&amp;x))</code> reads the same bytes as a different type; a value cast instead performs numeric conversion.</p>\n<h2>Approach</h2>\n<p>1. The value is a <code>float64</code>; reinterpret its 64 bits directly.</p>\n<p>2. The bug narrows to <code>float32</code> first, losing precision and reading 32 bits.</p>\n<p>3. Cast the address: <code><em>(</em>uint64)(unsafe.Pointer(&amp;f))</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nimport \"unsafe\"\n\nfunc Bits(f float64) uint64 {\n\treturn *(*uint64)(unsafe.Pointer(&amp;f))\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>float32(f)</code> rounds and yields a 32-bit pattern, so <code>Bits(1.5)</code> comes out wrong. Reading the original <code>float64</code> bits gives <code>0x3ff8000000000000</code>.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Narrowing to float32 changes the bit pattern and width.</li>\n<li>Reinterpret the float64 directly as uint64 (both 8 bytes).</li>\n</ul>",
    "starter": "// Package floatbits returns the raw IEEE-754 bits of a float64. A planted bug\n// converts the VALUE (truncating to an integer) instead of reinterpreting the\n// bit pattern via unsafe.Pointer.\npackage floatbits\n\nimport \"unsafe\"\n\n// Bits returns the 64-bit pattern of f (same as math.Float64bits).\nfunc Bits(f float64) uint64 {\n\t// CHANGE CODE BELOW THIS LINE\n\tg := float32(f)\n\treturn uint64(*(*uint32)(unsafe.Pointer(&g)))\n\t// CHANGE CODE ABOVE THIS LINE\n}\n",
    "debug": "",
    "file": "floatbits.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/staff/mapdelnil": {
    "id": "01-language-basics/05-pointers/staff/mapdelnil",
    "title": "Delete vs Nil in Pointer Map",
    "level": "staff",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Delete vs Nil in Pointer Map</h1><div class=\"p-meta\"><span class=\"pill lv-staff\">staff</span></div></div><h2>Context</h2>\n<p>Setting <code>m[id] = nil</code> keeps the key (with a nil value); the entry still counts</p>\n<p>in len and appears in iteration. Use the built-in <code>delete</code> to remove the key.</p>\n<h2>Task</h2>\n<p>Fix mapdelnil.go to actually remove the key.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Remove(m, 1)\nOutput: key 1 gone, len decreases\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  len(m) after Remove\nOutput: len-1\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  _, ok := m[1]\nOutput: false\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Nil value vs absent key</span><span class=\"tchip\">delete built-in</span><span class=\"tchip\">len/iteration</span></div></details>",
    "education": "<h1>Deleting map entries</h1>\n<h2>Intuition</h2>\n<p>Assigning a nil (or zero) value keeps the key; the <code>delete</code> built-in is the only way to remove an entry from a map.</p>\n<h2>Approach</h2>\n<p>1. <code>m[id] = nil</code> keeps the key with a nil value \u2014 length unchanged.</p>\n<p>2. <code>delete(m, id)</code> removes the entry entirely.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Remove(m map[int]*int, id int) {\n\tdelete(m, id)\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>Assigning nil leaves the key present, so <code>len(m)</code> and <code>ok</code> still report it. <code>delete</code> actually removes it.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>m[id] = nil</code> leaves the key present with a nil value.</li>\n<li><code>delete(m, id)</code> removes it entirely.</li>\n</ul>",
    "starter": "// Package mapdelnil removes a key from a map of pointers. A planted bug sets the\n// value to nil but leaves the KEY present, so len and iteration still see it.\npackage mapdelnil\n\n// Remove deletes id from the registry so it no longer appears.\nfunc Remove(m map[int]*int, id int) {\n\t// CHANGE CODE BELOW THIS LINE\n\tm[id] = nil\n\t// CHANGE CODE ABOVE THIS LINE\n}\n",
    "debug": "",
    "file": "mapdelnil.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/staff/mapstructptr": {
    "id": "01-language-basics/05-pointers/staff/mapstructptr",
    "title": "Mutate Pointee From Map Struct",
    "level": "staff",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Mutate Pointee From Map Struct</h1><div class=\"p-meta\"><span class=\"pill lv-staff\">staff</span></div></div><h2>Context</h2>\n<p>Even though the <code>Ref</code> value is a copy from the map, its <code>P</code> field still points</p>\n<p>at the shared int. Mutate through the pointer: <code>*r.P++</code>. The bug does a no-op</p>\n<p>self-assignment.</p>\n<h2>Task</h2>\n<p>Fix mapstructptr.go to increment the shared int.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  BumpVia(m, 1)\nOutput: *m[1].P += 1\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  *m[1].P == 10; BumpVia(m, 1)\nOutput: 11\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  BumpVia on missing key\nOutput: no write\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Copied struct, shared pointee</span><span class=\"tchip\">Mutate through the pointer</span><span class=\"tchip\">Map value addressability</span></div></details>",
    "education": "<h1>Pointers inside copied struct values</h1>\n<h2>Intuition</h2>\n<p>Copying a struct duplicates its pointer fields' addresses, so the copy still aliases the same pointee; mutate through the pointer even when the struct itself is a copy.</p>\n<h2>Approach</h2>\n<p>1. The bug discards the record with <code>_ = r</code> and never mutates.</p>\n<p>2. Follow the pointer field: <code>*r.P++</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\ntype Ref struct{ P *int }\n\nfunc BumpVia(m map[int]Ref, k int) {\n\tr := m[k]\n\t*r.P++\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>_ = r</code> reads the struct but changes nothing. <code>*r.P++</code> increments the int the record's pointer field references.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>r := m[k]</code> copies the struct but not the pointee.</li>\n<li><code>*r.P++</code> mutates the shared int.</li>\n</ul>",
    "starter": "// Package mapstructptr mutates an int referenced by a struct stored BY VALUE in\n// a map. Because map struct values aren't addressable, a planted bug tries to\n// mutate the map value's field directly is avoided; instead it fetches a copy\n// and mutates the copy's non-pointer field. The pointer field still aliases, so\n// mutate through the pointer.\npackage mapstructptr\n\ntype Ref struct{ P *int }\n\n// BumpVia increments the int referenced by m[k].P. The Ref is stored by value,\n// but its P pointer aliases a shared int.\nfunc BumpVia(m map[int]Ref, k int) {\n\tr := m[k]\n\t// CHANGE CODE BELOW THIS LINE\n\t_ = r\n\t// CHANGE CODE ABOVE THIS LINE\n}\n",
    "debug": "",
    "file": "mapstructptr.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/staff/methodexpr": {
    "id": "01-language-basics/05-pointers/staff/methodexpr",
    "title": "Method Expression Receiver",
    "level": "staff",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Method Expression Receiver</h1><div class=\"p-meta\"><span class=\"pill lv-staff\">staff</span></div></div><h2>Context</h2>\n<p>A method EXPRESSION <code>(*Counter).Add</code> is a function whose FIRST argument is the</p>\n<p>receiver: <code>f(c, d)</code> calls <code>c.Add(d)</code>. The bug ignores the passed receiver and</p>\n<p>mutates an unrelated local.</p>\n<h2>Task</h2>\n<p>Fix methodexpr.go to return the method expression.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  f := AdderExpr(); f(c, 5)\nOutput: c.N += 5\n</code></pre>\n<p>_Explanation:_ A method expression takes the receiver as its first argument.</p>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  f(&amp;Counter{N:1}, 4)\nOutput: N == 5\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  f(c, 0)\nOutput: c.N unchanged\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Method expression</span><span class=\"tchip\">Explicit receiver</span><span class=\"tchip\">Value vs expression</span></div></details>",
    "education": "<h1>Method expressions</h1>\n<h2>Intuition</h2>\n<p><code>T.Method</code> (or <code>(*T).Method</code>) yields a function with the receiver as an explicit first parameter, unlike a method value which binds a specific receiver.</p>\n<h2>Approach</h2>\n<p>1. A <strong>method expression</strong> <code>(*Counter).Add</code> is a function whose first parameter is the receiver.</p>\n<p>2. The bug ignores the passed receiver and mutates a captured local.</p>\n<p>3. Return <code>(*Counter).Add</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\ntype Counter struct{ N int }\n\nfunc (c *Counter) Add(d int) { c.N += d }\n\nfunc AdderExpr() func(*Counter, int) {\n\treturn (*Counter).Add\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>The bug's closure calls <code>c.Add</code> on its own hidden <code>c</code>, so <code>f(c, 5)</code> never touches the caller's counter. The method expression applies to whatever receiver you pass.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>(<em>Counter).Add</code> has signature <code>func(</em>Counter, int)</code>.</li>\n<li>A method VALUE (<code>c.Add</code>) binds one receiver; an EXPRESSION takes it as an argument.</li>\n</ul>",
    "starter": "// Package methodexpr builds a function from a method expression. A planted bug\n// uses the wrong form, dropping the explicit receiver argument that a method\n// EXPRESSION requires.\npackage methodexpr\n\ntype Counter struct{ N int }\n\nfunc (c *Counter) Add(d int) { c.N += d }\n\n// AdderExpr returns the method EXPRESSION Counter.Add as a function taking the\n// receiver explicitly: f(c, d) adds d to c.\nfunc AdderExpr() func(*Counter, int) {\n\t// CHANGE CODE BELOW THIS LINE\n\tvar c Counter\n\treturn func(_ *Counter, d int) { c.Add(d) }\n\t// CHANGE CODE ABOVE THIS LINE\n}\n",
    "debug": "",
    "file": "methodexpr.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/staff/nilslicedata": {
    "id": "01-language-basics/05-pointers/staff/nilslicedata",
    "title": "SliceData of Empty Slice",
    "level": "staff",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">SliceData of Empty Slice</h1><div class=\"p-meta\"><span class=\"pill lv-staff\">staff</span></div></div><h2>Context</h2>\n<p>For an empty slice <code>unsafe.SliceData(s)</code> may be nil; dereferencing it panics.</p>\n<p>Guard on <code>len(s) == 0</code> before reading the element.</p>\n<h2>Task</h2>\n<p>Fix nilslicedata.go so an empty slice returns the default.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  FirstOr(nil, 7)\nOutput: 7\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  FirstOr([]int{5}, 7)\nOutput: 5\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  FirstOr([]int{}, 7)\nOutput: 7\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Empty guard</span><span class=\"tchip\">SliceData may be nil</span><span class=\"tchip\">Default</span></div></details>",
    "education": "<h1>Data pointers of empty slices</h1>\n<h2>Intuition</h2>\n<p><code>unsafe.SliceData</code> of an empty slice can be nil; you must check the length before dereferencing it, just like any pointer.</p>\n<h2>Approach</h2>\n<p>1. <code>unsafe.SliceData</code> of an empty/nil slice yields a nil pointer; dereferencing panics.</p>\n<p>2. Guard <code>len(s) == 0</code> before reading.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nimport \"unsafe\"\n\nfunc FirstOr(s []int, def int) int {\n\tif len(s) == 0 {\n\t\treturn def\n\t}\n\treturn *unsafe.SliceData(s)\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>FirstOr(nil, 7)</code> should return the default, but <code>*SliceData(nil)</code> dereferences nil. The length guard returns <code>def</code> safely.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Empty slices may yield a nil data pointer.</li>\n<li>Guard <code>len(s) == 0</code> before reading element 0.</li>\n</ul>",
    "starter": "// Package nilslicedata returns the first element's value or a default for an\n// empty slice, using unsafe.SliceData. A planted bug dereferences the data\n// pointer without checking length; for an empty slice the data pointer may be\n// nil, panicking.\npackage nilslicedata\n\nimport \"unsafe\"\n\n// FirstOr returns s[0] via the data pointer, or def when s is empty.\nfunc FirstOr(s []int, def int) int {\n\t// CHANGE CODE BELOW THIS LINE\n\treturn *unsafe.SliceData(s)\n\t// CHANGE CODE ABOVE THIS LINE\n}\n",
    "debug": "",
    "file": "nilslicedata.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/staff/nilunsafe": {
    "id": "01-language-basics/05-pointers/staff/nilunsafe",
    "title": "Nil Check Before Unsafe Read",
    "level": "staff",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Nil Check Before Unsafe Read</h1><div class=\"p-meta\"><span class=\"pill lv-staff\">staff</span></div></div><h2>Context</h2>\n<p>Even with unsafe pointers, a nil check must PRECEDE the dereference. Reading</p>\n<p><code><em>(</em>int)(p)</code> first panics when p is nil.</p>\n<h2>Task</h2>\n<p>Fix nilunsafe.go so a nil pointer returns the default without panicking.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  ReadOr(nil, 7)\nOutput: 7\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  x := 5; ReadOr(unsafe.Pointer(&amp;x), 7)\nOutput: 5\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  ReadOr(nil, 0)\nOutput: 0\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Guard before deref</span><span class=\"tchip\">Order of operations</span><span class=\"tchip\">Default on nil</span></div></details>",
    "education": "<h1>Nil guards apply to unsafe pointers too</h1>\n<h2>Intuition</h2>\n<p><code>unsafe.Pointer</code> can be nil; dereferencing it panics like any nil pointer, so the guard must come before the read.</p>\n<h2>Approach</h2>\n<p>1. The bug dereferences <code>p</code> before the nil check \u2192 panic.</p>\n<p>2. Check <code>p == nil</code> first, then read.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nimport \"unsafe\"\n\nfunc ReadOr(p unsafe.Pointer, def int) int {\n\tif p == nil {\n\t\treturn def\n\t}\n\treturn *(*int)(p)\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>ReadOr(nil, 7)</code> should return the default, but reading <code><em>(</em>int)(p)</code> up front crashes. The nil guard must precede the load.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Reorder: nil check, then dereference.</li>\n<li>unsafe doesn't exempt you from nil safety.</li>\n</ul>",
    "starter": "// Package nilunsafe reads an int through an unsafe pointer, defaulting on nil. A\n// planted bug reads before the nil check, dereferencing a nil pointer.\npackage nilunsafe\n\nimport \"unsafe\"\n\n// ReadOr returns *(*int)(p), or def when p is nil.\nfunc ReadOr(p unsafe.Pointer, def int) int {\n\t// CHANGE CODE BELOW THIS LINE\n\tv := *(*int)(p)\n\tif p == nil {\n\t\treturn def\n\t}\n\treturn v\n\t// CHANGE CODE ABOVE THIS LINE\n}\n",
    "debug": "",
    "file": "nilunsafe.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/staff/offsetpad": {
    "id": "01-language-basics/05-pointers/staff/offsetpad",
    "title": "Field Offset With Padding",
    "level": "staff",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Field Offset With Padding</h1><div class=\"p-meta\"><span class=\"pill lv-staff\">staff</span></div></div><h2>Context</h2>\n<p><code>int64</code> must be 8-byte aligned, so <code>N</code> sits at offset 8, not 1 \u2014 there are 7</p>\n<p>padding bytes after the bool. Use <code>unsafe.Offsetof(r.N)</code> to get the true offset.</p>\n<h2>Task</h2>\n<p>Fix offsetpad.go to use the real field offset.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  ReadN(&amp;Rec{true, 123})\nOutput: 123\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  ReadN(&amp;Rec{false, 0})\nOutput: 0\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  ReadN(&amp;Rec{true, -7})\nOutput: -7\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Alignment padding</span><span class=\"tchip\">Offsetof</span><span class=\"tchip\">Not Sizeof of prior fields</span></div></details>",
    "education": "<h1>Padding and field offsets</h1>\n<h2>Intuition</h2>\n<p>Fields are placed at aligned offsets, so a field's position is not the sum of prior field sizes; <code>Offsetof</code> reports the true, padded offset.</p>\n<h2>Approach</h2>\n<p>1. <code>Sizeof(r.Flag)</code> ignores the padding before <code>N</code>.</p>\n<p>2. Use <code>unsafe.Offsetof(r.N)</code> to get N's true offset.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nimport \"unsafe\"\n\ntype Rec struct {\n\tFlag bool\n\tN    int64\n}\n\nfunc ReadN(r *Rec) int64 {\n\tbase := unsafe.Pointer(r)\n\toff := unsafe.Offsetof(r.N)\n\treturn *(*int64)(unsafe.Add(base, off))\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>A <code>bool</code> then an <code>int64</code> inserts 7 padding bytes; the size of the bool (1) is not the offset of N (8). <code>Offsetof</code> accounts for the padding.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>N</code> follows 1 bool + 7 padding bytes -&gt; offset 8.</li>\n<li><code>Offsetof(r.N)</code> gives the correct position.</li>\n</ul>",
    "starter": "// Package offsetpad reads the int64 field that follows a bool, through unsafe.\n// A planted bug assumes the field sits at offset 1 (right after the bool),\n// ignoring the 7 bytes of alignment padding.\npackage offsetpad\n\nimport \"unsafe\"\n\ntype Rec struct {\n\tFlag bool\n\tN    int64\n}\n\n// ReadN returns r.N read via an unsafe offset from the struct base.\nfunc ReadN(r *Rec) int64 {\n\tbase := unsafe.Pointer(r)\n\t// CHANGE CODE BELOW THIS LINE\n\toff := unsafe.Sizeof(r.Flag)\n\t// CHANGE CODE ABOVE THIS LINE\n\treturn *(*int64)(unsafe.Add(base, off))\n}\n",
    "debug": "",
    "file": "offsetpad.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/staff/poolreset": {
    "id": "01-language-basics/05-pointers/staff/poolreset",
    "title": "Reset Forgets a Field",
    "level": "staff",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Reset Forgets a Field</h1><div class=\"p-meta\"><span class=\"pill lv-staff\">staff</span></div></div><h2>Context</h2>\n<p>Resetting for reuse must clear ALL state. Zeroing only Len leaves Data holding</p>\n<p>stale bytes; also truncate the slice with <code>b.Data = b.Data[:0]</code> (keeping capacity</p>\n<p>for reuse).</p>\n<h2>Task</h2>\n<p>Fix poolreset.go to fully reset the buffer.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Reset(&amp;Buf{Data: [5]byte, Len: 5})\nOutput: Len 0 and Data length 0\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  after Reset, b.Len\nOutput: 0\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  after Reset, len(b.Data slice)\nOutput: 0\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Reset all fields</span><span class=\"tchip\">Truncate keeping capacity</span><span class=\"tchip\">Reuse safety</span></div></details>",
    "education": "<h1>Fully resetting pooled objects</h1>\n<h2>Intuition</h2>\n<p>Object-pool reuse requires clearing every field; truncating a slice with <code>[:0]</code> keeps its capacity for reuse while dropping the length.</p>\n<h2>Approach</h2>\n<p>1. Resetting must clear both the length counter and the data view.</p>\n<p>2. The bug sets only <code>b.Len = 0</code>; also <code>b.Data = b.Data[:0]</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\ntype Buf struct {\n\tData []byte\n\tLen  int\n}\n\nfunc Reset(b *Buf) {\n\tb.Len = 0\n\tb.Data = b.Data[:0]\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>Leaving the data slice full keeps stale bytes reachable. Reslicing <code>b.Data[:0]</code> alongside <code>Len = 0</code> fully resets the buffer.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>Zeroing one field leaves the rest stale.</li>\n<li><code>b.Data[:0]</code> empties the slice but keeps its backing array for reuse.</li>\n</ul>",
    "starter": "// Package poolreset resets a pooled buffer for reuse. A planted bug clears only\n// the length, leaving the data slice non-empty, so a reused buffer keeps stale\n// contents.\npackage poolreset\n\ntype Buf struct {\n\tData []byte\n\tLen  int\n}\n\n// Reset prepares b for reuse: Len must be 0 AND Data must be emptied (length 0,\n// keeping capacity).\nfunc Reset(b *Buf) {\n\t// CHANGE CODE BELOW THIS LINE\n\tb.Len = 0\n\t// CHANGE CODE ABOVE THIS LINE\n}\n",
    "debug": "",
    "file": "poolreset.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/staff/ptrfieldsize": {
    "id": "01-language-basics/05-pointers/staff/ptrfieldsize",
    "title": "Struct With Pointer Field Size",
    "level": "staff",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Struct With Pointer Field Size</h1><div class=\"p-meta\"><span class=\"pill lv-staff\">staff</span></div></div><h2>Context</h2>\n<p>A pointer is 8 bytes; a struct of two pointers is 16. Measuring one pointer</p>\n<p>returns 8. Measure the struct: <code>unsafe.Sizeof(Pair{})</code>.</p>\n<h2>Task</h2>\n<p>Fix ptrfieldsize.go to return the struct size.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Size()\nOutput: 16\n</code></pre>\n<p>_Explanation:_ A struct of two 8-byte pointers.</p>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Sizeof(Pair{})\nOutput: 16\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Sizeof((*int)(nil))\nOutput: 8 (wrong answer)\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Pointer size</span><span class=\"tchip\">Struct of pointers</span><span class=\"tchip\">Measure the struct</span></div></details>",
    "education": "<h1>Sizing pointer-holding structs</h1>\n<h2>Intuition</h2>\n<p>Each pointer field is word-sized; the struct's size is the sum (plus any padding). Measure the struct type, not a single pointer.</p>\n<h2>Approach</h2>\n<p>1. The bug measures a single pointer (8), not the struct.</p>\n<p>2. Measure the struct: <code>unsafe.Sizeof(Pair{})</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nimport \"unsafe\"\n\ntype Pair struct {\n\tA *int\n\tB *int\n}\n\nfunc Size() uintptr {\n\treturn unsafe.Sizeof(Pair{})\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>Sizeof((*int)(nil))</code> is one pointer word. The <code>Pair</code> holds two pointer fields, so its size is 16.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>One pointer is 8 bytes; two make 16.</li>\n<li><code>Sizeof(Pair{})</code> measures the whole struct.</li>\n</ul>",
    "starter": "// Package ptrfieldsize returns the size of a struct that holds two pointers. A\n// planted bug measures a single pointer instead of the whole struct.\npackage ptrfieldsize\n\nimport \"unsafe\"\n\ntype Pair struct {\n\tA *int\n\tB *int\n}\n\n// Size returns the byte size of Pair (two 8-byte pointers = 16 on 64-bit).\nfunc Size() uintptr {\n\t// CHANGE CODE BELOW THIS LINE\n\treturn unsafe.Sizeof((*int)(nil))\n\t// CHANGE CODE ABOVE THIS LINE\n}\n",
    "debug": "",
    "file": "ptrfieldsize.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/staff/retainbacking": {
    "id": "01-language-basics/05-pointers/staff/retainbacking",
    "title": "Sub-slice Retains Full Array",
    "level": "staff",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Sub-slice Retains Full Array</h1><div class=\"p-meta\"><span class=\"pill lv-staff\">staff</span></div></div><h2>Context</h2>\n<p><code>xs[:k]</code> keeps the parent's capacity, so appending to the prefix writes into</p>\n<p><code>xs[k]</code>. A full-slice expression <code>xs[:k:k]</code> caps capacity to k, isolating appends</p>\n<p>and letting the tail be garbage-collected.</p>\n<h2>Task</h2>\n<p>Fix retainbacking.go so appends to the prefix don't touch the parent.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  p := Prefix([1 2 3 4 5], 2); append(p, 99)\nOutput: xs[2] stays 3\n</code></pre>\n<p>_Explanation:_ Full-slice bound stops append from writing into xs.</p>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  len(Prefix(xs, 2))\nOutput: 2\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  cap(Prefix(xs, 2))\nOutput: 2\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Three-index slice</span><span class=\"tchip\">Capacity sharing</span><span class=\"tchip\">GC + safety</span></div></details>",
    "education": "<h1>Full-slice expressions and retention</h1>\n<h2>Intuition</h2>\n<p><code>s[:k:k]</code> limits capacity so appends reallocate instead of spilling; it also lets the unused tail be collected once the parent is gone.</p>\n<h2>Approach</h2>\n<p>1. <code>xs[:k]</code> keeps the original capacity, so appending overwrites <code>xs[k]</code>.</p>\n<p>2. Use a full-slice expression <code>xs[:k:k]</code> to cap capacity at k.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Prefix(xs []int, k int) []int {\n\treturn xs[:k:k]\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>With <code>xs[:2]</code> the capacity is still 5, so <code>append</code> writes into <code>xs[2]</code>. <code>xs[:2:2]</code> forces a reallocation on append, protecting <code>xs</code>.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>xs[:k]</code> keeps cap == cap(xs); appends spill into the parent.</li>\n<li><code>xs[:k:k]</code> (or a copy) isolates the prefix.</li>\n</ul>",
    "starter": "// Package retainbacking extracts a small prefix to keep long-term. A planted bug\n// returns a plain re-slice, which keeps the whole backing array alive AND lets\n// an append spill into the parent. The test detects the spill.\npackage retainbacking\n\n// Prefix returns the first k elements as a slice whose appends cannot reach the\n// parent's remaining elements.\nfunc Prefix(xs []int, k int) []int {\n\t// CHANGE CODE BELOW THIS LINE\n\treturn xs[:k]\n\t// CHANGE CODE ABOVE THIS LINE\n}\n",
    "debug": "",
    "file": "retainbacking.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/staff/shrinkretain": {
    "id": "01-language-basics/05-pointers/staff/shrinkretain",
    "title": "Shrinking Retains Pointers",
    "level": "staff",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Shrinking Retains Pointers</h1><div class=\"p-meta\"><span class=\"pill lv-staff\">staff</span></div></div><h2>Context</h2>\n<p>Re-slicing with <code>s[:last]</code> shrinks the length but leaves the popped pointer in</p>\n<p>the backing array, keeping the object alive. Nil the slot first: <code>s[last] = nil</code>.</p>\n<h2>Task</h2>\n<p>Fix shrinkretain.go to clear the vacated slot before shrinking.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  s := []*T{a, b}; Pop(&amp;s)\nOutput: backing slot nilled, len 1\n</code></pre>\n<p>_Explanation:_ Nil the dropped slot so the object can be GC'd.</p>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  len after Pop\nOutput: len-1\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  s[last] before reslice\nOutput: nil\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Retention via backing array</span><span class=\"tchip\">Clear before shrink</span><span class=\"tchip\">GC eligibility</span></div></details>",
    "education": "<h1>Clearing dropped pointer slots</h1>\n<h2>Intuition</h2>\n<p>Length reduction leaves stale pointers in the backing array; for pointer/interface elements you must nil the vacated slots or the referents leak.</p>\n<h2>Approach</h2>\n<p>1. Just reslicing <code>s[:last]</code> leaves the popped pointer live in the backing array, leaking it.</p>\n<p>2. Nil the slot first: <code>s[last] = nil</code>, then <code>*sp = s[:last]</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nfunc Pop(sp *[]*int) *int {\n\ts := *sp\n\tlast := len(s) - 1\n\tv := s[last]\n\ts[last] = nil\n\t*sp = s[:last]\n\treturn v\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>The bug keeps the last element reachable through the array's capacity, preventing collection. Nilling the slot before shrinking releases it.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>s[:last]</code> keeps the old element in the array's spare capacity.</li>\n<li>Set <code>s[last] = nil</code> before shrinking for pointer element types.</li>\n</ul>",
    "starter": "// Package shrinkretain shrinks a slice of pointers by one from the end. A planted\n// bug only re-slices, leaving the dropped element's pointer live in the backing\n// array (a retention leak). It must nil the dropped slot before shrinking.\npackage shrinkretain\n\n// Pop removes the last element of a *[]*int, returning it, and must clear the\n// vacated slot so the popped object can be garbage-collected.\nfunc Pop(sp *[]*int) *int {\n\ts := *sp\n\tlast := len(s) - 1\n\tv := s[last]\n\t// CHANGE CODE BELOW THIS LINE\n\t*sp = s[:last]\n\t// CHANGE CODE ABOVE THIS LINE\n\treturn v\n}\n",
    "debug": "",
    "file": "shrinkretain.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/staff/sizeofarray": {
    "id": "01-language-basics/05-pointers/staff/sizeofarray",
    "title": "Size of Whole Array",
    "level": "staff",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Size of Whole Array</h1><div class=\"p-meta\"><span class=\"pill lv-staff\">staff</span></div></div><h2>Context</h2>\n<p><code>unsafe.Sizeof(p[0])</code> is one element (4 bytes). The whole array is</p>\n<p><code>unsafe.Sizeof(<em>p)</code> = 4 </em> 4 = 16 bytes.</p>\n<h2>Task</h2>\n<p>Fix sizeofarray.go to return the whole array's size.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  TotalSize(&amp;[4]int32{})\nOutput: 16\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  TotalSize(&amp;[2]int64{})\nOutput: 16\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  TotalSize(&amp;[1]byte{})\nOutput: 1\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Array size</span><span class=\"tchip\">Element vs array</span><span class=\"tchip\">Fixed length in the type</span></div></details>",
    "education": "<h1>Sizeof over arrays</h1>\n<h2>Intuition</h2>\n<p>An array's size is length times element size and is a compile-time constant; measuring an element gives only one slot.</p>\n<h2>Approach</h2>\n<p>1. <code>Sizeof(p[0])</code> is one element; the whole array is <code>Sizeof(*p)</code>.</p>\n<p>2. Dereference the pointer to measure the array.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nimport \"unsafe\"\n\nfunc TotalSize(p *[4]int32) uintptr {\n\treturn unsafe.Sizeof(*p)\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>For <code>[4]int32</code>, <code>Sizeof(p[0])</code> is 4. <code>Sizeof(*p)</code> measures all four elements \u2192 16.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>Sizeof(*p)</code> is the array; <code>Sizeof(p[0])</code> is one element.</li>\n<li>Arrays carry their length in the type, so Sizeof knows the total.</li>\n</ul>",
    "starter": "// Package sizeofarray returns the total byte size of a [4]int32 array. A planted\n// bug measures one element instead of the whole array.\npackage sizeofarray\n\nimport \"unsafe\"\n\n// TotalSize returns the byte size of the entire array p points to.\nfunc TotalSize(p *[4]int32) uintptr {\n\t// CHANGE CODE BELOW THIS LINE\n\treturn unsafe.Sizeof(p[0])\n\t// CHANGE CODE ABOVE THIS LINE\n}\n",
    "debug": "",
    "file": "sizeofarray.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/staff/sizeofptr": {
    "id": "01-language-basics/05-pointers/staff/sizeofptr",
    "title": "Sizeof Pointer vs Pointee",
    "level": "staff",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Sizeof Pointer vs Pointee</h1><div class=\"p-meta\"><span class=\"pill lv-staff\">staff</span></div></div><h2>Context</h2>\n<p><code>unsafe.Sizeof(p)</code> is the size of the POINTER (8 on 64-bit), not what it points</p>\n<p>to. Measure an element: <code>unsafe.Sizeof(p[0])</code> (or <code>(*p)[0]</code>).</p>\n<h2>Task</h2>\n<p>Fix sizeofptr.go to return the element size.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  ElemSize(&amp;[8]int32{})\nOutput: 4\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  ElemSize(&amp;[8]int64{})\nOutput: 8\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  ElemSize(&amp;[2]byte{})\nOutput: 1\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Sizeof is static</span><span class=\"tchip\">Pointer vs pointee</span><span class=\"tchip\">Dereference to measure</span></div></details>",
    "education": "<h1>What unsafe.Sizeof measures</h1>\n<h2>Intuition</h2>\n<p><code>Sizeof</code> returns the size of its operand's type; a pointer is always word-sized regardless of the pointee. Measure the element expression instead.</p>\n<h2>Approach</h2>\n<p>1. <code>unsafe.Sizeof(p)</code> is the size of the <strong>pointer</strong> (8 on 64-bit), not the element.</p>\n<p>2. Measure an element: <code>unsafe.Sizeof(p[0])</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nimport \"unsafe\"\n\nfunc ElemSize(p *[8]int32) uintptr {\n\treturn unsafe.Sizeof(p[0])\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>The bug returns 8 for every pointer. <code>p[0]</code> is an <code>int32</code>, so its size is 4.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>Sizeof(p)</code> is 8 (a pointer); <code>Sizeof(p[0])</code> is the element size.</li>\n<li>Sizeof is a compile-time constant of the type.</li>\n</ul>",
    "starter": "// Package sizeofptr returns the size of the pointed-to array's element type. A\n// planted bug measures the POINTER (8 bytes) instead of the element it points to.\npackage sizeofptr\n\nimport \"unsafe\"\n\n// ElemSize returns the size in bytes of a single element of the array p points\n// to.\nfunc ElemSize(p *[8]int32) uintptr {\n\t// CHANGE CODE BELOW THIS LINE\n\treturn unsafe.Sizeof(p)\n\t// CHANGE CODE ABOVE THIS LINE\n}\n",
    "debug": "",
    "file": "sizeofptr.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/staff/sizeorder": {
    "id": "01-language-basics/05-pointers/staff/sizeorder",
    "title": "Field Order and Struct Size",
    "level": "staff",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Field Order and Struct Size</h1><div class=\"p-meta\"><span class=\"pill lv-staff\">staff</span></div></div><h2>Context</h2>\n<p>Each field aligns to its size; the compiler inserts padding. <code>bool,int64,bool</code></p>\n<p>pads to 24; ordering widest-first <code>int64,bool,bool</code> packs to 16.</p>\n<h2>Task</h2>\n<p>Fix the field order in sizeorder.go so <code>Record</code> is 16 bytes.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  unsafe.Sizeof(Record{})\nOutput: 16\n</code></pre>\n<p>_Explanation:_ Fields ordered wide-to-narrow pack without padding waste.</p>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  fields A bool, B int64, C bool\nOutput: 24 (padded)\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  fields B int64, A bool, C bool\nOutput: 16 (packed)\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Alignment and padding</span><span class=\"tchip\">Widest-first ordering</span><span class=\"tchip\">unsafe.Sizeof</span></div></details>",
    "education": "<h1>Struct layout and padding</h1>\n<h2>Intuition</h2>\n<p>The compiler pads fields to satisfy alignment and rounds the struct to its largest alignment; ordering fields widest-first minimises wasted padding.</p>\n<h2>Approach</h2>\n<p>1. Struct fields are laid out in declaration order with alignment padding.</p>\n<p>2. <code>bool, int64, bool</code> forces 7 bytes of padding twice.</p>\n<p>3. Reorder to <code>int64, bool, bool</code> so the two bools share the tail.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\ntype Record struct {\n\tB int64\n\tA bool\n\tC bool\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>With <code>A bool, B int64, C bool</code>, the int64 must start at offset 8, wasting padding after A, then C pads to 24. Putting <code>B int64</code> first packs A and C into one 8-byte tail \u2192 16.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>bool,int64,bool</code> -&gt; 24 bytes; <code>int64,bool,bool</code> -&gt; 16.</li>\n<li>Alignment is platform-dependent; this targets 64-bit.</li>\n</ul>",
    "starter": "// Package sizeorder defines a record whose field order must minimise padding to\n// 16 bytes on a 64-bit platform. A planted bug orders fields bool,int64,bool,\n// which pads to 24. Reorder to int64,bool,bool for 16.\npackage sizeorder\n\n// Record must be exactly 16 bytes wide (64-bit).\n// CHANGE CODE BELOW THIS LINE\ntype Record struct {\n\tA bool\n\tB int64\n\tC bool\n}\n\n// CHANGE CODE ABOVE THIS LINE\n",
    "debug": "",
    "file": "sizeorder.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/staff/slicedata": {
    "id": "01-language-basics/05-pointers/staff/slicedata",
    "title": "Address of Slice Data",
    "level": "staff",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Address of Slice Data</h1><div class=\"p-meta\"><span class=\"pill lv-staff\">staff</span></div></div><h2>Context</h2>\n<p><code>&amp;s</code> is the address of the slice HEADER (ptr,len,cap), not its data. To reach</p>\n<p>the first element take <code>&amp;s[0]</code> (equivalently <code>unsafe.SliceData(s)</code>).</p>\n<h2>Task</h2>\n<p>Fix slicedata.go to point at the element data.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  s := []int{1,2,3}; SetFirst(s, 42)\nOutput: s[0] == 42\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  SetFirst([]int{9}, 0)\nOutput: s[0] == 0\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  SetFirst(s, 7)\nOutput: s[0] == 7\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Slice header vs data</span><span class=\"tchip\">Element address</span><span class=\"tchip\">unsafe write</span></div></details>",
    "education": "<h1>Slice header vs backing data</h1>\n<h2>Intuition</h2>\n<p>A slice value is a header; <code>&amp;s</code> addresses that header, while <code>&amp;s[0]</code> (or <code>unsafe.SliceData</code>) addresses the actual data.</p>\n<h2>Approach</h2>\n<p>1. <code>&amp;s</code> is the address of the slice <strong>header</strong>, not the data.</p>\n<p>2. Point at the first element: <code>unsafe.Pointer(&amp;s[0])</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nimport \"unsafe\"\n\nfunc SetFirst(s []int, v int) {\n\tp := unsafe.Pointer(&amp;s[0])\n\t*(*int)(p) = v\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>Writing through <code>&amp;s</code> corrupts the header. <code>&amp;s[0]</code> addresses the backing array, so the store updates <code>s[0]</code>.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>&amp;s</code> writes over the slice header fields, not the elements.</li>\n<li><code>&amp;s[0]</code> is the data pointer.</li>\n</ul>",
    "starter": "// Package slicedata writes to a slice's first element through an unsafe pointer\n// to its data. A planted bug takes the address of the slice HEADER variable\n// instead of its first element.\npackage slicedata\n\nimport \"unsafe\"\n\n// SetFirst sets s[0] = v by writing through an unsafe pointer to the element.\n// s is non-empty.\nfunc SetFirst(s []int, v int) {\n\t// CHANGE CODE BELOW THIS LINE\n\tp := unsafe.Pointer(&s)\n\t// CHANGE CODE ABOVE THIS LINE\n\t*(*int)(p) = v\n}\n",
    "debug": "",
    "file": "slicedata.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/staff/strbytes": {
    "id": "01-language-basics/05-pointers/staff/strbytes",
    "title": "String Length From Header",
    "level": "staff",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">String Length From Header</h1><div class=\"p-meta\"><span class=\"pill lv-staff\">staff</span></div></div><h2>Context</h2>\n<p>A string header is (Data pointer, Len). The bug reads the Data word (an</p>\n<p>address); the length is the second word, <code>h.Len</code>.</p>\n<h2>Task</h2>\n<p>Fix strbytes.go to read the length field.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  ByteLen(\"hello\")\nOutput: 5\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  ByteLen(\"h\u00e9llo\")\nOutput: 6\n</code></pre>\n<p>_Explanation:_ The accented \u00e9 is two UTF-8 bytes.</p>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  ByteLen(\"\")\nOutput: 0\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">String header layout</span><span class=\"tchip\">Length word</span><span class=\"tchip\">Data vs length</span></div></details>",
    "education": "<h1>String header fields</h1>\n<h2>Intuition</h2>\n<p>A string is a two-word header: a data pointer and an int length. Reading the wrong word returns an address instead of the length.</p>\n<h2>Approach</h2>\n<p>1. The byte length is the header's <code>Len</code> field.</p>\n<p>2. The bug returns the data pointer as an int; return <code>h.Len</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nimport \"unsafe\"\n\ntype strHeader struct {\n\tData unsafe.Pointer\n\tLen  int\n}\n\nfunc ByteLen(s string) int {\n\th := (*strHeader)(unsafe.Pointer(&amp;s))\n\treturn h.Len\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>uintptr(h.Data)</code> is an address, not a length. <code>h.Len</code> is the string's byte count, so <code>\"h\u00e9llo\"</code> reports 6.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>The first word is the data pointer; the second is the length.</li>\n<li><code>len(s)</code> is the safe, idiomatic way; this exercises the header layout.</li>\n</ul>",
    "starter": "// Package strbytes reads a string's length from its header via unsafe, without\n// calling len. A planted bug reads the DATA pointer word instead of the LENGTH\n// word of the header.\npackage strbytes\n\nimport \"unsafe\"\n\ntype strHeader struct {\n\tData unsafe.Pointer\n\tLen  int\n}\n\n// ByteLen returns len(s) by reading the string header fields directly.\nfunc ByteLen(s string) int {\n\th := (*strHeader)(unsafe.Pointer(&s))\n\t// CHANGE CODE BELOW THIS LINE\n\treturn int(uintptr(h.Data))\n\t// CHANGE CODE ABOVE THIS LINE\n}\n",
    "debug": "",
    "file": "strbytes.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/staff/stringbyte": {
    "id": "01-language-basics/05-pointers/staff/stringbyte",
    "title": "First Byte of a String",
    "level": "staff",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">First Byte of a String</h1><div class=\"p-meta\"><span class=\"pill lv-staff\">staff</span></div></div><h2>Context</h2>\n<p><code>&amp;s</code> addresses the string HEADER (data ptr + len), not the characters. The data</p>\n<p>pointer is <code>unsafe.StringData(s)</code> (equivalently the first byte's address).</p>\n<h2>Task</h2>\n<p>Fix stringbyte.go to point at the string data.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  FirstByte(\"Xyz\")\nOutput: 'X' (88)\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  FirstByte(\"abc\")\nOutput: 'a' (97)\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  FirstByte(\"0\")\nOutput: '0' (48)\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">String header vs data</span><span class=\"tchip\">unsafe.StringData</span><span class=\"tchip\">Read-only</span></div></details>",
    "education": "<h1>String header vs data pointer</h1>\n<h2>Intuition</h2>\n<p>A string is a (data, len) header; <code>&amp;s</code> addresses that header, while <code>unsafe.StringData(s)</code> addresses the bytes.</p>\n<h2>Approach</h2>\n<p>1. <code>&amp;s</code> is the address of the string <strong>header</strong>, not its bytes.</p>\n<p>2. Use <code>unsafe.StringData(s)</code> to reach the first byte.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nimport \"unsafe\"\n\nfunc FirstByte(s string) byte {\n\tp := unsafe.Pointer(unsafe.StringData(s))\n\treturn *(*byte)(p)\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>Dereferencing <code>&amp;s</code> reads header words, not text. <code>unsafe.StringData(s)</code> points at the actual byte data, so the first byte is <code>'X'</code>.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>&amp;s</code> reads the header fields, not the characters.</li>\n<li><code>unsafe.StringData(s)</code> is the data pointer (bytes are immutable).</li>\n</ul>",
    "starter": "// Package stringbyte returns the first byte of a string via unsafe, without\n// allocating a []byte copy. A planted bug takes the address of the string HEADER\n// variable instead of its data.\npackage stringbyte\n\nimport \"unsafe\"\n\n// FirstByte returns s[0] read through the string's data pointer. s is non-empty.\nfunc FirstByte(s string) byte {\n\t// CHANGE CODE BELOW THIS LINE\n\tp := unsafe.Pointer(&s)\n\t// CHANGE CODE ABOVE THIS LINE\n\treturn *(*byte)(p)\n}\n",
    "debug": "",
    "file": "stringbyte.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/staff/structasarray": {
    "id": "01-language-basics/05-pointers/staff/structasarray",
    "title": "Reinterpret Struct as Array",
    "level": "staff",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Reinterpret Struct as Array</h1><div class=\"p-meta\"><span class=\"pill lv-staff\">staff</span></div></div><h2>Context</h2>\n<p>Reinterpreting <code><em>Pair</code> as <code></em>[2]int32</code> gives an array view whose elements are the</p>\n<p>two fields (same layout). The bug returns only <code>arr[0]</code>; sum both slots.</p>\n<h2>Task</h2>\n<p>Fix structasarray.go to sum both reinterpreted elements.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  Sum(&amp;Pair{3, 4})\nOutput: 7\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  Sum(&amp;Pair{0, 0})\nOutput: 0\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  Sum(&amp;Pair{-1, 5})\nOutput: 4\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Struct/array layout equivalence</span><span class=\"tchip\">Reinterpret view</span><span class=\"tchip\">Read all elements</span></div></details>",
    "education": "<h1>Reinterpreting structs as arrays</h1>\n<h2>Intuition</h2>\n<p>A struct of homogeneous fields has the same memory layout as an array of that type, so an unsafe reinterpret gives an indexable view of all fields.</p>\n<h2>Approach</h2>\n<p>1. Reinterpreting the struct as an array exposes both fields.</p>\n<p>2. The bug returns only <code>arr[0]</code>; add both: <code>arr[0] + arr[1]</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nimport \"unsafe\"\n\ntype Pair struct {\n\tA int32\n\tB int32\n}\n\nfunc Sum(p *Pair) int32 {\n\tarr := (*[2]int32)(unsafe.Pointer(p))\n\treturn arr[0] + arr[1]\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>Pair{3,4}</code> viewed as <code>[2]int</code> is <code>[3 4]</code>; summing both elements gives 7 rather than just 3.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>The reinterpreted array has one element per field.</li>\n<li>Reading only arr[0] ignores the rest of the struct.</li>\n</ul>",
    "starter": "// Package structasarray reads both fields of a two-int32 struct by viewing it as\n// a [2]int32 array through unsafe.Pointer. A planted bug reads only the first\n// element, ignoring the reinterpreted view's second slot.\npackage structasarray\n\nimport \"unsafe\"\n\ntype Pair struct {\n\tA int32\n\tB int32\n}\n\n// Sum returns A+B by reinterpreting the struct as a [2]int32 array.\nfunc Sum(p *Pair) int32 {\n\tarr := (*[2]int32)(unsafe.Pointer(p))\n\t// CHANGE CODE BELOW THIS LINE\n\treturn arr[0]\n\t// CHANGE CODE ABOVE THIS LINE\n}\n",
    "debug": "",
    "file": "structasarray.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/staff/structconv": {
    "id": "01-language-basics/05-pointers/staff/structconv",
    "title": "Convert Between Same-Layout Structs",
    "level": "staff",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Convert Between Same-Layout Structs</h1><div class=\"p-meta\"><span class=\"pill lv-staff\">staff</span></div></div><h2>Context</h2>\n<p>Two structs with identical field layout can be reinterpreted directly:</p>\n<p><code><em>(</em>Vec)(unsafe.Pointer(p))</code>. Building a Vec from only <code>p.X</code> drops <code>Y</code>.</p>\n<h2>Task</h2>\n<p>Fix structconv.go to reinterpret the whole struct.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  ToVec(&amp;Point{3, 4})\nOutput: {3 4}\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  ToVec(&amp;Point{0, 0})\nOutput: {0 0}\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  ToVec(&amp;Point{-1, 2})\nOutput: {-1 2}\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Same layout reinterpret</span><span class=\"tchip\">Whole struct</span><span class=\"tchip\">unsafe.Pointer bridge</span></div></details>",
    "education": "<h1>Reinterpreting compatible structs</h1>\n<h2>Intuition</h2>\n<p>Structs with identical memory layout can be converted with a single unsafe reinterpret, copying all fields at once.</p>\n<h2>Approach</h2>\n<p>1. Reinterpreting <code><em>Point</code> as <code></em>Vec</code> gives both fields at once.</p>\n<p>2. The bug rebuilds a <code>Vec</code> keeping only <code>X</code>; return the whole reinterpreted value.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nimport \"unsafe\"\n\ntype Point struct{ X, Y int32 }\ntype Vec struct{ X, Y int32 }\n\nfunc ToVec(p *Point) Vec {\n\treturn *(*Vec)(unsafe.Pointer(p))\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>Vec{X: v.X}</code> drops the Y field, yielding <code>{3 0}</code>. Returning <code><em>(</em>Vec)(unsafe.Pointer(p))</code> preserves both coordinates.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code><em>(</em>Vec)(unsafe.Pointer(p))</code> copies X and Y together.</li>\n<li>Rebuilding by hand risks dropping fields.</li>\n</ul>",
    "starter": "// Package structconv reinterprets a Point as a Vec (identical layout) via\n// unsafe. A planted bug builds a Vec from only the first field, dropping the\n// second.\npackage structconv\n\nimport \"unsafe\"\n\ntype Point struct{ X, Y int32 }\ntype Vec struct{ X, Y int32 }\n\n// ToVec reinterprets p as a *Vec (same layout) and returns its value.\nfunc ToVec(p *Point) Vec {\n\t// CHANGE CODE BELOW THIS LINE\n\tv := *(*Vec)(unsafe.Pointer(p))\n\treturn Vec{X: v.X}\n\t// CHANGE CODE ABOVE THIS LINE\n}\n",
    "debug": "",
    "file": "structconv.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/staff/unsafeslice": {
    "id": "01-language-basics/05-pointers/staff/unsafeslice",
    "title": "Build a Slice From a Pointer",
    "level": "staff",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Build a Slice From a Pointer</h1><div class=\"p-meta\"><span class=\"pill lv-staff\">staff</span></div></div><h2>Context</h2>\n<p><code>unsafe.Slice(ptr, n)</code> takes n as an ELEMENT count. <code>unsafe.Sizeof(*p)</code> is 16</p>\n<p>bytes, not 4 elements; pass the element count (4, or <code>len(p)</code>).</p>\n<h2>Task</h2>\n<p>Fix unsafeslice.go to use the element count.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  View(&amp;[4]int32{...})\nOutput: len 4\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  View(&amp;[1]int32{9})\nOutput: len 1\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  View(&amp;[4]int32{})[0] = 1\nOutput: aliases the array\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Element count</span><span class=\"tchip\">Bytes vs elements</span><span class=\"tchip\">Correct length</span></div></details>",
    "education": "<h1>unsafe.Slice length semantics</h1>\n<h2>Intuition</h2>\n<p><code>unsafe.Slice(ptr, n)</code> builds a slice of n ELEMENTS; passing a byte length creates an out-of-bounds view.</p>\n<h2>Approach</h2>\n<p>1. <code>unsafe.Slice(ptr, n)</code> takes an <strong>element count</strong>, not a byte size.</p>\n<p>2. The bug passes <code>Sizeof(*p)</code>; pass <code>len(p)</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nimport \"unsafe\"\n\nfunc View(p *[4]int32) []int32 {\n\treturn unsafe.Slice(&amp;p[0], len(p))\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p><code>Sizeof(*p)</code> is the array's byte size (16), producing a wildly oversized slice. <code>len(p)</code> yields the correct 4-element view.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>unsafe.Slice</code>'s length is in elements, not bytes.</li>\n<li>An over-long length yields a slice that reads past the array.</li>\n</ul>",
    "starter": "// Package unsafeslice builds a []int32 view over an array via unsafe.Slice. A\n// planted bug passes the length in BYTES instead of elements, creating an\n// over-long slice.\npackage unsafeslice\n\nimport \"unsafe\"\n\n// View returns a slice viewing all 4 elements of the array p points to.\nfunc View(p *[4]int32) []int32 {\n\t// CHANGE CODE BELOW THIS LINE\n\treturn unsafe.Slice(&p[0], unsafe.Sizeof(*p))\n\t// CHANGE CODE ABOVE THIS LINE\n}\n",
    "debug": "",
    "file": "unsafeslice.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/staff/unsafestride": {
    "id": "01-language-basics/05-pointers/staff/unsafestride",
    "title": "Pointer Arithmetic Stride",
    "level": "staff",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Pointer Arithmetic Stride</h1><div class=\"p-meta\"><span class=\"pill lv-staff\">staff</span></div></div><h2>Context</h2>\n<p><code>unsafe.Add(ptr, n)</code> advances by n BYTES. To step i elements you must multiply</p>\n<p>by the element size: <code>i * unsafe.Sizeof(arr[0])</code> (4 for int32).</p>\n<h2>Task</h2>\n<p>Fix the offset in unsafestride.go.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  At(&amp;[10 20 30 40], 2)\nOutput: 30\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  At(&amp;[10 20 30 40], 0)\nOutput: 10\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  At(&amp;[10 20 30 40], 3)\nOutput: 40\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Byte-based offset</span><span class=\"tchip\">Element stride</span><span class=\"tchip\">unsafe.Sizeof</span></div></details>",
    "education": "<h1>Pointer arithmetic in bytes</h1>\n<h2>Intuition</h2>\n<p><code>unsafe.Add</code> works in bytes; indexing into a typed array requires multiplying the index by the element size.</p>\n<h2>Approach</h2>\n<p>1. <code>unsafe.Add</code> advances by <strong>bytes</strong>, not elements.</p>\n<p>2. The bug adds <code>i</code> bytes; multiply by the element size.</p>\n<p>3. <code>unsafe.Add(base, uintptr(i)*unsafe.Sizeof(arr[0]))</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nimport \"unsafe\"\n\nfunc At(arr *[4]int32, i int) int32 {\n\tbase := unsafe.Pointer(arr)\n\tp := unsafe.Add(base, uintptr(i)*unsafe.Sizeof(arr[0]))\n\treturn *(*int32)(p)\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>For <code>i = 2</code> on <code>int</code>s, the byte offset must be <code>2 * 8 = 16</code>. Adding just <code>2</code> bytes lands mid-element and reads garbage.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li><code>unsafe.Add(base, i)</code> moves i bytes, not i int32s.</li>\n<li>Multiply by <code>unsafe.Sizeof(elem)</code> for element steps.</li>\n</ul>",
    "starter": "// Package unsafestride reads the i-th int32 of an array via unsafe pointer\n// arithmetic. A planted bug advances by i BYTES instead of i elements, reading\n// misaligned/wrong data.\npackage unsafestride\n\nimport \"unsafe\"\n\n// At returns arr[i] using unsafe.Add for the offset.\nfunc At(arr *[4]int32, i int) int32 {\n\tbase := unsafe.Pointer(arr)\n\t// CHANGE CODE BELOW THIS LINE\n\tp := unsafe.Add(base, i)\n\t// CHANGE CODE ABOVE THIS LINE\n\treturn *(*int32)(p)\n}\n",
    "debug": "",
    "file": "unsafestride.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  },
  "01-language-basics/05-pointers/staff/widthmismatch": {
    "id": "01-language-basics/05-pointers/staff/widthmismatch",
    "title": "Reinterpret With Wrong Width",
    "level": "staff",
    "tag": "backend",
    "description": "<div class=\"p-head\"><h1 class=\"p-title\">Reinterpret With Wrong Width</h1><div class=\"p-meta\"><span class=\"pill lv-staff\">staff</span></div></div><h2>Context</h2>\n<p>Reinterpreting an 8-byte int64 through <code>*uint32</code> reads only 4 bytes. The target</p>\n<p>type must match the source width: reinterpret through <code>*uint64</code>.</p>\n<h2>Task</h2>\n<p>Fix widthmismatch.go to read all 64 bits.</p>\n<p>Do <strong>not</strong> change the function signature or the tests.</p>\n<h2>Examples</h2>\n<p><strong>Example 1:</strong></p>\n<pre class=\"md\"><code>\nInput:  AsU64(-1)\nOutput: 0xffffffffffffffff\n</code></pre>\n<p><strong>Example 2:</strong></p>\n<pre class=\"md\"><code>\nInput:  AsU64(0)\nOutput: 0x0\n</code></pre>\n<p><strong>Example 3:</strong></p>\n<pre class=\"md\"><code>\nInput:  AsU64(1)\nOutput: 0x1\n</code></pre>\n<details class=\"topics-foot\"><summary class=\"topics-lbl\">Topics <span class=\"topics-n\">3</span></summary><div class=\"tchips\"><span class=\"tchip\">Matching widths</span><span class=\"tchip\">Truncated read</span><span class=\"tchip\">Correct target type</span></div></details>",
    "education": "<h1>Width matching in reinterprets</h1>\n<h2>Intuition</h2>\n<p>A reinterpret cast must use a target type the same size as the source; a narrower type reads only part of the memory.</p>\n<h2>Approach</h2>\n<p>1. The value is 64-bit; read 64 bits.</p>\n<p>2. The bug reads a <code>uint32</code>, dropping the high half.</p>\n<p>3. <code><em>(</em>uint64)(unsafe.Pointer(&amp;x))</code>.</p>\n<h2>Solution</h2>\n<pre class=\"md\"><code>\nimport \"unsafe\"\n\nfunc AsU64(x int64) uint64 {\n\treturn *(*uint64)(unsafe.Pointer(&amp;x))\n}\n</code></pre>\n<h2>Walkthrough</h2>\n<p>Reading only 32 bits of <code>-1</code> yields <code>0xffffffff</code>. The full 64-bit reinterpret gives <code>0xffffffffffffffff</code>.</p>\n<h2>Pitfalls</h2>\n<ul>\n<li>int64 is 8 bytes; <code>*uint32</code> reads only the low 4.</li>\n<li>Match widths: <code>*uint64</code> for an int64.</li>\n</ul>",
    "starter": "// Package widthmismatch reads a uint64 from an 8-byte value via unsafe. A planted\n// bug reinterprets through *uint32, reading only the low 4 bytes.\npackage widthmismatch\n\nimport \"unsafe\"\n\n// AsU64 returns the raw 64 bits of x reinterpreted as uint64.\nfunc AsU64(x int64) uint64 {\n\t// CHANGE CODE BELOW THIS LINE\n\treturn uint64(*(*uint32)(unsafe.Pointer(&x)))\n\t// CHANGE CODE ABOVE THIS LINE\n}\n",
    "debug": "",
    "file": "widthmismatch.go",
    "run": "",
    "runCustom": "",
    "customDefault": ""
  }
};

window.CATALOG = [
  {
    "topic": "01-language-basics \u00b7 Variables And Constants",
    "items": [
      {
        "id": "01-language-basics/01-variables-and-constants/junior/byteunits",
        "name": "Byte Units",
        "sub": "Variables And Constants",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/01-variables-and-constants/junior/discard",
        "name": "Discarded Remainder",
        "sub": "Variables And Constants",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/01-variables-and-constants/junior/endpoint",
        "name": "Derived Endpoint",
        "sub": "Variables And Constants",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/01-variables-and-constants/junior/plan-limits",
        "name": "Plan Rate Limits",
        "sub": "Variables And Constants",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/01-variables-and-constants/junior/retries",
        "name": "Retry Budget",
        "sub": "Variables And Constants",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/01-variables-and-constants/junior/shadow",
        "name": "Shadowed Tally",
        "sub": "Variables And Constants",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/01-variables-and-constants/junior/swap",
        "name": "Value Swap",
        "sub": "Variables And Constants",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/01-variables-and-constants/junior/temperature",
        "name": "Temperature Convert",
        "sub": "Variables And Constants",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/01-variables-and-constants/junior/typedconst",
        "name": "Typed Batch Limit",
        "sub": "Variables And Constants",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/01-variables-and-constants/junior/zerovalues",
        "name": "Zero Value Config",
        "sub": "Variables And Constants",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/01-variables-and-constants/middle/byte-shift",
        "name": "Binary Byte Units",
        "sub": "Variables And Constants",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/01-variables-and-constants/middle/clamp",
        "name": "Clamp to Range",
        "sub": "Variables And Constants",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/01-variables-and-constants/middle/frame-budget",
        "name": "Frame Budget",
        "sub": "Variables And Constants",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/01-variables-and-constants/middle/int-limits",
        "name": "Integer Limits",
        "sub": "Variables And Constants",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/01-variables-and-constants/middle/iota-perms",
        "name": "Permission Bits",
        "sub": "Variables And Constants",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/01-variables-and-constants/middle/pi-precision",
        "name": "High-Precision Pi",
        "sub": "Variables And Constants",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/01-variables-and-constants/middle/revoke",
        "name": "Revoke Permissions",
        "sub": "Variables And Constants",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/01-variables-and-constants/middle/score-tiers",
        "name": "Score Tiers",
        "sub": "Variables And Constants",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/01-variables-and-constants/middle/temp-scale",
        "name": "Typed Temperature Constants",
        "sub": "Variables And Constants",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/01-variables-and-constants/middle/weekday-iota",
        "name": "Weekday Enum",
        "sub": "Variables And Constants",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/01-variables-and-constants/senior/iota-offset",
        "name": "Off-By-One iota Flags",
        "sub": "Variables And Constants",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/01-variables-and-constants/senior/kb-shift",
        "name": "Byte-Scale Shift",
        "sub": "Variables And Constants",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/01-variables-and-constants/senior/maxint-signbit",
        "name": "Max Int Sign Bit",
        "sub": "Variables And Constants",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/01-variables-and-constants/senior/percent-order",
        "name": "Integer Percent Order",
        "sub": "Variables And Constants",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/01-variables-and-constants/senior/revoke-xor",
        "name": "XOR vs AND-NOT",
        "sub": "Variables And Constants",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/01-variables-and-constants/senior/rgb-pack",
        "name": "RGB Bit Packing",
        "sub": "Variables And Constants",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/01-variables-and-constants/senior/status-zero",
        "name": "Zero-Value Enum Trap",
        "sub": "Variables And Constants",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/01-variables-and-constants/senior/sum-shadow",
        "name": "Shadowed Accumulator",
        "sub": "Variables And Constants",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/01-variables-and-constants/senior/tier-threshold",
        "name": "Tier Threshold Off-By-One",
        "sub": "Variables And Constants",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/01-variables-and-constants/senior/uint-underflow",
        "name": "Unsigned Underflow",
        "sub": "Variables And Constants",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/01-variables-and-constants/staff/abs-minint",
        "name": "Abs of the Most-Negative",
        "sub": "Variables And Constants",
        "lv": "ST",
        "level": "staff",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/01-variables-and-constants/staff/fixed-cents",
        "name": "Fixed-Point Conversion Order",
        "sub": "Variables And Constants",
        "lv": "ST",
        "level": "staff",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/01-variables-and-constants/staff/float32-precision",
        "name": "Float Width & Precision",
        "sub": "Variables And Constants",
        "lv": "ST",
        "level": "staff",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/01-variables-and-constants/staff/hex-byte",
        "name": "Byte Extraction Stride",
        "sub": "Variables And Constants",
        "lv": "ST",
        "level": "staff",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/01-variables-and-constants/staff/iota-reset",
        "name": "iota Sequence Break",
        "sub": "Variables And Constants",
        "lv": "ST",
        "level": "staff",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/01-variables-and-constants/staff/kib-scale",
        "name": "Kibi vs Kilo",
        "sub": "Variables And Constants",
        "lv": "ST",
        "level": "staff",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/01-variables-and-constants/staff/minint-shift",
        "name": "MinInt Signed Shift",
        "sub": "Variables And Constants",
        "lv": "ST",
        "level": "staff",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/01-variables-and-constants/staff/overflow-mul",
        "name": "Widen Before Multiply",
        "sub": "Variables And Constants",
        "lv": "ST",
        "level": "staff",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/01-variables-and-constants/staff/signbit-mask",
        "name": "Sign Bit Mask",
        "sub": "Variables And Constants",
        "lv": "ST",
        "level": "staff",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/01-variables-and-constants/staff/untyped-ratio",
        "name": "Untyped Constant Division",
        "sub": "Variables And Constants",
        "lv": "ST",
        "level": "staff",
        "locked": false,
        "tag": "backend"
      }
    ]
  },
  {
    "topic": "01-language-basics \u00b7 Data Types",
    "items": [
      {
        "id": "01-language-basics/02-data-types/junior/almostequal",
        "name": "Float Almost-Equal",
        "sub": "Data Types",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/02-data-types/junior/average",
        "name": "Average",
        "sub": "Data Types",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/02-data-types/junior/bytecat",
        "name": "Strings and Bytes",
        "sub": "Data Types",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/02-data-types/junior/c2f",
        "name": "Celsius to Fahrenheit",
        "sub": "Data Types",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/02-data-types/junior/checksum",
        "name": "Checksum",
        "sub": "Data Types",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/02-data-types/junior/clockmod",
        "name": "24-Hour Clock",
        "sub": "Data Types",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/02-data-types/junior/complexabs",
        "name": "Complex Magnitude",
        "sub": "Data Types",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/02-data-types/junior/escapes",
        "name": "Escape Sequences",
        "sub": "Data Types",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/02-data-types/junior/evenodd",
        "name": "Integer Parity",
        "sub": "Data Types",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/02-data-types/junior/firstrune",
        "name": "First Rune",
        "sub": "Data Types",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/02-data-types/junior/hexdigit",
        "name": "Hex Digit",
        "sub": "Data Types",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/02-data-types/junior/majority",
        "name": "Boolean Majority",
        "sub": "Data Types",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/02-data-types/junior/nancheck",
        "name": "Finite Float Check",
        "sub": "Data Types",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/02-data-types/junior/narrowing",
        "name": "Narrowing Conversion",
        "sub": "Data Types",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/02-data-types/junior/quote",
        "name": "Quote a String",
        "sub": "Data Types",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/02-data-types/junior/rawpath",
        "name": "Raw String Path",
        "sub": "Data Types",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/02-data-types/junior/round2",
        "name": "Round to Decimals",
        "sub": "Data Types",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/02-data-types/junior/runecount",
        "name": "Rune Count",
        "sub": "Data Types",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/02-data-types/junior/truncate",
        "name": "Truncate Float",
        "sub": "Data Types",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/02-data-types/junior/vowelcount",
        "name": "Vowel Count",
        "sub": "Data Types",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/02-data-types/middle/atoi",
        "name": "Manual Atoi",
        "sub": "Data Types",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/02-data-types/middle/baseconv",
        "name": "Base Conversion",
        "sub": "Data Types",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/02-data-types/middle/caesar",
        "name": "Caesar Cipher",
        "sub": "Data Types",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/02-data-types/middle/clamp01",
        "name": "Saturate to [0,1]",
        "sub": "Data Types",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/02-data-types/middle/csvfield",
        "name": "CSV Field Quoting",
        "sub": "Data Types",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/02-data-types/middle/epsiloneq",
        "name": "Float Tolerance Equality",
        "sub": "Data Types",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/02-data-types/middle/gcd",
        "name": "Greatest Common Divisor",
        "sub": "Data Types",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/02-data-types/middle/hexencode",
        "name": "Hex Encode",
        "sub": "Data Types",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/02-data-types/middle/itoa",
        "name": "Manual Itoa",
        "sub": "Data Types",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/02-data-types/middle/lerp",
        "name": "Linear Interpolation",
        "sub": "Data Types",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/02-data-types/middle/luhn",
        "name": "Luhn Checksum",
        "sub": "Data Types",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/02-data-types/middle/moneyparse",
        "name": "Parse Money to Cents",
        "sub": "Data Types",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/02-data-types/middle/parsehex",
        "name": "Parse Hex",
        "sub": "Data Types",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/02-data-types/middle/popcount",
        "name": "Population Count",
        "sub": "Data Types",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/02-data-types/middle/reverserunes",
        "name": "Reverse by Runes",
        "sub": "Data Types",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/02-data-types/middle/rotate",
        "name": "Circular Bit Rotation",
        "sub": "Data Types",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/02-data-types/middle/semverparse",
        "name": "Semver Parse",
        "sub": "Data Types",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/02-data-types/middle/slugify",
        "name": "URL Slug",
        "sub": "Data Types",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/02-data-types/middle/titlecase",
        "name": "Title Case",
        "sub": "Data Types",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/02-data-types/middle/utf8truncate",
        "name": "Byte-Safe Truncate",
        "sub": "Data Types",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/02-data-types/senior/clampbyte",
        "name": "Saturating Pixel Clamp",
        "sub": "Data Types",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/02-data-types/senior/countwords",
        "name": "Robust Word Count",
        "sub": "Data Types",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/02-data-types/senior/csvsplit",
        "name": "Quoted CSV Split",
        "sub": "Data Types",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/02-data-types/senior/durationparse",
        "name": "Duration Minutes",
        "sub": "Data Types",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/02-data-types/senior/fletcher16",
        "name": "Fletcher-16 Modulus",
        "sub": "Data Types",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/02-data-types/senior/hexdecode",
        "name": "Hex Nibble Order",
        "sub": "Data Types",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/02-data-types/senior/ipv4parse",
        "name": "IPv4 Octet Range",
        "sub": "Data Types",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/02-data-types/senior/jsonescape",
        "name": "JSON String Escaping",
        "sub": "Data Types",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/02-data-types/senior/maskcard",
        "name": "Mask All But Last 4",
        "sub": "Data Types",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/02-data-types/senior/normspaces",
        "name": "Normalize Whitespace",
        "sub": "Data Types",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/02-data-types/senior/ordinal",
        "name": "English Ordinals",
        "sub": "Data Types",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/02-data-types/senior/padleft",
        "name": "Left Pad",
        "sub": "Data Types",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/02-data-types/senior/parsebool",
        "name": "Config Bool Parsing",
        "sub": "Data Types",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/02-data-types/senior/runeat",
        "name": "Rune Index Bounds",
        "sub": "Data Types",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/02-data-types/senior/runewidth",
        "name": "First Rune Byte Width",
        "sub": "Data Types",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/02-data-types/senior/splitlines",
        "name": "CRLF-Safe Line Split",
        "sub": "Data Types",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/02-data-types/senior/tolowerascii",
        "name": "ASCII Lowercase Range",
        "sub": "Data Types",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/02-data-types/senior/wordwrap",
        "name": "Word Wrap Width",
        "sub": "Data Types",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/02-data-types/staff/bankround",
        "name": "Banker's Rounding",
        "sub": "Data Types",
        "lv": "ST",
        "level": "staff",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/02-data-types/staff/base64val",
        "name": "Base64 Digit Offset",
        "sub": "Data Types",
        "lv": "ST",
        "level": "staff",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/02-data-types/staff/checkedadd",
        "name": "Checked Int64 Addition",
        "sub": "Data Types",
        "lv": "ST",
        "level": "staff",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/02-data-types/staff/endianswap",
        "name": "Byte-Order Swap",
        "sub": "Data Types",
        "lv": "ST",
        "level": "staff",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/02-data-types/staff/floatbits",
        "name": "Float64 Exponent Bias",
        "sub": "Data Types",
        "lv": "ST",
        "level": "staff",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/02-data-types/staff/fnv1a",
        "name": "FNV-1a Hash Order",
        "sub": "Data Types",
        "lv": "ST",
        "level": "staff",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/02-data-types/staff/graycode",
        "name": "Gray Code Encoding",
        "sub": "Data Types",
        "lv": "ST",
        "level": "staff",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/02-data-types/staff/parity",
        "name": "Parity Bit",
        "sub": "Data Types",
        "lv": "ST",
        "level": "staff",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/02-data-types/staff/rune2decode",
        "name": "Decode 2-Byte UTF-8",
        "sub": "Data Types",
        "lv": "ST",
        "level": "staff",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/02-data-types/staff/utf8count",
        "name": "Rune Count by Lead Bytes",
        "sub": "Data Types",
        "lv": "ST",
        "level": "staff",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/02-data-types/staff/utf8valid",
        "name": "UTF-8 Lead Byte Mask",
        "sub": "Data Types",
        "lv": "ST",
        "level": "staff",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/02-data-types/staff/varint",
        "name": "LEB128 Varint Shift",
        "sub": "Data Types",
        "lv": "ST",
        "level": "staff",
        "locked": false,
        "tag": "backend"
      }
    ]
  },
  {
    "topic": "01-language-basics \u00b7 Composite Types",
    "items": [
      {
        "id": "01-language-basics/03-composite-types/junior/arrayval",
        "name": "Array Value Semantics",
        "sub": "Composite Types",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/03-composite-types/junior/checkoutgrid",
        "name": "checkoutgrid \u2014 Seating Grid Marker",
        "sub": "Composite Types",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/03-composite-types/junior/concat",
        "name": "Concatenate Slices",
        "sub": "Composite Types",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/03-composite-types/junior/contains",
        "name": "Slice Contains",
        "sub": "Composite Types",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/03-composite-types/junior/copyslice",
        "name": "Independent Slice Copy",
        "sub": "Composite Types",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/03-composite-types/junior/counter",
        "name": "Frequency Counter",
        "sub": "Composite Types",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/03-composite-types/junior/dedupe",
        "name": "Slice Dedupe",
        "sub": "Composite Types",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/03-composite-types/junior/filterpos",
        "name": "Filter Positives",
        "sub": "Composite Types",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/03-composite-types/junior/flatten",
        "name": "Flatten a Grid",
        "sub": "Composite Types",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/03-composite-types/junior/getdefault",
        "name": "Map Get With Default",
        "sub": "Composite Types",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/03-composite-types/junior/gradebook",
        "name": "Gradebook Averages",
        "sub": "Composite Types",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/03-composite-types/junior/invert",
        "name": "Invert a Map",
        "sub": "Composite Types",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/03-composite-types/junior/keys",
        "name": "Sorted Map Keys",
        "sub": "Composite Types",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/03-composite-types/junior/lastn",
        "name": "Last N Elements",
        "sub": "Composite Types",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/03-composite-types/junior/lookup",
        "name": "Map Comma-Ok",
        "sub": "Composite Types",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/03-composite-types/junior/mapmerge",
        "name": "Merge Maps",
        "sub": "Composite Types",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/03-composite-types/junior/minmax",
        "name": "Min and Max",
        "sub": "Composite Types",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/03-composite-types/junior/point",
        "name": "Struct Point",
        "sub": "Composite Types",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/03-composite-types/junior/rectarea",
        "name": "Rectangle Struct",
        "sub": "Composite Types",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/03-composite-types/junior/removeindex",
        "name": "Remove by Index",
        "sub": "Composite Types",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/03-composite-types/junior/reverse",
        "name": "Reverse In Place",
        "sub": "Composite Types",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/03-composite-types/junior/rowsum",
        "name": "Row Sums",
        "sub": "Composite Types",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/03-composite-types/junior/structsum",
        "name": "Order Total",
        "sub": "Composite Types",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/03-composite-types/junior/structupdate",
        "name": "Mutate via Pointer Receiver",
        "sub": "Composite Types",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/03-composite-types/junior/subslice",
        "name": "Slice Copy Independence",
        "sub": "Composite Types",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/03-composite-types/junior/sumints",
        "name": "Sum a Slice",
        "sub": "Composite Types",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/03-composite-types/junior/uniquecount",
        "name": "Count Distinct",
        "sub": "Composite Types",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/03-composite-types/junior/wordfreq",
        "name": "Word Frequency",
        "sub": "Composite Types",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/03-composite-types/middle/anagramgroup",
        "name": "Count Anagram Groups",
        "sub": "Composite Types",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/03-composite-types/middle/chunk",
        "name": "Chunk a Slice",
        "sub": "Composite Types",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/03-composite-types/middle/dedupesorted",
        "name": "Dedupe Consecutive",
        "sub": "Composite Types",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/03-composite-types/middle/groupby",
        "name": "Group By First Letter",
        "sub": "Composite Types",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/03-composite-types/middle/groupstructs",
        "name": "Total by Customer",
        "sub": "Composite Types",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/03-composite-types/middle/histogram",
        "name": "Histogram Buckets",
        "sub": "Composite Types",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/03-composite-types/middle/insertat",
        "name": "Insert at Index",
        "sub": "Composite Types",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/03-composite-types/middle/joinmanual",
        "name": "Join with Separator",
        "sub": "Composite Types",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/03-composite-types/middle/matmul",
        "name": "Matrix Multiply",
        "sub": "Composite Types",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/03-composite-types/middle/mergesorted",
        "name": "Merge Sorted Slices",
        "sub": "Composite Types",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/03-composite-types/middle/mostcommon",
        "name": "Mode (Most Common)",
        "sub": "Composite Types",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/03-composite-types/middle/movezeros",
        "name": "Move Zeros",
        "sub": "Composite Types",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/03-composite-types/middle/partition",
        "name": "Partition by Predicate",
        "sub": "Composite Types",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/03-composite-types/middle/reverseinplace",
        "name": "Reverse In Place",
        "sub": "Composite Types",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/03-composite-types/middle/rotate",
        "name": "Rotate a Slice",
        "sub": "Composite Types",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/03-composite-types/middle/runlength",
        "name": "Run-Length Encode",
        "sub": "Composite Types",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/03-composite-types/middle/setdiff",
        "name": "Set Difference",
        "sub": "Composite Types",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/03-composite-types/middle/setintersect",
        "name": "Set Intersection",
        "sub": "Composite Types",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/03-composite-types/middle/setunion",
        "name": "Set Union",
        "sub": "Composite Types",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/03-composite-types/middle/sortstructs",
        "name": "Sort Structs by Field",
        "sub": "Composite Types",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/03-composite-types/middle/splitmanual",
        "name": "Split by Byte",
        "sub": "Composite Types",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/03-composite-types/middle/structfilter",
        "name": "Filter and Project",
        "sub": "Composite Types",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/03-composite-types/middle/takewhile",
        "name": "Take While Positive",
        "sub": "Composite Types",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/03-composite-types/middle/topk",
        "name": "Top K Frequent",
        "sub": "Composite Types",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/03-composite-types/middle/transpose",
        "name": "Matrix Transpose",
        "sub": "Composite Types",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/03-composite-types/middle/twosum",
        "name": "Two Sum",
        "sub": "Composite Types",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/03-composite-types/middle/window",
        "name": "Sliding Window Sums",
        "sub": "Composite Types",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/03-composite-types/middle/zip",
        "name": "Zip to Map",
        "sub": "Composite Types",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/03-composite-types/senior/appendcapshared",
        "name": "Append Clobbers via Shared Capacity",
        "sub": "Composite Types",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/03-composite-types/senior/appendnotcaptured",
        "name": "append Result Ignored",
        "sub": "Composite Types",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/03-composite-types/senior/arrayvalue",
        "name": "Arrays Are Values",
        "sub": "Composite Types",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/03-composite-types/senior/commaokzero",
        "name": "Zero vs Missing",
        "sub": "Composite Types",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/03-composite-types/senior/copyemptydst",
        "name": "copy Into a Length-0 Slice",
        "sub": "Composite Types",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/03-composite-types/senior/deduporder",
        "name": "Dedupe Preserving Order",
        "sub": "Composite Types",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/03-composite-types/senior/embeddingshadow",
        "name": "Embedded Field Shadowing",
        "sub": "Composite Types",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/03-composite-types/senior/emptystructset",
        "name": "Set Membership Value",
        "sub": "Composite Types",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/03-composite-types/senior/filterreuse",
        "name": "Filter Corrupts Input",
        "sub": "Composite Types",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/03-composite-types/senior/grid2dalias",
        "name": "Shared Grid Rows",
        "sub": "Composite Types",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/03-composite-types/senior/jsontag",
        "name": "JSON Tag Mismatch",
        "sub": "Composite Types",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/03-composite-types/senior/mapstructupdate",
        "name": "Update Struct in Map",
        "sub": "Composite Types",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/03-composite-types/senior/mergedirection",
        "name": "Merge Override Order",
        "sub": "Composite Types",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/03-composite-types/senior/nestedmapinit",
        "name": "Initialize the Inner Map",
        "sub": "Composite Types",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/03-composite-types/senior/nilmap",
        "name": "Nil Map Write",
        "sub": "Composite Types",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/03-composite-types/senior/nilvsempty",
        "name": "Nil vs Empty Slice",
        "sub": "Composite Types",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/03-composite-types/senior/preallocindex",
        "name": "Length vs Capacity for Indexing",
        "sub": "Composite Types",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/03-composite-types/senior/rangecopymutate",
        "name": "Mutating the Range Copy",
        "sub": "Composite Types",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/03-composite-types/senior/removeorder",
        "name": "Delete Off-By-One",
        "sub": "Composite Types",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/03-composite-types/senior/shallowclone",
        "name": "Struct Copy Shares Slice",
        "sub": "Composite Types",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/03-composite-types/senior/sliceequal",
        "name": "Slice Equality",
        "sub": "Composite Types",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/03-composite-types/senior/sortmutates",
        "name": "Sort Mutates Caller",
        "sub": "Composite Types",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/03-composite-types/senior/stackpop",
        "name": "Stack Pop Shrink",
        "sub": "Composite Types",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/03-composite-types/senior/subsliceclamp",
        "name": "Clamp the Sub-slice End",
        "sub": "Composite Types",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/03-composite-types/senior/twodshallow",
        "name": "Deep-Copy a Grid",
        "sub": "Composite Types",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/03-composite-types/senior/valuereceiver",
        "name": "Receiver Doesn't Mutate",
        "sub": "Composite Types",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/03-composite-types/staff/appendnilreturn",
        "name": "Append to Nil",
        "sub": "Composite Types",
        "lv": "ST",
        "level": "staff",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/03-composite-types/staff/appendtwobranch",
        "name": "Two Appends Share Capacity",
        "sub": "Composite Types",
        "lv": "ST",
        "level": "staff",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/03-composite-types/staff/bufconcat",
        "name": "Copy at the Right Offset",
        "sub": "Composite Types",
        "lv": "ST",
        "level": "staff",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/03-composite-types/staff/clipcap",
        "name": "Clip Capacity",
        "sub": "Composite Types",
        "lv": "ST",
        "level": "staff",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/03-composite-types/staff/copyshiftleft",
        "name": "copy Direction for Shift",
        "sub": "Composite Types",
        "lv": "ST",
        "level": "staff",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/03-composite-types/staff/gridcolsum",
        "name": "Column Sum Indexing",
        "sub": "Composite Types",
        "lv": "ST",
        "level": "staff",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/03-composite-types/staff/growthstale",
        "name": "Stale Pointer After Growth",
        "sub": "Composite Types",
        "lv": "ST",
        "level": "staff",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/03-composite-types/staff/insertshift",
        "name": "Insert Shift Direction",
        "sub": "Composite Types",
        "lv": "ST",
        "level": "staff",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/03-composite-types/staff/keyfromstruct",
        "name": "Struct Map Key Fields",
        "sub": "Composite Types",
        "lv": "ST",
        "level": "staff",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/03-composite-types/staff/maparraykey",
        "name": "Array Key Coordinate Order",
        "sub": "Composite Types",
        "lv": "ST",
        "level": "staff",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/03-composite-types/staff/mapclonesharedslice",
        "name": "Deep-Clone Map of Slices",
        "sub": "Composite Types",
        "lv": "ST",
        "level": "staff",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/03-composite-types/staff/mapniljson",
        "name": "Nil Map JSON",
        "sub": "Composite Types",
        "lv": "ST",
        "level": "staff",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/03-composite-types/staff/ringbuffer",
        "name": "Ring Buffer Wrap",
        "sub": "Composite Types",
        "lv": "ST",
        "level": "staff",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/03-composite-types/staff/rotate90",
        "name": "Rotate Matrix 90\u00b0",
        "sub": "Composite Types",
        "lv": "ST",
        "level": "staff",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/03-composite-types/staff/sliceleak",
        "name": "Sub-slice Memory Leak",
        "sub": "Composite Types",
        "lv": "ST",
        "level": "staff",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/03-composite-types/staff/slicetoarray",
        "name": "Slice-to-Array Length Check",
        "sub": "Composite Types",
        "lv": "ST",
        "level": "staff",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/03-composite-types/staff/stringimmutable",
        "name": "Strings Are Immutable",
        "sub": "Composite Types",
        "lv": "ST",
        "level": "staff",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/03-composite-types/staff/structpadding",
        "name": "Struct Field Padding",
        "sub": "Composite Types",
        "lv": "ST",
        "level": "staff",
        "locked": false,
        "tag": "backend"
      }
    ]
  },
  {
    "topic": "01-language-basics \u00b7 Functions",
    "items": [
      {
        "id": "01-language-basics/04-functions/junior/absif",
        "name": "Absolute Value",
        "sub": "Functions",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/junior/averagef",
        "name": "Variadic Average",
        "sub": "Functions",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/junior/cascade",
        "name": "Cascading Permissions",
        "sub": "Functions",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/junior/clampf",
        "name": "Clamp to Range",
        "sub": "Functions",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/junior/clamploop",
        "name": "Clamp Each Element",
        "sub": "Functions",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/junior/countdown",
        "name": "Countdown Slice",
        "sub": "Functions",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/junior/countword",
        "name": "Count Matching",
        "sub": "Functions",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/junior/dayname",
        "name": "Day Name via Switch",
        "sub": "Functions",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/junior/divmod",
        "name": "Quotient and Remainder",
        "sub": "Functions",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/junior/doublecopy",
        "name": "Value Copy Doesn't Leak",
        "sub": "Functions",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/junior/firstlast",
        "name": "First, Last, OK",
        "sub": "Functions",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/junior/fizzbuzz",
        "name": "FizzBuzz Line",
        "sub": "Functions",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/junior/gcdloop",
        "name": "Euclidean GCD",
        "sub": "Functions",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/junior/grade",
        "name": "Letter Grade Ladder",
        "sub": "Functions",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/junior/gradeif",
        "name": "Even/Odd Label",
        "sub": "Functions",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/junior/gridsum",
        "name": "2D Grid Sum",
        "sub": "Functions",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/junior/ifinit",
        "name": "If with Init Clause",
        "sub": "Functions",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/junior/joinv",
        "name": "Variadic Join",
        "sub": "Functions",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/junior/leapyear",
        "name": "Leap Year",
        "sub": "Functions",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/junior/max3",
        "name": "Max of Three",
        "sub": "Functions",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/junior/minmax",
        "name": "Min and Max",
        "sub": "Functions",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/junior/nocopysliceval",
        "name": "Sum Without Mutating",
        "sub": "Functions",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/junior/reverseloop",
        "name": "Reverse via Index Loop",
        "sub": "Functions",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/junior/signswitch",
        "name": "Sign via Switch",
        "sub": "Functions",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/junior/sumrange",
        "name": "Sum with For-Range",
        "sub": "Functions",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/junior/sumv",
        "name": "Variadic Sum",
        "sub": "Functions",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/junior/swap2",
        "name": "Swap Two",
        "sub": "Functions",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/junior/tagless",
        "name": "Tagless Switch Sign",
        "sub": "Functions",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/junior/tally",
        "name": "Count and Sum Evens",
        "sub": "Functions",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/junior/weekday",
        "name": "Weekday Name",
        "sub": "Functions",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/middle/adder",
        "name": "Adder Factory",
        "sub": "Functions",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/middle/appendall",
        "name": "Forward Variadic to Append",
        "sub": "Functions",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/middle/bumpptr",
        "name": "Pointer Parameter Mutates",
        "sub": "Functions",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/middle/compose",
        "name": "Compose Two Funcs",
        "sub": "Functions",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/middle/counter",
        "name": "Closure Counter",
        "sub": "Functions",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/middle/curry",
        "name": "Curried Add3",
        "sub": "Functions",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/middle/deferbump",
        "name": "Defer Adjusts Result",
        "sub": "Functions",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/middle/deferloop",
        "name": "Defer Inside Loop",
        "sub": "Functions",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/middle/deferorder",
        "name": "Defer LIFO Order",
        "sub": "Functions",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/middle/defersnapshot",
        "name": "Defer Argument Snapshot",
        "sub": "Functions",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/middle/dropwhile",
        "name": "Drop While",
        "sub": "Functions",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/middle/fallthru",
        "name": "Switch Fallthrough",
        "sub": "Functions",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/middle/filterints",
        "name": "Filter by Predicate",
        "sub": "Functions",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/middle/firstpair",
        "name": "Labeled Break Search",
        "sub": "Functions",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/middle/flatten",
        "name": "Flatten Slices",
        "sub": "Functions",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/middle/gotoretry",
        "name": "Retry with Goto",
        "sub": "Functions",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/middle/groupby",
        "name": "Group By Key",
        "sub": "Functions",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/middle/mapints",
        "name": "Map Over Slice",
        "sub": "Functions",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/middle/maxvar",
        "name": "Variadic Max with OK",
        "sub": "Functions",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/middle/memoize",
        "name": "Memoize a Function",
        "sub": "Functions",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/middle/namedret",
        "name": "Named Return Adjust",
        "sub": "Functions",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/middle/oncefn",
        "name": "Run Once",
        "sub": "Functions",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/middle/perindex",
        "name": "Closures Over Loop Variable",
        "sub": "Functions",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/middle/pipeline",
        "name": "Function Pipeline",
        "sub": "Functions",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/middle/reduceints",
        "name": "Reduce/Fold",
        "sub": "Functions",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/middle/repeatfn",
        "name": "Apply N Times",
        "sub": "Functions",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/middle/safecall",
        "name": "Recover From Panic",
        "sub": "Functions",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/middle/takewhile",
        "name": "Take While",
        "sub": "Functions",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/middle/tracker",
        "name": "Closure Sharing State",
        "sub": "Functions",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/middle/zipwith",
        "name": "Zip With",
        "sub": "Functions",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/senior/aliasbug",
        "name": "Assignment Shares Backing Array",
        "sub": "Functions",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/senior/allpositive",
        "name": "Early Return Placement",
        "sub": "Functions",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/senior/appendclobber",
        "name": "Append Clobbers Shared Cap",
        "sub": "Functions",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/senior/appendignored",
        "name": "Append Result Discarded",
        "sub": "Functions",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/senior/bsearchbug",
        "name": "Binary Search Bound",
        "sub": "Functions",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/senior/classifybug",
        "name": "Missing Default Case",
        "sub": "Functions",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/senior/cleanupcount",
        "name": "Defers Fire at Function Exit",
        "sub": "Functions",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/senior/copyshort",
        "name": "Copy Bounded by Destination",
        "sub": "Functions",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/senior/counternilmap",
        "name": "Nil Map Write in Helper",
        "sub": "Functions",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/senior/deferargbug",
        "name": "Defer Argument Timing",
        "sub": "Functions",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/senior/deferwipe",
        "name": "Defer Overwrites Result",
        "sub": "Functions",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/senior/deletebug",
        "name": "Ordered Slice Delete",
        "sub": "Functions",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/senior/factbug",
        "name": "Recursion Base Case",
        "sub": "Functions",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/senior/handlerbug",
        "name": "Capture the Value, Not the Cursor",
        "sub": "Functions",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/senior/insertbug",
        "name": "Insert Shifts the Wrong Way",
        "sub": "Functions",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/senior/nestedmap",
        "name": "Nested Map Inner Init",
        "sub": "Functions",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/senior/oddbug",
        "name": "Parity Test for Negatives",
        "sub": "Functions",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/senior/pairbug",
        "name": "Adjacent-Pair Bound",
        "sub": "Functions",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/senior/prefixbug",
        "name": "Prefix-Sum Query",
        "sub": "Functions",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/senior/rangemutate",
        "name": "Range Copy Doesn't Mutate",
        "sub": "Functions",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/senior/recoverdirect",
        "name": "Recover Must Be Deferred",
        "sub": "Functions",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/senior/reversebug",
        "name": "Two-Pointer Reverse Bound",
        "sub": "Functions",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/senior/runecount",
        "name": "Range Over String Yields Runes",
        "sub": "Functions",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/senior/shadowerr",
        "name": "Shadowed Named Return",
        "sub": "Functions",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/senior/skipbug",
        "name": "Continue Skips Wrong Branch",
        "sub": "Functions",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/senior/spreadmissing",
        "name": "Forgotten Spread Operator",
        "sub": "Functions",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/senior/stalecap",
        "name": "Append Invalidates Alias",
        "sub": "Functions",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/senior/thresholdbug",
        "name": "Boundary Comparison",
        "sub": "Functions",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/senior/tierbug",
        "name": "Accidental Fallthrough",
        "sub": "Functions",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/senior/windowmax",
        "name": "Sliding Window Bound",
        "sub": "Functions",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/staff/accreset",
        "name": "Accumulator Reset in Recursion",
        "sub": "Functions",
        "lv": "ST",
        "level": "staff",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/staff/appendwhilerange",
        "name": "Append While Ranging",
        "sub": "Functions",
        "lv": "ST",
        "level": "staff",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/staff/bareoverwrite",
        "name": "Named Return Left Unset",
        "sub": "Functions",
        "lv": "ST",
        "level": "staff",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/staff/bytesum",
        "name": "Accumulator Type Overflow",
        "sub": "Functions",
        "lv": "ST",
        "level": "staff",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/staff/capretain",
        "name": "Sub-slice Retains Backing",
        "sub": "Functions",
        "lv": "ST",
        "level": "staff",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/staff/commaok",
        "name": "Ignoring comma-ok",
        "sub": "Functions",
        "lv": "ST",
        "level": "staff",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/staff/compactcopy",
        "name": "Overlapping Copy Shift",
        "sub": "Functions",
        "lv": "ST",
        "level": "staff",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/staff/deferbeforeacq",
        "name": "Defer Before Acquisition",
        "sub": "Functions",
        "lv": "ST",
        "level": "staff",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/staff/deferslicearg",
        "name": "Defer Snapshots Slice Header",
        "sub": "Functions",
        "lv": "ST",
        "level": "staff",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/staff/elemptr",
        "name": "Pointer to Element Before Grow",
        "sub": "Functions",
        "lv": "ST",
        "level": "staff",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/staff/iifeinit",
        "name": "Immediately-Invoked Init",
        "sub": "Functions",
        "lv": "ST",
        "level": "staff",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/staff/labeledcont",
        "name": "Labeled Continue",
        "sub": "Functions",
        "lv": "ST",
        "level": "staff",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/staff/maskpanic",
        "name": "Deferred Panic Masks Original",
        "sub": "Functions",
        "lv": "ST",
        "level": "staff",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/staff/methodval",
        "name": "Method Value Binds Receiver",
        "sub": "Functions",
        "lv": "ST",
        "level": "staff",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/staff/nilappend",
        "name": "Append to Nil Is Valid",
        "sub": "Functions",
        "lv": "ST",
        "level": "staff",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/staff/nilcallback",
        "name": "Optional Callback Nil Check",
        "sub": "Functions",
        "lv": "ST",
        "level": "staff",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/staff/prealloc",
        "name": "Prealloc Length vs Append",
        "sub": "Functions",
        "lv": "ST",
        "level": "staff",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/staff/recoverlost",
        "name": "Recovered Error Lost to Local",
        "sub": "Functions",
        "lv": "ST",
        "level": "staff",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/staff/recovertype",
        "name": "Typed Recover Value",
        "sub": "Functions",
        "lv": "ST",
        "level": "staff",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/staff/repanic",
        "name": "Recover Then Re-panic",
        "sub": "Functions",
        "lv": "ST",
        "level": "staff",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/staff/reusedbuffer",
        "name": "Returning a Reused Buffer",
        "sub": "Functions",
        "lv": "ST",
        "level": "staff",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/staff/rotate3",
        "name": "Simultaneous Assignment",
        "sub": "Functions",
        "lv": "ST",
        "level": "staff",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/staff/runeindex",
        "name": "Index Into Runes, Not Bytes",
        "sub": "Functions",
        "lv": "ST",
        "level": "staff",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/staff/sharedcell",
        "name": "Closures Share One Cell",
        "sub": "Functions",
        "lv": "ST",
        "level": "staff",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/staff/shortcircuit",
        "name": "Short-Circuit Nil Guard",
        "sub": "Functions",
        "lv": "ST",
        "level": "staff",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/staff/stackpush",
        "name": "LIFO via Deferred Push",
        "sub": "Functions",
        "lv": "ST",
        "level": "staff",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/staff/switchinit",
        "name": "Switch Init Statement",
        "sub": "Functions",
        "lv": "ST",
        "level": "staff",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/staff/tracescope",
        "name": "Defer Scope in a Loop",
        "sub": "Functions",
        "lv": "ST",
        "level": "staff",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/staff/twoappend",
        "name": "Two Appends Share Spare Cap",
        "sub": "Functions",
        "lv": "ST",
        "level": "staff",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/04-functions/staff/wraperr",
        "name": "Defer Wraps the Snapshot",
        "sub": "Functions",
        "lv": "ST",
        "level": "staff",
        "locked": false,
        "tag": "backend"
      }
    ]
  },
  {
    "topic": "01-language-basics \u00b7 Pointers",
    "items": [
      {
        "id": "01-language-basics/05-pointers/junior/addvia",
        "name": "Add Delta Through Pointer",
        "sub": "Pointers",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/junior/applyptr",
        "name": "Apply Function In Place",
        "sub": "Pointers",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/junior/bumpfield",
        "name": "Mutate Struct Field via Pointer",
        "sub": "Pointers",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/junior/clampptr",
        "name": "Clamp In Place",
        "sub": "Pointers",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/junior/clearref",
        "name": "Clear a Pointer Field",
        "sub": "Pointers",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/junior/copyval",
        "name": "Copy Value Between Pointers",
        "sub": "Pointers",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/junior/countnil",
        "name": "Count Nil Pointers",
        "sub": "Pointers",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/junior/deposit",
        "name": "Deposit Into Account",
        "sub": "Pointers",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/junior/doubleidx",
        "name": "Double an Array Element",
        "sub": "Pointers",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/junior/fillptr",
        "name": "Fill Two Outputs",
        "sub": "Pointers",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/junior/incptr",
        "name": "Increment Through Pointer",
        "sub": "Pointers",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/junior/isempty",
        "name": "Nil or Empty List",
        "sub": "Pointers",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/junior/listlen",
        "name": "Linked List Length",
        "sub": "Pointers",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/junior/mapbump",
        "name": "Mutate Through Map of Pointers",
        "sub": "Pointers",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/junior/maxptr",
        "name": "Return Pointer to Larger",
        "sub": "Pointers",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/junior/negate",
        "name": "Negate In Place",
        "sub": "Pointers",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/junior/newint",
        "name": "Allocate and Return Pointer",
        "sub": "Pointers",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/junior/orzero",
        "name": "Deref or Zero",
        "sub": "Pointers",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/junior/pushslice",
        "name": "Append Through Slice Pointer",
        "sub": "Pointers",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/junior/reseat",
        "name": "Reassign via Double Pointer",
        "sub": "Pointers",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/junior/resetnode",
        "name": "Reset a Struct Pointer",
        "sub": "Pointers",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/junior/samecart",
        "name": "Same Struct Instance",
        "sub": "Pointers",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/junior/sameptr",
        "name": "Pointer Identity",
        "sub": "Pointers",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/junior/scaleall",
        "name": "Mutate Through Slice of Pointers",
        "sub": "Pointers",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/junior/setzero",
        "name": "Zero Through Pointer",
        "sub": "Pointers",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/junior/sumptrs",
        "name": "Sum Through Pointers",
        "sub": "Pointers",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/junior/swapcounts",
        "name": "Swap Two Struct Fields",
        "sub": "Pointers",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/junior/swapptr",
        "name": "Swap Through Pointers",
        "sub": "Pointers",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/junior/toggle",
        "name": "Toggle a Bool Pointer",
        "sub": "Pointers",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/junior/valueor",
        "name": "Nil-Safe Dereference",
        "sub": "Pointers",
        "lv": "J",
        "level": "junior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/middle/appendtail",
        "name": "Append at Tail via Double Pointer",
        "sub": "Pointers",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/middle/boundmethod",
        "name": "Bind a Pointer Method",
        "sub": "Pointers",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/middle/bstcontains",
        "name": "BST Search",
        "sub": "Pointers",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/middle/bstinsert",
        "name": "BST Insert",
        "sub": "Pointers",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/middle/bucketadd",
        "name": "Accumulate Into Bucket Pointers",
        "sub": "Pointers",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/middle/concatlist",
        "name": "Concatenate Two Lists",
        "sub": "Pointers",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/middle/copylist",
        "name": "Deep Copy a List",
        "sub": "Pointers",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/middle/counterinc",
        "name": "Pointer Receiver Increment",
        "sub": "Pointers",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/middle/countif",
        "name": "Count Nodes by Predicate",
        "sub": "Pointers",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/middle/countleaves",
        "name": "Count Leaf Nodes",
        "sub": "Pointers",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/middle/deduplist",
        "name": "Dedup Sorted List",
        "sub": "Pointers",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/middle/dellist",
        "name": "Delete First Match",
        "sub": "Pointers",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/middle/elemptrs",
        "name": "Pointers Into a Slice",
        "sub": "Pointers",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/middle/hascycle",
        "name": "Detect a Cycle",
        "sub": "Pointers",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/middle/listslice",
        "name": "List to Slice",
        "sub": "Pointers",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/middle/makeuser",
        "name": "new vs Composite Literal",
        "sub": "Pointers",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/middle/mergelists",
        "name": "Merge Two Sorted Lists",
        "sub": "Pointers",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/middle/midnode",
        "name": "Middle of List",
        "sub": "Pointers",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/middle/mirrortree",
        "name": "Mirror a Tree",
        "sub": "Pointers",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/middle/nthfromend",
        "name": "Nth Node From End",
        "sub": "Pointers",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/middle/ptrstack",
        "name": "Stack Push/Pop",
        "sub": "Pointers",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/middle/pushfront",
        "name": "Prepend to List",
        "sub": "Pointers",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/middle/revlist",
        "name": "Reverse a Linked List",
        "sub": "Pointers",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/middle/rotatelist",
        "name": "Rotate List Right",
        "sub": "Pointers",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/middle/setfield",
        "name": "Update Through Fetched Pointer",
        "sub": "Pointers",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/middle/sumlist",
        "name": "Recursive List Sum",
        "sub": "Pointers",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/middle/sumtree",
        "name": "Sum a Tree",
        "sub": "Pointers",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/middle/swappairs",
        "name": "Swap First Two Nodes",
        "sub": "Pointers",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/middle/treeheight",
        "name": "Tree Height",
        "sub": "Pointers",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/middle/vscale",
        "name": "Value Receiver Returns Copy",
        "sub": "Pointers",
        "lv": "M",
        "level": "middle",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/senior/applyinplace",
        "name": "Apply Discards the Result",
        "sub": "Pointers",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/senior/clonesharetail",
        "name": "List Copy Shares the Tail",
        "sub": "Pointers",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/senior/cyclenoguard",
        "name": "Fast Pointer Missing Guard",
        "sub": "Pointers",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/senior/dblappend",
        "name": "Append via Slice Pointer",
        "sub": "Pointers",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/senior/delhead",
        "name": "Delete Head Ignores New Head",
        "sub": "Pointers",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/senior/derefearly",
        "name": "Dereference Before Nil Check",
        "sub": "Pointers",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/senior/elemstale",
        "name": "Element Pointer After Append",
        "sub": "Pointers",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/senior/eqconfuse",
        "name": "Identity vs Value Confusion",
        "sub": "Pointers",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/senior/hoistedaddr",
        "name": "Address of a Hoisted Loop Var",
        "sub": "Pointers",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/senior/insertnoreattach",
        "name": "Insert Result Not Reattached",
        "sub": "Pointers",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/senior/insertoverwrite",
        "name": "Insert Overwrites the Root",
        "sub": "Pointers",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/senior/lenoffbyone",
        "name": "List Length Counts a Phantom",
        "sub": "Pointers",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/senior/mapptrupdate",
        "name": "Update Map Struct via Pointer",
        "sub": "Pointers",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/senior/mergelost",
        "name": "Merge Drops the Remainder",
        "sub": "Pointers",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/senior/methodcopy",
        "name": "Method Value Captures a Copy",
        "sub": "Pointers",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/senior/minseed",
        "name": "Min Pointer Bad Seed",
        "sub": "Pointers",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/senior/nestptr",
        "name": "Nested Pointer Map Init",
        "sub": "Pointers",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/senior/nthoff",
        "name": "Gap Off-by-One",
        "sub": "Pointers",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/senior/popnoguard",
        "name": "Pop Without Empty Guard",
        "sub": "Pointers",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/senior/pushnoassign",
        "name": "Push Discards Append Result",
        "sub": "Pointers",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/senior/removeatlist",
        "name": "Remove Nth Node Off-by-One",
        "sub": "Pointers",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/senior/reseatlocal",
        "name": "Reassigning a Local Pointer",
        "sub": "Pointers",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/senior/reverselose",
        "name": "Reverse Loses the Rest",
        "sub": "Pointers",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/senior/revreturnhead",
        "name": "Reverse Returns Old Head",
        "sub": "Pointers",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/senior/shallowclone",
        "name": "Struct Clone Shares Pointer Field",
        "sub": "Pointers",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/senior/shallowcopy",
        "name": "Shallow Tree Copy Shares Nodes",
        "sub": "Pointers",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/senior/sumtreebug",
        "name": "Tree Sum Skips Subtrees",
        "sub": "Pointers",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/senior/swapwrong",
        "name": "Swapping Pointers, Not Values",
        "sub": "Pointers",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/senior/tailnoupdate",
        "name": "Append Can't Grow Empty List",
        "sub": "Pointers",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/senior/valuerecv",
        "name": "Value Receiver Loses Mutation",
        "sub": "Pointers",
        "lv": "S",
        "level": "senior",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/staff/alignof",
        "name": "Alignment of a Type",
        "sub": "Pointers",
        "lv": "ST",
        "level": "staff",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/staff/arraysharing",
        "name": "Two Views Share the Array",
        "sub": "Pointers",
        "lv": "ST",
        "level": "staff",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/staff/arrtoslice",
        "name": "Array Pointer to Slice",
        "sub": "Pointers",
        "lv": "ST",
        "level": "staff",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/staff/clearptrs",
        "name": "Clear a Pointer Slice",
        "sub": "Pointers",
        "lv": "ST",
        "level": "staff",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/staff/deepptrfield",
        "name": "Copy Shares Pointer Field",
        "sub": "Pointers",
        "lv": "ST",
        "level": "staff",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/staff/escapereturn",
        "name": "Return Pointer to the Element",
        "sub": "Pointers",
        "lv": "ST",
        "level": "staff",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/staff/escapeslice",
        "name": "Store Distinct Struct Pointers",
        "sub": "Pointers",
        "lv": "ST",
        "level": "staff",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/staff/fieldoffset",
        "name": "Reach a Field by Offset",
        "sub": "Pointers",
        "lv": "ST",
        "level": "staff",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/staff/floatbits",
        "name": "Reinterpret Float Bits",
        "sub": "Pointers",
        "lv": "ST",
        "level": "staff",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/staff/mapdelnil",
        "name": "Delete vs Nil in Pointer Map",
        "sub": "Pointers",
        "lv": "ST",
        "level": "staff",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/staff/mapstructptr",
        "name": "Mutate Pointee From Map Struct",
        "sub": "Pointers",
        "lv": "ST",
        "level": "staff",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/staff/methodexpr",
        "name": "Method Expression Receiver",
        "sub": "Pointers",
        "lv": "ST",
        "level": "staff",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/staff/nilslicedata",
        "name": "SliceData of Empty Slice",
        "sub": "Pointers",
        "lv": "ST",
        "level": "staff",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/staff/nilunsafe",
        "name": "Nil Check Before Unsafe Read",
        "sub": "Pointers",
        "lv": "ST",
        "level": "staff",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/staff/offsetpad",
        "name": "Field Offset With Padding",
        "sub": "Pointers",
        "lv": "ST",
        "level": "staff",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/staff/poolreset",
        "name": "Reset Forgets a Field",
        "sub": "Pointers",
        "lv": "ST",
        "level": "staff",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/staff/ptrfieldsize",
        "name": "Struct With Pointer Field Size",
        "sub": "Pointers",
        "lv": "ST",
        "level": "staff",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/staff/retainbacking",
        "name": "Sub-slice Retains Full Array",
        "sub": "Pointers",
        "lv": "ST",
        "level": "staff",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/staff/shrinkretain",
        "name": "Shrinking Retains Pointers",
        "sub": "Pointers",
        "lv": "ST",
        "level": "staff",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/staff/sizeofarray",
        "name": "Size of Whole Array",
        "sub": "Pointers",
        "lv": "ST",
        "level": "staff",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/staff/sizeofptr",
        "name": "Sizeof Pointer vs Pointee",
        "sub": "Pointers",
        "lv": "ST",
        "level": "staff",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/staff/sizeorder",
        "name": "Field Order and Struct Size",
        "sub": "Pointers",
        "lv": "ST",
        "level": "staff",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/staff/slicedata",
        "name": "Address of Slice Data",
        "sub": "Pointers",
        "lv": "ST",
        "level": "staff",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/staff/strbytes",
        "name": "String Length From Header",
        "sub": "Pointers",
        "lv": "ST",
        "level": "staff",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/staff/stringbyte",
        "name": "First Byte of a String",
        "sub": "Pointers",
        "lv": "ST",
        "level": "staff",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/staff/structasarray",
        "name": "Reinterpret Struct as Array",
        "sub": "Pointers",
        "lv": "ST",
        "level": "staff",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/staff/structconv",
        "name": "Convert Between Same-Layout Structs",
        "sub": "Pointers",
        "lv": "ST",
        "level": "staff",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/staff/unsafeslice",
        "name": "Build a Slice From a Pointer",
        "sub": "Pointers",
        "lv": "ST",
        "level": "staff",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/staff/unsafestride",
        "name": "Pointer Arithmetic Stride",
        "sub": "Pointers",
        "lv": "ST",
        "level": "staff",
        "locked": false,
        "tag": "backend"
      },
      {
        "id": "01-language-basics/05-pointers/staff/widthmismatch",
        "name": "Reinterpret With Wrong Width",
        "sub": "Pointers",
        "lv": "ST",
        "level": "staff",
        "locked": false,
        "tag": "backend"
      }
    ]
  }
];
