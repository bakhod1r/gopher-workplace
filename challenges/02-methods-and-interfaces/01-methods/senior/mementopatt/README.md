# Memento Pattern

**Level:** senior
**Topic:** 02-methods-and-interfaces → 01-methods

## Task

Implement `Save` and `Restore` in [mementopatt.go](mementopatt.go):

1. `Save`: Return `Memento{state: e.Text}`.
2. `Restore`: `e.Text = m.state`.

## Validate

```bash
make verify
```
