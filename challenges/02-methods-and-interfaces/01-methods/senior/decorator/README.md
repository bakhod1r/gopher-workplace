# Decorator

**Level:** senior
**Topic:** 02-methods-and-interfaces → 01-methods

## Task

Implement `DoWork` in [decorator.go](decorator.go):

1. Call `d.Comp.DoWork()`.
2. Return `"[" + result + "]"`.

## Validate

```bash
make verify
```
