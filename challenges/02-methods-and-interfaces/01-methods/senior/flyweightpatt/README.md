# Flyweight Pattern

**Level:** senior
**Topic:** 02-methods-and-interfaces → 01-methods

## Task

Implement `Get` in [flyweightpatt.go](flyweightpatt.go):

1. Check map for `name`. Return if found.
2. Else create `&FontData{data: name}`, add to map, return it.

## Validate

```bash
make verify
```
