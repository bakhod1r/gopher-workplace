# Builder Pattern

**Level:** senior
**Topic:** 02-methods-and-interfaces → 01-methods

## Task

Implement `URL` and `Auth` in [builderpatt.go](builderpatt.go):

1. Set the respective fields on `b.req`.
2. Return `b` to allow chaining.

## Validate

```bash
make verify
```
