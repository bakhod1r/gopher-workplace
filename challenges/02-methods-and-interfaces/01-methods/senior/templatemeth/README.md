# Template Method

**Level:** senior
**Topic:** 02-methods-and-interfaces → 01-methods

## Task

Implement `Run` in [templatemeth.go](templatemeth.go):

1. Combine the results of `DoStep1()` and `DoStep2()` with a `"-"` between them.

## Validate

```bash
make verify
```
