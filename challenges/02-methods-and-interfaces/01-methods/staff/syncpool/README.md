# Sync Pool Wrapper

**Level:** staff
**Topic:** 02-methods-and-interfaces → 01-methods

## Task

Implement `Get` and `Put` in [syncpool.go](syncpool.go):

1. `Get`: `p.pool.Get().(*Buffer)`
2. `Put`: `p.pool.Put(b)`

## Validate

```bash
make verify
```
