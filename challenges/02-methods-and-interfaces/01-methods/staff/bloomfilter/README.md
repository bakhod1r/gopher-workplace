# Bloom Filter

**Level:** staff
**Topic:** 02-methods-and-interfaces → 01-methods

## Task

Implement `Add` and `MightContain` in [bloomfilter.go](bloomfilter.go):

1. `Add`: Set the bits at indices returned by `hash1` and `hash2`.
2. `MightContain`: Check if both bits are set.

## Validate

```bash
make verify
```
