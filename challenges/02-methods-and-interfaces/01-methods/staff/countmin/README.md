# Count-Min Sketch

**Level:** staff
**Topic:** 02-methods-and-interfaces → 01-methods

## Task

Implement `Add` and `Count` in [countmin.go](countmin.go):

1. `Add`: Increment the counts in both rows using `h1` and `h2`.
2. `Count`: Return the minimum of the two counts.

## Validate

```bash
make verify
```
