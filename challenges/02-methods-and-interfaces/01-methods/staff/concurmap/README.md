# Concurrent Map

**Level:** staff
**Topic:** 02-methods-and-interfaces → 01-methods

## Task

Implement `Get` and `Set` in [concurmap.go](concurmap.go):

1. `Get`: Use `RLock()` and `RUnlock()`.
2. `Set`: Use `Lock()` and `Unlock()`.

## Validate

```bash
make verify
```
