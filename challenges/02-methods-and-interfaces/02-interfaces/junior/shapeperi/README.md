# Shape Perimeter

**Level:** junior
**Topic:** 02-methods-and-interfaces → 02-interfaces

## Task

Implement `Perimeter` on `Circle` and `Square` in [shapeperi.go](shapeperi.go):

1. `Circle.Perimeter()` returns `2 * math.Pi * r`.
2. `Square.Perimeter()` returns `4 * side`.

## Validate

```bash
make verify
```
