// Package splitatgen — Gopher Workplace challenge.
package splitatgen

// SplitAt returns s cut at index i. The index is clamped
// into [0, len(s)].
func SplitAt[T any](s []T, i int) ([]T, []T) {
	// TODO(candidate): clamp the index, then copy both halves.
	panic("not implemented")
}
