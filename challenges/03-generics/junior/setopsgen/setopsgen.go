// Package setopsgen — Gopher Workplace challenge.
package setopsgen

// Union returns a set holding every element of a and b.
func Union[T comparable](a, b map[T]struct{}) map[T]struct{} {
	// TODO(candidate): collect the elements of both sets.
	panic("not implemented")
}

// Intersect returns a set holding the elements present in both a and b.
func Intersect[T comparable](a, b map[T]struct{}) map[T]struct{} {
	// TODO(candidate): keep only the elements present in both sets.
	panic("not implemented")
}
