// Package grade — Gopher Workplace challenge.
package grade

// Grade maps a 0–100 score to a letter using an if / else-if / else ladder:
// 90+ => "A", 80+ => "B", 70+ => "C", 60+ => "D", below 60 => "F". Boundaries
// are inclusive, so exactly 90 is an "A".
//
// Examples:
//
//	Grade(95) => "A"
//	Grade(90) => "A"
//	Grade(72) => "C"
//	Grade(59) => "F"
func Grade(score int) string {
	// TODO(candidate): implement this from scratch so all tests pass.
	panic("not implemented")
}
