// Package preallocmap — Gopher Workplace challenge.
package preallocmap

// Index maps each word to the position of its first occurrence. The final
// size is known before the loop starts, so the map must be created with a
// size hint rather than grown incrementally.
//
// Examples:
//
//	Index([]string{"a", "b", "a"}) => {"a":0, "b":1}
func Index(words []string) map[string]int {
	panic("not implemented")
}
