// Package deferorder — Gopher Workplace challenge.
package deferorder

// Order appends 1,2,3 to a slice via deferred calls, then returns it. Deferred
// calls run last-in-first-out, so the result is [3 2 1].
func Order() (out []int) {
	// TODO(candidate): implement this from scratch so all tests pass.
	panic("not implemented")
}
