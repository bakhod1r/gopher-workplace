// Package fizzbuzz — Gopher Workplace challenge.
package fizzbuzz

import "strconv"

var _ = strconv.Itoa // keep import; use it in your solution

// FizzBuzz returns "Fizz" if n divisible by 3, "Buzz" if by 5, "FizzBuzz"
// if by both, otherwise the number as a decimal string.
func FizzBuzz(n int) string {
	// TODO(candidate): implement this from scratch so all tests pass.
	panic("not implemented")
}
