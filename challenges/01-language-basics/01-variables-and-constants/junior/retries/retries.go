// Package retries — Gopher Workplace challenge.
package retries

// TODO(candidate): declare MaxRetries as the integer 3. Beware: '3' in single
// quotes is a rune (value 51), not the number 3.
const MaxRetries = 0

// Budget returns the total number of attempts (first try + retries).
func Budget() int {
	// TODO(candidate): implement this — first attempt plus MaxRetries.
	panic("not implemented")
}
